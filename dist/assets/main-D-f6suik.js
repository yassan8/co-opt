const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["assets/toolbar-collapsed-storage-oKonxCdL.js","assets/table-configuration-Beu6h-Rk.js","assets/event-H3glLpTN.js","assets/psf-calculator-Bc6xBvfp.js","assets/webviewWindow-Bw_7PlN0.js","assets/window-CcRczlwE.js","assets/loaded-file-storage-BCc7IGxJ.js","assets/optimizer-wasm-bridge-8Q3TNa4c.js"])))=>i.map(i=>d[i]);
import{a3 as lc,V as Jt,a4 as In,a5 as Cn,a6 as vi,a7 as xi,a8 as kt,a9 as uc,aa as pc,ab as dc,ac as At,S as mc,ad as Gr,O as fc,W as gc,ae as yc,af as hc,ag as bc,ah as Ir,ai as oo,aj as ro,_ as De,ak as wc,al as Qn,am as Je,an as ht,ao as Tt,ap as Si,i as st,aq as qr,ar as vc,as as xc,t as Sc,r as kn,at as Ac,au as Tr,av as io,aw as En,ax as Lo,ay as So,K as Go,az as Nc,aA as Bo,aB as Ps,aC as _c,G as Fc,o as Mc,k as Rc,m as Cc,aD as qo,aE as Ai,aF as Ic,aG as ks,aH as Es,aI as Tc,a0 as Vo,a2 as Vr,aJ as jn,aK as Os,f as Ni,C as Ds,z as Pc,A as kc,Z as js,$ as bn,a1 as Ls,aL as Ec,aM as Oc,l as Bs,aN as Dc,aO as jc,g as Lc,s as Bc,aP as Dt,aQ as _i,aR as Bt,aS as Yr,aT as Fi,aU as Qt,aV as en,aW as zs,aX as zc,aY as $c,aZ as On,a_ as Zr,a$ as xn,b0 as Ln,b1 as an,b2 as Pr,b3 as Wc,b4 as Yo,b5 as Uc,b6 as Hc,b7 as Gc,b8 as Mi,b9 as Ri,ba as Ci,bb as qc,bc as Ii,bd as $s,be as Eo,bf as br,bg as Ti,bh as Vc,bi as Yc,bj as Zc,bk as Xc,bl as Kc,bm as Jc,bn as Qc,bo as el,bp as tl,bq as nl,br as ol,bs as rl,bt as il,bu as sl,bv as al,bw as Ws,bx as Us,by as Hs,bz as cl,bA as kr,j as Gs,bB as qs,bC as Xr,h as Vs,bD as Ys,bE as ll,bF as ul,bG as pl,I as dl,J as ml,L as fl,y as gl}from"./toolbar-collapsed-storage-oKonxCdL.js";import{f as yl,h as Kr,l as Ot,i as lo,c as uo,e as at,s as Jr,d as Zs,B as po,j as Xs,k as Ks,m as hl,b as bl,v as Js,n as Pi,g as wl,a as ki,o as Qs,p as Ei,q as vl,r as xl}from"./table-configuration-Beu6h-Rk.js";import{PSFCalculator as Sl}from"./psf-calculator-Bc6xBvfp.js";import{listen as Zo}from"./event-H3glLpTN.js";const Oi={type:"change"},Qr={type:"start"},ea={type:"end"},Ao=new uc,Di=new pc,Al=Math.cos(70*dc.DEG2RAD),xt=new Jt,Lt=2*Math.PI,Ke={NONE:-1,ROTATE:0,DOLLY:1,PAN:2,TOUCH_ROTATE:3,TOUCH_PAN:4,TOUCH_DOLLY_PAN:5,TOUCH_DOLLY_ROTATE:6},wr=1e-6;class ta extends lc{constructor(t,n=null){super(t,n),this.state=Ke.NONE,this.target=new Jt,this.cursor=new Jt,this.minDistance=0,this.maxDistance=1/0,this.minZoom=0,this.maxZoom=1/0,this.minTargetRadius=0,this.maxTargetRadius=1/0,this.minPolarAngle=0,this.maxPolarAngle=Math.PI,this.minAzimuthAngle=-1/0,this.maxAzimuthAngle=1/0,this.enableDamping=!1,this.dampingFactor=.05,this.enableZoom=!0,this.zoomSpeed=1,this.enableRotate=!0,this.rotateSpeed=1,this.keyRotateSpeed=1,this.enablePan=!0,this.panSpeed=1,this.screenSpacePanning=!0,this.keyPanSpeed=7,this.zoomToCursor=!1,this.autoRotate=!1,this.autoRotateSpeed=2,this.keys={LEFT:"ArrowLeft",UP:"ArrowUp",RIGHT:"ArrowRight",BOTTOM:"ArrowDown"},this.mouseButtons={LEFT:In.ROTATE,MIDDLE:In.DOLLY,RIGHT:In.PAN},this.touches={ONE:Cn.ROTATE,TWO:Cn.DOLLY_PAN},this.target0=this.target.clone(),this.position0=this.object.position.clone(),this.zoom0=this.object.zoom,this._domElementKeyEvents=null,this._lastPosition=new Jt,this._lastQuaternion=new vi,this._lastTargetPosition=new Jt,this._quat=new vi().setFromUnitVectors(t.up,new Jt(0,1,0)),this._quatInverse=this._quat.clone().invert(),this._spherical=new xi,this._sphericalDelta=new xi,this._scale=1,this._panOffset=new Jt,this._rotateStart=new kt,this._rotateEnd=new kt,this._rotateDelta=new kt,this._panStart=new kt,this._panEnd=new kt,this._panDelta=new kt,this._dollyStart=new kt,this._dollyEnd=new kt,this._dollyDelta=new kt,this._dollyDirection=new Jt,this._mouse=new kt,this._performCursorZoom=!1,this._pointers=[],this._pointerPositions={},this._controlActive=!1,this._onPointerMove=_l.bind(this),this._onPointerDown=Nl.bind(this),this._onPointerUp=Fl.bind(this),this._onContextMenu=kl.bind(this),this._onMouseWheel=Cl.bind(this),this._onKeyDown=Il.bind(this),this._onTouchStart=Tl.bind(this),this._onTouchMove=Pl.bind(this),this._onMouseDown=Ml.bind(this),this._onMouseMove=Rl.bind(this),this._interceptControlDown=El.bind(this),this._interceptControlUp=Ol.bind(this),this.domElement!==null&&this.connect(this.domElement),this.update()}connect(t){super.connect(t),this.domElement.addEventListener("pointerdown",this._onPointerDown),this.domElement.addEventListener("pointercancel",this._onPointerUp),this.domElement.addEventListener("contextmenu",this._onContextMenu),this.domElement.addEventListener("wheel",this._onMouseWheel,{passive:!1}),this.domElement.getRootNode().addEventListener("keydown",this._interceptControlDown,{passive:!0,capture:!0}),this.domElement.style.touchAction="none"}disconnect(){this.domElement.removeEventListener("pointerdown",this._onPointerDown),this.domElement.ownerDocument.removeEventListener("pointermove",this._onPointerMove),this.domElement.ownerDocument.removeEventListener("pointerup",this._onPointerUp),this.domElement.removeEventListener("pointercancel",this._onPointerUp),this.domElement.removeEventListener("wheel",this._onMouseWheel),this.domElement.removeEventListener("contextmenu",this._onContextMenu),this.stopListenToKeyEvents(),this.domElement.getRootNode().removeEventListener("keydown",this._interceptControlDown,{capture:!0}),this.domElement.style.touchAction="auto"}dispose(){this.disconnect()}getPolarAngle(){return this._spherical.phi}getAzimuthalAngle(){return this._spherical.theta}getDistance(){return this.object.position.distanceTo(this.target)}listenToKeyEvents(t){t.addEventListener("keydown",this._onKeyDown),this._domElementKeyEvents=t}stopListenToKeyEvents(){this._domElementKeyEvents!==null&&(this._domElementKeyEvents.removeEventListener("keydown",this._onKeyDown),this._domElementKeyEvents=null)}saveState(){this.target0.copy(this.target),this.position0.copy(this.object.position),this.zoom0=this.object.zoom}reset(){this.target.copy(this.target0),this.object.position.copy(this.position0),this.object.zoom=this.zoom0,this.object.updateProjectionMatrix(),this.dispatchEvent(Oi),this.update(),this.state=Ke.NONE}update(t=null){const n=this.object.position;xt.copy(n).sub(this.target),xt.applyQuaternion(this._quat),this._spherical.setFromVector3(xt),this.autoRotate&&this.state===Ke.NONE&&this._rotateLeft(this._getAutoRotationAngle(t)),this.enableDamping?(this._spherical.theta+=this._sphericalDelta.theta*this.dampingFactor,this._spherical.phi+=this._sphericalDelta.phi*this.dampingFactor):(this._spherical.theta+=this._sphericalDelta.theta,this._spherical.phi+=this._sphericalDelta.phi);let o=this.minAzimuthAngle,r=this.maxAzimuthAngle;isFinite(o)&&isFinite(r)&&(o<-Math.PI?o+=Lt:o>Math.PI&&(o-=Lt),r<-Math.PI?r+=Lt:r>Math.PI&&(r-=Lt),o<=r?this._spherical.theta=Math.max(o,Math.min(r,this._spherical.theta)):this._spherical.theta=this._spherical.theta>(o+r)/2?Math.max(o,this._spherical.theta):Math.min(r,this._spherical.theta)),this._spherical.phi=Math.max(this.minPolarAngle,Math.min(this.maxPolarAngle,this._spherical.phi)),this._spherical.makeSafe(),this.enableDamping===!0?this.target.addScaledVector(this._panOffset,this.dampingFactor):this.target.add(this._panOffset),this.target.sub(this.cursor),this.target.clampLength(this.minTargetRadius,this.maxTargetRadius),this.target.add(this.cursor);let s=!1;if(this.zoomToCursor&&this._performCursorZoom||this.object.isOrthographicCamera)this._spherical.radius=this._clampDistance(this._spherical.radius);else{const a=this._spherical.radius;this._spherical.radius=this._clampDistance(this._spherical.radius*this._scale),s=a!=this._spherical.radius}if(xt.setFromSpherical(this._spherical),xt.applyQuaternion(this._quatInverse),n.copy(this.target).add(xt),this.object.lookAt(this.target),this.enableDamping===!0?(this._sphericalDelta.theta*=1-this.dampingFactor,this._sphericalDelta.phi*=1-this.dampingFactor,this._panOffset.multiplyScalar(1-this.dampingFactor)):(this._sphericalDelta.set(0,0,0),this._panOffset.set(0,0,0)),this.zoomToCursor&&this._performCursorZoom){let a=null;if(this.object.isPerspectiveCamera){const c=xt.length();a=this._clampDistance(c*this._scale);const i=c-a;this.object.position.addScaledVector(this._dollyDirection,i),this.object.updateMatrixWorld(),s=!!i}else if(this.object.isOrthographicCamera){const c=new Jt(this._mouse.x,this._mouse.y,0);c.unproject(this.object);const i=this.object.zoom;this.object.zoom=Math.max(this.minZoom,Math.min(this.maxZoom,this.object.zoom/this._scale)),this.object.updateProjectionMatrix(),s=i!==this.object.zoom;const l=new Jt(this._mouse.x,this._mouse.y,0);l.unproject(this.object),this.object.position.sub(l).add(c),this.object.updateMatrixWorld(),a=xt.length()}else console.warn("WARNING: OrbitControls.js encountered an unknown camera type - zoom to cursor disabled."),this.zoomToCursor=!1;a!==null&&(this.screenSpacePanning?this.target.set(0,0,-1).transformDirection(this.object.matrix).multiplyScalar(a).add(this.object.position):(Ao.origin.copy(this.object.position),Ao.direction.set(0,0,-1).transformDirection(this.object.matrix),Math.abs(this.object.up.dot(Ao.direction))<Al?this.object.lookAt(this.target):(Di.setFromNormalAndCoplanarPoint(this.object.up,this.target),Ao.intersectPlane(Di,this.target))))}else if(this.object.isOrthographicCamera){const a=this.object.zoom;this.object.zoom=Math.max(this.minZoom,Math.min(this.maxZoom,this.object.zoom/this._scale)),a!==this.object.zoom&&(this.object.updateProjectionMatrix(),s=!0)}return this._scale=1,this._performCursorZoom=!1,s||this._lastPosition.distanceToSquared(this.object.position)>wr||8*(1-this._lastQuaternion.dot(this.object.quaternion))>wr||this._lastTargetPosition.distanceToSquared(this.target)>wr?(this.dispatchEvent(Oi),this._lastPosition.copy(this.object.position),this._lastQuaternion.copy(this.object.quaternion),this._lastTargetPosition.copy(this.target),!0):!1}_getAutoRotationAngle(t){return t!==null?Lt/60*this.autoRotateSpeed*t:Lt/60/60*this.autoRotateSpeed}_getZoomScale(t){const n=Math.abs(t*.01);return Math.pow(.95,this.zoomSpeed*n)}_rotateLeft(t){this._sphericalDelta.theta-=t}_rotateUp(t){this._sphericalDelta.phi-=t}_panLeft(t,n){xt.setFromMatrixColumn(n,0),xt.multiplyScalar(-t),this._panOffset.add(xt)}_panUp(t,n){this.screenSpacePanning===!0?xt.setFromMatrixColumn(n,1):(xt.setFromMatrixColumn(n,0),xt.crossVectors(this.object.up,xt)),xt.multiplyScalar(t),this._panOffset.add(xt)}_pan(t,n){const o=this.domElement;if(this.object.isPerspectiveCamera){const r=this.object.position;xt.copy(r).sub(this.target);let s=xt.length();s*=Math.tan(this.object.fov/2*Math.PI/180),this._panLeft(2*t*s/o.clientHeight,this.object.matrix),this._panUp(2*n*s/o.clientHeight,this.object.matrix)}else this.object.isOrthographicCamera?(this._panLeft(t*(this.object.right-this.object.left)/this.object.zoom/o.clientWidth,this.object.matrix),this._panUp(n*(this.object.top-this.object.bottom)/this.object.zoom/o.clientHeight,this.object.matrix)):(console.warn("WARNING: OrbitControls.js encountered an unknown camera type - pan disabled."),this.enablePan=!1)}_dollyOut(t){this.object.isPerspectiveCamera||this.object.isOrthographicCamera?this._scale/=t:(console.warn("WARNING: OrbitControls.js encountered an unknown camera type - dolly/zoom disabled."),this.enableZoom=!1)}_dollyIn(t){this.object.isPerspectiveCamera||this.object.isOrthographicCamera?this._scale*=t:(console.warn("WARNING: OrbitControls.js encountered an unknown camera type - dolly/zoom disabled."),this.enableZoom=!1)}_updateZoomParameters(t,n){if(!this.zoomToCursor)return;this._performCursorZoom=!0;const o=this.domElement.getBoundingClientRect(),r=t-o.left,s=n-o.top,a=o.width,c=o.height;this._mouse.x=r/a*2-1,this._mouse.y=-(s/c)*2+1,this._dollyDirection.set(this._mouse.x,this._mouse.y,1).unproject(this.object).sub(this.object.position).normalize()}_clampDistance(t){return Math.max(this.minDistance,Math.min(this.maxDistance,t))}_handleMouseDownRotate(t){this._rotateStart.set(t.clientX,t.clientY)}_handleMouseDownDolly(t){this._updateZoomParameters(t.clientX,t.clientX),this._dollyStart.set(t.clientX,t.clientY)}_handleMouseDownPan(t){this._panStart.set(t.clientX,t.clientY)}_handleMouseMoveRotate(t){this._rotateEnd.set(t.clientX,t.clientY),this._rotateDelta.subVectors(this._rotateEnd,this._rotateStart).multiplyScalar(this.rotateSpeed);const n=this.domElement;this._rotateLeft(Lt*this._rotateDelta.x/n.clientHeight),this._rotateUp(Lt*this._rotateDelta.y/n.clientHeight),this._rotateStart.copy(this._rotateEnd),this.update()}_handleMouseMoveDolly(t){this._dollyEnd.set(t.clientX,t.clientY),this._dollyDelta.subVectors(this._dollyEnd,this._dollyStart),this._dollyDelta.y>0?this._dollyOut(this._getZoomScale(this._dollyDelta.y)):this._dollyDelta.y<0&&this._dollyIn(this._getZoomScale(this._dollyDelta.y)),this._dollyStart.copy(this._dollyEnd),this.update()}_handleMouseMovePan(t){this._panEnd.set(t.clientX,t.clientY),this._panDelta.subVectors(this._panEnd,this._panStart).multiplyScalar(this.panSpeed),this._pan(this._panDelta.x,this._panDelta.y),this._panStart.copy(this._panEnd),this.update()}_handleMouseWheel(t){this._updateZoomParameters(t.clientX,t.clientY),t.deltaY<0?this._dollyIn(this._getZoomScale(t.deltaY)):t.deltaY>0&&this._dollyOut(this._getZoomScale(t.deltaY)),this.update()}_handleKeyDown(t){let n=!1;switch(t.code){case this.keys.UP:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateUp(Lt*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(0,this.keyPanSpeed),n=!0;break;case this.keys.BOTTOM:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateUp(-Lt*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(0,-this.keyPanSpeed),n=!0;break;case this.keys.LEFT:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateLeft(Lt*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(this.keyPanSpeed,0),n=!0;break;case this.keys.RIGHT:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateLeft(-Lt*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(-this.keyPanSpeed,0),n=!0;break}n&&(t.preventDefault(),this.update())}_handleTouchStartRotate(t){if(this._pointers.length===1)this._rotateStart.set(t.pageX,t.pageY);else{const n=this._getSecondPointerPosition(t),o=.5*(t.pageX+n.x),r=.5*(t.pageY+n.y);this._rotateStart.set(o,r)}}_handleTouchStartPan(t){if(this._pointers.length===1)this._panStart.set(t.pageX,t.pageY);else{const n=this._getSecondPointerPosition(t),o=.5*(t.pageX+n.x),r=.5*(t.pageY+n.y);this._panStart.set(o,r)}}_handleTouchStartDolly(t){const n=this._getSecondPointerPosition(t),o=t.pageX-n.x,r=t.pageY-n.y,s=Math.sqrt(o*o+r*r);this._dollyStart.set(0,s)}_handleTouchStartDollyPan(t){this.enableZoom&&this._handleTouchStartDolly(t),this.enablePan&&this._handleTouchStartPan(t)}_handleTouchStartDollyRotate(t){this.enableZoom&&this._handleTouchStartDolly(t),this.enableRotate&&this._handleTouchStartRotate(t)}_handleTouchMoveRotate(t){if(this._pointers.length==1)this._rotateEnd.set(t.pageX,t.pageY);else{const o=this._getSecondPointerPosition(t),r=.5*(t.pageX+o.x),s=.5*(t.pageY+o.y);this._rotateEnd.set(r,s)}this._rotateDelta.subVectors(this._rotateEnd,this._rotateStart).multiplyScalar(this.rotateSpeed);const n=this.domElement;this._rotateLeft(Lt*this._rotateDelta.x/n.clientHeight),this._rotateUp(Lt*this._rotateDelta.y/n.clientHeight),this._rotateStart.copy(this._rotateEnd)}_handleTouchMovePan(t){if(this._pointers.length===1)this._panEnd.set(t.pageX,t.pageY);else{const n=this._getSecondPointerPosition(t),o=.5*(t.pageX+n.x),r=.5*(t.pageY+n.y);this._panEnd.set(o,r)}this._panDelta.subVectors(this._panEnd,this._panStart).multiplyScalar(this.panSpeed),this._pan(this._panDelta.x,this._panDelta.y),this._panStart.copy(this._panEnd)}_handleTouchMoveDolly(t){const n=this._getSecondPointerPosition(t),o=t.pageX-n.x,r=t.pageY-n.y,s=Math.sqrt(o*o+r*r);this._dollyEnd.set(0,s),this._dollyDelta.set(0,Math.pow(this._dollyEnd.y/this._dollyStart.y,this.zoomSpeed)),this._dollyOut(this._dollyDelta.y),this._dollyStart.copy(this._dollyEnd);const a=(t.pageX+n.x)*.5,c=(t.pageY+n.y)*.5;this._updateZoomParameters(a,c)}_handleTouchMoveDollyPan(t){this.enableZoom&&this._handleTouchMoveDolly(t),this.enablePan&&this._handleTouchMovePan(t)}_handleTouchMoveDollyRotate(t){this.enableZoom&&this._handleTouchMoveDolly(t),this.enableRotate&&this._handleTouchMoveRotate(t)}_addPointer(t){this._pointers.push(t.pointerId)}_removePointer(t){delete this._pointerPositions[t.pointerId];for(let n=0;n<this._pointers.length;n++)if(this._pointers[n]==t.pointerId){this._pointers.splice(n,1);return}}_isTrackingPointer(t){for(let n=0;n<this._pointers.length;n++)if(this._pointers[n]==t.pointerId)return!0;return!1}_trackPointer(t){let n=this._pointerPositions[t.pointerId];n===void 0&&(n=new kt,this._pointerPositions[t.pointerId]=n),n.set(t.pageX,t.pageY)}_getSecondPointerPosition(t){const n=t.pointerId===this._pointers[0]?this._pointers[1]:this._pointers[0];return this._pointerPositions[n]}_customWheelEvent(t){const n=t.deltaMode,o={clientX:t.clientX,clientY:t.clientY,deltaY:t.deltaY};switch(n){case 1:o.deltaY*=16;break;case 2:o.deltaY*=100;break}return t.ctrlKey&&!this._controlActive&&(o.deltaY*=10),o}}function Nl(e){this.enabled!==!1&&(this._pointers.length===0&&(this.domElement.setPointerCapture(e.pointerId),this.domElement.ownerDocument.addEventListener("pointermove",this._onPointerMove),this.domElement.ownerDocument.addEventListener("pointerup",this._onPointerUp)),!this._isTrackingPointer(e)&&(this._addPointer(e),e.pointerType==="touch"?this._onTouchStart(e):this._onMouseDown(e)))}function _l(e){this.enabled!==!1&&(e.pointerType==="touch"?this._onTouchMove(e):this._onMouseMove(e))}function Fl(e){switch(this._removePointer(e),this._pointers.length){case 0:this.domElement.releasePointerCapture(e.pointerId),this.domElement.ownerDocument.removeEventListener("pointermove",this._onPointerMove),this.domElement.ownerDocument.removeEventListener("pointerup",this._onPointerUp),this.dispatchEvent(ea),this.state=Ke.NONE;break;case 1:const t=this._pointers[0],n=this._pointerPositions[t];this._onTouchStart({pointerId:t,pageX:n.x,pageY:n.y});break}}function Ml(e){let t;switch(e.button){case 0:t=this.mouseButtons.LEFT;break;case 1:t=this.mouseButtons.MIDDLE;break;case 2:t=this.mouseButtons.RIGHT;break;default:t=-1}switch(t){case In.DOLLY:if(this.enableZoom===!1)return;this._handleMouseDownDolly(e),this.state=Ke.DOLLY;break;case In.ROTATE:if(e.ctrlKey||e.metaKey||e.shiftKey){if(this.enablePan===!1)return;this._handleMouseDownPan(e),this.state=Ke.PAN}else{if(this.enableRotate===!1)return;this._handleMouseDownRotate(e),this.state=Ke.ROTATE}break;case In.PAN:if(e.ctrlKey||e.metaKey||e.shiftKey){if(this.enableRotate===!1)return;this._handleMouseDownRotate(e),this.state=Ke.ROTATE}else{if(this.enablePan===!1)return;this._handleMouseDownPan(e),this.state=Ke.PAN}break;default:this.state=Ke.NONE}this.state!==Ke.NONE&&this.dispatchEvent(Qr)}function Rl(e){switch(this.state){case Ke.ROTATE:if(this.enableRotate===!1)return;this._handleMouseMoveRotate(e);break;case Ke.DOLLY:if(this.enableZoom===!1)return;this._handleMouseMoveDolly(e);break;case Ke.PAN:if(this.enablePan===!1)return;this._handleMouseMovePan(e);break}}function Cl(e){this.enabled===!1||this.enableZoom===!1||this.state!==Ke.NONE||(e.preventDefault(),this.dispatchEvent(Qr),this._handleMouseWheel(this._customWheelEvent(e)),this.dispatchEvent(ea))}function Il(e){this.enabled!==!1&&this._handleKeyDown(e)}function Tl(e){switch(this._trackPointer(e),this._pointers.length){case 1:switch(this.touches.ONE){case Cn.ROTATE:if(this.enableRotate===!1)return;this._handleTouchStartRotate(e),this.state=Ke.TOUCH_ROTATE;break;case Cn.PAN:if(this.enablePan===!1)return;this._handleTouchStartPan(e),this.state=Ke.TOUCH_PAN;break;default:this.state=Ke.NONE}break;case 2:switch(this.touches.TWO){case Cn.DOLLY_PAN:if(this.enableZoom===!1&&this.enablePan===!1)return;this._handleTouchStartDollyPan(e),this.state=Ke.TOUCH_DOLLY_PAN;break;case Cn.DOLLY_ROTATE:if(this.enableZoom===!1&&this.enableRotate===!1)return;this._handleTouchStartDollyRotate(e),this.state=Ke.TOUCH_DOLLY_ROTATE;break;default:this.state=Ke.NONE}break;default:this.state=Ke.NONE}this.state!==Ke.NONE&&this.dispatchEvent(Qr)}function Pl(e){switch(this._trackPointer(e),this.state){case Ke.TOUCH_ROTATE:if(this.enableRotate===!1)return;this._handleTouchMoveRotate(e),this.update();break;case Ke.TOUCH_PAN:if(this.enablePan===!1)return;this._handleTouchMovePan(e),this.update();break;case Ke.TOUCH_DOLLY_PAN:if(this.enableZoom===!1&&this.enablePan===!1)return;this._handleTouchMoveDollyPan(e),this.update();break;case Ke.TOUCH_DOLLY_ROTATE:if(this.enableZoom===!1&&this.enableRotate===!1)return;this._handleTouchMoveDollyRotate(e),this.update();break;default:this.state=Ke.NONE}}function kl(e){this.enabled!==!1&&e.preventDefault()}function El(e){e.key==="Control"&&(this._controlActive=!0,this.domElement.getRootNode().addEventListener("keyup",this._interceptControlUp,{passive:!0,capture:!0}))}function Ol(e){e.key==="Control"&&(this._controlActive=!1,this.domElement.getRootNode().removeEventListener("keyup",this._interceptControlUp,{passive:!0,capture:!0}))}const No=window;function vr(){const e=window;if(typeof e.__cooptIOSDetected=="boolean")return e.__cooptIOSDetected;try{const t=String(navigator.userAgent||"");if(/iPad|iPhone|iPod/i.test(t)||/Macintosh/i.test(t)&&Number(navigator.maxTouchPoints||0)>1)return!0}catch{}return!1}function Dl(){const e=vr(),t=window;t.__sceneSetupIOSDetected=e;let n=document.getElementById("threejs-canvas-container");const o=n?n.clientWidth:At.CANVAS_WIDTH,r=n?n.clientHeight:At.CANVAS_HEIGHT,s=o/r,a=At.VIEW_SIZE,c=new mc;c.userData=c.userData||{},c.userData.renderContext={three:Gr,global:typeof window<"u"?window:globalThis};const i=new fc(-a*s/2,a*s/2,a/2,-a/2,At.CAMERA_CLIP_NEAR,At.CAMERA_CLIP_FAR),l=new gc({antialias:!0,alpha:!0,precision:"highp",logarithmicDepthBuffer:!0});l.setPixelRatio(window.devicePixelRatio),l.setSize(o,r,!1),l.setClearColor(0,0),l.sortObjects=!1,l.shadowMap.enabled=!1;const f=()=>{const u=document.getElementById("threejs-canvas-container");if(!u)return null;const y=vr();if(y){console.log("📱 iOS detected - applying rendering fix"),u.style.display="block",u.style.position="relative",u.style.minHeight="600px",u.style.height="calc(100vh - 200px)",u.offsetHeight;const m=window.getComputedStyle(u);console.log("📱 iOS container computed style:",{display:m.display,position:m.position,width:m.width,height:m.height,overflow:m.overflow,justifyContent:m.justifyContent})}if(l.domElement.parentElement!==u&&u.appendChild(l.domElement),y){const m=u.getBoundingClientRect();console.log("📱 iOS container rect:",{x:m.x,y:m.y,width:m.width,height:m.height,left:m.left,right:m.right,top:m.top,bottom:m.bottom});const h=m.width>0?m.width:u.clientWidth||800,g=m.height>0?m.height:u.clientHeight||600;console.log(`📱 iOS container dimensions: ${h}x${g}`),l.setSize(h,g,!0),console.log(`📱 iOS canvas size set to: ${h}x${g} (updateStyle=true)`);const w=l.domElement;console.log("📱 iOS canvas attributes:",{width:w.width,height:w.height,styleWidth:w.style.width,styleHeight:w.style.height}),w.style.width=`${h}px`,w.style.height=`${g}px`,w.style.position="absolute",w.style.left="50%",w.style.top="50%",w.style.transform="translate(-50%, -50%)",w.style.webkitTransform="translate(-50%, -50%)",w.style.maxWidth="100%",w.style.maxHeight="100%",console.log(`📱 iOS canvas styled: ${h}px x ${g}px, positioned absolute with transform`),setTimeout(()=>{const R=w.getBoundingClientRect();console.log("📱 iOS canvas position after 100ms:",{x:R.x,y:R.y,width:R.width,height:R.height,left:R.left,right:R.right});const N=window.getComputedStyle(w);console.log("📱 iOS canvas computed style:",{position:N.position,left:N.left,top:N.top,transform:N.transform,width:N.width,height:N.height})},100),console.log("📱 iOS canvas positioned - CSS transform will center")}else l.domElement.style.width="100%",l.domElement.style.height="100%";return n=u,u},p=f();p&&p.clientWidth>0&&p.clientHeight>0&&l.setSize(p.clientWidth,p.clientHeight,!1);const d=new ta(i,l.domElement);return d.enableDamping=!0,d.dampingFactor=.05,d.screenSpacePanning=!0,d.enableZoom=!0,d.maxDistance=5e3,d.minDistance=10,d.enableRotate=!0,d.enablePan=!0,i.position.set(At.CAMERA_INITIAL_POSITION.x,At.CAMERA_INITIAL_POSITION.y,At.CAMERA_INITIAL_POSITION.z),i.lookAt(At.CAMERA_INITIAL_TARGET.x,At.CAMERA_INITIAL_TARGET.y,At.CAMERA_INITIAL_TARGET.z),d.target.set(At.CAMERA_INITIAL_TARGET.x,At.CAMERA_INITIAL_TARGET.y,At.CAMERA_INITIAL_TARGET.z),d.update(),i.updateProjectionMatrix(),window.addEventListener("resize",()=>{const u=vr();console.log(`🔄 Window resize event (scene-setup.js)${u?" [iOS]":""}`);const y=f();if(y){const m=y.clientWidth,h=y.clientHeight;if(u&&console.log(`📱 iOS resize - container: ${m}x${h}, devicePixelRatio: ${window.devicePixelRatio}`),m<=0||h<=0)return;if(l.setPixelRatio(window.devicePixelRatio),l.setSize(m,h,!1),l.domElement.style.width="100%",l.domElement.style.height="100%",t.updateCameraViewBounds)console.log("📷 Calling updateCameraViewBounds from resize handler"),t.updateCameraViewBounds();else{const g=m/h;i.left=-a*g/2,i.right=a*g/2,i.top=a/2,i.bottom=-a/2,i.updateProjectionMatrix()}console.log(`Canvas resized to: ${m}x${h} (pixelRatio: ${window.devicePixelRatio})`)}}),window.addEventListener("coopt:react-mounted",()=>{const u=f();if(!u)return;const y=u.clientWidth,m=u.clientHeight;y<=0||m<=0||(l.setPixelRatio(window.devicePixelRatio),l.setSize(y,m,!1))}),{scene:c,camera:i,renderer:l,controls:d}}function jl(e){try{const t=new yc(16777215,At.AMBIENT_LIGHT_INTENSITY);e.add(t);const n=new hc(16777215,At.DIRECTIONAL_LIGHT_INTENSITY);return n.position.set(At.DIRECTIONAL_LIGHT_POSITION.x,At.DIRECTIONAL_LIGHT_POSITION.y,At.DIRECTIONAL_LIGHT_POSITION.z),e.add(n),{ambientLight:t,directionalLight:n}}catch(t){throw console.error("❌ Error initializing lighting:",t),t}}function Ll(){function e(){requestAnimationFrame(e);const t=No.scene,n=No.camera,o=No.renderer,r=No.controls;t&&n&&o&&r&&(r.update(),o.render(t,n))}e()}function Bl(){try{const e=Kr("lastWavefrontSnapshot");if(!e)return null;const t=JSON.parse(e);return t&&typeof t=="object"?t:null}catch{return null}}function zl(e){try{yl("lastWavefrontSnapshot",JSON.stringify(e))}catch{}}function $l(){try{const e=Kr("lastPsfMeta");if(!e)return null;const t=JSON.parse(e);return t&&typeof t=="object"?t:null}catch{return null}}function Wl(){try{const e=Kr("lastPsfError");if(!e)return null;const t=JSON.parse(e);return t&&typeof t=="object"?t:null}catch{return null}}class qt{constructor(t){this.containerElementIdOrElement=t,this.plotlyConfig={displayModeBar:!0,modeBarButtonsToRemove:["pan2d","lasso2d"],responsive:!0}}_createWavefrontEngine(t,n=.5876){const o=typeof window<"u"?window:null,r=o?.opener||o,s=typeof r?.createOPDCalculator=="function"&&r.createOPDCalculator||typeof o?.createOPDCalculator=="function"&&o.createOPDCalculator||oo,a=typeof r?.createWavefrontAnalyzer=="function"&&r.createWavefrontAnalyzer||typeof o?.createWavefrontAnalyzer=="function"&&o.createWavefrontAnalyzer||ro,c=s?s(t,n):null;if(!c)throw new Error("波面収差計算機の初期化に失敗しました: OPDCalculator を作成できません");const i=a?a(c):null;if(!i)throw new Error("波面収差計算機の初期化に失敗しました: WavefrontAnalyzer を作成できません");return{calculator:c,analyzer:i}}_resolveHostWindow(){const t=typeof window<"u"?window:null;return t?.opener||t}_buildWavefrontMapFromOpdGrid(t,n=.5876){const o=Array.isArray(t)?t.length:0,r=o>1?Array.from({length:o},(p,d)=>-1+2*d/(o-1)):[0],s=[],a=[],c=[];for(let p=0;p<o;p++){const d=Array.isArray(t[p])?t[p]:[];for(let u=0;u<d.length;u++){const y=d[u];if(y==null)continue;const m=Number(y);if(!Number.isFinite(m))continue;const h=r[Math.min(u,r.length-1)]??0,g=r[Math.min(p,r.length-1)]??0;s.push({x:h,y:g,ix:u,iy:p}),c.push(m),a.push(m*n)}}const i=p=>{if(!Array.isArray(p)||p.length===0)return{count:0,sampleCount:0,mean:NaN,rms:NaN,peakToPeak:NaN,min:NaN,max:NaN};let d=0,u=0,y=1/0,m=-1/0,h=0;for(const w of p)Number.isFinite(w)&&(d+=w,u+=w*w,w<y&&(y=w),w>m&&(m=w),h+=1);if(h===0)return{count:0,sampleCount:0,mean:NaN,rms:NaN,peakToPeak:NaN,min:NaN,max:NaN};const g=d/h;return{count:h,sampleCount:h,mean:g,rms:Math.sqrt(Math.max(0,u/h)),peakToPeak:m-y,min:y,max:m}},l=i(c),f=i(a);return{pupilCoordinates:s,pupilRange:1,opds:a,opdsInWavelengths:c,wavefrontAberrations:c,raw:{opds:a.slice(),opdsInWavelengths:c.slice(),wavefrontAberrations:c.slice(),opdGrid:t,opdMicrons:f,opdWavelengths:l},display:{opds:a.slice(),opdsInWavelengths:c.slice(),wavefrontAberrations:c.slice(),opdGrid:t},statistics:{wavefront:l,opdMicrons:f,opdWavelengths:l,raw:{wavefront:l,opdMicrons:f,opdWavelengths:l},display:{opdMicrons:f,opdWavelengths:l}}}}_buildGridFromSamples(t,n,o){const r=Math.max(1,Math.floor(Number(o)||1)),s=Array.from({length:r},()=>Array.from({length:r},()=>null));if(!Array.isArray(t)||!Array.isArray(n))return s;const a=Math.min(t.length,n.length);for(let c=0;c<a;c++){const i=t[c],l=Number(n[c]);if(!i||!Number.isFinite(l))continue;const f=Number.isInteger(i?.ix)?i.ix:Math.round((Number(i?.x)+1)*.5*(r-1)),p=Number.isInteger(i?.iy)?i.iy:Math.round((Number(i?.y)+1)*.5*(r-1));!Number.isFinite(f)||!Number.isFinite(p)||p<0||p>=r||f<0||f>=r||(s[p][f]=l)}return s}_applyNativeZernikePostProcess(t,n,o={}){const r=Number(o?.wavelength),s=String(o?.opdDisplayMode||"pistonTiltRemoved"),a=!!o?.skipZernikeFit,c=Number.isFinite(Number(o?.zernikeMaxNoll))?Math.max(1,Math.floor(Number(o.zernikeMaxNoll))):37;if(t.opdMode="simple",t.opdDisplayModeRequested=s,t.skipZernikeFit=a,a)t.zernike=null;else{const h=Array.isArray(t?.raw?.opds)?t.raw.opds.length:0,g=Math.max(1,Math.min(c,h));t.zernike=n.fitZernikePolynomials({pupilCoordinates:t.pupilCoordinates,opds:t.raw.opds},g)}if(t?.zernike?.removedModelMicrons?.length===t?.opds?.length)for(let h=0;h<t.opds.length;h++){const g=Number(t.raw.opds[h]),w=Number(t.zernike.removedModelMicrons[h]),R=Number.isFinite(g)&&Number.isFinite(w)?g-w:NaN;t.opds[h]=R,t.opdsInWavelengths[h]=Number.isFinite(R)&&Number.isFinite(r)&&r>0?R/r:NaN,t.wavefrontAberrations[h]=t.opdsInWavelengths[h]}let i=null,l=null;if(s==="pistonTiltRemoved"){const h=n._removeBestFitPlane(t.pupilCoordinates,t.opds);if(h&&Array.isArray(h.residualMicrons)&&Array.isArray(h.residualWaves))i={mode:"pistonTiltRemoved",planeCoefficientsMicrons:h.coefficientsMicrons,opds:h.residualMicrons,opdsInWavelengths:h.residualWaves,wavefrontAberrations:h.residualWaves},l={mode:"pistonTiltRemoved",planeCoefficientsMicrons:h.coefficientsMicrons,opdMicrons:n.calculateStatistics(h.residualMicrons,{removePiston:!1}),opdWavelengths:n.calculateStatistics(h.residualWaves,{removePiston:!1})};else{const g=n._calculateLowOrderRemovedStats(t.pupilCoordinates,t.opds,{removeIndices:[0,1,2],maxOrder:2,pupilRange:t.pupilRange});g&&Array.isArray(g.residualMicrons)&&Array.isArray(g.residualWaves)&&(i={mode:"pistonTiltRemoved",opds:g.residualMicrons,opdsInWavelengths:g.residualWaves,wavefrontAberrations:g.residualWaves},l={mode:"pistonTiltRemoved",opdMicrons:n.calculateStatistics(g.residualMicrons,{removePiston:!1}),opdWavelengths:n.calculateStatistics(g.residualWaves,{removePiston:!1})})}}else if(s==="pistonTiltDefocusRemoved"){const h=n._calculateLowOrderRemovedStats(t.pupilCoordinates,t.opds,{removeIndices:[0,1,2,4],maxOrder:2,pupilRange:t.pupilRange});h&&Array.isArray(h.residualMicrons)&&Array.isArray(h.residualWaves)&&(i={mode:"pistonTiltDefocusRemoved",opds:h.residualMicrons,opdsInWavelengths:h.residualWaves,wavefrontAberrations:h.residualWaves},l={mode:"pistonTiltDefocusRemoved",opdMicrons:n.calculateStatistics(h.residualMicrons,{removePiston:!1}),opdWavelengths:n.calculateStatistics(h.residualWaves,{removePiston:!1})})}if(!i||!l){if(i={mode:"default",opds:t.opds.slice(),opdsInWavelengths:t.opdsInWavelengths.slice(),wavefrontAberrations:t.wavefrontAberrations.slice()},s!=="default"){const h=i.opds.slice();let g=0,w=0;for(const N of h)Number.isFinite(N)&&(g+=N,w+=1);const R=w>0?g/w:0;for(let N=0;N<h.length;N++){const I=h[N];h[N]=Number.isFinite(I)?I-R:NaN}i.opds=h,i.opdsInWavelengths=h.map(N=>Number.isFinite(N)&&Number.isFinite(r)&&r>0?N/r:NaN),i.wavefrontAberrations=i.opdsInWavelengths.slice(),i.mode=`${s} (piston-only fallback)`}l={mode:i.mode||"default",opdMicrons:n.calculateStatistics(i.opds,{removePiston:!1}),opdWavelengths:n.calculateStatistics(i.opdsInWavelengths,{removePiston:!1})}}const f=Array.isArray(t?.raw?.opdGrid)?t.raw.opdGrid.length:0;i.opdGrid=this._buildGridFromSamples(t.pupilCoordinates,i.opdsInWavelengths,f),t.display=i;const p=n._calculateLowOrderRemovedStats(t.pupilCoordinates,t.raw.opds,{removeIndices:[0,1,2,4],maxOrder:2,pupilRange:t.pupilRange}),d=t.opdMode||"simple",u=!!t.skipZernikeFit,y=t.pupilSamplingMode||null;t.statistics={wavefront:n.calculateStatistics(t.wavefrontAberrations,{removePiston:!0}),opdMicrons:l.opdMicrons,opdWavelengths:l.opdWavelengths,raw:{wavefront:n.calculateStatistics(t.raw.wavefrontAberrations,{removePiston:!1}),opdMicrons:n.calculateStatistics(t.raw.opds,{removePiston:!1}),opdWavelengths:n.calculateStatistics(t.raw.opdsInWavelengths,{removePiston:!1})},aberration:p,display:l};const m=[t.statistics?.wavefront,t.statistics?.opdMicrons,t.statistics?.opdWavelengths,t.statistics?.raw?.wavefront,t.statistics?.raw?.opdMicrons,t.statistics?.raw?.opdWavelengths,t.statistics?.display?.opdMicrons,t.statistics?.display?.opdWavelengths,t.statistics?.aberration?.opdMicrons,t.statistics?.aberration?.opdWavelengths];for(const h of m)!h||typeof h!="object"||(h.pupilSamplingMode=y,h.opdMode=d,h.skipZernikeFit=u);t.statistics?.display?.opdMicrons&&(t.statistics.display.opdMicrons.opdDisplayMode=s),t.statistics?.display?.opdWavelengths&&(t.statistics.display.opdWavelengths.opdDisplayMode=s),t.statistics?.aberration?.opdMicrons&&t.statistics?.aberration?.removeIndices&&(t.statistics.aberration.opdMicrons.removeIndices=t.statistics.aberration.removeIndices),t.statistics?.aberration?.opdWavelengths&&t.statistics?.aberration?.removeIndices&&(t.statistics.aberration.opdWavelengths.removeIndices=t.statistics.aberration.removeIndices)}async _computeNativeOpdWavefrontMap(t,n,o,r={}){const s=(x,T)=>{try{typeof r?.onProgress=="function"&&r.onProgress({percent:x,message:T,phase:"opd-native"})}catch{}};s(5,"Checking Rust/WASM OPD runtime..."),s(10,"Loading native OPD bridge...");const{runNativeOpdMap:a}=await De(async()=>{const{runNativeOpdMap:x}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(T=>T.bZ);return{runNativeOpdMap:x}},__vite__mapDeps([0,1])),c=this._resolveHostWindow();s(16,"Collecting optical/source/object rows...");const i=c&&typeof c.getSourceRows=="function"?c.getSourceRows():[],l=c&&typeof c.getObjectRows=="function"?c.getObjectRows():[],f=Number.isInteger(Number(n?.objectIndex))?Number(n.objectIndex):0,d=(()=>{const x=T=>{const j=typeof T=="string"?T.trim().toLowerCase():"";return j==="stop"||j==="entrance"?j:""};try{const T=globalThis?.__COOPT_FORCE_INFINITE_PUPIL_MODE??globalThis?.COOPT_FORCE_INFINITE_PUPIL_MODE,j=x(T);if(j)return j}catch{}try{const T=localStorage.getItem("coopt.forceInfinitePupilMode");return x(T)}catch{return""}})(),u=Array.isArray(l)&&l[f]?l[f]:null,y=String(u?.position??u?.object??u?.objectType??u?.type??n?.type??"").trim().toLowerCase(),m=y.includes("angle")||y==="point"||String(n?.type||"").trim().toLowerCase()==="angle",h=Number(u?.xHeightAngle??u?.xFieldAngle??u?.xAngle??n?.fieldAngle?.x??0),g=Number(u?.yHeightAngle??u?.yFieldAngle??u?.yAngle??u?.fieldAngle??n?.fieldAngle?.y??0),w=Math.abs(h)>1e-12||Math.abs(g)>1e-12,R=d==="stop"||d==="entrance"?d:m&&w?"entrance":void 0,N=(()=>{const x=Number(o);return Number.isFinite(x)?Math.max(17,Math.floor(x)):129})(),I=Number.isFinite(Number(o))?Math.floor(Number(o)):129;I<17&&I!==N&&s(20,`Grid request raised to minimum for native compute (${I} -> ${N})`),s(25,`Tracing rays in native Rust (grid=${N})...`);const b=`native-opd-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let A=null;try{const{listen:x}=await De(async()=>{const{listen:T}=await import("./event-H3glLpTN.js");return{listen:T}},__vite__mapDeps([2,0,1]));A=await x("analysis-progress",T=>{try{const j=T?.payload||{};if(!j||String(j.jobId||"")!==b)return;s(j.percent,j.phase||"opd-native",j.message||"Tracing rays in native Rust...")}catch{}})}catch{}let v;try{const x=Number(n?.wavelength);v=await a({jobId:b,opticalSystemRows:Array.isArray(t)?t:[],sourceRows:Array.isArray(i)?i:[],objectRows:Array.isArray(l)?l:[],objectIndex:f,wavelengthUm:Number.isFinite(x)&&x>0?x:void 0,gridSize:N,pupilSamplingMode:R,opdDisplayMode:r?.opdDisplayMode||"pistonTiltRemoved"})}finally{try{typeof A=="function"&&A()}catch{}}try{console.log("🧭 [OPD Backend] native",v?.backend,{gridSize:v?.gridSize,hitCount:v?.hitCount})}catch{}const S=Number(v?.hitCount)||0,_=Number(v?.sampleCount)||0,M=_>0?S/_*100:0,G=Number.isFinite(M)?M.toFixed(1):"0.0";s(75,`Converting OPD map (hits=${S}/${_}, valid=${G}%)...`);const z=x=>{if(!Array.isArray(x)||x.length===0)return!1;for(const T of x)if(Array.isArray(T)){for(const j of T)if(Number.isFinite(Number(j)))return!0}return!1},E=Array.isArray(v?.displayOpdGrid)?v.displayOpdGrid:[],L=Array.isArray(v?.rawOpdGrid)?v.rawOpdGrid:[],ne=x=>Array.isArray(x)?x.map(T=>Array.isArray(T)?T.map(j=>Number.isFinite(Number(j))?0:j):T):x,V=!!r?.forceIdealParaxialZero,K=V?ne(E):E,$=V?ne(L):L;if(V)try{console.log("ℹ️ [OPD] Ideal Paraxial/ThinLens-only system: OPD forced to 0 (diffraction limit).")}catch{}const J=z(K),Q=J?K:$;if(!J)try{console.warn("⚠️ [OPD Native] display grid had no finite samples; falling back to raw grid",{backend:v?.backend,message:v?.message,hitCount:v?.hitCount,sampleCount:v?.sampleCount})}catch{}if(!Q.length||!Array.isArray(Q[0])||!z(Q))throw new Error("Native OPD map is empty");const le=Number(n?.wavelength)||.5876,Z=z($)?$:Q,B=this._buildWavefrontMapFromOpdGrid(Z,le),C=this._buildWavefrontMapFromOpdGrid(Q,le),F={...C,raw:{...B?.raw||{},opdGrid:Z},display:{...C?.display||{},mode:r?.opdDisplayMode||"pistonTiltRemoved",opdGrid:Q},statistics:{...C?.statistics||{},raw:{...B?.statistics?.raw||{}},display:{...C?.statistics?.display||{},opdDisplayMode:r?.opdDisplayMode||"pistonTiltRemoved"}},zernike:null,opdMode:"native-grid",opdDisplayModeRequested:r?.opdDisplayMode||"pistonTiltRemoved",skipZernikeFit:!0};try{const x=r?.opdDisplayMode||"pistonTiltRemoved",T=String(v?.pupilSamplingMode||"").toLowerCase(),j=T==="stop"||T==="entrance"?T:String(v?.message||"").includes("stop -> entrance")?"entrance":"stop",P=String(v?.chiefReferenceMode||"").trim(),O=String(v?.backend||"").trim();F.pupilSamplingMode=j;const X=[F?.statistics?.wavefront,F?.statistics?.opdMicrons,F?.statistics?.opdWavelengths,F?.statistics?.raw?.wavefront,F?.statistics?.raw?.opdMicrons,F?.statistics?.raw?.opdWavelengths,F?.statistics?.display?.opdMicrons,F?.statistics?.display?.opdWavelengths];for(const k of X)!k||typeof k!="object"||(k.pupilSamplingMode=j,k.opdDisplayMode=x,P&&(k.chiefReferenceMode=P),O&&(k.backend=O),Number.isFinite(Number(v?.wavelengthUm))&&(k.wavelengthUm=Number(v.wavelengthUm),k.wavelengthNm=Number(v.wavelengthUm)*1e3),Number.isFinite(Number(v?.gridSize))&&(k.gridSize=Number(v.gridSize)),Number.isInteger(Number(v?.targetSurface))&&(k.targetSurface=Number(v.targetSurface)),Number.isInteger(Number(v?.stopSurface))&&(k.stopSurface=Number(v.stopSurface)),Number.isFinite(Number(v?.usedObjectX))&&(k.usedObjectX=Number(v.usedObjectX)),Number.isFinite(Number(v?.usedObjectY))&&(k.usedObjectY=Number(v.usedObjectY)),v?.usedObjectPosition&&(k.usedObjectPosition=String(v.usedObjectPosition)),k.hitCount=Number(v?.hitCount??0),k.sampleCount=Number(v?.sampleCount??0));F.nativeMeta={backend:O||"run_native_opd_map",message:v?.message||null,gridSource:J?"displayOpdGrid":"rawOpdGrid",hitCount:Number(v?.hitCount??0),sampleCount:Number(v?.sampleCount??0),pupilSamplingMode:j,chiefReferenceMode:P||null,wavelengthUm:Number(v?.wavelengthUm??0),gridSize:Number(v?.gridSize??0),targetSurface:Number(v?.targetSurface??0),stopSurface:Number(v?.stopSurface??0)};try{console.log("🧭 [OPD Native] reference policy",{backend:O||"(unknown)",chiefReferenceMode:P||"(unknown)",message:v?.message||null,hitCount:v?.hitCount,sampleCount:v?.sampleCount})}catch{}}catch{}return s(90,`Preparing plot samples (${Number(F?.statistics?.opdWavelengths?.count)||0})...`),F}static getBlueGreenRedColorscale(){return[[0,"rgb(0, 0, 255)"],[.5,"rgb(0, 255, 0)"],[1,"rgb(255, 0, 0)"]]}_extractHintText(t){const n=String(t??""),o=n.indexOf("hint=");return o<0?"":n.slice(o+5).trim()}_renderCalculationUnavailable(t,{title:n,message:o,wavefrontMap:r,fieldSetting:s,extra:a}={}){try{if(!t)return;try{t.style.display="block",t.style.visibility="visible",t.style.minHeight||(t.style.minHeight="120px")}catch{}try{const d=this.resolvePlotly(t);d&&typeof d.purge=="function"&&d.purge(t)}catch{}const c=String(o??r?.error?.message??"有効な光線データがありません。"),i=this._extractHintText(c),l=s?.displayName||s?.id?String(s.displayName??s.id):"Field Point",f=a?String(a):"";try{console.warn("⚠️ [WavefrontPlotter] calculation unavailable",{title:n||"波面計算不能",field:l,message:c,hint:i||null})}catch{}t.innerHTML=`
                <div style="padding: 16px; text-align: left; color: #b71c1c; border: 1px solid #d32f2f; border-radius: 6px; background-color: #ffebee;">
                    <h3 style="margin: 0 0 8px 0;">${n||"波面計算不能"}</h3>
                    <div style="margin: 0 0 8px 0; color: #333;">${l} では波面/OPD を計算できませんでした（ビネッティング/有効FOV外の可能性）。</div>
                    <pre style="margin: 0; white-space: pre-wrap; word-break: break-word; color: #b71c1c;">${c}</pre>
                    ${i?`<div style="margin-top: 10px; color: #333;"><b>hint</b>: ${i}</div>`:""}
                    ${f?`<div style="margin-top: 10px; color: #333;">${f}</div>`:""}
                </div>
            `;const p=this.resolveStatsContainer(t);p&&(p.textContent=""),this._setSystemDataText(`Wavefront/OPD unavailable for ${l}.

${c}${i?`

hint: ${i}`:""}`)}catch{}}resolveContainer(){return this.containerElementIdOrElement?typeof this.containerElementIdOrElement=="string"?document.getElementById(this.containerElementIdOrElement):this.containerElementIdOrElement:null}resolvePlotly(t){return t?.ownerDocument?.defaultView?.Plotly||(typeof Plotly<"u"?Plotly:null)}resolveStatsContainer(t){if(!t)return null;const n=t.id;return n?t.ownerDocument.getElementById(`${n}-stats`):null}_setSystemDataText(t){const n=o=>{const r=["system-data","systemData","popup-system-data"];for(const s of r){const a=o?.getElementById?.(s);if(a&&typeof a.value=="string")return a.value=t,!0}return!1};try{if(n(document))return}catch{}try{if(window.opener&&window.opener.document&&n(window.opener.document))return}catch{}}_updateSystemDataWithZernike(t,n,o=37){try{if(!t||typeof t.formatZernikeReportText!="function")return;const r=t.formatZernikeReportText(n,{maxNoll:o});this._setSystemDataText(typeof r=="string"?r:String(r??""))}catch{}}async plotOPDSurface(t,n,o=.5876,r=16,s={}){try{console.log("🌊 OPD 3Dサーフェスプロット生成開始...");const a=(I,b)=>{try{typeof s?.onProgress=="function"&&s.onProgress({percent:I,message:b,phase:"opd-render"})}catch{}},c=s?.opdDisplayMode||"pistonTiltRemoved",i=await this._computeNativeOpdWavefrontMap(t,n,r,{opdDisplayMode:c,onProgress:s?.onProgress||null,forceIdealParaxialZero:!!s?.forceIdealParaxialZero});if(i?.error)return this._renderCalculationUnavailable(this.resolveContainer(),{title:"OPD計算不能",message:i.error?.message,wavefrontMap:i,fieldSetting:n}),i;if(!(Array.isArray(i?.raw?.opds)?i.raw.opds.length:Array.isArray(i?.opds)?i.opds.length:0)){const I=(()=>{try{const b=i?.invalidReasonCounts;if(b&&typeof b=="object")return`invalid reasons: ${JSON.stringify(b)}`}catch{}return""})();return console.error("❌ OPD波面データが0点です（全光線失敗）。",i?.invalidReasonCounts||i?.error||{}),this._renderCalculationUnavailable(this.resolveContainer(),{title:"OPD計算不能",message:"波面データが0点です（全光線失敗）。",wavefrontMap:i,fieldSetting:n,extra:I}),i}try{if(typeof globalThis<"u"&&globalThis.__OPD_DEBUG===!0){const b=Array.isArray(i?.pupilCoordinates)?i.pupilCoordinates:[];let A=0,v=1/0,S=0;for(const _ of b){const M=Number(_?.x),G=Number(_?.y);if(!Number.isFinite(M)||!Number.isFinite(G))continue;const z=Math.hypot(M,G);Number.isFinite(z)&&(S++,z>A&&(A=z),z<v&&(v=z))}console.log("🔍 [OPD] Pupil coverage",{pupilSamplingMode:i?.pupilSamplingMode,bestEffortVignettedPupil:!!i?.bestEffortVignettedPupil,points:S,maxRho:A,minRho:Number.isFinite(v)?v:null})}}catch{}const f=Array.isArray(i?.display?.opdGrid)?i.display.opdGrid:[],p=Array.isArray(i?.raw?.opdGrid)?i.raw.opdGrid:[],d=Array.isArray(f)&&f.length&&Array.isArray(f[0])?f:p;if(!d.length||!Array.isArray(d[0]))throw new Error("Native OPD grid is unavailable for plotting");const u=d.length,y=255,m=u>y?y:u;if(u>m)try{console.warn(`⚠️ [OPD] Surface render grid downsampled ${u} -> ${m} (compute grid kept at ${u})`)}catch{}const h=m>1?Array.from({length:m},(I,b)=>-1+2*b/(m-1)):[0],g=I=>I==null||!Number.isFinite(Number(I))?NaN:Number(I),w=(()=>{const I=d.map(A=>(Array.isArray(A)?A:[]).map(v=>g(v)));if(m>=u)return I;const b=Array.from({length:m},()=>Array.from({length:m},()=>NaN));for(let A=0;A<m;A++){const v=Math.floor(A*u/m),S=Math.max(v,Math.min(u-1,Math.floor((A+1)*u/m)-1));for(let _=0;_<m;_++){const M=Math.floor(_*u/m),G=Math.max(M,Math.min(u-1,Math.floor((_+1)*u/m)-1));let z=0,E=0;for(let L=v;L<=S;L++){const ne=I[L]||[];for(let V=M;V<=G;V++){const K=ne[V];Number.isFinite(K)&&(z+=K,E+=1)}}E>0&&(b[A][_]=z/E)}}for(let A=0;A<2;A++){const v=b.map(S=>S.slice());for(let S=0;S<m;S++)for(let _=0;_<m;_++){if(Number.isFinite(b[S][_]))continue;let M=0,G=0;for(let z=-1;z<=1;z++)for(let E=-1;E<=1;E++){if(E===0&&z===0)continue;const L=S+z,ne=_+E;if(L<0||L>=m||ne<0||ne>=m)continue;const V=b[L][ne];Number.isFinite(V)&&(M+=V,G+=1)}G>=5&&(v[S][_]=M/G)}for(let S=0;S<m;S++)b[S]=v[S]}return b})(),R={type:"surface",x:h,y:h,z:w,connectgaps:!1,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:"OPD [λ]"}},N={title:{text:`Optical Path Difference (OPD) Distribution - ${n.displayName||"Field Point"}`,font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X",range:[-1.1,1.1],dtick:.5},yaxis:{title:"Pupil coordinate Y",range:[-1.1,1.1],dtick:.5},zaxis:{title:"OPD [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}};try{const I=this.resolveContainer();if(!I)throw new Error("プロットコンテナが見つかりません");console.log("✅ プロットコンテナ確認:",I);const b=this.resolvePlotly(I);if(!b)throw new Error("Plotly.jsライブラリが読み込まれていません");this.validatePlotlyData(R),console.log("✅ データ検証完了"),console.log("🎨 Plotly描画開始:",{container:I.id||"(element)",dataType:R.type,dataSize:`${R.z.length}x${R.z[0]?.length}`}),a(96,"Rendering OPD surface..."),N.autosize=!0,await b.newPlot(I,[R],N,this.plotlyConfig),a(100,"Completed"),console.log("✅ OPD 3Dサーフェス描画完了")}catch(I){console.error("❌ OPD 3Dサーフェス描画エラー:",I),console.log("📊 問題のデータ:",{x:R.x?R.x.length:"undefined",y:R.y?R.y.length:"undefined",z:R.z?`${R.z.length}x${R.z[0]?.length}`:"undefined"});const b=this.createFallbackSurfaceData("opd"),A=this.resolveContainer(),v=this.resolvePlotly(A);A&&v&&(N.autosize=!0,await v.newPlot(A,[b],N,this.plotlyConfig)),console.log("⚠️ フォールバックデータで描画しました")}{const I=i?.statistics?.display?.opdWavelengths?i.statistics.display.opdWavelengths:i?.statistics?.raw?.opdWavelengths||i?.statistics?.opdWavelengths;this.displayStatistics(I,"OPD","λ")}return console.log("✅ OPD 3Dサーフェスプロット生成完了"),i}catch(a){throw console.error("❌ OPD プロット生成エラー:",a),a}}async plotWavefrontAberrationSurface(t,n,o=.5876,r=16){try{console.log("🌊 波面収差（Wλ）3Dサーフェスプロット生成開始...");const s=typeof globalThis<"u"&&globalThis.__WAVEFRONT_PROFILE===!0,{analyzer:a}=this._createWavefrontEngine(t,o),c=typeof globalThis<"u"&&globalThis.__WAVEFRONT_DIAG_DISCONTINUITIES===!0;s&&console.time("⏱️ plotWavefrontSurface.generateWavefrontMap");const i=await a.generateWavefrontMap(n,r,"circular",{recordRays:!1,progressEvery:512,diagnoseDiscontinuities:c,diagTopK:5,opdMode:"simple",zernikeMaxNoll:37,renderFromZernike:!0,profile:s});if(s&&console.timeEnd("⏱️ plotWavefrontSurface.generateWavefrontMap"),i?.error)return this._renderCalculationUnavailable(this.resolveContainer(),{title:"波面計算不能",message:i.error?.message,wavefrontMap:i,fieldSetting:n}),i;if(!(Array.isArray(i?.raw?.opds)?i.raw.opds.length:Array.isArray(i?.opds)?i.opds.length:0)){const d=(()=>{try{const u=i?.invalidReasonCounts;if(u&&typeof u=="object")return`invalid reasons: ${JSON.stringify(u)}`}catch{}return""})();return console.error("❌ 波面データが0点です（全光線失敗）。",i?.invalidReasonCounts||i?.error||{}),this._renderCalculationUnavailable(this.resolveContainer(),{title:"波面計算不能",message:"波面データが0点です（全光線失敗）。",wavefrontMap:i,fieldSetting:n,extra:d}),i}options?.updateSystemData===!0&&this._updateSystemDataWithZernike(a,i,37);let f;try{const d=Math.floor(Number(i?.gridSize))||16,u=typeof globalThis<"u"&&Number.isFinite(globalThis.__WAVEFRONT_RENDER_GRID_MAX)?Math.max(33,Math.floor(Number(globalThis.__WAVEFRONT_RENDER_GRID_MAX))):257,y=typeof globalThis<"u"&&Number.isFinite(globalThis.__WAVEFRONT_RENDER_GRID_SCALE)?Math.max(1,Number(globalThis.__WAVEFRONT_RENDER_GRID_SCALE)):3,m=Math.max(129,Math.min(u,Math.floor(d*y-2)));let h=null;if(i?.renderFromZernike&&typeof a.generateZernikeRenderGrid=="function"){s&&console.time("⏱️ plotWavefrontSurface.generateZernikeRenderGrid");const g=i?.pupilSamplingMode&&i.pupilSamplingMode!=="finite";h=a.generateZernikeRenderGrid(i,m,"wavefront",{rhoMax:.99,useWavefrontMask:g}),s&&console.timeEnd("⏱️ plotWavefrontSurface.generateZernikeRenderGrid")}h&&h.x&&h.y&&h.z?f={type:"surface",x:h.x,y:h.y,z:h.z,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:"Wλ [波長]"},flatshading:!1,lighting:{ambient:.85,diffuse:.85,specular:.03,roughness:.95,fresnel:.05}}:f=this.convertToPlotlySurfaceData(i,"wavefront",{rawMode:!1})}catch{f=this.convertToPlotlySurfaceData(i,"wavefront",{rawMode:!1})}const p={title:{text:`Wavefront Aberration (Wλ) Distribution - ${n.displayName||"Field Point"}`,font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X",range:[-1.1,1.1],dtick:.5},yaxis:{title:"Pupil coordinate Y",range:[-1.1,1.1],dtick:.5},zaxis:{title:"Wavefront aberration [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}};try{const d=this.resolveContainer();if(!d)throw new Error("プロットコンテナが見つかりません");console.log("✅ プロットコンテナ確認:",d);const u=this.resolvePlotly(d);if(!u)throw new Error("Plotly.jsライブラリが読み込まれていません");this.validatePlotlyData(f),console.log("✅ データ検証完了"),console.log("🎨 Plotly描画開始:",{container:d.id||"(element)",dataType:f.type,dataSize:`${f.z.length}x${f.z[0]?.length}`}),p.autosize=!0,s&&console.time("⏱️ plotWavefrontSurface.Plotly.newPlot"),await u.newPlot(d,[f],p,this.plotlyConfig),s&&console.timeEnd("⏱️ plotWavefrontSurface.Plotly.newPlot"),console.log("✅ 波面収差 3Dサーフェス描画完了")}catch(d){console.error("❌ 波面収差 3Dサーフェス描画エラー:",d),console.log("📊 問題のデータ:",{x:f.x?f.x.length:"undefined",y:f.y?f.y.length:"undefined",z:f.z?`${f.z.length}x${f.z[0]?.length}`:"undefined"});const u=this.createFallbackSurfaceData("wavefront"),y=this.resolveContainer(),m=this.resolvePlotly(y);y&&m&&(p.autosize=!0,await m.newPlot(y,[u],p,this.plotlyConfig)),console.log("⚠️ フォールバックデータで描画しました")}return this.displayStatistics(i.statistics.wavefront,"Optical Path Difference","λ"),console.log("✅ 波面収差 3Dサーフェスプロット生成完了"),i}catch(s){throw console.error("❌ 波面収差プロット生成エラー:",s),s}}async plotOPDHeatmap(t,n,o=.5876,r=31,s={}){try{console.log("🌊 OPD ヒートマップ生成開始...");const a=(I,b)=>{try{typeof s?.onProgress=="function"&&s.onProgress({percent:I,message:b,phase:"opd-render"})}catch{}},c=s?.opdDisplayMode||"pistonTiltRemoved",i=await this._computeNativeOpdWavefrontMap(t,n,r,{opdDisplayMode:c,onProgress:s?.onProgress||null,forceIdealParaxialZero:!!s?.forceIdealParaxialZero});if(typeof globalThis<"u"&&globalThis.__WAVEFRONT_DEBUG_DUMP===!0&&console.log("🟦 wavefrontMap:",i),!i||!i.pupilCoordinates||i.pupilCoordinates.length===0)throw new Error("有効な光線データがありません。光学系設定を確認してください。");const l=Array.isArray(i?.display?.opdGrid)?i.display.opdGrid:[],f=Array.isArray(i?.raw?.opdGrid)?i.raw.opdGrid:[],p=Array.isArray(l)&&l.length&&Array.isArray(l[0])?l:f;if(!p.length||!Array.isArray(p[0]))throw new Error("Native OPD grid is unavailable for heatmap plotting");const d=p.length,u=d>1?Array.from({length:d},(I,b)=>-1+2*b/(d-1)):[0],y=p.map(I=>(Array.isArray(I)?I:[]).map(b=>b==null||!Number.isFinite(Number(b))?NaN:Number(b))),m={x:u,y:u,z:y,type:"heatmap",zsmooth:"best",colorscale:qt.getBlueGreenRedColorscale(),colorbar:{title:"OPD [λ]"}},h=m.x.length>0?[Math.min(...m.x)-.1,Math.max(...m.x)+.1]:[-1.1,1.1],g=m.y.length>0?[Math.min(...m.y)-.1,Math.max(...m.y)+.1]:[-1.1,1.1],w={title:{text:`Optical Path Difference (OPD) Heatmap - ${n.displayName||"Field Point"}`,font:{size:16}},xaxis:{title:"Pupil coordinate X",range:h,dtick:.5,constrain:"domain"},yaxis:{title:"Pupil coordinate Y",scaleanchor:"x",scaleratio:1,range:g,dtick:.5,constrain:"domain"},width:600,height:600,margin:{l:60,r:60,b:60,t:60}},R=this.resolveContainer(),N=this.resolvePlotly(R);if(!R||!N)throw new Error("Plotly.jsライブラリが読み込まれていません");w.autosize=!0,delete w.width,delete w.height,a(96,"Rendering OPD heatmap..."),await N.newPlot(R,[m],w,this.plotlyConfig),a(100,"Completed");{const I=i?.statistics?.display?.opdWavelengths?i.statistics.display.opdWavelengths:i?.statistics?.raw?.opdWavelengths||i?.statistics?.opdWavelengths;this.displayStatistics(I,"OPD","λ")}return console.log("✅ OPD ヒートマップ生成完了"),i}catch(a){throw console.error("❌ OPD ヒートマップ生成エラー:",a),a}}async plotWavefrontHeatmap(t,n,o=.5876,r=31){try{console.log("🌊 波面収差（Wλ）ヒートマップ生成開始...");const{analyzer:s}=this._createWavefrontEngine(t,o),a=typeof globalThis<"u"&&globalThis.__WAVEFRONT_DIAG_DISCONTINUITIES===!0,c=await s.generateWavefrontMap(n,r,"circular",{recordRays:!1,progressEvery:512,diagnoseDiscontinuities:a,diagTopK:5,opdMode:"simple",zernikeMaxNoll:37,renderFromZernike:!0});if(this._updateSystemDataWithZernike(s,c,37),!c||!c.pupilCoordinates||c.pupilCoordinates.length===0)throw new Error("有効な光線データがありません。光学系設定を確認してください。");const i=this.convertToPlotlyHeatmapData(c,"wavefront",r,{rawMode:!1}),l=i.x.length>0?[Math.min(...i.x)-.1,Math.max(...i.x)+.1]:[-1.1,1.1],f=i.y.length>0?[Math.min(...i.y)-.1,Math.max(...i.y)+.1]:[-1.1,1.1],p={title:{text:`Wavefront Aberration (Wλ) Heatmap - ${n.displayName||"Field Point"}`,font:{size:16}},xaxis:{title:"Pupil coordinate X",range:l,dtick:.5,constrain:"domain"},yaxis:{title:"Pupil coordinate Y",scaleanchor:"x",scaleratio:1,range:f,dtick:.5,constrain:"domain"},width:600,height:600,margin:{l:60,r:60,b:60,t:60}},d=this.resolveContainer(),u=this.resolvePlotly(d);if(!d||!u)throw new Error("Plotly.jsライブラリが読み込まれていません");return p.autosize=!0,delete p.width,delete p.height,await u.newPlot(d,[i],p,this.plotlyConfig),this.displayStatistics(c.statistics.wavefront,"Wavefront","λ"),console.log("✅ 波面収差（Wλ）ヒートマップ生成完了"),c}catch(s){throw console.error("❌ 波面収差（Wλ）ヒートマップ生成エラー:",s),s}}async plotMultiFieldComparison(t,n,o=.5876,r=16){try{console.log("🌊 マルチフィールド波面収差比較プロット生成開始...");const s=[],{analyzer:a}=this._createWavefrontEngine(t,o);for(const f of n){const p=await a.generateWavefrontMap(f,r,"circular",{recordRays:!1,progressEvery:512,opdMode:"simple",zernikeMaxNoll:37,renderFromZernike:!0}),d=this.convertToPlotlySurfaceData(p,"wavefront");d.name=f.displayName||`Field ${f.id}`,d.opacity=.8,s.push(d)}const c={title:{text:"Multi-field Wavefront Aberration Comparison",font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X"},yaxis:{title:"Pupil coordinate Y"},zaxis:{title:"Wavefront aberration [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}},i=this.resolveContainer(),l=this.resolvePlotly(i);if(!i||!l)throw new Error("Plotly.jsライブラリが読み込まれていません");c.autosize=!0,await l.newPlot(i,s,c,this.plotlyConfig),console.log("✅ マルチフィールド比較プロット生成完了")}catch(s){throw console.error("❌ マルチフィールド比較プロット生成エラー:",s),s}}async plotOPDMultiFieldComparisonRust(t,n,o=.5876,r=16,s={}){try{console.log("🌊 マルチフィールド OPD 比較プロット生成開始... (Rust)");const a=[];for(const f of n){const p=await this._computeNativeOpdWavefrontMap(t,f,r,{opdDisplayMode:"pistonTiltRemoved",forceIdealParaxialZero:!!s?.forceIdealParaxialZero}),d=p?.display?.opdsInWavelengths?{...p,opds:p.display.opds,opdsInWavelengths:p.display.opdsInWavelengths,wavefrontAberrations:p.display.wavefrontAberrations}:p,u=this.convertToPlotlySurfaceData(d,"opd",{rawMode:!0,fillHoles:!0});u.name=f?.displayName||`Field ${f?.id??""}`,u.opacity=.8,u.showscale=!1,a.push(u)}const c={title:{text:"Multi-field OPD Comparison",font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X"},yaxis:{title:"Pupil coordinate Y"},zaxis:{title:"OPD [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}},i=this.resolveContainer(),l=this.resolvePlotly(i);if(!i||!l)throw new Error("Plotly.jsライブラリが読み込まれていません");c.autosize=!0,await l.newPlot(i,a,c,this.plotlyConfig),console.log("✅ マルチフィールド OPD 比較プロット生成完了 (Rust)")}catch(a){throw console.error("❌ マルチフィールド OPD 比較プロット生成エラー:",a),a}}_median(t){const n=Array.isArray(t)?t.filter(s=>Number.isFinite(s)).slice():[],o=n.length;if(!o)return NaN;n.sort((s,a)=>s-a);const r=Math.floor(o/2);return o%2?n[r]:(n[r-1]+n[r])/2}_filterOutliersMAD(t,{label:n="values",madK:o=null,absMax:r=null,minScale:s=null}={}){try{if(!Array.isArray(t)||t.length===0)return t;const a=typeof globalThis<"u"?globalThis:null;if(a&&a.__WAVEFRONT_DISABLE_OUTLIER_FILTER===!0)return t;const c=t.filter(R=>Number.isFinite(R));if(c.length<16)return t;const i=this._median(c);if(!Number.isFinite(i))return t;const l=c.map(R=>Math.abs(R-i)),f=this._median(l),p=Number.isFinite(f)?1.4826*f:NaN,d=Number.isFinite(o)?Number(o):a&&Number.isFinite(a.__WAVEFRONT_OUTLIER_MAD_K)?Number(a.__WAVEFRONT_OUTLIER_MAD_K):8,u=Number.isFinite(r)?Number(r):a&&Number.isFinite(a.__WAVEFRONT_OUTLIER_ABS_MAX)?Number(a.__WAVEFRONT_OUTLIER_ABS_MAX):60,y=Number.isFinite(s)?Number(s):a&&Number.isFinite(a.__WAVEFRONT_OUTLIER_MIN_SCALE)?Number(a.__WAVEFRONT_OUTLIER_MIN_SCALE):1e-6,m=Number.isFinite(p)?Math.max(y,p):y,h=Math.max(y,m*Math.max(1,d));let g=0;const w=t.slice();for(let R=0;R<w.length;R++){const N=w[R];Number.isFinite(N)&&(Math.abs(N-i)>h||Math.abs(N)>u)&&(w[R]=NaN,g++)}return g>0&&console.log(`🧹 [WavefrontPlot] outlier filter (${n}): removed=${g}/${t.length}, median=${i.toFixed(4)}, mad=${Number.isFinite(f)?f.toFixed(4):"NaN"}, cutoff=${h.toFixed(4)}`),w}catch{return t}}convertToPlotlySurfaceData(t,n="wavefront",o={}){const{pupilCoordinates:r,wavefrontAberrations:s,opdsInWavelengths:a}=t,c=n==="opd"?a:s,i=!!o?.rawMode,l=o?.fillHoles!==void 0?!!o.fillHoles:!i,f=i&&n==="opd"?{label:`${n}:${i?"raw":"interp"}:surface`,madK:1e9,absMax:1e9}:{label:`${n}:${i?"raw":"interp"}:surface`},p=this._filterOutliersMAD(c,f),d=this._tryBuildRegularGrid(t,p,null,{fillHoles:l});if(d)return{type:"surface",x:d.x,y:d.y,z:d.z,connectgaps:!1,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};const u=[],y=[];for(let _=0;_<p.length;_++)isFinite(p[_])&&!isNaN(p[_])&&(u.push(r[_]),y.push(p[_]));if(console.log(`📊 有効データ: ${y.length}/${p.length} 点 (${(y.length/p.length*100).toFixed(1)}%)`),y.length<4)return console.warn("⚠️ 有効データが少なすぎます。簡易データを生成します。"),this.createFallbackSurfaceData(n);const m=[...new Set(u.map(_=>_.x))].sort((_,M)=>_-M),h=[...new Set(u.map(_=>_.y))].sort((_,M)=>_-M);if(console.log(`📊 グリッドサイズ: X=${m.length}, Y=${h.length}`),i){const _=[];let M=0,G=0;for(let E=0;E<h.length;E++){const L=[];for(let ne=0;ne<m.length;ne++){const V=m[ne],K=h[E];if(Math.sqrt(V*V+K*K)>1+1e-9){L.push(null),G++;continue}const J=u.find(Q=>Math.abs(Q.x-V)<1e-10&&Math.abs(Q.y-K)<1e-10);if(J){const Q=u.indexOf(J);L.push(y[Q]),M++}else L.push(null),G++}_.push(L)}console.log(`📊 [RawMode] grid cells: valid=${M}, null=${G}, total=${M+G}`);const z={type:"surface",x:m,y:h,z:_,connectgaps:!1,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(z)}let g=m,w=h;if(m.length<20||h.length<20){console.log("🔧 低密度データを補間用グリッドに拡張中...");const _=Math.min(...m),M=Math.max(...m),G=Math.min(...h),z=Math.max(...h),E=Math.max(32,Math.max(m.length,h.length)*2);g=[],w=[];for(let L=0;L<E;L++)g.push(_+(M-_)*L/(E-1)),w.push(G+(z-G)*L/(E-1));console.log(`📊 補間グリッドサイズ: X=${g.length}, Y=${w.length}`)}const R=[];let N=0,I=0;for(let _=0;_<w.length;_++){const M=[];for(let G=0;G<g.length;G++){const z=g[G],E=w[_];if(Math.sqrt(z*z+E*E)>1+1e-9){M.push(null),I++;continue}const ne=u.find(V=>Math.abs(V.x-z)<1e-10&&Math.abs(V.y-E)<1e-10);if(ne){const V=u.indexOf(ne);M.push(y[V]),N++}else{const V=this.interpolateValue(z,E,u,y);V!==null?(M.push(V),N++):(M.push(null),I++)}}R.push(M)}console.log(`📊 グリッドセル: 有効=${N}, null=${I}, 合計=${N+I}`);let b=1/0,A=-1/0,v=0;for(let _=0;_<R.length;_++){const M=R[_];if(Array.isArray(M))for(let G=0;G<M.length;G++){const z=M[G];z===null||!Number.isFinite(z)||(v++,z<b&&(b=z),z>A&&(A=z))}}v>0&&console.log(`📊 Z値範囲: ${b.toFixed(3)} ~ ${A.toFixed(3)}`);const S={type:"surface",x:g,y:w,z:R,connectgaps:!1,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(S)}validatePlotlyData(t){if(!t)throw new Error("データがありません");if(!t.x||!t.y||!t.z)throw new Error("x, y, z データが不足しています");if(!Array.isArray(t.x)||!Array.isArray(t.y)||!Array.isArray(t.z))throw new Error("x, y, z データが配列ではありません");if(t.x.length===0||t.y.length===0||t.z.length===0)throw new Error("データが空です");if(!Array.isArray(t.z[0]))throw new Error("z データが2次元配列ではありません");if(t.z.length!==t.y.length)throw new Error(`y座標数(${t.y.length})とz行数(${t.z.length})が一致しません`);if(t.z[0].length!==t.x.length)throw new Error(`x座標数(${t.x.length})とz列数(${t.z[0].length})が一致しません`);let n=0,o=0;for(let r=0;r<t.z.length;r++){const s=t.z[r];if(Array.isArray(s)){o+=s.length;for(let a=0;a<s.length;a++){const c=s[a];c!==null&&Number.isFinite(c)&&n++}}}if(n===0)throw new Error("有効なZ値がありません");console.log(`✅ データ検証完了: ${n}/${o} 有効値`)}_transposeZForPlotly(t){try{if(!t||!Array.isArray(t.z)||t.z.length===0||!Array.isArray(t.z[0]))return t;const n=t.z,o=n.length,r=Math.max(0,...n.map(a=>Array.isArray(a)?a.length:0));if(o===0||r===0)return t;const s=Array.from({length:r},(a,c)=>Array.from({length:o},(i,l)=>{const f=n[l];return Array.isArray(f)&&c<f.length?f[c]:null}));if(Array.isArray(t.x)&&Array.isArray(t.y)&&t.x.length!==t.y.length){const a=t.x;t.x=t.y,t.y=a}return t.z=s,t}catch{return t}}createFallbackSurfaceData(t){return{type:"surface",x:[-1,0,1],y:[-1,0,1],z:[[0,0,0],[0,0,0],[0,0,0]],colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:t==="opd"?"OPD [λ]":"Wλ [波長]"}}}convertToPlotlyHeatmapData(t,n="opd",o=31,r={}){const{pupilCoordinates:s,wavefrontAberrations:a,opdsInWavelengths:c,rayData:i}=t,l=n==="opd"?c:a,f=!!r?.rawMode,p=f&&n==="opd"?{label:`${n}:${f?"raw":"interp"}:heatmap`,madK:1e9,absMax:1e9}:{label:`${n}:${f?"raw":"interp"}:heatmap`},d=this._filterOutliersMAD(l,p),u=this._tryBuildRegularGrid(t,d,o,{fillHoles:!f});if(u){let v=0,S=0;try{const z=u.z;if(Array.isArray(z)){for(const E of z)if(Array.isArray(E))for(const L of E)S++,L!==null&&Number.isFinite(L)&&v++}}catch{}const _=S>0?v/S:0,M=!f&&_>=.85,G={type:"heatmap",x:u.x,y:u.y,z:u.z,zsmooth:f?!1:M?"best":!1,connectgaps:f?!1:M,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(G)}const y=[],m=[];for(let v=0;v<d.length;v++){const S=d[v],_=s[v];isFinite(S)&&!isNaN(S)&&_&&(y.push(_),m.push(S))}console.log(`📊 ヒートマップ有効データ: ${m.length}/${d.length} 点 (${(m.length/d.length*100).toFixed(1)}%)`);const h=[...new Set(y.map(v=>v.x))].sort((v,S)=>v-S),g=[...new Set(y.map(v=>v.y))].sort((v,S)=>v-S);if(console.log(`📊 ヒートマップグリッドサイズ: X=${h.length}, Y=${g.length}`),f){const v=[];for(let _=0;_<g.length;_++){const M=[];for(let G=0;G<h.length;G++){const z=y.find(E=>Math.abs(E.x-h[G])<1e-10&&Math.abs(E.y-g[_])<1e-10);if(z){const E=y.indexOf(z);M.push(m[E])}else M.push(null)}v.push(M)}const S={type:"heatmap",x:h,y:g,z:v,zsmooth:!1,connectgaps:!1,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(S)}const w=[];let R=0,N=0;for(let v=0;v<g.length;v++){const S=[];for(let _=0;_<h.length;_++){const M=y.find(G=>Math.abs(G.x-h[_])<1e-10&&Math.abs(G.y-g[v])<1e-10);if(M){const G=y.indexOf(M);S.push(m[G]),R++}else S.push(null),N++}w.push(S)}console.log(`📊 ヒートマップグリッドセル: 有効=${R}, null=${N}, 合計=${R+N}`);const b=(R+N>0?R/(R+N):0)>=.85,A={type:"heatmap",x:h,y:g,z:w,zsmooth:b?"best":!1,connectgaps:b,colorscale:qt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [λ]"}};return this._transposeZForPlotly(A)}displayStatistics(t,n,o){const r=this.resolveContainer(),s=this.resolveStatsContainer(r);if(!s)return;const a=t?.pupilSamplingMode,c=t?.opdMode,i=t?.skipZernikeFit,l=a==="entrance"?'<div class="stats-note"><strong>Pupil sampling:</strong> entrance (best effort / vignetting possible)</div>':a==="stop"?'<div class="stats-note"><strong>Pupil sampling:</strong> stop (requires reaching stop surface)</div>':"",f=c?`<div class="stats-note"><strong>OPD mode:</strong> ${String(c)}</div>`:"",p=t?.opdDisplayMode,d=p?`<div class="stats-note"><strong>OPD display mode:</strong> ${String(p)}</div>`:"",u=t?.backend,y=u?`<div class="stats-note"><strong>Backend:</strong> ${String(u)}</div>`:"",m=t?.chiefReferenceMode,h=m?`<div class="stats-note"><strong>Chief reference mode:</strong> ${String(m)}</div>`:"",g=typeof i=="boolean"?`<div class="stats-note"><strong>Zernike fit:</strong> ${i?"OFF (raw)":"ON"}</div>`:"",w=!i&&Number.isFinite(t?.rawMean)?`<div class="stats-note"><strong>Raw mean (piston):</strong> ${Number(t.rawMean).toFixed(4)} ${o}</div>`:"",R=Array.isArray(t?.removeIndices)&&t.removeIndices.length?`<div class="stats-note"><strong>Stats removal (OSA):</strong> [${t.removeIndices.join(", ")}] (piston/tilt/defocus)</div>`:"",N=Number.isFinite(t?.wavelengthUm)?`<div class="stats-note"><strong>Wavelength:</strong> ${(t.wavelengthUm*1e3).toFixed(2)} nm (${t.wavelengthUm.toFixed(6)} μm)</div>`:"",I=Number.isFinite(t?.gridSize)?`<div class="stats-note"><strong>Grid size:</strong> ${Math.floor(t.gridSize)} × ${Math.floor(t.gridSize)}</div>`:"",b=(()=>{const E=Number.isInteger(t?.stopSurface)?Number(t.stopSurface):null,L=Number.isInteger(t?.targetSurface)?Number(t.targetSurface):null;if(E!==null||L!==null){const ne=[];return E!==null&&ne.push(`stop=${E}`),L!==null&&ne.push(`target=${L}`),`<div class="stats-note"><strong>Surfaces:</strong> ${ne.join(", ")}</div>`}return""})(),A=(()=>{const E=t?.usedObjectPosition,L=Number.isFinite(t?.usedObjectX)?Number(t.usedObjectX):null,ne=Number.isFinite(t?.usedObjectY)?Number(t.usedObjectY):null;if(E||L!==null||ne!==null){let V="";return E&&(V+=String(E)),L!==null&&(V+=` x=${L.toFixed(2)}`),ne!==null&&(V+=` y=${ne.toFixed(2)}`),`<div class="stats-note"><strong>Object:</strong> ${V.trim()}</div>`}return""})(),v=(()=>{const E=Number(t?.hitCount),L=Number(t?.sampleCount);if(Number.isFinite(E)&&Number.isFinite(L)&&L>0){const ne=(E/L*100).toFixed(1);return`<div class="stats-note"><strong>Ray hits:</strong> ${E}/${L} (${ne}%)</div>`}return""})(),S=(E,L=4)=>{const ne=Number(E);return Number.isFinite(ne)?ne.toFixed(L):"n/a"},_=Number(t?.mean),M=Math.abs(_),G=o==="λ"&&Number.isFinite(M)&&M>10?`<div class="stats-warning" style="color: #ff6b6b; margin-top: 8px;">
                ⚠️ <strong>Large piston (mean)</strong>: mean=${S(_,2)} ${o}<br>
                → Enable piston removal in the statistics to make the mean closer to 0.
               </div>`:"",z=`
            <div class="wavefront-statistics">
                <h4>${n} Statistics</h4>
                ${l}
                ${f}
                ${d}
                ${y}
                ${h}
                ${g}
                ${w}
                ${R}
                ${N}
                ${I}
                ${b}
                ${A}
                ${v}
                <div class="stats-grid">
                    <div><strong>Data points:</strong> ${Number.isFinite(Number(t?.count))?Number(t.count):"n/a"}</div>
                    <div><strong>Mean:</strong> ${S(t?.mean,4)} ${o}</div>
                    <div><strong>RMS:</strong> ${S(t?.rms,4)} ${o}</div>
                    <div><strong>Peak-to-Peak:</strong> ${S(t?.peakToPeak,4)} ${o}</div>
                    <div><strong>Min:</strong> ${S(t?.min,4)} ${o}</div>
                    <div><strong>Max:</strong> ${S(t?.max,4)} ${o}</div>
                </div>
                ${G}
            </div>
        `;s.innerHTML=z}interpolateValue(t,n,o,r){const s=typeof globalThis<"u"?globalThis:null,a=s&&Number.isFinite(s.__WAVEFRONT_INTERP_MAX_DIST)?Math.max(.01,Number(s.__WAVEFRONT_INTERP_MAX_DIST)):.12,c=s&&Number.isFinite(s.__WAVEFRONT_INTERP_MIN_NEIGHBORS)?Math.max(2,Math.floor(Number(s.__WAVEFRONT_INTERP_MIN_NEIGHBORS))):6,i=s&&Number.isFinite(s.__WAVEFRONT_INTERP_MAX_NEIGHBORS)?Math.max(c,Math.floor(Number(s.__WAVEFRONT_INTERP_MAX_NEIGHBORS))):12,l=[];for(let u=0;u<o.length;u++){const y=o[u],m=r[u];if(!y||!Number.isFinite(y.x)||!Number.isFinite(y.y)||!Number.isFinite(m))continue;const h=Math.hypot(y.x-t,y.y-n);l.push({index:u,value:m,distance:h})}if(l.length===0)return null;if(l.sort((u,y)=>u.distance-y.distance),l[0].distance<.01)return l[0].value;const f=[];for(const u of l)if(u.distance>a||(f.push(u),f.length>=i))break;if(f.length<c)return null;let p=0,d=0;for(const u of f){const y=1/(u.distance*u.distance+1e-6);p+=u.value*y,d+=y}return d>0?p/d:null}_tryBuildRegularGrid(t,n,o=null,r={}){try{const s=Number(t?.pupilRange),a=Number.isFinite(o)?Math.floor(o):Math.floor(Number(t?.gridSize));if(!Number.isFinite(s)||s<=0||!Number.isFinite(a)||a<2)return null;const c=[],i=[];for(let m=0;m<a;m++)c.push(m/(a-1)*2*s-s),i.push(m/(a-1)*2*s-s);const l=Array.from({length:a},()=>Array.from({length:a},()=>0)),f=Array.from({length:a},()=>Array.from({length:a},()=>0)),p=t?.pupilCoordinates||[],d=Math.min(p.length,n?.length||0);if(d===0)return null;const u=(a-1)/(2*s);for(let m=0;m<d;m++){const h=p[m],g=n[m];if(!h||!isFinite(h.x)||!isFinite(h.y)||!isFinite(g)||isNaN(g))continue;let w=Number.isInteger(h.ix)?h.ix:Math.round((h.x+s)*u),R=Number.isInteger(h.iy)?h.iy:Math.round((h.y+s)*u);w<0||w>=a||R<0||R>=a||Math.hypot(h.x,h.y)>s+1e-9||(l[R][w]+=g,f[R][w]+=1)}const y=Array.from({length:a},()=>Array.from({length:a},()=>null));for(let m=0;m<a;m++)for(let h=0;h<a;h++){const g=f[m][h];g>0&&(y[m][h]=l[m][h]/g)}if(r?.fillHoles){const m=s*.9;let h=0,g=0,w=0;for(let I=0;I<a;I++){const b=i[I];for(let A=0;A<a;A++){const v=c[A];Math.hypot(v,b)>m+1e-12||(g++,y[I][A]===null?h++:w++)}}const R=g>0?w/g:0,N=h>0&&R>=.6;if(!N&&h>0&&console.log(`🩹 [WavefrontPlot] surface hole-fill skipped (sparse): coreValidFrac=${R.toFixed(3)}, coreValid=${w}, coreNulls=${h}`),N){const b=(S=>{const _=S.map(E=>E.slice());let M=0,G=0;const z=2;for(let E=0;E<a;E++){const L=i[E];for(let ne=0;ne<a;ne++){if(_[E][ne]!==null)continue;const V=c[ne];if(Math.hypot(V,L)>m+1e-12)continue;let K=0,$=0,J=0;for(let Q=1;Q<=z;Q++){for(let le=-Q;le<=Q;le++)for(let Z=-Q;Z<=Q;Z++){if(Z===0&&le===0)continue;const B=ne+Z,C=E+le;if(B<0||B>=a||C<0||C>=a)continue;const F=S[C][B];if(F===null||!isFinite(F))continue;const T=1/(Math.hypot(Z,le)+1e-6);K+=F*T,$+=T,J++}if(J>=6)break}J>=6&&$>0?(_[E][ne]=K/$,M++):G++}}return{out:_,filled:M,remaining:G}})(y),A=b.filled||0,v=b.remaining;A>0&&console.log(`🩹 [WavefrontPlot] surface hole-fill: coreNulls=${h}, filled=${A}, remaining=${v}`);for(let S=0;S<a;S++)for(let _=0;_<a;_++)y[S][_]=b.out[S][_]}}return{x:c,y:i,z:y}}catch{return null}}}function Ul(e=[]){if(!Array.isArray(e)||e.length===0)return!1;const t=r=>String(r??"").trim().toLowerCase(),n=r=>t(r).replace(/[\s_-]+/g,"");let o=!1;for(const r of e){if(!r||typeof r!="object")continue;const s=n(r?.["object type"]??r?.objectType??r?.object??""),a=n(r?.surfType??r?.["surf type"]??r?.type??r?.surfaceType??""),c=n(r?._blockType??r?.blockType??""),i=n(r?.kind??"");if(c==="paraxial"||c==="thinlens"||a==="thinlens"||Number.isFinite(Number(r?._thinLensFocalLengthX))||Number.isFinite(Number(r?._thinLensFocalLengthY))){o=!0;continue}if(!(s==="object"||s==="image"||s==="stop"||a==="stop"||a==="gap"||a==="airgap"||c==="gap"||c==="coordbreak"||i==="gap"))return!1}return o}async function Hl(e="surface",t="wavefront",n=64,o=0,r={}){try{const s=r?.cancelToken||null,a=r?.onProgress||null,c=()=>{try{if(typeof localStorage>"u")return"";const v=Ot(),S=v?.activeConfigId,_=Array.isArray(v?.configurations)?v.configurations.find(M=>String(M?.id)===String(S)):null;return _?`id=${_.id} name=${_.name||""}`.trim():S!=null?`id=${S}`:""}catch{return""}},i=window.getOpticalSystemRows?window.getOpticalSystemRows():null,l=window.getObjectRows?window.getObjectRows():[];try{const v=i?.[4],S=i?.[5],_=i?.[6],M=c()}catch{}if(!i||i.length===0)throw console.error("❌ 光学系データがありません"),new Error("光学系データがありません。JSONファイルをロードしてください。");if(!l||l.length===0){console.warn("⚠️ Objectデータがありません、軸上設定を使用");const v={id:1,xHeightAngle:0,yHeightAngle:0,position:"Angle"};l.push(v),o=0}(o<0||o>=l.length)&&(console.warn(`⚠️ 無効なObjectインデックス: ${o}, デフォルト(0)を使用`),o=0);const f=l[o],p=(()=>{try{if(typeof window<"u"&&typeof window.getPrimaryWavelength=="function"){const v=Number(window.getPrimaryWavelength());if(Number.isFinite(v)&&v>0)return v}}catch{}throw new Error("Primary wavelength is unavailable. Please set Source Primary Wavelength.")})(),d=(()=>{const S=i?.[0]?.thickness;return S==="INF"||S==="Infinity"||S===1/0})(),u=v=>{const S=typeof v=="number"?v:parseFloat(v);return Number.isFinite(S)?S:0},y=(v,S)=>{const _=String(v?.position??v?.Position??v?.type??"").toLowerCase(),M=u(v?.xHeightAngle),G=u(v?.yHeightAngle),z=_==="angle"||_==="field angle"||_==="angles"||_==="point",E=_==="rectangle"||_==="height";let L={x:0,y:0},ne=0,V=0,K=v?.position??v?.type??"";z?(L={x:M,y:G},ne=0,V=0,K="Angle"):E?(L={x:0,y:0},ne=M,V=G,K="Rectangle"):d?(L={x:M,y:G},ne=0,V=0,K="Angle"):(L={x:0,y:0},ne=M,V=G,K="Rectangle");const $=K==="Angle"?`(${L.x||0}°, ${L.y||0}°)`:`(${ne||0}mm, ${V||0}mm)`;return{id:v?.id||S+1,displayName:`Object ${S+1} ${$}`,type:K,fieldAngle:L,xHeight:ne,yHeight:V,objectIndex:S,wavelength:p}},m=y(f,o),h=l.map((v,S)=>y(v,S)),g=new qt(r?.containerElement||"wavefront-container"),w=r?.opdDisplayMode||"pistonTiltRemoved",R=Ul(i),N=w,I=!1;try{R&&w==="pistonTiltDefocusRemoved"&&console.log("ℹ️ [OPD] Ideal Paraxial/ThinLens system detected; defocus removal remains under explicit user control.")}catch{}const b=v=>{try{bc(v,{plotType:e,dataType:t,gridSize:n,selectedObjectIndex:o,wavelength:p,fieldSetting:m},window);try{if(typeof localStorage<"u"){const _=v?.statistics||null,M={at:new Date().toISOString(),from:"eva-wavefront-plot.js:storeLast",wavefront:{meta:Ir(window),hasError:!!v?.error,error:v?.error?{message:String(v.error?.message||v.error||"Wavefront error").slice(0,600),code:v.error?.code||null}:null,statistics:_?{opdMicrons:_.opdMicrons?{rms:Number.isFinite(Number(_.opdMicrons.rms))?Number(_.opdMicrons.rms):null,peakToPeak:Number.isFinite(Number(_.opdMicrons.peakToPeak))?Number(_.opdMicrons.peakToPeak):null}:null,rawOpdMicrons:_.raw?.opdMicrons?{rms:Number.isFinite(Number(_.raw.opdMicrons.rms))?Number(_.raw.opdMicrons.rms):null,peakToPeak:Number.isFinite(Number(_.raw.opdMicrons.peakToPeak))?Number(_.raw.opdMicrons.peakToPeak):null}:null}:null}};try{const z=(window.lastWavefrontAnalyzer||null)?.opdCalculator||null,E=typeof z?.getLastRayCalculation=="function"?z.getLastRayCalculation():z?.lastRayCalculation??null;E&&(M.opdLastRay={success:E.success??null,error:E.error??null,fieldKey:E.fieldKey??null,pupilCoord:E.pupilCoord??null,stopHit:E.stopHit??null})}catch{}zl(M)}}catch{}try{window.dispatchEvent(new CustomEvent("coopt:lastWavefrontMapUpdated",{detail:Ir(window)}))}catch{}}catch{}};let A=null;switch(e){case"surface":if(t==="opd"){const v=await g.plotOPDSurface(i,m,p,n,{cancelToken:s,onProgress:a,opdDisplayMode:N,wasmFastOnly:r?.wasmFastOnly,enableHeavyDiagnostics:r?.enableHeavyDiagnostics,forceIdealParaxialZero:I});b(v),A=v}else{const v=await g.plotWavefrontAberrationSurface(i,m,p,n);b(v),A=v}break;case"heatmap":if(t==="opd"){const v=await g.plotOPDHeatmap(i,m,p,n,{cancelToken:s,onProgress:a,opdDisplayMode:N,wasmFastOnly:r?.wasmFastOnly,enableHeavyDiagnostics:r?.enableHeavyDiagnostics,forceIdealParaxialZero:I});b(v),A=v}else{const v=await g.plotWavefrontHeatmap(i,m,p,n);b(v),A=v}break;case"multifield":t==="opd"?await g.plotOPDMultiFieldComparisonRust(i,h,p,n,{forceIdealParaxialZero:I}):await g.plotMultiFieldComparison(i,h,p,n),A={success:!0};break;default:throw new Error(`未対応のプロットタイプ: ${e}`)}if(A?.error)throw new Error(String(A?.error?.message||A?.error||"Wavefront plot failed"));return console.log("✅ 波面収差図表示完了"),A||{success:!0}}catch(s){if(console.error("❌ 波面収差図表示エラー:",s),r?.showAlert!==!1&&alert(`波面収差図エラー: ${s?.message||s}`),r?.throwOnError===!0)throw s;return{error:{message:String(s?.message||s||"Wavefront plot failed")}}}}class na{constructor(t=4){this.workerCount=t,t<1&&(this.workerCount=1),t>16&&(this.workerCount=16)}workers=[];workerBusy=new Map;taskMap=new Map;taskQueue=[];workerTaskMap=new Map;async initialize(){const t=`
// Simple 1D FFT for MTF extraction
function fft1D(real, imag) {
    const n = real.length;
    if (n <= 1) return { real: real.slice(), imag: imag.slice() };
    
    // Bit reversal
    let j = 0;
    for (let i = 0; i < n - 1; i++) {
        if (i < j) {
            [real[i], real[j]] = [real[j], real[i]];
            [imag[i], imag[j]] = [imag[j], imag[i]];
        }
        let m = n >> 1;
        while (m > 0 && j >= m) {
            j -= m;
            m >>= 1;
        }
        j += m;
    }
    
    // Cooley-Tukey FFT
    for (let len = 2; len <= n; len <<= 1) {
        const angle = -2 * Math.PI / len;
        for (let i = 0; i < n; i += len) {
            for (let k = 0; k < len / 2; k++) {
                const w = Math.cos(angle * k);
                const v = Math.sin(angle * k);
                const t0 = i + k;
                const t1 = i + k + len / 2;
                const c0 = real[t1] * w - imag[t1] * v;
                const c1 = real[t1] * v + imag[t1] * w;
                real[t1] = real[t0] - c0;
                imag[t1] = imag[t0] - c1;
                real[t0] += c0;
                imag[t0] += c1;
            }
        }
    }
    
    return { real, imag };
}

// Extract 1D MTF from 2D PSF
function extractMTFFrom2DPSF(psfGrid, rows, cols, targetFreqLpmm, wavelengthMicrons, pupilDiameterMm) {
    // Extract 1D slices (center rows/cols)
    const centerRow = Math.floor(rows / 2);
    const centerCol = Math.floor(cols / 2);
    
    // Tangential slice (along x-axis, center row)
    const tangentialReal = [];
    const tangentialImag = [];
    for (let col = 0; col < cols; col++) {
        tangentialReal.push(psfGrid[centerRow * cols + col]);
        tangentialImag.push(0);
    }
    
    // Sagittal slice (along y-axis, center col)
    const sagittalReal = [];
    const sagittalImag = [];
    for (let row = 0; row < rows; row++) {
        sagittalReal.push(psfGrid[row * cols + centerCol]);
        sagittalImag.push(0);
    }
    
    // Compute FFT for both
    const tangFFT = fft1D(tangentialReal, tangentialImag);
    const sagFFT = fft1D(sagittalReal, sagittalImag);
    
    // Compute magnitude (OTF) and normalize
    const tangMTF = tangFFT.real.map((r, i) => {
        const mag = Math.sqrt(r * r + tangFFT.imag[i] * tangFFT.imag[i]);
        return mag / (tangFFT.real[0] || 1);
    });
    
    const sagMTF = sagFFT.real.map((r, i) => {
        const mag = Math.sqrt(r * r + sagFFT.imag[i] * sagFFT.imag[i]);
        return mag / (sagFFT.real[0] || 1);
    });
    
    // Compute spatial frequency array (assuming sampling = pupilDiameterMm)
    const pixelSize = pupilDiameterMm / cols;
    const freqLpmm = [];
    for (let i = 0; i < cols; i++) {
        const freq = (i / (cols * pixelSize)) * 1000;  // Convert to lp/mm
        freqLpmm.push(freq);
    }
    
    // Find closest frequency to target
    let bestIdxTang = 0, bestIdxSag = 0;
    let minDiffTang = Infinity, minDiffSag = Infinity;
    
    for (let i = 0; i < freqLpmm.length; i++) {
        const diff = Math.abs(freqLpmm[i] - targetFreqLpmm);
        if (diff < minDiffTang) {
            minDiffTang = diff;
            bestIdxTang = i;
        }
        if (diff < minDiffSag) {
            minDiffSag = diff;
            bestIdxSag = i;
        }
    }
    
    return {
        mtfValue: (tangMTF[bestIdxTang] + sagMTF[bestIdxSag]) / 2,
        mtfTangential: tangMTF[bestIdxTang],
        mtfSagittal: sagMTF[bestIdxSag]
    };
}

self.onmessage = async (event) => {
    const {
        taskId,
        defocusShiftMm,
        psfGrid,
        psfRows,
        psfCols,
        wavelengthMicrons,
        targetFrequencyLpmm,
        pupilDiameterMm
    } = event.data;
    
    try {
        const startTime = performance.now();
        
        // Extract MTF from pre-computed PSF grid
        const result = extractMTFFrom2DPSF(
            psfGrid,
            psfRows,
            psfCols,
            targetFrequencyLpmm,
            wavelengthMicrons,
            pupilDiameterMm || 10
        );
        
        const timeMs = performance.now() - startTime;
        
        self.postMessage({
            taskId,
            defocusShiftMm,
            mtfValue: result.mtfValue,
            mtfTangential: result.mtfTangential,
            mtfSagittal: result.mtfSagittal,
            timeMs
        });
    } catch (error) {
        const timeMs = performance.now() - performance.now();
        self.postMessage({
            taskId,
            defocusShiftMm,
            mtfValue: 0,
            mtfTangential: 0,
            mtfSagittal: 0,
            timeMs,
            error: String(error instanceof Error ? error.message : error)
        });
    }
};
`,n=new Blob([t],{type:"application/javascript"}),o=URL.createObjectURL(n);for(let r=0;r<this.workerCount;r++){const s=new Worker(o);this.workers.push(s),this.workerBusy.set(r,!1),s.onmessage=a=>this._handleWorkerResult(a.data,r),s.onerror=a=>{console.error(`⚠️ [TFMTF] Worker ${r} error:`,a),this.workerBusy.set(r,!1),this._processQueue()}}console.log(`✅ [TFMTF] Worker pool initialized with ${this.workerCount} workers`)}async queueTask(t){return new Promise((n,o)=>{this.taskMap.set(t.taskId,{resolve:n,reject:o}),this.taskQueue.push(t),this._processQueue()})}_processQueue(){for(;this.taskQueue.length>0;){let t=-1;for(let o=0;o<this.workerCount;o++)if(!this.workerBusy.get(o)){t=o;break}if(t===-1)break;const n=this.taskQueue.shift();if(!n)break;this.workerBusy.set(t,!0),this.workerTaskMap.set(t,n.taskId);try{this.workers[t].postMessage(n,[n.psfGrid.buffer])}catch{this.workers[t].postMessage(n)}}}_handleWorkerResult(t,n){const o=this.taskMap.get(t.taskId);o&&(t.error?(console.warn(`⚠️ [TFMTF] Task ${t.taskId} error:`,t.error),o.reject(new Error(t.error))):o.resolve(t),this.taskMap.delete(t.taskId)),this.workerBusy.set(n,!1),this.workerTaskMap.delete(n),this._processQueue()}async calculateTFMTF(t,n,o,r={}){const{defocusRange:s=[-.1,.1],defocusSteps:a=21,targetFrequencyLpmm:c=10,wavelengthMicrons:i=.555,pupilDiameterMm:l=10,onProgress:f}=r,[p,d]=s,u=Array.from({length:a},(g,w)=>p+(d-p)*(a>1?w/(a-1):0)),y=new Map,m=[];console.log(`🔄 [TFMTF] Queueing ${a} MTF extractions across ${this.workerCount} workers`);for(let g=0;g<u.length;g++){const w=u[g],R=`tfmtf_${g}`,N=this.queueTask({taskId:R,defocusShiftMm:w,psfGrid:t.slice(),psfRows:n,psfCols:o,wavelengthMicrons:i,targetFrequencyLpmm:c,pupilDiameterMm:l});m.push({taskId:R,promise:N,index:g})}for(const{taskId:g,promise:w,index:R}of m)try{const N=await w;y.set(g,N);const I=Math.ceil((R+1)/m.length*100);f&&f({percent:I,message:`TFMTF ${I}% (${R+1}/${m.length})`})}catch(N){console.error(`❌ [TFMTF] Task ${g} failed:`,N),y.set(g,{taskId:g,defocusShiftMm:u[R],mtfValue:0,mtfTangential:0,mtfSagittal:0,timeMs:0,error:String(N instanceof Error?N.message:N)})}const h=u.map((g,w)=>{const R=`tfmtf_${w}`,N=y.get(R);return{defocusMm:g,mtfValue:N?.mtfValue??0,mtfTangential:N?.mtfTangential??0,mtfSagittal:N?.mtfSagittal??0}});return console.log(`✅ [TFMTF] Calculation complete (${h.length} points)`),h}terminate(){this.workers.forEach(t=>t.terminate()),this.workers=[],this.workerBusy.clear(),this.taskMap.clear(),this.workerTaskMap.clear(),this.taskQueue=[],console.log("✅ [TFMTF] Worker pool terminated")}}let _o=null;async function oa(e=4){return _o||(_o=new na(e),await _o.initialize()),_o}const Gl=Object.freeze(Object.defineProperty({__proto__:null,TFMTFWorkerPool:na,getGlobalTFMTFWorkerPool:oa},Symbol.toStringTag,{value:"Module"}));let xr=null;async function ql(){return xr||(xr=(async()=>{const{PSFCalculator:e}=await De(async()=>{const{PSFCalculator:t}=await import("./psf-calculator-Bc6xBvfp.js");return{PSFCalculator:t}},__vite__mapDeps([3,0,1]));return new e})()),xr}function Vl(e=[]){if(!Array.isArray(e)||e.length===0)return!1;let t=!1;for(const n of e){if(!n||typeof n!="object")continue;const o=String(n?.["object type"]??n?.object??n?.Object??"").trim().toLowerCase(),r=String(n?.surfType??n?.type??n?.surfaceType??"").trim().toLowerCase(),s=String(n?._blockType??n?.blockType??"").trim().toLowerCase();if(s==="paraxial"||s==="thinlens"||r==="thinlens"||Number.isFinite(Number(n?._thinLensFocalLengthX))||Number.isFinite(Number(n?._thinLensFocalLengthY))){t=!0;continue}if(!(o===""||o==="object"||o==="image"||o==="stop"||r==="gap"||r==="air gap"||s==="gap"||s==="air gap"||r==="coordinate break"||r==="coordbrk"||s==="coordinate break"||s==="coordbrk"))return!1}return t}function Yl(e,t,n=!1){const o=Number(t);if(!Array.isArray(e))return[];const r=e.map(p=>p&&typeof p=="object"?{...p}:p);if(!Number.isFinite(o)||Math.abs(o)<1e-15)return r;const s=r.findIndex(p=>p&&(p["object type"]==="Image"||p.object==="Image")),a=s>0?s-1:Math.max(0,r.length-2);if(a<0||a>=r.length)return r;const c=r[a]&&typeof r[a]=="object"?{...r[a]}:{},i=Number(c.thickness),l=Number.isFinite(i)?i:0,f=l+o;return Be(`🔍 [TFMTF Defocus] Conjugate: ${n?"FINITE":"INFINITE"}, Shift: ${o.toFixed(4)} mm, Target surface ${a}: ${l.toFixed(4)} → ${f.toFixed(4)} mm`),c.thickness=f,r[a]=c,r}function Be(...e){try{console.log(...e)}catch{}try{typeof window<"u"&&window.opener&&window.opener.console&&window.opener.console.log(...e)}catch{}}function Oo(...e){try{console.error(...e)}catch{}try{typeof window<"u"&&window.opener&&window.opener.console&&window.opener.console.error(...e)}catch{}}async function fn({wavelengthMicrons:e,objectIndex:t,objectOverride:n,maxFrequencyLpmm:o,samplingSize:r,samplingPoints:s,containerElement:a,onProgress:c,opdDisplayMode:i,defocusShiftMm:l,skipPlot:f,showDiffractionLimit:p,zeroPadTo:d,legacyBaselineMode:u,plotPointCount:y}={}){const m=(D,U)=>{const ee=Number(D);return Number.isFinite(ee)?ee:U},h=D=>D<.45?"#8B00FF":D<.495?"#0000FF":D<.57?"#00FF00":D<.59?"#9ACD32":D<.62?"#FF8800":"#FF0000",g=(D,U)=>{try{if(typeof c!="function")return;c({percent:D,message:U})}catch{}},w=!!(typeof globalThis<"u"&&globalThis.__COOPT_MTF_WASM_FAST_ONLY),R=!(typeof globalThis<"u"&&globalThis.__COOPT_MTF_PROFILE===!1),N=typeof u=="boolean"?u:!(typeof globalThis<"u"&&globalThis.__COOPT_MTF_LEGACY_BASELINE===!1),I=(D,U,ee)=>Math.max(U,Math.min(ee,D)),b=typeof globalThis<"u"?Number(globalThis.__COOPT_MTF_PLOT_POINT_COUNT):NaN,A=Math.floor(m(y,b)),v=I(Number.isFinite(A)?A:121,5,2001),S=typeof window<"u"&&typeof window.getPrimaryWavelength=="function"?Number(window.getPrimaryWavelength()):NaN,_=typeof i=="string"&&i?i:"pistonTiltRemoved",M=typeof e=="string"?String(e).toLowerCase()==="all":!1;if((M||!Number.isFinite(Number(e)))&&!(Number.isFinite(S)&&S>0))throw new Error("Primary wavelength is unavailable. Please set Source Primary Wavelength.");const G=M?S:m(e,S),z=Number.isFinite(Number(t))?Math.max(0,Math.floor(Number(t))):0,E=Math.max(0,m(o,100)),L=D=>Number.isInteger(D)&&D>0&&(D&D-1)===0,ne=D=>{let U=1;const ee=Math.max(1,Math.floor(Number(D)||1));for(;U<ee&&U<4096;)U<<=1;return U},V=Math.floor(m(r,NaN)),K=Math.floor(m(s,NaN)),$=Number.isFinite(V)?V:K,J=L($)?I($,32,4096):256,Q=Math.floor(m(d,NaN)),le=Number.isFinite(Q)&&Q>=J&&L(Q),Z=le?I(Q,32,4096):0,B=typeof p=="boolean"?p:!0,C=!f;let x=(()=>{if(a)return a;const D=U=>{try{return document.getElementById(U)}catch{return null}};return D("mtf-container")||D("popup-mtf-container")||D("popup-through-focus-mtf-container")||null})();if(C&&!x)try{if(!document?.body)throw new Error("document.body is unavailable");const D="mtf-container-auto",U=document.getElementById(D);x=U||document.createElement("div"),U||(x.id=D,x.style.position="fixed",x.style.right="16px",x.style.bottom="16px",x.style.width="760px",x.style.height="520px",x.style.background="#ffffff",x.style.border="1px solid #d0d0d0",x.style.boxShadow="0 4px 16px rgba(0,0,0,0.15)",x.style.zIndex="99999",x.style.borderRadius="6px",document.body.appendChild(x)),Be("⚠️ MTF container not found. Auto-created #mtf-container-auto for plotting.")}catch{throw new Error("MTF container element not found")}if(C)try{x.innerHTML=""}catch{}g(0,"Starting..."),g(2,"Checking WASM readiness...");const T=typeof globalThis<"u"?globalThis:null;await wc();const j=Si(),P=!!(T&&T.__COOPT_MTF_WASM_STRICT===!0);Qn(P);const O=async(D,U)=>{if(!T)return await U();const ee=T.__COOPT_FORCE_INFINITE_PUPIL_MODE;try{if(D==null||D==="")try{delete T.__COOPT_FORCE_INFINITE_PUPIL_MODE}catch{T.__COOPT_FORCE_INFINITE_PUPIL_MODE=void 0}else T.__COOPT_FORCE_INFINITE_PUPIL_MODE=D;return await U()}finally{if(ee==null)try{delete T.__COOPT_FORCE_INFINITE_PUPIL_MODE}catch{T.__COOPT_FORCE_INFINITE_PUPIL_MODE=void 0}else T.__COOPT_FORCE_INFINITE_PUPIL_MODE=ee}},X=D=>{const U=typeof D=="string"?D.trim().toLowerCase():"";return U==="stop"||U==="entrance"?U:""},k=()=>{try{const D=X(T?.__COOPT_FORCE_INFINITE_PUPIL_MODE??T?.COOPT_FORCE_INFINITE_PUPIL_MODE);if(D)return D}catch{}try{return X(localStorage.getItem("coopt.forceInfinitePupilMode"))}catch{return""}},Y=D=>{const U=k();if(U==="stop"||U==="entrance")return{mode:U,forced:!0};const ee=String(D?.type||"").trim().toLowerCase(),oe=Number(D?.fieldAngle?.x??0),ae=Number(D?.fieldAngle?.y??0),Ae=Math.abs(oe)>1e-12||Math.abs(ae)>1e-12;return{mode:ee==="angle"&&Ae?"entrance":void 0,forced:!1}};try{const D=C?x?.ownerDocument?.defaultView?.Plotly||(typeof window<"u"?window.Plotly:null):null;if(C&&!D)throw new Error("Plotly is not available");g(5,"Loading modules...");const{createOPDCalculator:U}=await De(async()=>{const{createOPDCalculator:Ne}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(qe=>qe.bV);return{createOPDCalculator:Ne}},__vite__mapDeps([0,1])),{WavefrontAberrationAnalyzer:ee}=await De(async()=>{const{WavefrontAberrationAnalyzer:Ne}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(qe=>qe.bV);return{WavefrontAberrationAnalyzer:Ne}},__vite__mapDeps([0,1])),{SimpleFFT:oe}=await De(async()=>{const{SimpleFFT:Ne}=await import("./psf-calculator-Bc6xBvfp.js");return{SimpleFFT:Ne}},__vite__mapDeps([3,0,1])),{derivePupilAndFocalLengthMmFromParaxial:ae}=await De(async()=>{const{derivePupilAndFocalLengthMmFromParaxial:Ne}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(qe=>qe.bU);return{derivePupilAndFocalLengthMmFromParaxial:Ne}},__vite__mapDeps([0,1]));g(10,"Preparing optical system...");const Ae=Je(window.tableOpticalSystem),te=ht(window.tableObject),re=!!(n&&typeof n=="object");if(!re){if(!te||te.length===0)throw new Error("オブジェクトデータがありません。まずオブジェクトを設定してください。");if(z>=te.length)throw new Error("指定されたオブジェクトが見つかりません。")}const se=re?n:te[z],ue=String(se.position??se.object??se.Object??se.objectType??"Point"),me=ue.toLowerCase(),ge=/\bangle\b/.test(me);Be("📥 [MTF] showMTFDiagram called with objectOverride:",{hasOverride:re,objectOverride:n?{x:n.x,y:n.y,xHeightAngle:n.xHeightAngle,yHeightAngle:n.yHeightAngle,position:n.position}:null,selectedObject:{position:se.position,x:se.x,y:se.y,xHeightAngle:se.xHeightAngle,yHeightAngle:se.yHeightAngle},defocusShiftMm:l,callStack:"showMTFDiagram"});let xe=!1;try{const Ne=Ae&&Ae.length>0?Ae[0]:null;if(Ne){const qe=Ne.thickness??Ne.Thickness;if(!(qe==="INF"||qe===1/0)){const ct=parseFloat(qe);Number.isFinite(ct)&&ct>0&&(xe=!0)}}}catch{}const W=ge?se.xHeightAngle??se.x??0:se.x??se.xHeight??se.xHeightAngle??0,fe=ge?se.yHeightAngle??se.y??0:se.y??se.yHeight??se.yHeightAngle??0;Be(`🔍 [TFMTF Setup] Object ${z}: type="${ue}", isAngleType=${ge}, isFiniteObject=${xe}, objectX=${W.toFixed(4)}, objectY=${fe.toFixed(4)}, defocusShift=${l} mm`);const we=Yl(Ae,l,xe),Se=Vl(we);if(!we||we.length===0)throw new Error("光学システムデータがありません。まず光学システムを設定してください。");let ye={x:0,y:0},ce=0,pe=0;/\bangle\b/.test(me)?ye={x:m(W,0),y:m(fe,0)}:(ce=m(W,0),pe=m(fe,0)),Be(`🔍 [TFMTF Field] fieldAngle={${ye.x}, ${ye.y}}, xHeight=${ce.toFixed(4)}, yHeight=${pe.toFixed(4)}`);const he=/\bangle\b/.test(me)?{x:m(ye?.x,0),y:m(ye?.y,0)}:{x:m(ce,0),y:m(pe,0)};let _e=he.x,ve=he.y;Math.abs(_e)>0||Math.abs(ve)>0||(_e=1,ve=0);const Ce=Math.abs(_e)>=Math.abs(ve)?"x":"y",Te=Ce==="x"?"y":"x",Ie=await ql(),be=M?(()=>{try{const Ne=Tt(window.tableSource),qe=[];for(let ot=0;ot<(Array.isArray(Ne)?Ne.length:0);ot++){const ct=Number(Ne[ot]?.wavelength);!Number.isFinite(ct)||ct<=0||qe.push(ct)}return qe}catch{return[]}})():[G],Pe=Array.from(new Set(be.map(Ne=>Number(Ne)).filter(Ne=>Number.isFinite(Ne)&&Ne>0)));if(Pe.length===0)if(Number.isFinite(S)&&S>0)Pe.push(S);else throw new Error("Primary wavelength is unavailable. Please set Source Primary Wavelength.");const Ee=[];let Ue=0;const Ge=new Set,He=(Ne,qe,ot)=>{if(!Array.isArray(Ne)||!Array.isArray(qe)||Ne.length===0||qe.length!==Ne.length||!Number.isFinite(ot))return null;const ct=Ne.length-1,$t=Number(Ne[0]),lt=Number(Ne[ct]);if(!Number.isFinite($t)||!Number.isFinite(lt))return null;if(ot<=$t){const Ve=Number(qe[0]);return Number.isFinite(Ve)?Ve:null}if(ot>=lt){const Ve=Number(qe[ct]);return Number.isFinite(Ve)?Ve:null}for(let Ve=1;Ve<Ne.length;Ve++){const Ze=Number(Ne[Ve-1]),Nt=Number(Ne[Ve]);if(!(!Number.isFinite(Ze)||!Number.isFinite(Nt)||Nt<=Ze)&&ot<=Nt){const Vt=Number(qe[Ve-1]),Kt=Number(qe[Ve]);if(!Number.isFinite(Vt)||!Number.isFinite(Kt))return null;const bt=(ot-Ze)/(Nt-Ze);return Vt+bt*(Kt-Vt)}}return null},Me=(Ne,qe,ot)=>{if(!Ne||!Array.isArray(Ne.freq)||!Array.isArray(Ne.mtfVals))return Ne;const ct=Ne.freq.map(Kt=>Number(Kt)),$t=Ne.mtfVals;if(ct.length===0||$t.length!==ct.length)return Ne;const lt=Number(qe);if(!Number.isFinite(lt)||lt<=0)return Ne;const Ve=Math.max(2,Math.floor(Number(ot)||2)),Ze=[],Nt=[],Vt=Number(ct[ct.length-1]);for(let Kt=0;Kt<Ve;Kt++){const bt=Ve<=1?0:Kt/(Ve-1),$n=lt*bt;if(Ze.push($n),Number.isFinite(Vt)&&$n>Vt+1e-12)Nt.push(0);else{const ho=He(ct,$t,$n);Nt.push(Number.isFinite(Number(ho))?Number(ho):null)}}return Nt.length>0&&(Nt[0]=1),{freq:Ze,mtfVals:Nt}},ze=(Ne,qe=null)=>{if(!Ne||!Array.isArray(Ne.freq)||!Array.isArray(Ne.mtfVals))return Ne;const ot=Ne.mtfVals.map((ct,$t)=>{const lt=Number(ct);if(!Number.isFinite(lt))return null;let Ve=Math.max(0,Math.min(1,lt));if(Array.isArray(qe)&&$t<qe.length){const Ze=Number(qe[$t]);Number.isFinite(Ze)&&(Ve=Math.min(Ve,Math.max(0,Math.min(1,Ze))))}return Ve});return ot.length>0&&ot[0]!==null&&(ot[0]=1),{...Ne,mtfVals:ot}},Fe=(Ne,qe,ot)=>{const ct=Number(Ne),$t=Number(qe),lt=Number(ot);if(!Number.isFinite(ct)||ct<0||!Number.isFinite($t)||$t<=0||!Number.isFinite(lt)||lt<=0)return null;const Ve=1e3/($t*lt);if(!Number.isFinite(Ve)||Ve<=0)return null;const Ze=ct/Ve;if(!Number.isFinite(Ze))return null;if(Ze>=1)return 0;if(Ze<=0)return 1;const Nt=Math.max(-1,Math.min(1,Ze)),Vt=2/Math.PI*(Math.acos(Nt)-Nt*Math.sqrt(Math.max(0,1-Nt*Nt)));return Number.isFinite(Vt)?Math.max(0,Math.min(1,Vt)):null},$e=async(Ne,qe,ot)=>{const lt=10+qe*85/Math.max(1,ot),Ve=85/Math.max(1,ot),Ze={objectIndex:z,type:ue,fieldAngle:ye,xHeight:ce,yHeight:pe,wavelength:Ne};Be(`🔍 [MTF Field] CACHE KEY INPUTS for wavelength ${(Ne*1e3).toFixed(1)}nm:`),Be(`   fieldAngle.x=${Ze.fieldAngle.x.toFixed(4)}, fieldAngle.y=${Ze.fieldAngle.y.toFixed(4)}`),Be(`   xHeight=${Ze.xHeight.toFixed(4)},  yHeight=${Ze.yHeight.toFixed(4)}`),Be(`   type="${Ze.type}"`);const Nt=J,Vt=U(we,Ne),Kt=new ee(Vt),bt=(Ne*1e3).toFixed(1);g(lt,`λ=${bt} nm: Generating wavefront...`);const $n=Re=>{try{const ke=Number(Re?.percent),Oe=Re?.message||Re?.phase||"Generating wavefront...";Number.isFinite(ke)?g(lt+ke/100*(Ve*.55),`λ=${bt} nm: ${Oe}`):g(void 0,`λ=${bt} nm: ${Oe}`)}catch{}},ho=async(Re,ke=Ze)=>await O(Re,async()=>{Be("🔍 [Wavefront] Calling generateWavefrontMap with customFieldSetting:",ke);const Oe=await Kt.generateWavefrontMap(ke,Nt,"circular",{recordRays:!1,progressEvery:512,profile:R,suppressReferenceRayError:!0,zernikeMaxNoll:37,renderFromZernike:!1,skipZernikeFit:!0,wasmFastOnly:w,traceOptions:w?{requireWasmRayTracing:!0,allowNonStrict:!1}:null,opdMode:"simple",opdDisplayMode:_,onProgress:$n,useCache:!1});if(Oe?.opdGrid){const je=Oe.opdGrid;let Le=0;const _t=Math.max(1,Math.floor(je.length/100));for(let rt=0;rt<je.length;rt+=_t)Le+=Math.abs(je[rt]||0);const vt=(Le%1e4).toFixed(0);Be(`✅ [Wavefront] generateWavefrontMap completed: gridSize=${Oe?.gridSize}, OPD_CHECKSUM=${vt}, fieldSetting.xHeight=${ke.xHeight}, fieldSetting.yHeight=${ke.yHeight}`)}else Be("✅ [Wavefront] generateWavefrontMap completed: no OPD grid");return Oe}),Qa=Re=>/entrance.*fail|entrance pupil|entrance unreachable/i.test(String(Re||"")),An=async(Re,ke=Ze,Oe=!0)=>{const je=Si();try{Oe||Qn(!1);const Le=await ho(Re,ke);return Le?.error?{map:null,error:String(Le.error?.message||"Wavefront generation failed")}:{map:Le,error:""}}catch(Le){return{map:null,error:String(Le?.message||Le||"Wavefront generation failed")}}finally{Oe||Qn(je)}};let wt=null;const cn=[],tr=Y(Ze),Wn=tr.mode,nr=Wn||"auto",Un=await An(Wn,Ze,!0);if(Un.map?(wt=Un.map,console.log(`✅ [TFMTF Pupil] Primary mode succeeded: ${nr}${tr.forced?" (forced)":""}`)):(cn.push(`${nr}=${Un.error}`),console.warn(`⚠️ [TFMTF Pupil] Primary mode failed: ${nr} (${Un.error})`)),!wt&&!tr.forced){const Re=[];Wn==="entrance"?Re.push("stop"):Wn==="stop"?Re.push("entrance"):Re.push("entrance","stop");for(const ke of Re){if(!Qa(Un.error)&&Wn==="entrance"&&ke==="stop")continue;g(lt+Ve*.1,`λ=${bt} nm: Retrying with ${ke} mode...`);const Oe=await An(ke,Ze,!0);if(Oe.map){wt=Oe.map;break}cn.push(`${ke}=${Oe.error}`)}}if(!wt&&Math.abs(Number(ye?.x||0))<1e-12&&Math.abs(Number(ye?.y||0))<1e-12&&Math.abs(Number(ce||0))<1e-12&&Math.abs(Number(pe||0))<1e-12){g(lt+Ve*.14,`λ=${bt} nm: Retrying with finite on-axis field...`);const ke={...Ze,type:"Height",fieldAngle:{x:0,y:0},xHeight:0,yHeight:0,forceFinite:!0},Oe=Y(ke),je=Oe.mode,Le=je||"auto",_t=await An(je,ke,!0);if(_t.map)wt=_t.map;else if(cn.push(`finite-${Le}=${_t.error}`),!Oe.forced){const vt=[];je==="entrance"?vt.push("stop"):je==="stop"?vt.push("entrance"):vt.push("entrance","stop");for(const rt of vt){const jt=await An(rt,ke,!0);if(jt.map){wt=jt.map;break}cn.push(`finite-${rt}=${jt.error}`)}}}if(!wt){const Re=cn.join("; ");if(/No valid OPD samples|trace to eval failed/i.test(Re)){g(lt+Ve*.18,`λ=${bt} nm: Retrying with compatibility ray tracing...`);const Oe=Y(Ze),je=Oe.mode,Le=je||"auto",_t=await An(je,Ze,!1);if(_t.map)wt=_t.map;else if(cn.push(`compat-${Le}=${_t.error}`),!Oe.forced){const vt=[];je==="entrance"?vt.push("stop"):je==="stop"?vt.push("entrance"):vt.push("entrance","stop");for(const rt of vt){const jt=await An(rt,Ze,!1);if(jt.map){wt=jt.map;break}cn.push(`compat-${rt}=${jt.error}`)}}}}if(!wt)throw new Error(cn.join("; ")||"Wavefront generation failed");g(lt+Ve*.6,`λ=${bt} nm: Building OPD grid...`);const mt=Math.max(16,Math.floor(Number(Nt))),ui=Array.from({length:mt},()=>new Float32Array(mt)),or=Array.from({length:mt},()=>new Float32Array(mt));console.log(`🔍 [TFMTF OPD] Building ${mt}x${mt} OPD grid from wavefront map...`);const rr=Array.from({length:mt},()=>Array(mt).fill(!1)),ir=new Float32Array(mt),sr=new Float32Array(mt),pi=Number.isFinite(Number(wt?.pupilRange))&&Number(wt.pupilRange)>0?Number(wt.pupilRange):1;for(let Re=0;Re<mt;Re++){const ke=Re/(mt-1||1)*2-1;ir[Re]=ke*pi,sr[Re]=ke*pi}const ar=Array.isArray(wt?.pupilCoordinates)?wt.pupilCoordinates:[],cr=_!=="raw"&&Array.isArray(wt?.display?.opds)?wt.display.opds:Array.isArray(wt?.opds)?wt.opds:[],di=Math.min(ar.length,cr.length);for(let Re=0;Re<di;Re++){const ke=ar[Re],Oe=Number.isInteger(ke?.ix)?ke.ix:null,je=Number.isInteger(ke?.iy)?ke.iy:null;if(Oe===null||je===null||Oe<0||Oe>=mt||je<0||je>=mt)continue;const Le=Number(cr[Re]);Number.isFinite(Le)&&(rr[je][Oe]=!0,ui[je][Oe]=Le,or[je][Oe]=1)}const mi=[];for(let Re=0;Re<di;Re++){const ke=ar[Re],Oe=Number(ke?.x),je=Number(ke?.y),Le=Number(cr[Re]);!Number.isFinite(Oe)||!Number.isFinite(je)||!Number.isFinite(Le)||mi.push({pupilX:Oe,pupilY:je,opd:Le,isVignetted:!1})}const ec={gridSize:mt,wavelength:Ne,rayData:mi,gridData:{opd:ui,amplitude:or,pupilMask:rr,xCoords:ir,yCoords:sr}},tc=/\bangle\b/.test(me),fi=ae(we,Ne,tc),bo=fi.pupilDiameterMm,lr=fi.focalLengthMm,ur=bo>0?Ne*lr/bo:1,nc=lo(we,Ne),gi=Number(nc?.fNumberWorking),oc=Math.max(2,v),rc=Math.max(J,2*(oc-1)),yi=ne(rc),ic=yi>J?I(yi,32,4096):0;let pr=le?Z:ic;le&&Z===J&&J<=32&&(pr=64,console.warn("⚠️ MTF: sampling 32 with zero-pad none is unstable; promoted FFT size to 64 for robust plotting.")),g(lt+Ve*.75,`λ=${bt} nm: Calculating PSF...`);const Nn=await Ie.calculatePSF(ec,{samplingSize:mt,zeroPadTo:pr,pupilDiameter:bo,focalLength:lr,pixelSize:null,forceImplementation:N?null:"javascript",removeTilt:!1});!N&&String(Nn?.implementationUsed||"").toLowerCase()!=="javascript"&&console.warn("⚠️ MTF PSF path expected JavaScript (for zero-padding), but got a different implementation."),g(lt+Ve*.85,`λ=${bt} nm: Computing OTF/MTF...`);const yn=Nn?.psfData||Nn?.psf||Nn?.intensity||null,Hn=m(Nn?.options?.pixelSize,m(ur,1));if(!yn||!Array.isArray(yn)||!Array.isArray(yn[0]))throw new Error("PSF data missing for MTF");const Ft=yn.length;if(Ft<2||yn[0].length!==Ft)throw new Error("PSF grid must be NxN");console.log(`🔍 [TFMTF PSF] PSF grid: ${Ft}x${Ft}, pixelSize=${Hn.toFixed(6)}µm`);const dr=1/(Ft*Hn)*1e3,wo=.5/Hn*1e3,hn=E>0?E:wo;Ue=Math.max(Ue,hn);const sc=Math.floor(Ft/2),mr=Math.max(0,Math.min(sc,Math.floor(wo/(dr||1e-9)))),hi=Array.from({length:mr+1},(Re,ke)=>ke*dr);if(E>0&&hn>wo+1e-9){const Re=`Requested Max(lp/mm)=${hn.toFixed(2)} exceeds Nyquist=${wo.toFixed(2)} at λ=${bt}nm. Data beyond Nyquist is zero-filled.`;Ge.add(Re),console.warn(`⚠️ [MTF] ${Re}`)}const fr=new Float64Array(Ft*Ft);for(let Re=0;Re<Ft;Re++)for(let ke=0;ke<Ft;ke++)fr[Re*Ft+ke]=m(yn[Re]?.[ke],0);const vo=N?null:Ie?.wasmCalculator?.calculateMTFAxesFromPSF;let Ye=null,it=null;if(!N&&st())try{const Re=Math.max(2,Math.min(1024,v)),ke=await qr({psfData:yn,pixelSizeUm:Hn,maxFrequencyLpmm:hn,points:Re}),Oe=Array.isArray(ke?.frequencyAxis)?ke.frequencyAxis:[],je=Array.isArray(ke?.mtfTangential)?ke.mtfTangential:[],Le=Array.isArray(ke?.mtfSagittal)?ke.mtfSagittal:[];if(Oe.length>1&&je.length===Oe.length&&Le.length===Oe.length){const _t=Ce==="x"?Le:je,vt=Te==="x"?Le:je;Ye={freq:Array.from(Oe,rt=>Number(rt)),mtfVals:Array.from(_t,rt=>Number.isFinite(Number(rt))?Number(rt):null)},it={freq:Array.from(Oe,rt=>Number(rt)),mtfVals:Array.from(vt,rt=>Number.isFinite(Number(rt))?Number(rt):null)},Ye.mtfVals.length>0&&(Ye.mtfVals[0]=1),it.mtfVals.length>0&&(it.mtfVals[0]=1)}}catch(Re){console.warn("⚠️ Native Rust MTF extraction failed; falling back to JS/WASM path.",Re),Ye=null,it=null}if((!Ye||!it)&&typeof vo=="function")try{const Re=vo.call(Ie.wasmCalculator,fr,Ft,mr);if(Re?.xAxis&&Re?.yAxis){const ke=Ce==="x"?Re.xAxis:Re.yAxis,Oe=Te==="x"?Re.xAxis:Re.yAxis;Ye={freq:hi,mtfVals:Array.from(ke,je=>Number.isFinite(je)?je:null)},it={freq:hi,mtfVals:Array.from(Oe,je=>Number.isFinite(je)?je:null)},Ye.mtfVals.length>0&&(Ye.mtfVals[0]=1),it.mtfVals.length>0&&(it.mtfVals[0]=1)}}catch{Ye=null,it=null}if(!Ye||!it){const Re=Array.from({length:Ft},(rt,jt)=>Array.from({length:Ft},(nn,Ht)=>fr[jt*Ft+Ht])),ke=Array.from({length:Ft},()=>Array.from({length:Ft},()=>0)),Oe=oe.fft2D(Re,ke),je=m(Oe?.real?.[0]?.[0],0),Le=m(Oe?.imag?.[0]?.[0],0),_t=Math.hypot(je,Le);if(!Number.isFinite(_t)||_t<=0)throw new Error("Invalid OTF DC component");const vt=rt=>{const jt=[],nn=[];for(let Ht=0;Ht<=mr;Ht++){const Gn=Ht*dr;let Gt=0,dt=0;rt==="x"?(Gt=m(Oe.real?.[0]?.[Ht],0),dt=m(Oe.imag?.[0]?.[Ht],0)):(Gt=m(Oe.real?.[Ht]?.[0],0),dt=m(Oe.imag?.[Ht]?.[0],0));const Wt=Math.hypot(Gt,dt)/_t;jt.push(Gn),nn.push(Number.isFinite(Wt)?Wt:null)}return nn.length>0&&(nn[0]=1),{freq:jt,mtfVals:nn}};Ye=vt(Ce),it=vt(Te)}Ye=Me(Ye,hn,v),it=Me(it,hn,v);let Yt=null;if(B||Se){try{const Re=Array.from({length:mt},()=>new Float32Array(mt)),ke={gridSize:mt,wavelength:Ne,gridData:{opd:Re,amplitude:or,pupilMask:rr,xCoords:ir,yCoords:sr}},Oe=await Ie.calculatePSF(ke,{samplingSize:mt,zeroPadTo:pr,pupilDiameter:bo,focalLength:lr,pixelSize:null,forceImplementation:N?null:"javascript",removeTilt:!1}),je=Oe?.psfData||Oe?.psf||Oe?.intensity||null;if(je&&Array.isArray(je)&&Array.isArray(je[0])){const Le=je.length;if(Le>=2&&je[0].length===Le){const _t=m(Oe?.options?.pixelSize,m(Hn,ur)),vt=1/(Le*_t)*1e3,rt=.5/_t*1e3,jt=Math.floor(Le/2),nn=Math.max(0,Math.min(jt,Math.floor(rt/(vt||1e-9)))),Ht=Array.from({length:nn+1},(dt,Wt)=>Wt*vt),Gn=new Float64Array(Le*Le);for(let dt=0;dt<Le;dt++)for(let Wt=0;Wt<Le;Wt++)Gn[dt*Le+Wt]=m(je[dt]?.[Wt],0);let Gt=null;if(typeof vo=="function")try{const dt=vo.call(Ie.wasmCalculator,Gn,Le,nn);if(dt?.xAxis&&dt?.yAxis){const Wt=Ce==="x"?dt.xAxis:dt.yAxis;Gt={freq:Ht,mtfVals:Array.from(Wt,on=>Number.isFinite(on)?on:null)},Gt.mtfVals.length>0&&(Gt.mtfVals[0]=1)}}catch{Gt=null}if(!Gt){const dt=Array.from({length:Le},(qn,un)=>Array.from({length:Le},(xo,Vn)=>Gn[un*Le+Vn])),Wt=Array.from({length:Le},()=>Array.from({length:Le},()=>0)),on=oe.fft2D(dt,Wt),ac=m(on?.real?.[0]?.[0],0),cc=m(on?.imag?.[0]?.[0],0),hr=Math.hypot(ac,cc);if(Number.isFinite(hr)&&hr>0){const qn=[];for(let un=0;un<=nn;un++){let xo=0,Vn=0;Ce==="x"?(xo=m(on.real?.[0]?.[un],0),Vn=m(on.imag?.[0]?.[un],0)):(xo=m(on.real?.[un]?.[0],0),Vn=m(on.imag?.[un]?.[0],0));const wi=Math.hypot(xo,Vn)/hr;qn.push(Number.isFinite(wi)?wi:null)}qn.length>0&&(qn[0]=1),Gt={freq:Ht,mtfVals:qn}}}if(Gt){const dt=Me(Gt,hn,v);dt?.mtfVals&&dt.mtfVals.length===Ye.freq.length&&(Yt=dt.mtfVals)}}}}catch{Yt=null}Yt||(Yt=Ye.freq.map(Re=>Fe(Re,Ne,gi)))}const gr=Array.isArray(Yt)&&Yt.length===Ye.freq.length?Yt.map(Re=>Number.isFinite(Number(Re))?Math.max(0,Math.min(1,Number(Re))):null):null,bi=Ye.freq.map(Re=>{const ke=Fe(Re,Ne,gi);return Number.isFinite(Number(ke))?Math.max(0,Math.min(1,Number(ke))):null}),ln=Array.isArray(gr)&&gr.some(Re=>Number.isFinite(Number(Re)))?gr:bi.some(Re=>Number.isFinite(Number(Re)))?bi:null;if(Se){const Re=Array.isArray(ln)&&ln.length===Ye.freq.length?ln.slice():Ye.freq.map((ke,Oe)=>{const je=Number(Ye?.mtfVals?.[Oe]),Le=Number(it?.mtfVals?.[Oe]);return Number.isFinite(je)&&Number.isFinite(Le)?Math.max(0,Math.min(1,.5*(je+Le))):Number.isFinite(je)?Math.max(0,Math.min(1,je)):Number.isFinite(Le)?Math.max(0,Math.min(1,Le)):null});Ye={...Ye,mtfVals:Re.slice()},it={...it,mtfVals:Re.slice()},Yt=Re.slice();try{console.log(`ℹ️ [MTF] Pure Paraxial/ThinLens system detected; using the analytic diffraction-limited M/S curve at λ=${bt}nm.`)}catch{}}else Ye=ze(Ye,ln),it=ze(it,ln),Array.isArray(ln)&&ln.length===Ye.freq.length&&(Yt=ln.slice());const yr=h(Ne);console.log(`🔍 [TFMTF MTF] λ=${bt}nm: tan=${Ye.freq.length}pts(${Math.min(...Ye.mtfVals).toFixed(3)}-${Math.max(...Ye.mtfVals).toFixed(3)}), sag=${it.freq.length}pts(${Math.min(...it.mtfVals).toFixed(3)}-${Math.max(...it.mtfVals).toFixed(3)})`),Ee.push({x:Ye.freq,y:Ye.mtfVals,type:"scatter",mode:"lines",name:`Tangential (${bt}nm)`,showlegend:!0,line:{color:yr,width:2,dash:"solid"}}),Ee.push({x:it.freq,y:it.mtfVals,type:"scatter",mode:"lines",name:`Sagittal (${bt}nm)`,showlegend:!0,line:{color:yr,width:2,dash:"dot"}}),B&&Array.isArray(Yt)&&Ee.push({x:Ye.freq,y:Yt,type:"scatter",mode:"lines",name:`Diffraction Limit (${bt}nm)`,showlegend:!0,meta:{overlayType:"diffractionLimit"},line:{color:yr,width:1.75,dash:"dash"}})},Qe=Pe.length;for(let Ne=0;Ne<Qe;Ne++)await $e(Pe[Ne],Ne,Qe);const pt=M?"All wavelengths":`${(G*1e3).toFixed(1)} nm`,Xt=E>0?E:Ue||0,Ct={title:`Modulation Transfer Function (${pt}, Object ${z})`,xaxis:{title:"Spatial frequency (lp/mm)",range:[0,Xt]},yaxis:{title:"MTF",range:[0,1.05]},margin:{l:60,r:20,t:50,b:50}};if(Ge.size>0){const Ne=Array.from(Ge).join("<br>");Ct.annotations=[{xref:"paper",yref:"paper",x:0,y:1.08,xanchor:"left",yanchor:"bottom",align:"left",showarrow:!1,font:{size:11,color:"#b45309"},text:`Calculation limit: ${Ne}`}]}return C&&(g(95,"Rendering plot..."),await D.newPlot(x,Ee,Ct,{responsive:!0,displaylogo:!1})),g(100,"Done"),console.log(`✅ [TFMTF COMPLETE] Returning ${Ee.length} traces, maxPlotLpmm=${Ue.toFixed(1)}`),{traces:Ee,layout:Ct,maxPlotLpmmGlobal:Ue}}finally{Qn(j)}}async function Er({wavelengthMicrons:e,objectIndex:t,targetFrequencyLpmm:n,defocusMinMm:o,defocusMaxMm:r,steps:s,samplingSize:a,samplingPoints:c,zeroPadTo:i,containerElement:l,onProgress:f,opdDisplayMode:p}={}){const d=(V,K)=>{const $=Number(V);return Number.isFinite($)?$:K},u=(V,K,$)=>Math.max(K,Math.min($,V)),y=l||document.getElementById("mtf-container");if(!y)throw new Error("MTF container element not found");try{y.innerHTML=""}catch{}const m=y?.ownerDocument?.defaultView?.Plotly||(typeof window<"u"?window.Plotly:null);if(!m)throw new Error("Plotly is not available");const h=(V,K,$,J)=>{try{if(typeof f!="function")return;f({percent:V,message:K,trace:$,subMessage:J})}catch{}},g=d(o,-.1),w=d(r,.1),R=u(Math.floor(d(s,21)),3,201),N=Math.max(0,d(n,10)),I=Math.floor(d(a,d(c,256))),b=Number.isFinite(I)&&I>0?I:256,A=Array.from({length:R},(V,K)=>{if(R<=1)return g;const $=K/(R-1);return g+$*(w-g)}),v=new Map;h(5,"Initializing worker pool...",void 0,void 0);let S=null;try{S=await oa(4)}catch(V){console.warn("⚠️ [TFMTF] Failed to initialize worker pool, falling back to sequential processing:",V)}h(10,"Computing PSF for all defocus points...",void 0,void 0);const _=[],M=4,G=[];for(let V=0;V<A.length;V+=M){const K=[];for(let $=V;$<Math.min(V+M,A.length);$++)K.push({shift:A[$],index:$});G.push(K)}Be(`🚀 [TFMTF] Starting PSF batch processing: ${A.length} defocus values in ${G.length} batches (batch size: ${M})`);for(let V=0;V<G.length;V++){const K=G[V],$=V+1,J=G.length;h(12+V*2,`Computing PSF: Batch ${$}/${J} (${K.length} points)`,void 0,void 0);const Q=K.map(({shift:B,index:C})=>(async()=>{let F="";const x=T=>{if(T?.message){F=`Defocus ${B.toFixed(4)}mm(${C+1}/${A.length}) `+T.message;const P=Math.floor(10+C/Math.max(1,A.length)*50);h(P,`Computing PSF: Defocus ${B.toFixed(4)} mm (${C+1}/${A.length})`,void 0,F)}};try{Be(`   → TFMTF Batch ${$}/${J} defocus ${B.toFixed(4)}mm: Calling showMTFDiagram`);const T=await fn({wavelengthMicrons:e,objectIndex:t,maxFrequencyLpmm:N,samplingSize:b,zeroPadTo:i,opdDisplayMode:p,defocusShiftMm:B,skipPlot:!0,onProgress:x,containerElement:l});return Be("   ← TFMTF completed"),{shift:B,index:C,psfGrid:new Float64Array(b*b),rows:b,cols:b,metadata:{wavelengthMicrons:e,targetFreq:N},mfResult:T,success:!0}}catch(T){return Oo(`❌ [TFMTF] PSF calculation failed for defocus ${B}:`,T),{shift:B,index:C,psfGrid:new Float64Array(0),rows:0,cols:0,metadata:{},mfResult:null,success:!1,error:T}}})()),le=await Promise.allSettled(Q),Z=[];for(let B=0;B<le.length;B++){const C=le[B];C.status==="fulfilled"&&Z.push({index:C.value.index,data:C.value})}Z.sort((B,C)=>B.index-C.index);for(const{data:B}of Z)B.success?_.push({shift:B.shift,psfGrid:B.psfGrid,rows:B.rows,cols:B.cols,metadata:B.metadata,mfResult:B.mfResult}):_.push({shift:B.shift,psfGrid:new Float64Array(0),rows:0,cols:0,metadata:B.metadata,mfResult:null});Be(`✅ [TFMTF] Batch ${$}/${J} completed: ${Z.length}/${K.length} items successful`)}h(60,"Extracting MTF values from PSF...",void 0,void 0),Be(`✅ [TFMTF] All PSF calculations completed. Results collected: ${_.length}/${A.length}`);const z=typeof e=="string"&&String(e).toLowerCase()==="all"?"All wavelengths":`${(d(e,.5876)*1e3).toFixed(1)} nm`,E=Number.isFinite(Number(t))?Math.max(0,Math.floor(Number(t))):0,L={title:`Through-Focus MTF (${N.toFixed(1)} lp/mm, ${z}, Object ${E})`,xaxis:{title:"Defocus shift (mm)",range:[Math.min(g,w),Math.max(g,w)]},yaxis:{title:"MTF",range:[0,1.05]},margin:{l:60,r:20,t:50,b:50}};h(62,"Initializing plot...",void 0,void 0),m.newPlot(y,[],L,{responsive:!0,displaylogo:!1});for(let V=0;V<_.length;V++){const{shift:K,mfResult:$}=_[V];let J="";const Q=Array.isArray($?.traces)?$.traces:[];for(const F of Q){if(F?.meta?.overlayType==="diffractionLimit")continue;const T=String(F?.name??"MTF").replace(/^Tangential\b/,"Meridional"),j=Array.isArray(F?.x)?F.x:[],P=Array.isArray(F?.y)?F.y:[];if(j.length===0||P.length===0)continue;let O=0,X=1/0;for(let U=0;U<j.length;U++){const ee=Number(j[U]);if(!Number.isFinite(ee))continue;const oe=Math.abs(ee-N);oe<X&&(X=oe,O=U)}const k=Number(P[O]),Y=Number.isFinite(k)?k:null;if(!v.has(T)){const U={x:[],y:[],type:"scatter",mode:typeof F?.mode=="string"&&F.mode?F.mode:"lines",name:T,showlegend:!0};F?.line&&typeof F.line=="object"?U.line={...F.line}:U.line={width:2},F?.marker&&typeof F.marker=="object"&&(U.marker={...F.marker}),v.set(T,U)}const D=v.get(T);D.x.push(K),D.y.push(Y)}const le=Array.from(v.values());m.newPlot(y,le,L,{responsive:!0,displaylogo:!1});const Z=Math.floor(60+(V+1)/_.length*35),B=Array.from(v.values()).map(F=>({...F,x:[...F.x],y:[...F.y]})),C=Array.from(new Set(Q.map(F=>{const T=String(F?.name??"").match(/([0-9]+(?:\.[0-9]+)?)\s*nm/i);if(!T)return null;const j=Number(T[1]);return Number.isFinite(j)?`${j.toFixed(1)}nm`:null}).filter(F=>typeof F=="string"&&F.length>0)));if(C.length>0)for(const F of C)h(Z,`Extracting MTF: λ=${F}, step ${V+1}/${_.length}`,B,J);else h(Z,`Extracting MTF: step ${V+1}/${_.length}`,B,J)}const ne=Array.from(v.values());return h(98,"Finalizing plot...",void 0,void 0),m.newPlot(y,ne,L,{responsive:!0,displaylogo:!1}),h(100,"Done",void 0,void 0),console.log(`✅ [TFMTF] Computed ${_.length} through-focus points with ${R} steps`),{traces:ne,layout:L}}async function ra({wavelengthMicrons:e,firstFrequencyLpmm:t,secondFrequencyLpmm:n,fieldMin:o,fieldMax:r,steps:s,samplingSize:a,samplingPoints:c,zeroPadTo:i,containerElement:l,onProgress:f,opdDisplayMode:p,fieldAxisMode:d}={}){const u=(B,C)=>{const F=Number(B);return Number.isFinite(F)?F:C},y=(B,C,F)=>Math.max(C,Math.min(F,B));let h=(()=>{if(l)return l;const B=C=>{try{return document.getElementById(C)}catch{return null}};return B("mtf-container")||B("popup-field-mtf-container")||B("popup-through-focus-mtf-container")||null})();if(!h)throw new Error("MTF container element not found");try{h.innerHTML=""}catch{}const g=h?.ownerDocument?.defaultView?.Plotly||(typeof window<"u"?window.Plotly:null);if(!g)throw new Error("Plotly is not available");const w=(B,C,F,x)=>{try{if(typeof f!="function")return;f({percent:B,message:C,trace:F,subMessage:x})}catch{}},R=B=>{try{const O=Je(window.tableOpticalSystem),X=Array.isArray(O)&&O.length>0?O[0]:null;if(X){const k=X.thickness??X.Thickness;if(k==="INF"||k===1/0||String(k).trim().toUpperCase()==="INF")return Be("[inferObjectFieldMode] first surface thickness=INF → angle mode"),"angle";const D=parseFloat(String(k));Number.isFinite(D)&&D>0&&Be(`[inferObjectFieldMode] first surface thickness=${D} (finite) → defer to position field`)}}catch{}const C=Array.isArray(B)?B:[],F=O=>(O?.position??O?.object??O?.objectType??"").toString().toLowerCase(),x=C.map(F).filter(Boolean),T=x.some(O=>O.includes("rect")||O.includes("rectangle")),j=x.some(O=>O.includes("height"));return T||j?"height":(x.some(O=>/\bangle\b/.test(O)),"angle")},N=(()=>{if(d==="angle"||d==="height")return d;const B=ht(window.tableObject);return R(B)})(),I=N==="angle"?"deg":"mm",b=N==="angle"?"Object Angle (deg)":"Object Height (mm)";Be("========== 🔍 showFieldMTFDiagram EXECUTION START =========="),Be("📊 Input Parameters:",{fieldMin:o,fieldMax:r,steps:s,axisMode:N,wavelengthMicrons:e,firstFrequencyLpmm:t,secondFrequencyLpmm:n});const A=u(o,0),v=u(r,10),S=Math.min(A,v),_=Math.max(A,v),M=y(Math.floor(u(s,21)),3,201),G=Math.max(0,u(t,10)),z=Math.max(0,u(n,30)),E=Math.floor(u(a,u(c,256))),L=Number.isFinite(E)&&E>0?E:256,ne=Array.from({length:M},(B,C)=>{if(M<=1)return S;const F=C/(M-1);return S+F*(_-S)});Be(`📈 Field sweep: axisMode=${N}, min=${S}, max=${_}, nSteps=${M}`);const V=[],K=new Map;w(10,"Computing MTF for all field points...",void 0,void 0);const $=1,J=[];for(let B=0;B<ne.length;B+=$){const C=[];for(let F=B;F<Math.min(B+$,ne.length);F++)C.push({fieldValue:ne[F],index:F});J.push(C)}Be(`🚀 [Object MTF] Starting field batch processing: ${ne.length} field values in ${J.length} batches (batch size: ${$})`);for(let B=0;B<J.length;B++){const C=J[B],F=B+1,x=J.length;w(10+B*2,`Computing MTF: Batch ${F}/${x} (${C.length} points)`,void 0,void 0);const T=C.map(({fieldValue:O,index:X})=>(async()=>{let k="";const Y=U=>{if(U?.message){k=`Field ${O.toFixed(4)}${I}(${X+1}/${ne.length}) `+U.message;const oe=Math.floor(10+X/Math.max(1,ne.length)*50);w(oe,`Computing MTF: Field ${O.toFixed(4)} ${I} (${X+1}/${ne.length})`,void 0,k)}},D=N==="angle"?{x:0,y:O,xHeightAngle:0,yHeightAngle:O,position:"Angle"}:{x:0,y:O,xHeight:0,yHeight:O,position:"Rectangle"};Be(`
🔄 [Object MTF] ===== ITERATION START =====`),Be(`📍 Batch ${F}/${x}, Step ${X+1}/${ne.length}`),Be(`   axisMode=${N}, fieldValue=${O.toFixed(4)}${I}`),Be(`   objectOverride: x=${D.x}, y=${D.y}, xHeight=${D.xHeight}, yHeight=${D.yHeight}, position=${D.position}`);try{Be("   → Calling showMTFDiagram with objectOverride, expecting NEW OPD calculation");const U=await fn({wavelengthMicrons:e,objectIndex:0,objectOverride:D,maxFrequencyLpmm:Math.max(G,z)*2,samplingSize:L,zeroPadTo:i,opdDisplayMode:p,skipPlot:!0,onProgress:Y,containerElement:l});return Be("   ← showMTFDiagram completed"),{fieldValue:O,index:X,mfResult:U,success:!0}}catch(U){return Oo(`❌ [Object MTF] MTF calculation FAILED for field ${O.toFixed(4)}${I}:`,U),Oo("   Error details:",{errorMessage:U?.message,errorStack:U?.stack}),{fieldValue:O,index:X,mfResult:null,success:!1,error:U}}})()),j=await Promise.allSettled(T),P=[];for(let O=0;O<j.length;O++){const X=j[O];X.status==="fulfilled"&&P.push({index:X.value.index,data:X.value})}P.sort((O,X)=>O.index-X.index);for(const{data:O}of P)O.success?((!O.mfResult||!O.mfResult.traces||O.mfResult.traces.length===0)&&Be(`⚠️ [Object MTF] No traces returned for field ${O.fieldValue.toFixed(4)}${I}`),V.push({fieldValue:O.fieldValue,mfResult:O.mfResult}),Be(`✅ [Object MTF] Batch ${F}/${x} Step ${O.index+1}/${ne.length} completed: field=${O.fieldValue.toFixed(4)}${I}, traces=${O.mfResult?.traces?.length||0}`)):(V.push({fieldValue:O.fieldValue,mfResult:null}),Oo(`❌ [Object MTF] Batch ${F}/${x} Step ${O.index+1}/${ne.length} failed for field ${O.fieldValue.toFixed(4)}${I}`));Be(`✅ [Object MTF] Batch ${F}/${x} completed: ${P.length}/${C.length} items processed`)}const Q=typeof e=="string"&&String(e).toLowerCase()==="all"?"All wavelengths":`${(u(e,.5876)*1e3).toFixed(1)} nm`,le={title:`Object MTF (${G.toFixed(1)} / ${z.toFixed(1)} lp/mm, ${Q})`,xaxis:{title:b,range:[Math.min(S,_),Math.max(S,_)]},yaxis:{title:"MTF",range:[0,1.05]},margin:{l:60,r:20,t:50,b:50},showlegend:!0,legend:{x:1.02,y:1,xanchor:"left",yanchor:"top"}};w(62,"Initializing plot...",void 0,void 0),g.newPlot(h,[],le,{responsive:!0,displaylogo:!1}),Be(`✅ [Object MTF] All MTF calculations completed. Results collected: ${V.length}/${ne.length}`),Be(`🔍 [Object MTF] Aggregating ${V.length} field points into traces...`);for(let B=0;B<V.length;B++){const{fieldValue:C,mfResult:F}=V[B],x=Array.isArray(F?.traces)?F.traces:[];if(x.length===0){Be(`⚠️ [Object MTF] No traces for field ${B}: fieldValue=${C.toFixed(4)}`);continue}for(const O of x){if(O?.meta?.overlayType==="diffractionLimit")continue;const X=String(O?.name??"MTF"),k=/^Tangential\b/.test(X),Y=/^Sagittal\b/.test(X);if(!k&&!Y){Be(`⚠️ [Object MTF] Skipped trace with unmatched name: "${X}" at field ${C.toFixed(4)}`);continue}const D=Array.isArray(O?.x)?O.x:[],U=Array.isArray(O?.y)?O.y:[];if(D.length===0||U.length===0){Be(`⚠️ [Object MTF] Trace "${X}" has empty x/y at field ${C.toFixed(4)} (x.len=${D.length}, y.len=${U.length})`);continue}const ee=X.replace(/^(Tangential|Sagittal)\b/,"").trim(),oe=k?"Meridional":"Sagittal",ae=[{freq:G,label:"1st"},{freq:z,label:"2nd"}];for(const{freq:Ae,label:te}of ae){let re=0,se=1/0;for(let fe=0;fe<D.length;fe++){const we=Number(D[fe]);if(!Number.isFinite(we))continue;const Se=Math.abs(we-Ae);Se<se&&(se=Se,re=fe)}const ue=Number(U[re]),me=Number.isFinite(ue)?ue:null,ge=Ae.toFixed(1),xe=ee?`${oe} ${ge} lp/mm ${ee}`:`${oe} ${ge} lp/mm`;if(!K.has(xe)){const fe={x:[],y:[],type:"scatter",mode:typeof O?.mode=="string"&&O.mode?O.mode:"lines",name:xe,showlegend:!0};O?.line&&typeof O.line=="object"?fe.line={...O.line}:fe.line={width:2},O?.marker&&typeof O.marker=="object"&&(fe.marker={...O.marker}),K.set(xe,fe)}const W=K.get(xe);W.x.push(C),W.y.push(me)}}const T=Array.from(K.values());g.newPlot(h,T,le,{responsive:!0,displaylogo:!1});const j=Math.floor(60+(B+1)/V.length*35),P=Array.from(K.values()).map(O=>({...O,x:[...O.x],y:[...O.y]}));w(j,`Extracting MTF: ${B+1}/${V.length} (${K.size} traces)`,P,void 0)}const Z=Array.from(K.values());return Be("========== ✅ [Object MTF] FINAL RESULTS =========="),Be(`✅ Final trace count: ${Z.length}`),Be(`✅ Field points per trace: ${Z[0]?.x?.length||0}`),Be("✅ Trace names:",Z.map(B=>B.name)),Z.length>0&&(Be(`✅ First trace X-axis range: [${Z[0].x?.[0]}, ${Z[0].x?.[Z[0].x.length-1]}]`),Be(`✅ First trace Y-axis range: [${Math.min(...Z[0].y||[])}, ${Math.max(...Z[0].y||[])}]`)),Be("========== showFieldMTFDiagram COMPLETE =========="),w(98,"Finalizing plot...",void 0,void 0),g.newPlot(h,Z,le,{responsive:!0,displaylogo:!1}),w(100,"Done",void 0,void 0),Be(`✅ [Object MTF] Computed ${V.length} field points with ${M} steps`),{traces:Z,layout:le}}async function ia({wavelengthMicrons:e,objectIndex:t,maxFrequencyLpmm:n,samplingSize:o,samplingPoints:r,containerElement:s,onProgress:a,opdDisplayMode:c,defocusShiftMm:i,zeroPadTo:l,showDelta:f,timeoutMs:p}={}){const d=(x,T)=>{const j=Number(x);return Number.isFinite(j)?j:T};let y=(()=>{if(s)return s;const x=T=>{try{return document.getElementById(T)}catch{return null}};return x("mtf-container")||x("popup-mtf-container")||x("popup-through-focus-mtf-container")||null})();if(!y)try{if(!document?.body)throw new Error("document.body is unavailable");const x="mtf-comparison-container-auto",T=document.getElementById(x);y=T||document.createElement("div"),T||(y.id=x,y.style.position="fixed",y.style.right="16px",y.style.bottom="16px",y.style.width="760px",y.style.height="520px",y.style.background="#ffffff",y.style.border="1px solid #d0d0d0",y.style.boxShadow="0 4px 16px rgba(0,0,0,0.15)",y.style.zIndex="99999",y.style.borderRadius="6px",document.body.appendChild(y)),console.warn("⚠️ MTF container not found. Auto-created #mtf-comparison-container-auto for plotting.")}catch{throw new Error("MTF container element not found")}try{y.innerHTML=""}catch{}const m=y?.ownerDocument?.defaultView?.Plotly||(typeof window<"u"?window.Plotly:null);if(!m)throw new Error("Plotly is not available");const h=(x,T)=>{try{if(typeof a!="function")return;a({percent:x,message:T})}catch{}},g=typeof f=="boolean"?f:!0,w=Math.max(1e3,d(p,12e4)),R=(x,T=null)=>{try{typeof globalThis<"u"&&(globalThis.__mtfComparisonState={phase:x,at:Date.now(),extra:T})}catch{}},N=async(x,T,j)=>{let P=null;try{const O=new Promise((X,k)=>{P=setTimeout(()=>{k(new Error(`${j} timed out after ${T} ms`))},T)});return await Promise.race([x,O])}finally{try{P&&clearTimeout(P)}catch{}}};R("start"),h(2,"Running current MTF..."),R("current-mtf");const I=await N(fn({wavelengthMicrons:e,objectIndex:t,maxFrequencyLpmm:n,samplingSize:o,samplingPoints:r,containerElement:y,onProgress:null,opdDisplayMode:c,defocusShiftMm:i,skipPlot:!0,showDiffractionLimit:!1,zeroPadTo:l,legacyBaselineMode:!1}),w,"Current MTF run");h(45,"Running Feb-7 baseline-compatible MTF..."),R("legacy-mtf");const b=await N(fn({wavelengthMicrons:e,objectIndex:t,maxFrequencyLpmm:n,samplingSize:o,samplingPoints:r,containerElement:y,onProgress:null,opdDisplayMode:c,defocusShiftMm:i,skipPlot:!0,showDiffractionLimit:!1,zeroPadTo:l,legacyBaselineMode:!0}),w,"Feb-7 compatible MTF run"),A=Array.isArray(I?.traces)?I.traces:[],v=Array.isArray(b?.traces)?b.traces:[],S=x=>String(x||"").replace(/\s*\(.*\)\s*$/,"").trim(),_=x=>{const T=S(x).toLowerCase();return T==="tangential"||T==="sagittal"},M=A.filter(x=>_(String(x?.name??""))),G=v.filter(x=>_(String(x?.name??""))),z=new Map;for(const x of G)z.set(S(String(x?.name??"")).toLowerCase(),x);const E=(x,T,j)=>{if(!Array.isArray(x)||!Array.isArray(T)||x.length===0||T.length===0||x.length!==T.length||!Number.isFinite(j))return null;if(j<=Number(x[0])){const O=Number(T[0]);return Number.isFinite(O)?O:null}const P=x.length-1;if(j>=Number(x[P])){const O=Number(T[P]);return Number.isFinite(O)?O:null}for(let O=1;O<x.length;O++){const X=Number(x[O-1]),k=Number(x[O]);if(!(!Number.isFinite(X)||!Number.isFinite(k)||k===X)&&j<=k){const Y=Number(T[O-1]),D=Number(T[O]);if(!Number.isFinite(Y)||!Number.isFinite(D))return null;const U=(j-X)/(k-X);return Y+U*(D-Y)}}return null},L=[];let ne=0;for(const x of M){const T=S(String(x?.name??"")).toLowerCase(),j=z.get(T);if(!j)continue;const P=Array.isArray(x?.x)?x.x:[],O=Array.isArray(x?.y)?x.y:[],X=Array.isArray(j?.x)?j.x:[],k=Array.isArray(j?.y)?j.y:[],Y=S(String(x?.name??"MTF")),D=x?.line?.color;if(L.push({x:P,y:O,type:"scatter",mode:"lines",name:`${Y} (Current)`,line:{...x?.line||{},width:2.5}}),L.push({x:X,y:k,type:"scatter",mode:"lines",name:`${Y} (Feb-7 Compat)`,line:{color:D||"#888",width:1.75,dash:"dash"}}),g){const U=[],ee=[];for(let oe=0;oe<P.length;oe++){const ae=Number(P[oe]),Ae=Number(O[oe]);if(!Number.isFinite(ae)||!Number.isFinite(Ae))continue;const te=E(X,k,ae);if(!Number.isFinite(Number(te)))continue;const re=Ae-Number(te);U.push(ae),ee.push(re),ne=Math.max(ne,Math.abs(re))}L.push({x:U,y:ee,type:"scatter",mode:"lines",name:`${Y} Δ(Current-Compat)`,yaxis:"y2",line:{color:D||"#444",width:1.2,dash:"dot"}})}}const V=d(I?.maxPlotLpmmGlobal,0),K=d(b?.maxPlotLpmmGlobal,0),$=Math.max(V,K,0),J=typeof window<"u"&&typeof window.getPrimaryWavelength=="function"?Number(window.getPrimaryWavelength()):.5876,le=typeof e=="string"&&String(e).toLowerCase()==="all"?"All wavelengths":`${(d(e,J)*1e3).toFixed(1)} nm`,Z=Number.isFinite(Number(t))?Math.max(0,Math.floor(Number(t))):0,B=Math.max(.02,Math.min(.5,ne*1.2||.02)),C={title:`MTF Comparison (Current vs Feb-7 Compat, ${le}, Object ${Z})`,xaxis:{title:"Spatial frequency (lp/mm)",range:[0,$||0]},yaxis:{title:"MTF",range:[0,1.05]},margin:{l:60,r:g?60:20,t:50,b:50},legend:{orientation:"h"}};g&&(C.yaxis2={title:"ΔMTF",overlaying:"y",side:"right",range:[-B,B],zeroline:!0}),h(92,"Rendering comparison plot..."),R("rendering",{traceCount:L.length}),await N(m.newPlot(y,L,C,{responsive:!0,displaylogo:!1}),Math.max(1e3,Math.floor(w/2)),"MTF comparison plotting"),h(100,"Done");const F={curveCount:L.length,maxPlotLpmmGlobal:$,maxAbsDelta:ne,wavelengthMicrons:e,objectIndex:Z};try{typeof globalThis<"u"&&(globalThis.__lastMTFComparisonResult=F,globalThis.__mtfComparisonState={phase:"done",at:Date.now(),extra:F}),console.info("📊 [MTF Comparison] Summary:",F)}catch{}return{curveCount:L.length,traces:L,layout:C,maxPlotLpmmGlobal:$,maxAbsDelta:ne,current:I,legacy:b}}const sa=Object.freeze(Object.defineProperty({__proto__:null,showFieldMTFDiagram:ra,showMTFComparisonDiagram:ia,showMTFDiagram:fn,showThroughFocusMTFDiagram:Er},Symbol.toStringTag,{value:"Module"}));function Zl(){try{return typeof localStorage>"u"?null:Ot()}catch{return null}}function Xl(e){if(!e||!Array.isArray(e.configurations))return null;const t=e.activeConfigId;return e.configurations.find(n=>n&&String(n.id)===String(t))||e.configurations[0]||null}async function Kl(){const e=typeof localStorage<"u"?Zl():null,t=e?Xl(e):null,n=e&&Array.isArray(e.systemRequirements)?e.systemRequirements:[],o=e&&Array.isArray(e.meritFunction)?e.meritFunction:[],r=t&&Array.isArray(t.opticalSystem)&&t.opticalSystem.length?t.opticalSystem:Je(),s=t&&Array.isArray(t.object)&&t.object.length?t.object:ht(),a=Tt(),c=b=>{if(b===""||b===null||b===void 0)return null;const A=Number(b);return Number.isFinite(A)?A:null},i=b=>{const v=(b==null?"":String(b)).trim();return v===""?null:v},l=r.map((b,A)=>{const v=i(b?.type??b?.["object type"]??b?.object??b?._blockType??b?.surfType),_=i(b?.material??b?.glass)??"AIR",M=c(b?.radius),G=M===null?i(b?.radius):null,z=c(b?.semidia??b?.semiDiameter??b?.semiDia??b?.semi_diameter);return{surf:A,surf1:A+1,type:v,radius:M,radiusRaw:G,thickness:c(b?.thickness),material:_,isAir:_==="AIR",semidia:z,conic:c(b?.conic)??0,isImage:v?v.toLowerCase().includes("image"):!1,isObject:v?v.toLowerCase().includes("object"):!1}}),f=(b,A=1200)=>{const v=b==null?"":String(b);return v.length>A?v.slice(0,A)+"…":v},p=b=>{const A=b&&b.name?String(b.name):"Error",v=b&&b.message?String(b.message):String(b||"Unknown error"),S=b&&b.stack?String(b.stack):null;return{name:A,message:f(v,600),stack:S?f(S,1600):null}},d=(()=>{const b=[],A=Array.isArray(r)?r:[],v=Array.isArray(a)?a:[],S=Array.isArray(s)?s:[];A.length===0&&b.push({kind:"fatal",code:"NO_SURFACES",message:"optical system has 0 surfaces"}),v.length===0&&b.push({kind:"warn",code:"NO_SOURCES",message:"source table is empty"}),S.length===0&&b.push({kind:"warn",code:"NO_OBJECTS",message:"object table is empty"});for(let M=0;M<A.length;M++){const G=A[M]||{},z=c(G.thickness);if(z!==null&&z<0){b.push({kind:"warn",code:"NEGATIVE_THICKNESS",surf:M,message:`negative thickness at surf ${M}`});break}}const _=A.findIndex(M=>String(M?.type??"").toLowerCase().includes("stop"));return _>=0&&c(A[_]?.semidia??A[_]?.semiDiameter??A[_]?.semiDia)===null&&b.push({kind:"warn",code:"STOP_SEMIDIA_NULL",surf:_,message:"stop surface semidia is null/non-numeric"}),b})(),u={version:1,timestamp:new Date().toISOString(),inputs:{surfaceCount:Array.isArray(r)?r.length:0,sourceCount:Array.isArray(a)?a.length:0,objectCount:Array.isArray(s)?s.length:0},preflightIssues:d,steps:[]},y=(()=>{const b=Array.isArray(a)?a:[],A=v=>{const S=v==null?"":String(v).trim().toLowerCase();return S==="true"||S==="yes"||S==="1"||S==="primary"};for(const v of b)if(v&&(A(v.primary)||v.isPrimary===!0)){const S=c(v.wavelength);if(S!==null&&S>0)return S}for(const v of b){const S=c(v?.wavelength);if(S!==null&&S>0)return S}return .5875618})();u.inputs.primaryWavelengthUm=y;let m={},h=null,g=null;try{h=uo(r,a,s),u.steps.push({step:"paraxial",ok:!0})}catch(b){u.steps.push({step:"paraxial",ok:!1,error:p(b)})}try{g=vc(r,y,s),u.steps.push({step:"seidel",ok:!0})}catch(b){u.steps.push({step:"seidel",ok:!1,error:p(b)})}u.steps.some(b=>b&&b.ok===!1)?(console.warn("AI Context: Failed to calculate performance metrics",u),m={error:"Calculation failed",diagnostics:u}):m={focalLength:h?.focalLength??null,fNumber:h?.fNumber??null,magnification:h?.magnification??null,seidel:{spherical:g?.sumS1??null,coma:g?.sumS2??null,astigmatism:g?.sumS3??null,fieldCurvature:g?.sumS4??null,distortion:g?.sumS5??null,lca:g?.sumLch??null,tca:g?.sumTch??null},diagnostics:u};const R=(()=>{const b=typeof globalThis<"u"?globalThis:null;if(!b)return null;const A={};try{const v=Ir(b)||null,S=xc(b)||null;if(v||S){const z=S&&S.error?{message:f(S.error.message||S.error||"Wavefront error",600),code:S.error.code||null,details:S.error}:null,E=S&&S.statistics?{opdMicrons:S.statistics.opdMicrons?{rms:c(S.statistics.opdMicrons.rms),peakToPeak:c(S.statistics.opdMicrons.peakToPeak)}:null,rawOpdMicrons:S.statistics.raw?.opdMicrons?{rms:c(S.statistics.raw.opdMicrons.rms),peakToPeak:c(S.statistics.raw.opdMicrons.peakToPeak)}:null}:null;A.wavefront={meta:v?{source:v.source??null,gridSize:v.gridSize??null,wavelength:v.wavelength??null,fieldSetting:v.fieldSetting??null}:null,hasError:!!z,error:z,statistics:E}}const M=(b.lastWavefrontAnalyzer||null)?.opdCalculator||null,G=typeof M?.getLastRayCalculation=="function"?M.getLastRayCalculation():M?.lastRayCalculation??null;G&&(A.opdLastRay={success:G.success??null,error:G.error??null,fieldKey:G.fieldKey??null,pupilCoord:G.pupilCoord??null,stopHit:G.stopHit??null})}catch{}try{const v=b.lastPsfError||null;v&&(A.psfError={at:v.at??null,code:v.code??null,message:f(v.message??v.rawMessage??"PSF error",700),hint:f(v.hint??"",300)||null,wavelength:v.wavelength??null,gridSize:v.gridSize??null,objectIndex:v.objectIndex??null,debugMode:v.debugMode??null});const S=b.lastPsfResult||null;if(S){const _=S.metrics||S.characteristics||null;A.psfLastResult={wavelength:S.wavelength??b.lastPsfWavelength??null,gridSize:S.gridSize??b.lastPsfGridSize??null,calculationTime:S.calculationTime??null,hasMetrics:!!_,metricKeys:_?Object.keys(_).slice(0,30):[]}}}catch{}try{if(typeof localStorage<"u"){try{const v=Bl();if(v&&typeof v=="object"){if(!A.wavefront&&v.wavefront&&typeof v.wavefront=="object"){const S=v.wavefront;A.wavefront={meta:S.meta??null,hasError:!!S.hasError,error:S.error??null,statistics:S.statistics??null,from:v.from??"localStorage:lastWavefrontSnapshot"}}!A.opdLastRay&&v.opdLastRay&&typeof v.opdLastRay=="object"&&(A.opdLastRay={success:v.opdLastRay.success??null,error:v.opdLastRay.error??null,fieldKey:v.opdLastRay.fieldKey??null,pupilCoord:v.opdLastRay.pupilCoord??null,stopHit:v.opdLastRay.stopHit??null,from:v.from??"localStorage:lastWavefrontSnapshot"})}}catch{}if(!A.psfLastResult){const v=$l();v&&typeof v=="object"&&(A.psfLastResult={wavelength:v.wavelength??null,gridSize:v.gridSize??null,calculationTime:v.calculationTime??null,hasMetrics:!!v.hasMetrics,metricKeys:Array.isArray(v.metricKeys)?v.metricKeys.slice(0,30):[],psfMethod:v.psfMethod??null,performanceMode:v.performanceMode??null,zernikeFitSamplingSize:v.zernikeFitSamplingSize??null,objectIndex:v.objectIndex??null,psfSummary:v.psfSummary??null,at:v.at??null,from:"localStorage:lastPsfMeta"})}if(!A.psfError){const v=Wl();v&&typeof v=="object"&&(A.psfError={at:v.at??null,code:v.code??null,message:f(v.message??v.rawMessage??"PSF error",700),hint:f(v.hint??"",300)||null,wavelength:v.wavelength??null,gridSize:v.gridSize??null,objectIndex:v.objectIndex??null,debugMode:v.debugMode??null,from:"localStorage:lastPsfError"})}}}catch{}try{const v=typeof document<"u"?document:null;if(v){const S=v.getElementById("psf-object-select"),_=v.getElementById("psf-sampling-select"),M=v.getElementById("psf-zernike-sampling-select"),G=v.getElementById("psf-performance-select"),z=v.getElementById("psf-wasm-status");A.psfUI={objectIndex:S?.value??null,samplingSize:_?.value??null,zernikeFitSamplingSize:M?.value??null,performanceMode:G?.value??null,wasmStatusText:z?.textContent?f(z.textContent,200):null}}}catch{}try{const v=b.__lastSurfaceEdit||null;if(v){const S=v.row||{};A.lastUserSurfaceEdit={field:v.field??null,oldValue:v.oldValue??null,newValue:v.newValue??null,rowSummary:{type:i(S?.type??S?.surfType),radius:i(S?.radius),thickness:i(S?.thickness),material:i(S?.material??S?.glass),semidia:i(S?.semidia??S?.semiDiameter)}}}}catch{}return Object.keys(A).length?A:null})(),N=(()=>{const b=Array.isArray(n)?n:[],A=b.filter(S=>S&&typeof S=="object").filter(S=>S.enabled===void 0||S.enabled===null?!0:!!S.enabled).slice(0,60).map(S=>({id:S.id??null,enabled:S.enabled===void 0||S.enabled===null?!0:!!S.enabled,operand:S.operand??null,op:S.op??null,tol:c(S.tol),target:c(S.target),weight:c(S.weight)??1,configId:S.configId===void 0||S.configId===null?null:String(S.configId),rationale:i(S.rationale),current:c(S.current),status:i(S.status)})),v=(Array.isArray(o)?o:[]).filter(S=>S&&typeof S=="object").slice(0,40).map(S=>({id:S.id??null,operand:S.operand??null,target:c(S.target),weight:c(S.weight),configId:S.configId===void 0||S.configId===null?null:String(S.configId)}));return{requirements:{count:b.length,enabledCount:A.length,rows:A},meritFunction:{count:Array.isArray(o)?o.length:0,preview:v}}})(),I=(()=>{const b=(L,ne)=>{const V=Number(L),K=Number(ne);return!Number.isFinite(V)||!Number.isFinite(K)||K===0?null:V/K},A=m?.focalLength,v=m?.fNumber,S=N?.requirements?.rows,_=Array.isArray(S)?S.find(L=>String(L?.operand||"").trim()==="FL")?.target??null:null,M=Array.isArray(S)?S.find(L=>String(L?.operand||"").trim()==="FNO_WRK")?.target??S.find(L=>String(L?.operand||"").trim()==="FNO_IMG")?.target??S.find(L=>String(L?.operand||"").trim()==="FNO_OBJ")?.target??null:null,G=_!==null?b(A,_):null,z=M!==null?b(v,M):null,E=L=>L===null?null:(L-1)*100;return{focalLength:{current:typeof A=="number"?A:null,target:typeof _=="number"?_:null,ratioToTarget:G,percentError:E(G)},fNumber:{current:typeof v=="number"?v:null,target:typeof M=="number"?M:null,ratioToTarget:z,percentError:E(z)}}})();return{meta:{surfIndexing:"0-based",surfaceCount:l.length,activeConfig:t?{id:t.id??null,name:t.name??null,activeScenarioId:t.activeScenarioId??null}:null},system:l,designIntent:t&&Array.isArray(t.blocks)?{blockCount:t.blocks.length,blocks:t.blocks.slice(0,80).map(b=>({blockId:b?.blockId??null,blockType:b?.blockType??null,parameters:{glass:i(b?.parameters?.glass??b?.parameters?.material)??null,radius:c(b?.parameters?.radius),thickness:c(b?.parameters?.thickness),semiDiameter:c(b?.parameters?.semiDiameter)},variables:b?.variables?Object.keys(b.variables).slice(0,30):[]}))}:null,sources:a.map(b=>({id:c(b?.id),wavelength:c(b?.wavelength),weight:c(b?.weight),primary:i(b?.primary),angle:c(b?.angle)})),objects:s.map(b=>({id:c(b?.id),position:i(b?.position),xHeightAngle:c(b?.xHeightAngle),yHeightAngle:c(b?.yHeightAngle),angle:c(b?.angle)})),targets:N,normalizedPerformance:I,performance:m,runtimeDiagnostics:R,timestamp:new Date().toISOString()}}const aa="ai_assistant_config",Or="ai_assistant_history",ca={provider:"gemini",apiKey:"",model:""};let St={...ca},zt=[],ji=!1,Fo=null,la=1,Sr=!1;const ua=6,Xo=12e4,pa=18e4;try{typeof window<"u"&&(window.__COOPT_AI_ASSISTANT_VERSION||(window.__COOPT_AI_ASSISTANT_VERSION="co-opt 1.9.6 (ai-assistant) 2026-01-05"),typeof window.DEFAULT_AI_REQUEST_TIMEOUT_MS>"u"&&(window.DEFAULT_AI_REQUEST_TIMEOUT_MS=Xo),typeof window.DEFAULT_AI_STREAM_TIMEOUT_MS>"u"&&(window.DEFAULT_AI_STREAM_TIMEOUT_MS=pa))}catch{}function Ko(e,t={},n=Xo){const o=new AbortController,r=setTimeout(()=>{try{o.abort()}catch{}},n),s={...t,signal:o.signal};return fetch(e,s).catch(a=>{throw String(a?.name||"")==="AbortError"?new Error(`Request timed out after ${n}ms`):a}).finally(()=>{try{clearTimeout(r)}catch{}})}let Li=!1;function Jl(){if(Li)return;Li=!0;const e=(t,n)=>{const o=n&&typeof n=="object"&&"message"in n?String(n.message):String(n),r=n&&typeof n=="object"&&"stack"in n?String(n.stack):"",s=[`❌ ${t}: ${o}`,r?`
${r}`:""].join("");try{document.getElementById("ai-chat-history")&&Do("assistant",s)}catch{}try{console.error(`[AI Assistant] ${t}:`,n)}catch{}};try{window.addEventListener("error",t=>{e("Unhandled error",t?.error||t?.message||t)}),window.addEventListener("unhandledrejection",t=>{e("Unhandled promise rejection",t?.reason||t)})}catch{}}const Bi=`あなたは「co-opt」（ブラウザで動作する光学設計・解析ツール）の熟練した光学設計コンサルタントです。
ユーザーの目的に合わせて、設計・解析・最適化の方針を日本語で簡潔に提案してください。

重要:
- co-opt の面番号は UI 上は Surf 0..N-1 の 0-index です（context.system[].surf がそれ）。
- context.system[].surf1 は参考用の 1-index です。面番号の混同を避けてください。
- 最終行が像面（Image）で thickness が null/空でも、それ自体は必ずしもエラーではありません。
    エラー断定はせず、必要なら「どの計算が失敗したか」「どの入力が欠けているか」を確認する質問をしてください。
        - もし context.performance.error があり、かつ context.performance.diagnostics がある場合は、それを最優先の根拠にしてください。
            特に diagnostics.steps（paraxial / seidel 等）の ok=false と error.message/stack を引用し、「どの計算が失敗したか」を明確に述べてください。
            diagnostics.preflightIssues がある場合は、推測ではなくその指摘（例: NEGATIVE_THICKNESS 等）に沿って確認手順を提案してください。
        - もし diagnostics.steps がすべて ok=true なのにユーザーが「計算が失敗する」と言っている場合、原因は paraxial/seidel 以外（例: Wavefront/OPD/PSF/Spot/最適化）にある可能性が高いです。
            その場合は context.runtimeDiagnostics（wavefront/opdLastRay/psfError など）があれば必ず引用し、どの機能が失敗しているかを切り分けてください。
- context.system[].material が "AIR" の行は「空気（材料指定なし）」として正常です。ガラス未指定と断定しないでください。
- Design Intent（blocks）上の material 系キーはガラス名の文字列です（例: Lens.material, Doublet.material1/material2, Triplet.material1/material2/material3）。
    - これらは数値ではなく離散(categorical)変数として Optimize(V) 対象にできます。"non-numeric" だからといって最適化不可とは限りません。
    - ただし Lens/Doublet/Triplet の material* に "AIR" を提案・設定しないでください（空気はレンズ材として不正）。
    - 材料変更は set_block_param で parameters 側（key=material/material1/material2/material3）を更新してください。
- object は height ではなく xHeightAngle / yHeightAngle（フィールド定義）を主に使います。
- 口径は context.system[].semidia（光学系テーブルの semidia 列）です。
- 半径は context.system[].radius が数値、数値でない場合は context.system[].radiusRaw（例: "INF"）になります。null=欠落と決めつけないでください。

設計変更について:
- Design Intent（blocks）を最優先に維持してください。基本はブロックパラメータ更新→展開（Expanded Optical System）です。
- ユーザーが「〜を〜に変更」「set_*」「Surf/面番号指定」「semidia/semiDiameter/radius/thickness/material 等の値変更」を求めた場合は、必ずツール関数を呼んで“実際に反映”してください。文章だけで「変更します」と宣言して終わらないでください。
- blockId が不明でも、Surf（0-index）が指定されていれば set_block_param の surf を使って対象ブロックを解決できます。
- blocks が存在する場合は set_block_param を必ず優先し、set_surface_field は「blocks が無い場合のみ」のフォールバックとして使ってください。
- 「全てのconfig / all configurations」に同じ変更を適用したい場合は、set_block_param に applyToAllConfigs=true を指定してください（blockId が config 間で一致しない場合があるので、surf 指定が安全です）。
- 変更できない/失敗した場合は、理由を1行で述べ、次に必要な情報を最大1つだけ質問してください。
- 変更しない場合は、適用可能な具体値を含む提案（または ACTION_PLAN_JSON）を返してください。

出力:
- まず「現状のボトルネック（根拠=contextの値）」→「修正方針」→「具体的な数値案」の順で。
- context.performance.diagnostics がある場合、回答冒頭に「診断要約」を2〜6行で書いてください。
`,zi="ACTION_PLAN_JSON",da="COOPT_AI_REFRESH_UI";let $i=!1;function Ql(){if(!$i){$i=!0;try{window.addEventListener("message",e=>{try{const t=e?.data;if(!t||typeof t!="object"||t.type!==da)return;ma(window)}catch{}})}catch{}}}async function ma(e){const t=e;if(t){try{const n=t.ConfigurationManager,o=n&&typeof n.loadActiveConfigurationToTables=="function"?n.loadActiveConfigurationToTables:typeof t.loadActiveConfigurationToTables=="function"?t.loadActiveConfigurationToTables:null;o&&await o.call(n||t,{applyToUI:!0,suppressOpticalSystemDataChanged:!0})}catch{}try{kn(t)}catch{}try{t.meritFunctionEditor&&typeof t.meritFunctionEditor.calculateMerit=="function"&&t.meritFunctionEditor.calculateMerit()}catch{}try{t.systemRequirementsEditor&&typeof t.systemRequirementsEditor.scheduleEvaluateAndUpdate=="function"&&t.systemRequirementsEditor.scheduleEvaluateAndUpdate()}catch{}}}function eu(e){const t=String(e||"");return[/\bset_(block_param|surface_field)\b/i,/\bSurf\s*\d+\b/i,/semidia|semi\s*dia(meter)?/i,/\b(radius|thickness|material|glass|conic|coef)\b/i,/変更|変(更|えて|える|えたい)/].some(o=>o.test(t))}function tu(e,t){const n=Number(t);if(!Number.isInteger(n)||n<=0)return null;const o=e?.blocks;if(!Array.isArray(o)||o.length===0)return null;const s=o.filter(c=>c&&(c.blockType==="Lens"||c.blockType==="PositiveLens"||c.blockType==="Paraxial"))[n-1];return(typeof s?.blockId=="string"?s.blockId.trim():"")||null}async function nu(e){const t=String(e||"");if(!/(非球面|aspher(e|ic))/i.test(t))return null;const n=/(全(て)?|全部|すべて|all)\s*(system\s*)?config/i.test(t),o=t.match(/(?:\bLens\b|レンズ)\s*[-#]?\s*(\d+)\s*(?:の)?\s*R\s*([12])\b/i);if(!o)return null;const r=Number(o[1]),s=Number(o[2]);if(!Number.isInteger(r)||r<=0||!(s===1||s===2))return null;const a=Bn(),c=zn(a);if(!a||!c||!Array.isArray(c.blocks)||c.blocks.length===0)return null;const i=s===1?"front":"back",l=p=>{try{const d=tu(p,r);if(!d)return{ok:!1,configId:p?.id,error:`Lens-${r} not found in blocks`};const u=mo(p.blocks),y=Array.isArray(u)?u.find(w=>w&&String(w.blockId)===String(d)):null;if(!y)return{ok:!1,configId:p?.id,error:`blockId not found: ${d}`};It(y.parameters)||(y.parameters={}),y.parameters[`${i}SurfType`]="Aspheric even",y.parameters[`${i}Conic`]=0,p.blocks=u;const m=ti(p,a),h=Array.isArray(p?.opticalSystem)?p.opticalSystem:null,g=at(p.blocks);return g&&Array.isArray(g.rows)&&(h&&h.length>0&&Jo(g.rows,h),m!==void 0&&g.rows[0]&&typeof g.rows[0]=="object"&&(g.rows[0].thickness=m),p.opticalSystem=g.rows),{ok:!0,configId:p?.id,blockId:d}}catch(d){return{ok:!1,configId:p?.id,error:String(d?.message||d)}}};if(n){const p=[];for(const y of a.configurations||[])p.push(l(y));await wn(a);const d=p.filter(y=>y.ok).length,u=p.length-d;return d>0&&u===0?`全configの Lens-${r} の R${s}（${i}）を非球面（Aspheric even）に設定しました。`:`全configへ適用: ok=${d}, ng=${u}。Lens-${r} が存在しないconfigがある可能性があります。`}return l(c).ok?(await wn(a),`Lens-${r} の R${s}（${i}）を非球面（Aspheric even）に設定しました。`):`⚠️ Lens-${r} を blocks から特定できませんでした。`}function ou(){ji||(ji=!0,iu(),au(),lu(),pu(),mu(),fu(),ru(),Ql(),Jl())}function ru(){const e=document.getElementById("ai-assistant-panel");if(!e)return;const t=e.querySelector(".ai-panel-header");if(!(t instanceof HTMLElement))return;let n=!1,o=0,r=0,s=0,a=0,c=0,i=0,l=null;const f=(y,m,h)=>Math.max(m,Math.min(h,y)),p=y=>{if(!(y instanceof PointerEvent)||y.button!==0)return;const m=y.target;if(m instanceof HTMLElement&&(m.closest(".ai-panel-controls")||m.tagName==="BUTTON"))return;const h=e.getBoundingClientRect();c=h.width,i=h.height,e.style.right="auto",e.style.bottom="auto",e.style.left||(e.style.left=`${h.left}px`),e.style.top||(e.style.top=`${h.top}px`),s=Number.parseFloat(e.style.left)||h.left,a=Number.parseFloat(e.style.top)||h.top,o=y.clientX,r=y.clientY,n=!0,l=y.pointerId;try{t.setPointerCapture(l)}catch{}y.preventDefault()},d=y=>{if(!(y instanceof PointerEvent)||!n||l!==null&&y.pointerId!==l)return;const m=y.clientX-o,h=y.clientY-r,g=Math.max(0,window.innerWidth-c),w=Math.max(0,window.innerHeight-i),R=f(s+m,0,g),N=f(a+h,0,w);e.style.left=`${R}px`,e.style.top=`${N}px`},u=y=>{if(y instanceof PointerEvent&&n&&!(l!==null&&y.pointerId!==l)){n=!1;try{t.releasePointerCapture(l)}catch{}l=null}};t.addEventListener("pointerdown",p),t.addEventListener("pointermove",d),t.addEventListener("pointerup",u),t.addEventListener("pointercancel",u)}function iu(){const e=localStorage.getItem(aa);if(e)try{St={...ca,...JSON.parse(e)}}catch(t){console.error("Failed to parse AI config",t)}}function su(){localStorage.setItem(aa,JSON.stringify(St))}function au(){const e=localStorage.getItem(Or);if(e)try{const t=JSON.parse(e),n=cu(t);zt=n.history,la=n.nextId}catch(t){console.error("Failed to parse AI history",t)}}function cu(e){const t=Array.isArray(e)?e:[];let n=0;for(const s of t){const a=Number(s?.id);Number.isFinite(a)&&a>n&&(n=a)}const o=new Set,r=[];for(const s of t){if(!s||typeof s!="object")continue;let a=Number(s.id);(!Number.isFinite(a)||a<=0||o.has(a))&&(n+=1,a=n),o.add(a);const c=s.role==="assistant"||s.role==="user"||s.role==="system"?s.role:"assistant",i=s.content===void 0||s.content===null?"":String(s.content),l=Number(s.timestamp),f=Array.isArray(s.actions)?s.actions:null;r.push({id:a,role:c,content:i,timestamp:Number.isFinite(l)?l:Date.now(),actions:f})}return{history:r,nextId:Math.max(1,n+1)}}function ei(){try{localStorage.setItem(Or,JSON.stringify(zt));return}catch(e){try{console.warn("[AI Assistant] saveHistory failed; pruning history",e)}catch{}}try{Array.isArray(zt)&&zt.length>200&&(zt=zt.slice(-200)),localStorage.setItem(Or,JSON.stringify(zt))}catch(e){try{console.warn("[AI Assistant] saveHistory retry failed; disabling persistence for this session",e)}catch{}}}function lu(){if(document.getElementById("ai-assistant-btn"))return;const e=document.getElementById("optimize-design-intent-btn"),t=e?.parentElement||document.querySelector(".top-buttons-row");if(!t)return;const n=document.createElement("button");n.id="ai-assistant-btn",n.textContent="🤖 AI Assistant",n.title="Open AI Optical Consultant",n.onclick=()=>{uu()||ga()},e&&e.parentElement===t?t.insertBefore(n,e.nextSibling):t.appendChild(n)}function uu(){try{if(Fo&&!Fo.closed)return Fo.focus(),!0;const e=Math.min(720,Math.max(520,Math.floor(window.innerWidth*.45))),t=Math.min(900,Math.max(640,Math.floor(window.innerHeight*.8))),n=Math.max(0,Math.floor((window.screenX||0)+(window.outerWidth-e)/2)),o=Math.max(0,Math.floor((window.screenY||0)+(window.outerHeight-t)/2)),r=[`width=${e}`,`height=${t}`,`left=${n}`,`top=${o}`,"resizable=yes","scrollbars=yes"].join(","),s=window.open("","coopt-ai-assistant",r);if(!s||!s.document){try{s?.close()}catch{}return!1}Fo=s;const c=`<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <base href="${new URL(".",window.location.href).toString()}" />
    <title>AI Assistant</title>
    <link rel="stylesheet" href="styles.css" />
</head>
<body>
    <script>window['__AI_ASSISTANT_MODE'] = 'popup';<\/script>
    <script type="module">
        import { initAIAssistantPopup } from './ai-assistant.ts';
        initAIAssistantPopup();
    <\/script>
</body>
</html>`;try{s.document.open()}catch{}try{s.document.write(c)}catch{}try{s.document.close()}catch{}try{s.focus()}catch{}return!0}catch(e){return console.warn("Failed to open AI Assistant popup:",e),!1}}function pu(){document.body.insertAdjacentHTML("beforeend",`
        <div id="ai-settings-modal" class="modal">
            <div class="modal-content">
                <span class="close-modal">&times;</span>
                <h2>AI Assistant Settings</h2>
                <div class="form-group">
                    <label for="ai-provider">Provider:</label>
                    <select id="ai-provider">
                        <option value="gemini">Google Gemini</option>
                        <option value="openai">OpenAI</option>
                        <option value="anthropic">Anthropic</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="ai-model-select">Model:</label>
                    <select id="ai-model-select"></select>
                    <input type="text" id="ai-model-custom" placeholder="Custom model id" style="display:none; margin-top:6px;">
                </div>
                <div class="form-group">
                    <label for="ai-api-key">API Key:</label>
                    <input type="password" id="ai-api-key" placeholder="Enter your API Key">
                </div>
                <div class="modal-actions">
                    <button id="save-ai-settings-btn">Save Settings</button>
                </div>
            </div>
        </div>
    `)}function du(e){const t=String(e||"").trim();return t==="openai"?[{value:"",label:"(Auto)"},{value:"gpt-4o-mini",label:"gpt-4o-mini"},{value:"gpt-4o",label:"gpt-4o"},{value:"__custom__",label:"Custom…"}]:t==="anthropic"?[{value:"",label:"(Auto)"},{value:"claude-3-5-sonnet-latest",label:"claude-3-5-sonnet-latest"},{value:"claude-3-5-haiku-latest",label:"claude-3-5-haiku-latest"},{value:"__custom__",label:"Custom…"}]:[{value:"",label:"(Auto: ListModels)"},{value:"gemini-1.5-flash",label:"gemini-1.5-flash"},{value:"gemini-1.5-pro",label:"gemini-1.5-pro"},{value:"gemini-2.0-flash",label:"gemini-2.0-flash"},{value:"__custom__",label:"Custom…"}]}function Wi({provider:e,options:t,desiredModel:n}){const o=document.getElementById("ai-model-select"),r=document.getElementById("ai-model-custom");if(!(o instanceof HTMLSelectElement)||!(r instanceof HTMLInputElement))return;const s=Array.isArray(t)?t:[];o.innerHTML=s.map(l=>`<option value="${String(l.value)}">${ya(String(l.label))}</option>`).join("");const a=String(n||"").trim(),c=a!==""&&s.some(l=>String(l.value)===a);a===""?(o.value="",r.style.display="none",r.value=""):c?(o.value=a,r.style.display="none",r.value=""):(o.value="__custom__",r.style.display="",r.value=a),o.value==="__custom__"?(r.style.display="",r.value||(r.value=a)):r.style.display="none"}async function Ui({provider:e,apiKey:t,desiredModel:n}){const o=String(e||"").trim();if(Wi({provider:o,options:du(o),desiredModel:n}),o==="gemini"){const r=String(t||"").trim();if(!r)return;try{const a=(await ha(r)).map(i=>String(i).replace(/^models\//,"")).filter(Boolean),c=[{value:"",label:"(Auto: ListModels)"}].concat(a.slice(0,60).map(i=>({value:i,label:i}))).concat([{value:"__custom__",label:"Custom…"}]);Wi({provider:o,options:c,desiredModel:n})}catch{}}}function mu(){try{if(document.getElementById("ai-assistant-panel")){$o();return}}catch{}document.body.insertAdjacentHTML("beforeend",`
        <div id="ai-assistant-panel" class="ai-panel" style="display: none;">
            <div class="ai-panel-header">
                <h3>AI Optical Consultant</h3>
                <div class="ai-panel-controls">
                    <button id="ai-settings-open-btn" title="Settings">⚙️</button>
                    <button id="ai-clear-chat-btn" title="Clear Chat">🗑️</button>
                    <button id="ai-close-panel-btn" title="Close">✖️</button>
                </div>
            </div>
            <div id="ai-chat-history" class="ai-chat-history">
                <!-- Messages will appear here -->
            </div>
            <div class="ai-input-area">
                <textarea id="ai-user-input" placeholder="Ask about your optical system... (e.g., 'Why is the spot size large?')"></textarea>
                <button id="ai-send-btn">Send</button>
            </div>
        </div>
    `),$o()}function fu(){const e=document.getElementById("ai-settings-modal"),t=e?.querySelector?.(".close-modal"),n=document.getElementById("save-ai-settings-btn"),o=document.getElementById("ai-settings-open-btn"),r=document.getElementById("ai-provider"),s=document.getElementById("ai-api-key"),a=document.getElementById("ai-model-select"),c=document.getElementById("ai-model-custom");o&&e&&(o.onclick=()=>{r&&(r.value=St.provider),s&&(st()?(s.value="",s.disabled=!0,s.placeholder="Desktop mode: API key is managed by Rust backend env vars"):(s.disabled=!1,s.value=St.apiKey)),e.style.display="block",Ui({provider:r?.value,apiKey:s?.value,desiredModel:St.model})}),t&&e&&(t.onclick=()=>e.style.display="none"),e&&window.addEventListener("click",u=>{try{u&&u.target===e&&(e.style.display="none")}catch{}}),n&&e&&(n.onclick=()=>{St.provider=r?.value||"gemini";const u=a?.value??"",y=u==="__custom__"?c?.value??"":u;St.model=String(y||"").trim(),St.apiKey=st()?"":s?.value??"",su(),e.style.display="none",alert("AI Settings Saved!")}),r&&r.addEventListener("change",()=>{Ui({provider:r.value,apiKey:s?.value,desiredModel:""})}),a&&c&&a.addEventListener("change",()=>{c.style.display=a.value==="__custom__"?"":"none"});const i=document.getElementById("ai-close-panel-btn");i&&(i.onclick=ga);const l=document.getElementById("ai-clear-chat-btn");l&&(l.onclick=()=>{confirm("Clear chat history?")&&(zt=[],ei(),$o())});const f=document.getElementById("ai-send-btn"),p=document.getElementById("ai-user-input"),d=async()=>{try{const u=p?.value?.trim?.()||"";if(!u)return;Do("user",u),p&&(p.value="");const y=Do("assistant","");ut(y,{content:"Collecting system context..."});let m={};try{m=await Promise.race([Kl(),new Promise((M,G)=>setTimeout(()=>G(new Error("getSystemContext timed out")),15e3))])}catch(M){console.error("Failed to get system context",M),m={error:"Failed to retrieve system data"}}ut(y,{content:"Thinking..."});const h=gu(u,m);if(h){ut(y,{content:h});return}const g=await bu(u);if(g){ut(y,{content:g});return}const w=await nu(u);if(w){ut(y,{content:w});return}const R=JSON.stringify(m),N={role:"system",content:Bi},I={role:"user",content:`CONTEXT_JSON (do not edit; read-only):
${R}`},b={role:"user",content:`ユーザーの要望: ${u}`},A=[N,I,b],v=eu(u),S=Bi,_=[{role:"user",parts:[{text:`CONTEXT_JSON (do not edit; read-only):
${R}`}]},{role:"user",parts:[{text:`ユーザーの要望: ${u}`}]}];if(console.log("Sending to AI:",{provider:St.provider,model:St.model||"(auto)",userText:u}),st()){try{const M=await Ac({provider:St.provider,model:St.model||"auto",userMessage:u});ut(y,{content:M?.answer||"(no response)"})}catch(M){const G=M&&typeof M=="object"&&"message"in M?String(M.message):String(M);ut(y,{content:`❌ Desktop AI command failed: ${G}`})}return}if(!St.apiKey){ut(y,{content:"⚠️ Please configure your API Key in settings (⚙️)."});return}try{St.provider==="openai"?await qu({messages:A,apiKey:St.apiKey,model:St.model,thinkingMsgId:y,forceToolCall:v}):St.provider==="gemini"?await Cu({systemInstruction:S,contents:_,apiKey:St.apiKey,model:St.model,thinkingMsgId:y,forceToolCall:v}):ut(y,{content:"Provider not supported yet."})}catch(M){console.error("AI API Error:",M);const G=M&&typeof M=="object"&&"message"in M?String(M.message):String(M),z=M&&typeof M=="object"&&"stack"in M?String(M.stack):"";ut(y,{content:`❌ Error: ${G}${z?`

${z}`:""}`})}}catch(u){console.error("[AI Assistant] handleSend failed:",u);const y=u&&typeof u=="object"&&"message"in u?String(u.message):String(u),m=u&&typeof u=="object"&&"stack"in u?String(u.stack):"";try{Do("assistant",`❌ Internal error: ${y}${m?`

${m}`:""}`)}catch{}}};f&&(f.onclick=d),p&&(p.onkeydown=u=>{if(!(!u||u.isComposing)&&u.key==="Enter"&&(u.metaKey||u.ctrlKey)){u.preventDefault(),d();return}})}function fa(){Sr||(Sr=!0,requestAnimationFrame(()=>{Sr=!1;try{$o()}catch{}}))}function ut(e,t){const n=zt.findIndex(o=>o&&o.id===e);n<0||(zt[n]={...zt[n],...t},ei(),fa())}function zo(e){return!Array.isArray(e)||e.length===0?"":[`✅ Applied ${e.length} change(s).`,"","---","(適用ログ)",...e.map(t=>`- ${t}`),"---",""].join(`
`)}function gu(e,t){const n=wu(e),o=vu(e),r=xu(e),s=yu(e);if(!n&&!o&&!r&&!s)return null;if(s)return hu(t);const a=t?.runtimeDiagnostics;if(!a)return["runtimeDiagnostics が context に存在しません。","この質問に答えるには、まず PSF 計算を実行してから AI を呼び出してください。"].join(`
`);const c=[];if(n){const i=a.psfLastResult||null,l=a.psfError||null,f=a.psfUI||null;c.push("PSF（runtimeDiagnostics.psfLastResult）要約（引用ベース）:"),i?(c.push("- psfLastResult:"),c.push(Yn(i,["from","at","wavelength","objectIndex","psfMethod","performanceMode","psfSamplingSize","zernikeFitSamplingSize","gridSize","calculationTime","calculationTimeMs","hasMetrics","metricKeys","psfSummary"])),(i.hasMetrics===!1||Array.isArray(i.metricKeys)&&i.metricKeys.length===0)&&c.push("補足: この実行結果にはメトリクスが含まれていません（hasMetrics=false または metricKeys が空）。")):c.push("- psfLastResult: null（PSFの直近成功結果が保存されていません）"),l&&(c.push("- psfError（直近エラー）:"),c.push(Yn(l,["from","at","code","message","hint","wavelength","objectIndex","gridSize","psfSamplingSize","zernikeFitSamplingSize"]))),f&&(c.push("- psfUI（取得できたUI値）:"),c.push(Yn(f,["objectIndex","samplingSize","zernikeFitSamplingSize","performanceMode","wasmStatusText"]))),c.push("次の確認:"),c.push("- PSF をもう一度実行し、psfLastResult.psfSummary が入るか（または hasMetrics/metricKeys が変わるか）を確認"),c.push("- もし psfError が出ている場合は、まず psfError.message/hint の内容に沿って切り分け")}if(o){const i=a.wavefront||null;c.push("Wavefront（runtimeDiagnostics.wavefront）要約（引用ベース）:"),i?(c.push("- wavefront:"),c.push(Yn(i,["from","meta","hasError","error","statistics"])),i?.hasError&&c.push("補足: wavefront.hasError=true なので wavefront.error を根拠に切り分けしてください。")):c.push("- wavefront: null（直近の波面/OPDマップ結果が保存されていません）")}if(r){const i=a.opdLastRay||null;c.push("OPD last ray（runtimeDiagnostics.opdLastRay）要約（引用ベース）:"),i?(c.push("- opdLastRay:"),c.push(Yn(i,["success","error","fieldKey","pupilCoord","stopHit"]))):c.push("- opdLastRay: null（直近のOPDレイ計算が保存されていません）")}if(o||r){const i=o&&!a.wavefront,l=r&&!a.opdLastRay;(i||l)&&(c.push("次の確認:"),i&&c.push("- 波面/光路差表示を一度実行し、runtimeDiagnostics.wavefront が埋まるか確認"),l&&c.push("- Show wavefront diagram を一度実行し、runtimeDiagnostics.opdLastRay が埋まるか確認"))}return c.join(`
`)}function yu(e){if(!e)return!1;const t=String(e);return t.includes("現在の光学系")||t.includes("この光学系")||t.includes("光学系について説明")||t.includes("システムについて説明")||/describe\s+(the\s+)?(current\s+)?optical\s+system/i.test(t)}function hu(e){if(!e||typeof e!="object")return"context が取得できませんでした。";const t=[],o=(e.meta||{}).activeConfig||null,r=Array.isArray(e.system)?e.system:[],s=Array.isArray(e.sources)?e.sources:[],a=Array.isArray(e.objects)?e.objects:[],c=e.performance||{},i=c?.diagnostics||null,l=e.targets||null,f=e.designIntent||null;if(t.push("光学系サマリ（CONTEXT_JSONより）:"),o){const g=o.name?String(o.name):"(no name)",w=o.id===null||o.id===void 0?"(no id)":String(o.id),R=o.activeScenarioId===null||o.activeScenarioId===void 0?null:String(o.activeScenarioId);t.push(`- Active config: ${g} (id=${w}${R?`, scenario=${R}`:""})`)}t.push(`- Surface count: ${r.length} (Surf 0..${Math.max(0,r.length-1)})`);const p=r.findIndex(g=>g&&(g.isObject||String(g.type||"").toLowerCase().includes("object"))),d=r.findIndex(g=>g&&(g.isImage||String(g.type||"").toLowerCase().includes("image"))),u=r.findIndex(g=>g&&String(g.type||"").toLowerCase().includes("stop"));if(p>=0&&t.push(`- Object surface: Surf ${p}`),u>=0){const g=r[u]?.semidia;t.push(`- Stop: Surf ${u}${typeof g=="number"?` (semidia=${g})`:""}`)}d>=0&&t.push(`- Image surface: Surf ${d}`);const y=new Set;let m=0;for(const g of r){const w=g&&g.material?String(g.material):"AIR";w==="AIR"?m++:y.add(w)}if(t.push(`- Media: AIR rows=${m}, glass kinds=${y.size}${y.size?` (${Array.from(y).slice(0,6).join(", ")}${y.size>6?", …":""})`:""}`),f&&typeof f=="object"){const g=typeof f.blockCount=="number"?f.blockCount:null;t.push(`- Design Intent (blocks): ${g!==null?g:"n/a"} block(s)`)}else t.push("- Design Intent (blocks): not available (surface-only / import mode の可能性)");if(s.length){const g=s.map(I=>I?.wavelength).filter(I=>typeof I=="number"&&Number.isFinite(I)),w=s.find(I=>String(I?.primary||"").toLowerCase().includes("primary"))?.wavelength,R=g.length?Math.min(...g):null,N=g.length?Math.max(...g):null;t.push(`- Sources: ${s.length} (λ: ${R!==null?R:"n/a"} .. ${N!==null?N:"n/a"} μm${typeof w=="number"?`, primary=${w} μm`:""})`)}else t.push("- Sources: 0");if(a.length){const g=Array.from(new Set(a.map(w=>String(w?.position||"")).filter(Boolean)));t.push(`- Objects/Fields: ${a.length}${g.length?` (position: ${g.join(", ")})`:""}`)}else t.push("- Objects/Fields: 0");if(i&&Array.isArray(i.steps)&&i.steps.some(g=>g&&g.ok===!1)){const g=i.steps.filter(w=>w&&w.ok===!1);t.push("診断要約:");for(const w of g.slice(0,3)){const R=w?.error?.message?String(w.error.message):"(no message)";t.push(`- 計算失敗: ${String(w.step)}: ${R}`)}g.length>3&&t.push(`- (他 ${g.length-3} 件)`)}else{const g=typeof c.focalLength=="number"?c.focalLength:null,w=typeof c.fNumber=="number"?c.fNumber:null,R=typeof c.magnification=="number"?c.magnification:null;t.push("近軸/Seidel（計算できた範囲）:"),t.push(`- focalLength: ${g!==null?g:"n/a"}`),t.push(`- fNumber: ${w!==null?w:"n/a"}`),t.push(`- magnification: ${R!==null?R:"n/a"}`);const N=c?.seidel||{};["spherical","coma","astigmatism","fieldCurvature","distortion","lca","tca"].some(A=>typeof N[A]=="number")&&(t.push(`- seidel sums: S1=${N.spherical??"n/a"}, S2=${N.coma??"n/a"}, S3=${N.astigmatism??"n/a"}, S4=${N.fieldCurvature??"n/a"}, S5=${N.distortion??"n/a"}`),(typeof N.lca=="number"||typeof N.tca=="number")&&t.push(`- chromatic: LCA=${N.lca??"n/a"}, TCA=${N.tca??"n/a"}`))}const h=l?.requirements?.rows;if(Array.isArray(h)&&h.length){const g=h.filter(w=>typeof w?.current=="number"||w?.status);t.push(`Requirements: enabled=${h.length}${g.length?` (evaluated=${g.length})`:""}`);for(const w of g.slice(0,6)){const R=w.op?String(w.op):"",N=typeof w.target=="number"?w.target:"n/a",I=typeof w.current=="number"?w.current:"n/a",b=w.status?String(w.status):"";t.push(`- ${String(w.operand)} ${R} target=${N} current=${I}${b?` status=${b}`:""}`)}}return t.push("次にできること:"),t.push("- どの評価結果（Spot/PSF/Wavefront）を根拠に改善するかを指定すると、blocks優先で具体的な変更案を出せます。"),t.join(`
`)}async function bu(e){const t=String(e||""),n=/(全て|すべて|全部|全\s*config|全\s*コンフィグ|全\s*構成|all|every)/i.test(t),o=/(config|configs|configuration|configurations|system\s*config|sytem\s*config|コンフィグ|構成)/i.test(t);if(!(n&&o))return null;const r=t.match(/\bSurf\s*(\d+)\b/i)||t.match(/面\s*(\d+)/);if(!r)return null;const s=Number(r[1]);if(!Number.isInteger(s)||s<0||!/stop/i.test(t)||!/semidia|semi\s*dia(meter)?/i.test(t))return null;const a=t.match(/([0-9]+(?:\.[0-9]+)?)\s*(?:に|へ)(?:\s|$|[、。,.])/)||t.match(/=\s*([0-9]+(?:\.[0-9]+)?)/)||t.match(/\b([0-9]+(?:\.[0-9]+)?)\b\s*$/);if(!a)return null;const c=Number(a[1]);if(!Number.isFinite(c)||c<=0)return null;const i=Bn(),l=zn(i);if(!i||!l)return"❌ Error: systemConfigurations / active configuration not found";const f=[];for(const m of i.configurations||[])try{if(!Array.isArray(m.blocks)||m.blocks.length===0){f.push({ok:!1,configId:m?.id,error:"no blocks"});continue}const g=at(m.blocks)?.rows?.[s],w=g?._blockId,R=g?._blockType;if(!w){f.push({ok:!1,configId:m?.id,error:`surf=${s} has no _blockId`});continue}if(R!=="Stop"){f.push({ok:!1,configId:m?.id,error:`surf=${s} is not Stop (blockType=${String(R)})`});continue}const N=mo(m.blocks),I=N.find(_=>_&&String(_.blockId)===String(w));if(!I){f.push({ok:!1,configId:m?.id,error:`blockId not found: ${String(w)}`});continue}if(String(I.blockType||"").trim()!=="Stop"){f.push({ok:!1,configId:m?.id,error:`blockId=${String(w)} is not Stop`});continue}It(I.parameters)||(I.parameters={}),I.parameters.semiDiameter=c,It(I.variables)||(I.variables={});const b=I.variables.semiDiameter;It(b)?b.value=c:I.variables.semiDiameter={value:c},m.blocks=N;const A=ti(m,i),v=Array.isArray(m?.opticalSystem)?m.opticalSystem:null,S=at(m.blocks);S&&Array.isArray(S.rows)&&(v&&v.length>0&&Jo(S.rows,v),A!==void 0&&S.rows[0]&&typeof S.rows[0]=="object"&&(S.rows[0].thickness=A),m.opticalSystem=S.rows),f.push({ok:!0,configId:m?.id,blockId:String(w)})}catch(h){f.push({ok:!1,configId:m?.id,error:h?.message||String(h)})}await wn(i);const p=f.filter(m=>m.ok).length,d=f.length,u=f.filter(m=>!m.ok),y=u.slice(0,3).map(m=>`- ${String(m.configId)}: ${m.error}`);return[`Surf ${s}（Stop）のsemiDiameterを${c}に変更しました（全config）。`,"","---","(適用ログ)",`- set_block_param(all configs): ok=${p}/${d}${u.length?` fail=${u.length}`:""}`,...y.length?y:[]].join(`
`)}function wu(e){if(!e)return!1;const t=String(e);return(t.includes("runtimeDiagnostics.psfLastResult")||t.includes("psfLastResult"))&&(t.includes("引用")||t.includes("要約"))}function vu(e){if(!e)return!1;const t=String(e),n=t.includes("runtimeDiagnostics.wavefront")||/wavefront/i.test(t)||t.includes("波面"),o=t.includes("引用")||t.includes("要約");return n&&o}function xu(e){if(!e)return!1;const t=String(e),n=t.includes("runtimeDiagnostics.opdLastRay")||t.includes("opdLastRay")||/\bOPD\b/i.test(t)||t.includes("光路差"),o=t.includes("引用")||t.includes("要約");return n&&o}function Yn(e,t){if(!e||typeof e!="object")return"  - (none)";const n=Array.isArray(t)&&t.length?t.filter(o=>Object.prototype.hasOwnProperty.call(e,o)):Object.keys(e);return n.length===0?"  - (no fields)":n.map(o=>{const r=e[o];let s;if(r==null)s=String(r);else if(typeof r=="string")s=r;else try{s=JSON.stringify(r)}catch{s=String(r)}return`  - ${o}: ${s}`}).join(`
`)}function ga(){const e=document.getElementById("ai-assistant-panel"),t=e.style.display!=="none";e.style.display=t?"none":"flex"}function Do(e,t){const n=la++;return zt.push({id:n,role:e,content:t,timestamp:Date.now(),actions:null}),ei(),fa(),n}function $o(){const e=document.getElementById("ai-chat-history");e&&(e.innerHTML=zt.map(t=>{const o=Array.isArray(t.actions)&&t.actions.length>0?`<div class="ai-message-actions"><button class="ai-apply-btn" data-ai-action="apply" data-ai-msg-id="${t.id}">提案を適用</button></div>`:"";return`
        <div class="ai-message ai-message-${t.role}" data-ai-msg-id="${t.id}">
            <div class="ai-message-content">${ya(t.content)}</div>
            ${o}
        </div>`}).join(""),e.scrollTop=e.scrollHeight)}typeof document<"u"&&document?.addEventListener&&document.addEventListener("click",async e=>{const t=e?.target;if(!(t instanceof HTMLElement)||t.dataset?.aiAction!=="apply")return;const n=Number(t.dataset.aiMsgId);if(!Number.isFinite(n))return;const o=zt.find(s=>s&&s.id===n),r=o?.actions;if(!(!Array.isArray(r)||r.length===0)){t.setAttribute("disabled","true");try{const s=await Uu(r),a=[o.content,`

---
適用結果:`,s.ok?`✅ OK: ${s.appliedCount} actions`:`❌ Failed: ${s.reason||"unknown reason"}`,...Array.isArray(s.logs)?s.logs.map(c=>`- ${c}`):[]].join(`
`);ut(n,{content:a,actions:null})}catch(s){ut(n,{content:`${o.content}

❌ Apply error: ${s?.message||s}`})}finally{try{t.removeAttribute("disabled")}catch{}}}});function ya(e){const t=document.createElement("div");return t.textContent=e,t.innerHTML.replace(/\n/g,"<br>")}function Su(e){const t=String(e||"").trim();return t?t.startsWith("models/")?t.slice(7):t:""}async function ha(e){const t=`https://generativelanguage.googleapis.com/v1beta/models?key=${e}`,n=await Ko(t,{method:"GET"},2e4);if(!n.ok){let a="";try{a=(await n.json())?.error?.message||""}catch{}throw new Error(a||"Gemini ListModels failed")}const o=await n.json();return(Array.isArray(o?.models)?o.models:[]).filter(a=>{const c=a?.supportedGenerationMethods;return Array.isArray(c)?c.includes("generateContent"):!0}).map(a=>a?.name).filter(Boolean)}function Au(e){const n=(Array.isArray(e)?e:[]).map(r=>String(r)).filter(Boolean).map(r=>r.replace(/^models\//,"")),o=[r=>r.includes("gemini-1.5-flash"),r=>r.includes("gemini-2.0-flash"),r=>r.includes("flash"),r=>r.includes("gemini-1.5-pro"),r=>r.includes("pro"),r=>r.includes("gemini")];for(const r of o){const s=n.find(r);if(s)return s}return n[0]||""}async function Nu(e,t){const n=Su(t);if(n)return n;const o=await ha(e),r=Au(o);if(!r)throw new Error("Gemini: no available models returned by ListModels");return r}function _u(){return[{functionDeclarations:[{name:"set_block_param",description:"Design Intent (blocks) の特定ブロックのパラメータ/変数を更新し、blocks→expanded optical system に再展開してUIへ反映します。",parameters:{type:"object",properties:{blockId:{type:"string",description:"対象ブロックの blockId（不明な場合は surf を指定可）"},surf:{type:"integer",description:"0-based surface index（expanded optical system の行）。blockId が不明な場合に使用"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（surf 指定推奨）"},section:{type:"string",enum:["parameters","variables"],description:"更新先: parameters または variables"},key:{type:"string",description:"更新キー（blockTypeに依存。例: Lens=frontRadius/backRadius/centerThickness/material、asphere=frontSurfType/frontConic/frontCoef1..10、Doublet=radius1..3/material1..2、Triplet=radius1..4/material1..3、Stop=semiDiameter、AirGap=thickness）"},value:{description:"新しい値（数値または文字列）"}},required:["section","key","value"]}},{name:"set_surface_field",description:"フォールバック: Expanded Optical System の表（active config opticalSystem）上の特定面のフィールドを更新します（blocks が無い場合など）。",parameters:{type:"object",properties:{surf:{type:"integer",description:"0-based surface index"},key:{type:"string",description:"更新キー（例: radius, thickness, material, semidia, conic）"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（blocks がある場合は可能な限り blocks に同期）"},value:{description:"新しい値（数値または文字列）"}},required:["surf","key","value"]}},{name:"set_surface_color",description:"Render Optical System の Surface Colors（面ごとの色）を更新します。localStorage の coopt.surfaceColorOverrides を更新し、3D描画を再描画します。",parameters:{type:"object",properties:{all:{type:"boolean",description:"true の場合、全Surface対象（色=Default/None で全解除）"},surf:{type:"integer",description:"0-based surface index（Surface Colors の # と同じ）"},surfaceId:{type:"integer",description:"surface row の id（存在する場合）。指定すると id:NN で更新します。"},blockId:{type:"string",description:"provenance 用 blockId（_blockId）。surfaceRole と組で指定すると p:blockId|surfaceRole を更新します。"},surfaceRole:{type:"string",description:"provenance 用 surfaceRole（_surfaceRole）。blockId と組で指定します。"},color:{type:"string",description:'色。#RRGGBB / 0xRRGGBB / "Light Pink" 等。"Default"/"None" で解除。'}},required:["color"]}},{name:"apply_optical_system_rows",description:"Optical System（surface rows）を active configuration に取り込み、可能なら Design Intent（blocks）へ自動変換してUIへ反映します。特許の処方表/表をAIが行配列に整形した後に適用する用途。",parameters:{type:"object",properties:{applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration の opticalSystem を同じ rows に置換します（blocksの自動変換も試みます）"},rows:{type:"array",description:"Optical system rows (Surf 0..N-1). Each row may include: object type, radius, thickness, semidia, material, surfType, conic, coef1..coef10, comment",items:{type:"object"}}},required:["rows"]}}]}]}async function Fu({apiKey:e,model:t,systemInstruction:n,contents:o,tools:r,forceToolCall:s}){const c=`https://generativelanguage.googleapis.com/v1beta/models/${await Nu(e,t)}:generateContent?key=${e}`,i={contents:Array.isArray(o)?o:[]};i.systemInstruction={parts:[{text:String(n)}]},Array.isArray(r)&&r.length&&(i.tools=r,i.toolConfig={functionCallingConfig:{mode:s?"ANY":"AUTO"}});const l=await Ko(c,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(i)},Xo);if(!l.ok){let f="Gemini API failed";try{f=(await l.json())?.error?.message||f}catch{try{f=await l.text()}catch{}}throw new Error(f)}return await l.json()}function Mu(e){const t=e?.content?.parts;return Array.isArray(t)?t.map(n=>n?.text).filter(Boolean).join(""):""}function Ru(e){const t=e?.content?.parts;if(!Array.isArray(t))return[];const n=[];for(const o of t){const r=o?.functionCall;r&&r.name&&n.push({name:String(r.name),args:It(r.args)?r.args:r.args??{}})}return n}async function Cu({systemInstruction:e,contents:t,apiKey:n,model:o,thinkingMsgId:r,forceToolCall:s}){const a=_u();let c=Array.isArray(t)?[...t]:[];const i=[];for(let l=0;l<ua;l++){const f=await Fu({apiKey:n,model:o,systemInstruction:e,contents:c,tools:a,forceToolCall:!!s&&l===0}),p=Array.isArray(f?.candidates)?f.candidates[0]:null,d=Ru(p),u=Mu(p);if(!d.length){const h=u||"(Gemini: empty response)",g=zo(i);ut(r,{content:g?g+h:h});const w=xa(h);w?.actions&&ut(r,{actions:w.actions});return}ut(r,{content:`Applying ${d.length} change(s)...`});const y=[];for(const h of d){let g=null;try{h.name==="set_block_param"?g=await Qo(h.args):h.name==="set_surface_field"?g=await oi(h.args):h.name==="set_surface_color"?g=await va(h.args):h.name==="apply_optical_system_rows"?g=await wa(h.args):g={ok:!1,error:`Unknown tool: ${h.name}`}}catch(w){g={ok:!1,error:w?.message||String(w)}}i.push(`${h.name}: ${g?.ok?"ok":"fail"}${g?.error?` (${g.error})`:""}`),y.push({functionResponse:{name:h.name,response:g}})}const m=zo(i);m&&ut(r,{content:m}),c=c.concat([{role:"user",parts:y}])}ut(r,{content:"❌ Tool loop exceeded max iterations. Please re-try with a narrower request."})}function ba(e){return String(e||"").trim()||"gpt-4o-mini"}function Iu(){return[{type:"function",function:{name:"set_block_param",description:"Design Intent (blocks) の特定ブロックのパラメータ/変数を更新し、blocks→expanded optical system に再展開してUIへ反映します。",parameters:{type:"object",additionalProperties:!1,properties:{blockId:{type:"string",description:"対象ブロックの blockId（不明な場合は surf を指定可）"},surf:{type:"integer",description:"0-based surface index（expanded optical system の行）。blockId が不明な場合に使用"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（surf 指定推奨）"},section:{type:"string",enum:["parameters","variables"],description:"更新先: parameters または variables"},key:{type:"string",description:"更新キー（blockTypeに依存。例: Lens=frontRadius/backRadius/centerThickness/material、asphere=frontSurfType/frontConic/frontCoef1..10、Doublet=radius1..3/material1..2、Triplet=radius1..4/material1..3、Stop=semiDiameter、AirGap=thickness）"},value:{description:"新しい値（数値または文字列）"}},required:["section","key","value"],anyOf:[{required:["blockId"]},{required:["surf"]}]}}},{type:"function",function:{name:"set_surface_field",description:"フォールバック: Expanded Optical System の表（active config opticalSystem）上の特定面のフィールドを更新します（blocks が無い場合など）。",parameters:{type:"object",additionalProperties:!1,properties:{surf:{type:"integer",description:"0-based surface index"},key:{type:"string",description:"更新キー（例: radius, thickness, material, semidia, conic）"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（blocks がある場合は可能な限り blocks に同期）"},value:{description:"新しい値（数値または文字列）"}},required:["surf","key","value"]}}},{type:"function",function:{name:"set_surface_color",description:"Render Optical System の Surface Colors（面ごとの色）を更新します。localStorage の coopt.surfaceColorOverrides を更新し、3D描画を再描画します。",parameters:{type:"object",additionalProperties:!1,properties:{all:{type:"boolean",description:"true の場合、全Surface対象（色=Default/None で全解除）"},surf:{type:"integer",description:"0-based surface index（Surface Colors の # と同じ）"},surfaceId:{type:"integer",description:"surface row の id（存在する場合）。指定すると id:NN で更新します。"},blockId:{type:"string",description:"provenance 用 blockId（_blockId）。surfaceRole と組で指定すると p:blockId|surfaceRole を更新します。"},surfaceRole:{type:"string",description:"provenance 用 surfaceRole（_surfaceRole）。blockId と組で指定します。"},color:{type:"string",description:'色。#RRGGBB / 0xRRGGBB / "Light Pink" 等。"Default"/"None" で解除。'}},required:["color"],anyOf:[{required:["all"]},{required:["surf"]},{required:["surfaceId"]},{required:["blockId","surfaceRole"]}]}}},{type:"function",function:{name:"apply_optical_system_rows",description:"Optical System（surface rows）を active configuration に取り込み、可能なら Design Intent（blocks）へ自動変換してUIへ反映します。特許の処方表/表をAIが行配列に整形した後に適用する用途。",parameters:{type:"object",additionalProperties:!1,properties:{applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration の opticalSystem を同じ rows に置換します（blocksの自動変換も試みます）"},rows:{type:"array",items:{type:"object"},description:"Optical system rows (Surf 0..N-1). Each row may include: object type, radius, thickness, semidia, material, surfType, conic, coef1..coef10, comment"}},required:["rows"]}}}]}function Hi(e){const t=Array.isArray(e)?mo(e):[];for(let n=0;n<t.length;n++)(!t[n]||typeof t[n]!="object")&&(t[n]={}),t[n].id=n,t[n].type!==void 0&&t[n]["object type"]===void 0&&(t[n]["object type"]=t[n].type),t[n].glass!==void 0&&t[n].material===void 0&&(t[n].material=t[n].glass),t[n].semiDiameter!==void 0&&t[n].semidia===void 0&&(t[n].semidia=t[n].semiDiameter);return t}async function wa(e){const t=Bn(),n=zn(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");const o=!!e?.applyToAllConfigs,r=e?.rows,s=Hi(r);if(!Array.isArray(s)||s.length===0)throw new Error("rows must be a non-empty array");const a=o?t.configurations||[]:[n],c=[];for(const i of a)try{const l=Hi(s);i.opticalSystem=l;const f=Zs(l),p=Array.isArray(f?.issues)?f.issues:[],d=p.some(u=>u&&u.severity==="fatal");if(!d&&Array.isArray(f?.blocks)&&f.blocks.length>0){i.schemaVersion=i.schemaVersion||po,i.blocks=f.blocks;const u=at(i.blocks);if(u&&Array.isArray(u.rows)){try{Jo(u.rows,l)}catch{}try{const y=l?.[0]?.thickness;String(y??"").trim()!==""&&u.rows[0]&&typeof u.rows[0]=="object"&&(u.rows[0].thickness=y)}catch{}i.opticalSystem=u.rows}(!i.metadata||typeof i.metadata!="object")&&(i.metadata={}),i.metadata.importAnalyzeMode=!1}else(!i.metadata||typeof i.metadata!="object")&&(i.metadata={}),i.metadata.importAnalyzeMode=!0;c.push({ok:!0,configId:i?.id,blocksDerived:!d&&Array.isArray(f?.blocks)&&f.blocks.length>0,issues:p})}catch(l){c.push({ok:!1,configId:i?.id,error:l?.message||String(l)})}return await wn(t),{ok:c.some(i=>i.ok),applyToAllConfigs:o,appliedToConfigs:c}}function It(e){return!!e&&typeof e=="object"&&!Array.isArray(e)}function mo(e){return JSON.parse(JSON.stringify(e))}function Bn(){try{return typeof localStorage>"u"?null:Ot()}catch{return null}}function zn(e){if(!e||!Array.isArray(e.configurations))return null;const t=e.activeConfigId;return e.configurations.find(n=>n&&String(n.id)===String(t))||e.configurations[0]||null}function ti(e,t){try{if(Array.isArray(e?.blocks)&&e.blocks.some(o=>String(o?.blockType??"").trim()==="ObjectSurface"))return}catch{}try{const n=e?.opticalSystem?.[0]?.thickness;if(String(n??"").trim()!=="")return n}catch{}try{if(t&&String(t.activeConfigId)===String(e?.id)){const n=Sc();if(n){const o=n?.[0]?.thickness;if(String(o??"").trim()!=="")return o}}}catch{}}function Jo(e,t){if(!Array.isArray(e)||!Array.isArray(t))return;const n=Math.min(e.length,t.length),o=s=>s==null?!1:String(s).trim()!=="",r=s=>!s||typeof s!="object"?null:s.semidia??s["Semi Diameter"]??s["semi diameter"]??s.semiDiameter??s.semiDia;for(let s=0;s<n;s++){const a=e[s],c=t[s];if(!a||typeof a!="object"||!c||typeof c!="object")continue;const i=String(a["object type"]??a.object??"").trim().toLowerCase();if(i==="stop"||i==="image")continue;const l=r(c);o(l)&&(a.semidia=l)}}async function wn(e){try{typeof localStorage<"u"&&Jr(e)}catch{}const n=(()=>{try{if(window.ConfigurationManager||window.tableOpticalSystem)return window}catch{}try{if(window.opener&&!window.opener.closed)return window.opener}catch{}return window})();try{if(window.opener&&!window.opener.closed){const o=typeof window.location?.origin=="string"&&window.location.origin&&window.location.origin!=="null"?window.location.origin:"*";window.opener.postMessage({type:da},o)}}catch{}await ma(n)}const sn=(e,t,n)=>{const o=[];for(let r=t;r<=n;r++)o.push(`${e}${r}`);return o},Tu=new Set(["frontRadius","backRadius","centerThickness","material","frontSurfType","backSurfType","frontConic","backConic",...sn("frontCoef",1,10),...sn("backCoef",1,10)]),Pu=new Set(["surfType","focalLengthX","focalLengthY","focalLength"]),ku=new Set(["radius1","radius2","radius3","thickness1","thickness2","material1","material2","surf1SurfType","surf2SurfType","surf3SurfType","surf1Conic","surf2Conic","surf3Conic",...sn("surf1Coef",1,10),...sn("surf2Coef",1,10),...sn("surf3Coef",1,10)]),Eu=new Set(["radius1","radius2","radius3","radius4","thickness1","thickness2","thickness3","material1","material2","material3","surf1SurfType","surf2SurfType","surf3SurfType","surf4SurfType","surf1Conic","surf2Conic","surf3Conic","surf4Conic",...sn("surf1Coef",1,10),...sn("surf2Coef",1,10),...sn("surf3Coef",1,10),...sn("surf4Coef",1,10)]),Ou=new Set(["thickness"]),Du=new Set(["semiDiameter"]);function ju(e){const t=String(e||"").trim();return t==="Paraxial"?Pu:t==="Lens"||t==="PositiveLens"?Tu:t==="Doublet"?ku:t==="Triplet"?Eu:t==="AirGap"?Ou:t==="Stop"?Du:null}const Lu=new Set(["radius","radiusRaw","thickness","material","glass","semidia","conic","type"]),ni="coopt.surfaceColorOverrides",Gi=new Map([["light pink","#F8BBD0"],["light red","#FFCDD2"],["light orange","#FFE0B2"],["light amber","#FFECB3"],["light yellow","#FFF9C4"],["light lime","#F0F4C3"],["light green","#C8E6C9"],["light mint","#B2DFDB"],["light cyan","#B3E5FC"],["light sky","#BBDEFB"],["light blue","#90CAF9"],["light indigo","#C5CAE9"],["light purple","#E1BEE7"],["light lavender","#D1C4E9"],["light peach","#FFCCBC"],["light gray","#ECEFF1"]]);function Bu(){try{const e=localStorage.getItem(ni);if(!e)return{};const t=JSON.parse(e);return It(t)?t:{}}catch{return{}}}function zu(e){try{localStorage.setItem(ni,JSON.stringify(e||{}))}catch{}}function $u(e){const t=String(e??"").trim();if(!t)return null;const n=t.toLowerCase();if(n==="default"||n==="none"||n==="clear"||n==="reset"||n==="auto")return null;if(/^#[0-9a-fA-F]{6}$/.test(t))return t.toUpperCase();if(/^0x[0-9a-fA-F]{6}$/.test(t))return("#"+t.slice(2)).toUpperCase();const o=Gi.get(n);if(o)return o;const r=n.replace(/\s+/g," ").trim(),s=Gi.get(r);if(s)return s;throw new Error(`Unsupported color: ${t}`)}function Wu({row:e,surf:t,surfaceId:n,blockId:o,surfaceRole:r}){try{const a=String(e?._blockId??o??"").trim(),c=String(e?._surfaceRole??r??"").trim();if(a&&c)return"p:"+a+"|"+c}catch{}try{const a=Number(e?.id??n);if(Number.isFinite(a))return"id:"+String(Math.floor(a))}catch{}const s=Number.isInteger(Number(t))?Number(t):0;return"i:"+String(Math.floor(s))}function qi(){const e=t=>{try{return!t||t.closed?!1:(t.postMessage({action:"request-redraw"},"*"),!0)}catch{return!1}};if(!e(window.popup3DWindow))try{if(window.opener&&!window.opener.closed&&e(window.opener.popup3DWindow))return}catch{}}async function va(e){const t=Bn(),n=zn(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");const o=$u(e?.color);if(!!e?.all){if(o!==null)throw new Error("all=true is currently only supported with color=Default/None (reset)");return localStorage.removeItem(ni),qi(),{ok:!0,applied:{key:"(all)",color:"Default"}}}const s=Number.isInteger(Number(e?.surf))&&Number(e?.surf)>=0,a=Number.isFinite(Number(e?.surfaceId)),c=String(e?.blockId??"").trim()&&String(e?.surfaceRole??"").trim();if(!s&&!a&&!c)throw new Error("surf or surfaceId or (blockId+surfaceRole) is required");const i=s?Number(e.surf):null;let l=null;try{if(s)Array.isArray(n?.blocks)&&n.blocks.length>0?l=at(n.blocks)?.rows?.[i]??null:Array.isArray(n?.opticalSystem)&&(l=n.opticalSystem?.[i]??null);else if(a){const d=Math.floor(Number(e.surfaceId)),u=Array.isArray(n?.blocks)&&n.blocks.length>0?at(n.blocks)?.rows||[]:Array.isArray(n?.opticalSystem)?n.opticalSystem:[];l=Array.isArray(u)&&u.find(y=>Number(y?.id)===d)||null}}catch{l=null}const f=Wu({row:l,surf:s?i:0,surfaceId:a?Number(e.surfaceId):null,blockId:e?.blockId,surfaceRole:e?.surfaceRole}),p=Bu();return o===null?delete p[f]:p[f]=o,zu(p),qi(),{ok:!0,applied:{key:f,color:o??"Default"}}}async function Qo(e){const t=Bn(),n=zn(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");if(!Array.isArray(n.blocks)||n.blocks.length===0)throw new Error("Active configuration has no blocks (Design Intent).");const o=!!e?.applyToAllConfigs,r=e?.surf;let s=String(e?.blockId||"").trim();const a=String(e?.section||"").trim();let c=String(e?.key||"").trim(),i=e?.value;const l=d=>{const u=s?String(s):"";if(u&&Array.isArray(d?.blocks)&&d.blocks.some(R=>R&&String(R.blockId)===u))return u;const y=Number(r);if(!Number.isInteger(y)||y<0)return null;const g=at(d.blocks)?.rows?.[y]?._blockId;return g?String(g):null},f=d=>{if(!Array.isArray(d?.blocks)||d.blocks.length===0)return{ok:!1,configId:d?.id,error:"no blocks"};const u=l(d);if(!u)return{ok:!1,configId:d?.id,error:"blockId or surf (0-based) required / could not resolve blockId"};const y=mo(d.blocks),m=y.find(E=>E&&String(E.blockId)===u);if(!m)return{ok:!1,configId:d?.id,error:`blockId not found: ${u}`};const h=String(m.blockType||"").trim();if(h==="ImageSurface")return{ok:!1,configId:d?.id,error:"ImageSurface is not editable"};let g=a,w=c,R=i;w==="glass"&&(w="material"),w==="glass1"&&(w="material1"),w==="glass2"&&(w="material2"),w==="glass3"&&(w="material3"),h==="Stop"&&w==="semiDiameter"&&(g="parameters");const N=ju(h);if(!N||!N.has(w))return{ok:!1,configId:d?.id,error:`Unsupported key for blockType=${h}: ${w}`};const I=E=>{if(typeof E=="number")return Number.isFinite(E)?E:NaN;if(typeof E=="string"){const L=E.trim();if(L==="")return NaN;if(/^inf(inity)?$/i.test(L))return 1/0;const ne=Number(L);return Number.isFinite(ne)?ne:NaN}return NaN};if(w.toLowerCase().includes("thickness")){const E=I(R);if(Number.isFinite(E)&&E<0)return{ok:!1,configId:d?.id,error:`Negative thickness is not allowed: ${E}`};if(h==="AirGap"&&Number.isFinite(E)&&E<0)return{ok:!1,configId:d?.id,error:`Negative AirGap thickness is not allowed: ${E}`}}if(w==="semiDiameter"){const E=I(R);if(!Number.isFinite(E)||E<=0)return{ok:!1,configId:d?.id,error:`Stop.semiDiameter must be positive: ${String(R)}`};R=E}const b=()=>{try{if(window.undoHistory&&window.SetBlockParameterCommand&&!window.undoHistory.isExecuting){const E=It(m.parameters)?m.parameters[w]:void 0;if(E!==R){const L=new window.SetBlockParameterCommand(d.id,u,`parameters.${w}`,E,R);window.undoHistory.record(L)}}}catch{}It(m.parameters)||(m.parameters={}),m.parameters[w]=R},A=()=>{try{if(window.undoHistory&&window.SetBlockParameterCommand&&!window.undoHistory.isExecuting){const L=It(m.variables)&&It(m.variables[w])?m.variables[w].value:void 0;if(L!==R){const ne=new window.SetBlockParameterCommand(d.id,u,`variables.${w}.value`,L,R);window.undoHistory.record(ne)}}}catch{}It(m.variables)||(m.variables={});const E=m.variables[w];It(E)?E.value=R:m.variables[w]={value:R}},v=It(m.parameters)&&Object.prototype.hasOwnProperty.call(m.parameters,w),S=It(m.variables)&&Object.prototype.hasOwnProperty.call(m.variables,w);if(v&&b(),S&&A(),!v&&!S&&(g==="parameters"?b():A()),h==="Stop"&&w==="semiDiameter"){It(m.variables)||(m.variables={});const E=m.variables[w];It(E)?E.value=R:m.variables[w]={value:R}}d.blocks=y;const _=ti(d,t),M=Array.isArray(d?.opticalSystem)?d.opticalSystem:null,G=at(d.blocks);G&&Array.isArray(G.rows)&&(M&&M.length>0&&Jo(G.rows,M),_!==void 0&&G.rows[0]&&typeof G.rows[0]=="object"&&(G.rows[0].thickness=_),d.opticalSystem=G.rows);const z=v||!v&&!S&&g==="parameters"?"parameters":"variables";return{ok:!0,configId:d?.id,applied:{blockId:u,section:z,key:w,value:R}}};if(!(a==="parameters"||a==="variables"))throw new Error("section must be parameters|variables");if(!c)throw new Error("key is required");if(o){const d=[];for(const u of t.configurations||[])d.push(f(u));return await wn(t),{ok:d.some(u=>u.ok),applyToAllConfigs:!0,appliedToConfigs:d}}const p=f(n);if(!p.ok)throw new Error(p.error||"failed");return await wn(t),{ok:!0,applyToAllConfigs:!1,applied:p.applied}}async function oi(e){const t=Bn(),n=zn(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");const o=Number(e?.surf);let r=String(e?.key||"").trim();const s=e?.value,a=!!e?.applyToAllConfigs;if(!Number.isInteger(o)||o<0)throw new Error("surf must be a 0-based non-negative integer");if(!r)throw new Error("key is required");if(r==="radiusRaw"&&(r="radius"),!Lu.has(r))throw new Error(`Unsupported surface key: ${r}`);if(r==="semidia")try{const l=typeof s=="number"?s:Number(String(s).trim());if(!Number.isFinite(l)||l<=0)throw new Error(`semidia must be positive number: ${String(s)}`);const f=await Qo({surf:o,applyToAllConfigs:a,section:"parameters",key:"semiDiameter",value:l});return{ok:!0,applyToAllConfigs:a,applied:{surf:o,key:r,value:s,translatedTo:"set_block_param"},result:f}}catch{}const c=a?t.configurations||[]:[n],i=[];for(const l of c)try{const f=Array.isArray(l.opticalSystem)?mo(l.opticalSystem):[];if(!f[o])throw new Error(`surface out of range: ${o}`);f[o][r]=s,l.opticalSystem=f,i.push({ok:!0,configId:l?.id})}catch(f){i.push({ok:!1,configId:l?.id,error:f?.message||String(f)})}return await wn(t),{ok:i.some(l=>l.ok),applyToAllConfigs:a,applied:{surf:o,key:r,value:s},appliedToConfigs:i}}async function Uu(e){if(!Array.isArray(e)||e.length===0)return{ok:!1,reason:"no actions"};const t=[];let n=0;for(const o of e){const r=String(o?.type||"").trim();if(r==="set_block_param"){const s=await Qo(o);n++,t.push(`set_block_param ${s.applied.blockId} ${s.applied.section}.${s.applied.key}=${JSON.stringify(s.applied.value)}`)}else if(r==="set_surface_field"){const s=await oi(o);n++,t.push(`set_surface_field surf=${s.applied.surf} ${s.applied.key}=${JSON.stringify(s.applied.value)}`)}else t.push(`skip unknown action type: ${r||"(missing)"}`)}return{ok:!0,appliedCount:n,logs:t}}function xa(e){if(!e)return null;const t=String(e),n=t.indexOf(zi);if(n>=0){const r=t.slice(n+zi.length),s=r.match(/```json\s*([\s\S]*?)```/i)||r.match(/```\s*([\s\S]*?)```/);if(s&&s[1])try{const a=JSON.parse(s[1]);if(a&&Array.isArray(a.actions))return a}catch{}}const o=[...t.matchAll(/```json\s*([\s\S]*?)```/gi)];for(const r of o)try{const s=JSON.parse(r[1]);if(s&&Array.isArray(s.actions))return s}catch{}return null}async function Hu({apiKey:e,model:t,payload:n}){const r=await Ko("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${e}`},body:JSON.stringify({model:ba(t),...n,stream:!1})},Xo);if(!r.ok){let s="OpenAI API failed";try{s=(await r.json())?.error?.message||s}catch{try{s=await r.text()}catch{}}throw new Error(s)}return await r.json()}async function Gu({apiKey:e,model:t,messages:n,onDelta:o}){const s=await Ko("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${e}`},body:JSON.stringify({model:ba(t),messages:n,stream:!0})},pa);if(!s.ok){let l="OpenAI stream failed";try{l=(await s.json())?.error?.message||l}catch{try{l=await s.text()}catch{}}throw new Error(l)}const a=s.body.getReader(),c=new TextDecoder("utf-8");let i="";for(;;){const{value:l,done:f}=await a.read();if(f)break;i+=c.decode(l,{stream:!0});const p=i.split(`
`);i=p.pop()||"";for(const d of p){const u=d.trim();if(!u.startsWith("data:"))continue;const y=u.slice(5).trim();if(y==="[DONE]")return;try{const h=JSON.parse(y)?.choices?.[0]?.delta?.content;h&&o(String(h))}catch{}}}}async function qu({messages:e,apiKey:t,model:n,thinkingMsgId:o,forceToolCall:r}){const s=Iu();let a=0,c=[...e];const i=[];for(;a++<ua;){const f=!!r&&i.length===0?{type:"function",function:{name:"set_block_param"}}:"auto",d=(await Hu({apiKey:t,model:n,payload:{messages:c,tools:s,tool_choice:f}}))?.choices?.[0]?.message,u=Array.isArray(d?.tool_calls)?d.tool_calls:[],y=d?.content||"";if(u.length===0){let m="";const h=zo(i);ut(o,{content:h||""}),await Gu({apiKey:t,model:n,messages:c.concat([{role:"assistant",content:y}]),onDelta:w=>{m+=w,ut(o,{content:(h||"")+m})}});const g=xa(m);g?.actions&&ut(o,{actions:g.actions});return}c=c.concat([{role:"assistant",content:y,tool_calls:u}]);for(const m of u){const h=m?.function?.name,g=m?.function?.arguments;let w={};try{w=g?JSON.parse(g):{}}catch{w={}}let R=null;try{h==="set_block_param"?R=await Qo(w):h==="set_surface_field"?R=await oi(w):h==="set_surface_color"?R=await va(w):h==="apply_optical_system_rows"?R=await wa(w):R={ok:!1,error:`Unknown tool: ${h}`}}catch(I){R={ok:!1,error:I?.message||String(I)}}i.push(`${h}: ${R?.ok?"ok":"fail"}${R?.error?` (${R.error})`:""}`);const N=zo(i);N&&ut(o,{content:N}),c.push({role:"tool",tool_call_id:m.id,content:JSON.stringify(R)})}}ut(o,{content:"❌ Tool loop exceeded max iterations. Please re-try with a narrower request."})}function Vu(e){return e==null||String(e).trim()===""}function tt(e){if(e==null)return null;if(typeof e=="number"&&Number.isFinite(e))return e;const t=String(e).trim();if(t==="")return null;if(/^inf(inity)?$/i.test(t))return 1/0;const n=Number(t);return Number.isFinite(n)?n:null}function Yu(e){const t=String(e??"").trim();return t===""||t.toUpperCase()==="AIR"||t.toUpperCase()==="VACUUM"?"AIR":t.replace(/\s+/g,"")}function Zu(e){const t=new Uint8Array(2+e.length*2);t[0]=255,t[1]=254;for(let n=0;n<e.length;n++){const o=e.charCodeAt(n);t[2+n*2]=o&255,t[2+n*2+1]=o>>8&255}return t}function Xu(e){const t=[];for(let n=0;n<e.length;n++){const o=e[n]??{};String(o.surfType??"").trim()==="Aspheric odd"&&t.push(n+1)}return{odd:t}}function Ku(e){const t=String(e?.surfType??e?.["surface type"]??e?.surfaceType??"").trim().toLowerCase(),n=String(e?.["object type"]??e?.object??"").trim().toLowerCase();return t==="coord trans"||t==="coordinate transform"||t==="ct"||t==="coordtrans"||t==="coordinatetransform"||n==="coord trans"||n==="coordinate transform"||n==="ct"||n==="coordtrans"||n==="coordinatetransform"}function Ju(e){const t=String(e??"").trim().toLowerCase();return t==="orientation"?1:t==="xy"?2:t==="xyz"?3:0}function ft(e){if(!Number.isFinite(e))return"0";const t=Math.abs(e);return t!==0&&(t<1e-6||t>1e6)?String(e):Number(e.toFixed(15)).toString()}function Vi(e){return Array.isArray(e)?e:[]}function Qu(e){for(let t=0;t<e.length;t++){const n=e[t]??{};if(String(n.primary??"").trim())return t+1}return 1}function ep(e,t={}){const n=Array.isArray(e)?e:[],{odd:o}=Xu(n);if(o.length>0){const f=o.slice(0,20).join(", "),p=o.length>20?` (+${o.length-20} more)`:"",d=new Error(`Zemax export: Aspheric odd surfaces are not supported yet. Surface(s): ${f}${p}`);throw d.code="ZMX_UNSUPPORTED_ODD",d}const r=String(t.title??"co-opt export").replace(/\r?\n/g," ").trim(),s=String(t.units??"MM").trim().toUpperCase(),a=[];a.push(`UNIT ${s}`),r&&a.push(`NAME "${r.replace(/\"/g,"'")}"`),a.push('NOTE "Generated by co-opt"');const c=Vi(t.sourceRows);if(c.length>0){for(let p=0;p<c.length;p++){const d=c[p]??{},u=tt(d.wavelength);if(u===null||!Number.isFinite(u)||u<=0)continue;const y=tt(d.weight),m=y!==null&&Number.isFinite(y)?y:1;a.push(`WAVM ${p+1} ${ft(u)} ${ft(m)}`)}const f=Qu(c);f>0&&a.push(`PWAV ${f}`)}const i=Vi(t.objectRows);if(i.length>0){a.push("FTYP 0 0 2 3 0 0 0 2");const f=[],p=[],d=[];for(let u=0;u<i.length;u++){const y=i[u]??{},m=tt(y.xHeightAngle),h=tt(y.yHeightAngle);(m===null||!Number.isFinite(m))&&(h===null||!Number.isFinite(h))||(f.push(ft(Number.isFinite(m)?m:0)),p.push(ft(Number.isFinite(h)?h:0)),d.push("1"))}f.length>0&&(a.push(`XFLN ${f.join(" ")}`),a.push(`YFLN ${p.join(" ")}`),a.push(`FWGN ${d.join(" ")}`))}const l=tt(t.entrancePupilDiameterMm??t.pupilDiameterMm);l!==null&&Number.isFinite(l)&&l>0&&a.push(`ENPD ${ft(l)}`);for(let f=0;f<n.length;f++){const p=n[f]??{};if(a.push(`SURF ${f}`),Ku(p)){a.push("TYPE COORDBRK"),a.push("CURV 0");const I=(()=>{const E=tt(p.decenterZ);if(E!==null&&Number.isFinite(E))return E;const L=tt(p.thickness);return L!==null&&Number.isFinite(L)?L:0})();a.push(`DISZ ${ft(I)}`);const b=(()=>{const E=tt(p.decenterX);if(E!==null&&Number.isFinite(E))return E;const L=tt(p.semidia);return L!==null&&Number.isFinite(L)?L:0})(),A=(()=>{const E=tt(p.decenterY);if(E!==null&&Number.isFinite(E))return E;const L=tt(p.material);return L!==null&&Number.isFinite(L)?L:0})(),v=(()=>{const E=tt(p.tiltX);if(E!==null&&Number.isFinite(E))return E;const L=tt(p.rindex);return L!==null&&Number.isFinite(L)?L:0})(),S=(()=>{const E=tt(p.tiltY);if(E!==null&&Number.isFinite(E))return E;const L=tt(p.abbe);return L!==null&&Number.isFinite(L)?L:0})(),_=(()=>{const E=tt(p.tiltZ);if(E!==null&&Number.isFinite(E))return E;const L=tt(p.conic);return L!==null&&Number.isFinite(L)?L:0})(),M=(()=>{const E=tt(p.order),L=tt(p.coef1);return(E!==null&&Number.isFinite(E)?E:L!==null&&Number.isFinite(L)?L:0)===1?1:0})();a.push(`PARM 1 ${ft(b)}`),a.push(`PARM 2 ${ft(A)}`),a.push(`PARM 3 ${ft(v)}`),a.push(`PARM 4 ${ft(S)}`),a.push(`PARM 5 ${ft(_)}`),a.push(`PARM 6 ${ft(M)}`);const G=Ju(p.coordReturn);if(G!==0){a.push(`PARM 7 ${ft(G)}`);const E=tt(p.toSurf),L=E!==null&&Number.isFinite(E)&&E>=0?Math.trunc(E):0;a.push(`PARM 8 ${ft(L)}`)}const z=String(p.comment??"").trim();z&&a.push(`COMM "${z.replace(/\"/g,"'")}"`);continue}String(p["object type"]??"").trim()==="Stop"&&a.push("STOP");const u=tt(p.radius);let y=0;u===null||u===1/0||u===0?y=0:y=1/u,a.push(`CURV ${ft(y)}`);const m=tt(p.thickness);let h=0;m===null?h=0:m===1/0?h=1e10:h=m,a.push(`DISZ ${ft(h)}`);const g=Yu(p.material);!Vu(g)&&String(g).toUpperCase()!=="AIR"&&a.push(`GLAS ${g}`);const w=tt(p.semidia);if(w!==null&&Number.isFinite(w)&&w>0&&a.push(`DIAM ${ft(w)} 1 0 0 1 ""`),String(p.surfType??"").trim()==="Aspheric even"){a.push("TYPE EVENASPH");const I=tt(p.conic);I!==null&&Number.isFinite(I)&&I!==0&&a.push(`CONI ${ft(I)}`),a.push("PARM 1 0");for(let b=1;b<=10;b++){const A=`coef${b}`,v=tt(p[A]);v===null||!Number.isFinite(v)||v===0||a.push(`PARM ${b+1} ${ft(v)}`)}}else{const I=tt(p.conic);I!==null&&Number.isFinite(I)&&I!==0&&a.push(`CONI ${ft(I)}`)}const N=String(p.comment??"").trim();N&&a.push(`COMM "${N.replace(/\"/g,"'")}"`)}return a.push(""),a.join(`
`)}function tp(e,t){const n=String(t??"co-opt-export.zmx").trim()||"co-opt-export.zmx",o=n.toLowerCase().endsWith(".zmx")?n:`${n}.zmx`,r=Zu(String(e??"")),s=new Blob([r],{type:"text/plain"}),a=URL.createObjectURL(s),c=document.createElement("a");c.href=a,c.download=o,c.click(),URL.revokeObjectURL(a)}const Yi="analysis-progress-wrap",Zi="analysis-progress-label",Xi="analysis-progress-bar";let _n=null,Dr=null,dn=null;function ri(){if(_n)return _n;const e=document.getElementById(Yi),t=document.getElementById(Zi),n=document.getElementById(Xi);if(e&&t&&n instanceof HTMLProgressElement)return _n={wrap:e,label:t,prog:n},_n;const o=document.createElement("div");o.id=Yi,o.className="analysis-progress-wrap",o.style.display="none";const r=document.createElement("div");r.id=Zi,r.className="analysis-progress-label",r.textContent="";const s=document.createElement("progress");return s.id=Xi,s.className="analysis-progress-bar",s.max=100,s.value=0,o.appendChild(r),o.appendChild(s),document.body.appendChild(o),_n={wrap:o,label:r,prog:s},_n}function ii(e="Computing analysis...",t=180){const n=ri();n&&(dn!==null&&(clearTimeout(dn),dn=null),t<=0?n.wrap.style.display="block":dn=window.setTimeout(()=>{dn=null;try{n.wrap.style.display="block"}catch{}},t),vn({label:e,percent:null}))}function vn(e){const t=ri();if(!t)return;const n=String(e?.label??"Computing analysis..."),o=Number(e?.percent),r=Number.isFinite(o),s=r?Math.max(0,Math.min(100,o)):0,a=Date.now(),c=Dr;if(c){const i=c.label===n,l=c.value===s,f=i&&!l&&a-c.at<120;if(i&&l||f)return}t.label.textContent=n,r?(t.prog.max=100,t.prog.value=s,t.prog.removeAttribute("data-indeterminate")):(t.prog.value=0,t.prog.setAttribute("data-indeterminate","1")),Dr={label:n,value:s,max:100,at:a}}function si(){dn!==null&&(clearTimeout(dn),dn=null);const e=ri();e&&(e.wrap.style.display="none",Dr=null)}const H=window,Ki="coopt.render.showDesignIntentLabels";function jr(){try{const e=String(localStorage.getItem(Ki)??"").trim().toLowerCase();if(e==="true"||e==="1"||e==="yes"||e==="on")return!0;if(e==="false"||e==="0"||e==="no"||e==="off")return!1}catch{}try{const e=window.opener,t=String(e?.localStorage?.getItem?.(Ki)??"").trim().toLowerCase();if(t==="true"||t==="1"||t==="yes"||t==="on")return!0;if(t==="false"||t==="0"||t==="no"||t==="off")return!1}catch{}try{if(typeof H.__cooptRenderShowDesignIntentLabels=="boolean")return!!H.__cooptRenderShowDesignIntentLabels}catch{}return!1}function Ut(){let e=[],t=[],n=[];try{const o=Je(H.tableOpticalSystem);Array.isArray(o)&&(e=o)}catch{}try{const o=Tt(H.tableSource);Array.isArray(o)&&(t=o)}catch{}try{const o=ht(H.tableObject);Array.isArray(o)&&(n=o)}catch{}try{let o=null;if(typeof H.getActiveConfiguration=="function"&&(o=H.getActiveConfiguration()),!o){const r=Ot(),s=r?.activeConfigId,a=Number(s),c=Array.isArray(r?.configurations)?r.configurations:[];Number.isFinite(a)&&(o=c.find(i=>Number(i?.id)===a)||null),!o&&c.length>0&&(o=c[0])}if(o){let r=Array.isArray(o?.opticalSystem)?o.opticalSystem.map(a=>a&&typeof a=="object"?{...a}:a):[];if(Array.isArray(o?.blocks)&&o.blocks.length>0){const a=H.expandBlocksToOpticalSystemRows?H.expandBlocksToOpticalSystemRows(o.blocks):null;Array.isArray(a?.rows)&&a.rows.length>0&&(r=a.rows.map(c=>c&&typeof c=="object"?{...c}:c))}const s=Array.isArray(o?.object)?o.object.map(a=>a&&typeof a=="object"?{...a}:a):[];r.length>0&&(e=r),s.length>0&&(n=s)}}catch{}if(!Array.isArray(n)||n.length===0)try{const o=H.tableObject&&Array.isArray(H.tableObject.data)?H.tableObject.data:null;Array.isArray(o)&&o.length>0&&(n=o)}catch{}if(!Array.isArray(n)||n.length===0)try{const o=localStorage.getItem("objectTableData");if(o){const r=JSON.parse(o);Array.isArray(r)&&r.length>0&&(n=r)}}catch{}if(!Array.isArray(n)||n.length===0)try{let o=null;if(typeof H.getActiveConfiguration=="function"&&(o=H.getActiveConfiguration()),!o){const s=Ot(),a=Number(s?.activeConfigId),c=Array.isArray(s?.configurations)?s.configurations:[];Number.isFinite(a)&&(o=c.find(i=>Number(i?.id)===a)||null),!o&&c.length>0&&(o=c[0])}const r=o&&Array.isArray(o.object)?o.object:null;Array.isArray(r)&&r.length>0&&(n=r.map(s=>s&&typeof s=="object"?{...s}:s))}catch{}return{opticalSystemRows:e,sourceRows:t,objectRows:n}}function tn(e){try{if(!Array.isArray(e)||e.length===0)return .5876;const t=o=>{if(o===!0||o===1)return!0;const r=String(o??"").trim().toLowerCase();return r.includes("primary")||r==="true"||r==="yes"||r==="1"};let n=NaN;for(let o=0;o<e.length;o++){const r=e[o],s=Number(r?.wavelength);if(!(!Number.isFinite(s)||s<=0)&&(Number.isFinite(n)||(n=s),t(r?.primary)))return s}return Number.isFinite(n)&&n>0?n:.5876}catch{return .5876}}function fo(e,t){try{const n=ks(e,t,!0),o=Number(n?.pupilDiameterMm),r=Number(n?.focalLengthMm);return{pupilDiameterMm:o,focalLengthMm:r}}catch{}return{pupilDiameterMm:NaN,focalLengthMm:NaN}}function np(e,t){try{const n=tn(t),{pupilDiameterMm:o,focalLengthMm:r}=fo(e,n);if(![n,o,r].every(Number.isFinite))return NaN;if(n<=0||o<=0||r<=0)return NaN;const s=r/o;if(!Number.isFinite(s)||s<=0)return NaN;const a=1.22*n*s;return Number.isFinite(a)&&a>0?a:NaN}catch{return NaN}}async function op(e){const t=`analysis-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let n=null;ii("Starting analysis...",180);const{opticalSystemRows:o,sourceRows:r,objectRows:s}=Ut();try{try{n=await Zo("analysis-progress",c=>{try{const i=c?.payload;if(!i||String(i.jobId||"")!==t)return;const l=String(i?.message||i?.phase||"Computing analysis..."),f=Number(i?.percent),p=Number.isFinite(f)?f:null;vn({label:l,percent:p})}catch{}})}catch(c){try{console.warn("[analysis-progress] listen unavailable in this window, continuing without progress events:",c)}catch{}n=null}const a=await Nc({...e,jobId:t,opticalSystemRows:o,sourceRows:r,objectRows:s});return vn({label:"Done",percent:100}),a}finally{if(typeof n=="function")try{n()}catch{}window.setTimeout(()=>{try{si()}catch{}},220)}}H.runDesktopAnalysisComputeForPopup=op;async function Sa(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut(),r=`native-opd-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let s=null;try{ii("Native OPD: starting...",140);try{s=await Zo("analysis-progress",y=>{try{const m=y?.payload||{};if(!m||String(m.jobId||"")!==r)return;const h=Number(m.percent);vn({label:m.message||"Native OPD running...",percent:Number.isFinite(h)?Math.max(0,Math.min(100,h)):null})}catch{}})}catch(y){try{console.warn("[analysis-progress] listen unavailable for native OPD, continuing without progress events:",y)}catch{}s=null}const a=er(o,Number.isFinite(Number(e?.objectIndex))?Number(e.objectIndex):0,Number.isFinite(Number(e?.wavelengthUm))?Number(e.wavelengthUm):tn(n||[])),c=Sn(),i=Number(a?.fieldAngle?.x??0),l=Number(a?.fieldAngle?.y??0),f=Math.abs(i)>1e-12||Math.abs(l)>1e-12,p=c==="stop"||c==="entrance"?c:String(a?.type||"").toLowerCase()==="angle"&&f?"entrance":void 0,d=(()=>{const y=Number(e?.surfaceIndex);if(Number.isInteger(y)&&y>=0)return y;let m=-1;for(let h=0;h<t.length;h++){const g=t[h]||{};String(g?.["object type"]??g?.object??g?.Object??"").trim().toLowerCase()==="image"&&(m=h)}return m>=0?m:Math.max(0,t.length-1)})(),u=await Bo({jobId:r,opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:Number.isFinite(Number(e?.objectIndex))?Number(e.objectIndex):0,gridSize:Number.isFinite(Number(e?.gridSize))?Number(e.gridSize):129,wavelengthUm:Number.isFinite(Number(e?.wavelengthUm))?Number(e.wavelengthUm):void 0,surfaceIndex:d,pupilSamplingMode:p,opdDisplayMode:e?.opdDisplayMode||"pistonTiltRemoved"});return vn({label:"Native OPD done",percent:100}),u}finally{try{s&&s()}catch{}setTimeout(()=>{try{si()}catch{}},220)}}H.runDesktopNativeOpdMapForPopup=Sa;async function Aa(e){const t=`native-psf-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let n=null;try{ii("Native PSF: starting...",140);try{n=await Zo("analysis-progress",r=>{try{const s=r?.payload||{};if(!s||String(s.jobId||"")!==t)return;const a=Number(s.percent);vn({label:s.message||"Native PSF running...",percent:Number.isFinite(a)?Math.max(0,Math.min(100,a)):null})}catch{}})}catch(r){try{console.warn("[analysis-progress] listen unavailable for native PSF, continuing without progress events:",r)}catch{}n=null}const o=await Ps({jobId:t,gridOpd:Array.isArray(e?.gridOpd)?e.gridOpd:[],pupilMask:Array.isArray(e?.pupilMask)?e.pupilMask:[],gridAmplitude:Array.isArray(e?.gridAmplitude)?e.gridAmplitude:void 0,wavelengthUm:Number(e?.wavelengthUm),pixelSizeUm:Number.isFinite(Number(e?.pixelSizeUm))?Number(e?.pixelSizeUm):void 0,removeTilt:!!e?.removeTilt,zeroPadTo:Number.isFinite(Number(e?.zeroPadTo))?Number(e?.zeroPadTo):void 0,recenterIfWrapped:!!e?.recenterIfWrapped});return vn({label:"Native PSF done",percent:100}),o}finally{try{n&&n()}catch{}setTimeout(()=>{try{si()}catch{}},220)}}H.runDesktopNativePsfMapForPopup=Aa;async function Na(e){return await qr({psfData:Array.isArray(e?.psfData)?e.psfData:[],pixelSizeUm:Number(e?.pixelSizeUm),maxFrequencyLpmm:Number.isFinite(Number(e?.maxFrequencyLpmm))?Number(e.maxFrequencyLpmm):void 0,points:Number.isFinite(Number(e?.points))?Number(e.points):void 0})}H.runDesktopNativeMtfMapForPopup=Na;async function _a(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut(),r=`native-tfmtf-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let s=null;const a=Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e.samplingSize))):256,c=Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e.zeroPadTo)):0,i=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):0,l=er(o,i,Number.isFinite(Number(e?.wavelengths?.[0]))?Number(e.wavelengths?.[0]):tn(n||[])),f=Sn(),p=Number(l?.fieldAngle?.x??0),d=Number(l?.fieldAngle?.y??0),u=Math.abs(p)>1e-12||Math.abs(d)>1e-12,y=f==="stop"||f==="entrance"?f:String(l?.type||"").toLowerCase()==="angle"&&u?"entrance":void 0;let m=Number.NaN;if(Array.isArray(e?.wavelengths)&&e.wavelengths.length>0){const b=Number(e.wavelengths[0]);Number.isFinite(b)&&b>0&&(m=b)}(!Number.isFinite(m)||m<=0)&&(m=tn(n||[])),(!Number.isFinite(m)||m<=0)&&(m=.5876);let h=Number.NaN,g=Number.NaN;try{const b=lo(t||[],m),A=Number(b?.fNumberWorking),v=Number(b?.focalLengthMm);Number.isFinite(A)&&A>0&&Number.isFinite(v)&&v>0&&(g=Math.abs(v),h=g/A)}catch{}if(!(Number.isFinite(h)&&h>0)||!(Number.isFinite(g)&&g>0)){const b=fo(t||[],m);h=Number(b?.pupilDiameterMm),g=Number(b?.focalLengthMm)}const w=!c||c===0?Math.max(a,512):Math.max(a,c),N=m*Math.abs(Number(g))/Math.max(1e-12,Math.abs(Number(h)))*(a/w),I=b=>{try{const A=e?.onProgress;if(typeof A!="function")return;A({percent:Number.isFinite(Number(b?.percent))?Number(b?.percent):void 0,message:typeof b?.message=="string"&&b.message.trim()?b.message:void 0})}catch{}};if(st())try{s=await Zo("analysis-progress",b=>{try{const A=b?.payload||{};if(!A||String(A.jobId||"")!==r)return;const v=Number(A?.percent),S=String(A?.message||A?.phase||"Computing Through-Focus MTF...");I({percent:Number.isFinite(v)?Math.max(0,Math.min(100,v)):void 0,message:S})}catch{}})}catch{s=null}try{return await _c({jobId:r,opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:i,pupilSamplingMode:e?.pupilSamplingMode==="stop"||e?.pupilSamplingMode==="entrance"?e.pupilSamplingMode:y,wavelengths:Array.isArray(e?.wavelengths)?e.wavelengths.filter(b=>Number.isFinite(Number(b))&&Number(b)>0).map(b=>Number(b)):[],targetFrequencyLpmm:Number.isFinite(Number(e?.targetFrequencyLpmm))?Number(e.targetFrequencyLpmm):10,defocusMinMm:Number.isFinite(Number(e?.defocusMinMm))?Number(e.defocusMinMm):-.5,defocusMaxMm:Number.isFinite(Number(e?.defocusMaxMm))?Number(e.defocusMaxMm):.5,steps:Number.isFinite(Number(e?.steps))?Math.floor(Number(e.steps)):21,samplingSize:a,zeroPadTo:w,pixelSizeUm:N,opdDisplayMode:String(e?.opdDisplayMode||"pistonTiltRemoved")},I)}finally{try{s&&s()}catch{}}}H.runDesktopNativeThroughFocusMtfForPopup=_a;async function rp(e){if(!st())throw new Error("Desktop runtime is not available");const{opticalSystemRows:t,sourceRows:n}=Ut(),o=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):0,r=Number.isFinite(Number(e?.targetFrequencyLpmm))?Number(e.targetFrequencyLpmm):10,s=Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e.samplingSize))):256,a=Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e.zeroPadTo)):0,c=!a||a===0?Math.max(s,512):Math.max(s,a),i=String(e?.opdDisplayMode||"pistonTiltRemoved"),l=i==="raw"||i==="pistonTiltDefocusRemoved"?i:"pistonTiltRemoved",f=Number(e?.wavelengthUm),p=Number.isFinite(f)&&f>0?f:tn(n||[]),d=Number.isFinite(p)&&p>0?p:.5876,u=(F,x,T)=>{if(!Array.isArray(F)||!Array.isArray(x)||F.length!==x.length)return NaN;const j=F.map((P,O)=>({x:Number(P),y:Number(x[O])})).filter(P=>Number.isFinite(P.x)&&Number.isFinite(P.y)).sort((P,O)=>P.x-O.x);if(!j.length)return NaN;if(j.length===1||T<=j[0].x)return j[0].y;if(T>=j[j.length-1].x)return j[j.length-1].y;for(let P=1;P<j.length;P++){const O=j[P-1],X=j[P];if(T<=X.x&&X.x>O.x){const k=(T-O.x)/(X.x-O.x);return O.y+k*(X.y-O.y)}}return j[j.length-1].y},y=(F,x)=>{if(!Array.isArray(F)||!Array.isArray(x)||F.length!==x.length||F.length===0)return NaN;let T=0,j=1/0;for(let O=0;O<F.length;O++){const X=Number(F[O]);if(!Number.isFinite(X))continue;const k=Math.abs(X);k<j&&(j=k,T=O)}const P=Number(x[T]);return Number.isFinite(P)?P:NaN},m=await _a({objectIndex:o,pupilSamplingMode:e?.pupilSamplingMode,wavelengths:[d],targetFrequencyLpmm:r,defocusMinMm:0,defocusMaxMm:0,steps:3,samplingSize:s,zeroPadTo:c,opdDisplayMode:l}),h=Array.isArray(m?.series)?m.series[0]:null,g=Array.isArray(m?.xAxis)?m.xAxis:[],w=y(g,Array.isArray(h?.mtfTangential)?h.mtfTangential:[]),R=y(g,Array.isArray(h?.mtfSagittal)?h.mtfSagittal:[]),N=await Sa({objectIndex:o,gridSize:s,wavelengthUm:d,opdDisplayMode:l}),I=s,b=Array.from({length:I},()=>new Float32Array(I)),A=Array.from({length:I},()=>new Float32Array(I)),v=Array.from({length:I},()=>Array(I).fill(!1)),S=Array.isArray(N?.displayOpdGrid)?N.displayOpdGrid:[],_=Array.isArray(N?.rawOpdGrid)?N.rawOpdGrid:[];for(let F=0;F<I;F++){const x=S[F]||[],T=_[F]||[];for(let j=0;j<I;j++){const P=T[j];if(P==null)continue;const O=Number(P);if(!Number.isFinite(O))continue;const X=x[j],k=X==null?NaN:Number(X),Y=Number.isFinite(k)?k:O;b[F][j]=Y*d,A[F][j]=1,v[F][j]=!0}}let M=Number.NaN,G=Number.NaN,z=Number.NaN,E=Number.NaN,L=Number.NaN;try{const F=lo(t||[],d),x=Number(F?.fNumberWorking),T=Number(F?.focalLengthMm),j=Number(F?.naImage),P=Number(F?.cutoffLpmm);Number.isFinite(x)&&x>0&&Number.isFinite(T)&&T>0&&(G=Math.abs(T),M=G/x,z=x),Number.isFinite(j)&&j>0&&(E=j),Number.isFinite(P)&&P>0&&(L=P)}catch{}if(!(Number.isFinite(M)&&M>0)||!(Number.isFinite(G)&&G>0)){const F=fo(t||[],d);M=Number(F?.pupilDiameterMm),G=Number(F?.focalLengthMm)}const V=d*Math.abs(Number(G))/Math.max(1e-12,Math.abs(Number(M)))*(s/c);if(!(Number.isFinite(z)&&z>0)){const F=Math.abs(Number(G))/Math.max(1e-12,Math.abs(Number(M)));Number.isFinite(F)&&F>0&&(z=F)}!(Number.isFinite(L)&&L>0)&&Number.isFinite(z)&&z>0&&(L=1e3/(Math.max(1e-12,d)*z));const K=await Aa({gridOpd:Array.from({length:I},(F,x)=>Array.from(b[x]||[])),gridAmplitude:Array.from({length:I},(F,x)=>Array.from(A[x]||[])),pupilMask:Array.from({length:I},(F,x)=>Array.from(v[x]||[])),wavelengthUm:d,pixelSizeUm:V,removeTilt:!1,zeroPadTo:c,recenterIfWrapped:!1}),$=await Na({psfData:K?.psfData,pixelSizeUm:V,maxFrequencyLpmm:Number.isFinite(r)?Math.max(1,r):10,points:121}),J=Array.isArray($?.frequencyAxis)?$.frequencyAxis:[],Q=Array.isArray($?.mtfTangential)?$.mtfTangential:[],le=Array.isArray($?.mtfSagittal)?$.mtfSagittal:[],Z=u(J,Q,r),B=u(J,le,r),C={backend:"desktop-native-rust",conditions:{objectIndex:o,wavelengthUm:d,targetFrequencyLpmm:r,samplingSize:s,zeroPadTo:c,opdDisplayMode:l,pupilSamplingMode:String(N?.pupilSamplingMode||""),pixelSizeUm:V,fNumberForDiffraction:z,naImage:E,cutoffLpmm:L},mtf:{tangential:Z,sagittal:B,nyquistLpmm:Number($?.nyquistLpmm)},tfmtfAtDefocus0:{tangential:w,sagittal:R},delta:{tangential:Number.isFinite(Z)&&Number.isFinite(w)?w-Z:NaN,sagittal:Number.isFinite(B)&&Number.isFinite(R)?R-B:NaN}};return console.log("📊 [Native Rust MTF vs TFMTF parity]",C),C}H.runDesktopNativeCompareMtfVsTfmtfForPopup=rp;async function Fa(e){if(!st())throw new Error("Desktop runtime is not available");const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut(),r=Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e.samplingSize))):256,s=Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e.zeroPadTo)):0,a=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):0,c=Sn();let i=e?.pupilSamplingMode==="stop"||e?.pupilSamplingMode==="entrance"?e.pupilSamplingMode:c==="stop"||c==="entrance"?c:void 0;if(!(i==="stop"||i==="entrance"))try{const C=String(await Fc("coopt.forceInfinitePupilMode")||"").trim().toLowerCase();(C==="stop"||C==="entrance")&&(i=C)}catch{}const l=C=>Number.isInteger(C)&&C>0&&(C&C-1)===0,f=C=>{let F=1;const x=Math.max(1,Math.floor(Number(C)||1));for(;F<x&&F<4096;)F<<=1;return F},p=Number.isFinite(Number(e?.firstFrequencyLpmm))?Number(e.firstFrequencyLpmm):10,d=Number.isFinite(Number(e?.secondFrequencyLpmm))?Number(e.secondFrequencyLpmm):30,u=Number.isFinite(Number(e?.fieldMin))?Number(e.fieldMin):0,y=Number.isFinite(Number(e?.fieldMax))?Number(e.fieldMax):10,m=Math.min(u,y),h=Math.max(u,y),g=Number.isFinite(Number(e?.steps))?Math.max(3,Math.floor(Number(e.steps))):21,w=String(e?.opdDisplayMode||"pistonTiltRemoved"),R=w==="raw"||w==="pistonTiltDefocusRemoved"?w:"pistonTiltRemoved",N=e?.fieldAxisMode==="height"?"height":"angle",I=121,b=Number.isFinite(s)&&s>=r&&l(s),A=Math.max(r,2*(I-1)),v=f(A);let S=b?s:v;b&&S===r&&r<=32&&(S=64);const _=(()=>{const C=Array.isArray(e?.wavelengths)?e.wavelengths.filter(x=>Number.isFinite(Number(x))&&Number(x)>0).map(x=>Number(x)):[];if(C.length>0)return C;const F=tn(n||[]);return[Number.isFinite(F)&&F>0?F:.5876]})(),M=Array.from({length:g},(C,F)=>{if(g<=1)return m;const x=F/(g-1);return m+x*(h-m)}),G=(C,F)=>{try{const x=e?.onProgress;typeof x=="function"&&x({percent:C,message:F})}catch{}};let z=null,E=null,L=null;try{const{preloadRustRayTracingWasm:C}=await De(async()=>{const{preloadRustRayTracingWasm:x}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(T=>T.bO);return{preloadRustRayTracingWasm:x}},__vite__mapDeps([0,1])),F=await C();z=typeof F?.run_native_opd_map_wasm_json=="function"?F.run_native_opd_map_wasm_json:null,E=typeof F?.run_native_psf_from_opd_wasm_json=="function"?F.run_native_psf_from_opd_wasm_json:null,L=typeof F?.run_native_mtf_from_psf_wasm_json=="function"?F.run_native_mtf_from_psf_wasm_json:null}catch{z=null,E=null,L=null}const ne=(C,F,x)=>{if(!Array.isArray(C)||!Array.isArray(F)||C.length!==F.length)return 0;const T=C.map((j,P)=>({x:Number(j),y:Number(F[P])})).filter(j=>Number.isFinite(j.x)&&Number.isFinite(j.y)).sort((j,P)=>j.x-P.x);if(!T.length)return 0;if(T.length===1||x<=T[0].x)return Math.max(0,Math.min(1,T[0].y));if(x>=T[T.length-1].x)return Math.max(0,Math.min(1,T[T.length-1].y));for(let j=1;j<T.length;j++){const P=T[j-1],O=T[j];if(x<=O.x&&O.x>P.x){const X=(x-P.x)/(O.x-P.x);return Math.max(0,Math.min(1,P.y+X*(O.y-P.y)))}}return Math.max(0,Math.min(1,T[T.length-1].y))},V=(C,F)=>{if(!Array.isArray(C)||C.length===0||!Number.isFinite(F))return[null,null];const x=C.map(T=>Number(T)).filter(T=>Number.isFinite(T)).sort((T,j)=>T-j);if(!x.length)return[null,null];if(F<=x[0])return[x[0],x[0]];if(F>=x[x.length-1])return[x[x.length-1],x[x.length-1]];for(let T=1;T<x.length;T++)if(F<=x[T])return[x[T-1],x[T]];return[x[x.length-1],x[x.length-1]]},K=C=>{if(!Array.isArray(C)||C.length===0)return;const F=C.length;let x=-1;for(let P=0;P<F;P++)if(Number.isFinite(C[P])){x=P;break}if(x<0){for(let P=0;P<F;P++)C[P]=0;return}for(let P=0;P<x;P++)C[P]=C[x];let T=x,j=x+1;for(;j<F;){if(Number.isFinite(C[j])){T=j,j+=1;continue}const P=j;for(;j<F&&!Number.isFinite(C[j]);)j+=1;const O=j-1;if(j<F){const X=C[T],k=C[j],Y=j-T;for(let D=P;D<=O;D++){const U=(D-T)/Y;C[D]=X+U*(k-X)}T=j}else for(let X=P;X<=O;X++)C[X]=C[T]}},$=C=>{const F=Array.isArray(o)?o.map(j=>{try{return JSON.parse(JSON.stringify(j))}catch{return{...j||{}}}}):[];F.length||F.push(N==="angle"?{name:"AutoField0",position:"Angle",xHeightAngle:0,yHeightAngle:C,x:0,y:C}:{name:"AutoField0",position:"Rectangle",xHeight:0,yHeight:C,x:0,y:C});const x=Math.max(0,Math.min(a,F.length-1)),T=F[x]&&typeof F[x]=="object"?F[x]:{};return N==="angle"?(T.position="Angle",T.xHeightAngle=0,T.yHeightAngle=C,T.x=0,T.y=C):(T.position="Rectangle",T.xHeight=0,T.yHeight=C,T.x=0,T.y=C),F[x]=T,F},J=C=>{const x=Number(C);return Math.abs(0)>0||Math.abs(x)>0?Math.abs(0)>=Math.abs(x)?"x":"y":"x"},Q=C=>{let F=Number.NaN,x=Number.NaN;try{const X=lo(t||[],C),k=Number(X?.fNumberWorking),Y=Number(X?.focalLengthMm);Number.isFinite(Y)&&Y>0&&Number.isFinite(k)&&k>0&&(x=Math.abs(Y),F=x/k)}catch{}const T=fo(t||[],C),j=Number(T?.pupilDiameterMm),P=Number(T?.focalLengthMm);if(!(Number.isFinite(F)&&F>0)&&Number.isFinite(j)&&j>0&&(F=Math.abs(j)),!(Number.isFinite(x)&&x>0)&&Number.isFinite(P)&&P>0&&(x=Math.abs(P)),!(Number.isFinite(F)&&F>0))try{const X=Number(Xs(t||[])),k=Number.isFinite(X)&&X>=0?t?.[X]:null,Y=k?.semidia??k?.Semidia??k?.["Semi Diameter"]??k?.aperture??k?.Aperture??NaN,D=Math.abs(parseFloat(Y));if(Number.isFinite(D)&&D>0){const ee=!!(k&&(k.aperture!==void 0||k.Aperture!==void 0))?D*.5:D;Number.isFinite(ee)&&ee>0&&(F=ee*2)}}catch{}if(!(Number.isFinite(x)&&x>0))try{const X=Number(Ks(t||[],C));Number.isFinite(X)&&Math.abs(X)>1e-9&&X!==1/0&&(x=Math.abs(X))}catch{}return Number.isFinite(x)&&x>0||(x=100),Number.isFinite(F)&&F>0||(F=10),C*Math.abs(Number(x))/Math.max(1e-12,Math.abs(Number(F)))*(r/S)},le=[],Z=Math.max(1,_.length*M.length);let B=0;G(5,`Computing Object MTF: ${_.length} wavelength(s), ${M.length} field points`);for(const C of _){const F=[],x=[],T=[],j=[],P=[],O=Q(C);let X,k;const Y=N==="angle"&&i!=="stop";try{const D=M.length>0&&Number.isFinite(Number(M[0]))?Number(M[0]):0,U=$(D),ae={opticalSystemRows:t,sourceRows:n,objectRows:U,objectIndex:a,surfaceIndex:void 0,gridSize:r,wavelengthUm:C,pupilSamplingMode:i||(N==="angle"?"entrance":void 0),opdDisplayMode:R},Ae=(()=>{if(z){const se=z(JSON.stringify(ae));return typeof se=="string"?JSON.parse(se):se}return null})()||await Bo(ae),te=Number(Ae?.targetSurface);Number.isFinite(te)&&(X=te);const re=Number(Ae?.effectivePupilRadiusMm);Y&&Number.isFinite(re)&&re>0&&(k=re)}catch{}for(let D=0;D<M.length;D++){const U=M[D],ee=B+1,oe=(C*1e3).toFixed(1),ae=N==="height"?"mm":"deg",Ae=5+ee/Z*90;G(Math.max(5,Math.min(95,Ae)),`Computing Object MTF: λ=${oe}nm, point ${D+1}/${M.length} (${Number(U).toFixed(3)} ${ae})`);const te=$(U),se=i||(N==="angle"?"entrance":void 0);let ue=NaN,me=NaN,ge=NaN,xe=NaN,W=null,fe=null,we=null,Se=null,ye={};try{const he={opticalSystemRows:t,sourceRows:n,objectRows:te,objectIndex:a,surfaceIndex:X,gridSize:r,wavelengthUm:C,pupilRadiusMm:k,pupilSamplingMode:se,opdDisplayMode:R},_e=(()=>{if(z){const Fe=z(JSON.stringify(he));return typeof Fe=="string"?JSON.parse(Fe):Fe}return null})()||await Bo(he);ye=_e;const ve=r,Ce=Array.from({length:ve},()=>Array.from({length:ve},()=>0)),Te=Array.from({length:ve},()=>Array.from({length:ve},()=>!1)),Ie=Array.isArray(_e?.displayOpdGrid)?_e.displayOpdGrid:[],de=Array.isArray(_e?.rawOpdGrid)?_e.rawOpdGrid:[];for(let Fe=0;Fe<ve;Fe++){const $e=Ie[Fe]||[],Qe=de[Fe]||[];for(let pt=0;pt<ve;pt++){const Xt=Qe[pt];if(Xt==null||Xt==="")continue;const Ct=Number(Xt);if(!Number.isFinite(Ct))continue;const Ne=$e[pt],qe=Ne==null||Ne===""?NaN:Number(Ne),ot=Number.isFinite(qe)?qe:Ct;Ce[Fe][pt]=ot*C,Te[Fe][pt]=!0}}const be=(()=>{if(E){const Fe=E(JSON.stringify({rawOpdGrid:_e?.rawOpdGrid,displayOpdGrid:_e?.displayOpdGrid,wavelengthUm:C,pixelSizeUm:O,removeTilt:!1,zeroPadTo:S}));return typeof Fe=="string"?JSON.parse(Fe):Fe}return null})()||await Ps({gridOpd:Ce,pupilMask:Te,wavelengthUm:C,pixelSizeUm:O,removeTilt:!1,zeroPadTo:S,recenterIfWrapped:!1}),Pe=(()=>{if(L){const Fe=L(JSON.stringify({psfData:be?.psfData,pixelSizeUm:O,maxFrequencyLpmm:Math.max(1,Math.max(p,d)*2),points:121}));return typeof Fe=="string"?JSON.parse(Fe):Fe}return null})()||await qr({psfData:be?.psfData,pixelSizeUm:O,maxFrequencyLpmm:Math.max(1,Math.max(p,d)*2),points:121}),Ee=Array.isArray(Pe?.frequencyAxis)?Pe.frequencyAxis:[],Ue=Array.isArray(Pe?.mtfTangential)?Pe.mtfTangential:[],Ge=Array.isArray(Pe?.mtfSagittal)?Pe.mtfSagittal:[],He=J(U),Me=He==="x"?Ge:Ue,ze=He==="x"?Ue:Ge;ue=ne(Ee,Me,p),me=ne(Ee,ze,p),ge=ne(Ee,Me,d),xe=ne(Ee,ze,d),[W,fe]=V(Ee,p),[we,Se]=V(Ee,d)}catch(he){ye={error:String(he)}}F.push(Number.isFinite(ue)?ue:NaN),x.push(Number.isFinite(me)?me:NaN),T.push(Number.isFinite(ge)?ge:NaN),j.push(Number.isFinite(xe)?xe:NaN);const ce=Number(ye?.sampleCount||0),pe=Number(ye?.hitCount||0);P.push({fieldValue:U,effectivePupilSamplingMode:String(ye?.pupilSamplingMode||""),effectivePupilRadiusMm:Number(ye?.effectivePupilRadiusMm),usedObjectPosition:String(ye?.usedObjectPosition||""),targetSurfaceIndex:Number(ye?.targetSurface),usedObjectIndex:Number(ye?.usedObjectIndex),opdSampleCount:ce,opdHitCount:pe,opdHitRate:ce>0?pe/ce:0,opdMessage:String(ye?.message||ye?.error||""),firstFrequencyLpmm:p,firstBracketLowLpmm:W,firstBracketHighLpmm:fe,firstValueMeridional:ue,firstValueSagittal:me,secondFrequencyLpmm:d,secondBracketLowLpmm:we,secondBracketHighLpmm:Se,secondValueMeridional:ge,secondValueSagittal:xe}),B+=1}le.push({wavelengthUm:C,label:`${(C*1e3).toFixed(1)}nm`,meridionalFirst:F,sagittalFirst:x,meridionalSecond:T,sagittalSecond:j,fieldDiagnostics:P}),K(F),K(x),K(T),K(j)}return{backend:"desktop-native-field-mtf-via-analysis-mtf",xAxis:M,axisMode:N,series:le,message:"Native Object MTF computed via background analysis MTF sampling"}}H.runDesktopNativeFieldMtfForPopup=Fa;async function ip(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut();return Mc({opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e?.objectIndex))):0,pupilSamplingMode:e?.pupilSamplingMode,wavelengths:Array.isArray(e?.wavelengths)?e.wavelengths:void 0,firstFrequencyLpmm:Number.isFinite(Number(e?.firstFrequencyLpmm))?Number(e?.firstFrequencyLpmm):void 0,secondFrequencyLpmm:Number.isFinite(Number(e?.secondFrequencyLpmm))?Number(e?.secondFrequencyLpmm):void 0,fieldMin:Number.isFinite(Number(e?.fieldMin))?Number(e?.fieldMin):void 0,fieldMax:Number.isFinite(Number(e?.fieldMax))?Number(e?.fieldMax):void 0,steps:Number.isFinite(Number(e?.steps))?Math.max(3,Math.floor(Number(e?.steps))):void 0,samplingSize:Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e?.samplingSize))):void 0,zeroPadTo:Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e?.zeroPadTo)):void 0,opdDisplayMode:e?.opdDisplayMode,fieldAxisMode:e?.fieldAxisMode,onProgress:e?.onProgress})}H.runPortableFieldMtfForPopup=ip;function sp(e){const t=Array.isArray(e)?e:[],n=t.map(r=>String(r?.position??r?.fieldType??r?.field_type??r?.field??r?.type??"").toLowerCase()).filter(Boolean);return n.some(r=>r.includes("rect")||r.includes("rectangle")||r.includes("height"))?"height":n.some(r=>r.includes("angle"))?"angle":t.some(r=>{const s=parseFloat(r?.yHeight??r?.y??r?.height??r?.y_height??Number.NaN);return Number.isFinite(s)&&Math.abs(s)>0})?"height":"angle"}function ap(e){if(sp(e)==="height"){const a=(Array.isArray(e)?e:[]).map(p=>parseFloat(p?.yHeight??p?.y??p?.height??p?.y_height??Number.NaN)).filter(p=>Number.isFinite(p));if(!a.length)return{fieldValues:[.001],heightMode:!0};let c=Math.min(...a),i=Math.max(...a);if(c<=0&&(c=.001,i<c&&(i=c)),c===i)return{fieldValues:[c],heightMode:!0};const l=10;return{fieldValues:Array.from({length:l},(p,d)=>{const u=d/(l-1);return parseFloat((c+(i-c)*u).toFixed(6))}),heightMode:!0}}let n=0;for(const a of Array.isArray(e)?e:[]){const c=[a?.yFieldAngle,a?.yAngle,a?.fieldAngle,a?.xFieldAngle,a?.xAngle,a?.xHeightAngle,a?.yHeightAngle];for(const i of c)typeof i=="number"&&Number.isFinite(i)&&(n=Math.max(n,Math.abs(i)))}n>0||(n=20);let o=1;n<=5?o=.5:n<=15?o=1:n<=40?o=2:o=Math.ceil(n/25);const r=n*.001,s=[];for(let a=r;a<=n+1e-9;a+=o)s.push(parseFloat(a.toFixed(6)));return s[s.length-1]!==n&&s.push(n),{fieldValues:s,heightMode:!1}}async function cp(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut(),{fieldValues:r,heightMode:s}=ap(o||[]),a=(()=>{const i=Array.isArray(e?.wavelengths)?e.wavelengths.filter(p=>Number.isFinite(Number(p))&&Number(p)>0).map(p=>Number(p)):[];if(i.length)return i;const l=(Array.isArray(n)?n:[]).map(p=>Number(p?.wavelength)).filter(p=>Number.isFinite(p)&&p>0);if(l.length)return l;const f=tn(n||[]);return[Number.isFinite(f)&&f>0?f:.5876]})(),c=[];for(let i=0;i<a.length;i++){const l=a[i],f=i/Math.max(1,a.length)*100,p=100/Math.max(1,a.length),d=await Rc({opticalSystemRows:t,sourceRows:n,objectRows:o,fieldSamples:r,heightMode:s,wavelength:l});c.push({fieldValues:Array.isArray(d?.fieldValues)?d.fieldValues:r,idealHeights:Array.isArray(d?.idealHeights)?d.idealHeights:[],realHeights:Array.isArray(d?.realHeights)?d.realHeights:[],distortion:Array.isArray(d?.distortion)?d.distortion:[],distortionPercent:Array.isArray(d?.distortionPercent)?d.distortionPercent:[],meta:{...d?.meta||{},wavelength:l,heightMode:s}});try{e?.onProgress?.({percent:f+p,message:`Distortion (λ=${l.toFixed(4)} μm)`})}catch{}}return{backend:"portable-distortion",allData:c}}async function lp(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut(),r=Number.isFinite(Number(e?.wavelength))&&Number(e?.wavelength)>0?Number(e?.wavelength):(()=>{const a=tn(n||[]);return Number.isFinite(a)&&a>0?a:.5876})(),s=await Cc({opticalSystemRows:t,sourceRows:n,objectRows:o,gridSize:Number.isFinite(Number(e?.gridSize))?Number(e?.gridSize):20,wavelength:r});try{e?.onProgress?.({percent:100,message:"Done"})}catch{}return{backend:"portable-grid-distortion",idealGrid:{x:Array.isArray(s?.idealX)?s.idealX:[],y:Array.isArray(s?.idealY)?s.idealY:[]},realGrid:{x:Array.isArray(s?.realX)?s.realX:[],y:Array.isArray(s?.realY)?s.realY:[]},gridSize:Number.isFinite(Number(s?.gridSize))?Number(s.gridSize):20,maxFieldAngle:Number.isFinite(Number(s?.maxFieldAngle))?Number(s.maxFieldAngle):0,meta:{...s?.meta||{},wavelength:r}}}H.runPortableDistortionDataForPopup=cp;H.runPortableGridDistortionForPopup=lp;async function up(e){const t=await Fa(e||{}),n=Array.isArray(t?.series)?t.series:[],o=Array.isArray(n?.[0]?.fieldDiagnostics)?n[0].fieldDiagnostics:[],r=[];let s=0,a=0,c=0;for(let u=1;u<o.length;u++){const y=o[u-1]||{},m=o[u]||{};String(y.effectivePupilSamplingMode||"")!==String(m.effectivePupilSamplingMode||"")&&(s+=1),String(y.usedObjectPosition||"")!==String(m.usedObjectPosition||"")&&(a+=1),Number(y.targetSurfaceIndex)!==Number(m.targetSurfaceIndex)&&(c+=1);const h=Math.abs(Number(m.firstValueMeridional)-Number(y.firstValueMeridional)),g=Math.abs(Number(m.firstValueSagittal)-Number(y.firstValueSagittal)),w=Math.abs(Number(m.secondValueMeridional)-Number(y.secondValueMeridional)),R=Math.abs(Number(m.secondValueSagittal)-Number(y.secondValueSagittal));[h,g,w,R].some(N=>Number.isFinite(N)&&N>.12)&&r.push({fromField:Number(y.fieldValue),toField:Number(m.fieldValue),deltaFirstMeridional:h,deltaFirstSagittal:g,deltaSecondMeridional:w,deltaSecondSagittal:R,opdHitRateFrom:Number(y.opdHitRate),opdHitRateTo:Number(m.opdHitRate),opdHitRateDelta:Math.abs(Number(m.opdHitRate)-Number(y.opdHitRate)),modeFrom:String(y.effectivePupilSamplingMode||""),modeTo:String(m.effectivePupilSamplingMode||""),usedObjectPositionFrom:String(y.usedObjectPosition||""),firstBracketTo:[Number(m.firstBracketLowLpmm),Number(m.firstBracketHighLpmm)],secondBracketTo:[Number(m.secondBracketLowLpmm),Number(m.secondBracketHighLpmm)],usedObjectPositionTo:String(m.usedObjectPosition||""),targetSurfaceFrom:Number(y.targetSurfaceIndex),targetSurfaceTo:Number(m.targetSurfaceIndex),usedObjectIndexFrom:Number(y.usedObjectIndex),usedObjectIndexTo:Number(m.usedObjectIndex)})}let i=0,l=0,f=0,p=0;for(const u of r)String(u.modeFrom||"")!==String(u.modeTo||"")&&(i+=1),String(u.usedObjectPositionFrom||"")!==String(u.usedObjectPositionTo||"")&&(l+=1),Number(u.targetSurfaceFrom)!==Number(u.targetSurfaceTo)&&(f+=1),Number.isFinite(Number(u.opdHitRateDelta))&&Number(u.opdHitRateDelta)>.03&&(p+=1);const d={conditions:{objectIndex:Number.isFinite(Number(e?.objectIndex))?Number(e?.objectIndex):0,firstFrequencyLpmm:Number.isFinite(Number(e?.firstFrequencyLpmm))?Number(e?.firstFrequencyLpmm):10,secondFrequencyLpmm:Number.isFinite(Number(e?.secondFrequencyLpmm))?Number(e?.secondFrequencyLpmm):30,fieldMin:Number.isFinite(Number(e?.fieldMin))?Number(e?.fieldMin):0,fieldMax:Number.isFinite(Number(e?.fieldMax))?Number(e?.fieldMax):10,steps:Number.isFinite(Number(e?.steps))?Number(e?.steps):21,samplingSize:Number.isFinite(Number(e?.samplingSize))?Number(e?.samplingSize):256,zeroPadTo:Number.isFinite(Number(e?.zeroPadTo))?Number(e?.zeroPadTo):0,opdDisplayMode:String(e?.opdDisplayMode||"pistonTiltRemoved"),fieldAxisMode:e?.fieldAxisMode==="height"?"height":"angle",pupilSamplingMode:e?.pupilSamplingMode},diagnosticsCount:o.length,summary:{discontinuityCount:r.length,modeSwitchAcrossAll:s,positionSwitchAcrossAll:a,surfaceSwitchAcrossAll:c,modeSwitchOnDiscontinuity:i,positionSwitchOnDiscontinuity:l,surfaceSwitchOnDiscontinuity:f,hitRateJumpOnDiscontinuity:p},discontinuities:r,diagnostics:o};try{console.log("📊 [Object MTF native diagnostics await]",d)}catch{}return d}H.runDesktopNativeFieldMtfDiagnosticsForPopup=up;function er(e,t,n){const o=Array.isArray(e)?e:[],r=Number.isInteger(t)?Math.max(0,Math.min(t,Math.max(0,o.length-1))):0,s=o[r]||{},a=String(s?.position??s?.object??s?.type??"").trim().toLowerCase();return a.includes("angle")||a==="point"?{objectIndex:r,type:"Angle",fieldAngle:{x:Number(s?.xHeightAngle??s?.xFieldAngle??s?.xAngle??s?.x??0)||0,y:Number(s?.yHeightAngle??s?.yFieldAngle??s?.fieldAngle??s?.yAngle??s?.angle??s?.y??0)||0},xHeight:0,yHeight:0,wavelength:Number.isFinite(n)&&n>0?n:.5876}:{objectIndex:r,type:"Height",fieldAngle:{x:0,y:0},xHeight:Number(s?.xHeight??s?.x??0)||0,yHeight:Number(s?.yHeight??s?.y??s?.height??0)||0,wavelength:Number.isFinite(n)&&n>0?n:.5876}}function Ji(e,t,n=12){const o=Array.isArray(e)?e:[],r=Array.isArray(t)?t:[],s=Math.min(o.length,r.length),a=s>0?Math.min(Array.isArray(o[0])?o[0].length:0,Array.isArray(r[0])?r[0].length:0):0,c=[];let i=0,l=0,f=0,p=0,d=0,u=0,y=0,m=0,h=0;for(let M=0;M<s;M++){const G=Array.isArray(o[M])?o[M]:[],z=Array.isArray(r[M])?r[M]:[];for(let E=0;E<a;E++){const L=G[E]===null||G[E]===void 0?NaN:Number(G[E]),ne=z[E]===null||z[E]===void 0?NaN:Number(z[E]);if(!Number.isFinite(L)||!Number.isFinite(ne))continue;const V=Math.abs(L-ne);i+=1,l+=V,f+=V*V,V>p&&(p=V),d+=L,u+=ne,y+=L*L,m+=ne*ne,h+=L*ne,c.push({x:E,y:M,ts:L,native:ne,abs:V})}}const g=i>0?d/i:0,w=i>0?u/i:0,R=y-d*d/Math.max(1,i),N=m-u*u/Math.max(1,i),I=h-d*u/Math.max(1,i),b=Math.abs(R)>1e-20?I/R:NaN,A=Number.isFinite(b)?w-b*g:NaN,v=R>1e-20&&N>1e-20?I/Math.sqrt(R*N):NaN;let S=NaN,_=NaN;if(i>0){let M=0,G=0;for(const z of c){if(Number.isFinite(b)&&Number.isFinite(A)){const L=b*z.ts+A,ne=z.native-L;M+=ne*ne}const E=z.native- -z.ts;G+=E*E}Number.isFinite(b)&&Number.isFinite(A)&&(S=Math.sqrt(M/i)),_=Math.sqrt(G/i)}return c.sort((M,G)=>G.abs-M.abs),{overlap:{width:a,height:s,comparedPointCount:i},maxAbsDeltaWaves:p,meanAbsDeltaWaves:i>0?l/i:0,rmsDeltaWaves:i>0?Math.sqrt(f/i):0,affine:{slopeTsToNative:b,interceptTsToNative:A,correlation:v,rmsAfterAffineFit:S,rmsAfterNegTs:_,meanTs:g,meanNative:w},topDiffs:c.slice(0,Math.max(1,Math.floor(n))).map(M=>({x:M.x,y:M.y,tsWaves:M.ts,nativeWaves:M.native,absDeltaWaves:M.abs}))}}async function pp(e={}){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut();if(!Array.isArray(t)||t.length===0)throw new Error("No optical system data");if(!Array.isArray(o)||o.length===0)throw new Error("No object data");const r=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):Math.max(0,o.length-1),s=Math.max(0,Math.min(r,o.length-1)),a=Math.max(16,Math.floor(Number(e?.gridSize??128))),c=Number.isFinite(Number(e?.wavelengthUm))?Number(e.wavelengthUm):tn(n),i=Number.isFinite(Number(e?.zeroPadTo))?Number(e.zeroPadTo):0,l=String(e?.opdDisplayMode||"pistonTiltRemoved"),f=er(o,s,c),p=oo(t,c),u=await ro(p).generateWavefrontMap(f,a,"circular",{recordRays:!1,progressEvery:0,renderFromZernike:!1,skipZernikeFit:!0,opdMode:"simple",opdDisplayMode:l});if(u&&u.error){const U=String(u.error?.message||"Wavefront map generation failed");throw new Error(U)}const y=a,m=Array.from({length:y},()=>new Float32Array(y)),h=Array.from({length:y},()=>new Float32Array(y)),g=Array.from({length:y},()=>Array(y).fill(!1)),w=new Float32Array(y),R=new Float32Array(y),N=Number.isFinite(Number(u?.pupilRange))&&Number(u.pupilRange)>0?Number(u.pupilRange):1;for(let U=0;U<y;U++){const ee=U/(y-1||1)*2-1;w[U]=ee*N,R[U]=ee*N}const I=Array.isArray(u?.pupilCoordinates)?u.pupilCoordinates:[],b=u?.display&&Array.isArray(u.display.opds)?u.display.opds:Array.isArray(u?.opds)?u.opds:[],A=Math.min(I.length,b.length);for(let U=0;U<A;U++){const ee=I[U],oe=Number.isInteger(ee?.ix)?ee.ix:null,ae=Number.isInteger(ee?.iy)?ee.iy:null;if(oe===null||ae===null||oe<0||oe>=y||ae<0||ae>=y)continue;const Ae=Number(b[U]);Number.isFinite(Ae)&&(g[ae][oe]=!0,m[ae][oe]=Ae,h[ae][oe]=1)}const v={gridSize:y,wavelength:c,rayData:[],gridData:{opd:m,amplitude:h,pupilMask:g,xCoords:w,yCoords:R}},S=fo(t,c),_=Number.isFinite(Number(S?.pupilDiameterMm))&&Number(S.pupilDiameterMm)>0?Number(S.pupilDiameterMm):10,M=Number.isFinite(Number(S?.focalLengthMm))&&Number(S.focalLengthMm)>0?Number(S.focalLengthMm):100,G=new Sl,z={samplingSize:y,wavelength:c,zeroPadTo:i,pupilDiameter:_,focalLength:M,removeTilt:!1,recenterIfWrapped:!1},E=await G.calculatePSF(v,{...z,forceImplementation:st()?"native":"javascript"}),L=await G.calculatePSF(v,{...z,forceImplementation:"javascript"}),ne=Array.isArray(E?.psfData)?E.psfData:[],V=Array.isArray(L?.psfData)?L.psfData:[],K=Math.min(ne.length,V.length),$=K>0?Math.min((ne[0]||[]).length,(V[0]||[]).length):0;let J=0,Q=0,le=0,Z=0;for(let U=0;U<K;U++)for(let ee=0;ee<$;ee++){const oe=Number(ne[U]?.[ee]),ae=Number(V[U]?.[ee]);if(!Number.isFinite(oe)||!Number.isFinite(ae))continue;const Ae=oe-ae,te=Math.abs(Ae);Q+=Ae*Ae,le+=te,te>Z&&(Z=te),J++}const B=J>0?Math.sqrt(Q/J):NaN,C=J>0?le/J:NaN,F=E?.metrics||{},x=L?.metrics||{},T=Number(F?.fwhm?.average),j=Number(x?.fwhm?.average),P=Number(F?.strehlRatio),O=Number(x?.strehlRatio),X=F?.centerPosition||{},k=x?.centerPosition||{},Y="📊 [PSF Parity][main] native-vs-js grid="+K+"x"+$+" n="+J+" rmsAbs="+(Number.isFinite(B)?B.toExponential(4):"NaN")+" meanAbs="+(Number.isFinite(C)?C.toExponential(4):"NaN")+" maxAbs="+(Number.isFinite(Z)?Z.toExponential(4):"NaN")+" strehlΔ="+(Number.isFinite(P)&&Number.isFinite(O)?(P-O).toExponential(4):"NaN")+" fwhmAvgΔ="+(Number.isFinite(T)&&Number.isFinite(j)?(T-j).toExponential(4):"NaN")+"µm centerΔ=("+(Number(X?.x)-Number(k?.x))+","+(Number(X?.y)-Number(k?.y))+")",D={objectIndex:s,gridSize:y,wavelengthUm:c,impl:{native:String(E?.implementationUsed||"unknown"),js:String(L?.implementationUsed||"unknown")},abs:{rms:Number.isFinite(B)?B:null,mean:Number.isFinite(C)?C:null,max:Number.isFinite(Z)?Z:null},delta:{strehl:Number.isFinite(P)&&Number.isFinite(O)?P-O:null,fwhmAvgUm:Number.isFinite(T)&&Number.isFinite(j)?T-j:null,centerX:Number(X?.x)-Number(k?.x),centerY:Number(X?.y)-Number(k?.y)}};return globalThis.__lastPsfParityLine=Y,globalThis.__lastPsfParity=D,console.log(Y),{parityLine:Y,parity:D,nativeResult:E,jsResult:L}}H.runPsfParityInMain=pp;function dp(e,t){const n=Array.isArray(e)?e:[],o=n.length,r=o>0&&Array.isArray(n[0])?n[0].length:0;if(o===0||r===0)return[];const s=(c,i)=>{const l=n[i];return Array.isArray(l)?l[c]??null:null},a=(c,i,l)=>{const f=Array.from({length:c},()=>Array.from({length:i},()=>null));for(let p=0;p<c;p++)for(let d=0;d<i;d++)f[p][d]=l(d,p);return f};switch(t){case"identity":return a(o,r,(c,i)=>s(c,i));case"flipX":return a(o,r,(c,i)=>s(r-1-c,i));case"flipY":return a(o,r,(c,i)=>s(c,o-1-i));case"flipXY":return a(o,r,(c,i)=>s(r-1-c,o-1-i));case"transpose":return a(r,o,(c,i)=>s(i,c));case"transposeFlipX":return a(r,o,(c,i)=>s(i,o-1-c));case"transposeFlipY":return a(r,o,(c,i)=>s(r-1-i,c));case"transposeFlipXY":return a(r,o,(c,i)=>s(r-1-i,o-1-c));default:return a(o,r,(c,i)=>s(c,i))}}function Qi(e,t,n){const o=["identity","flipX","flipY","flipXY","transpose","transposeFlipX","transposeFlipY","transposeFlipXY"];let r={alignment:"identity",summary:Ji(e,t,n)};for(const s of o){const a=dp(e,s),c=Ji(a,t,n),i=Number(c?.overlap?.comparedPointCount||0),l=Number(r?.summary?.overlap?.comparedPointCount||0),f=Number(c?.rmsDeltaWaves||1/0),p=Number(r?.summary?.rmsDeltaWaves||1/0);(i>l||i===l&&f<p)&&(r={alignment:s,summary:c})}return r}function es(e,t,n,o="display"){const r=b=>!Array.isArray(b)||b.length===0?!1:Array.isArray(b[0]),s=b=>(Array.isArray(b)?b:[]).map(v=>Array.isArray(v)?v.map(S=>{if(S==null)return null;const _=Number(S);return Number.isFinite(_)?_:null}):[]),a=Array.isArray(e?.pupilCoordinates)?e.pupilCoordinates:[],c=Number.isFinite(Number(t))?Math.max(3,Math.floor(Number(t))):129,i=Array.from({length:c},()=>Array.from({length:c},()=>null)),l=e?.display?.opdsInWavelengths,f=e?.display?.opds,p=e?.raw?.opdsInWavelengths??e?.opdsInWavelengths,d=e?.raw?.opds??e?.opds,u=o==="raw"?p:l,y=o==="raw"?d:f,m=o==="raw"?l:p,h=o==="raw"?f:d;if(r(u))return s(u);if(r(m))return s(m);const g=Array.isArray(u)?u:Array.isArray(m)?m:Array.isArray(y)?y:Array.isArray(h)?h:[],w=!Array.isArray(u)&&!Array.isArray(m);if(!Array.isArray(g)||g.length===0)return i;const R=b=>{if(b==null)return null;const A=Number(b);return Number.isFinite(A)?w?!Number.isFinite(n)||n<=0?null:A/n:A:null};let N=0;for(let b=0;b<g.length;b++){const A=a[b];if(!A||!Number.isFinite(Number(A?.ix))||!Number.isFinite(Number(A?.iy)))continue;const v=Math.floor(Number(A.ix)),S=Math.floor(Number(A.iy));if(v<0||v>=c||S<0||S>=c)continue;const _=R(g[b]);Number.isFinite(Number(_))&&(i[S][v]=Number(_),N+=1)}if(N>0)return i;const I=Math.min(g.length,c*c);for(let b=0;b<I;b++){const A=Math.floor(b/c),v=b%c,S=R(g[b]);Number.isFinite(Number(S))&&(i[A][v]=Number(S))}return i}async function mp(e={}){if(!st())throw new Error("Desktop runtime is not available");const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut();if(!Array.isArray(t)||t.length===0)throw new Error("compareOpdTsVsRustForPopup: opticalSystemRows is empty");const r=Number.isInteger(e?.objectIndex)?Number(e.objectIndex):0,s=Number.isFinite(Number(e?.gridSize))?Math.max(17,Math.floor(Number(e.gridSize))):129,a=Number.isFinite(Number(e?.wavelengthUm))?Number(e?.wavelengthUm):tn(n),c=e?.opdDisplayMode||"pistonTiltRemoved",i=er(o,r,a),l=Sn(),f=Number(i?.fieldAngle?.x??0),p=Number(i?.fieldAngle?.y??0),d=Math.abs(f)>1e-12||Math.abs(p)>1e-12,u=l==="stop"||l==="entrance"?l:String(i?.type||"").toLowerCase()==="angle"&&d?"entrance":void 0,y=oo(t,a),m=Number(y?._getCachedStopRadiusMm?.()),h=await Bo({opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:r,gridSize:s,wavelengthUm:a,pupilRadiusMm:Number.isFinite(m)&&m>0?m:void 0,pupilSamplingMode:u,opdDisplayMode:c}),w=await ro(y).generateWavefrontMap(i,s,"circular",{recordRays:!1,progressEvery:0,renderFromZernike:!1,skipZernikeFit:!0,opdMode:"simple",opdDisplayMode:c}),R=es(w,s,a,"display"),N=es(w,s,a,"raw"),I=Array.isArray(h?.displayOpdGrid)?h.displayOpdGrid:Array.isArray(h?.rawOpdGrid)?h.rawOpdGrid:[],b=Array.isArray(h?.rawOpdGrid)?h.rawOpdGrid:Array.isArray(h?.displayOpdGrid)?h.displayOpdGrid:[],A=Qi(R,I,Number(e?.topK)||12),v=Qi(N,b,Number(e?.topK)||12),S={...A.summary,alignment:A.alignment},_={...v.summary,alignment:v.alignment},M={config:{objectIndex:r,gridSize:s,wavelengthUm:a,opdDisplayMode:c},nativeMeta:{backend:h?.backend,targetSurface:h?.targetSurface,stopSurface:h?.stopSurface,usedObjectPosition:h?.usedObjectPosition,usedObjectX:h?.usedObjectX,usedObjectY:h?.usedObjectY,hitCount:h?.hitCount,sampleCount:h?.sampleCount,message:h?.message},rawSummary:_,summary:S};try{console.log("📊 [OPD TS-Rust][opener] summary",{objectIndex:r,gridSize:s,wavelengthUm:a,mode:c,tsGridShape:{h:Array.isArray(R)?R.length:0,w:Array.isArray(R)&&Array.isArray(R[0])?R[0].length:0},comparedPointCount:S?.overlap?.comparedPointCount,maxAbsDeltaWaves:S?.maxAbsDeltaWaves,meanAbsDeltaWaves:S?.meanAbsDeltaWaves,rmsDeltaWaves:S?.rmsDeltaWaves,rawComparedPointCount:_?.overlap?.comparedPointCount,rawMaxAbsDeltaWaves:_?.maxAbsDeltaWaves,rawMeanAbsDeltaWaves:_?.meanAbsDeltaWaves,rawRmsDeltaWaves:_?.rmsDeltaWaves,alignment:S?.alignment,rawAlignment:_?.alignment,affineSlopeTsToNative:S?.affine?.slopeTsToNative,affineInterceptTsToNative:S?.affine?.interceptTsToNative,affineCorrelation:S?.affine?.correlation,affineRmsAfterFit:S?.affine?.rmsAfterAffineFit,affineRmsAfterNegTs:S?.affine?.rmsAfterNegTs,rawAffineSlopeTsToNative:_?.affine?.slopeTsToNative,rawAffineInterceptTsToNative:_?.affine?.interceptTsToNative,rawAffineCorrelation:_?.affine?.correlation,rawAffineRmsAfterFit:_?.affine?.rmsAfterAffineFit,rawAffineRmsAfterNegTs:_?.affine?.rmsAfterNegTs,nativeHitCount:h?.hitCount,nativeSampleCount:h?.sampleCount})}catch{}try{H.__COOPT_LAST_OPD_TS_RUST_DIFF={...M,at:Date.now()}}catch{}return M}H.compareOpdTsVsRustForPopup=mp;function fp(e,t){if(!Array.isArray(e))return[];const n=Number(t),o=e.map(l=>l&&typeof l=="object"?{...l}:l);if(!Number.isFinite(n)||Math.abs(n)<1e-15)return o;const r=o.findIndex(l=>String(l?.["object type"]??l?.object??l?.Object??"").trim().toLowerCase()==="image"),s=r>0?r-1:Math.max(0,o.length-2);if(s<0||s>=o.length)return o;const a=o[s]&&typeof o[s]=="object"?{...o[s]}:{},c=Number(a.thickness),i=Number.isFinite(c)?c:0;return a.thickness=i+n,o[s]=a,o}async function gp(e){const t=A=>{const v=Number(A);return!Number.isFinite(v)||v<=0?null:v>10?v/1e3:v},n=A=>{if(A===!0||A===1)return!0;const v=String(A??"").trim().toLowerCase();return v==="1"||v==="true"||v==="yes"||v.includes("primary")},o=(A,v)=>{const S=String(A?.id??"").trim();if(S)return S;const _=String(A?.name??"").trim();if(_)return _;const M=String(A?.position??A?.object??"").trim();return M?`Object ${v+1} (${M})`:`Object ${v+1}`},r=(A,v)=>{const S=String(v||"all").trim().toLowerCase()==="primary"?"primary":"all",_=["#2563eb","#dc2626","#16a34a","#7c3aed","#ea580c","#0891b2","#4f46e5","#0f766e"],M=(Array.isArray(A)?A:[]).map((z,E)=>{const L=t(z?.wavelength??z?.Wavelength);return!Number.isFinite(L)||L<=0?null:{wavelengthUm:L,isPrimary:n(z?.primary??z?.Primary),name:String(z?.name??"").trim(),idx:E}}).filter(z=>!!z);if(M.length===0)return[{wavelengthUm:.5876,label:"Primary 587.6nm",color:_[0],isPrimary:!0}];const G=[];for(let z=0;z<M.length;z++){const E=M[z];if(G.some(V=>Math.abs(V.wavelengthUm-E.wavelengthUm)<1e-12))continue;const L=E.wavelengthUm*1e3,ne=Number.isFinite(L)?L.toFixed(1):"587.6";G.push({wavelengthUm:E.wavelengthUm,label:E.isPrimary?`Primary ${ne}nm`:E.name||`${ne}nm`,color:_[G.length%_.length],isPrimary:E.isPrimary})}if(S==="primary"){const z=G.find(E=>E.isPrimary)||G[0];return z?[z]:[]}return G},{opticalSystemRows:s,sourceRows:a,objectRows:c}=Ut(),i=Array.isArray(e?.objectRows)?e.objectRows:null,l=i&&i.length>0?i:c,f=fp(s,e?.defocusMm),p=A=>{if(!(!Array.isArray(A)||A.length===0)){for(let v=A.length-1;v>=0;v--){const S=A[v]||{};if(String(S?.["object type"]??S?.object??S?.Object??"").trim().toLowerCase()==="image")return v}return Math.max(0,A.length-1)}},d=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):p(f),u=typeof performance<"u"&&typeof performance.now=="function"?performance.now():Date.now(),y=e?.pattern?String(e.pattern):"annular",m=np(f,a),h=typeof performance<"u"&&typeof performance.now=="function"?performance.now():Date.now(),g={opticalSystemRows:f,sourceRows:a,objectRows:l,surfaceIndex:d,rayCount:Number.isInteger(e?.rayCount)?e.rayCount:void 0,ringCount:Number.isInteger(e?.ringCount)?e.ringCount:void 0,pattern:y,wavelengthMode:e?.wavelengthMode?String(e.wavelengthMode):void 0};try{const A=y==="grid"||y==="annular"?y:"annular",v=Number.isInteger(e?.rayCount)?Math.max(1,Number(e?.rayCount)):501,S=Number.isInteger(e?.ringCount)?Math.max(1,Number(e?.ringCount)):10,_=r(a,e?.wavelengthMode?String(e.wavelengthMode):void 0),M=[];if(Array.isArray(l)&&l.length>0&&_.length>0)for(let G=0;G<l.length;G++){const z=l[G];for(let E=0;E<_.length;E++){const L=_[E],ne=qo(z,f,v,null,{annularRingCount:S,wavelengthUm:L.wavelengthUm,pattern:A}),V=(Array.isArray(ne)?ne:[]).map(K=>({startP:{x:Number(K?.startP?.x)||0,y:Number(K?.startP?.y)||0,z:Number(K?.startP?.z)||0},dir:{x:Number(K?.dir?.x)||0,y:Number(K?.dir?.y)||0,z:Number(K?.dir?.z)||1},wavelengthUm:L.wavelengthUm,pupilU:Number.isFinite(Number(K?.planeCoords?.u))?Number(K.planeCoords.u):void 0,pupilV:Number.isFinite(Number(K?.planeCoords?.v))?Number(K.planeCoords.v):void 0,isChief:K?.isChief===!0||K?.isChief==null&&(K?.rayIndex===0||K?.index===0)})).filter(K=>Number.isFinite(K.startP.x)&&Number.isFinite(K.startP.y)&&Number.isFinite(K.startP.z));V.length!==0&&M.push({label:`${o(z,G)} ${L.label}`,color:L.color,hasFieldAngle:String(z?.position??z?.object??"").trim().toLowerCase()==="angle",rays:V})}}M.length>0&&(g.raySeries=M)}catch{}let w=await Ai(g);if((Number(w?.totalHitRays)||0)<=0){const v=(S=>{if(!Array.isArray(S)||S.length===0)return;const _=M=>String(M??"").trim().toLowerCase().replace(/[\s_-]+/g,"");for(let M=0;M<S.length;M++){const G=S[M]||{},z=_(G?.["object type"]??G?.object??G?.Object??G?.type??G?.surfType);if(z==="image"||z.startsWith("image"))return M}for(let M=S.length-1;M>=0;M--){const G=S[M]||{},z=_(G?.["object type"]??G?.object??G?.Object??G?.type??G?.surfType);if(z!=="object"&&z!=="gap"&&z!=="airgap")return M}})(f);if(Number.isInteger(v)&&v!==d){w=await Ai({...g,surfaceIndex:v});try{w.__surfaceIndexRetriedFrom=d,w.__surfaceIndexRetriedTo=v}catch{}}}const N=typeof performance<"u"&&typeof performance.now=="function"?performance.now():Date.now(),I=Number(w?.rayGenerationMs),b=Number(w?.traceMs);return{...w,airyRadiusUm:Number.isFinite(m)?m:void 0,timingMs:{raySeriesGeneration:Number.isFinite(I)?I:0,nativeInvoke:Math.max(0,N-h),nativeTrace:Number.isFinite(b)?b:0,total:Math.max(0,N-u)}}}H.runDesktopNativeSpotRaytraceForPopup=gp;async function yp(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=Ut();return Ic({opticalSystemRows:t,sourceRows:n,objectRows:o,surfaceIndex:Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):void 0,rayCount:Number.isInteger(e?.rayCount)?Number(e.rayCount):void 0,referenceFocusMode:e?.referenceFocusMode||"current-paraxial",wavelengthMode:e?.wavelengthMode||"all"})}H.runDesktopNativeSphericalAberrationForPopup=yp;function hp(e){const t=r=>{const s=Number(r);return!Number.isFinite(s)||s<=0?null:s>10?s/1e3:s},n=Array.isArray(e)?e:[],o=[];for(const r of n){const s=t(r?.wavelength??r?.Wavelength);if(!(s===null||!Number.isFinite(s)||s<=0)&&(o.some(a=>Math.abs(a-s)<1e-12)||o.push(s),o.length>=6))break}return o.length>0?o:[.5876]}function bp(e,t){const n=p=>{const d=new Map;for(const u of Array.isArray(p)?p:[]){const y=Number(u?.wavelength);if(!Number.isFinite(y))continue;const m=y.toFixed(7);d.set(m,u)}return d},o=[],r=(p,d,u)=>{const y=n(d),m=n(u),h=[];for(const[g,w]of m.entries()){const R=y.get(g);if(!R)continue;const N=Array.isArray(R?.points)?R.points:[],I=Array.isArray(w?.points)?w.points:[];if(N.length===0||I.length===0)continue;const b=new Map;for(const _ of N){const M=Number(_?.pupilCoordinate),G=Number(_?.longitudinalAberration);!Number.isFinite(M)||!Number.isFinite(G)||b.set(M.toFixed(6),G)}let A=0,v=0,S=0;for(const _ of I){const M=Number(_?.pupilCoordinate),G=Number(_?.longitudinalAberration);if(!Number.isFinite(M)||!Number.isFinite(G))continue;const z=b.get(M.toFixed(6));if(!Number.isFinite(z))continue;const E=Math.abs(G-z);A=Math.max(A,E),v+=E,S+=1}h.push({wl:Number(w?.wavelength),maxAbs:A,sumAbs:v,count:S})}return h},s=r("meridional",e?.meridionalData,t?.meridionalData),a=r("sagittal",e?.sagittalData,t?.sagittalData),c=new Map;for(const p of s){if(!Number.isFinite(p.wl))continue;const d=c.get(p.wl)||{wavelengthUm:p.wl,meridionalMaxAbsDeltaMm:0,sagittalMaxAbsDeltaMm:0,comparedPointCount:0,sumAbs:0};d.meridionalMaxAbsDeltaMm=Math.max(d.meridionalMaxAbsDeltaMm,p.maxAbs),d.comparedPointCount+=p.count,d.sumAbs+=p.sumAbs,c.set(p.wl,d)}for(const p of a){if(!Number.isFinite(p.wl))continue;const d=c.get(p.wl)||{wavelengthUm:p.wl,meridionalMaxAbsDeltaMm:0,sagittalMaxAbsDeltaMm:0,comparedPointCount:0,sumAbs:0};d.sagittalMaxAbsDeltaMm=Math.max(d.sagittalMaxAbsDeltaMm,p.maxAbs),d.comparedPointCount+=p.count,d.sumAbs+=p.sumAbs,c.set(p.wl,d)}let i=0,l=0,f=0;for(const p of c.values())i=Math.max(i,p.meridionalMaxAbsDeltaMm,p.sagittalMaxAbsDeltaMm),l+=p.sumAbs,f+=p.comparedPointCount,o.push({wavelengthUm:p.wavelengthUm,meridionalMaxAbsDeltaMm:p.meridionalMaxAbsDeltaMm,sagittalMaxAbsDeltaMm:p.sagittalMaxAbsDeltaMm,comparedPointCount:p.comparedPointCount});return o.sort((p,d)=>p.wavelengthUm-d.wavelengthUm),{comparedPointCount:f,maxAbsDeltaMm:i,meanAbsDeltaMm:f>0?l/f:0,largeDeltaThresholdMm:.001,hasLargeDelta:i>.001,perWavelength:o}}async function wp(e){const t=e?.rustResult;if(!t||typeof t!="object")throw new Error("compareSphericalAberrationTsVsRustForPopup: rustResult is required");const{opticalSystemRows:n,sourceRows:o}=Ut();if(!Array.isArray(n)||n.length===0)throw new Error("compareSphericalAberrationTsVsRustForPopup: opticalSystemRows is empty");const r=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):n.length-1,s=Number.isInteger(e?.rayCount)?Number(e.rayCount):51,a=e?.referenceFocusMode||"current-paraxial",c=hp(o),{calculateLongitudinalAberrationAsync:i}=await De(async()=>{const{calculateLongitudinalAberrationAsync:p}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(d=>d.bW);return{calculateLongitudinalAberrationAsync:p}},__vite__mapDeps([0,1])),l=await i(n,r,c,s,{silent:!0,referenceFocusMode:a}),f=bp(l,t);try{console.log("📊 [SA TS-Rust][opener] summary",{comparedPointCount:f?.comparedPointCount,maxAbsDeltaMm:f?.maxAbsDeltaMm,meanAbsDeltaMm:f?.meanAbsDeltaMm,hasLargeDelta:f?.hasLargeDelta})}catch{}try{H.__COOPT_LAST_SA_TS_RUST_DIFF={...f,at:Date.now(),referenceFocusMode:a,rayCount:s,surfaceIndex:r}}catch{}return f}H.compareSphericalAberrationTsVsRustForPopup=wp;function vp(){try{if(typeof window<"u"&&window.__COOPT_DISABLE_DESKTOP_RUST_ANALYSIS===!0)return!1;if(typeof window<"u"&&window.__COOPT_FORCE_DESKTOP_RUST_ANALYSIS===!0)return!0}catch{}return!0}H.shouldUseDesktopRustAnalysis=vp;const Lr="coopt.forceInfinitePupilMode",Mo="coopt-force-infinite-pupil-mode-changed";function so(e){const t=typeof e=="string"?e.trim().toLowerCase():"";return t==="stop"||t==="entrance"?t:""}function Sn(){try{const e=H.__COOPT_FORCE_INFINITE_PUPIL_MODE;if(e)return so(e)}catch{}try{const e=localStorage.getItem(Lr);return so(e)}catch{return""}}function Br(e){const t=so(e);try{if(t)H.__COOPT_FORCE_INFINITE_PUPIL_MODE=t;else try{delete H.__COOPT_FORCE_INFINITE_PUPIL_MODE}catch{H.__COOPT_FORCE_INFINITE_PUPIL_MODE=void 0}}catch{}try{t?localStorage.setItem(Lr,t):localStorage.removeItem(Lr)}catch{}}function xp(){const e=Sn();if(e)try{H.__COOPT_FORCE_INFINITE_PUPIL_MODE=e}catch{}}function Ma(){st()&&(H.__cooptForceInfinitePupilModeBridgeInstalled||(H.__cooptForceInfinitePupilModeBridgeInstalled=!0,H.__cooptBroadcastForceInfinitePupilMode=e=>{const t=so(e);Br(t),(async()=>{try{const n=await De(()=>import("./event-H3glLpTN.js"),__vite__mapDeps([2,0,1]));if(n&&typeof n.emit=="function"&&await n.emit(Mo,{mode:t}),n&&typeof n.emitTo=="function"){try{await n.emitTo("main",Mo,{mode:t})}catch{}try{await n.emitTo("settings-window",Mo,{mode:t})}catch{}}}catch{}})()},(async()=>{try{const e=await De(()=>import("./event-H3glLpTN.js"),__vite__mapDeps([2,0,1]));if(!e||typeof e.listen!="function")return;const t=await e.listen(Mo,n=>{const o=so(n?.payload?.mode);Br(o)});H.__cooptForceInfinitePupilModeBridgeUnlisten=t}catch{}})()))}H.__cooptGetForceInfinitePupilMode=Sn;H.__cooptSetForceInfinitePupilMode=Br;xp();Ma();function jo(){return{getOpticalSystemRows:H.getOpticalSystemRows||(()=>[]),getObjectRows:H.getObjectRows||(()=>[]),generateCrossBeam:H.generateCrossBeam||(()=>({results:[]})),generateInfiniteSystemCrossBeam:H.generateInfiniteSystemCrossBeam||(()=>({results:[]})),drawOpticalSystemSurfaces:H.drawOpticalSystemSurfaces||(()=>{}),drawCrossBeamRays:H.drawCrossBeamRays||(()=>{}),harmonizeSceneGeometry:H.harmonizeSceneGeometry||(()=>{}),clearAllOpticalElements:H.clearAllOpticalElements||(()=>{})}}const Sp="coopt.surfaceColorOverrides";function Ap(e){return!!e&&typeof e=="object"&&!Array.isArray(e)}function Np(e){if(typeof e=="number"&&Number.isFinite(e))return e;if(typeof e!="string")return null;const t=e.trim();if(!t)return null;if(/^0x[0-9a-fA-F]{6}$/.test(t))return parseInt(t.slice(2),16);if(/^#[0-9a-fA-F]{6}$/.test(t))return parseInt(t.slice(1),16);const n=Number(t);return Number.isFinite(n)?n:null}function _p(e,t){try{const n=String(e?._blockId??"").trim(),o=String(e?._surfaceRole??"").trim();if(n&&o)return`p:${n}|${o}`}catch{}try{const n=Number(e?.id);if(Number.isFinite(n))return`id:${Math.floor(n)}`}catch{}return`i:${Math.floor(Number(t)||0)}`}function zr(e){const t=String(e?.surfType||e?.type||"").trim().toLowerCase(),n=String(e?.["object type"]||"").trim().toLowerCase();return t==="coord break"||t==="coordinate break"||t==="cb"||t==="coordtrans"||t==="coordinatebreak"||t==="coord trans"||t==="coordinate transform"||t==="ct"||n==="coord break"||n==="coordinate break"||n==="cb"||n==="coordtrans"||n==="coordinatebreak"}function ao(e){const t=String(e?._blockType??e?.blockType??"").trim().toLowerCase();if(t==="gap"||t==="airgap")return!0;const n=String(e?.["object type"]??e?.type??"").trim().toLowerCase();if(n==="gap"||n==="air gap"||n==="airgap")return!0;const o=String(e?._surfaceRole??"").trim().toLowerCase();return o==="gap"||o==="airgap"}function ts(e){const t=String(e??"").trim().toUpperCase();return!(!t||t==="AIR"||t==="0"||t==="MIRROR")}function ns(e){const t=String(e?._blockType??e?.blockType??"").trim().toLowerCase(),n=String(e?._surfaceRole??e?.surfaceRole??"").trim().toLowerCase();return t==="lens"||t==="glass"||t==="element"||n==="lens"||n==="front"||n==="back"}function Fp(e,t){if(!e||!t||(e["object type"]||"")==="Object"||ao(e)||ao(t)||zr(e)||zr(t))return!1;const n=ts(e.material),o=ts(t.material),r=ns(e),s=ns(t);return n||o||r||s}function Ro(e){const t=[{value:e?.semidia,isDiameter:!1},{value:e?.semiDiameter,isDiameter:!1},{value:e?.["semi-diameter"],isDiameter:!1},{value:e?.semi_diameter,isDiameter:!1},{value:e?.clearAperture,isDiameter:!1},{value:e?.Clear_Aperture,isDiameter:!1},{value:e?.diameter,isDiameter:!0}];for(const n of t){const o=n.value,r=Number(o),s=Number.isFinite(r)?r:parseFloat(String(o??"")),a=Number.isFinite(s)?s:NaN;if(Number.isFinite(a)&&a>0)return n.isDiameter?a*.5:a}return null}function Mp(e){return String(e?.["object type"]??e?.type??"").trim().toLowerCase()==="object"}function os(e){return!(!e||Mp(e)||ao(e)||zr(e))}function rs(e){const t=[];if(!Array.isArray(e)||e.length===0)return t;let n=0;for(let o=0;o<e.length;o++){t.push({origin:{x:0,y:0,z:n}});const r=e[o]?.thickness,s=Number.isFinite(Number(r))?Number(r):parseFloat(String(r??""));Number.isFinite(s)&&(n+=s)}return t}function Rp(){try{if(typeof localStorage>"u")return{};const e=localStorage.getItem(Sp);if(!e)return{};const t=JSON.parse(e);return Ap(t)?t:{}}catch{return{}}}function Cp(e){if(!e)return;const t=[];e.traverse(n=>{n?.userData?.type==="popupLensFill"&&t.push(n)}),[...new Set(t)].forEach(n=>{e.remove(n),n.geometry&&n.geometry.dispose(),n.material&&(Array.isArray(n.material)?n.material.forEach(o=>o.dispose()):n.material.dispose())})}function Ar(e,t){if(!e||!e.geometry||!e.geometry.attributes?.position||!t)return[];const n=e.geometry.attributes.position,o=[];for(let r=0;r<n.count;r++){const s=new t.Vector3(n.getX(r),n.getY(r),n.getZ(r));e.localToWorld(s),Number.isFinite(s.x)&&Number.isFinite(s.y)&&Number.isFinite(s.z)&&o.push(s)}return o}function is(e,t,n){if(!Array.isArray(e)||e.length<2||!t||!n)return e||[];const o=e[0].distanceTo(t)+e[e.length-1].distanceTo(n),r=e[0].distanceTo(n)+e[e.length-1].distanceTo(t);return o<=r?e.slice():e.slice().reverse()}function Ra(e){return""}function Nr(e,t,n){const o=Ra(),r=`${t}${o}`;try{const s=e?.document?.getElementById?.("status");s&&(s.textContent=r)}catch{}try{e?.postMessage?.({status:r},"*")}catch{}}function to(e){const{popupWindow:t,scene:n,viewAxis:o,opticalSystemRows:r,source:s="unknown"}=e;if(!n||!Array.isArray(r)||r.length<2)return;const a=t?.THREE||H.THREE||Gr;if(!a)return;const c=!1;Cp(n);const i=new Map,l=new Map;n.traverse(g=>{const w=g?.userData||{};if(w.type==="surfaceProfile"&&(w.profileType==="XZ"||w.profileType==="YZ")){const R=Number(w.surfaceIndex),N=Number.isFinite(R)?r[R-1]:null;if(N&&ao(N)){g.visible=!1;return}g.visible=!0,w.profileType===o&&Number.isFinite(R)&&i.set(R,g);return}if(w.type==="connectionLine"&&(w.direction==="XZ"||w.direction==="YZ")){const R=Number(w.surfaceIndex),N=Number.isFinite(R)?r[R-1]:null;if(N&&ao(N)){g.visible=!1;return}g.visible=!0,w.direction===o&&Number.isFinite(R)&&(l.has(R)||l.set(R,[]),l.get(R).push(g));return}(w.isRayLine||w.type==="ray"||w.type==="optical-ray")&&(g.renderOrder=Math.max(Number(g.renderOrder)||0,1e5),g.material&&(Array.isArray(g.material)?g.material:[g.material]).forEach(N=>{N&&(N.depthTest=!1,N.depthWrite=!1,N.transparent=!0)}))});const f=Rp(),p=(()=>{try{const g=Go(r);return Array.isArray(g)&&g.length>0?g:rs(r)}catch{return rs(r)}})(),d=g=>o==="YZ"?g.y:g.x,u=[];for(let g=0;g<r.length-1;g++){const w=r[g],R=r[g+1];Fp(w,R)&&u.push(g)}const y=[];if(u.length===0){console.warn("[RenderWindow] Lens fill strict interval detection matched 0 pairs. Falling back to geometry-based adjacent pairing.");for(let g=0;g<r.length-1;g++){const w=r[g],R=r[g+1];if(!os(w)||!os(R))continue;const N=Ro(w),I=Ro(R);Number.isFinite(N)&&Number.isFinite(I)&&N>0&&I>0&&y.push(g)}}const m=u.length>0?u:y;let h=0;for(const g of m){const w=r[g],R=r[g+1],N=g+1,I=g+2,b=i.get(N),A=i.get(I),v=l.get(N)||[];let S=b?Ar(b,a):[],_=A?Ar(A,a):[];if(S.length<2||_.length<2){const D=p?.[g]?.origin,U=p?.[g+1]?.origin,ee=Ro(w),oe=Ro(R);D&&U&&Number.isFinite(ee)&&Number.isFinite(oe)&&ee>0&&oe>0&&(o==="YZ"?(S=[new a.Vector3(Number(D.x)||0,-ee,Number(D.z)||0),new a.Vector3(Number(D.x)||0,ee,Number(D.z)||0)],_=[new a.Vector3(Number(U.x)||0,-oe,Number(U.z)||0),new a.Vector3(Number(U.x)||0,oe,Number(U.z)||0)]):(S=[new a.Vector3(-ee,Number(D.y)||0,Number(D.z)||0),new a.Vector3(ee,Number(D.y)||0,Number(D.z)||0)],_=[new a.Vector3(-oe,Number(U.y)||0,Number(U.z)||0),new a.Vector3(oe,Number(U.y)||0,Number(U.z)||0)]))}if(S.length<2||_.length<2)continue;const M=[],G=v.map(D=>{const U=Ar(D,a);if(U.length<2)return null;const ee=U.reduce((oe,ae)=>oe+d(ae),0)/U.length;return{pts:U,avg:ee}}).filter(Boolean).sort((D,U)=>D.avg-U.avg);if(G.length>=2){const D=G[0].pts,U=G[G.length-1].pts,ee=S[0],oe=S[S.length-1],ae=_[0],Ae=_[_.length-1],te=is(U,oe,Ae),re=is(D,ae,ee);M.push(...S),M.push(...te.slice(1)),M.push(..._.slice().reverse().slice(1)),M.push(...re.slice(1))}else{const D=S[0],U=S[S.length-1],ee=_[0],oe=_[_.length-1],ae=D.distanceToSquared(ee)+U.distanceToSquared(oe),Ae=D.distanceToSquared(oe)+U.distanceToSquared(ee),te=ae<=Ae?_.slice():_.slice().reverse();M.push(...S),M.push(...te.slice().reverse().slice(1))}const z=S[0],E=S[S.length-1],L=_[0],ne=_[_.length-1],V=z.distanceToSquared(L)+E.distanceToSquared(ne),K=z.distanceToSquared(ne)+E.distanceToSquared(L),$=V<=K?_.slice():_.slice().reverse(),J=Math.max(2,Math.min(S.length,$.length)),Q=(D,U)=>{if(!Array.isArray(D)||D.length===0||U<2)return[];const ee=[];for(let oe=0;oe<U;oe++){const ae=oe/(U-1),Ae=Math.round(ae*(D.length-1)),te=D[Math.max(0,Math.min(Ae,D.length-1))];Number.isFinite(te?.x)&&Number.isFinite(te?.y)&&Number.isFinite(te?.z)&&ee.push(te.clone())}return ee},le=Q(S,J),Z=Q($,J);if(le.length<2||Z.length<2||le.length!==Z.length)continue;const B=le.length*2,C=new Float32Array(B*3);for(let D=0;D<le.length;D++){const U=le[D],ee=Z[D],oe=D*2,ae=oe+1;C[oe*3]=U.x,C[oe*3+1]=U.y,C[oe*3+2]=U.z,C[ae*3]=ee.x,C[ae*3+1]=ee.y,C[ae*3+2]=ee.z}const F=[];for(let D=0;D<le.length-1;D++){const U=D*2,ee=U+1,oe=U+2,ae=U+3;F.push(U,ee,oe),F.push(ee,ae,oe)}if(F.length<3)continue;const x=B>65535?new Uint32Array(F):new Uint16Array(F),T=new a.BufferGeometry;T.setAttribute("position",new a.BufferAttribute(C,3)),T.setIndex(new a.BufferAttribute(x,1)),T.computeVertexNormals();const j=_p(w,g),P=Np(f?.[j]),X=P!==null&&!(D=>{if(D===null||!Number.isFinite(D))return!1;const U=D>>16&255,ee=D>>8&255,oe=D&255;return U>=245&&ee>=245&&oe>=245})(P)?P:52479,k=new a.MeshBasicMaterial({color:X,transparent:!0,opacity:.72,side:a.DoubleSide,depthTest:!1,depthWrite:!1}),Y=new a.Mesh(T,k);Y.frustumCulled=!1,Y.renderOrder=1400,Y.userData={type:"popupLensFill",viewAxis:o,surfaceIndex:N,isOpticalElement:!0},n.add(Y),h+=1}try{H.__cooptLastFillDebug={time:Date.now(),viewAxis:o,source:s,createdMeshCount:h,sceneChildCount:Array.isArray(n?.children)?n.children.length:0,forceDebugFill:c}}catch{}}function Ip(){if(H.popupMessageHandlerRegistered)return;H.popupMessageHandlerRegistered=!0;const e=t=>{try{const n=H.popupCamera||t?.camera,o=H.popupRenderer||t?.renderer;if(!n?.isOrthographicCamera||!o||typeof o.getSize!="function")return;const r=t?.THREE||H.THREE;if(!r?.Vector2)return;const s=o.getSize(new r.Vector2),a=Number(s?.x)||0,c=Number(s?.y)||0;if(!Number.isFinite(a)||!Number.isFinite(c)||a<2||c<2)return;const i=a/c,l=n.top-n.bottom||1,f=(n.left+n.right)/2,p=(n.top+n.bottom)/2,d=l*i;n.left=f-d/2,n.right=f+d/2,n.top=p+l/2,n.bottom=p-l/2,n.updateProjectionMatrix()}catch{}};window.addEventListener("message",async t=>{const n=H.popup3DWindow;if(!n||t.source!==n)return;const o=t.data||{};if(o.action==="popup-ready"){const r=H.popup3DWindow;if(r&&!r.closed)try{const a={userAdjustedView:!1,viewAxis:"YZ",rayCount:(()=>{const c=document.getElementById("draw-ray-count-input"),i=parseInt(c?.value||"51",10);return Number.isFinite(i)&&i>0?i:51})(),rayColorMode:"object"};setTimeout(()=>{window.postMessage({action:"draw-cross",...a},"*")},100)}catch(s){console.error("Failed to trigger initial draw:",s)}return}if(o.action==="popup-resize"){if(!H.popup3DWindow){console.warn("⚠️ Popup window reference is unavailable (resize)");return}const r=H.popup3DWindow,a=(o?.viewAxis||H.__currentPopupViewAxis||"YZ").toString().toUpperCase()==="XZ"?"XZ":"YZ";try{const i=r.camera?.userData?.__drawCrossOrthoBounds,l=Number.isFinite(i?.centerZ)?i.centerZ:void 0,f={camera:r.camera,controls:r.controls,scene:r.scene,renderer:r.renderer,includeRayStartMargin:!0,preserveDrawCrossBounds:!0,storeDrawCrossBounds:!1,...Number.isFinite(l)?{centerZOverride:l}:{}};a==="XZ"&&typeof H.setCameraForXZCrossSection=="function"?H.setCameraForXZCrossSection(f):a==="YZ"&&typeof H.setCameraForYZCrossSection=="function"&&H.setCameraForYZCrossSection(f),e(r);const p=H.popupRenderer||r?.renderer,d=H.popupScene||r?.scene,u=H.popupCamera||r?.camera;p&&d&&u&&p.render(d,u)}catch(c){console.error("❌ Popup resize handling error:",c)}return}if(o.action==="draw-cross"){try{const r=H.popup3DWindow,a=(o?.viewAxis||"YZ").toString().toUpperCase()==="XZ"?"XZ":"YZ",c=o?.userAdjustedView===!0,i=o?.target&&Number.isFinite(o.target.x)&&Number.isFinite(o.target.y)&&Number.isFinite(o.target.z)?o.target:null,l=(()=>{const K=parseInt(o?.rayCount??51,10);return Number.isFinite(K)&&K>0?K:51})(),f=o?.rayColorMode==="segment"?"segment":"object";try{typeof H.setRayColorMode=="function"&&H.setRayColorMode(f)}catch{}const p=typeof globalThis<"u"?!!H.__cooptOptimizerIsRunning:!1;if(p||typeof H.loadActiveConfigurationToTables=="function"&&H.loadActiveConfigurationToTables(),!p)try{typeof globalThis<"u"&&(H.__cooptOpticalSystemRowsOverride=null)}catch{}const{getOpticalSystemRows:d,getObjectRows:u,drawOpticalSystemSurfaces:y,harmonizeSceneGeometry:m,clearAllOpticalElements:h,generateCrossBeam:g,generateInfiniteSystemCrossBeam:w,drawCrossBeamRays:R}=jo(),N=d();try{const K=Array.isArray(N)?N.map(($,J)=>({index0:J,id:$&&typeof $=="object"?$.id??null:null,type:$&&typeof $=="object"?$.type??$["object type"]??"":"",_blockId:$&&typeof $=="object"?$._blockId??"":"",_surfaceRole:$&&typeof $=="object"?$._surfaceRole??"":""})):[];H.popup3DWindow?.postMessage({action:"surface-list",surfaces:K},"*")}catch{}if(!N||N.length===0){H.popup3DWindow.postMessage({status:"Error: No optical system data"},"*");return}const I=H.popupScene||H.popup3DWindow?.scene||null;if(!I){H.popup3DWindow.postMessage({status:"Error: Scene not ready"},"*");return}const b=H.popupRenderer||H.popup3DWindow?.renderer||null;I&&[...I.children].forEach($=>{I.remove($),$.geometry&&$.geometry.dispose(),$.material&&(Array.isArray($.material)?$.material.forEach(J=>J.dispose()):$.material.dispose())}),typeof h=="function"&&h(I),typeof y=="function"&&y({opticalSystemData:N,scene:I,showSemidiaRing:!0,showSurfaceOrigins:!1,crossSectionOnly:!1,showDesignIntentLabels:jr()}),typeof m=="function"&&m(I);let A=[];try{typeof u=="function"&&(A=u()||[])}catch{}if(!Array.isArray(A)||A.length===0)try{if(H.tableObject&&typeof H.tableObject.getData=="function")A=H.tableObject.getData();else{const K=document.getElementById("table-object");K&&K.tabulator?A=K.tabulator.getData():A=[]}}catch{A=[]}const S=(N[0]||{})?.thickness,_=S!=null&&S!=="",M=_?String(S).trim().toUpperCase():"",G=Number(S),z=_&&(S===1/0||M==="INF"||M==="INFINITY"||M==="∞"||Number.isFinite(G)&&Math.abs(G)>1e6),E=!A||A.length===0||A.every(K=>K.position==="Angle"||!K.height&&!K.y&&!K.xHeightAngle&&!K.yHeightAngle||parseFloat(K.height||0)===0),L=_?z:E,ne=(()=>{try{if(typeof H.getPrimaryWavelength=="function"){const K=Number(H.getPrimaryWavelength());if(Number.isFinite(K)&&K>0)return K}}catch{}throw new Error("Primary wavelength is unavailable. Please set Source Primary Wavelength.")})();let V;if(L){const K=A.map(le=>({x:parseFloat(le.xHeightAngle)||0,y:parseFloat(le.yHeightAngle)||0})),$=le=>{const Z=le?.["object type"]??le?.object??le?.Object??le?.type??"",B=String(Z).trim().toLowerCase().replace(/[\s_-]+/g,"");return B==="image"||B.startsWith("image")},J=N.findIndex(le=>le&&$(le)),Q=J>=0?J:Math.max(0,N.length-1);J<0&&console.warn(`⚠️ [DrawCross] Image surface not detected by object type. Falling back to last row index=${Q}.`),V=await w(N,K,{rayCount:l,debugMode:!1,wavelength:ne,crossType:"both",targetSurfaceIndex:Q,pupilSamplingMode:"entrance",logEntrancePupilConfig:!1,angleUnit:"deg",chiefZ:-20})}else{const K=J=>{const Q=parseFloat(J);return Number.isFinite(Q)?Q:0},$=(A||[]).map((J,Q)=>{if(Array.isArray(J))return{x:K(J[1]),y:K(J[2]),z:0,objectIndex:Q};const le=K(J.xHeightAngle??J.x??J.height??J.heightX),Z=K(J.yHeightAngle??J.y??J.height??J.heightY);return{x:le,y:Z,z:0,objectIndex:J.objectIndex??Q}});$.length===0&&$.push({x:0,y:0,z:0,objectIndex:0}),V=await g(N,$,{rayCount:l,debugMode:!1,wavelength:ne,crossType:"both"})}if(V.success){let K=[];if(V.results&&Array.isArray(V.results)?V.results.forEach(Q=>{Q.rays&&Array.isArray(Q.rays)&&(K=K.concat(Q.rays))}):V.allTracedRays&&Array.isArray(V.allTracedRays)?K=V.allTracedRays:V.tracedRays&&Array.isArray(V.tracedRays)?K=V.tracedRays:Array.isArray(V)&&(K=V),K&&K.length>0){R(K,I);try{r.__lastCrossRays=K,H.__lastCrossRays=K}catch{}}try{to({popupWindow:r,scene:I,viewAxis:a,opticalSystemRows:N,source:"draw-cross"})}catch(Q){console.warn("⚠️ Popup lens fill postprocess failed:",Q)}if(m(I),!r)console.warn("⚠️ Popup window reference missing (camera)");else if(a==="XZ"&&typeof H.setCameraForXZCrossSection=="function")H.setCameraForXZCrossSection({camera:H.popupCamera||r.camera,controls:H.popupControls||r.controls,scene:H.popupScene||r.scene,renderer:H.popupRenderer||r.renderer,includeRayStartMargin:!0,preserveDrawCrossBounds:c===!0,preserveCurrentOrthoBounds:c===!0,storeDrawCrossBounds:c!==!0,...c===!0&&i?{targetOverride:i}:{}});else if(a==="YZ"&&typeof H.setCameraForYZCrossSection=="function")H.setCameraForYZCrossSection({camera:H.popupCamera||r.camera,controls:H.popupControls||r.controls,scene:H.popupScene||r.scene,renderer:H.popupRenderer||r.renderer,includeRayStartMargin:!0,preserveDrawCrossBounds:c===!0,preserveCurrentOrthoBounds:c===!0,storeDrawCrossBounds:c!==!0,...c===!0&&i?{targetOverride:i}:{}});else{const Q=H.popupRenderer||r?.renderer,le=H.popupCamera||r?.camera;Q&&I&&le&&Q.render(I,le),console.warn(`⚠️ setCameraFor${a}CrossSection not available`)}e(r);const $=H.popupRenderer||r?.renderer,J=H.popupCamera||r?.camera;$&&I&&J&&$.render(I,J),Nr(H.popup3DWindow,"Drawing complete",a)}else H.popup3DWindow.postMessage({status:"Error: "+V.error},"*")}catch(r){console.error("Error stack:",r.stack),H.popup3DWindow.postMessage({status:"Error: "+r.message},"*")}return}if(o.action==="view-xz"||o.action==="view-yz"){if(console.log("🎥 Handling popup view action:",o.action),!H.popup3DWindow)return;const r=H.popup3DWindow,s=H.popupScene||r.scene||null,a=H.popupCamera||r.camera||null,c=H.popupControls||r.controls||null,i=H.popupRenderer||r.renderer||null,l=r.document?.getElementById("status")||null;try{const f=o.action==="view-xz"?"XZ":"YZ",p=o?.userAdjustedView===!0,d=o?.target&&Number.isFinite(o.target.x)&&Number.isFinite(o.target.y)&&Number.isFinite(o.target.z)?o.target:null;if(!!a?.userData?.__drawCrossOrthoBounds&&s&&a&&c&&i&&typeof H.setCameraForXZCrossSection=="function"&&typeof H.setCameraForYZCrossSection=="function"){(({viewAxis:h,target:g})=>{const w=a,R=c,N=i,I=s;if(!w||!R)return;const b=()=>{try{if(!w||!w.isOrthographicCamera||!N||typeof N.getSize!="function")return;const $=r?.THREE||H.THREE,J=N.getSize(new $.Vector2),Q=Number(J?.x)||0,le=Number(J?.y)||0;if(!Number.isFinite(Q)||!Number.isFinite(le)||Q<2||le<2)return;const Z=Q/le,B=w.top-w.bottom||1,C=(w.left+w.right)/2,F=(w.top+w.bottom)/2,x=B*Z;w.left=C-x/2,w.right=C+x/2,w.top=F+B/2,w.bottom=F-B/2}catch{}},A=g||{x:R.target?.x??0,y:R.target?.y??0,z:R.target?.z??0},v=(w.position?.x??0)-A.x,S=(w.position?.y??0)-A.y,_=Math.abs(S)>Math.abs(v)?"XZ":"YZ";let M={...A};_==="YZ"&&h==="XZ"?M={x:A.y,y:0,z:A.z}:_==="XZ"&&h==="YZ"?M={x:0,y:A.x,z:A.z}:(h==="XZ"&&(M.y=0),h==="YZ"&&(M.x=0));const G=M.x-A.x,z=M.y-A.y,E=M.z-A.z;w.position.set((w.position?.x??0)+G,(w.position?.y??0)+z,(w.position?.z??0)+E),R.target.set(M.x,M.y,M.z);const L=(w.position?.x??0)-M.x,ne=(w.position?.y??0)-M.y,V=(w.position?.z??0)-M.z,K=Math.hypot(L,ne,V)||300;h==="XZ"?(w.position.set(M.x,M.y+K,M.z),w.up.set(1,0,0)):(w.position.set(M.x-K,M.y,M.z),w.up.set(0,1,0)),w.lookAt(M.x,M.y,M.z),R.update(),b(),w.updateProjectionMatrix(),N&&I&&N.render(I,w)})({viewAxis:f,target:p&&d?d:null});try{(v=>{if(!v)return;const S=[];v.traverse(_=>{const M=_.userData||{};if(M.type==="popupLensFill"||M.isUltraDebugOverlay===!0||M.isRayLine||M.type==="ray")return;const G=(_.name||"").toString(),z=M.type==="semidiaRing"||M.type==="ring"||M.surfaceType==="ring"||G.toLowerCase().includes("ring"),E=M.isLensSurface||M.surfaceType==="3DSurface"||G.toLowerCase().includes("lenssurface")||G.startsWith("surface")||G.startsWith("lens"),L=!!(_.material&&_.material.transparent&&typeof _.material.opacity=="number"&&_.material.opacity<1);(_.isMesh&&(E||L)||_.isLine&&z)&&S.push(_)}),[...new Set(S)].forEach(_=>{v.remove(_),_.geometry&&_.geometry.dispose(),_.material&&(Array.isArray(_.material)?_.material.forEach(M=>M.dispose()):_.material.dispose())})})(s);const{getOpticalSystemRows:g,drawOpticalSystemSurfaces:w,drawCrossBeamRays:R,harmonizeSceneGeometry:N}=jo(),I=v=>{if(!v)return;const S=[];v.traverse(_=>{const M=_.userData||{};(M.isRayLine||M.type==="ray")&&S.push(_)}),[...new Set(S)].forEach(_=>{v.remove(_),_.geometry&&_.geometry.dispose(),_.material&&(Array.isArray(_.material)?_.material.forEach(M=>M.dispose()):_.material.dispose())})},b=g();Array.isArray(b)&&b.length>0&&(w({opticalSystemData:b,scene:s,showSemidiaRing:!1,showSurfaceOrigins:!1,crossSectionOnly:!0,crossSectionDirection:f,showDesignIntentLabels:jr()}),N(s));const A=r.__lastCrossRays||H.__lastCrossRays;Array.isArray(A)&&A.length>0&&typeof R=="function"&&(I(s),R(A,s),N(s));try{to({popupWindow:r,scene:s,viewAxis:f,opticalSystemRows:Array.isArray(b)?b:[],source:"view-switch-camera-only"})}catch(v){console.warn("⚠️ Popup lens fill postprocess failed:",v)}i&&s&&a&&i.render(s,a)}catch(h){console.error("Error updating surfaces:",h)}Nr(r,`${f==="XZ"?"X-Z":"Y-Z"} view ready`,f)}else{await Wo({viewAxis:f,statusElement:l,targetScene:s,targetCamera:a,targetControls:c,targetRenderer:i,showAlerts:!1});try{const{getOpticalSystemRows:m}=jo(),h=m();to({popupWindow:r,scene:s,viewAxis:f,opticalSystemRows:Array.isArray(h)?h:[],source:"view-switch-execute-after"}),i&&s&&a&&i.render(s,a)}catch(m){console.warn("⚠️ Popup lens fill postprocess failed:",m)}Nr(r,`${f==="XZ"?"X-Z":"Y-Z"} view ready`,f)}}catch(f){r.postMessage({status:`Error: ${f.message}`},"*")}return}})}function Wo(e){const{viewAxis:t,buttonElement:n=null,statusElement:o=null,targetScene:r=null,targetCamera:s=null,targetControls:a=null,targetRenderer:c=null,showAlerts:i=!1}=e,l=()=>n?{originalText:n.textContent,disabled:n.disabled}:null,f=d=>{!n||!d||(n.textContent=d.originalText,n.disabled=d.disabled)},p=l();n&&(n.textContent="Drawing...",n.disabled=!0);try{const d=!!H.__cooptOptimizerIsRunning;if(!d){const Q=H.loadActiveConfigurationToTables;typeof Q=="function"&&Q()}if(!d)try{H.__cooptOpticalSystemRowsOverride=null}catch{}const{getOpticalSystemRows:u,getObjectRows:y,generateCrossBeam:m,generateInfiniteSystemCrossBeam:h,drawOpticalSystemSurfaces:g,drawCrossBeamRays:w}=jo(),R=u(),N=y();if(!R||R.length===0){i&&alert("No optical system data available."),f(p);return}const I=N&&N.length>0&&N[0]?N[0].thickness:null,b=String(I).trim().toUpperCase(),A=I===1/0||b==="INF"||b==="INFINITY"||Number(I)>1e6,v=(()=>{const Q=document.getElementById("draw-ray-count-input"),le=parseInt(Q?.value||"51",10);return Number.isFinite(le)&&le>0?le:51})();let S;A?typeof h=="function"&&(S=h(R,{rayCount:v})):typeof m=="function"&&N&&N.length>0&&(S=m(R,N,{rayCount:v}));const M=(Q=>{if(!Q)return[];let le=[];return Array.isArray(Q.results)&&(Q.results.forEach(Z=>{Z?.rays&&Array.isArray(Z.rays)&&(le=le.concat(Z.rays))}),le.length>0)?le:Array.isArray(Q.allTracedRays)?Q.allTracedRays:Array.isArray(Q.tracedRays)?Q.tracedRays:Array.isArray(Q)?Q:[]})(S),G=r||H.scene,z=s||H.camera,E=a||H.controls,L=c||H.renderer;G&&typeof Lo=="function"&&Lo(G),G&&typeof g=="function"&&g({scene:G,opticalSystemData:R,crossSectionOnly:!0,crossSectionDirection:t,showDesignIntentLabels:jr()});const ne=t==="XZ"?"XZ":"YZ";try{to({popupWindow:window,scene:G,viewAxis:ne,opticalSystemRows:R,source:"execute-cross-initial"})}catch(Q){console.warn("[RenderWindow] Lens fill postprocess failed:",Q)}G&&typeof So=="function"&&So(G);const V=()=>{if(!z||!G)return;const Q=z.userData?.__drawCrossOrthoBounds;if(Q&&Q.left!==void 0){const Z=L&&L.domElement.width/L.domElement.height||1,{left:B,right:C,top:F,bottom:x}=Q;if((C-B)/(F-x)>Z){const j=(C-B)/Z;z.top=j/2,z.bottom=-j/2,z.left=B,z.right=C}else{const j=(F-x)*Z;z.left=-j/2,z.right=j/2,z.top=F,z.bottom=x}}else{const Z=H.calculateOpticalSystemZRange;let B=-10,C=100,F=10,x=50;if(typeof Z=="function")try{const X=Z(R);X&&(B=X.minZ??B,C=X.maxZ??C,F=X.maxY??F,x=X.centerZ??x)}catch{}const T=C-B,j=F*2,P=L&&L.domElement.width/L.domElement.height||1;if(T/j>P){const X=T/P;z.top=X/2,z.bottom=-X/2,z.left=-T/2,z.right=T/2}else{const X=j*P;z.left=-X/2,z.right=X/2,z.top=j/2,z.bottom=-j/2}}z.position.set(0,300,Q?.centerZ??50),z.lookAt(0,0,Q?.centerZ??50),z.up.set(1,0,0),z.updateProjectionMatrix(),E&&(E.target.set(0,0,Q?.centerZ??50),E.update())},K=()=>{if(!z||!G)return;const Q=z.userData?.__drawCrossOrthoBounds;if(Q&&Q.left!==void 0){const Z=L&&L.domElement.width/L.domElement.height||1,{left:B,right:C,top:F,bottom:x}=Q;if((C-B)/(F-x)>Z){const j=(C-B)/Z;z.top=j/2,z.bottom=-j/2,z.left=B,z.right=C}else{const j=(F-x)*Z;z.left=-j/2,z.right=j/2,z.top=F,z.bottom=x}}else{const Z=H.calculateOpticalSystemZRange;let B=-10,C=100,F=10,x=50;if(typeof Z=="function")try{const X=Z(R);X&&(B=X.minZ??B,C=X.maxZ??C,F=X.maxY??F,x=X.centerZ??x)}catch{}const T=C-B,j=F*2,P=L&&L.domElement.width/L.domElement.height||1;if(T/j>P){const X=T/P;z.top=X/2,z.bottom=-X/2,z.left=-T/2,z.right=T/2}else{const X=j*P;z.left=-X/2,z.right=X/2,z.top=j/2,z.bottom=-j/2}}z.position.set(-300,0,Q?.centerZ??50),z.lookAt(0,0,Q?.centerZ??50),z.up.set(0,1,0),z.updateProjectionMatrix(),E&&(E.target.set(0,0,Q?.centerZ??50),E.update())},$=H.setCameraForXZCrossSection,J=H.setCameraForYZCrossSection;t==="XZ"?typeof $=="function"?$(G,z,{includeRayStartMargin:!0,preserveDrawCrossBounds:!0}):V():typeof J=="function"?J(G,z,{includeRayStartMargin:!0,preserveDrawCrossBounds:!0}):K(),M&&M.length>0&&G&&typeof w=="function"&&w(M,G),G&&typeof So=="function"&&So(G);try{to({popupWindow:window,scene:G,viewAxis:ne,opticalSystemRows:R,source:"execute-cross-final"})}catch(Q){console.warn("[RenderWindow] Lens fill postprocess (final pass) failed:",Q)}if(L&&G&&z&&L.render(G,z),o){const Q=Ra(t);o.textContent=`${t} view complete${Q}`}}catch(d){console.error("Error in executeCrossSectionView:",d),i&&alert(`Failed to generate ${t} view: ${d}`)}finally{f(p)}}H.executeCrossSectionView=Wo;function Tp(){const e=document.getElementById("annular-pattern-btn"),t=document.getElementById("grid-pattern-btn"),n=o=>{e&&(o==="annular"?e.classList.add("active"):e.classList.remove("active")),t&&(o==="grid"?t.classList.add("active"):t.classList.remove("active"))};e&&e.addEventListener("click",()=>{Tr("annular"),n("annular")}),t&&t.addEventListener("click",()=>{Tr("grid"),n("grid")})}function Pp(){const e=document.getElementById("object-color-btn"),t=document.getElementById("segment-color-btn"),n=o=>{e&&(o==="object"?e.classList.add("active"):e.classList.remove("active")),t&&(o==="segment"?t.classList.add("active"):t.classList.remove("active"))};e&&e.addEventListener("click",()=>{io("object"),n("object")}),t&&t.addEventListener("click",()=>{io("segment"),n("segment")})}function kp(){const e=document.getElementById("view-xz-btn"),t=document.getElementById("view-yz-btn");e&&e.addEventListener("click",()=>{Wo({viewAxis:"XZ",buttonElement:e,showAlerts:!0})}),t&&t.addEventListener("click",()=>{Wo({viewAxis:"YZ",buttonElement:t,showAlerts:!0})})}function Tn(e){if(H.__opticalSystemChangeListenersBound)return;H.__opticalSystemChangeListenersBound=!0;const t=H.tableOpticalSystem;if(t){const r=()=>{};t.on("cellEdited",r),t.on("rowAdded",r),t.on("rowDeleted",r),t.on("dataChanged",r)}Ip();const n=document.getElementById("open-3d-window-btn"),o=()=>{const r=H.popup3DWindow;if(r&&!r.closed)try{if(r.focus(),r.document&&r.document.getElementById("threejs-container"))return}catch{}const s=window.open("","3D Optical System","width=800,height=600");if(!s){alert("Popup blocked. Please allow popups for this site.");return}s.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <title>Render Optical System</title>
    <style>
        html, body { height: 100%; width: 100%; overflow: hidden; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f0f0f0;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
        }
        .header #status {
            font-size: 12px;
            font-weight: 400;
            color: #666;
            margin-left: auto;
            white-space: nowrap;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f9f9f9;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls button.active { background: #d0d0d0; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            width: 80px;
        }
        #main {
            flex: 1 1 auto;
            display: flex;
            flex-direction: row;
            min-height: 0;
            position: relative;
            width: 100%;
            max-width: 100vw;
            overflow: hidden;
        }
        #threejs-container {
            flex: 1 1 auto;
            min-height: 0;
            position: relative;
            background: white;
            width: 100%;
            max-width: 100%;
            overflow: hidden;
        }
        #surface-colors {
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            width: 240px;
            display: flex;
            flex-direction: column;
            background: #fafafa;
            border-left: 1px solid #ddd;
            overflow: hidden;
            transition: width 0.2s;
            z-index: 10;
        }
        #surface-colors.collapsed {
            width: 32px;
        }
        #surface-colors .header-row {
            padding: 8px 12px;
            background: #f0f0f0;
            border-bottom: 1px solid #ddd;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex: 0 0 auto;
        }
        #surface-colors .title {
            font-size: 12px;
            font-weight: 600;
            color: #333;
        }
        #surface-colors.collapsed .title {
            display: none;
        }
        #surface-colors-toggle {
            cursor: pointer;
            user-select: none;
            font-size: 14px;
            color: #666;
            padding: 0 4px;
        }
        #surface-colors-toggle:hover {
            color: #333;
        }
        .table-wrap {
            flex: 1 1 auto;
            overflow: auto;
            padding: 8px;
        }
        #surface-colors.collapsed .table-wrap {
            display: none;
        }
        #surface-colors table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
        }
        #surface-colors th {
            text-align: left;
            padding: 6px 8px;
            background: #e8e8e8;
            border-bottom: 1px solid #ccc;
            position: sticky;
            top: 0;
            z-index: 1;
        }
        #surface-colors td {
            padding: 4px 8px;
            border-bottom: 1px solid #eee;
        }
        #surface-colors select {
            width: 100%;
            padding: 4px;
            font-size: 11px;
            border: 1px solid #bbb;
            border-radius: 3px;
        }
    </style>
</head>
<body>
    <div class="header">
        <span>Render Optical System</span>
        <span id="status"></span>
    </div>
    <div class="controls">
        <button id="draw-btn" type="button">Render</button>
        <button id="view-xz-btn" type="button">X-Z View</button>
        <button id="view-yz-btn" type="button">Y-Z View</button>
        <button id="clear-btn" type="button">Clear</button>
        <label for="draw-ray-count-input">Ray number:</label>
        <input type="number" id="draw-ray-count-input" value="5" min="1" max="10001" step="2" />
        <label>Ray colors by:</label>
        <button id="object-color-btn" type="button" class="active">Object</button>
        <button id="segment-color-btn" type="button">Segment</button>
    </div>
    <div id="main">
        <div id="threejs-container"></div>
        <div id="surface-colors" class="collapsed">
            <div class="header-row">
                <span class="title">Surface Colors</span>
                <span id="surface-colors-toggle">▶</span>
            </div>
            <div class="table-wrap">
                <table>
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Color</th>
                        </tr>
                    </thead>
                    <tbody id="surface-colors-tbody"></tbody>
                </table>
            </div>
        </div>
    </div>

    <script type="module">
        import * as THREE from 'https://esm.sh/three@0.182.0';
        import { OrbitControls } from 'https://esm.sh/three@0.182.0/examples/jsm/controls/OrbitControls.js';
        
        console.log('THREE.js loaded in popup:', !!THREE);
        
        // Try to get THREE from parent first, fallback to imported version
        const parentTHREE = window.opener && window.opener.THREE;
        const useTHREE = parentTHREE || THREE;
        const useOrbitControls = (window.opener && window.opener.OrbitControls) || OrbitControls;
        
        console.log('Using THREE from:', parentTHREE ? 'parent window' : 'local import');
        
        function initializePopup(THREE, OrbitControls) {
            const container = document.getElementById('threejs-container');
            const status = document.getElementById('status');
            const MAX_SAFE_DIMENSION = 8192;

            const scene = new THREE.Scene();
            scene.userData.renderContext = {
                three: THREE,
                global: window.opener
            };

            const pickSafeViewportDimension = (candidates, fallback) => {
                const dpr = Number(window.devicePixelRatio || 1);
                const normalized = [];

                const addCandidate = (value) => {
                    const n = Number(value);
                    if (!Number.isFinite(n)) return;
                    if (n >= 200 && n <= MAX_SAFE_DIMENSION) normalized.push(n);
                    if (dpr > 1.25) {
                        const cssLike = n / dpr;
                        if (cssLike >= 200 && cssLike <= MAX_SAFE_DIMENSION) normalized.push(cssLike);
                    }
                };

                for (const value of candidates) addCandidate(value);
                if (normalized.length === 0) return fallback;

                normalized.sort((a, b) => a - b);
                return Math.round(normalized[0]);
            };

            const getSafeViewportSize = () => {
                const width = pickSafeViewportDimension([
                    window.visualViewport && window.visualViewport.width,
                    window.innerWidth,
                    document.documentElement && document.documentElement.clientWidth,
                    window.outerWidth,
                    window.screen && window.screen.width
                ], 1280);
                const height = pickSafeViewportDimension([
                    window.visualViewport && window.visualViewport.height,
                    window.innerHeight,
                    document.documentElement && document.documentElement.clientHeight,
                    window.outerHeight,
                    window.screen && window.screen.height
                ], 720);
                return { width, height };
            };

            let lastGoodContainerWidth = 960;
            let lastGoodContainerHeight = 640;

            const getClampedContainerSize = () => {
                const rect = container.getBoundingClientRect();
                const rawWidth = Number(rect.width || container.clientWidth || 0);
                const rawHeight = Number(rect.height || container.clientHeight || 0);
                const viewport = getSafeViewportSize();
                const baselineWidthCandidates = [
                    viewport.width,
                    window.visualViewport && window.visualViewport.width,
                    window.innerWidth,
                    document.documentElement && document.documentElement.clientWidth,
                    window.screen && window.screen.width
                ]
                    .map((v) => Number(v))
                    .filter((v) => Number.isFinite(v) && v >= 200 && v <= MAX_SAFE_DIMENSION)
                    .sort((a, b) => a - b);
                const baselineHeightCandidates = [
                    viewport.height,
                    window.visualViewport && window.visualViewport.height,
                    window.innerHeight,
                    document.documentElement && document.documentElement.clientHeight,
                    window.screen && window.screen.height
                ]
                    .map((v) => Number(v))
                    .filter((v) => Number.isFinite(v) && v >= 200 && v <= MAX_SAFE_DIMENSION)
                    .sort((a, b) => a - b);

                const viewportWidthBaseline = baselineWidthCandidates[0] || viewport.width;
                const viewportHeightBaseline = baselineHeightCandidates[0] || viewport.height;

                const widthFactor = 1.35;
                const heightFactor = 1.5;

                const maxWidth = Math.min(MAX_SAFE_DIMENSION, Math.max(320, Math.round(viewportWidthBaseline * widthFactor)));
                const maxHeight = Math.min(MAX_SAFE_DIMENSION, Math.max(320, Math.round(viewportHeightBaseline * heightFactor)));

                const preferredWidth = Math.min(maxWidth, Math.max(2, Math.round(viewportWidthBaseline)));
                const preferredHeight = Math.min(maxHeight, Math.max(2, Math.round(viewportHeightBaseline)));

                const pickValue = (raw, fallback, min, max) => {
                    if (!Number.isFinite(raw)) return fallback;
                    if (raw < min || raw > max) return fallback;
                    return Math.round(raw);
                };

                let width = pickValue(rawWidth, preferredWidth, 2, maxWidth);
                let height = pickValue(rawHeight, preferredHeight, 2, maxHeight);

                if (width < 2) width = Math.min(maxWidth, Math.max(2, viewport.width));
                if (height < 2) height = Math.min(maxHeight, Math.max(2, viewport.height));

                if (Number.isFinite(rawWidth) && Math.round(rawWidth) !== width) {
                    console.warn('[popup] Ignored abnormal container width:', rawWidth, '->', width, '(max=', maxWidth, ')');
                }
                if (Number.isFinite(rawHeight) && Math.round(rawHeight) !== height) {
                    console.warn('[popup] Ignored abnormal container height:', rawHeight, '->', height, '(max=', maxHeight, ')');
                }

                lastGoodContainerWidth = width;
                lastGoodContainerHeight = height;
                return { width, height };
            };
            
            const viewSize = 50;
            const initialSize = getClampedContainerSize();
            const aspect = initialSize.width / initialSize.height || 1;
            const camera = new THREE.OrthographicCamera(
                -viewSize * aspect / 2,
                viewSize * aspect / 2,
                viewSize / 2,
                -viewSize / 2,
                0.1,
                10000
            );
            
            const rendererOptions = { antialias: true, alpha: true, precision: 'highp', logarithmicDepthBuffer: true };
            const renderer = new THREE.WebGLRenderer(rendererOptions);
            renderer.setPixelRatio(window.devicePixelRatio || 1);
            renderer.setSize(initialSize.width, initialSize.height, false);
            renderer.setClearColor(0xffffff, 1);
            renderer.sortObjects = false;
            renderer.shadowMap.enabled = false;
            container.appendChild(renderer.domElement);
            
            const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
            scene.add(ambientLight);
            const directionalLight = new THREE.DirectionalLight(0xffffff, 0.4);
            directionalLight.position.set(10, 10, 10);
            scene.add(directionalLight);
            
            const controls = new OrbitControls(camera, renderer.domElement);
            controls.enableDamping = true;
            controls.dampingFactor = 0.05;
            controls.enableRotate = true;
            controls.enablePan = true;
            controls.enableZoom = true;

            let popupUserAdjustedView = false;
            let popupCurrentViewAxis = 'YZ';
            let popupRayColorMode = 'object';
            let popupSurfaceColorsCollapsed = true;

            function setPopupUserAdjustedView(v) {
                popupUserAdjustedView = v === true;
            }

            function getPopupUserAdjustedView() {
                return popupUserAdjustedView === true;
            }

            function setCurrentViewAxis(axis) {
                popupCurrentViewAxis = axis === 'XZ' ? 'XZ' : 'YZ';
            }

            function getCurrentViewAxis() {
                return popupCurrentViewAxis;
            }

            function setPopupRayColorModeValue(mode) {
                popupRayColorMode = mode === 'segment' ? 'segment' : 'object';
            }

            function getPopupRayColorMode() {
                return popupRayColorMode;
            }

            function setSurfaceColorsCollapsed(collapsed) {
                popupSurfaceColorsCollapsed = collapsed === true;
            }

            function getSurfaceColorsCollapsed() {
                return popupSurfaceColorsCollapsed === true;
            }

            function toggleSurfaceColorsCollapsed() {
                setSurfaceColorsCollapsed(!getSurfaceColorsCollapsed());
            }
            
            setPopupUserAdjustedView(false);
            controls.addEventListener('start', () => {
                setPopupUserAdjustedView(true);
            });
            
            camera.position.set(0, 50, 100);
            camera.lookAt(0, 0, 0);
            camera.up.set(0, 1, 0);
            controls.target.set(0, 0, 100);
            controls.update();

            const renderNow = () => {
                renderer.render(scene, camera);
            };
            
            function animate() {
                requestAnimationFrame(animate);
                controls.update();
                renderNow();
            }
            animate();
            
            let resizeScheduled = false;
            let lastResizeSentAt = 0;
            let lastResizeWidth = -1;
            let lastResizeHeight = -1;

            const shouldSendPopupResize = (now, width, height, thresholdMs, deltaPx) => {
                const hasLastSize = lastResizeWidth > 0 && lastResizeHeight > 0;
                const sizeChangedEnough = !hasLastSize ||
                    Math.abs(lastResizeWidth - width) >= deltaPx ||
                    Math.abs(lastResizeHeight - height) >= deltaPx;
                return !lastResizeSentAt ||
                    (now - lastResizeSentAt > thresholdMs) ||
                    sizeChangedEnough;
            };

            const markPopupResizeSent = (now, width, height) => {
                lastResizeSentAt = now;
                lastResizeWidth = width;
                lastResizeHeight = height;
            };

            const scheduleResize = () => {
                if (resizeScheduled) return;
                resizeScheduled = true;
                requestAnimationFrame(() => {
                    requestAnimationFrame(() => {
                        resizeScheduled = false;
                        applyResize();
                    });
                });
            };
            
            const applyResize = () => {
                const size = getClampedContainerSize();
                const w = Math.max(1, size.width);
                const h = Math.max(1, size.height);
                if (w < 2 || h < 2) return;
                
                renderer.setPixelRatio(window.devicePixelRatio || 1);
                renderer.setSize(w, h, false);
                controls.update();
                
                const aspect = w / h;
                const currentHeight = camera.top - camera.bottom;
                const currentCenterX = (camera.left + camera.right) / 2;
                const currentCenterY = (camera.top + camera.bottom) / 2;
                const newWidth = currentHeight * aspect;
                camera.left = currentCenterX - newWidth / 2;
                camera.right = currentCenterX + newWidth / 2;
                camera.top = currentCenterY + currentHeight / 2;
                camera.bottom = currentCenterY - currentHeight / 2;
                camera.updateProjectionMatrix();
                
                if (!getPopupUserAdjustedView()) {
                    const now = Date.now();
                    const threshold = 200;
                    const deltaPx = 8;
                    const shouldSend = shouldSendPopupResize(now, w, h, threshold, deltaPx);
                    
                    if (shouldSend && window.opener) {
                        markPopupResizeSent(now, w, h);
                        try {
                            const axis = getCurrentViewAxis() || 'YZ';
                            window.opener.postMessage({ action: 'popup-resize', viewAxis: axis }, '*');
                        } catch (_) {}
                    }
                }
                renderNow();
            };
            
            let resizeObserver = null;
            if (typeof ResizeObserver !== 'undefined') {
                resizeObserver = new ResizeObserver(() => scheduleResize());
                resizeObserver.observe(container);
            } else {
                let lastW = -1;
                let lastH = -1;
                setInterval(() => {
                    const w = container.clientWidth;
                    const h = container.clientHeight;
                    if (w !== lastW || h !== lastH) {
                        lastW = w;
                        lastH = h;
                        scheduleResize();
                    }
                }, 300);
            }
            window.addEventListener('resize', scheduleResize);
            
            const normalizeSceneGeometry = (scene) => {
                const normalizeArray = (attr, isIndex) => {
                    if (!attr || !attr.array) return;
                    const arr = attr.array;
                    
                    if (Array.isArray(arr)) {
                        const TypedArray = isIndex
                            ? (arr.length < 65536 ? Uint16Array : Uint32Array)
                            : Float32Array;
                        attr.array = new TypedArray(arr);
                        attr.needsUpdate = true;
                        return;
                    }
                    
                    if (arr.constructor && arr.constructor.name && 
                        arr.constructor.name.includes('Array') &&
                        arr.buffer && arr.buffer.constructor &&
                        arr.buffer.constructor.name === 'ArrayBuffer') {
                        const TypedArray = isIndex
                            ? (arr.length < 65536 ? Uint16Array : Uint32Array)
                            : Float32Array;
                        attr.array = new TypedArray(arr);
                        attr.needsUpdate = true;
                    }
                };
                
                scene.traverse((obj) => {
                    const geom = obj.geometry;
                    if (!geom) return;
                    
                    if (geom.attributes) {
                        for (const name in geom.attributes) {
                            const attr = geom.attributes[name];
                            if (attr.isInterleavedBufferAttribute && attr.data && attr.data.array) {
                                normalizeArray(attr.data, false);
                            } else {
                                normalizeArray(attr, false);
                            }
                        }
                    }
                    
                    if (geom.morphAttributes) {
                        for (const name in geom.morphAttributes) {
                            const morphs = geom.morphAttributes[name];
                            if (Array.isArray(morphs)) {
                                morphs.forEach(attr => normalizeArray(attr, false));
                            }
                        }
                    }
                    
                    if (geom.index) {
                        normalizeArray(geom.index, true);
                    }
                });
            };
            
            const findBadGeometry = (scene) => {
                const tempScene = new THREE.Scene();
                const tempCamera = new THREE.PerspectiveCamera();
                const tempRenderer = new THREE.WebGLRenderer();
                
                const objects = [];
                scene.traverse(obj => {
                    if (obj.geometry) objects.push(obj);
                });
                
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    tempScene.add(obj.clone());
                    
                    try {
                        tempRenderer.render(tempScene, tempCamera);
                    } catch (err) {
                        console.error('Bad geometry found:', {
                            name: obj.name,
                            uuid: obj.uuid,
                            type: obj.type,
                            geometryType: obj.geometry?.type,
                            hasIndex: !!obj.geometry?.index,
                            attributes: Object.keys(obj.geometry?.attributes || {}).map(k => {
                                const attr = obj.geometry.attributes[k];
                                return {
                                    name: k,
                                    itemSize: attr?.itemSize,
                                    count: attr?.count,
                                    arrayType: attr?.array?.constructor?.name,
                                    isInterleaved: !!attr?.isInterleavedBufferAttribute
                                };
                            }),
                            index: obj.geometry?.index ? {
                                count: obj.geometry.index.count,
                                arrayType: obj.geometry.index.array?.constructor?.name
                            } : null,
                            userData: obj.userData
                        });
                        tempRenderer.dispose();
                        return obj;
                    }
                }
                
                tempRenderer.dispose();
                return null;
            };
            
            try {
                window.scene = scene;
                window.camera = camera;
                window.renderer = renderer;
                window.controls = controls;
            } catch (_) {}

            if (window.opener) {
                window.opener.popupScene = scene;
                window.opener.popupCamera = camera;
                window.opener.popupRenderer = renderer;
                window.opener.popupControls = controls;
                window.opener.popup3DWindow = window;
            }
            
            const SURFACE_COLOR_OVERRIDES_STORAGE_KEY = 'coopt.surfaceColorOverrides';
            
            const COLOR_PALETTE = [
                { name: 'Light Pink', hex: '#FFB6C1' },
                { name: 'Light Red', hex: '#FF6B6B' },
                { name: 'Light Orange', hex: '#FFA07A' },
                { name: 'Light Amber', hex: '#FFBF00' },
                { name: 'Light Yellow', hex: '#FFFF99' },
                { name: 'Light Lime', hex: '#CCFF66' },
                { name: 'Light Green', hex: '#90EE90' },
                { name: 'Light Mint', hex: '#98FF98' },
                { name: 'Light Cyan', hex: '#AFEEEE' },
                { name: 'Light Sky', hex: '#87CEEB' },
                { name: 'Light Blue', hex: '#ADD8E6' },
                { name: 'Light Indigo', hex: '#9FA8DA' },
                { name: 'Light Purple', hex: '#DDA0DD' },
                { name: 'Light Lavender', hex: '#E6E6FA' },
                { name: 'Light Peach', hex: '#FFDAB9' },
                { name: 'Light Gray', hex: '#D3D3D3' }
            ];
            
            function surfaceColorKey(surf) {
                if (surf._blockId && surf._surfaceRole) {
                    return 'p:' + surf._blockId + '|' + surf._surfaceRole;
                }
                if (surf.id !== undefined && surf.id !== null) {
                    return 'id:' + surf.id;
                }
                if (surf.index0 !== undefined && surf.index0 !== null) {
                    return 'i:' + surf.index0;
                }
                return '';
            }
            
            function loadColorOverrides() {
                try {
                    const raw = localStorage.getItem(SURFACE_COLOR_OVERRIDES_STORAGE_KEY);
                    return raw ? JSON.parse(raw) : {};
                } catch (_) {
                    return {};
                }
            }
            
            function saveColorOverrides(map) {
                try {
                    localStorage.setItem(SURFACE_COLOR_OVERRIDES_STORAGE_KEY, JSON.stringify(map));
                } catch (_) {}
            }
            
            function requestRedrawFromPopup() {
                if (!window.opener) return;
                const viewState = getPopupViewState();
                try {
                    window.opener.postMessage({ action: 'draw-cross', ...viewState }, '*');
                } catch (_) {}
            }
            
            function renderSurfaceColorsTable(surfaces) {
                const tbody = document.getElementById('surface-colors-tbody');
                if (!tbody) return;
                
                tbody.innerHTML = '';
                
                const overrides = loadColorOverrides();
                
                for (let i = 0; i < surfaces.length; i++) {
                    const surf = surfaces[i];
                    const key = surfaceColorKey(surf);
                    if (!key) continue;
                    
                    const tr = document.createElement('tr');
                    
                    const tdIndex = document.createElement('td');
                    tdIndex.textContent = String(i);
                    
                    const tdColor = document.createElement('td');
                    const sel = document.createElement('select');
                    
                    const defaultOpt = document.createElement('option');
                    defaultOpt.value = '';
                    defaultOpt.textContent = 'Default';
                    sel.appendChild(defaultOpt);
                    
                    for (const c of COLOR_PALETTE) {
                        const opt = document.createElement('option');
                        opt.value = c.hex;
                        opt.textContent = c.name;
                        sel.appendChild(opt);
                    }
                    
                    const current = overrides[key] || '';
                    sel.value = current;
                    applySelectSwatch(sel);
                    sel.addEventListener('change', () => {
                        const next = String(sel.value || '').trim();
                        const nextMap = loadColorOverrides();
                        if (!next) {
                            delete nextMap[key];
                        } else {
                            nextMap[key] = next;
                        }
                        saveColorOverrides(nextMap);
                        applySelectSwatch(sel);
                        requestRedrawFromPopup();
                    });
                    
                    tdColor.appendChild(sel);
                    
                    tr.appendChild(tdIndex);
                    tr.appendChild(tdColor);
                    tbody.appendChild(tr);
                }
            }
            
            function applySelectSwatch(sel) {
                const val = String(sel.value || '').trim();
                if (val && val.startsWith('#')) {
                    sel.style.backgroundColor = val;
                } else {
                    sel.style.backgroundColor = '';
                }
            }
            
            function applySurfaceColorsCollapsedState(collapsed) {
                const surfaceColorsPanel = document.getElementById('surface-colors');
                const surfaceColorsToggle = document.getElementById('surface-colors-toggle');
                if (!surfaceColorsPanel || !surfaceColorsToggle) return;
                const isCollapsed = collapsed === true;
                surfaceColorsPanel.classList.toggle('collapsed', isCollapsed);
                surfaceColorsToggle.textContent = isCollapsed ? '◀' : '▶';
            }

            setSurfaceColorsCollapsed(true);
            applySurfaceColorsCollapsedState(getSurfaceColorsCollapsed());
            
            const surfaceColorsToggle = document.getElementById('surface-colors-toggle');
            if (surfaceColorsToggle) {
                surfaceColorsToggle.addEventListener('click', () => {
                    toggleSurfaceColorsCollapsed();
                    applySurfaceColorsCollapsedState(getSurfaceColorsCollapsed());
                });
            }
            
            setPopupRayColorModeValue('object');
            
            function setPopupRayColorMode(mode) {
                setPopupRayColorModeValue(mode);
                const objectColorBtn = document.getElementById('object-color-btn');
                const segmentColorBtn = document.getElementById('segment-color-btn');
                if (objectColorBtn && segmentColorBtn) {
                    objectColorBtn.classList.toggle('active', getPopupRayColorMode() === 'object');
                    segmentColorBtn.classList.toggle('active', getPopupRayColorMode() === 'segment');
                }
            }
            
            const objectColorBtn = document.getElementById('object-color-btn');
            const segmentColorBtn = document.getElementById('segment-color-btn');
            if (objectColorBtn) {
                objectColorBtn.addEventListener('click', () => setPopupRayColorMode('object'));
            }
            if (segmentColorBtn) {
                segmentColorBtn.addEventListener('click', () => setPopupRayColorMode('segment'));
            }
            
            console.log('Buttons:', {
                drawBtn: document.getElementById('draw-btn'),
                xzBtn: document.getElementById('view-xz-btn'),
                yzBtn: document.getElementById('view-yz-btn'),
                clearBtn: document.getElementById('clear-btn'),
                status: document.getElementById('status')
            });
            
            window.addEventListener('message', (event) => {
                if (!window.opener || event.source !== window.opener) {
                    return;
                }
                const data = event.data || {};
                if (data && data.action === 'surface-list') {
                    try {
                        renderSurfaceColorsTable(data.surfaces);
                    } catch (e) {}
                    return;
                }
                if (data && data.action === 'set-user-adjusted-view') {
                    try {
                        setPopupUserAdjustedView(data.value === true);
                    } catch (_) {}
                    return;
                }
                if (data && data.action === 'request-redraw') {
                    try {
                        const axisRaw = (data.viewAxis || getCurrentViewAxis() || 'YZ').toString().toUpperCase();
                        setCurrentViewAxis(axisRaw);
                    } catch (_) {}
                    
                    try {
                        const viewState = getPopupViewState();
                        if (status) {
                            status.textContent = 'Redrawing...';
                        }
                        window.opener.postMessage({ action: 'draw-cross', ...viewState }, '*');
                    } catch (e) {}
                    return;
                }
                if (typeof data.status === 'string' && status) {
                    status.textContent = data.status;
                }
            });
            
            function getPopupViewState() {
                const rayCountInput = document.getElementById('draw-ray-count-input');
                const rayCount = (() => {
                    const v = parseInt(rayCountInput?.value || '51', 10);
                    return Number.isFinite(v) && v > 0 ? v : 51;
                })();
                return {
                    userAdjustedView: getPopupUserAdjustedView(),
                    viewAxis: getCurrentViewAxis(),
                    rayCount,
                    rayColorMode: getPopupRayColorMode(),
                    target: {
                        x: controls?.target?.x ?? 0,
                        y: controls?.target?.y ?? 0,
                        z: controls?.target?.z ?? 0
                    },
                    camera: {
                        x: camera?.position?.x ?? 0,
                        y: camera?.position?.y ?? 0,
                        z: camera?.position?.z ?? 0
                    },
                    zoom: camera?.zoom ?? 1
                };
            }

            const drawBtn = document.getElementById('draw-btn');
            if (drawBtn) {
                drawBtn.addEventListener('click', () => {
                    const viewState = getPopupViewState();
                    console.log('📤 Sending message to parent:', { action: 'draw-cross', ...viewState });
                    if (window.opener) {
                        window.opener.postMessage({ action: 'draw-cross', ...viewState }, '*');
                        status.textContent = 'Drawing...';
                    }
                });
            }
            
            const xzBtn = document.getElementById('view-xz-btn');
            if (xzBtn) {
                xzBtn.addEventListener('click', () => {
                    setCurrentViewAxis('XZ');
                    if (window.opener) {
                        const viewState = getPopupViewState();
                        window.opener.postMessage({ action: 'view-xz', ...viewState }, '*');
                        status.textContent = 'Switching to X-Z view...';
                    }
                });
            }
            
            const yzBtn = document.getElementById('view-yz-btn');
            if (yzBtn) {
                yzBtn.addEventListener('click', () => {
                    setCurrentViewAxis('YZ');
                    if (window.opener) {
                        const viewState = getPopupViewState();
                        window.opener.postMessage({ action: 'view-yz', ...viewState }, '*');
                        status.textContent = 'Switching to Y-Z view...';
                    }
                });
            }
            
            const clearBtn = document.getElementById('clear-btn');
            if (clearBtn) {
                clearBtn.addEventListener('click', () => {
                    const objectsToRemove = [];
                    scene.traverse((object) => {
                        if (object !== scene && !(object instanceof THREE.Light)) {
                            objectsToRemove.push(object);
                        }
                    });
                    objectsToRemove.forEach((obj) => {
                        scene.remove(obj);
                        if (obj.geometry) obj.geometry.dispose();
                        if (obj.material) {
                            if (Array.isArray(obj.material)) {
                                obj.material.forEach(mat => mat.dispose());
                            } else {
                                obj.material.dispose();
                            }
                        }
                    });
                    renderNow();
                    status.textContent = 'Cleared';
                });
            }
            
            if (window.opener) {
                window.opener.postMessage({ action: 'popup-ready' }, '*');
            }
            
            if (drawBtn && window.opener) {
                setTimeout(() => {
                    try {
                        drawBtn.click();
                    } catch (e) {}
                }, 0);
            }
        }
        
        function initPopup() {
            if (THREE) {
                setupScene();
            } else {
                setTimeout(initPopup, 100);
            }
        }
        
        // Initialize with the loaded THREE.js
        initializePopup(useTHREE, useOrbitControls);
    <\/script>
</body>
</html>
            `),s.document.close(),H.popup3DWindow=s};if(typeof window<"u")try{window.__open3DWindowLegacy=o}catch{}n&&(n.getAttribute("data-react-handled")==="1"||n.addEventListener("click",o))}function Pn(){if(!!!(document.getElementById("open-system-data-window-btn")||document.getElementById("open-spot-diagram-window-btn")||document.getElementById("open-opd-window-btn")||document.getElementById("open-psf-window-btn")||document.getElementById("open-mtf-window-btn")||document.getElementById("open-through-focus-spot-window-btn")||document.getElementById("open-through-focus-mtf-window-btn")||document.getElementById("open-field-mtf-window-btn"))||H.__analysisWindowsBound)return;const t=(()=>{try{return new URL(window.location.href).searchParams.get("coopt_analysis_window")==="1"}catch{return!1}})();if(st()&&!t){H.__analysisWindowsBound=!0;return}H.__analysisWindowsBound=!0;const n=(N,I)=>{try{const b=H.__preopenedAnalysisPopupMap,A=b&&b[N];if(A&&!A.closed){try{delete b[N]}catch{}try{A.focus()}catch{}return A}}catch{}return window.open("",N,I)},o=document.getElementById("open-system-data-window-btn");o&&(o.getAttribute("data-react-handled")==="1"||o.addEventListener("click",()=>{if(H.__systemDataPopup&&!H.__systemDataPopup.closed){try{H.__systemDataPopup.focus()}catch{}return}const I=n("System Data","width=1200,height=600");if(!I){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__systemDataPopup=I,I.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>System Data</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: white;
            color: #333;
            font-weight: 600;
            flex: 0 0 auto;
            border-bottom: 1px solid #ddd;
        }
        .controls {
            padding: 10px 12px;
            background: white;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
        }
        .content {
            flex: 1 1 auto;
            padding: 10px 12px;
            min-height: 0;
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        textarea {
            flex: 1 1 auto;
            width: 100%;
            resize: none;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
            font-size: 12px;
            line-height: 1.4;
            border: 1px solid #bbb;
            border-radius: 4px;
            padding: 10px;
            box-sizing: border-box;
            min-height: 0;
            background: white;
        }
    </style>
</head>
<body>
    <div class="header">System Data</div>
    <div class="controls">
        <button id="popup-calculate-paraxial">Calculate Paraxial</button>
        <button id="popup-calculate-seidel">Aberration Coefficients</button>
        <button id="popup-calculate-seidel-afocal">Aberration Coefficients (Afocal)</button>
        <label for="popup-reference-focal-length">Reference Focal Length:</label>
        <input type="text" id="popup-reference-focal-length" placeholder="Auto" style="width: 80px;" />
        <button id="popup-coord-transform">Coord Transform</button>
    </div>
    <div class="content">
        <div id="popup-transform-error-bar" style="display:none; padding:8px 12px; margin-bottom:8px; background:#fff3cd; border:1px solid #ffc107; border-radius:3px; color:#856404;">
            <strong>Error:</strong> <span id="popup-transform-error-text"></span>
        </div>
        <div id="popup-transform-progress-wrapper" style="display:none; padding:8px 12px; border-bottom:1px solid #eee; background:#fff; margin-bottom:8px;">
            <div id="popup-transform-progress-text">Calculating...</div>
            <progress id="popup-transform-progressbar" max="100" value="0" style="width:100%; margin-top:4px;"></progress>
        </div>
        <textarea id="popup-system-data" placeholder="System information will appear here..."></textarea>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function syncFromOpener() {
            const ref = getOpenerEl('reference-focal-length');
            const src = getOpenerEl('system-data');
            const popupRef = document.getElementById('popup-reference-focal-length');
            const popupText = document.getElementById('popup-system-data');
            let cached = '';
            try {
                cached = (window.opener && typeof window.opener.__cooptSystemDataText === 'string')
                    ? window.opener.__cooptSystemDataText
                    : '';
            } catch (_) {
                cached = '';
            }

            if (popupRef && ref && popupRef.value !== ref.value) {
                popupRef.value = ref.value;
            }
            if (popupText) {
                if (src && popupText.value !== src.value) {
                    popupText.value = src.value;
                } else if (!src && cached.length > 0 && popupText.value !== cached) {
                    popupText.value = cached;
                }
            }
        }

        function triggerOpenerClick(id) {
            const btn = getOpenerEl(id);
            if (btn) {
                btn.click();
                // allow async handlers to update textarea
                setTimeout(syncFromOpener, 50);
                setTimeout(syncFromOpener, 200);
                setTimeout(syncFromOpener, 500);
                setTimeout(syncFromOpener, 1000);
                setTimeout(syncFromOpener, 2000);
            }
        }

        document.getElementById('popup-calculate-paraxial').addEventListener('click', () => triggerOpenerClick('calculate-paraxial-btn'));
        document.getElementById('popup-calculate-seidel').addEventListener('click', () => triggerOpenerClick('calculate-seidel-btn'));
        document.getElementById('popup-calculate-seidel-afocal').addEventListener('click', () => triggerOpenerClick('calculate-seidel-afocal-btn'));
        document.getElementById('popup-coord-transform').addEventListener('click', () => triggerOpenerClick('coord-transform-btn'));

        document.getElementById('popup-reference-focal-length').addEventListener('input', (e) => {
            const value = e.target.value;
            const ref = getOpenerEl('reference-focal-length');
            if (ref) ref.value = value;
            try {
                import('../data/table-configuration.ts').then(({ saveReferenceFocalLengthProjection }) => {
                    try { saveReferenceFocalLengthProjection(value); } catch (_) {}
                });
            } catch (_) {}
        });

        // Coordinate transformation controls in popup
        const popupTransformSurfaceSelect = document.getElementById('popup-transform-surface-select');
        const popupShowLocalCoordsBtn = document.getElementById('popup-show-local-coords-btn');
        const popupCancelTransformBtn = document.getElementById('popup-cancel-transform-btn');
        const popupSaveLocalCoordsBtn = document.getElementById('popup-save-local-coords-btn');
        const popupErrorBar = document.getElementById('popup-transform-error-bar');
        const popupErrorText = document.getElementById('popup-transform-error-text');
        const popupProgressWrapper = document.getElementById('popup-transform-progress-wrapper');
        const popupProgressText = document.getElementById('popup-transform-progress-text');
        const popupProgressBar = document.getElementById('popup-transform-progressbar');

        function showPopupError(message) {
            if (popupErrorBar && popupErrorText) {
                popupErrorText.textContent = message;
                popupErrorBar.style.display = '';
            }
        }

        function hidePopupError() {
            if (popupErrorBar) popupErrorBar.style.display = 'none';
        }

        function setPopupProgress(percent, message) {
            if (popupProgressWrapper) popupProgressWrapper.style.display = 'block';
            if (popupProgressBar && Number.isFinite(percent)) {
                popupProgressBar.value = Math.max(0, Math.min(100, percent));
            }
            if (popupProgressText && message) popupProgressText.textContent = message;
        }

        function hidePopupProgress() {
            if (popupProgressWrapper) popupProgressWrapper.style.display = 'none';
        }

        // Update surface select from opener
        function updatePopupSurfaceSelect() {
            if (!popupTransformSurfaceSelect) return;
            try {
                const getOpticalSystemRows = window.opener && window.opener.getOpticalSystemRows;
                if (typeof getOpticalSystemRows !== 'function') return;
                
                const opticalSystemRows = getOpticalSystemRows();
                if (!opticalSystemRows || opticalSystemRows.length === 0) return;
                
                popupTransformSurfaceSelect.innerHTML = '<option value="">Select surface...</option>';
                
                opticalSystemRows.forEach((row, index) => {
                    const objectType = String(row?.['object type'] ?? row?.object ?? '').toLowerCase();
                    if (objectType === 'object') return;
                    
                    const surfType = String(row?.surfType ?? row?.type ?? '').toLowerCase();
                    if (surfType === 'ct' || surfType === 'coordtrans' || surfType === 'coordinatebreak' ||
                        surfType === 'coord trans' || surfType === 'coordinate break') {
                        return;
                    }
                    
                    const option = document.createElement('option');
                    option.value = index;
                    
                    let label = 'Surf ' + index;
                    if (row.comment) label += ': ' + row.comment;
                    else if (row.material && row.material !== 'AIR') label += ': ' + row.material;
                    
                    option.textContent = label;
                    popupTransformSurfaceSelect.appendChild(option);
                });
            } catch (error) {
                console.error('Error updating popup surface select:', error);
            }
        }

        // Show Local Coords button
        if (popupShowLocalCoordsBtn) {
            popupShowLocalCoordsBtn.addEventListener('click', async function() {
                hidePopupError();
                
                try {
                    const surfaceIndex = parseInt(popupTransformSurfaceSelect?.value);
                    if (!surfaceIndex && surfaceIndex !== 0) {
                        showPopupError('Please select a surface first.');
                        return;
                    }
                    
                    const calculateAllSurfacesLocalCoordinates = window.opener && window.opener.calculateAllSurfacesLocalCoordinates;
                    const getOpticalSystemRows = window.opener && window.opener.getOpticalSystemRows;
                    const tableOpticalSystem = window.opener && window.opener.tableOpticalSystem;
                    
                    if (typeof calculateAllSurfacesLocalCoordinates !== 'function') {
                        showPopupError('Coordinate transformation function not available.');
                        return;
                    }
                    
                    if (typeof getOpticalSystemRows !== 'function') {
                        showPopupError('Optical system data not available.');
                        return;
                    }
                    
                    const opticalSystemRows = getOpticalSystemRows();
                    if (!opticalSystemRows || opticalSystemRows.length === 0) {
                        showPopupError('No optical system data. Please load or create an optical system.');
                        return;
                    }
                    
                    popupShowLocalCoordsBtn.disabled = true;
                    if (popupCancelTransformBtn) popupCancelTransformBtn.style.display = '';
                    if (popupSaveLocalCoordsBtn) popupSaveLocalCoordsBtn.style.display = 'none';
                    
                    if (window.opener) window.opener._transformCalculationCancelled = false;
                    
                    const result = await calculateAllSurfacesLocalCoordinates(
                        opticalSystemRows,
                        surfaceIndex,
                        (percent, message) => setPopupProgress(percent, message)
                    );
                    
                    if (window.opener) {
                        window.opener._cachedLocalCoords = result;
                        window.opener._showLocalCoords = true;
                    }
                    
                    if (tableOpticalSystem) {
                        tableOpticalSystem.redraw();
                    }
                    
                    if (popupSaveLocalCoordsBtn) popupSaveLocalCoordsBtn.style.display = '';
                    
                    hidePopupProgress();
                    
                } catch (error) {
                    console.error('Coordinate transformation error:', error);
                    showPopupError(error.message || 'Failed to calculate local coordinates.');
                    hidePopupProgress();
                } finally {
                    popupShowLocalCoordsBtn.disabled = false;
                    if (popupCancelTransformBtn) popupCancelTransformBtn.style.display = 'none';
                }
            });
        }

        // Cancel button
        if (popupCancelTransformBtn) {
            popupCancelTransformBtn.addEventListener('click', function() {
                if (window.opener) window.opener._transformCalculationCancelled = true;
                if (popupCancelTransformBtn) popupCancelTransformBtn.style.display = 'none';
                hidePopupProgress();
                showPopupError('Calculation cancelled by user.');
            });
        }

        // Save as JSON button
        if (popupSaveLocalCoordsBtn) {
            popupSaveLocalCoordsBtn.addEventListener('click', function() {
                try {
                    const data = window.opener && window.opener._cachedLocalCoords;
                    if (!data) {
                        showPopupError('No coordinate data to save. Please calculate first.');
                        return;
                    }
                    
                    const json = JSON.stringify(data, null, 2);
                    const blob = new Blob([json], { type: 'application/json' });
                    const url = URL.createObjectURL(blob);
                    
                    const timestamp = new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19);
                    const surfaceIndex = data.metadata?.targetSurfaceIndex ?? 'unknown';
                    const filename = 'local-coords-surf' + surfaceIndex + '-' + timestamp + '.json';
                    
                    const a = document.createElement('a');
                    a.href = url;
                    a.download = filename;
                    a.click();
                    
                    URL.revokeObjectURL(url);
                    
                } catch (error) {
                    console.error('Save error:', error);
                    showPopupError('Failed to save JSON file: ' + error.message);
                }
            });
        }

        // Update surface select on load and periodically
        updatePopupSurfaceSelect();
        setInterval(updatePopupSurfaceSelect, 1000);

        // Keep in sync with the main window.
        setInterval(syncFromOpener, 500);
        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();
    <\/script>
</body>
</html>
                        `);try{I.document.close()}catch{}}));const r=document.getElementById("open-spot-diagram-window-btn");r&&r.addEventListener("click",()=>{if(H.__spotDiagramPopup&&!H.__spotDiagramPopup.closed){try{H.__spotDiagramPopup.close()}catch{}H.__spotDiagramPopup=null}try{En(H)}catch(I){console.warn("Failed to update spot diagram selects:",I)}const N=n("Spot Diagram","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__spotDiagramPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Spot Diagram</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: white;
            color: #333;
            font-weight: 600;
            flex: 0 0 auto;
            border-bottom: 1px solid #ddd;
        }
        .controls {
            padding: 10px 12px;
            background: white;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls input, .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .pattern-btn.active { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: auto;
            background: white;
        }
        #popup-spot-diagram-container {
            min-height: 100%;
        }
        .note {
            padding: 10px 12px;
            color: #666;
            font-size: 12px;
            border-bottom: 1px solid #eee;
            background: #fff;
        }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="header"></div>
    <div class="controls">
        <label for="popup-surface-number-select">Surf:</label>
        <select id="popup-surface-number-select"></select>

        <label for="popup-ray-count-input">Ray number:</label>
        <input type="number" id="popup-ray-count-input" value="501" min="1" step="1" />

        <label for="popup-ring-count-select">Ring count:</label>
        <select id="popup-ring-count-select">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
            <option value="6">6</option>
            <option value="7">7</option>
            <option value="8">8</option>
            <option value="9">9</option>
            <option value="10" selected>10</option>
            <option value="12">12</option>
            <option value="15">15</option>
            <option value="16">16</option>
            <option value="20">20</option>
            <option value="24">24</option>
            <option value="32">32</option>
        </select>

        <label for="popup-pattern-select">Ray pattern:</label>
        <select id="popup-pattern-select">
            <option value="annular" selected>Annular</option>
            <option value="grid">Rectangle</option>
        </select>

        <button id="popup-show-spot-diagram-btn" type="button">Show spot diagram</button>
    </div>
    <div id="popup-spot-debug" style="display:none;"></div>
    <div id="popup-spot-run-debug" style="display:none;"></div>
    <div id="popup-spot-stats" style="display:none;"></div>
    <div id="popup-spot-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-spot-progress-text" style="margin-bottom: 6px;">Calculating spot diagram...</div>
        <progress id="popup-spot-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-spot-diagram-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function setPopupDebug(text) {
            return;
        }

        function setPopupRunDebug(text) {
            return;
        }

        function setPopupStats(text) {
            try {
                const el = document.getElementById('popup-spot-stats');
                if (el) el.textContent = String(text || '');
            } catch (_) {}
        }

        function syncSurfaceOptionsFromOpener() {
            const openerSelect = getOpenerEl('surface-number-select');
            const popupSelect = document.getElementById('popup-surface-number-select');
            if (!popupSelect) return;

            const populateFromOpenerRows = () => {
                try {
                    const rows = (() => {
                        try {
                            if (!window.opener) return null;
                            if (typeof window.opener.getOpticalSystemRows === 'function') {
                                const r = window.opener.getOpticalSystemRows(window.opener.tableOpticalSystem);
                                if (Array.isArray(r) && r.length > 0) return r;
                                const r2 = window.opener.getOpticalSystemRows();
                                if (Array.isArray(r2) && r2.length > 0) return r2;
                            }
                        } catch (_) {}
                        try {
                            if (window.opener && window.opener.tableOpticalSystem && typeof window.opener.tableOpticalSystem.getData === 'function') {
                                const r = window.opener.tableOpticalSystem.getData();
                                if (Array.isArray(r) && r.length > 0) return r;
                            }
                        } catch (_) {}
                        return null;
                    })();
                    if (!Array.isArray(rows) || rows.length === 0) {
                        setPopupDebug('Surf sync: opener rows unavailable (0 rows)');
                        return false;
                    }

                    const sharedOptions = (() => {
                        try {
                            if (window.opener && typeof window.opener.generateSurfaceOptions === 'function') {
                                const opts = window.opener.generateSurfaceOptions(rows);
                                if (Array.isArray(opts) && opts.length > 0) return opts;
                            }
                        } catch (_) {}
                        return null;
                    })();

                    if (Array.isArray(sharedOptions) && sharedOptions.length > 0) {
                        popupSelect.innerHTML = '';
                        const placeholder = document.createElement('option');
                        placeholder.value = '';
                        placeholder.textContent = 'Select Surf';
                        popupSelect.appendChild(placeholder);

                        for (const o of sharedOptions) {
                            const opt = document.createElement('option');
                            opt.value = String(o?.value ?? o?.surfaceId ?? '');
                            opt.textContent = String(o?.label ?? ('Surf ' + String(o?.surfaceId ?? '')));
                            {
                                const labelLower = String(opt.textContent || '').toLowerCase();
                                const imageFlag = !!(o?.isImage === true || labelLower.includes('(image)') || labelLower.includes(' image'));
                                opt.dataset.isImage = imageFlag ? '1' : '0';
                            }
                            if (o && o.rowId !== undefined && o.rowId !== null && String(o.rowId) !== '') {
                                opt.dataset.rowId = String(o.rowId);
                            }
                            if (o && Number.isInteger(o.rowIndex)) {
                                opt.dataset.rowIndex = String(o.rowIndex);
                            }
                            popupSelect.appendChild(opt);
                        }

                        if (String(popupSelect.value || '').trim() === '') {
                            const opts = Array.from(popupSelect.options || []);
                            const image = opts.find((opt) => String(opt.textContent || '').includes('(Image)') && String(opt.value || '').trim() !== '');
                            if (image) popupSelect.value = String(image.value);
                            else {
                                const last = opts.filter((opt) => String(opt.value || '').trim() !== '').pop();
                                if (last) popupSelect.value = String(last.value);
                            }
                        }

                        setPopupDebug('Surf sync: populated from shared generateSurfaceOptions. rows=' + rows.length + ', options=' + sharedOptions.length + ', selected=' + String(popupSelect.value || '(none)'));
                        return true;
                    }

                    const normalizeType = (v) => String(v || '').trim().toLowerCase();
                    const compactType = (v) => normalizeType(v).replace(/[s_-]+/g, '');
                    const isObjectType = (v) => {
                        const n = normalizeType(v);
                        const c = compactType(v);
                        if (!n && !c) return false;
                        if (n === 'object' || c === 'object' || c === 'objectsurface') return true;
                        return n.startsWith('object ') || n.startsWith('object-') || n.startsWith('object_');
                    };
                    const isCoordTransType = (v) => {
                        const n = normalizeType(v);
                        const c = compactType(v);
                        return n === 'ct' || n === 'coord trans' || n === 'coordinate break' || c === 'ct' || c === 'coordtrans' || c === 'coordinatebreak';
                    };
                    const isGapType = (v) => {
                        const n = normalizeType(v);
                        const c = compactType(v);
                        return n === 'gap' || n === 'air gap' || c === 'gap' || c === 'airgap';
                    };
                    const isSkippableRow = (row) => {
                        const objTypeRaw = row && (row['object type'] ?? row.objectType ?? row.object ?? '');
                        const surfTypeRaw = row && (row.surfType ?? row['surf type'] ?? row.type ?? '');
                        const surfaceType = objTypeRaw || surfTypeRaw || 'Standard';
                        const blockType = String(row && (row._blockType ?? row.blockType ?? '')).trim().toLowerCase();
                        const blockRole = String(row && (row._surfaceRole ?? row.surfaceRole ?? '')).trim().toLowerCase();
                        const isParaxialBack = (blockType === 'paraxial' || blockType === 'thinlens') && blockRole === 'back';
                        return (
                            isObjectType(objTypeRaw) || isObjectType(surfTypeRaw) || isObjectType(surfaceType) || isObjectType(blockType) ||
                            isCoordTransType(objTypeRaw) || isCoordTransType(surfTypeRaw) || isCoordTransType(surfaceType) ||
                            isGapType(objTypeRaw) || isGapType(surfTypeRaw) || isGapType(surfaceType) ||
                            isGapType(blockType) || isGapType(blockRole) || isParaxialBack
                        );
                    };

                    let surfaceId = 0;
                    let added = 0;
                    popupSelect.innerHTML = '';

                    const placeholder = document.createElement('option');
                    placeholder.value = '';
                    placeholder.textContent = 'Select Surf';
                    popupSelect.appendChild(placeholder);

                    for (const [i, row] of rows.entries()) {
                        const objTypeRaw = row && (row['object type'] ?? row.objectType ?? row.object ?? '');
                        const surfTypeRaw = row && (row.surfType ?? row['surf type'] ?? row.type ?? '');
                        const surfaceType = objTypeRaw || surfTypeRaw || 'Standard';
                        const blockType = String(row && (row._blockType ?? row.blockType ?? '')).trim();
                        const isParaxialLike = /^(paraxial|thinlens)$/i.test(blockType);

                        if (isSkippableRow(row)) continue;
                        surfaceId++;

                        const n = normalizeType(surfaceType);
                        const c = compactType(surfaceType);
                        const isStop = (n === 'stop' || c === 'stop' || n.includes('stop'));
                        const isImage = (n === 'image' || c === 'image' || n.includes('image'));

                        let label = 'Surf ' + surfaceId;
                        if (isStop) label += ' (Stop)';
                        else if (isImage) label += ' (Image)';
                        else if (isParaxialLike) label += ' (' + (blockType.toLowerCase() === 'paraxial' ? 'Paraxial' : 'ThinLens') + ')';
                        else label += ' (' + surfaceType + ')';

                        const opt = document.createElement('option');
                        opt.value = String(surfaceId);
                        opt.textContent = label;
                        opt.dataset.rowIndex = String(i);
                        opt.dataset.isImage = isImage ? '1' : '0';
                        if (row && row.id !== undefined && row.id !== null && String(row.id) !== '') {
                            opt.dataset.rowId = String(row.id);
                        }
                        try {
                            const norm = (v) => String(v ?? '').trim().toLowerCase();
                            const n0 = (v) => {
                                const x = Number(v);
                                return Number.isFinite(x) ? String(x) : norm(v);
                            };
                            const rowSig = [
                                't:' + norm(surfaceType),
                                'r:' + n0(row?.radius ?? row?.R ?? ''),
                                'th:' + n0(row?.thickness ?? row?.T ?? ''),
                                'sd:' + n0(row?.semidia ?? row?.semiDia ?? ''),
                                'm:' + norm(row?.material ?? row?.glass ?? row?.['glass'] ?? row?.refractiveIndex ?? ''),
                                'c:' + norm(row?.comment ?? row?.name ?? '')
                            ].join('|');
                            if (rowSig) opt.dataset.rowSig = rowSig;
                        } catch (_) {}
                        popupSelect.appendChild(opt);
                        added++;
                    }
                    if (added > 0 && String(popupSelect.value || '').trim() === '') {
                        const opts = Array.from(popupSelect.options || []);
                        const image = opts.find((opt) => String(opt.textContent || '').includes('(Image)') && String(opt.value || '').trim() !== '');
                        if (image) popupSelect.value = String(image.value);
                        else {
                            const last = opts.filter((opt) => String(opt.value || '').trim() !== '').pop();
                            if (last) popupSelect.value = String(last.value);
                        }
                    }
                    setPopupDebug('Surf sync: populated from opener rows. rows=' + rows.length + ', options=' + added + ', selected=' + String(popupSelect.value || '(none)'));
                    return added > 0;
                } catch (_) {
                    setPopupDebug('Surf sync: failed to read opener rows');
                    return false;
                }
            };

            const normalizeLabel = (text) => {
                const t = String(text || '').trim();
                // Drop leading "Surf N:" / "Surface N:" / "面 N" etc.
                return t
                    .replace(/^Surfs*d+s*[:-]?s*/i, '')
                    .replace(/^Surfaces*d+s*[:-]?s*/i, '')
                    .replace(/^面s*d+s*[:-]?s*/i, '')
                    .trim();
            };

            const prevValue = popupSelect.value;
            const prevText = popupSelect.options && popupSelect.selectedIndex >= 0
                ? popupSelect.options[popupSelect.selectedIndex]?.textContent
                : '';
            const prevKey = normalizeLabel(prevText);
            const prevWasLast = popupSelect.options && popupSelect.options.length > 0
                ? popupSelect.selectedIndex === (popupSelect.options.length - 1)
                : false;

            popupSelect.innerHTML = '';
            if (!openerSelect || !openerSelect.options || openerSelect.options.length <= 1) {
                if (populateFromOpenerRows()) {
                    if (prevValue !== '') popupSelect.value = prevValue;
                    setPopupDebug('Surf sync: fallback row population used');
                    return;
                }
                const opt = document.createElement('option');
                opt.value = '';
                opt.textContent = 'Select Surf';
                popupSelect.appendChild(opt);
                setPopupDebug('Surf sync: opener select unavailable and fallback failed');
                return;
            }

            for (const o of openerSelect.options) {
                const opt = document.createElement('option');
                opt.value = o.value;
                // Replace Japanese "面" prefix and "Surface" prefix with "Surf".
                const label = (o.textContent || '').replace(/^面s*/,'Surf ').replace(/^Surfaces*/,'Surf ');
                opt.textContent = label;
                if (o.dataset && o.dataset.rowIndex) opt.dataset.rowIndex = String(o.dataset.rowIndex);
                if (o.dataset && o.dataset.rowId) opt.dataset.rowId = String(o.dataset.rowId);
                if (o.dataset && o.dataset.rowSig) opt.dataset.rowSig = String(o.dataset.rowSig);
                if (o.dataset && o.dataset.isImage) opt.dataset.isImage = String(o.dataset.isImage);
                else {
                    const labelLower = String(label || '').toLowerCase();
                    opt.dataset.isImage = (labelLower.includes('(image)') || labelLower.includes(' image')) ? '1' : '0';
                }
                popupSelect.appendChild(opt);
            }

            // Preserve selection only when the user explicitly changed Surf in the popup.
            const hasValue = (v) => Array.from(popupSelect.options || []).some((opt) => String(opt.value) === String(v));
            const userLockedSurface = !!(popupSelect.dataset && popupSelect.dataset.userLocked === '1');
            if (userLockedSurface && prevValue !== '' && hasValue(prevValue)) {
                popupSelect.value = prevValue;
                return;
            }
            if (userLockedSurface && prevKey) {
                const opts = Array.from(popupSelect.options || []);
                const match = opts.find((opt) => normalizeLabel(opt.textContent) === prevKey);
                if (match) {
                    popupSelect.value = match.value;
                    return;
                }
            }
            if (userLockedSurface && prevWasLast && popupSelect.options && popupSelect.options.length > 0) {
                popupSelect.selectedIndex = popupSelect.options.length - 1;
                return;
            }
            // Default Spot Diagram to the Image plane unless the user explicitly locked a
            // different surface in this popup, or the opener is explicitly on an Image surface.
            const openerSelectedOption = (openerSelect.selectedOptions && openerSelect.selectedOptions.length > 0)
                ? openerSelect.selectedOptions[0]
                : null;
            const openerIsImage = !!(openerSelectedOption && (
                (openerSelectedOption.dataset && String(openerSelectedOption.dataset.isImage || '') === '1') ||
                String(openerSelectedOption.textContent || '').toLowerCase().includes('(image)')
            ));

            const opts = Array.from(popupSelect.options || []);
            const image = opts.find((opt) => String(opt.textContent || '').includes('(Image)') && String(opt.value || '').trim() !== '');
            if (openerIsImage && String(openerSelect.value || '').trim() !== '' && hasValue(openerSelect.value)) {
                popupSelect.value = openerSelect.value;
            } else if (image) {
                popupSelect.value = String(image.value);
            } else if (String(openerSelect.value || '').trim() !== '' && hasValue(openerSelect.value)) {
                popupSelect.value = openerSelect.value;
            } else {
                const last = opts.filter((opt) => String(opt.value || '').trim() !== '').pop();
                if (last) popupSelect.value = String(last.value);
            }
            setPopupDebug('Surf sync: preferred image/default selection. openerOptions=' + openerSelect.options.length + ', popupOptions=' + Math.max(0, popupSelect.options.length - 1) + ', selected=' + String(popupSelect.value || '(none)'));
        }

        function resolvePopupSurfaceRowIndexForNative(popupSelect, selectedOption) {
            try {
                const ds = selectedOption && selectedOption.dataset ? selectedOption.dataset : null;
                const rowIndexRaw = ds ? ds.rowIndex : undefined;
                const rowIndex = Number(rowIndexRaw);
                if (Number.isInteger(rowIndex) && rowIndex >= 0) {
                    return rowIndex;
                }

                const ordinal = Number.parseInt(String(popupSelect && popupSelect.value !== undefined ? popupSelect.value : ''), 10);
                if (!Number.isInteger(ordinal) || ordinal <= 0) {
                    return undefined;
                }

                const rows = (() => {
                    try {
                        if (!window.opener) return null;
                        if (typeof window.opener.getOpticalSystemRows === 'function') {
                            const r = window.opener.getOpticalSystemRows(window.opener.tableOpticalSystem);
                            if (Array.isArray(r) && r.length > 0) return r;
                            const r2 = window.opener.getOpticalSystemRows();
                            if (Array.isArray(r2) && r2.length > 0) return r2;
                        }
                    } catch (_) {}
                    try {
                        if (window.opener && window.opener.tableOpticalSystem && typeof window.opener.tableOpticalSystem.getData === 'function') {
                            const r = window.opener.tableOpticalSystem.getData();
                            if (Array.isArray(r) && r.length > 0) return r;
                        }
                    } catch (_) {}
                    return null;
                })();

                if (!Array.isArray(rows) || rows.length === 0) {
                    return undefined;
                }

                const normalizeType = (v) => String(v || '').trim().toLowerCase();
                const compactType = (v) => normalizeType(v).replace(/[s_-]+/g, '');
                const isObjectType = (v) => {
                    const n = normalizeType(v);
                    const c = compactType(v);
                    if (!n && !c) return false;
                    if (n === 'object' || c === 'object' || c === 'objectsurface') return true;
                    return n.startsWith('object ') || n.startsWith('object-') || n.startsWith('object_');
                };
                const isCoordTransType = (v) => {
                    const n = normalizeType(v);
                    const c = compactType(v);
                    return n === 'ct' || n === 'coord trans' || n === 'coordinate break' || c === 'ct' || c === 'coordtrans' || c === 'coordinatebreak';
                };
                const isGapType = (v) => {
                    const n = normalizeType(v);
                    const c = compactType(v);
                    return n === 'gap' || n === 'air gap' || c === 'gap' || c === 'airgap';
                };
                const isSkippableRow = (row) => {
                    const objTypeRaw = row && (row['object type'] ?? row.objectType ?? row.object ?? '');
                    const surfTypeRaw = row && (row.surfType ?? row['surf type'] ?? row.type ?? '');
                    const surfaceType = objTypeRaw || surfTypeRaw || 'Standard';
                    const blockType = String(row && (row._blockType ?? row.blockType ?? '')).trim().toLowerCase();
                    const blockRole = String(row && (row._surfaceRole ?? row.surfaceRole ?? '')).trim().toLowerCase();
                    const isParaxialBack = (blockType === 'paraxial' || blockType === 'thinlens') && blockRole === 'back';
                    return (
                        isObjectType(objTypeRaw) || isObjectType(surfTypeRaw) || isObjectType(surfaceType) || isObjectType(blockType) ||
                        isCoordTransType(objTypeRaw) || isCoordTransType(surfTypeRaw) || isCoordTransType(surfaceType) ||
                        isGapType(objTypeRaw) || isGapType(surfTypeRaw) || isGapType(surfaceType) ||
                        isGapType(blockType) || isGapType(blockRole) || isParaxialBack
                    );
                };

                let count = 0;
                for (let i = 0; i < rows.length; i++) {
                    if (isSkippableRow(rows[i])) continue;
                    count += 1;
                    if (count === ordinal) return i;
                }

                return undefined;
            } catch (_) {
                return undefined;
            }
        }

        function syncInputsFromOpener() {
            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');
            const popupRay = document.getElementById('popup-ray-count-input');
            const popupRing = document.getElementById('popup-ring-count-select');

            if (popupRay && openerRay && popupRay.value !== openerRay.value) popupRay.value = openerRay.value;
            if (popupRing && openerRing && popupRing.value !== openerRing.value) popupRing.value = openerRing.value;

            // pattern
            const annular = getOpenerEl('annular-pattern-btn');
            const popupPattern = document.getElementById('popup-pattern-select');
            if (popupPattern) {
                const userLocked = popupPattern.dataset && popupPattern.dataset.userLocked === '1';
                if (!userLocked) {
                    const isAnnular = !!annular && annular.classList.contains('active');
                    popupPattern.value = isAnnular ? 'annular' : 'grid';
                }
            }
        }

        function setPopupPattern(pattern) {
            const isAnnular = String(pattern || 'annular') !== 'grid';
            const normalizedPattern = isAnnular ? 'annular' : 'grid';

            const openerAnnular = getOpenerEl('annular-pattern-btn');
            const openerGrid = getOpenerEl('grid-pattern-btn');
            if (isAnnular && openerAnnular) openerAnnular.click();
            if (!isAnnular && openerGrid) openerGrid.click();

            try {
                if (window.opener) {
                    window.opener.__cooptSpotPattern = normalizedPattern;
                }
            } catch (_) {}
            try {
                window.__cooptSpotPattern = normalizedPattern;
            } catch (_) {}
            try {
                localStorage.setItem('spotDiagramPattern', normalizedPattern);
            } catch (_) {}
        }

        document.getElementById('popup-pattern-select').addEventListener('change', (e) => {
            const v = e && e.target && e.target.value ? String(e.target.value) : 'annular';
            try {
                const popupPattern = document.getElementById('popup-pattern-select');
                if (popupPattern && popupPattern.dataset) popupPattern.dataset.userLocked = '1';
            } catch (_) {}
            setPopupPattern(v);
        });

        document.getElementById('popup-surface-number-select').addEventListener('change', () => {
            try {
                const popupSurface = document.getElementById('popup-surface-number-select');
                if (popupSurface && popupSurface.dataset) popupSurface.dataset.userLocked = '1';
            } catch (_) {}
        });

        document.getElementById('popup-show-spot-diagram-btn').addEventListener('click', async () => {
            const popupContainer = document.getElementById('popup-spot-diagram-container');
            if (popupContainer) popupContainer.innerHTML = '';

            const opener = window.opener;
            if (!opener) {
                if (popupContainer) popupContainer.textContent = 'Main window is not available.';
                return;
            }

            try {
                if (typeof opener.loadActiveConfigurationToTables === 'function') {
                    await opener.loadActiveConfigurationToTables({
                        applyToUI: true,
                        suppressOpticalSystemDataChanged: true,
                    });
                    await new Promise((resolve) => setTimeout(resolve, 0));
                }
            } catch (_) {}

            // Ensure surface indices/options are up-to-date (CB insert/delete shifts indices).
            try { syncSurfaceOptionsFromOpener(); } catch (_) {}

            const progressWrapper = document.getElementById('popup-spot-progress-wrapper');
            const progressBarEl = document.getElementById('popup-spot-progressbar');
            const progressTextEl = document.getElementById('popup-spot-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl) {
                        const pct = Number.isFinite(value) ? (String(Math.round(Math.max(0, Math.min(100, Number(value))))) + '%') : '';
                        const msg = (typeof text === 'string' && text.trim().length > 0) ? text : 'Working...';
                        progressTextEl.textContent = pct ? (msg + ' (' + pct + ')') : msg;
                    }
                } catch (_) {}
            };

            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');
            const openerSurface = getOpenerEl('surface-number-select');
            const popupRay = document.getElementById('popup-ray-count-input');
            const popupRing = document.getElementById('popup-ring-count-select');
            const popupSurface = document.getElementById('popup-surface-number-select');
            const popupPattern = document.getElementById('popup-pattern-select');
            const popupSurfaceSelectedOption = (popupSurface && popupSurface.selectedOptions && popupSurface.selectedOptions.length > 0)
                ? popupSurface.selectedOptions[0]
                : null;
            const popupSurfaceIsImage = !!(popupSurfaceSelectedOption && (
                (popupSurfaceSelectedOption.dataset && String(popupSurfaceSelectedOption.dataset.isImage || '') === '1') ||
                String(popupSurfaceSelectedOption.textContent || '').toLowerCase().includes('(image)')
            ));
            const popupSurfaceRowIndex = resolvePopupSurfaceRowIndexForNative(popupSurface, popupSurfaceSelectedOption);
            const popupSurfaceRowId = (() => {
                const raw = popupSurfaceSelectedOption && popupSurfaceSelectedOption.dataset
                    ? popupSurfaceSelectedOption.dataset.rowId
                    : undefined;
                const s = String(raw || '').trim();
                return s !== '' ? s : undefined;
            })();
            const popupSurfaceRowSig = (() => {
                const raw = popupSurfaceSelectedOption && popupSurfaceSelectedOption.dataset
                    ? popupSurfaceSelectedOption.dataset.rowSig
                    : undefined;
                const s = String(raw || '').trim();
                return s !== '' ? s : undefined;
            })();

            if (!popupSurface || String(popupSurface.value || '').trim() === '') {
                if (popupContainer) {
                    popupContainer.textContent = 'Please select Surf before running Spot Diagram.';
                }
                setPopupRunDebug('Show failed: Surf not selected. popupOptions=' + ((popupSurface && popupSurface.options) ? Math.max(0, popupSurface.options.length - 1) : 0));
                setProgress(100, 'Failed: Surf not selected');
                return;
            }

            if (openerRay && popupRay) openerRay.value = popupRay.value;
            if (openerRing && popupRing) openerRing.value = popupRing.value;
            if (openerSurface && popupSurface) openerSurface.value = popupSurface.value;

            try {
                setProgress(0, 'Starting...');
                setPopupStats('Native stats: running...');

                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();
                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    typeof opener.runDesktopAnalysisComputeForPopup === 'function'
                );
                const canUseNativeRustSpot = canUseDesktopRust && !!(
                    typeof opener.runDesktopNativeSpotRaytraceForPopup === 'function'
                );
                const canUseRustSpotDiagram = canUseDesktopRust && !!(
                    typeof window !== 'undefined'
                    && window.__COOPT_ENABLE_RUST_SPOT_DIAGRAM !== false
                );

                if (canUseNativeRustSpot) {
                    try {
                        setProgress(25, 'Computing Spot Diagram (Native Rust)...');
                        const nativeStart = (typeof performance !== 'undefined' && typeof performance.now === 'function') ? performance.now() : Date.now();
                        try {
                            const objCount = Array.isArray(opener.getObjectRows ? opener.getObjectRows(opener.tableObject) : null)
                                ? opener.getObjectRows(opener.tableObject).length
                                : (Array.isArray(opener.tableObject?.data) ? opener.tableObject.data.length : 0);
                            setPopupRunDebug('Native Spot input: objectRows=' + objCount);
                        } catch (_) {}
                        const objectRowsForNative = (() => {
                            try {
                                if (opener.tableObject && typeof opener.tableObject.getData === 'function') {
                                    const rows = opener.tableObject.getData();
                                    if (Array.isArray(rows)) return rows;
                                }
                            } catch (_) {}
                            try {
                                if (typeof opener.getObjectRows === 'function') {
                                    const rows = opener.getObjectRows(opener.tableObject);
                                    if (Array.isArray(rows)) return rows;
                                }
                            } catch (_) {}
                            try {
                                const raw = opener.localStorage && opener.localStorage.getItem('objectTableData');
                                if (raw) {
                                    const parsed = JSON.parse(raw);
                                    if (Array.isArray(parsed)) return parsed;
                                }
                            } catch (_) {}
                            return [];
                        })();
                        try {
                            const highFieldObjects = (Array.isArray(objectRowsForNative) ? objectRowsForNative : []).filter((row) => {
                                const ax = Number(row?.xAngle ?? row?.objectAngleX ?? row?.xHeightAngle ?? row?.x ?? row?.angleX);
                                const ay = Number(row?.yAngle ?? row?.objectAngleY ?? row?.yHeightAngle ?? row?.y ?? row?.angle ?? row?.angleY);
                                const maxField = Math.max(Math.abs(Number.isFinite(ax) ? ax : 0), Math.abs(Number.isFinite(ay) ? ay : 0));
                                return maxField >= 15;
                            });
                            setPopupRunDebug(
                                'Native Spot preflight: objects=' + String(objectRowsForNative.length) +
                                ', highFieldObjects=' + String(highFieldObjects.length)
                            );
                        } catch (_) {}

                        const result = await opener.runDesktopNativeSpotRaytraceForPopup({
                            surfaceIndex: (popupSurfaceRowIndex !== undefined)
                                ? popupSurfaceRowIndex
                                : (popupSurface && popupSurface.value !== '' ? parseInt(popupSurface.value, 10) : undefined),
                            rayCount: popupRay && popupRay.value !== '' ? parseInt(popupRay.value, 10) : undefined,
                            ringCount: popupRing && popupRing.value !== '' ? parseInt(popupRing.value, 10) : undefined,
                            pattern: popupPattern ? String(popupPattern.value || 'annular') : 'annular',
                            wavelengthMode: 'all',
                            objectRows: objectRowsForNative,
                        });
                        const series = Array.isArray(result?.series) ? result.series : [];
                        const totalPointCount = series.reduce((sum, s) => {
                            const pts = Array.isArray(s?.points) ? s.points.length : 0;
                            return sum + pts;
                        }, 0);
                        const firstSeries = series.length > 0 ? series[0] : null;
                        const firstPoint = (firstSeries && Array.isArray(firstSeries.points) && firstSeries.points.length > 0)
                            ? firstSeries.points[0]
                            : null;
                        try {
                            setPopupRunDebug(
                                'Native Spot run: selected=' + String(popupSurface && popupSurface.value || '(none)') +
                                ', rowIndex=' + String(popupSurfaceRowIndex !== undefined ? popupSurfaceRowIndex : '(unresolved)') +
                                ', hit=' + String(Number(result?.totalHitRays) || 0) +
                                ', series=' + String(series.length) +
                                ', points=' + String(totalPointCount) +
                                (firstPoint
                                    ? ', first=(' + Number(firstPoint?.xUm || 0).toFixed(2) + ',' + Number(firstPoint?.yUm || 0).toFixed(2) + ')'
                                    : ', first=(none)')
                            );
                        } catch (_) {}
                        if (!series.length) {
                            throw new Error('Native Rust Spot result is empty');
                        }
                        const airyRadiusUm = Number(result?.airyRadiusUm);
                        try {
                            const requestedRays = Number(result?.requestedRays);
                            const generatedRays = Number(result?.generatedRays);
                            const totalAttemptedRays = Number(result?.totalAttemptedRays);
                            const totalHitRays = Number(result?.totalHitRays);
                            const meanHitRatePercent = Number(result?.meanHitRatePercent);
                            const maxHitRays = Number(result?.maxHitRays);
                            const wavelengthCount = Number(result?.wavelengthCount);
                            const raySeriesGenerationMs = Number(result?.timingMs?.raySeriesGeneration);
                            const nativeInvokeMs = Number(result?.timingMs?.nativeInvoke);
                            const nativeTraceMs = Number(result?.timingMs?.nativeTrace);
                            const stats = Array.isArray(result?.seriesStats) ? result.seriesStats : [];
                            const statsText = stats.slice(0, 3).map((s) => {
                                const label = String(s?.label || '-');
                                const h = Number(s?.hitRays) || 0;
                                const a = Number(s?.attemptedRays) || 0;
                                const r = Number(s?.hitRatePercent) || 0;
                                return label + ': ' + h + '/' + a + ' (' + r.toFixed(1) + '%)';
                            }).join(', ');
                            const timingsText =
                                'timing(ms): gen=' + (Number.isFinite(raySeriesGenerationMs) ? raySeriesGenerationMs.toFixed(1) : '-') +
                                ', trace=' + (Number.isFinite(nativeTraceMs) ? nativeTraceMs.toFixed(1) : '-') +
                                ', native=' + (Number.isFinite(nativeInvokeMs) ? nativeInvokeMs.toFixed(1) : '-');
                            setPopupStats(
                                'Native stats: requested=' + (Number.isFinite(requestedRays) ? requestedRays : '-') +
                                ', generated=' + (Number.isFinite(generatedRays) ? generatedRays : '-') +
                                ', attempted=' + (Number.isFinite(totalAttemptedRays) ? totalAttemptedRays : '-') +
                                ', hits=' + (Number.isFinite(totalHitRays) ? totalHitRays : '-') +
                                ', meanHitRate=' + (Number.isFinite(meanHitRatePercent) ? meanHitRatePercent.toFixed(1) + '%' : '-') +
                                ', maxHit=' + (Number.isFinite(maxHitRays) ? maxHitRays : '-') +
                                ', wavelengths=' + (Number.isFinite(wavelengthCount) ? wavelengthCount : '-') +
                                (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0 ? ', airyR=' + airyRadiusUm.toFixed(2) + 'µm' : '') +
                                (statsText ? ' | ' + statsText : '') +
                                ' | ' + timingsText
                            );
                        } catch (_) {}
                        if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                            throw new Error('Plotly is not available in Spot Diagram popup');
                        }

                        const traces = [];
                        const toWavelengthLabel = (rawLabel) => {
                            const text = String(rawLabel || '').trim();
                            const nm = text.match(/(d+(?:.d+)?)s*nm/i);
                            if (nm && nm[1]) return 'Wavelength ' + nm[1] + 'nm';
                            const lower = text.toLowerCase();
                            if (lower.includes('primary')) return 'Wavelength Primary';
                            return 'Wavelength ' + text;
                        };
                        const wavelengthLabelFromSeries = (seriesItem, rawLabel) => {
                            const wl = Number(seriesItem?.wavelengthUm);
                            if (Number.isFinite(wl) && wl > 0) {
                                return 'Wavelength ' + (wl * 1000).toFixed(1) + 'nm';
                            }
                            return toWavelengthLabel(rawLabel);
                        };
                        const parseSeriesLabel = (label, fallbackObjectLabel) => {
                            const raw = String(label || '').trim();
                            if (!raw) {
                                return { objectLabel: fallbackObjectLabel, wavelengthLabel: 'Wavelength Primary' };
                            }
                            const m = raw.match(/(Primary(?:s*([^)]*))?|d+(?:.d+)?s*nm)s*$/i);
                            if (m && m[1]) {
                                const wlRaw = String(m[1] || 'Primary');
                                const prefix = raw.slice(0, Math.max(0, m.index || 0)).replace(/[|@-s]+$/, '').trim();
                                return {
                                    objectLabel: prefix || fallbackObjectLabel,
                                    wavelengthLabel: toWavelengthLabel(wlRaw),
                                };
                            }
                            return { objectLabel: raw, wavelengthLabel: 'Wavelength Primary' };
                        };

                        const calcPercentile = (arr, q) => {
                            if (!Array.isArray(arr) || arr.length === 0) return 0;
                            const sorted = arr
                                .filter((v) => Number.isFinite(v) && v >= 0)
                                .sort((a, b) => a - b);
                            if (sorted.length === 0) return 0;
                            const qq = Math.max(0, Math.min(1, Number(q) || 0));
                            const idx = Math.floor((sorted.length - 1) * qq);
                            return sorted[Math.max(0, Math.min(sorted.length - 1, idx))] || 0;
                        };

                        const cullExtremeSpotOutliers = (rawPoints, centerXUm, centerYUm) => {
                            const pts = (Array.isArray(rawPoints) ? rawPoints : [])
                                .map((p) => ({
                                    xUm: Number(p?.xUm),
                                    yUm: Number(p?.yUm),
                                }))
                                .filter((p) => Number.isFinite(p.xUm) && Number.isFinite(p.yUm));
                            if (pts.length < 40) {
                                return { points: pts, culled: 0 };
                            }

                            const cx = Number.isFinite(centerXUm) ? centerXUm : 0;
                            const cy = Number.isFinite(centerYUm) ? centerYUm : 0;
                            const radii = pts.map((p) => {
                                const dx = p.xUm - cx;
                                const dy = p.yUm - cy;
                                return Math.sqrt(dx * dx + dy * dy);
                            });

                            const p95 = calcPercentile(radii, 0.95);
                            const p99 = calcPercentile(radii, 0.99);
                            const maxR = radii.reduce((m, v) => Math.max(m, Number(v) || 0), 0);
                            const clipR = Math.max(5, p99 * 1.35, p95 * 2.2);
                            if (!Number.isFinite(clipR) || clipR <= 0 || maxR <= clipR) {
                                return { points: pts, culled: 0 };
                            }

                            const filtered = [];
                            for (let i = 0; i < pts.length; i++) {
                                const p = pts[i];
                                const dx = p.xUm - cx;
                                const dy = p.yUm - cy;
                                const r = Math.sqrt(dx * dx + dy * dy);
                                if (r <= clipR) {
                                    filtered.push(p);
                                }
                            }

                            const culled = pts.length - filtered.length;
                            const maxAllowCull = Math.max(3, Math.floor(pts.length * 0.02));
                            if (culled <= 0 || culled > maxAllowCull || filtered.length < 10) {
                                return { points: pts, culled: 0 };
                            }

                            return { points: filtered, culled };
                        };

                        const groupedByObject = new Map();
                        for (let i = 0; i < series.length; i++) {
                            const s = series[i] || {};
                            const parsed = parseSeriesLabel(s?.label, 'Object ' + (i + 1));
                            if (!groupedByObject.has(parsed.objectLabel)) {
                                groupedByObject.set(parsed.objectLabel, []);
                            }
                            groupedByObject.get(parsed.objectLabel).push(s);
                        }
                        const objectEntries = Array.from(groupedByObject.entries());

                        let culledPointsCount = 0;
                        const preparedObjects = objectEntries.map(([objectLabel, objectSeries], index) => {
                            const groups = [];
                            for (let sIdx = 0; sIdx < objectSeries.length; sIdx++) {
                                const s = objectSeries[sIdx] || {};
                                const parsed = parseSeriesLabel(s?.label, objectLabel);
                                const points = Array.isArray(s?.points) ? s.points : [];
                                const hasFieldAngle = !!s?.hasFieldAngle;
                                const chiefXUm = Number(s?.chiefPointUm?.xUm);
                                const chiefYUm = Number(s?.chiefPointUm?.yUm);
                                const useChiefCentering = hasFieldAngle && Number.isFinite(chiefXUm) && Number.isFinite(chiefYUm);
                                const centeredPoints = useChiefCentering
                                    ? points.map((p) => ({
                                        xUm: (Number(p?.xUm) || 0) - chiefXUm,
                                        yUm: (Number(p?.yUm) || 0) - chiefYUm,
                                    }))
                                    : points.map((p) => ({
                                        xUm: Number(p?.xUm) || 0,
                                        yUm: Number(p?.yUm) || 0,
                                    }));

                                const culled = cullExtremeSpotOutliers(
                                    centeredPoints,
                                    useChiefCentering ? 0 : undefined,
                                    useChiefCentering ? 0 : undefined,
                                );
                                culledPointsCount += Number(culled?.culled) || 0;

                                groups.push({
                                    label: wavelengthLabelFromSeries(s, parsed.wavelengthLabel),
                                    color: String(s?.color || '#2563eb'),
                                    points: culled.points,
                                });
                            }
                            return { objectLabel, objectIndex: index + 1, groups };
                        });

                        const absSamples = [];
                        for (const obj of preparedObjects) {
                            for (const g of obj.groups) {
                                const pts = Array.isArray(g?.points) ? g.points : [];
                                for (const p of pts) {
                                    const x = Math.abs(Number(p?.xUm) || 0);
                                    const y = Math.abs(Number(p?.yUm) || 0);
                                    absSamples.push(x, y);
                                }
                            }
                        }
                        const maxAbs = absSamples.reduce((m, v) => Math.max(m, Number(v) || 0), 0);
                        const p98Abs = calcPercentile(absSamples, 0.98);
                        let unifiedRangeAbs = Math.max(1, p98Abs * 1.25);
                        if (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) {
                            unifiedRangeAbs = Math.max(unifiedRangeAbs, airyRadiusUm * 1.5);
                        }
                        unifiedRangeAbs = Math.min(unifiedRangeAbs, Math.max(50, maxAbs * 1.05 || 50));

                        const layout = {
                            margin: { l: 40, r: 16, t: 92, b: 32 },
                            showlegend: true,
                            paper_bgcolor: '#ffffff',
                            plot_bgcolor: '#ffffff',
                            annotations: [],
                            shapes: [],
                            legend: {
                                orientation: 'h',
                                yanchor: 'bottom',
                                y: 1.12,
                                xanchor: 'left',
                                x: 0,
                                traceorder: 'normal',
                                font: { size: 11 },
                            },
                            legendgroupclick: 'togglegroup',
                        };

                        const count = Math.max(1, preparedObjects.length);
                        const cols = Math.max(1, Math.min(3, Math.ceil(Math.sqrt(count))));
                        const rows = Math.max(1, Math.ceil(count / cols));
                        const hGap = 0.05;
                        const vGap = 0.12;
                        const cellW = (1 - (cols - 1) * hGap) / cols;
                        const cellH = (1 - (rows - 1) * vGap) / rows;
                        layout.height = Math.max(360, rows * 340);

                        const legendAdded = new Set();
                        const legendColorByLabel = new Map();

                        for (let i = 0; i < preparedObjects.length; i++) {
                            const obj = preparedObjects[i];
                            const objectLabel = obj.objectLabel;
                            const groups = obj.groups;

                            const axisSuffix = i === 0 ? '' : String(i + 1);
                            const xAxisName = 'x' + axisSuffix;
                            const yAxisName = 'y' + axisSuffix;
                            const xAxisLayoutKey = 'xaxis' + axisSuffix;
                            const yAxisLayoutKey = 'yaxis' + axisSuffix;

                            const col = i % cols;
                            const rowFromTop = Math.floor(i / cols);
                            const x0 = col * (cellW + hGap);
                            const x1 = x0 + cellW;
                            const y1 = 1 - rowFromTop * (cellH + vGap);
                            const y0 = y1 - cellH;

                            layout[xAxisLayoutKey] = {
                                domain: [x0, x1],
                                zeroline: true,
                                range: [-unifiedRangeAbs, unifiedRangeAbs],
                                title: rowFromTop === (rows - 1) ? 'X (µm)' : '',
                                anchor: yAxisName,
                            };
                            layout[yAxisLayoutKey] = {
                                domain: [y0, y1],
                                zeroline: true,
                                range: [-unifiedRangeAbs, unifiedRangeAbs],
                                title: col === 0 ? 'Y (µm)' : '',
                                anchor: xAxisName,
                                scaleanchor: xAxisName,
                                scaleratio: 1,
                            };

                            if (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) {
                                layout.shapes.push({
                                    type: 'circle',
                                    xref: xAxisName,
                                    yref: yAxisName,
                                    x0: -airyRadiusUm,
                                    y0: -airyRadiusUm,
                                    x1: airyRadiusUm,
                                    y1: airyRadiusUm,
                                    line: { color: '#111827', width: 1 },
                                    fillcolor: 'rgba(0,0,0,0)',
                                });
                            }

                            for (const g of groups) {
                                const wlLabel = String(g?.label || 'Wavelength');
                                const pts = Array.isArray(g?.points) ? g.points : [];
                                traces.push({
                                    x: pts.map((p) => Number(p?.xUm) || 0),
                                    y: pts.map((p) => Number(p?.yUm) || 0),
                                    xaxis: xAxisName,
                                    yaxis: yAxisName,
                                    type: 'scattergl',
                                    mode: 'markers',
                                    name: wlLabel,
                                    legendgroup: wlLabel,
                                    showlegend: false,
                                    marker: {
                                        size: 6,
                                        color: String(g?.color || '#2563eb'),
                                        opacity: 0.85,
                                        symbol: 'circle',
                                        line: {
                                            width: 0.8,
                                            color: '#333333',
                                        },
                                    },
                                    hovertemplate: 'x=%{x:.2f}µm<br>y=%{y:.2f}µm<extra></extra>',
                                });
                                if (!legendColorByLabel.has(wlLabel)) {
                                    legendColorByLabel.set(wlLabel, String(g?.color || '#2563eb'));
                                }
                            }
                        }

                        for (const [wlLabel, color] of legendColorByLabel.entries()) {
                            if (legendAdded.has(wlLabel)) continue;
                            legendAdded.add(wlLabel);
                            traces.push({
                                x: [null],
                                y: [null],
                                type: 'scatter',
                                mode: 'markers',
                                name: wlLabel,
                                legendgroup: wlLabel,
                                showlegend: true,
                                marker: {
                                    size: 8,
                                    color: color,
                                    symbol: 'circle',
                                },
                                hoverinfo: 'skip',
                            });
                        }

                        setProgress(85, 'Rendering Spot Diagram...');
                        const plotStart = (typeof performance !== 'undefined' && typeof performance.now === 'function') ? performance.now() : Date.now();
                        await window.Plotly.newPlot(popupContainer, traces, layout, { responsive: true, displaylogo: false });
                        const plotEnd = (typeof performance !== 'undefined' && typeof performance.now === 'function') ? performance.now() : Date.now();
                        try {
                            if (progressWrapper) progressWrapper.style.display = 'none';
                        } catch (_) {}
                        return;
                    } catch (nativeErr) {
                        setPopupRunDebug('Native spot failed; fallback to Rust-WASM: ' + String(nativeErr && nativeErr.message ? nativeErr.message : nativeErr));
                        setPopupStats('Native stats: unavailable (fallback to Rust-WASM)');
                    }
                }

                if (canUseRustSpotDiagram) {
                    setProgress(25, 'Computing Spot Diagram (Rust)...');
                    const result = await opener.runDesktopAnalysisComputeForPopup({
                        kind: 'spot-diagram',
                        surfaceIndex: popupSurface && popupSurface.value !== '' ? parseInt(popupSurface.value, 10) : undefined,
                        rayCount: popupRay && popupRay.value !== '' ? parseInt(popupRay.value, 10) : undefined,
                        ringCount: popupRing && popupRing.value !== '' ? parseInt(popupRing.value, 10) : undefined,
                        pattern: popupPattern ? String(popupPattern.value || 'annular') : 'annular',
                    });
                    const series = Array.isArray(result?.spotDiagramSeries) ? result.spotDiagramSeries : [];
                    if (!series.length) {
                        throw new Error('Rust Spot Diagram result is empty');
                    }
                    if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                        throw new Error('Plotly is not available in Spot Diagram popup');
                    }

                    const traces = [];
                    for (const s of series) {
                        const points = Array.isArray(s?.points) ? s.points : [];
                        traces.push({
                            x: points.map((p) => Number(p?.xUm) || 0),
                            y: points.map((p) => Number(p?.yUm) || 0),
                            type: 'scattergl',
                            mode: 'markers',
                            name: String(s?.label || 'Series'),
                            marker: {
                                size: 3,
                                color: String(s?.color || '#2563eb'),
                                opacity: 0.6,
                            },
                            hovertemplate: 'x=%{x:.2f}µm<br>y=%{y:.2f}µm<extra></extra>',
                        });
                    }

                    setProgress(85, 'Rendering Spot Diagram...');
                    await window.Plotly.newPlot(popupContainer, traces, {
                        margin: { l: 52, r: 20, t: 20, b: 45 },
                        xaxis: { title: 'X (µm)', zeroline: true },
                        yaxis: { title: 'Y (µm)', zeroline: true, scaleanchor: 'x', scaleratio: 1 },
                        showlegend: true,
                        paper_bgcolor: '#ffffff',
                        plot_bgcolor: '#ffffff',
                    }, { responsive: true, displaylogo: false });
                    try {
                        if (progressWrapper) progressWrapper.style.display = 'none';
                    } catch (_) {}
                    return;
                }

                if (typeof opener.showSpotDiagram !== 'function') {
                    throw new Error('showSpotDiagram is not available on opener');
                }

                setProgress(25, 'Computing Spot Diagram (Rust-WASM raytrace)...');
                await opener.showSpotDiagram({
                    containerElement: popupContainer,
                    surfaceIndex: popupSurface && popupSurface.value !== '' ? parseInt(popupSurface.value, 10) : undefined,
                    surfaceRowIndex: popupSurfaceRowIndex,
                    surfaceRowId: popupSurfaceRowId,
                    surfaceRowSig: popupSurfaceRowSig,
                    surfaceIsImage: popupSurfaceIsImage,
                    rayCount: popupRay && popupRay.value !== '' ? parseInt(popupRay.value, 10) : undefined,
                    ringCount: popupRing && popupRing.value !== '' ? parseInt(popupRing.value, 10) : undefined,
                    pattern: popupPattern ? String(popupPattern.value || 'annular') : 'annular',
                    wavelengthMode: 'all',
                    forceRustWasmTrace: true,
                    requireRustWasmTrace: true,
                    onProgress: (evt) => {
                        try {
                            const p = Number(evt && evt.percent);
                            const msg = (evt && evt.message) || (evt && evt.phase) || 'Working...';
                            if (Number.isFinite(p)) setProgress(p, msg);
                            else setProgress(undefined, msg);
                        } catch (_) {}
                    },
                });
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            } catch (e) {
                if (popupContainer) popupContainer.textContent = String(e && e.message ? e.message : e);
                setProgress(100, 'Failed');
            }
        });

        function syncAll() {
            syncSurfaceOptionsFromOpener();
            syncInputsFromOpener();
        }

        try {
            w.__cooptSpotPopupSyncAll = syncAll;
        } catch (_) {}
        window.addEventListener('message', (ev) => {
            try {
                const data = ev && ev.data;
                if (!data || data.action !== 'coopt-spot-sync') return;
                syncAll();
            } catch (_) {}
        });

        window.addEventListener('focus', syncAll);
        setInterval(syncAll, 1000);
        syncAll();
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const s=document.getElementById("open-spherical-aberration-window-btn");s&&s.addEventListener("click",()=>{if(H.__sphericalAberrationPopup&&!H.__sphericalAberrationPopup.closed){try{H.__sphericalAberrationPopup.focus()}catch{}try{typeof H.__sphericalAberrationPopup.renderSphericalAberration=="function"&&H.__sphericalAberrationPopup.renderSphericalAberration()}catch{}return}const N=n("Spherical Aberration","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__sphericalAberrationPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Spherical Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 90px;
        }
        .note {
            padding: 10px 12px;
            color: #666;
            font-size: 12px;
            border-bottom: 1px solid #eee;
            background: #fff;
        }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: auto;
            background: white;
        }
        #popup-longitudinal-aberration-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-longitudinal-ray-count-input">Ray number:</label>
        <input type="number" id="popup-longitudinal-ray-count-input" value="100" min="1" max="1001" step="1" />
        <span class="note-inline" style="font-size:12px;color:#666;">(Always normalized by stop diameter)</span>
        <label for="popup-longitudinal-reference-focus-mode" style="margin-left:6px;">Reference focus:</label>
        <select id="popup-longitudinal-reference-focus-mode" style="font-size:12px;">
            <option value="primary-paraxial">Primary paraxial</option>
            <option value="current-paraxial" selected>Current paraxial</option>
            <option value="chief-ray">Chief ray</option>
        </select>
        <button id="popup-show-spherical-aberration-btn" type="button">Show</button>
    </div>
    <div id="popup-spherical-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-spherical-progress-text" style="margin-bottom: 6px;">Calculating spherical aberration...</div>
        <progress id="popup-spherical-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-longitudinal-aberration-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerRay = getOpenerEl('longitudinal-ray-count-input');
            const popupRay = document.getElementById('popup-longitudinal-ray-count-input');
            if (openerRay && popupRay) {
                popupRay.value = openerRay.value;
            }
            const openerMode = getOpenerEl('longitudinal-reference-focus-mode');
            const popupMode = document.getElementById('popup-longitudinal-reference-focus-mode');
            if (openerMode && popupMode && openerMode.value) {
                popupMode.value = openerMode.value;
            }
        }

        window['renderSphericalAberration'] = async () => {
            const progressWrapper = document.getElementById('popup-spherical-progress-wrapper');
            const progressBarEl = document.getElementById('popup-spherical-progressbar');
            const progressTextEl = document.getElementById('popup-spherical-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl) {
                        const pct = Number.isFinite(value) ? (String(Math.round(Math.max(0, Math.min(100, Number(value))))) + '%') : '';
                        const msg = (typeof text === 'string' && text.trim().length > 0) ? text : 'Working...';
                        progressTextEl.textContent = pct ? (msg + ' (' + pct + ')') : msg;
                    }
                } catch (_) {}
            };

            const popupRay = document.getElementById('popup-longitudinal-ray-count-input');
            const rayCount = popupRay ? parseInt(popupRay.value, 10) : 51;
            const openerRay = getOpenerEl('longitudinal-ray-count-input');
            if (openerRay && Number.isFinite(rayCount)) {
                openerRay.value = String(rayCount);
            }
            const popupMode = document.getElementById('popup-longitudinal-reference-focus-mode');
            const referenceFocusMode = popupMode && popupMode.value ? String(popupMode.value) : 'current-paraxial';
            const openerMode = getOpenerEl('longitudinal-reference-focus-mode');
            if (openerMode && referenceFocusMode) {
                openerMode.value = referenceFocusMode;
            }

            const opener = window.opener;
            const shouldUseDesktopRust = (() => {
                try {
                    if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                        return !!window['shouldUseDesktopRustAnalysis']();
                    }
                    if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                        return !!opener.shouldUseDesktopRustAnalysis();
                    }
                        return true;
                } catch (_) {
                    return false;
                }
            })();
            const canUseDesktopRust = shouldUseDesktopRust && !!(
                opener
                && typeof opener.runDesktopNativeSphericalAberrationForPopup === 'function'
            );
            try {
                console.log('📊 [SA TS-Rust] mode', {
                    canUseDesktopRust,
                    hasNativeRunner: !!(opener && typeof opener.runDesktopNativeSphericalAberrationForPopup === 'function'),
                    referenceFocusMode,
                    rayCount,
                });
            } catch (_) {}

            const onProgress = (evt) => {
                try {
                    const p = Number(evt?.percent);
                    const msg = evt?.message || evt?.phase || 'Working...';
                    if (Number.isFinite(p)) setProgress(p, msg);
                    else setProgress(undefined, msg);
                } catch (_) {}
            };

            const containerEl = document.getElementById('popup-longitudinal-aberration-container');
            if (containerEl) containerEl.innerHTML = '';

            try {
                setProgress(0, 'Starting...');
                if (!opener || typeof opener.showLongitudinalAberrationDiagram !== 'function') {
                    throw new Error('showLongitudinalAberrationDiagram is not available on opener');
                }

                if (canUseDesktopRust) {
                    setProgress(25, 'Computing spherical aberration (Rust)...');
                    const rustResult = await opener.runDesktopNativeSphericalAberrationForPopup({
                        rayCount: Number.isFinite(rayCount) ? rayCount : 51,
                        referenceFocusMode: referenceFocusMode,
                        wavelengthMode: 'all',
                    });
                    setProgress(80, 'Rendering...');
                    await opener.showLongitudinalAberrationDiagram({
                        containerElement: containerEl,
                        onProgress,
                        precomputedAberrationData: rustResult,
                    });
                } else {
                    // Web fallback: run the existing JS/WASM spherical aberration path.
                    setProgress(25, 'Computing spherical aberration (Web)...');
                    await opener.showLongitudinalAberrationDiagram({
                        containerElement: containerEl,
                        onProgress,
                        rayCount: Number.isFinite(rayCount) ? rayCount : 51,
                        referenceFocusMode,
                    });
                }

                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
                return;
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate spherical aberration diagram. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-spherical-aberration-btn').addEventListener('click', () => {
            window.renderSphericalAberration();
        });

        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderSphericalAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const a=document.getElementById("open-astigmatism-window-btn");a&&a.addEventListener("click",()=>{if(H.__astigmatismPopup&&!H.__astigmatismPopup.closed){try{H.__astigmatismPopup.focus()}catch{}try{typeof H.__astigmatismPopup.renderAstigmatism=="function"&&H.__astigmatismPopup.renderAstigmatism()}catch{}return}const N=n("Astigmatism","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__astigmatismPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Astigmatism</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-astigmatic-field-curves-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-astigmatism-chief-ray" style="font-size:12px;color:#333;white-space:nowrap;">Chief ray:</label>
        <select id="popup-astigmatism-chief-ray" style="padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;">
            <option value="stop-center">Stop center</option>
            <option value="beam-midpoint">Beam midpoint</option>
            <option value="beam-centroid">Beam centroid</option>
        </select>
        <label for="popup-astigmatism-beam" style="font-size:12px;color:#333;white-space:nowrap;">Beam:</label>
        <select id="popup-astigmatism-beam" style="padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;">
            <option value="cross">Cross</option>
            <option value="grid">Grid</option>
            <option value="annular" selected>Annular</option>
        </select>
        <label for="popup-astigmatism-ray-count" style="font-size:12px;color:#333;white-space:nowrap;">Rays:</label>
        <input id="popup-astigmatism-ray-count" type="number" min="9" max="2001" step="1" value="30" style="width:72px;padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;" />
        <label id="popup-astigmatism-ring-label" for="popup-astigmatism-ring-count" style="font-size:12px;color:#333;white-space:nowrap;">Rings:</label>
        <input id="popup-astigmatism-ring-count" type="number" min="1" max="64" step="1" value="32" style="width:72px;padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;" />
        <button id="popup-show-astigmatism-btn" type="button">Show</button>
    </div>
    <div id="popup-astigmatism-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-astigmatism-progress-text" style="margin-bottom: 6px;">Calculating...</div>
        <progress id="popup-astigmatism-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-astigmatic-field-curves-container"></div>
    </div>

    <script>
        window['renderAstigmatism'] = async () => {
            const containerEl = document.getElementById('popup-astigmatic-field-curves-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-astigmatism-progress-wrapper');
            const progressBarEl = document.getElementById('popup-astigmatism-progressbar');
            const progressTextEl = document.getElementById('popup-astigmatism-progress-text');
            const chiefRaySelect = document.getElementById('popup-astigmatism-chief-ray');
            const beamSelect = document.getElementById('popup-astigmatism-beam');
            const rayCountInput = document.getElementById('popup-astigmatism-ray-count');
            const ringCountInput = document.getElementById('popup-astigmatism-ring-count');
            const ringCountLabel = document.getElementById('popup-astigmatism-ring-label');
            const chiefRayDefinition = (chiefRaySelect && chiefRaySelect.value) ? chiefRaySelect.value : 'stop-center';
            const beamPattern = (() => {
                const v = String((beamSelect && beamSelect.value) ? beamSelect.value : 'annular').trim().toLowerCase();
                return (v === 'cross' || v === 'grid' || v === 'annular') ? v : 'annular';
            })();
            const openerRay = (window.opener && window.opener.document)
                ? window.opener.document.getElementById('ray-count-input')
                : null;
            const openerRing = (window.opener && window.opener.document)
                ? window.opener.document.getElementById('ring-count-select')
                : null;
            const initialRayCount = (() => {
                const n = Number(openerRay && openerRay.value);
                return Number.isFinite(n) ? Math.max(9, Math.min(2001, Math.round(n))) : 30;
            })();
            const initialRingCount = (() => {
                const n = Number(openerRing && openerRing.value);
                return Number.isFinite(n) ? Math.max(1, Math.min(64, Math.round(n))) : 32;
            })();
            if (rayCountInput && !rayCountInput.value) rayCountInput.value = String(initialRayCount);
            if (ringCountInput && !ringCountInput.value) ringCountInput.value = String(initialRingCount);

            const updateRingVisibility = () => {
                const isAnnular = beamSelect && String(beamSelect.value || '').toLowerCase() === 'annular';
                if (ringCountLabel) ringCountLabel.style.display = isAnnular ? '' : 'none';
                if (ringCountInput) ringCountInput.style.display = isAnnular ? '' : 'none';
            };
            updateRingVisibility();
            if (beamSelect) beamSelect.addEventListener('change', updateRingVisibility);

            const rayCount = (() => {
                const fromInput = Number(rayCountInput && rayCountInput.value);
                if (Number.isFinite(fromInput)) return Math.max(9, Math.min(2001, Math.round(fromInput)));
                return initialRayCount;
            })();
            const ringCount = (() => {
                const fromInput = Number(ringCountInput && ringCountInput.value);
                if (Number.isFinite(fromInput)) return Math.max(1, Math.min(64, Math.round(fromInput)));
                return initialRingCount;
            })();

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            try {
                if (!window.opener || typeof window.opener.showAstigmatismDiagram !== 'function') {
                    throw new Error('showAstigmatismDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                const onProgress = (evt) => {
                    try {
                        try { console.log('[ASTIG_POPUP][progress]', evt); } catch (_) {}
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                await window.opener.showAstigmatismDiagram({
                    containerElement: containerEl,
                    rayCount,
                    ringCount,
                    pattern: beamPattern,
                    requireRustWasm: true,
                    forceRustWasmTrace: true,
                    requireRustWasmTrace: true,
                    onProgress,
                    chiefRayDefinition,
                    logChiefRayDefinition: true,
                    useActiveConfigSnapshot: false
                });
                try { console.log('[ASTIG_POPUP][done] showAstigmatismDiagram resolved'); } catch (_) {}
                setProgress(100, '');
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate astigmatism diagram. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-astigmatism-btn').addEventListener('click', () => {
            window.renderAstigmatism();
        });

        const popupBeamSelect = document.getElementById('popup-astigmatism-beam');
        const popupRingLabel = document.getElementById('popup-astigmatism-ring-label');
        const popupRingInput = document.getElementById('popup-astigmatism-ring-count');
        const syncAstigRingVisibility = () => {
            const isAnnular = popupBeamSelect && String(popupBeamSelect.value || '').toLowerCase() === 'annular';
            if (popupRingLabel) popupRingLabel.style.display = isAnnular ? '' : 'none';
            if (popupRingInput) popupRingInput.style.display = isAnnular ? '' : 'none';
        };
        syncAstigRingVisibility();
        if (popupBeamSelect) popupBeamSelect.addEventListener('change', syncAstigRingVisibility);

        // Do not auto-render on open; user triggers calculation via "Show".
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const c=document.getElementById("open-distortion-window-btn");c&&c.addEventListener("click",()=>{if(!st()){if(H.__distortionPopup&&!H.__distortionPopup.closed){try{H.__distortionPopup.focus()}catch{}return}try{const I=new URL(window.location.href);I.searchParams.set("coopt_analysis_window","1"),I.searchParams.set("coopt_analysis","distortion");const b=window.open(I.toString(),"Distortion","width=800,height=600");if(!b){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__distortionPopup=b;return}catch{}}if(H.__distortionPopup&&!H.__distortionPopup.closed){try{H.__distortionPopup.focus()}catch{}try{typeof H.__distortionPopup.renderDistortion=="function"&&H.__distortionPopup.renderDistortion()}catch{}return}const N=n("Distortion","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__distortionPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Distortion</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: block;
        }
        .plot-area { width: 100%; height: 100%; min-height: 0; }
        #popup-distortion-percent { height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <button id="popup-show-distortion-btn" type="button">Show distortion diagram</button>
    </div>
    <div id="popup-distortion-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-distortion-progress-text" style="margin-bottom: 6px;">Calculating distortion...</div>
        <progress id="popup-distortion-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-distortion-percent-area" class="plot-area"><div id="popup-distortion-percent"></div></div>
    </div>

    <script>
        function getWavelengthColorLocal(wavelength) {
            if (wavelength < 0.45) return '#8B00FF';
            if (wavelength < 0.495) return '#0000FF';
            if (wavelength < 0.57) return '#00FF00';
            if (wavelength < 0.59) return '#9ACD32';
            if (wavelength < 0.62) return '#FF8800';
            return '#FF0000';
        }

        function plotDistortionLocal(dataList, targetEl) {
            if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                throw new Error('Plotly is not available in distortion popup');
            }
            const list = Array.isArray(dataList) ? dataList.filter(Boolean) : [];
            if (!list.length) throw new Error('No distortion data to plot');

            const traces = list.map((data) => {
                const wavelength = Number(data?.meta?.wavelength || 0.5876);
                const wavelengthNm = (wavelength * 1000).toFixed(1);
                const color = getWavelengthColorLocal(wavelength);
                const isHeightMode = !!data?.meta?.heightMode;
                const label = isHeightMode ? 'h' : 'θ';
                return {
                    x: Array.isArray(data?.distortionPercent) ? data.distortionPercent : [],
                    y: Array.isArray(data?.fieldValues) ? data.fieldValues : [],
                    name: 'DIST ' + wavelengthNm + 'nm (' + label + ')',
                    mode: 'lines',
                    line: { color, width: 2 },
                    type: 'scatter',
                };
            }).filter((trace) => Array.isArray(trace.x) && trace.x.length > 0 && Array.isArray(trace.y) && trace.y.length > 0);

            if (!traces.length) throw new Error('No plottable distortion traces');

            const maxFieldValue = Math.max(...list.map((data) => Array.isArray(data?.fieldValues) ? Math.max(...data.fieldValues) : 0));
            const minFieldValue = Math.min(...list.map((data) => Array.isArray(data?.fieldValues) ? Math.min(...data.fieldValues) : 0));
            const heightMode = list.some((d) => !!d?.meta?.heightMode);

            return window.Plotly.newPlot(targetEl, traces, {
                title: heightMode ? 'Distortion vs Object Height' : 'Distortion vs Object Angle',
                xaxis: { title: 'Distortion (%)', range: [-5, 5], dtick: 1 },
                yaxis: { title: heightMode ? 'Object Height (mm)' : 'Object Angle θ (deg)' },
                showlegend: true,
                legend: { orientation: 'v', x: 1.02, y: 1 },
                autosize: true,
                shapes: [{ type: 'line', x0: 0, x1: 0, y0: minFieldValue, y1: maxFieldValue, line: { color: 'black', width: 1, dash: 'dot' } }],
                margin: { l: 60, r: 120, t: 50, b: 50 },
            }, { responsive: true, displayModeBar: true, displaylogo: false });
        }

        function resizePlots() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const a = document.getElementById('popup-distortion-percent');
                if (a) plotly.Plots.resize(a);
            } catch (_) {}
        }

        window['renderDistortion'] = async () => {
            const percentEl = document.getElementById('popup-distortion-percent');
            if (percentEl) percentEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-distortion-progress-wrapper');
            const progressBarEl = document.getElementById('popup-distortion-progressbar');
            const progressTextEl = document.getElementById('popup-distortion-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };
            let renderSucceeded = false;

            try {
                if (!window.opener || typeof window.opener.runPortableDistortionDataForPopup !== 'function') {
                    throw new Error('runPortableDistortionDataForPopup is not available on opener');
                }
                setProgress(0, 'Starting...');
                await new Promise((resolve) => setTimeout(resolve, 0));
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                const result = await window.opener.runPortableDistortionDataForPopup({ onProgress });
                const allData = Array.isArray(result?.allData) ? result.allData : [];
                await plotDistortionLocal(allData, percentEl);
                renderSucceeded = true;
                setTimeout(resizePlots, 0);
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (percentEl) {
                    percentEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate distortion diagram. Check console.</div>';
                }
            } finally {
                if (renderSucceeded && progressWrapper) {
                    setTimeout(() => {
                        try { progressWrapper.style.display = 'none'; } catch (_) {}
                    }, 250);
                }
            }
        };

        document.getElementById('popup-show-distortion-btn').addEventListener('click', () => window.renderDistortion());
        window.addEventListener('resize', resizePlots);

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderDistortion(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const i=document.getElementById("open-distortion-grid-window-btn");i&&i.addEventListener("click",()=>{if(!st()){if(H.__distortionGridPopup&&!H.__distortionGridPopup.closed){try{H.__distortionGridPopup.focus()}catch{}return}try{const I=new URL(window.location.href);I.searchParams.set("coopt_analysis_window","1"),I.searchParams.set("coopt_analysis","distortion-grid");const b=window.open(I.toString(),"Distortion Grid","width=800,height=600");if(!b){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__distortionGridPopup=b;return}catch{}}if(H.__distortionGridPopup&&!H.__distortionGridPopup.closed){try{H.__distortionGridPopup.focus()}catch{}return}const N=n("Distortion Grid","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__distortionGridPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Distortion Grid</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-distortion-grid { width: 100%; height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-distortion-grid-size">Grid Size:</label>
        <select id="popup-distortion-grid-size">
            <option value="10">10×10</option>
            <option value="15">15×15</option>
            <option value="20" selected>20×20</option>
            <option value="25">25×25</option>
            <option value="30">30×30</option>
            <option value="35">35×35</option>
            <option value="40">40×40</option>
            <option value="45">45×45</option>
            <option value="50">50×50</option>
        </select>
        <button id="popup-show-distortion-grid-btn" type="button">Show distortion grid</button>
    </div>
    <div id="popup-distortion-grid-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-distortion-grid-progress-text" style="margin-bottom: 6px;">Calculating grid distortion...</div>
        <progress id="popup-distortion-grid-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-distortion-grid"></div>
    </div>

    <script>
        function getWavelengthColorLocal(wavelength) {
            if (wavelength < 0.45) return '#8B00FF';
            if (wavelength < 0.495) return '#0000FF';
            if (wavelength < 0.57) return '#00FF00';
            if (wavelength < 0.59) return '#9ACD32';
            if (wavelength < 0.62) return '#FF8800';
            return '#FF0000';
        }

        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerGrid = getOpenerEl('grid-size-select');
            const popupGrid = document.getElementById('popup-distortion-grid-size');
            if (openerGrid && popupGrid) {
                popupGrid.value = openerGrid.value;
            }
        }

        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const grid = document.getElementById('popup-distortion-grid');
                if (grid) plotly.Plots.resize(grid);
            } catch (_) {}
        }

        async function plotGridLocal(data, targetEl, onProgress) {
            if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                throw new Error('Plotly is not available in distortion grid popup');
            }
            if (!data || !data.idealGrid || !data.realGrid) {
                throw new Error('Invalid grid distortion data');
            }

            const progress = (typeof onProgress === 'function') ? onProgress : null;
            const reportProgress = (percent, message) => {
                try { progress && progress({ percent, message }); } catch (_) {}
            };

            const idealGrid = data.idealGrid;
            const realGrid = data.realGrid;
            const gridSize = Number(data.gridSize) || 20;
            const meta = data.meta || {};
            const traces = [];

            for (let i = 0; i < gridSize; i++) {
                const startIdx = i * gridSize;
                const endIdx = startIdx + gridSize - 1;
                traces.push({
                    x: idealGrid.x.slice(startIdx, endIdx + 1),
                    y: idealGrid.y.slice(startIdx, endIdx + 1),
                    mode: 'lines',
                    line: { color: '#888888', width: 1 },
                    showlegend: i === 0,
                    name: i === 0 ? 'Ideal Grid' : undefined,
                    hoverinfo: 'skip',
                    type: 'scatter',
                });
                if ((i + 1) % 10 === 0) await new Promise((resolve) => setTimeout(resolve, 0));
            }

            for (let j = 0; j < gridSize; j++) {
                const xLine = [];
                const yLine = [];
                for (let i = 0; i < gridSize; i++) {
                    const idx = i * gridSize + j;
                    xLine.push(idealGrid.x[idx]);
                    yLine.push(idealGrid.y[idx]);
                }
                traces.push({
                    x: xLine,
                    y: yLine,
                    mode: 'lines',
                    line: { color: '#888888', width: 1 },
                    showlegend: false,
                    hoverinfo: 'skip',
                    type: 'scatter',
                });
                if ((j + 1) % 10 === 0) await new Promise((resolve) => setTimeout(resolve, 0));
            }

            const realGridColor = getWavelengthColorLocal(Number(meta.wavelength || 0.5876));

            for (let i = 0; i < gridSize; i++) {
                const rowX = [];
                const rowY = [];
                for (let j = 0; j < gridSize; j++) {
                    const idx = i * gridSize + j;
                    const x = realGrid.x[idx];
                    const y = realGrid.y[idx];
                    rowX.push((x !== null && x !== undefined && isFinite(x)) ? x : null);
                    rowY.push((y !== null && y !== undefined && isFinite(y)) ? y : null);
                }
                traces.push({
                    x: rowX,
                    y: rowY,
                    mode: 'lines',
                    line: { color: realGridColor, width: 1.2 },
                    showlegend: i === 0,
                    name: i === 0 ? 'Real Grid (λ=' + Number(meta.wavelength || 0.5876).toFixed(4) + ' μm)' : undefined,
                    hoverinfo: 'skip',
                    connectgaps: false,
                    type: 'scatter',
                });
            }

            for (let j = 0; j < gridSize; j++) {
                const colX = [];
                const colY = [];
                for (let i = 0; i < gridSize; i++) {
                    const idx = i * gridSize + j;
                    const x = realGrid.x[idx];
                    const y = realGrid.y[idx];
                    colX.push((x !== null && x !== undefined && isFinite(x)) ? x : null);
                    colY.push((y !== null && y !== undefined && isFinite(y)) ? y : null);
                }
                traces.push({
                    x: colX,
                    y: colY,
                    mode: 'lines',
                    line: { color: realGridColor, width: 1.2 },
                    showlegend: false,
                    hoverinfo: 'skip',
                    connectgaps: false,
                    type: 'scatter',
                });
            }

            const realX = [];
            const realY = [];
            const totalPoints = Math.max(1, Array.isArray(realGrid.x) ? realGrid.x.length : 0);
            for (let i = 0; i < totalPoints; i++) {
                const x = realGrid.x[i];
                const y = realGrid.y[i];
                const idealX = idealGrid.x[i];
                const idealY = idealGrid.y[i];
                if (x !== null && y !== null && x !== undefined && y !== undefined && isFinite(x) && isFinite(y) && isFinite(idealX) && isFinite(idealY)) {
                    realX.push(x);
                    realY.push(y);
                }
                reportProgress(((i + 1) / totalPoints) * 100, 'Grid distortion: ' + (i + 1) + '/' + totalPoints);
                if ((i + 1) % 50 === 0) await new Promise((resolve) => setTimeout(resolve, 0));
            }

            traces.push({
                x: realX,
                y: realY,
                mode: 'markers',
                marker: {
                    color: realGridColor,
                    size: 6,
                    symbol: 'circle',
                    opacity: 0.9,
                    line: {
                        width: 0.8,
                        color: '#333333'
                    },
                },
                showlegend: false,
                name: 'Real Positions (λ=' + Number(meta.wavelength || 0.5876).toFixed(4) + ' μm)',
                hovertemplate: 'Real: (%{x:.3f}, %{y:.3f}) mm<extra></extra>',
                type: 'scatter',
            });

            const maxAbsIdealX = idealGrid.x.reduce((m, v) => (isFinite(v) ? Math.max(m, Math.abs(v)) : m), 0);
            const maxAbsIdealY = idealGrid.y.reduce((m, v) => (isFinite(v) ? Math.max(m, Math.abs(v)) : m), 0);
            const equalRangeHalf = Math.max(maxAbsIdealX, maxAbsIdealY, 1e-9);

            return window.Plotly.newPlot(targetEl, traces, {
                title: 'Grid Distortion (' + gridSize + '×' + gridSize + ', λ=' + Number(meta.wavelength || 0.5876).toFixed(4) + ' μm)',
                xaxis: { title: 'Image Height X (mm)', scaleanchor: 'y', scaleratio: 1, zeroline: false, range: [-equalRangeHalf, equalRangeHalf] },
                yaxis: { title: 'Image Height Y (mm)', zeroline: false, range: [-equalRangeHalf, equalRangeHalf] },
                hovermode: 'closest',
                showlegend: true,
                legend: { x: 1.02, y: 1 },
                autosize: true,
                margin: { l: 60, r: 120, t: 50, b: 50 },
            }, { responsive: true, displayModeBar: true, displaylogo: false });
        }

        window['renderDistortionGrid'] = async () => {
            if (window.__distortionGridRenderInFlight) return;
            window.__distortionGridRenderInFlight = true;
            const gridEl = document.getElementById('popup-distortion-grid');
            const gridBtn = document.getElementById('popup-show-distortion-grid-btn');
            if (gridBtn) gridBtn.disabled = true;
            if (gridEl) gridEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-distortion-grid-progress-wrapper');
            const progressBarEl = document.getElementById('popup-distortion-grid-progressbar');
            const progressTextEl = document.getElementById('popup-distortion-grid-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const gridSizeEl = document.getElementById('popup-distortion-grid-size');
            const gridSize = gridSizeEl ? parseInt(gridSizeEl.value, 10) : 20;
            const openerGrid = getOpenerEl('grid-size-select');
            if (openerGrid && Number.isFinite(gridSize)) openerGrid.value = String(gridSize);
            let renderSucceeded = false;

            try {
                if (!window.opener || typeof window.opener.runPortableGridDistortionForPopup !== 'function') {
                    throw new Error('runPortableGridDistortionForPopup is not available on opener');
                }
                setProgress(0, 'Starting...');
                await new Promise((resolve) => setTimeout(resolve, 0));
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                const data = await window.opener.runPortableGridDistortionForPopup({ gridSize: Number.isFinite(gridSize) ? gridSize : 20, onProgress });
                await plotGridLocal(data, gridEl, onProgress);
                renderSucceeded = true;
                setTimeout(resizePlot, 0);
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (gridEl) {
                    gridEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate grid distortion. Check console.</div>';
                }
            } finally {
                window.__distortionGridRenderInFlight = false;
                if (gridBtn) gridBtn.disabled = false;
                if (renderSucceeded && progressWrapper) {
                    setTimeout(() => {
                        try { progressWrapper.style.display = 'none'; } catch (_) {}
                    }, 250);
                }
            }
        };

        document.getElementById('popup-show-distortion-grid-btn').addEventListener('click', () => window.renderDistortionGrid());
        window.addEventListener('resize', resizePlot);
        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const l=document.getElementById("open-magnification-chromatic-aberration-window-btn");l&&l.addEventListener("click",()=>{if(H.__magnificationChromaticAberrationPopup&&!H.__magnificationChromaticAberrationPopup.closed){try{H.__magnificationChromaticAberrationPopup.focus()}catch{}try{typeof H.__magnificationChromaticAberrationPopup.renderMagnificationChromaticAberration=="function"&&H.__magnificationChromaticAberrationPopup.renderMagnificationChromaticAberration()}catch{}return}const N=n("Lateral Chromatic Aberration","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__magnificationChromaticAberrationPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Lateral Chromatic Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            width: 90px;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-mca-container { flex: 1 1 auto; min-height: 0; }
        .note { padding: 6px 12px; font-size: 12px; color: #666; background: #fff; border-bottom: 1px solid #eee; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-mca-xmin">Lateral displacement:</label>
        <input type="number" id="popup-mca-xmin" value="-0.05" step="0.01" />
        <span style="font-size:12px;color:#333;">to</span>
        <input type="number" id="popup-mca-xmax" value="0.05" step="0.01" />
        <span style="font-size:12px;color:#666;">(mm)</span>
        <label for="popup-mca-points" style="margin-left:6px;">Points:</label>
        <input type="number" id="popup-mca-points" value="21" min="2" max="201" step="1" />
        <label for="popup-mca-chief-ray" style="margin-left:6px;">Chief ray:</label>
        <select id="popup-mca-chief-ray" style="padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;">
            <option value="stop-center">Stop center</option>
            <option value="beam-midpoint">Beam midpoint</option>
            <option value="beam-centroid">Beam centroid</option>
        </select>
        <button id="popup-show-mca-btn" type="button">Show lateral chromatic aberration</button>
    </div>
    <div id="popup-mca-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-mca-progress-text" style="margin-bottom: 6px;">Calculating lateral chromatic aberration...</div>
        <progress id="popup-mca-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-mca-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerMin = getOpenerEl('mca-xmin-input');
            const openerMax = getOpenerEl('mca-xmax-input');
            const openerPoints = getOpenerEl('mca-point-count-input');
            const popupMin = document.getElementById('popup-mca-xmin');
            const popupMax = document.getElementById('popup-mca-xmax');
            const popupPoints = document.getElementById('popup-mca-points');
            if (openerMin && popupMin && openerMin.value !== '') popupMin.value = openerMin.value;
            if (openerMax && popupMax && openerMax.value !== '') popupMax.value = openerMax.value;
            if (openerPoints && popupPoints && openerPoints.value !== '') popupPoints.value = openerPoints.value;
        }

        window['renderMagnificationChromaticAberration'] = async () => {
            const containerEl = document.getElementById('popup-mca-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-mca-progress-wrapper');
            const progressBarEl = document.getElementById('popup-mca-progressbar');
            const progressTextEl = document.getElementById('popup-mca-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const xMinEl = document.getElementById('popup-mca-xmin');
            const xMaxEl = document.getElementById('popup-mca-xmax');
            const pointEl = document.getElementById('popup-mca-points');
            const chiefRayEl = document.getElementById('popup-mca-chief-ray');
            const xMin = xMinEl ? parseFloat(xMinEl.value) : -0.5;
            const xMax = xMaxEl ? parseFloat(xMaxEl.value) : 0.5;
            const pointCount = pointEl ? parseInt(pointEl.value, 10) : 11;
            const chiefRayDefinition = (chiefRayEl && chiefRayEl.value) ? chiefRayEl.value : 'stop-center';

            try {
                if (!window.opener || typeof window.opener.showMagnificationChromaticAberrationDiagram !== 'function') {
                    throw new Error('showMagnificationChromaticAberrationDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                await window.opener.showMagnificationChromaticAberrationDiagram({
                    containerElement: containerEl,
                    xMin,
                    xMax,
                    pointCount,
                    chiefRayDefinition,
                    onProgress
                });
                hideProgress();
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate magnification chromatic aberration. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-mca-btn').addEventListener('click', () => window.renderMagnificationChromaticAberration());
        window.addEventListener('focus', syncFromOpener);
        window.addEventListener('load', () => {
            syncFromOpener();
            try { window.renderMagnificationChromaticAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const f=document.getElementById("open-integrated-aberration-window-btn");f&&f.addEventListener("click",()=>{if(H.__integratedAberrationPopup&&!H.__integratedAberrationPopup.closed){try{H.__integratedAberrationPopup.focus()}catch{}try{typeof H.__integratedAberrationPopup.renderIntegratedAberration=="function"&&H.__integratedAberrationPopup.renderIntegratedAberration()}catch{}return}const N=n("Integrated Aberration","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__integratedAberrationPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Integrated Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-integrated-aberration-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div id="popup-integrated-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <progress id="popup-integrated-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-integrated-aberration-container"></div>
    </div>

    <script>
        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const el = document.getElementById('popup-integrated-aberration-container');
                if (el) plotly.Plots.resize(el);
            } catch (_) {}
        }

        window['renderIntegratedAberration'] = async () => {
            const containerEl = document.getElementById('popup-integrated-aberration-container');
            if (containerEl) containerEl.innerHTML = '';
            resizePlot();

            const progressWrapper = document.getElementById('popup-integrated-progress-wrapper');
            const progressBarEl = document.getElementById('popup-integrated-progressbar');
            const progressTextEl = document.getElementById('popup-integrated-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            try {
                if (!window.opener || typeof window.opener.showIntegratedAberrationDiagram !== 'function') {
                    throw new Error('showIntegratedAberrationDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                await window.opener.showIntegratedAberrationDiagram({
                    containerElement: containerEl,
                    onProgress,
                    useActiveConfigSnapshot: true
                });
                hideProgress();
                resizePlot();
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate integrated aberration diagram. Check console.</div>';
                }
            }
        };

        window.addEventListener('resize', resizePlot);

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderIntegratedAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const p=document.getElementById("open-opd-window-btn");p&&p.addEventListener("click",()=>{if(H.__opdPopup&&!H.__opdPopup.closed){try{H.__opdPopup.close()}catch{}H.__opdPopup=null}const N=n("Optical Path Difference","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__opdPopup=N;try{N.document.open()}catch{}N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Optical Path Difference</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-wavefront-container { flex: 1 1 auto; min-height: 0; }
        #popup-wavefront-container-stats { flex: 0 0 auto; padding: 8px 12px; font-size: 12px; color: #333; border-top: 1px solid #eee; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-wavefront-object-select">Object:</label>
        <select id="popup-wavefront-object-select"></select>
        <label for="popup-wavefront-plot-type-select">Plot type:</label>
        <select id="popup-wavefront-plot-type-select">
            <option value="surface">3D Surface</option>
            <option value="heatmap">Heatmap</option>
            <option value="multifield">Multi-field Comparison</option>
        </select>
        <label for="popup-wavefront-grid-size-select">Grid size:</label>
        <select id="popup-wavefront-grid-size-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-zernike-fit-checkbox" type="checkbox" />
            Zernike (calc)
        </label>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-opd-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <button id="popup-show-wavefront-btn" type="button">Show wavefront diagram</button>
        <button id="popup-stop-opd-btn" type="button" disabled>Stop</button>
    </div>
    <div id="popup-opd-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-opd-progress-text" style="margin-bottom: 6px;">Calculating OPD...</div>
        <progress id="popup-opd-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-wavefront-container"></div>
        <div id="popup-wavefront-container-stats"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncObjectOptionsFromOpener() {
            const openerSelect = getOpenerEl('wavefront-object-select');
            const popupSelect = document.getElementById('popup-wavefront-object-select');
            if (!popupSelect) return;

            const current = popupSelect.value;
            const nextOptions = [];

            // MTF-style: build from opener.getObjectRows() first.
            let opener = null;
            try { opener = window.opener || null; } catch (_) { opener = null; }
            let objects = [];
            if (opener && typeof opener.getObjectRows === 'function') {
                try { objects = opener.getObjectRows(opener.tableObject); } catch (_) { objects = []; }
            }
            if (Array.isArray(objects) && objects.length > 0) {
                const toFiniteNumber = (v) => {
                    const n = (typeof v === 'number') ? v : parseFloat(v);
                    return (Number.isFinite(n) ? n : NaN);
                };
                const pickNumber = (obj, keys, fallback) => {
                    for (let i = 0; i < keys.length; i++) {
                        const k = keys[i];
                        if (!k) continue;
                        const raw = obj ? obj[k] : undefined;
                        if (raw === undefined || raw === null || raw === '') continue;
                        const n = toFiniteNumber(raw);
                        if (Number.isFinite(n)) return n;
                    }
                    return fallback;
                };
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(obj.position ?? obj.object ?? obj.Object ?? obj.objectType ?? 'Point');
                    const x = (obj.x ?? obj.xHeightAngle ?? 0);
                    const y = (obj.y ?? obj.yHeightAngle ?? 0);
                    nextOptions.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }

            // Fallback: clone opener select.
            if (nextOptions.length === 0 && openerSelect && openerSelect.options) {
                Array.from(openerSelect.options).forEach(opt => {
                    nextOptions.push({ value: String(opt.value), label: String(opt.textContent ?? '') });
                });
            }
            // Last fallback: placeholder + schedule a retry (opener tables may not be ready yet).
            if (nextOptions.length === 0) {
                nextOptions.push({ value: '0', label: '1' });
                setTimeout(() => {
                    try { syncObjectOptionsFromOpener(); } catch (_) {}
                }, 250);
            }

            popupSelect.innerHTML = '';
            for (const opt of nextOptions) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                popupSelect.appendChild(o);
            }

            if (current && Array.from(popupSelect.options).some(o => o.value === current)) {
                popupSelect.value = current;
            } else if (openerSelect && openerSelect.value && Array.from(popupSelect.options).some(o => o.value === openerSelect.value)) {
                popupSelect.value = openerSelect.value;
            } else {
                popupSelect.value = popupSelect.options[0]?.value ?? '0';
            }

            try {
                if (typeof window.__opdStage === 'function') {
                    window.__opdStage('Object options synchronized', 'count=' + popupSelect.options.length + ', selected=' + String(popupSelect.value ?? 'n/a'));
                }
            } catch (_) {}
        }

        function syncInputsFromOpener() {
            const openerPlotType = getOpenerEl('wavefront-plot-type-select');
            const openerGrid = getOpenerEl('wavefront-grid-size-select');
            const openerRemovePtd = getOpenerEl('opd-remove-ptd-checkbox');
            const popupPlotType = document.getElementById('popup-wavefront-plot-type-select');
            const popupGrid = document.getElementById('popup-wavefront-grid-size-select');
            const popupRemovePtd = document.getElementById('popup-opd-remove-ptd-checkbox');
            if (openerPlotType && popupPlotType) popupPlotType.value = openerPlotType.value;
            if (openerGrid && popupGrid) popupGrid.value = openerGrid.value;
            if (popupRemovePtd) popupRemovePtd.checked = !!(openerRemovePtd && openerRemovePtd.checked);
        }

        function isIdealParaxialOnlySystem() {
            try {
                const opener = window.opener || null;
                const rows = (opener && typeof opener.getOpticalSystemRows === 'function')
                    ? opener.getOpticalSystemRows()
                    : [];
                if (!Array.isArray(rows) || rows.length === 0) return false;

                const normalize = (value) => String(value ?? '').trim().toLowerCase();
                const compact = (value) => normalize(value).replace(/[s_-]+/g, '');
                let hasIdealParaxial = false;

                for (const row of rows) {
                    if (!row || typeof row !== 'object') continue;

                    const objectType = compact(row['object type'] ?? row.objectType ?? row.object ?? '');
                    const surfType = compact(row.surfType ?? row['surf type'] ?? row.type ?? row.surfaceType ?? '');
                    const blockType = compact(row._blockType ?? row.blockType ?? '');
                    const kind = compact(row.kind ?? '');

                    const isIdealParaxial = (
                        blockType === 'paraxial'
                        || blockType === 'thinlens'
                        || surfType === 'thinlens'
                        || Number.isFinite(Number(row._thinLensFocalLengthX))
                        || Number.isFinite(Number(row._thinLensFocalLengthY))
                    );

                    if (isIdealParaxial) {
                        hasIdealParaxial = true;
                        continue;
                    }

                    const isPassiveRow = (
                        objectType === 'object'
                        || objectType === 'image'
                        || objectType === 'stop'
                        || surfType === 'stop'
                        || surfType === 'gap'
                        || surfType === 'airgap'
                        || blockType === 'gap'
                        || blockType === 'coordbreak'
                        || kind === 'gap'
                    );

                    if (isPassiveRow) continue;
                    return false;
                }

                return hasIdealParaxial;
            } catch (_) {
                return false;
            }
        }

        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const el = document.getElementById('popup-wavefront-container');
                if (el) plotly.Plots.resize(el);
            } catch (_) {}
        }

        function createStageTraceHelpers() {
            const clear = (_label = 'Idle') => {};
            const push = (_stage, _detail = '', _level = 'info') => {};
            const pushWarn = (_stage, _detail = '') => {};

            return { clear, push, pushWarn };
        }

        const opdStageTrace = createStageTraceHelpers();
        window.__opdStage = opdStageTrace.push;

        window['renderOPD'] = async () => {
            const containerEl = document.getElementById('popup-wavefront-container');
            if (containerEl) containerEl.innerHTML = '';

            try {
                opdStageTrace.clear('Preparing...');
                opdStageTrace.push('Render requested');
            } catch (_) {}

            const progressWrapper = document.getElementById('popup-opd-progress-wrapper');
            const progressBarEl = document.getElementById('popup-opd-progressbar');
            const progressTextEl = document.getElementById('popup-opd-progress-text');
            const PROGRESS_UI_UPDATE_INTERVAL_MS = 120;
            let lastProgressPercent = null;
            let lastProgressText = null;
            let progressVisible = false;
            let lastProgressUiUpdateAt = 0;
            let pendingProgressEvent = null;
            let progressFlushTimer = null;
            let lastComputingMilestone = 0;

            const setProgress = (value, text, force = false) => {
                try {
                    if (progressWrapper && !progressVisible) {
                        progressWrapper.style.display = 'block';
                        progressVisible = true;
                    }

                    if (progressBarEl && Number.isFinite(value)) {
                        const nextValue = Math.max(0, Math.min(100, value));
                        if (force || !Number.isFinite(lastProgressPercent) || Math.abs(nextValue - lastProgressPercent) >= 0.001) {
                            progressBarEl.value = nextValue;
                            lastProgressPercent = nextValue;
                        }
                    }

                    if (progressTextEl && typeof text === 'string') {
                        if (force || text !== lastProgressText) {
                            progressTextEl.textContent = text;
                            lastProgressText = text;
                        }
                    }
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                    progressVisible = false;
                } catch (_) {}
            };

            const flushPendingProgress = (force = false) => {
                if (progressFlushTimer) {
                    clearTimeout(progressFlushTimer);
                    progressFlushTimer = null;
                }
                const evt = pendingProgressEvent;
                if (!evt) return;
                pendingProgressEvent = null;
                const p = Number(evt?.percent);
                const msg = evt?.message || evt?.phase || 'Working...';
                if (Number.isFinite(p)) setProgress(p, msg, force);
                else setProgress(undefined, msg, force);
                lastProgressUiUpdateAt = Date.now();
            };

            const popupObject = document.getElementById('popup-wavefront-object-select');
            const popupPlotType = document.getElementById('popup-wavefront-plot-type-select');
            const popupGrid = document.getElementById('popup-wavefront-grid-size-select');
            const popupZernikeFit = document.getElementById('popup-zernike-fit-checkbox');
            const popupRemovePtd = document.getElementById('popup-opd-remove-ptd-checkbox');

            try {
                if (popupRemovePtd && isIdealParaxialOnlySystem()) {
                    opdStageTrace.push('Ideal Paraxial detected', 'Keeping the selected OPD mode so image-plane defocus remains visible.');
                }
            } catch (_) {}

            try {
                if (!popupObject) {
                    opdStageTrace.push('Object selector missing', 'popup-wavefront-object-select not found; fallback index=0');
                } else {
                    opdStageTrace.push('Object selector ready', 'options=' + String(popupObject.options?.length ?? 0));
                }
            } catch (_) {}

            const objectIndex = (() => {
                if (!popupObject) return 0;
                const v = parseInt(String(popupObject.value), 10);
                if (Number.isFinite(v)) return v;
                const idx = Number(popupObject.selectedIndex);
                return Number.isFinite(idx) && idx >= 0 ? idx : 0;
            })();
            const plotType = popupPlotType ? popupPlotType.value : 'surface';
            const gridSize = popupGrid ? parseInt(popupGrid.value, 10) : 256;
            const opdDisplayMode = (popupRemovePtd && popupRemovePtd.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';

            try {
                opdStageTrace.push('Input resolved', 'object=' + String(objectIndex) + ', plot=' + String(plotType) + ', grid=' + String(gridSize) + ', mode=' + String(opdDisplayMode));
            } catch (_) {}

            try {
                const optionCount = Number(popupObject?.options?.length ?? 0);
                const selectedText = popupObject?.selectedOptions?.[0]?.textContent || popupObject?.options?.[popupObject?.selectedIndex || 0]?.textContent || '';
                const inRange = optionCount <= 0 ? true : (objectIndex >= 0 && objectIndex < optionCount);
                if (!inRange) {
                    opdStageTrace.push('Object index out of range', 'index=' + String(objectIndex) + ', options=' + String(optionCount));
                } else {
                    opdStageTrace.push('Object resolved', 'index=' + String(objectIndex) + (selectedText ? (', label=' + String(selectedText)) : ''));
                }
            } catch (_) {}

            const openerObject = getOpenerEl('wavefront-object-select');
            const openerPlotType = getOpenerEl('wavefront-plot-type-select');
            const openerGrid = getOpenerEl('wavefront-grid-size-select');
            const openerRemovePtd = getOpenerEl('opd-remove-ptd-checkbox');
            if (openerObject && Number.isFinite(objectIndex)) openerObject.value = String(objectIndex);
            if (openerPlotType) openerPlotType.value = plotType;
            if (openerGrid && Number.isFinite(gridSize)) openerGrid.value = String(gridSize);
            if (openerRemovePtd) openerRemovePtd.checked = (opdDisplayMode === 'pistonTiltDefocusRemoved');

            try {
                const openerSyncOk = !!openerObject;
                if (openerSyncOk) {
                    opdStageTrace.push('Opener sync', 'object=' + String(objectIndex));
                } else {
                    opdStageTrace.pushWarn('Opener sync', 'opener object selector not found');
                }
            } catch (_) {}

            try {
                const computeInPopup = false;
                
                // Create cancel token (reuse PSF helper if available, or inline)
                const createCancelToken = window.opener.createCancelToken || (() => {
                    let aborted = false;
                    let reason = null;
                    const listeners = [];
                    return {
                        get aborted() { return aborted; },
                        get reason() { return reason; },
                        abort(r = 'User requested stop') {
                            if (aborted) return;
                            aborted = true;
                            reason = r;
                            listeners.forEach(fn => { try { fn(r); } catch (_) {} });
                        },
                        onAbort(fn) { listeners.push(fn); }
                    };
                });
                
                const popupCancelToken = createCancelToken();
                window['__popupOpdCancelToken'] = popupCancelToken;
                
                const stopBtn = document.getElementById('popup-stop-opd-btn');
                
                
                if (stopBtn) {
                    stopBtn.disabled = false;
                    stopBtn.textContent = 'Stop';
                }

                setProgress(0, 'Starting...');

                const opener = window.opener;
                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();

                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    opener
                    && typeof opener.showWavefrontDiagram === 'function'
                );
                const canUseWavefront = !!(
                    opener
                    && typeof opener.showWavefrontDiagram === 'function'
                );

                try {
                    opdStageTrace.push('Runtime check', 'desktopRust=' + String(!!canUseDesktopRust));
                } catch (_) {}

                if (!canUseWavefront) {
                    try { opdStageTrace.push('Blocked', 'Wavefront renderer unavailable on opener'); } catch (_) {}
                    throw new Error('OPD renderer is unavailable on opener window.');
                }

                try {
                    if (popupObject && popupObject.options && popupObject.options.length > 0) {
                        const optionCount = popupObject.options.length;
                        if (objectIndex < 0 || objectIndex >= optionCount) {
                            opdStageTrace.push('Blocked', 'Object index is invalid for current options');
                        }
                    }
                } catch (_) {}

                // NOTE: Wavefront generator supports only options.onProgress (same as PSF)
                const onProgress = (evt) => {
                    try {
                        pendingProgressEvent = evt;
                        const pct = Number(evt?.percent);
                        if (Number.isFinite(pct)) {
                            const msg = String(evt?.message || evt?.phase || 'working');
                            const milestones = [25, 50, 75, 100];
                            for (const milestone of milestones) {
                                if (milestone > lastComputingMilestone && pct >= (milestone - 0.001)) {
                                    opdStageTrace.push('Computing', msg + ' (' + String(milestone) + '%)');
                                    lastComputingMilestone = milestone;
                                }
                            }
                        }
                        const now = Date.now();
                        const elapsed = now - lastProgressUiUpdateAt;
                        if (elapsed >= PROGRESS_UI_UPDATE_INTERVAL_MS) {
                            flushPendingProgress(false);
                            return;
                        }

                        if (!progressFlushTimer) {
                            const delay = Math.max(0, PROGRESS_UI_UPDATE_INTERVAL_MS - elapsed);
                            progressFlushTimer = setTimeout(() => {
                                progressFlushTimer = null;
                                flushPendingProgress(false);
                            }, delay);
                        }
                    } catch (_) {}
                };
                
                try {
                    if (!window.opener || typeof window.opener.showWavefrontDiagram !== 'function') {
                        try { opdStageTrace.push('Blocked', 'showWavefrontDiagram is unavailable'); } catch (_) {}
                        throw new Error('showWavefrontDiagram is not available on opener');
                    }
                    try { opdStageTrace.push('Wavefront call started', 'Delegating to opener.showWavefrontDiagram'); } catch (_) {}
                    const wavefrontResult = await window.opener.showWavefrontDiagram(plotType, 'opd', Number.isFinite(gridSize) ? gridSize : 256, Number.isFinite(objectIndex) ? objectIndex : 0, {
                        containerElement: containerEl,
                        cancelToken: popupCancelToken,
                        onProgress,
                        opdDisplayMode,
                        throwOnError: true,
                        showAlert: false
                    });
                    if (wavefrontResult?.error) {
                        throw new Error(String(wavefrontResult?.error?.message || wavefrontResult?.error || 'Wavefront plot failed'));
                    }
                    if (lastComputingMilestone < 100) {
                        try {
                            const completionMilestones = [25, 50, 75, 100];
                            for (const milestone of completionMilestones) {
                                if (milestone > lastComputingMilestone) {
                                    const detail = (milestone === 100)
                                        ? 'Plot data prepared (100%)'
                                        : ('Progress checkpoint (' + String(milestone) + '%)');
                                    opdStageTrace.push('Computing', detail);
                                    lastComputingMilestone = milestone;
                                }
                            }
                        } catch (_) {}
                    }
                    try { opdStageTrace.push('Wavefront call completed', 'Plot render returned'); } catch (_) {}

                    // Optional: Zernike fit + push report to System Data
                    const shouldZernikeFit = !!(popupZernikeFit && popupZernikeFit.checked);
                    if (shouldZernikeFit) {
                        try {
                            if (String(plotType) === 'multifield') {
                                try { opdStageTrace.push('Zernike skipped', 'Not supported for multi-field'); } catch (_) {}
                                setProgress(100, 'Zernike fit is not available for Multi-field');
                            } else {
                                try { opdStageTrace.push('Zernike started'); } catch (_) {}
                                setProgress(98, 'Zernike fitting...');

                                const opener = window.opener;
                                const runtimeState = (() => {
                                    try {
                                        const key = '__cooptLastWavefrontRuntime';
                                        const s = opener ? opener[key] : null;
                                        return (s && typeof s === 'object') ? s : null;
                                    } catch (_) {
                                        return null;
                                    }
                                })();
                                const map = runtimeState && runtimeState.map ? runtimeState.map : null;
                                const meta = runtimeState && runtimeState.meta ? runtimeState.meta : null;
                                if (!map || map?.error) {
                                    throw new Error('No valid wavefrontMap to fit');
                                }

                                const wavelength = (() => {
                                    const fromMeta = Number(meta?.wavelength);
                                    if (Number.isFinite(fromMeta) && fromMeta > 0) return fromMeta;
                                    try {
                                        const w = Number(opener?.getPrimaryWavelength?.());
                                        if (Number.isFinite(w) && w > 0) return w;
                                    } catch (_) {}
                                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                                })();

                                const opticalSystemRows = (typeof opener?.getOpticalSystemRows === 'function')
                                    ? opener.getOpticalSystemRows()
                                    : null;
                                const host = opener || window;
                                const calculatorFactory =
                                    (host && typeof host.createOPDCalculator === 'function' && host.createOPDCalculator)
                                    || (typeof window.createOPDCalculator === 'function' && window.createOPDCalculator)
                                    || (typeof createOPDCalculator === 'function' ? createOPDCalculator : null);
                                const analyzerFactory =
                                    (host && typeof host.createWavefrontAnalyzer === 'function' && host.createWavefrontAnalyzer)
                                    || (typeof window.createWavefrontAnalyzer === 'function' && window.createWavefrontAnalyzer)
                                    || (typeof createWavefrontAnalyzer === 'function' ? createWavefrontAnalyzer : null);
                                const calculator = calculatorFactory ? calculatorFactory(opticalSystemRows, wavelength) : null;
                                const analyzer = (calculator && analyzerFactory) ? analyzerFactory(calculator) : null;

                                if (!analyzer || typeof analyzer.generateWavefrontMap !== 'function' || typeof analyzer.formatZernikeReportText !== 'function') {
                                    throw new Error('Wavefront analyzer is not available for Zernike fitting');
                                }

                                const objectRows = (typeof opener?.getObjectRows === 'function')
                                    ? opener.getObjectRows()
                                    : [];
                                const fitObjectIndex = Number.isFinite(Number(objectIndex))
                                    ? Math.max(0, Math.floor(Number(objectIndex)))
                                    : 0;
                                const selectedObject = Array.isArray(objectRows) && objectRows[fitObjectIndex]
                                    ? objectRows[fitObjectIndex]
                                    : null;
                                const fitFieldSetting = (() => {
                                    if (meta?.fieldSetting && typeof meta.fieldSetting === 'object') {
                                        return {
                                            ...meta.fieldSetting,
                                            objectIndex: fitObjectIndex,
                                            wavelength
                                        };
                                    }

                                    const pos = String(
                                        selectedObject?.position
                                        ?? selectedObject?.Position
                                        ?? selectedObject?.type
                                        ?? ''
                                    ).toLowerCase();
                                    const xVal = Number(selectedObject?.xHeightAngle ?? selectedObject?.x ?? 0) || 0;
                                    const yVal = Number(selectedObject?.yHeightAngle ?? selectedObject?.y ?? 0) || 0;
                                    const isAngleMode = pos === 'angle' || pos === 'field angle' || pos === 'angles' || pos === 'point';

                                    return {
                                        id: selectedObject?.id || fitObjectIndex + 1,
                                        displayName: 'Object ' + String(fitObjectIndex + 1),
                                        type: isAngleMode ? 'Angle' : 'Rectangle',
                                        fieldAngle: isAngleMode ? { x: xVal, y: yVal } : { x: 0, y: 0 },
                                        xHeight: isAngleMode ? 0 : xVal,
                                        yHeight: isAngleMode ? 0 : yVal,
                                        objectIndex: fitObjectIndex,
                                        wavelength
                                    };
                                })();

                                const fitGridSize = Number.isFinite(Number(gridSize))
                                    ? Math.max(16, Math.floor(Number(gridSize)))
                                    : 256;
                                const maxNoll = 37;
                                const legacyWavefrontMap = await analyzer.generateWavefrontMap(fitFieldSetting, fitGridSize, 'circular', {
                                    recordRays: false,
                                    progressEvery: 0,
                                    opdMode: 'simple',
                                    opdDisplayMode,
                                    renderFromZernike: false,
                                    skipZernikeFit: false,
                                    zernikeMaxNoll: maxNoll
                                });
                                if (!legacyWavefrontMap || legacyWavefrontMap?.error) {
                                    throw new Error(String(legacyWavefrontMap?.error?.message || 'Legacy Zernike wavefront generation failed'));
                                }

                                const fit = legacyWavefrontMap?.zernike;
                                if (!fit || !fit.coefficientsMicrons) {
                                    throw new Error('Legacy Zernike fit did not produce coefficients');
                                }

                                // Store coefficients for the main window Zernike Fit button
                                try {
                                    const key = '__cooptLastWavefrontRuntime';
                                    const base = (runtimeState && typeof runtimeState === 'object')
                                        ? runtimeState
                                        : { map: (map && typeof map === 'object') ? map : {}, meta: meta || null };
                                    const current = (base.map && typeof base.map === 'object') ? base.map : {};
                                    current.zernike = fit;
                                    current.statistics = legacyWavefrontMap.statistics || current.statistics || (map?.statistics || {});
                                    current.raw = legacyWavefrontMap.raw || current.raw || (map?.raw || {});
                                    current.statistics.skipZernikeFit = false;
                                    base.map = current;
                                    if (opener) opener[key] = base;
                                } catch (_) {}

                                const reportText = analyzer.formatZernikeReportText(legacyWavefrontMap, { maxNoll });

                                const pushSystemData = (text) => {
                                    const value = String(text || '');
                                    let wrote = false;

                                    const tryWriteTextarea = (doc, id) => {
                                        try {
                                            const ta = doc?.getElementById?.(id);
                                            if (!ta || typeof ta.value !== 'string') return false;
                                            ta.value = value;
                                            return true;
                                        } catch (_) {
                                            return false;
                                        }
                                    };

                                    try {
                                        // Keep a fallback cache so System Data windows can read latest text
                                        // even when opener's hidden textarea is not mounted in this route.
                                        if (opener) {
                                            opener.__cooptSystemDataText = value;
                                            if (typeof opener.__cooptPushSystemDataText === 'function') {
                                                opener.__cooptPushSystemDataText(value);
                                                wrote = true;
                                            }
                                        }
                                        wrote = true;
                                    } catch (_) {}

                                    wrote = tryWriteTextarea(opener?.document, 'system-data') || wrote;
                                    wrote = tryWriteTextarea(opener?.__systemDataPopup?.document, 'system-data') || wrote;
                                    wrote = tryWriteTextarea(opener?.__systemDataPopup?.document, 'popup-system-data') || wrote;
                                    try {
                                        if (typeof opener?.__systemDataPopup?.__cooptPushSystemDataText === 'function') {
                                            opener.__systemDataPopup.__cooptPushSystemDataText(value);
                                            wrote = true;
                                        }
                                    } catch (_) {}

                                    return wrote;
                                };

                                const tryOpenSystemDataWindow = (text) => {
                                    const value = String(text || '');
                                    const tryWriteTextarea = (doc, id) => {
                                        try {
                                            const ta = doc?.getElementById?.(id);
                                            if (!ta || typeof ta.value !== 'string') return false;
                                            ta.value = value;
                                            return true;
                                        } catch (_) {
                                            return false;
                                        }
                                    };

                                    try {
                                        const w = opener?.__systemDataPopup;
                                        if (w && !w.closed) {
                                            // Existing popup may use either legacy (#popup-system-data)
                                            // or app route (#system-data) textarea id.
                                            tryWriteTextarea(w.document, 'popup-system-data');
                                            tryWriteTextarea(w.document, 'system-data');
                                            try {
                                                if (typeof w.__cooptPushSystemDataText === 'function') {
                                                    w.__cooptPushSystemDataText(value);
                                                }
                                            } catch (_) {}
                                            try { w.focus(); } catch (_) {}
                                            return true;
                                        }
                                    } catch (_) {}

                                    // Some browsers block popups triggered by synthetic clicks.
                                    // We still try the main-window button first, but fall back to
                                    // opening the System Data window directly from this user action.
                                    try {
                                        const btn = opener?.document?.getElementById?.('open-system-data-window-btn');
                                        if (btn) {
                                            btn.click();
                                            // If the click worked, the opener should set __systemDataPopup.
                                            try {
                                                const w = opener?.__systemDataPopup;
                                                if (w && !w.closed) {
                                                    tryWriteTextarea(w.document, 'popup-system-data');
                                                    tryWriteTextarea(w.document, 'system-data');
                                                    try {
                                                        if (typeof w.__cooptPushSystemDataText === 'function') {
                                                            w.__cooptPushSystemDataText(value);
                                                        }
                                                    } catch (_) {}
                                                    try { w.focus(); } catch (_) {}
                                                    return true;
                                                }
                                            } catch (_) {}
                                        }
                                    } catch (_) {}

                                    // Fallback: open and render a minimal System Data popup directly.
                                    try {
                                        const popup = opener?.open?.('', 'System Data', 'width=1200,height=600');
                                        if (!popup) return false;
                                        try { opener.__systemDataPopup = popup; } catch (_) {}
                                        try { popup.document.open(); } catch (_) {}

                                        const html = [
                                            '<!DOCTYPE html>',
                                            '<html>',
                                            '<head>',
                                            '  <meta charset="UTF-8" />',
                                            '  <title>System Data</title>',
                                            '  <style>',
                                            '    html, body { height: 100%; }',
                                            '    body { margin: 0; font-family: Arial, sans-serif; display: flex; flex-direction: column; height: 100vh; background: #f4f4f4; }',
                                            '    .header { padding: 10px 12px; background: white; color: #333; font-weight: 600; border-bottom: 1px solid #ddd; }',
                                            '    .content { flex: 1 1 auto; padding: 10px 12px; min-height: 0; display: flex; }',
                                            '    textarea { flex: 1 1 auto; width: 100%; resize: none; font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; font-size: 12px; line-height: 1.4; border: 1px solid #bbb; border-radius: 4px; padding: 10px; box-sizing: border-box; min-height: 0; background: white; }',
                                            '  </style>',
                                            '</head>',
                                            '<body onload="(function(){function getOpenerEl(id){try{return window.opener&&window.opener.document?window.opener.document.getElementById(id):null;}catch(e){return null;}}function sync(){var src=getOpenerEl(\\'system-data\\');var dst=document.getElementById(\\'popup-system-data\\');if(dst&&src&&dst.value!==src.value){dst.value=src.value;}}setInterval(sync,500);window.addEventListener(\\'focus\\',sync);sync();})();">',
                                            '  <div class="header">System Data</div>',
                                            '  <div class="content">',
                                            '    <textarea id="popup-system-data" placeholder="System information will appear here..."></textarea>',
                                            '  </div>',
                                            '</body>',
                                            '</html>'
                                        ].join('\\n');

                                        popup.document.write(html);
                                        try { popup.document.close(); } catch (_) {}
                                        // Prime the fallback popup immediately without waiting for sync tick.
                                        tryWriteTextarea(popup.document, 'popup-system-data');
                                        tryWriteTextarea(popup.document, 'system-data');
                                        try {
                                            if (typeof popup.__cooptPushSystemDataText === 'function') {
                                                popup.__cooptPushSystemDataText(value);
                                            }
                                        } catch (_) {}
                                        try { popup.focus(); } catch (_) {}
                                        return true;
                                    } catch (_) {}

                                    return false;
                                };

                                const pushed = pushSystemData(reportText);
                                const opened = tryOpenSystemDataWindow(reportText);
                                if (pushed && opened) {
                                    try { opdStageTrace.push('Zernike completed', 'Report pushed to System Data'); } catch (_) {}
                                    setProgress(100, 'Zernike report pushed to System Data');
                                } else if (pushed) {
                                    try { opdStageTrace.push('Zernike completed', 'Report pushed, popup open skipped'); } catch (_) {}
                                    setProgress(100, 'Zernike report pushed. See System data.');
                                } else {
                                    try { opdStageTrace.push('Zernike partial', 'Could not push report to System Data'); } catch (_) {}
                                    setProgress(100, 'Zernike fit done (could not write System Data). See System data.');
                                }
                            }
                        } catch (e) {
                            try { opdStageTrace.push('Zernike failed', String(e?.message || e || 'unknown error')); } catch (_) {}
                            setProgress(100, 'Zernike fit failed. See console.');
                        }
                    }

                    flushPendingProgress(true);
                    resizePlot();
                    try { opdStageTrace.push('Render complete'); } catch (_) {}
                } catch (err) {
                    if (err?.message?.includes('Cancelled')) {
                        flushPendingProgress(true);
                        setProgress(100, 'Cancelled');
                        try { opdStageTrace.push('Cancelled', 'Stopped by user'); } catch (_) {}
                        console.log('🛑 OPD calculation cancelled by user');
                    } else {
                        try {
                            const em = String(err?.message || err || 'unknown error').toLowerCase();
                            if (em.includes('object')) {
                                opdStageTrace.push('Object-stage failure', String(err?.message || err || 'unknown error'));
                            }
                        } catch (_) {}
                        try { opdStageTrace.push('Wavefront call failed', String(err?.message || err || 'unknown error')); } catch (_) {}
                        throw err;
                    }
                } finally {
                    flushPendingProgress(true);
                    setTimeout(() => {
                        try { hideProgress(); } catch (_) {}
                    }, 250);
                    if (stopBtn) {
                        stopBtn.disabled = true;
                        stopBtn.textContent = 'Stop';
                    }
                    window['__popupOpdCancelToken'] = null;
                }
            } catch (err) {
                console.error(err);
                flushPendingProgress(true);
                setProgress(100, 'Failed');
                try { opdStageTrace.push('Failed', String(err?.message || err || 'unknown error')); } catch (_) {}
                setTimeout(() => {
                    try { hideProgress(); } catch (_) {}
                }, 600);
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate OPD diagram. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-wavefront-btn').addEventListener('click', () => window.renderOPD());
        document.getElementById('popup-stop-opd-btn').addEventListener('click', () => {
            console.log('🛑 Popup OPD Stop button clicked');
            const token = window.__popupOpdCancelToken;
            if (token && typeof token.abort === 'function') {
                token.abort('Stopped by user');
                const stopBtn = document.getElementById('popup-stop-opd-btn');
                if (stopBtn) {
                    stopBtn.disabled = true;
                    stopBtn.textContent = 'Stopping...';
                }
            }
        });

        function syncAll() {
            syncObjectOptionsFromOpener();
            syncInputsFromOpener();
        }
        window.addEventListener('resize', resizePlot);
        window.addEventListener('focus', syncAll);
        syncAll();
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const d=document.getElementById("open-psf-window-btn");if(d){d.addEventListener("click",()=>{if(H.__psfPopup&&!H.__psfPopup.closed){try{H.__psfPopup.close()}catch{}H.__psfPopup=null}const b=n("Point Spread Function","width=800,height=600");if(!b||!b.document){try{b?.close()}catch{}alert("Popup could not be opened. Please allow popups for this site.");return}H.__psfPopup=b;const v=(()=>{try{if(H.tableObject&&typeof H.tableObject.getData=="function"){const _=H.tableObject.getData();if(Array.isArray(_)&&_.length>0)return _}}catch{}try{if(typeof H.getObjectRows=="function"){const _=H.getObjectRows(H.tableObject);if(Array.isArray(_)&&_.length>0)return _}}catch{}try{const _=localStorage.getItem("objectTableData"),M=_?JSON.parse(_):null;if(Array.isArray(M)&&M.length>0)return M}catch{}try{if(typeof H.loadSystemConfigurations=="function"){const _=H.loadSystemConfigurations(),M=Array.isArray(_&&_.configurations)?_.configurations:[],G=Number(_&&_.activeConfigId),z=Number.isFinite(G)?M.find(L=>Number(L&&L.id)===G)||null:M[0]||null,E=z&&Array.isArray(z.object)?z.object:null;if(Array.isArray(E)&&E.length>0)return E}}catch{}return[]})(),S=JSON.stringify(v).replace(/</g,"\\u003c");try{b.document.open()}catch{}b.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Point Spread Function</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-psf-container { flex: 1 1 auto; min-height: 0; }
        #popup-psf-container-stats { flex: 0 0 auto; padding: 8px 12px; font-size: 12px; color: #333; border-top: 1px solid #eee; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="header"></div>
    <div class="controls">
        <label for="popup-psf-object-select">Object:</label>
        <select id="popup-psf-object-select"><option value="0">1</option></select>
        <label for="popup-psf-sampling-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-psf-sampling-select" title="Auto: pad to at least 512. None: no padding (FFT size = OPD grid). Or choose an explicit FFT size.">
            <option value="auto" selected>Auto (≥512)</option>
            <option value="none">None</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label for="popup-psf-zernike-sampling-select">OPD grid:</label>
        <select id="popup-psf-zernike-sampling-select" title="Ray-traced OPD grid size (number of rays traced across pupil)">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label><input type="checkbox" id="popup-psf-log-scale-checkbox"> Log scale</label>
        <label><input type="checkbox" id="popup-psf-remove-ptd-checkbox"> Remove P/T/D</label>
        <button id="popup-show-psf-btn" type="button">Show PSF</button>
        <button id="popup-stop-psf-btn" type="button" disabled>Stop</button>
        <span id="popup-psf-pipeline-badge"></span>
    </div>
    <div id="popup-psf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-psf-progress-text" style="margin-bottom: 6px;">Calculating PSF...</div>
        <progress id="popup-psf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-psf-container"></div>
        <div id="popup-psf-container-stats"></div>
        <div id="popup-psf-opd-parity-diff" style="display:none;height:220px;border-top:1px solid #eee;"></div>
    </div>
        <script>
        const __PSF_INITIAL_OBJECT_ROWS = ${S};
        // Debug: confirm the popup script version in console.
                // build tag intentionally not shown
        function isIOSLike() {
            try {
                const ua = String(navigator.userAgent || '');
                if (/iPad|iPhone|iPod/i.test(ua)) return true;
                // iPadOS 13+ may masquerade as Mac
                if (/Macintosh/i.test(ua) && Number(navigator.maxTouchPoints || 0) > 1) return true;
            } catch (_) {}
            return false;
        }

        function isPopupTauriRuntime() {
            try {
                if (window && window.__TAURI_INTERNALS__) return true;
            } catch (_) {}
            try {
                if (window && window.opener && window.opener.__TAURI_INTERNALS__) return true;
            } catch (_) {}
            return false;
        }

        function createCancelToken() {
            return {
                aborted: false,
                reason: null,
                _listeners: [],
                abort(reason = 'User requested stop') {
                    if (this.aborted) return;
                    this.aborted = true;
                    this.reason = reason;
                    const ls = Array.isArray(this._listeners) ? this._listeners.slice() : [];
                    for (const fn of ls) {
                        try { fn(reason); } catch (_) {}
                    }
                },
                onAbort(fn) {
                    if (typeof fn !== 'function') return;
                    if (this.aborted) {
                        try { fn(this.reason); } catch (_) {}
                        return;
                    }
                    this._listeners.push(fn);
                }
            };
        }

        let activeCancelToken = null;

        let __psfObjectSyncRetries = 0;
        const __psfScheduleObjectResync = () => {
            // On some loads, the opener's Tabulator may not be ready yet; retry briefly.
            if (__psfObjectSyncRetries >= 60) return;
            const delay = Math.min(2000, 150 + (__psfObjectSyncRetries * 150));
            __psfObjectSyncRetries++;
            setTimeout(() => {
                try { syncObjectOptionsFromOpener(); } catch (_) {}
            }, delay);
        };

        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncObjectOptionsFromOpener() {
            const openerPsfSelect = getOpenerEl('psf-object-select');
            const openerWavefrontSelect = getOpenerEl('wavefront-object-select');
            const openerSelect = openerWavefrontSelect || openerPsfSelect;
            const popupSelect = document.getElementById('popup-psf-object-select');
            if (!popupSelect) return;

            const toFiniteNumber = (v) => {
                const n = (typeof v === 'number') ? v : parseFloat(v);
                return Number.isFinite(n) ? n : NaN;
            };
            const pickNumber = (obj, keys, fallback) => {
                for (let i = 0; i < keys.length; i++) {
                    const k = keys[i];
                    if (!k) continue;
                    const raw = obj ? obj[k] : undefined;
                    if (raw === undefined || raw === null || raw === '') continue;
                    const n = toFiniteNumber(raw);
                    if (Number.isFinite(n)) return n;
                }
                return fallback;
            };

            const current = popupSelect.value;
            const nextOptions = [];

            // OPD/MTF-style: resolve object rows from multiple opener sources.
            let opener = null;
            try { opener = window.opener || null; } catch (_) { opener = null; }
            let objects = [];
            if (opener) {
                try {
                    if (opener.tableObject && typeof opener.tableObject.getData === 'function') {
                        const rows = opener.tableObject.getData();
                        if (Array.isArray(rows) && rows.length > 0) objects = rows;
                    }
                } catch (_) {}

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (typeof opener.getObjectRows === 'function') {
                            const rows = opener.getObjectRows(opener.tableObject);
                            if (Array.isArray(rows) && rows.length > 0) objects = rows;
                        }
                    } catch (_) {}
                }

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (opener.localStorage && typeof opener.localStorage.getItem === 'function') {
                            const raw = opener.localStorage.getItem('objectTableData');
                            const parsed = raw ? JSON.parse(raw) : null;
                            if (Array.isArray(parsed) && parsed.length > 0) objects = parsed;
                        }
                    } catch (_) {}
                }

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (typeof opener.getActiveConfiguration === 'function') {
                            const cfg = opener.getActiveConfiguration();
                            const rows = cfg && Array.isArray(cfg.object) ? cfg.object : null;
                            if (Array.isArray(rows) && rows.length > 0) objects = rows;
                        }
                    } catch (_) {}
                }

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (typeof opener.loadSystemConfigurations === 'function') {
                            const all = opener.loadSystemConfigurations();
                            const list = Array.isArray(all && all.configurations) ? all.configurations : [];
                            const activeId = Number(all && all.activeConfigId);
                            const active = Number.isFinite(activeId)
                                ? (list.find((c) => Number(c && c.id) === activeId) || null)
                                : (list[0] || null);
                            const rows = active && Array.isArray(active.object) ? active.object : null;
                            if (Array.isArray(rows) && rows.length > 0) objects = rows;
                        }
                    } catch (_) {}
                }
            }

            if ((!Array.isArray(objects) || objects.length === 0) && Array.isArray(__PSF_INITIAL_OBJECT_ROWS) && __PSF_INITIAL_OBJECT_ROWS.length > 0) {
                objects = __PSF_INITIAL_OBJECT_ROWS;
            }

            if (!Array.isArray(objects) || objects.length === 0) {
                try {
                    const raw = localStorage.getItem('objectTableData');
                    const parsed = raw ? JSON.parse(raw) : null;
                    if (Array.isArray(parsed) && parsed.length > 0) objects = parsed;
                } catch (_) {}
            }

            if (!Array.isArray(objects) || objects.length === 0) {
                try {
                    const rawCfg = localStorage.getItem('systemConfigurations');
                    const parsedCfg = rawCfg ? JSON.parse(rawCfg) : null;
                    const list = Array.isArray(parsedCfg && parsedCfg.configurations) ? parsedCfg.configurations : [];
                    const activeId = Number(parsedCfg && parsedCfg.activeConfigId);
                    const active = Number.isFinite(activeId)
                        ? (list.find((c) => Number(c && c.id) === activeId) || null)
                        : (list[0] || null);
                    const rows = active && Array.isArray(active.object) ? active.object : null;
                    if (Array.isArray(rows) && rows.length > 0) objects = rows;
                } catch (_) {}
            }

            if (Array.isArray(objects) && objects.length > 0) {
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(
                        (obj.position !== undefined && obj.position !== null) ? obj.position :
                        (obj.object !== undefined && obj.object !== null) ? obj.object :
                        (obj.Object !== undefined && obj.Object !== null) ? obj.Object :
                        (obj.objectType !== undefined && obj.objectType !== null) ? obj.objectType :
                        'Point'
                    );
                    const x = pickNumber(obj, ['x', 'X', 'xFieldAngle', 'xAngle', 'xHeightAngle', 'XHeightAngle', 'x_height_angle', 'x_field_angle', 'x_angle'], 0);
                    const y = pickNumber(obj, ['y', 'Y', 'yFieldAngle', 'yAngle', 'yHeightAngle', 'YHeightAngle', 'y_height_angle', 'y_field_angle', 'y_angle', 'fieldAngle', 'angle'], 0);
                    nextOptions.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }

            // Fallback: clone opener select options if present (but ignore single placeholder).
            if (nextOptions.length === 0 && openerSelect && openerSelect.options && openerSelect.options.length > 0) {
                const opts = Array.from(openerSelect.options);
                let looksLikePlaceholder = false;
                if (opts.length === 1) {
                    const t = String(opts[0].textContent || '').trim().toLowerCase();
                    looksLikePlaceholder = (t === '0' || t === 'object 1' || t === 'object1');
                }
                if (!looksLikePlaceholder) {
                    const normalizeLabel = (label) => {
                        const s = String(label || '').trim();
                        // Convert e.g. "Object1 : Angle (...)" / "Object 1: ..." / "object1" => "1: ..." / "1"
                        const m = s.match(/^objects*(d+)s*[:：]?s*(.*)$/i);
                        if (m) {
                            const n = m[1];
                            const rest = String(m[2] || '').trim();
                            return rest ? (n + ': ' + rest) : String(n);
                        }
                        return s;
                    };
                    opts.forEach(opt => {
                        const raw = String((opt.textContent !== undefined && opt.textContent !== null) ? opt.textContent : '');
                        nextOptions.push({ value: String(opt.value), label: normalizeLabel(raw) });
                    });
                }
            }

            if (nextOptions.length === 0) {
                nextOptions.push({ value: '0', label: '1' });
                __psfScheduleObjectResync();
            } else {
                __psfObjectSyncRetries = 0;
            }

            popupSelect.innerHTML = '';
            for (const opt of nextOptions) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                popupSelect.appendChild(o);
            }

            if (current && Array.from(popupSelect.options).some(o => o.value === current)) {
                popupSelect.value = current;
                return;
            }
            const preferred = (openerPsfSelect && openerPsfSelect.value)
                ? openerPsfSelect.value
                : (openerSelect && openerSelect.value ? openerSelect.value : '');
            if (preferred && Array.from(popupSelect.options).some(o => o.value === preferred)) {
                popupSelect.value = preferred;
            } else {
                const firstOpt = (popupSelect.options && popupSelect.options.length > 0) ? popupSelect.options[0] : null;
                popupSelect.value = (firstOpt && firstOpt.value !== undefined && firstOpt.value !== null) ? firstOpt.value : '0';
            }
        }

        function syncInputsFromOpener() {
            const openerZeroPad = getOpenerEl('psf-zeropad-select');
            const openerZernikeSampling = getOpenerEl('psf-zernike-sampling-select');
            const openerLog = getOpenerEl('psf-log-scale-checkbox');
            const openerRemovePtd = getOpenerEl('psf-remove-ptd-checkbox');
            const popupSampling = document.getElementById('popup-psf-sampling-select');
            const popupZernikeSampling = document.getElementById('popup-psf-zernike-sampling-select');
            const popupLog = document.getElementById('popup-psf-log-scale-checkbox');
            const popupRemovePtd = document.getElementById('popup-psf-remove-ptd-checkbox');
            if (openerZeroPad && popupSampling && openerZeroPad.value) {
                if (Array.from(popupSampling.options || []).some(o => String(o.value) === String(openerZeroPad.value))) {
                    popupSampling.value = openerZeroPad.value;
                }
            }
            if (popupZernikeSampling && openerZernikeSampling && openerZernikeSampling.value) popupZernikeSampling.value = openerZernikeSampling.value;
            try {
                if (popupLog && openerLog) popupLog.checked = !!openerLog.checked;
            } catch (_) {}
            try {
                if (popupRemovePtd) popupRemovePtd.checked = !!(openerRemovePtd && openerRemovePtd.checked);
            } catch (_) {}
        }

        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const el = document.getElementById('popup-psf-container');
                if (el) plotly.Plots.resize(el);
                const diffEl = document.getElementById('popup-psf-opd-parity-diff');
                if (diffEl && diffEl.style.display !== 'none') {
                    plotly.Plots.resize(diffEl);
                }
            } catch (_) {}
        }

        function clearOpdParityHeatmap() {
            try {
                const diffEl = document.getElementById('popup-psf-opd-parity-diff');
                if (!diffEl) return;
                diffEl.style.display = 'none';
                diffEl.innerHTML = '';
            } catch (_) {}
        }

        async function renderOpdParityHeatmap(diffGridWaves, meta) {
            try {
                const diffEl = document.getElementById('popup-psf-opd-parity-diff');
                if (!diffEl) return;
                if (!Array.isArray(diffGridWaves) || diffGridWaves.length === 0 || !window.Plotly) {
                    clearOpdParityHeatmap();
                    return;
                }

                const maxAbs = Number(meta && meta.maxAbsWaves);
                const zAbs = (Number.isFinite(maxAbs) && maxAbs > 0) ? maxAbs : 1;
                const title = 'OPD parity map (Native - JS) [waves]';

                diffEl.style.display = 'block';
                await window.Plotly.newPlot(diffEl, [{
                    z: diffGridWaves,
                    type: 'heatmap',
                    colorscale: 'RdBu',
                    zmid: 0,
                    zmin: -zAbs,
                    zmax: zAbs,
                    colorbar: { title: 'waves' }
                }], {
                    title: { text: title, font: { size: 12 } },
                    margin: { l: 50, r: 20, t: 32, b: 40 },
                    xaxis: { title: 'Pupil ix' },
                    yaxis: { title: 'Pupil iy', autorange: 'reversed' }
                }, { responsive: true, displaylogo: false });
            } catch (_) {
                clearOpdParityHeatmap();
            }
        }

        window['renderPSF'] = async () => {
            const containerEl = document.getElementById('popup-psf-container');
            if (containerEl) containerEl.innerHTML = '';
            clearOpdParityHeatmap();

            const setPopupPsfBadge = (status) => {
                try {
                    const el = document.getElementById('popup-psf-pipeline-badge');
                    if (!el) return;
                    const text = String(status || '');
                    el.textContent = text;
                    if (text) {
                        el.setAttribute('title', text);
                    } else {
                        el.removeAttribute('title');
                    }
                } catch (_) {}
            };

            const progressWrapper = document.getElementById('popup-psf-progress-wrapper');
            const progressEl = document.getElementById('popup-psf-progress');
            const progressTextEl = document.getElementById('popup-psf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const stopBtn = document.getElementById('popup-stop-psf-btn');
            if (stopBtn) {
                stopBtn.disabled = false;
            }

            activeCancelToken = createCancelToken();

            const popupObject = document.getElementById('popup-psf-object-select');
            const popupSampling = document.getElementById('popup-psf-sampling-select');
            const popupZernikeSampling = document.getElementById('popup-psf-zernike-sampling-select');
            const popupLog = document.getElementById('popup-psf-log-scale-checkbox');
            const popupRemovePtd = document.getElementById('popup-psf-remove-ptd-checkbox');

            let objectIndex = popupObject ? parseInt(popupObject.value, 10) : 0;
            if (!Number.isFinite(objectIndex) && popupObject && Number.isFinite(popupObject.selectedIndex)) {
                objectIndex = popupObject.selectedIndex;
            }
            const zernikeSampling = popupZernikeSampling ? parseInt(popupZernikeSampling.value, 10) : 128;
            const zeroPadRaw = popupSampling ? String(popupSampling.value || 'auto') : 'auto';
            const logScale = !!(popupLog && popupLog.checked);
            const opdDisplayMode = (popupRemovePtd && popupRemovePtd.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const PSF_DEBUG = !!(typeof globalThis !== 'undefined' && globalThis.__PSF_DEBUG);

            const openerObject = getOpenerEl('psf-object-select');
            const openerSampling = getOpenerEl('psf-sampling-select');
            const openerZeroPad = getOpenerEl('psf-zeropad-select');
            const openerZernikeSampling = getOpenerEl('psf-zernike-sampling-select');
            const openerLog = getOpenerEl('psf-log-scale-checkbox');
            const openerRemovePtd = getOpenerEl('psf-remove-ptd-checkbox');
            if (openerObject && Number.isFinite(objectIndex)) openerObject.value = String(objectIndex);
            if (openerSampling && Number.isFinite(zernikeSampling)) openerSampling.value = String(zernikeSampling);
            if (openerZeroPad && zeroPadRaw) {
                if (Array.from(openerZeroPad.options || []).some(o => String(o.value) === String(zeroPadRaw))) {
                    openerZeroPad.value = zeroPadRaw;
                }
            }
            try {
                if (openerRemovePtd) openerRemovePtd.checked = (opdDisplayMode === 'pistonTiltDefocusRemoved');
            } catch (_) {}
            if (openerZernikeSampling && Number.isFinite(zernikeSampling)) openerZernikeSampling.value = String(zernikeSampling);
            if (openerLog) openerLog.checked = logScale;

            try {
                setPopupPsfBadge('Running');
                setProgress(0, 'Starting...');
                // Allow the popup to paint the progress UI before heavy computation begins.
                await new Promise(r => setTimeout(r, 0));

                const onProgress = (evt) => {
                    try {
                        const p = Number(evt && evt.percent);
                        const msg = (evt && evt.message) || (evt && evt.phase) || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };

                // Always compute inside the popup to avoid background throttling of the opener
                // when the main window is hidden/minimized/unfocused.
                {
                    const moduleURL = (relPath) => {
                        const baseHref = (() => {
                            try {
                                return (window.opener && window.opener.location && window.opener.location.href)
                                    ? window.opener.location.href
                                    : window.location.href;
                            } catch (_) {
                                return window.location.href;
                            }
                        })();
                        const url = new URL(relPath, baseHref);
                        return url.href;
                    };

                    const throwIfCancelled = (token) => {
                        if (token && token.aborted) {
                            const err = new Error(String(token.reason || 'Cancelled'));
                            err.code = 'CANCELLED';
                            throw err;
                        }
                    };
                    const raceWithCancel = async (promise, token) => {
                        if (!token) return await promise;
                        throwIfCancelled(token);
                        const cancelPromise = new Promise((_, reject) => {
                            token.onAbort((reason) => {
                                const err = new Error(String(reason || 'Cancelled'));
                                err.code = 'CANCELLED';
                                reject(err);
                            });
                        });
                        return await Promise.race([promise, cancelPromise]);
                    };

                    // Modules are already imported at the top of this file

                    const cloneRows = (rows) => {
                        if (!Array.isArray(rows)) return rows;
                        try {
                            if (typeof structuredClone === 'function') return structuredClone(rows);
                        } catch (_) {}
                        try {
                            return JSON.parse(JSON.stringify(rows));
                        } catch (_) {
                            return rows;
                        }
                    };

                    const opticalSystemRows = (() => {
                        try {
                            if (window.opener && typeof window.opener.getOpticalSystemRows === 'function') {
                                // Prefer live Tabulator table data if available.
                                const r = window.opener.getOpticalSystemRows(window.opener.tableOpticalSystem);
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        // Fallback: try opener again without parameter
                        try {
                            if (window.opener && typeof window.opener.getOpticalSystemRows === 'function') {
                                const r = window.opener.getOpticalSystemRows();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        return [];
                    })();

                    const objects = (() => {
                        if (Array.isArray(__PSF_INITIAL_OBJECT_ROWS) && __PSF_INITIAL_OBJECT_ROWS.length > 0) {
                            return cloneRows(__PSF_INITIAL_OBJECT_ROWS);
                        }
                        try {
                            if (window.opener && window.opener.tableObject && typeof window.opener.tableObject.getData === 'function') {
                                const r = window.opener.tableObject.getData();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        try {
                            if (window.opener && typeof window.opener.getObjectRows === 'function') {
                                const r = window.opener.getObjectRows(window.opener.tableObject);
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        // Fallback: try opener again without parameter
                        try {
                            if (window.opener && typeof window.opener.getObjectRows === 'function') {
                                const r = window.opener.getObjectRows();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        try {
                            const raw = localStorage.getItem('objectTableData');
                            const parsed = raw ? JSON.parse(raw) : null;
                            if (Array.isArray(parsed) && parsed.length > 0) return cloneRows(parsed);
                        } catch (_) {}
                        try {
                            const rawCfg = localStorage.getItem('systemConfigurations');
                            const parsedCfg = rawCfg ? JSON.parse(rawCfg) : null;
                            const list = Array.isArray(parsedCfg && parsedCfg.configurations) ? parsedCfg.configurations : [];
                            const activeId = Number(parsedCfg && parsedCfg.activeConfigId);
                            const active = Number.isFinite(activeId)
                                ? (list.find((c) => Number(c && c.id) === activeId) || null)
                                : (list[0] || null);
                            const rows = active && Array.isArray(active.object) ? active.object : null;
                            if (Array.isArray(rows) && rows.length > 0) return cloneRows(rows);
                        } catch (_) {}
                        return [];
                    })();

                    const sources = (() => {
                        try {
                            if (window.opener && typeof window.opener.getSourceRows === 'function') {
                                const r = window.opener.getSourceRows(window.opener.tableSource);
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        // Fallback: try opener again without tableSource parameter
                        try {
                            if (window.opener && typeof window.opener.getSourceRows === 'function') {
                                const r = window.opener.getSourceRows();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        return [];
                    })();
                    if (!Array.isArray(opticalSystemRows) || opticalSystemRows.length === 0) throw new Error('No optical system data (popup).');
                    if (!Array.isArray(objects) || objects.length === 0) throw new Error('No object data (popup).');

                    const toFiniteNumber = (v) => {
                        const n = (typeof v === 'number') ? v : parseFloat(v);
                        return (Number.isFinite(n) ? n : NaN);
                    };
                    const pickNumber = (obj, keys, fallback) => {
                        for (let i = 0; i < keys.length; i++) {
                            const k = keys[i];
                            if (!k) continue;
                            const raw = obj ? obj[k] : undefined;
                            if (raw === undefined || raw === null || raw === '') continue;
                            const n = toFiniteNumber(raw);
                            if (Number.isFinite(n)) return n;
                        }
                        return fallback;
                    };

                    let idx = Number.isFinite(objectIndex) ? objectIndex : 0;
                    // If the select values are 1-based for any reason, normalize.
                    if ((idx >= objects.length) && idx > 0 && (idx - 1) < objects.length) idx = idx - 1;
                    if (idx < 0) idx = 0;
                    const selectedObject = objects[idx];
                    if (!selectedObject) throw new Error('Selected object not found (popup).');

                    const primaryWl = (() => {
                        try {
                            if (window.opener && typeof window.opener.getPrimaryWavelength === 'function') {
                                const v = Number(window.opener.getPrimaryWavelength());
                                if (Number.isFinite(v) && v > 0) return v;
                            }
                        } catch (_) {}
                        return NaN;
                    })();
                    const s0 = (sources && sources.length > 0) ? sources[0] : null;
                    const wl0 = (s0 && s0.wavelength !== undefined && s0.wavelength !== null) ? Number(s0.wavelength) : NaN;
                    const wavelength = Number.isFinite(primaryWl)
                        ? primaryWl
                        : (() => { throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.'); })();

                    // Match showPSFDiagram(): build a fieldSetting compatible with eva-wavefront.js.
                    const objectX = pickNumber(selectedObject, ['x', 'X', 'xFieldAngle', 'xAngle', 'xHeightAngle', 'XHeightAngle', 'x_height_angle', 'x_field_angle', 'x_angle'], 0);
                    const objectY = pickNumber(selectedObject, ['y', 'Y', 'yFieldAngle', 'yAngle', 'yHeightAngle', 'YHeightAngle', 'y_height_angle', 'y_field_angle', 'y_angle', 'fieldAngle', 'angle'], 0);
                    const objectTypeRaw = String(
                        (selectedObject && selectedObject.position !== undefined && selectedObject.position !== null) ? selectedObject.position :
                        (selectedObject && selectedObject.object !== undefined && selectedObject.object !== null) ? selectedObject.object :
                        (selectedObject && selectedObject.Object !== undefined && selectedObject.Object !== null) ? selectedObject.Object :
                        (selectedObject && selectedObject.objectType !== undefined && selectedObject.objectType !== null) ? selectedObject.objectType :
                        'Point'
                    );
                    const objectType = objectTypeRaw;
                    const objectTypeLower = String(objectTypeRaw).toLowerCase();
                    let fieldAngle = { x: 0, y: 0 };
                    let xHeight = 0;
                    let yHeight = 0;
                    if (/\\bangle\\b/.test(objectTypeLower)) {
                        fieldAngle = { x: Number(objectX) || 0, y: Number(objectY) || 0 };
                        xHeight = 0;
                        yHeight = 0;
                    } else {
                        fieldAngle = { x: 0, y: 0 };
                        xHeight = Number(objectX) || 0;
                        yHeight = Number(objectY) || 0;
                    }
                    const fieldSetting = {
                        objectIndex: idx,
                        type: objectType,
                        fieldAngle,
                        xHeight,
                        yHeight,
                        wavelength
                    };

                    const getActiveConfigLabel = () => {
                        try {
                            if (typeof localStorage === 'undefined') return '';
                            const sys = loadSystemConfigurations();
                            const activeId = sys?.activeConfigId;
                            const cfg = Array.isArray(sys?.configurations)
                                ? sys.configurations.find(c => String(c?.id) === String(activeId))
                                : null;
                            if (!cfg) return (activeId !== undefined && activeId !== null) ? ('id=' + activeId) : '';
                            return ('id=' + cfg.id + ' name=' + (cfg.name || '')).trim();
                        } catch (_) {
                            return '';
                        }
                    };

                    const calcFNV1a32 = (str) => {
                        let hash = 0x811c9dc5;
                        for (let i = 0; i < str.length; i++) {
                            hash ^= str.charCodeAt(i);
                            hash = Math.imul(hash, 0x01000193);
                        }
                        return (hash >>> 0).toString(16);
                    };

                    const summarizeOpticalSystemRows = (rows) => {
                        if (!Array.isArray(rows) || rows.length === 0) return { checksum: '0' };
                        const parts = [];
                        for (const r of rows) {
                            if (!r) continue;
                            const obj = r['object type'] ?? r.object ?? r.Object ?? '';
                            const radius = r.radius ?? r.Radius ?? '';
                            const thickness = r.thickness ?? r.Thickness ?? '';
                            const material = r.material ?? r.Material ?? '';
                            const semidia = r.semidia ?? r.semidiameter ?? r.SemiDia ?? '';
                            const id = r.id ?? '';
                            parts.push(String(id) + '|' + String(obj) + '|' + String(radius) + '|' + String(thickness) + '|' + String(material) + '|' + String(semidia));
                        }
                        return { checksum: calcFNV1a32(parts.join(';')) };
                    };

                    if (PSF_DEBUG) {
                        try {
                            const fa = (fieldSetting && fieldSetting.fieldAngle && typeof fieldSetting.fieldAngle === 'object')
                                ? fieldSetting.fieldAngle
                                : { x: 0, y: 0 };
                            const line = '🧭 [PSF popup] objectIndex=' + idx + ' type=' + objectType +
                                ' fieldAngle=(' + (Number(fa.x) || 0) + ',' + (Number(fa.y) || 0) + ')' +
                                ' height=(' + (Number(fieldSetting && fieldSetting.xHeight) || 0) + ',' + (Number(fieldSetting && fieldSetting.yHeight) || 0) + ')' +
                                ' wl=' + (Number(wavelength) || 0);
                            console.log(line);
                            try {
                                if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                    window.opener.console.log(line);
                                }
                            } catch (_) {}
                        } catch (_) {}
                    }

                    if (PSF_DEBUG) {
                        try {
                            const summary = summarizeOpticalSystemRows(opticalSystemRows);
                            const idLine = '🧾 [PSF popup] activeConfig=' + (getActiveConfigLabel() || '(none)') +
                                ' rows=' + (Array.isArray(opticalSystemRows) ? opticalSystemRows.length : 0) +
                                ' checksum=' + (summary && summary.checksum ? summary.checksum : '0');
                            console.log(idLine);
                            try {
                                if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                    window.opener.console.log(idLine);
                                }
                            } catch (_) {}
                        } catch (_) {}
                    }

                    // Also log PSF physical scale inputs (pupil diameter / focal length) once available.
                    // This is critical when the plot auto-normalizes intensity and hides differences.
                    const logScaleInputs = (pupilDiameterMm, focalLengthMm, stopIndex) => {
                        if (!PSF_DEBUG) return;
                        try {
                            const line = '📏 [PSF popup] pupilDiameterMm=' + (Number(pupilDiameterMm) || 0) +
                                ' focalLengthMm=' + (Number(focalLengthMm) || 0) +
                                ' stopIndex=' + (Number.isFinite(Number(stopIndex)) ? Number(stopIndex) : -1);
                            console.log(line);
                            try {
                                if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                    window.opener.console.log(line);
                                }
                            } catch (_) {}
                        } catch (_) {}
                    };

                    const wavefrontGridSize = Number.isFinite(zernikeSampling)
                        ? Math.max(16, Math.floor(Number(zernikeSampling)))
                        : 128;
                    onProgress({ percent: 0, phase: 'opd', message: 'OPD...' });

                    let wavefrontMap = null;
                    let nativeOpdResp = null;
                    let nativeOpdDiag = null;
                    const forceJsOpdMap = !!(typeof window !== 'undefined' && window.__PSF_POPUP_FORCE_JS_OPD_MAP === true);
                    const preferNativeOpdMap = (typeof window !== 'undefined' && window.__PSF_POPUP_USE_NATIVE_OPD_MAP === false)
                        ? false
                        : true;
                    const canUseNativeOpdMap = !forceJsOpdMap && preferNativeOpdMap
                        && !!(window.opener && typeof window.opener.runDesktopNativeOpdMapForPopup === 'function');

                    if (canUseNativeOpdMap) {
                        try {
                            nativeOpdResp = await raceWithCancel(window.opener.runDesktopNativeOpdMapForPopup({
                                objectIndex: idx,
                                gridSize: wavefrontGridSize,
                                wavelengthUm: wavelength,
                                opdDisplayMode,
                            }), activeCancelToken);
                            throwIfCancelled(activeCancelToken);
                            {
                                const usedIdx = Number(nativeOpdResp?.usedObjectIndex);
                                if (Number.isFinite(usedIdx) && Number.isFinite(idx) && usedIdx !== idx) {
                                    throw new Error('Native OPD object mismatch: requested=' + String(idx) + ', used=' + String(usedIdx));
                                }
                            }
                            try {
                                const opdLine = '🧪 [PSF popup] OPD backend=' + String(nativeOpdResp?.backend || 'unknown') +
                                    ' reqObjIdx=' + (nativeOpdResp?.requestedObjectIndex ?? 'n/a') +
                                    ' usedObjIdx=' + Number(nativeOpdResp?.usedObjectIndex ?? -1) +
                                    ' usedObj=' + String(nativeOpdResp?.usedObjectPosition || 'n/a') +
                                    ' field=(' + Number(nativeOpdResp?.usedObjectX ?? 0) + ',' + Number(nativeOpdResp?.usedObjectY ?? 0) + ')' +
                                    ' hitCount=' + Number(nativeOpdResp?.hitCount || 0) +
                                    ' sampleCount=' + Number(nativeOpdResp?.sampleCount || 0);
                                console.log(opdLine);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                        window.opener.console.log(opdLine);
                                    }
                                } catch (_) {}
                            } catch (_) {}

                            try {
                                const enableOpdParityDiag = !!(typeof window !== 'undefined' && window.__PSF_POPUP_ENABLE_OPD_PARITY_DIAG === true);
                                if (!enableOpdParityDiag) {
                                    // Skip heavy JS re-computation parity diagnostics unless explicitly enabled.
                                    throw new Error('__skip_opd_parity_diag__');
                                }

                                const host = window.opener || window;
                                const calculatorFactory =
                                    (host && typeof host.createOPDCalculator === 'function' && host.createOPDCalculator)
                                    || (typeof window.createOPDCalculator === 'function' && window.createOPDCalculator)
                                    || (typeof createOPDCalculator === 'function' ? createOPDCalculator : null);
                                const analyzerFactory =
                                    (host && typeof host.createWavefrontAnalyzer === 'function' && host.createWavefrontAnalyzer)
                                    || (typeof window.createWavefrontAnalyzer === 'function' && window.createWavefrontAnalyzer)
                                    || (typeof createWavefrontAnalyzer === 'function' ? createWavefrontAnalyzer : null);
                                const opdCalculator = calculatorFactory ? calculatorFactory(opticalSystemRows, wavelength) : null;
                                const analyzer = analyzerFactory ? analyzerFactory(opdCalculator) : null;
                                if (analyzer && Array.isArray(nativeOpdResp?.displayOpdGrid)) {
                                    try {
                                        const nativeTarget = Number(nativeOpdResp?.targetSurface);
                                        const nativeStop = Number(nativeOpdResp?.stopSurface);
                                        const isFiniteFieldObj = !!(fieldSetting && String(fieldSetting.type || '').toLowerCase().includes('angle'));

                                        if (Number.isFinite(nativeStop) && nativeStop >= 0 && opdCalculator) {
                                            opdCalculator.stopSurfaceIndex = Math.floor(nativeStop);
                                        }
                                        if (Number.isFinite(nativeTarget) && nativeTarget >= 0 && opdCalculator) {
                                            opdCalculator.evaluationSurfaceIndex = Math.floor(nativeTarget);
                                        }
                                        if (opdCalculator) {
                                            const evalIdx = Number(opdCalculator.evaluationSurfaceIndex);
                                            const stopIdx = Number(opdCalculator.stopSurfaceIndex);
                                            const maxIdx = Math.max(
                                                Number.isFinite(evalIdx) ? evalIdx : 0,
                                                Number.isFinite(stopIdx) ? stopIdx : 0
                                            );
                                            opdCalculator.traceMaxSurfaceIndex = maxIdx;

                                            if (isFiniteFieldObj && typeof opdCalculator._setInfinitePupilMode === 'function') {
                                                const nativeMode = String(nativeOpdResp?.pupilSamplingMode || '').toLowerCase();
                                                opdCalculator._setInfinitePupilMode(fieldSetting, nativeMode === 'stop' ? 'stop' : 'entrance');
                                            }

                                            // Rebuild chief/reference after overriding indices/mode.
                                            opdCalculator.referenceOpticalPath = null;
                                            opdCalculator.lastFieldKey = null;
                                            if (typeof opdCalculator.setReferenceRay === 'function') {
                                                opdCalculator.setReferenceRay(fieldSetting);
                                            }
                                        }
                                    } catch (_) {}

                                    const jsDiagMap = await raceWithCancel(analyzer.generateWavefrontMap(fieldSetting, wavefrontGridSize, 'circular', {
                                        recordRays: false,
                                        progressEvery: 0,
                                        renderFromZernike: false,
                                        skipZernikeFit: true,
                                        opdMode: 'simple',
                                        opdDisplayMode,
                                        diagnoseDiscontinuities: false,
                                        cancelToken: activeCancelToken,
                                    }), activeCancelToken);

                                    if (jsDiagMap && !jsDiagMap.error) {
                                        const sDiag = Math.max(2, Math.floor(Number(wavefrontGridSize)));
                                        const jsGrid = Array.from({ length: sDiag }, () => Array(sDiag).fill(NaN));
                                        const coords = Array.isArray(jsDiagMap?.pupilCoordinates) ? jsDiagMap.pupilCoordinates : [];
                                        const jsOpdMicrons = (jsDiagMap?.display && Array.isArray(jsDiagMap.display.opds))
                                            ? jsDiagMap.display.opds
                                            : (Array.isArray(jsDiagMap?.opds) ? jsDiagMap.opds : []);
                                        const jsRawOpdMicrons = (jsDiagMap?.raw && Array.isArray(jsDiagMap.raw.opds))
                                            ? jsDiagMap.raw.opds
                                            : (Array.isArray(jsDiagMap?.opds) ? jsDiagMap.opds : []);
                                        const nDiag = Math.min(coords.length, jsOpdMicrons.length);
                                        for (let k = 0; k < nDiag; k++) {
                                            const c = coords[k];
                                            const ix = Number.isInteger(c?.ix) ? c.ix : null;
                                            const iy = Number.isInteger(c?.iy) ? c.iy : null;
                                            if (ix === null || iy === null) continue;
                                            if (ix < 0 || ix >= sDiag || iy < 0 || iy >= sDiag) continue;
                                            const rawV = jsOpdMicrons[k];
                                            if (rawV === null || rawV === undefined || rawV === '') continue;
                                            const v = Number(rawV);
                                            if (!Number.isFinite(v)) continue;
                                            jsGrid[iy][ix] = v;
                                        }

                                        const jsRawGrid = Array.from({ length: sDiag }, () => Array(sDiag).fill(NaN));
                                        const nRawDiag = Math.min(coords.length, jsRawOpdMicrons.length);
                                        for (let k = 0; k < nRawDiag; k++) {
                                            const c = coords[k];
                                            const ix = Number.isInteger(c?.ix) ? c.ix : null;
                                            const iy = Number.isInteger(c?.iy) ? c.iy : null;
                                            if (ix === null || iy === null) continue;
                                            if (ix < 0 || ix >= sDiag || iy < 0 || iy >= sDiag) continue;
                                            const rawV = jsRawOpdMicrons[k];
                                            if (rawV === null || rawV === undefined || rawV === '') continue;
                                            const v = Number(rawV);
                                            if (!Number.isFinite(v)) continue;
                                            jsRawGrid[iy][ix] = v;
                                        }

                                        let count = 0;
                                        let sumAbs = 0;
                                        let sumSq = 0;
                                        let maxAbs = 0;
                                        let sumA = 0;
                                        let sumB = 0;
                                        let sumBB = 0;
                                        let sumBA = 0;
                                        let maxIx = -1;
                                        let maxIy = -1;
                                        const diffGridWaves = Array.from({ length: sDiag }, () => Array(sDiag).fill(NaN));
                                        const radial = {
                                            center: { count: 0, sumAbs: 0, sumSq: 0 },
                                            mid: { count: 0, sumAbs: 0, sumSq: 0 },
                                            edge: { count: 0, sumAbs: 0, sumSq: 0 },
                                        };
                                        const cx = (sDiag - 1) / 2;
                                        const cy = (sDiag - 1) / 2;
                                        const rMax = Math.max(1, Math.hypot(cx, cy));
                                        for (let iy = 0; iy < sDiag; iy++) {
                                            const nRow = nativeOpdResp.displayOpdGrid[iy] || [];
                                            for (let ix = 0; ix < sDiag; ix++) {
                                                const rawA = nRow[ix];
                                                const rawB = jsGrid[iy][ix];
                                                if (rawA === null || rawA === undefined || rawA === '' || rawB === null || rawB === undefined || rawB === '') continue;
                                                const a = Number(rawA);
                                                const b = Number(rawB);
                                                if (!Number.isFinite(a) || !Number.isFinite(b)) continue;
                                                // Native OPD map is returned in waves, while JS map here is in microns.
                                                // Convert JS OPD to waves before parity comparison.
                                                const bWaves = b / wavelength;
                                                const dWaves = a - bWaves;
                                                diffGridWaves[iy][ix] = dWaves;
                                                const ad = Math.abs(dWaves);
                                                sumAbs += ad;
                                                sumSq += dWaves * dWaves;
                                                sumA += a;
                                                sumB += bWaves;
                                                sumBB += bWaves * bWaves;
                                                sumBA += bWaves * a;
                                                if (ad > maxAbs) {
                                                    maxAbs = ad;
                                                    maxIx = ix;
                                                    maxIy = iy;
                                                }
                                                const rn = Math.hypot(ix - cx, iy - cy) / rMax;
                                                const band = (rn < 0.35) ? radial.center : (rn < 0.7 ? radial.mid : radial.edge);
                                                band.count++;
                                                band.sumAbs += ad;
                                                band.sumSq += dWaves * dWaves;
                                                count++;
                                            }
                                        }

                                        if (count > 0) {
                                            let fitScale = NaN;
                                            let fitOffset = NaN;
                                            let fitResidualRms = NaN;
                                            if (count >= 2) {
                                                const denom = (count * sumBB) - (sumB * sumB);
                                                if (Number.isFinite(denom) && Math.abs(denom) > 1e-18) {
                                                    fitScale = ((count * sumBA) - (sumB * sumA)) / denom;
                                                    fitOffset = (sumA - fitScale * sumB) / count;

                                                    let fitSumSq = 0;
                                                    let fitCount = 0;
                                                    for (let iy = 0; iy < sDiag; iy++) {
                                                        const nRow2 = nativeOpdResp.displayOpdGrid[iy] || [];
                                                        const jRow2 = jsGrid[iy] || [];
                                                        for (let ix = 0; ix < sDiag; ix++) {
                                                            const rawA2 = nRow2[ix];
                                                            const rawB2 = jRow2[ix];
                                                            if (rawA2 === null || rawA2 === undefined || rawA2 === '' || rawB2 === null || rawB2 === undefined || rawB2 === '') continue;
                                                            const a2 = Number(rawA2);
                                                            const b2 = Number(rawB2);
                                                            if (!Number.isFinite(a2) || !Number.isFinite(b2)) continue;
                                                            const bw2 = b2 / wavelength;
                                                            const e2 = a2 - (fitScale * bw2 + fitOffset);
                                                            fitSumSq += e2 * e2;
                                                            fitCount++;
                                                        }
                                                    }
                                                    fitResidualRms = fitCount > 0 ? Math.sqrt(fitSumSq / fitCount) : NaN;
                                                }
                                            }

                                            let rawCount = 0;
                                            let rawSumAbs = 0;
                                            let rawSumSq = 0;
                                            let rawMaxAbs = 0;
                                            for (let iy = 0; iy < sDiag; iy++) {
                                                const nRowRaw = Array.isArray(nativeOpdResp.rawOpdGrid) ? (nativeOpdResp.rawOpdGrid[iy] || []) : [];
                                                const jRowRaw = jsRawGrid[iy] || [];
                                                for (let ix = 0; ix < sDiag; ix++) {
                                                    const rawARaw = nRowRaw[ix];
                                                    const rawBRaw = jRowRaw[ix];
                                                    if (rawARaw === null || rawARaw === undefined || rawARaw === '' || rawBRaw === null || rawBRaw === undefined || rawBRaw === '') continue;
                                                    const aRaw = Number(rawARaw);
                                                    const bRaw = Number(rawBRaw);
                                                    if (!Number.isFinite(aRaw) || !Number.isFinite(bRaw)) continue;
                                                    const dRawWaves = aRaw - (bRaw / wavelength);
                                                    const adRaw = Math.abs(dRawWaves);
                                                    rawSumAbs += adRaw;
                                                    rawSumSq += dRawWaves * dRawWaves;
                                                    if (adRaw > rawMaxAbs) rawMaxAbs = adRaw;
                                                    rawCount++;
                                                }
                                            }

                                            const rawDiag = (rawCount > 0)
                                                ? {
                                                    count: rawCount,
                                                    rmsWaves: Math.sqrt(rawSumSq / rawCount),
                                                    meanAbsWaves: rawSumAbs / rawCount,
                                                    maxAbsWaves: rawMaxAbs,
                                                }
                                                : null;

                                            const maxAllowedRmsWaves = (() => {
                                                const v = Number((typeof window !== 'undefined')
                                                    ? window.__PSF_POPUP_NATIVE_OPD_PARITY_MAX_RMS_WAVES
                                                    : NaN);
                                                return (Number.isFinite(v) && v > 0) ? v : 5e-2;
                                            })();
                                            nativeOpdDiag = {
                                                count,
                                                rmsWaves: Math.sqrt(sumSq / count),
                                                meanAbsWaves: sumAbs / count,
                                                maxAbsWaves: maxAbs,
                                                maxLoc: { ix: maxIx, iy: maxIy },
                                                radialBands: {
                                                    center: {
                                                        count: radial.center.count,
                                                        rmsWaves: radial.center.count > 0 ? Math.sqrt(radial.center.sumSq / radial.center.count) : NaN,
                                                        meanAbsWaves: radial.center.count > 0 ? (radial.center.sumAbs / radial.center.count) : NaN,
                                                    },
                                                    mid: {
                                                        count: radial.mid.count,
                                                        rmsWaves: radial.mid.count > 0 ? Math.sqrt(radial.mid.sumSq / radial.mid.count) : NaN,
                                                        meanAbsWaves: radial.mid.count > 0 ? (radial.mid.sumAbs / radial.mid.count) : NaN,
                                                    },
                                                    edge: {
                                                        count: radial.edge.count,
                                                        rmsWaves: radial.edge.count > 0 ? Math.sqrt(radial.edge.sumSq / radial.edge.count) : NaN,
                                                        meanAbsWaves: radial.edge.count > 0 ? (radial.edge.sumAbs / radial.edge.count) : NaN,
                                                    },
                                                },
                                                fit: {
                                                    scale: fitScale,
                                                    offsetWaves: fitOffset,
                                                    residualRmsWaves: fitResidualRms,
                                                },
                                                rawParity: rawDiag,
                                            };
                                            const diagLine = '📐 [PSF popup][OPD parity] Native-vs-JS count=' + count +
                                                ' rms=' + Number(nativeOpdDiag.rmsWaves).toExponential(3) + 'λ' +
                                                ' meanAbs=' + Number(nativeOpdDiag.meanAbsWaves).toExponential(3) + 'λ' +
                                                ' maxAbs=' + Number(nativeOpdDiag.maxAbsWaves).toExponential(3) + 'λ' +
                                                ' @(' + String(maxIx) + ',' + String(maxIy) + ')' +
                                                ' fitScale=' + (Number.isFinite(fitScale) ? Number(fitScale).toExponential(3) : 'NaN') +
                                                ' fitOffset=' + (Number.isFinite(fitOffset) ? Number(fitOffset).toExponential(3) : 'NaN') + 'λ' +
                                                ' fitResidual=' + (Number.isFinite(fitResidualRms) ? Number(fitResidualRms).toExponential(3) : 'NaN') + 'λ' +
                                                (rawDiag
                                                    ? (' | raw rms=' + Number(rawDiag.rmsWaves).toExponential(3) + 'λ meanAbs=' + Number(rawDiag.meanAbsWaves).toExponential(3) + 'λ maxAbs=' + Number(rawDiag.maxAbsWaves).toExponential(3) + 'λ')
                                                    : '');
                                            const hasReliableRawParity = !!(rawDiag && Number.isFinite(Number(rawDiag.rmsWaves)) && Number(rawDiag.count) >= 64);
                                            const hasReliableDisplayParity = Number.isFinite(Number(nativeOpdDiag.rmsWaves)) && Number(count) >= 64;
                                            const parityForFallback = hasReliableRawParity
                                                ? Number(rawDiag.rmsWaves)
                                                : (hasReliableDisplayParity ? Number(nativeOpdDiag.rmsWaves) : NaN);
                                            const shouldFallbackToJsOpd = Number.isFinite(parityForFallback)
                                                ? (parityForFallback > maxAllowedRmsWaves)
                                                : false;

                                            if (shouldFallbackToJsOpd) {
                                                console.warn('⚠️ ' + diagLine);
                                            } else {
                                                console.log(diagLine);
                                            }
                                            try {
                                                if (window.opener && window.opener.console) {
                                                    const logFn = shouldFallbackToJsOpd
                                                        ? window.opener.console.warn
                                                        : window.opener.console.log;
                                                    if (typeof logFn === 'function') {
                                                        logFn.call(window.opener.console, (shouldFallbackToJsOpd ? '⚠️ ' : '') + diagLine);
                                                    }
                                                }
                                            } catch (_) {}

                                            try {
                                                window['__lastOpdParityDiffWaves'] = diffGridWaves;
                                                window['__lastOpdParitySummary'] = nativeOpdDiag;
                                                window['__lastOpdParityRawSummary'] = rawDiag;
                                                if (window.opener) {
                                                    window.opener['__lastOpdParitySummary'] = nativeOpdDiag;
                                                    window.opener['__lastOpdParityRawSummary'] = rawDiag;
                                                }
                                            } catch (_) {}

                                            await renderOpdParityHeatmap(diffGridWaves, nativeOpdDiag);

                                            if (shouldFallbackToJsOpd) {
                                                try {
                                                    const fbMsg = '⚠️ [PSF popup] Native OPD parity exceeded threshold (rms=' +
                                                        Number(parityForFallback).toExponential(3) + 'λ > ' +
                                                        Number(maxAllowedRmsWaves).toExponential(3) + 'λ). Falling back to JS OPD.';
                                                    console.warn(fbMsg);
                                                    if (window.opener && window.opener.console && typeof window.opener.console.warn === 'function') {
                                                        window.opener.console.warn(fbMsg);
                                                    }
                                                } catch (_) {}
                                                nativeOpdResp = null;
                                            }
                                        }
                                    }
                                }
                            } catch (diagErr) {
                                try {
                                    if (String(diagErr?.message || '') !== '__skip_opd_parity_diag__') {
                                        // Non-fatal diagnostic path failure should not block native pipeline.
                                        console.warn('⚠️ [PSF popup] OPD parity diagnostic skipped due to error:', diagErr);
                                    }
                                } catch (_) {}
                            }

                            if (nativeOpdResp) {
                                const nativePupilMode = String(nativeOpdResp?.pupilSamplingMode || '').toLowerCase();
                                wavefrontMap = {
                                    pupilSamplingMode: nativePupilMode === 'stop' ? 'stop' : 'entrance',
                                    pupilPhysicalRadiusMm: NaN,
                                    entranceEffectiveRadiusMm: NaN,
                                    pupilRange: 1.0,
                                    pupilCoordinates: [],
                                    display: { opds: [] },
                                    opds: []
                                };
                            }
                        } catch (nativeErr) {
                            nativeOpdResp = null;
                            try {
                                const msg = '⚠️ [PSF popup] Native OPD failed, falling back to JS OPD: ' + String(nativeErr?.message || nativeErr || 'unknown');
                                console.warn(msg);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.warn === 'function') {
                                        window.opener.console.warn(msg);
                                    }
                                } catch (_) {}
                            } catch (_) {}
                        }
                    }

                    if (!nativeOpdResp) {
                        const host = window.opener || window;
                        const calculatorFactory =
                            (host && typeof host.createOPDCalculator === 'function' && host.createOPDCalculator)
                            || (typeof window.createOPDCalculator === 'function' && window.createOPDCalculator)
                            || (typeof createOPDCalculator === 'function' ? createOPDCalculator : null);
                        const analyzerFactory =
                            (host && typeof host.createWavefrontAnalyzer === 'function' && host.createWavefrontAnalyzer)
                            || (typeof window.createWavefrontAnalyzer === 'function' && window.createWavefrontAnalyzer)
                            || (typeof createWavefrontAnalyzer === 'function' ? createWavefrontAnalyzer : null);
                        const opdCalculator = calculatorFactory ? calculatorFactory(opticalSystemRows, wavelength) : null;
                        if (!opdCalculator) {
                            throw new Error('createOPDCalculator is not available');
                        }
                        const analyzer = analyzerFactory ? analyzerFactory(opdCalculator) : null;
                        if (!analyzer) {
                            throw new Error('WavefrontAberrationAnalyzer is not available');
                        }

                        wavefrontMap = await raceWithCancel(analyzer.generateWavefrontMap(fieldSetting, wavefrontGridSize, 'circular', {
                            recordRays: false,
                            progressEvery: 0,
                            renderFromZernike: false,
                            skipZernikeFit: true,
                            opdMode: 'simple',
                            opdDisplayMode,
                            diagnoseDiscontinuities: PSF_DEBUG,
                            diagTopK: 8,
                            cancelToken: activeCancelToken,
                            onProgress: (evt) => {
                                const p = Number(evt && evt.percent);
                                const msg = (evt && evt.message) || (evt && evt.phase) || 'OPD...';
                                const phase = (evt && evt.phase) ? evt.phase : 'opd';
                                if (Number.isFinite(p)) onProgress({ percent: Math.max(0, Math.min(80, p * 0.8)), phase, message: msg });
                                else onProgress({ percent: null, phase, message: msg });
                            }
                        }), activeCancelToken);
                        throwIfCancelled(activeCancelToken);

                        if (wavefrontMap && wavefrontMap.error) {
                            const err = new Error((wavefrontMap.error && wavefrontMap.error.message) ? wavefrontMap.error.message : 'Wavefront generation failed (popup).');
                            err.code = 'WAVEFRONT_UNAVAILABLE';
                            err.wavefrontError = wavefrontMap.error;
                            throw err;
                        }
                    }

                    if (PSF_DEBUG) {
                    }
                    
                    // CRITICAL: Use actual entrance pupil radius for spatial frequency scaling
                    // In entrance pupil mode, pupilPhysicalRadiusMm (stop radius) != actual entrance pupil radius
                    const actualPupilRadiusMm = (wavefrontMap.pupilSamplingMode === 'entrance' && 
                                                  Number.isFinite(wavefrontMap.entranceEffectiveRadiusMm))
                        ? wavefrontMap.entranceEffectiveRadiusMm
                        : wavefrontMap.pupilPhysicalRadiusMm;
                    

                    // Convert to PSF calculator format (gridData)
                    // Build OPD grid from the wavefront map samples (piston+tilt removed display OPD).
                    const gridSize = wavefrontGridSize;
                    const s = Math.max(2, Math.floor(Number(gridSize)));
                    const opdGrid = Array.from({ length: s }, () => new Float32Array(s));
                    const ampGrid = Array.from({ length: s }, () => new Float32Array(s));
                    const maskGrid = Array.from({ length: s }, () => Array(s).fill(false));
                    const xCoords = new Float32Array(s);
                    const yCoords = new Float32Array(s);

                    const pupilRange = (Number.isFinite(Number(wavefrontMap?.pupilRange)) && Number(wavefrontMap.pupilRange) > 0)
                        ? Number(wavefrontMap.pupilRange)
                        : 1.0;
                    for (let i = 0; i < s; i++) {
                        const t = (i / (s - 1 || 1)) * 2 - 1;
                        xCoords[i] = t * pupilRange;
                        yCoords[i] = t * pupilRange;
                    }

                    let rayData = [];
                    let coords = [];
                    let opdMicrons = [];
                    if (nativeOpdResp && Array.isArray(nativeOpdResp.displayOpdGrid)) {
                        for (let iy = 0; iy < s; iy++) {
                            const rowDisplay = nativeOpdResp.displayOpdGrid[iy] || [];
                            const rowRaw = Array.isArray(nativeOpdResp.rawOpdGrid) ? (nativeOpdResp.rawOpdGrid[iy] || []) : [];
                            for (let ix = 0; ix < s; ix++) {
                                const rawCell = rowRaw[ix];
                                if (rawCell === null || rawCell === undefined || rawCell === '') continue;
                                const vRawWaves = Number(rawCell);
                                // IMPORTANT: Use raw OPD occupancy as the pupil validity mask.
                                // Native display grid can be detrended/fitted and become dense, which
                                // incorrectly turns PSF pupil mask into full grid.
                                if (!Number.isFinite(vRawWaves)) continue;

                                const displayCell = rowDisplay[ix];
                                const vDisplayWaves = (displayCell === null || displayCell === undefined || displayCell === '')
                                    ? NaN
                                    : Number(displayCell);
                                const vWaves = Number.isFinite(vDisplayWaves) ? vDisplayWaves : vRawWaves;
                                // Native OPD map grid values are in waves; PSF calculator expects microns.
                                const vMicrons = vWaves * wavelength;
                                coords.push({ ix, iy, x: Number(xCoords[ix]), y: Number(yCoords[iy]) });
                                opdMicrons.push(vMicrons);
                            }
                        }
                    } else {
                        coords = Array.isArray(wavefrontMap?.pupilCoordinates) ? wavefrontMap.pupilCoordinates : [];
                        opdMicrons = (wavefrontMap?.display && Array.isArray(wavefrontMap.display.opds))
                            ? wavefrontMap.display.opds
                            : (Array.isArray(wavefrontMap?.opds) ? wavefrontMap.opds : []);
                    }

                    const n = Math.min(coords.length, opdMicrons.length);
                    for (let k = 0; k < n; k++) {
                        const c = coords[k];
                        const ix = Number.isInteger(c?.ix) ? c.ix : null;
                        const iy = Number.isInteger(c?.iy) ? c.iy : null;
                        if (ix === null || iy === null) continue;
                        if (ix < 0 || ix >= s || iy < 0 || iy >= s) continue;
                        const vMicrons = Number(opdMicrons[k]);
                        if (!Number.isFinite(vMicrons)) continue;
                        maskGrid[iy][ix] = true;
                        opdGrid[iy][ix] = vMicrons;
                        ampGrid[iy][ix] = 1.0;
                    }

                    for (let k = 0; k < n; k++) {
                        const c = coords[k];
                        const x = Number(c?.x);
                        const y = Number(c?.y);
                        const vMicrons = Number(opdMicrons[k]);
                        if (!Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(vMicrons)) continue;
                        rayData.push({
                            pupilX: x,
                            pupilY: y,
                            opd: vMicrons,
                            isVignetted: false
                        });
                    }

                    let filledCount = 0;
                    for (let iy = 0; iy < s; iy++) {
                        for (let ix = 0; ix < s; ix++) {
                            if (maskGrid[iy][ix]) filledCount++;
                        }
                    }

                    const opdData = {
                        gridSize: s,
                        wavelength: wavelength,
                        rayData,
                        gridData: {
                            opd: opdGrid,
                            amplitude: ampGrid,
                            pupilMask: maskGrid,
                            xCoords,
                            yCoords
                        }
                    };

                    if (PSF_DEBUG) try {
                        let valid = 0;
                        let sum = 0;
                        let sum2 = 0;
                        let min = Infinity;
                        let max = -Infinity;
                        for (let iy = 0; iy < s; iy++) {
                            for (let ix = 0; ix < s; ix++) {
                                if (!maskGrid[iy][ix]) continue;
                                const v = Number(opdGrid[iy][ix]);
                                if (!Number.isFinite(v)) continue;
                                valid++;
                                sum += v;
                                sum2 += v * v;
                                if (v < min) min = v;
                                if (v > max) max = v;
                            }
                        }
                        const mean = valid ? (sum / valid) : NaN;
                        const rms = valid ? Math.sqrt(Math.max(0, sum2 / valid - mean * mean)) : NaN;
                        const ptp = (Number.isFinite(min) && Number.isFinite(max)) ? (max - min) : NaN;
                        const rmsW = (Number.isFinite(rms) && Number.isFinite(wavelength) && wavelength > 0) ? (rms / wavelength) : NaN;
                        const ptpW = (Number.isFinite(ptp) && Number.isFinite(wavelength) && wavelength > 0) ? (ptp / wavelength) : NaN;
                        const line = '📌 [PSF popup] OPD grid stats: valid=' + valid + '/' + (s * s) +
                            ' (' + (100 * valid / (s * s)).toFixed(1) + '%)' +
                            ' rms=' + (Number.isFinite(rms) ? rms.toExponential(3) : String(rms)) + 'µm' +
                            ' (' + (Number.isFinite(rmsW) ? rmsW.toExponential(3) : String(rmsW)) + 'λ)' +
                            ' ptp=' + (Number.isFinite(ptp) ? ptp.toExponential(3) : String(ptp)) + 'µm' +
                            ' (' + (Number.isFinite(ptpW) ? ptpW.toExponential(3) : String(ptpW)) + 'λ)';
                        console.log(line);
                        try {
                            if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                window.opener.console.log(line);
                            }
                        } catch (_) {}
                        if (wavefrontMap?.pupilMaskStats) {
                            console.log('📌 [PSF popup] pupilMaskStats:', wavefrontMap.pupilMaskStats);
                        }

                        try {
                            const mode = wavefrontMap && wavefrontMap.pupilSamplingMode;
                            const bestEffort = !!(wavefrontMap && wavefrontMap.bestEffortVignettedPupil);
                            if (mode) {
                                const msg = '📌 [PSF popup] pupilSamplingMode=' + String(mode) + (bestEffort ? ' (bestEffortVignettedPupil=true)' : '');
                                console.log(msg);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                        window.opener.console.log(msg);
                                    }
                                } catch (_) {}
                            }

                            const reasons = (wavefrontMap && wavefrontMap.invalidReasonCounts) ? wavefrontMap.invalidReasonCounts : null;
                            const top = reasons
                                ? Object.entries(reasons).sort((a, b) => ((b && b[1]) || 0) - ((a && a[1]) || 0)).slice(0, 8)
                                : [];
                            if (top.length) {
                                const msg = '📌 [PSF popup] invalid reasons top: ' + top.map((kv) => String(kv && kv[0]) + ':' + String(kv && kv[1])).join(', ');
                                console.log(msg);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                        window.opener.console.log(msg);
                                    }
                                } catch (_) {}
                            }
                        } catch (_) {}
                    } catch (_) {}

                    let pupilDiameterMm = actualPupilRadiusMm * 2;
                    let focalLengthMm = Number.NaN;
                    let stopIndexForLog = -1;

                    try {
                        const si = Number(findStopSurfaceIndex(opticalSystemRows));
                        if (Number.isFinite(si)) stopIndexForLog = si;
                    } catch (_) {}

                    try {
                        const diffParams = calculateImageSpaceDiffractionParams(opticalSystemRows, wavelength);
                        const fWork = Number(diffParams?.fNumberWorking);
                        const derivedFocal = Number(diffParams?.focalLengthMm);

                        if (Number.isFinite(fWork) && fWork > 0 && Number.isFinite(derivedFocal) && derivedFocal > 0) {
                            focalLengthMm = Math.abs(derivedFocal);
                            pupilDiameterMm = focalLengthMm / fWork;
                        }
                    } catch (_) {}

                    // Fallback to stop diameter only when paraxial/image-space data is unavailable.
                    if (!(Number.isFinite(pupilDiameterMm) && pupilDiameterMm > 0)) {
                        try {
                            const si = Number((window.opener && typeof window.opener.findStopSurfaceIndex === 'function')
                                ? window.opener.findStopSurfaceIndex(opticalSystemRows)
                                : findStopSurfaceIndex(opticalSystemRows));
                            const stopRow = (Number.isFinite(si) && si >= 0) ? opticalSystemRows?.[si] : null;
                            const sdRaw = stopRow?.semidia ?? stopRow?.Semidia ?? stopRow?.['Semi Diameter'] ?? stopRow?.aperture ?? stopRow?.Aperture ?? NaN;
                            const sd = Math.abs(parseFloat(sdRaw));
                            if (Number.isFinite(sd) && sd > 0) {
                                const isApertureField = !!(stopRow && (stopRow.aperture !== undefined || stopRow.Aperture !== undefined));
                                const stopRadiusMm = isApertureField ? (sd * 0.5) : sd;
                                if (Number.isFinite(stopRadiusMm) && stopRadiusMm > 0) {
                                    pupilDiameterMm = stopRadiusMm * 2;
                                }
                            }
                        } catch (_) {}
                    }

                    if (!(Number.isFinite(focalLengthMm) && focalLengthMm > 0)) {
                        // Last-resort safeguard to keep FFT scaling defined even when paraxial FL is unavailable.
                        focalLengthMm = 100.0;
                    }

                    logScaleInputs(pupilDiameterMm, focalLengthMm, stopIndexForLog);

                    const psfSamplingSize = Number.isFinite(zernikeSampling) ? zernikeSampling : 128;
                    const zeroPadTo = (zeroPadRaw === 'none')
                        ? psfSamplingSize
                        : (zeroPadRaw === 'auto')
                            ? 0
                            : (Number.isFinite(parseInt(zeroPadRaw)) ? parseInt(zeroPadRaw) : 0);
                    const canUseNativePsfMap = !!(window.opener && typeof window.opener.runDesktopNativePsfMapForPopup === 'function');
                    if (!canUseNativePsfMap) {
                        throw new Error('Native Rust PSF map path is required but unavailable.');
                    }

                    const minRecommendedFftSize = 512;
                    const requestedFftSize = (!zeroPadTo || zeroPadTo === 0)
                        ? Math.max(psfSamplingSize, minRecommendedFftSize)
                        : Math.max(psfSamplingSize, zeroPadTo);
                    const basePixelPitchUm = (Number(wavelength) * Math.abs(Number(focalLengthMm))) / Math.max(1e-12, Math.abs(Number(pupilDiameterMm)));
                    const pixelSizeUm = basePixelPitchUm * (psfSamplingSize / requestedFftSize);

                    onProgress({ percent: 80, phase: 'psf', message: 'PSF (native)...' });
                    const nativePsfResp = await raceWithCancel(window.opener.runDesktopNativePsfMapForPopup({
                        gridOpd: Array.from({ length: s }, (_, iy) => Array.from(opdGrid[iy] || [])),
                        gridAmplitude: Array.from({ length: s }, (_, iy) => Array.from(ampGrid[iy] || [])),
                        pupilMask: Array.from({ length: s }, (_, iy) => Array.from(maskGrid[iy] || [])),
                        wavelengthUm: wavelength,
                        pixelSizeUm,
                        removeTilt: false,
                        zeroPadTo: requestedFftSize,
                        recenterIfWrapped: false,
                    }), activeCancelToken);
                    const psfResult = {
                        psfData: nativePsfResp?.psfData,
                        metrics: nativePsfResp?.metrics,
                        samplingSize: psfSamplingSize,
                        wavelength,
                        gridData: opdData?.gridData,
                        options: { pupilDiameter: pupilDiameterMm, focalLength: focalLengthMm, pixelSize: pixelSizeUm },
                        metadata: {
                            method: 'native-rust-psf-map',
                            backend: nativePsfResp?.backend,
                            samplingSize: psfSamplingSize,
                            fftSize: nativePsfResp?.fftSize,
                            wavelength,
                            pixelSize: pixelSizeUm,
                        },
                        implementationUsed: 'NativeRust',
                    };
                    throwIfCancelled(activeCancelToken);

                    setPopupPsfBadge('');

                    try {
                        const impl = String(psfResult?.implementationUsed || 'unknown');
                        const method = String(psfResult?.metadata?.method || '');
                        const implLine = '🧪 [PSF popup] implementation=' + impl + (method ? (' method=' + method) : '');
                        const pixelSizeUm = Number(psfResult?.options?.pixelSize);
                        const scaleLine = '📏 [PSF popup] scale pupilDiameterMm=' + Number(pupilDiameterMm).toFixed(6)
                            + ' focalLengthMm=' + Number(focalLengthMm).toFixed(6)
                            + ' pixelSizeUm=' + (Number.isFinite(pixelSizeUm) ? pixelSizeUm.toFixed(6) : 'n/a');
                        console.log(implLine);
                        console.log(scaleLine);
                        try {
                            window['__lastPsfImplLine'] = implLine;
                            window['__lastPsfScaleLine'] = scaleLine;
                        } catch (_) {}
                        try {
                            if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                window.opener.console.log(implLine);
                                window.opener.console.log(scaleLine);
                            }
                        } catch (_) {}

                        const statsEl = document.getElementById('popup-psf-container-stats');
                        if (statsEl) {
                            statsEl.innerHTML = '';
                            statsEl.textContent = '';
                            statsEl.style.display = 'none';
                            if (nativeOpdDiag && Number.isFinite(nativeOpdDiag.rmsWaves)) {
                                const maxAllowedRmsWaves = (() => {
                                    const v = Number((typeof window !== 'undefined')
                                        ? window.__PSF_POPUP_NATIVE_OPD_PARITY_MAX_RMS_WAVES
                                        : NaN);
                                    return (Number.isFinite(v) && v > 0) ? v : 5e-2;
                                })();
                                const warn = Number(nativeOpdDiag.rmsWaves) > maxAllowedRmsWaves;
                                const prefix = warn ? '⚠️ ' : 'ℹ️ ';
                                const centerRms = Number(nativeOpdDiag?.radialBands?.center?.rmsWaves);
                                const midRms = Number(nativeOpdDiag?.radialBands?.mid?.rmsWaves);
                                const edgeRms = Number(nativeOpdDiag?.radialBands?.edge?.rmsWaves);
                                const maxIx = Number(nativeOpdDiag?.maxLoc?.ix);
                                const maxIy = Number(nativeOpdDiag?.maxLoc?.iy);
                                const fitScale = Number(nativeOpdDiag?.fit?.scale);
                                const fitOffset = Number(nativeOpdDiag?.fit?.offsetWaves);
                                const fitResidual = Number(nativeOpdDiag?.fit?.residualRmsWaves);
                                const rawRms = Number(nativeOpdDiag?.rawParity?.rmsWaves);
                                const rawMean = Number(nativeOpdDiag?.rawParity?.meanAbsWaves);
                                const rawMax = Number(nativeOpdDiag?.rawParity?.maxAbsWaves);
                                const bandMsg = ' bands(rms λ): C=' + (Number.isFinite(centerRms) ? centerRms.toExponential(2) : 'n/a')
                                    + ' M=' + (Number.isFinite(midRms) ? midRms.toExponential(2) : 'n/a')
                                    + ' E=' + (Number.isFinite(edgeRms) ? edgeRms.toExponential(2) : 'n/a');
                                const fitMsg = ' fit: s=' + (Number.isFinite(fitScale) ? fitScale.toExponential(2) : 'n/a')
                                    + ' b=' + (Number.isFinite(fitOffset) ? fitOffset.toExponential(2) : 'n/a') + 'λ'
                                    + ' rmsRes=' + (Number.isFinite(fitResidual) ? fitResidual.toExponential(2) : 'n/a') + 'λ';
                                const rawMsg = (Number.isFinite(rawRms) || Number.isFinite(rawMean) || Number.isFinite(rawMax))
                                    ? (' raw(rms/mean/max λ)=' +
                                        (Number.isFinite(rawRms) ? rawRms.toExponential(2) : 'n/a') + '/' +
                                        (Number.isFinite(rawMean) ? rawMean.toExponential(2) : 'n/a') + '/' +
                                        (Number.isFinite(rawMax) ? rawMax.toExponential(2) : 'n/a'))
                                    : '';
                                const msg = prefix + 'OPD parity (Native vs JS): ' +
                                    'rms=' + Number(nativeOpdDiag.rmsWaves).toExponential(3) + 'λ, ' +
                                    'meanAbs=' + Number(nativeOpdDiag.meanAbsWaves).toExponential(3) + 'λ, ' +
                                    'maxAbs=' + Number(nativeOpdDiag.maxAbsWaves).toExponential(3) + 'λ@(' + (Number.isFinite(maxIx) ? String(maxIx) : '?') + ',' + (Number.isFinite(maxIy) ? String(maxIy) : '?') + ')' +
                                    bandMsg + fitMsg + rawMsg;
                                void msg;
                                statsEl.style.color = warn ? '#b91c1c' : '#334155';
                            }
                        }
                    } catch (_) {}

                    try {
                        const enablePsfParityDiag = !!(typeof window !== 'undefined' && window.__PSF_POPUP_ENABLE_PSF_PARITY_DIAG === true);
                        if (!enablePsfParityDiag) {
                            throw new Error('__skip_psf_parity_diag__');
                        }

                        const isNative = String(psfResult?.implementationUsed || '').toLowerCase() === 'nativerust';
                        const parityEnabled = isPopupTauriRuntime() && isNative;
                        const parityRan = !!window['__psfNativeParityDone'];
                        if (parityEnabled && !parityRan) {
                            window['__psfNativeParityDone'] = true;
                            void (async () => {
                                try {
                                    const psfCalculator = (() => {
                                        const host = window.opener || window;
                                        const PsfCalculatorCtor =
                                            (host && host.PSFCalculator)
                                            || window.PSFCalculator;
                                        if (typeof PsfCalculatorCtor !== 'function') {
                                            throw new Error('PSFCalculator is not available');
                                        }
                                        return new PsfCalculatorCtor();
                                    })();
                                    const jsResult = await psfCalculator.calculatePSF(opdData, {
                                        samplingSize: psfSamplingSize,
                                        wavelength,
                                        zeroPadTo,
                                        pupilDiameter: pupilDiameterMm,
                                        focalLength: focalLengthMm,
                                        forceImplementation: 'javascript',
                                        removeTilt: false,
                                    });

                                    const nativeGrid = Array.isArray(psfResult?.psfData) ? psfResult.psfData : [];
                                    const jsGrid = Array.isArray(jsResult?.psfData) ? jsResult.psfData : [];
                                    const h = Math.min(nativeGrid.length, jsGrid.length);
                                    const w = h > 0 ? Math.min((nativeGrid[0] || []).length, (jsGrid[0] || []).length) : 0;

                                    let n = 0;
                                    let sumSq = 0;
                                    let maxAbs = 0;
                                    let sumAbs = 0;
                                    for (let iy = 0; iy < h; iy++) {
                                        for (let ix = 0; ix < w; ix++) {
                                            const a = Number(nativeGrid[iy]?.[ix]);
                                            const b = Number(jsGrid[iy]?.[ix]);
                                            if (!Number.isFinite(a) || !Number.isFinite(b)) continue;
                                            const d = a - b;
                                            const ad = Math.abs(d);
                                            sumSq += d * d;
                                            sumAbs += ad;
                                            if (ad > maxAbs) maxAbs = ad;
                                            n++;
                                        }
                                    }

                                    const rmsAbs = n > 0 ? Math.sqrt(sumSq / n) : NaN;
                                    const meanAbs = n > 0 ? (sumAbs / n) : NaN;

                                    const nMetrics = psfResult?.metrics || {};
                                    const jMetrics = jsResult?.metrics || {};
                                    const fwhmNative = Number(nMetrics?.fwhm?.average);
                                    const fwhmJs = Number(jMetrics?.fwhm?.average);
                                    const strehlNative = Number(nMetrics?.strehlRatio);
                                    const strehlJs = Number(jMetrics?.strehlRatio);
                                    const centerNative = nMetrics?.centerPosition || {};
                                    const centerJs = jMetrics?.centerPosition || {};

                                    const parityLine =
                                        '📊 [PSF Parity] native-vs-js ' +
                                        'grid=' + h + 'x' + w + ' n=' + n + ' ' +
                                        'rmsAbs=' + (Number.isFinite(rmsAbs) ? rmsAbs.toExponential(4) : 'NaN') + ' ' +
                                        'meanAbs=' + (Number.isFinite(meanAbs) ? meanAbs.toExponential(4) : 'NaN') + ' ' +
                                        'maxAbs=' + (Number.isFinite(maxAbs) ? maxAbs.toExponential(4) : 'NaN') + ' ' +
                                        'strehlΔ=' + (strehlNative - strehlJs).toExponential(4) + ' ' +
                                        'fwhmAvgΔ=' + (fwhmNative - fwhmJs).toExponential(4) + 'µm ' +
                                        'centerΔ=(' + (Number(centerNative?.x) - Number(centerJs?.x)) + ',' + (Number(centerNative?.y) - Number(centerJs?.y)) + ')';

                                    const paritySummary = {
                                        grid: { h, w, n },
                                        abs: {
                                            rms: Number.isFinite(rmsAbs) ? rmsAbs : null,
                                            mean: Number.isFinite(meanAbs) ? meanAbs : null,
                                            max: Number.isFinite(maxAbs) ? maxAbs : null,
                                        },
                                        delta: {
                                            strehl: Number.isFinite(strehlNative) && Number.isFinite(strehlJs) ? (strehlNative - strehlJs) : null,
                                            fwhmAvgUm: Number.isFinite(fwhmNative) && Number.isFinite(fwhmJs) ? (fwhmNative - fwhmJs) : null,
                                            centerX: Number(centerNative?.x) - Number(centerJs?.x),
                                            centerY: Number(centerNative?.y) - Number(centerJs?.y),
                                        },
                                    };

                                    try {
                                        window['__lastPsfParityLine'] = parityLine;
                                        window['__lastPsfParity'] = paritySummary;
                                        if (window.opener) {
                                            window.opener['__lastPsfParityLine'] = parityLine;
                                            window.opener['__lastPsfParity'] = paritySummary;
                                        }
                                    } catch (_) {}

                                    console.log(parityLine);
                                    try {
                                        if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                            window.opener.console.log(parityLine);
                                        }
                                    } catch (_) {}
                                } catch (parityErr) {
                                    console.warn('⚠️ [PSF Parity] native-vs-js compare failed:', parityErr);
                                }
                            })();
                        }
                    } catch (parityDiagErr) {
                        try {
                            if (String(parityDiagErr?.message || '') !== '__skip_psf_parity_diag__') {
                                console.warn('⚠️ [PSF popup] PSF parity diagnostic skipped due to error:', parityDiagErr);
                            }
                        } catch (_) {}
                    }

                    const plotter = (() => {
                        if (window.opener && window.opener.PSFPlotter) {
                            return new window.opener.PSFPlotter(containerEl);
                        }
                        throw new Error('PSFPlotter not available from opener window');
                    })();
                    await plotter.plot2DPSF(psfResult, { logScale, title: '', recenterToCentroid: false });
                }

                hideProgress();
                resizePlot();
                hideProgress();
            } catch (err) {
                setPopupPsfBadge('Error');
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const msg = String((err && err.message) || err || 'Unknown error');
                    const stack = String((err && err.stack) || '');
                    const escapeHtml = (s) => String(s).replace(/[&<>"']/g, (ch) => {
                        switch (ch) {
                            case '&': return '&amp;';
                            case '<': return '&lt;';
                            case '>': return '&gt;';
                            case '"': return '&quot;';
                            case "'": return '&#39;';
                            default: return ch;
                        }
                    });
                    const details = escapeHtml(msg + (stack ? '\\n\\n' + stack : ''));
                    containerEl.innerHTML =
                        '<div style="padding:20px;color:red;font-family:Arial;">' +
                            '<div style="font-weight:bold;margin-bottom:8px;">Failed to generate PSF</div>' +
                            '<pre style="white-space:pre-wrap;word-break:break-word;">' + details + '</pre>' +
                        '</div>';
                }
            } finally {
                try {
                    if (stopBtn) stopBtn.disabled = true;
                } catch (_) {}
            }
        };

        document.getElementById('popup-show-psf-btn').addEventListener('click', () => window.renderPSF());

        document.getElementById('popup-stop-psf-btn').addEventListener('click', () => {
            try {
                const el = document.getElementById('popup-psf-pipeline-badge');
                if (el) el.textContent = '';
            } catch (_) {}
            try {
                if (activeCancelToken && typeof activeCancelToken.abort === 'function') {
                    activeCancelToken.abort('Stopped by user');
                }
            } catch (_) {}
        });

        function syncAll() {
            syncObjectOptionsFromOpener();
            syncInputsFromOpener();
        }
        // Expose for opener-triggered refresh when reusing an existing popup window.
        window['syncAll'] = syncAll;
        window['syncObjectOptionsFromOpener'] = syncObjectOptionsFromOpener;
        window.addEventListener('resize', resizePlot);
        window.addEventListener('focus', syncAll);
        syncAll();

        // Do not auto-render on open; user triggers calculation via "Show PSF".
        window.addEventListener('load', () => {
            try {
                const popupSampling = document.getElementById('popup-psf-sampling-select');
                const popupZernikeSampling = document.getElementById('popup-psf-zernike-sampling-select');
                const popupLog = document.getElementById('popup-psf-log-scale-checkbox');
                if (popupSampling && !popupSampling.value) popupSampling.value = 'auto';
                if (popupZernikeSampling && !popupZernikeSampling.value) popupZernikeSampling.value = '256';
                if (popupLog && typeof popupLog.checked !== 'boolean') popupLog.checked = false;
            } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{b.document.close()}catch{}}),H.runPsfParityCapture=async(b={})=>{const A=b||{},v=Math.max(1e3,Number(A.timeoutMs)||15e3),S=Math.max(50,Number(A.pollMs)||120),_=Date.now();try{(!H.__psfPopup||H.__psfPopup.closed)&&d.click()}catch{}for(;(!H.__psfPopup||H.__psfPopup.closed)&&Date.now()-_<v;)await new Promise(z=>setTimeout(z,S));const M=H.__psfPopup;if(!M||M.closed)throw new Error("PSF popup could not be opened. Please open it manually and run again.");for(;typeof M.renderPSF!="function"&&Date.now()-_<v;)await new Promise(z=>setTimeout(z,S));if(typeof M.renderPSF!="function")throw new Error("PSF popup is open but renderPSF is not ready yet. Try again in 1-2 seconds.");M.__PSF_DEBUG=!0,M.__PSF_USE_DESKTOP_ANALYSIS_PREVIEW=!1,M.__psfNativeParityDone=!1,await M.renderPSF();const G={implLine:M.__lastPsfImplLine||null,scaleLine:M.__lastPsfScaleLine||null,parityLine:M.__lastPsfParityLine||H.__lastPsfParityLine||null,parity:M.__lastPsfParity||H.__lastPsfParity||null};try{console.log("🧪 [runPsfParityCapture] result:",G)}catch{}return G};const N=document.getElementById("show-psf-btn");N&&!N.__cooptUnifiedPsfPipelineBound&&(N.__cooptUnifiedPsfPipelineBound=!0,N.addEventListener("click",async()=>{const b=S=>{try{const _=document.getElementById("psf-pipeline-badge");if(!_)return;const M=String(S||"");_.textContent=M,M?_.setAttribute("title",M+" (Main Show PSF uses Popup renderPSF)"):_.removeAttribute("title")}catch{}},A=S=>{try{const M=String(S?.__lastPsfImplLine||"").match(/implementation=([^\s]+)/),G=M&&M[1]?String(M[1]):"";return G?"Done ("+G+")":"Done"}catch{return"Done"}},v=async(S,_=12e3)=>{const M=Date.now();for(;Date.now()-M<_;){try{if(S&&!S.closed&&typeof S.renderPSF=="function")return!0}catch{}await new Promise(G=>setTimeout(G,80))}return!1};try{if(b("Running"),H.__psfPopup&&!H.__psfPopup.closed){try{typeof H.__psfPopup.syncAll=="function"&&H.__psfPopup.syncAll()}catch{}try{H.__psfPopup.focus()}catch{}if(typeof H.__psfPopup.renderPSF=="function"){await H.__psfPopup.renderPSF(),b(A(H.__psfPopup));return}}d.click();const S=H.__psfPopup;if(!await v(S,15e3))throw new Error("PSF popup is not ready.");try{typeof S.syncAll=="function"&&S.syncAll()}catch{}await S.renderPSF(),b(A(S))}catch(S){b("Error"),console.error("❌ [PSF Unified] Failed to render via popup pipeline:",S),alert(`PSF calculation failed: ${String(S?.message||S||"Unknown error")}`)}}));const I=document.getElementById("stop-psf-btn");I&&!I.__cooptUnifiedPsfPipelineBound&&(I.__cooptUnifiedPsfPipelineBound=!0,I.addEventListener("click",()=>{try{const b=document.getElementById("psf-pipeline-badge");b&&(b.textContent="")}catch{}try{const b=H.__psfPopup;if(!b||b.closed)return;const A=b.document&&b.document.getElementById("popup-stop-psf-btn");if(A&&typeof A.click=="function"){A.click();return}typeof b.activeCancelToken?.abort=="function"&&b.activeCancelToken.abort("Stopped by user")}catch{}}))}const u=document.getElementById("open-mtf-window-btn");u&&u.addEventListener("click",()=>{if(H.__mtfPopup&&!H.__mtfPopup.closed){try{H.__mtfPopup.focus()}catch{}return}const N=n("Modulation Transfer Function","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__mtfPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Modulation Transfer Function</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 120px;
        }
        .controls input[type="checkbox"] {
            width: auto;
            padding: 0;
            border: none;
            background: transparent;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-mtf-container { flex: 1 1 auto; min-height: 0; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-mtf-wavelength-select">Wavelength:</label>
        <select id="popup-mtf-wavelength-select"></select>
        <label for="popup-mtf-object-select">Object:</label>
        <select id="popup-mtf-object-select"></select>
        <label for="popup-mtf-max-freq-input">Max (lp/mm):</label>
        <input id="popup-mtf-max-freq-input" type="number" min="0" step="1" value="100" />
        <label for="popup-mtf-sampling-select">Sampling:</label>
        <select id="popup-mtf-sampling-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label for="popup-mtf-zeropad-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-mtf-zeropad-select">
            <option value="none">None</option>
            <option value="auto" selected>Auto</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-mtf-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-mtf-show-diff-limit-checkbox" type="checkbox" checked />
            Diffraction Limit
        </label>
        <button id="popup-show-mtf-btn" type="button">Show MTF</button>
    </div>
    <div id="popup-mtf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-mtf-progress-text" style="margin-bottom: 6px;">Calculating MTF...</div>
        <progress id="popup-mtf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-mtf-container"></div>
    </div>

    <script>
        function safeCall(fn, fallback) {
            try { return fn(); } catch (_) { return fallback; }
        }

        function getOpener() {
            try { return window.opener || window; } catch (_) { return window; }
        }

        function popupLog(...args) {
            try { console.log(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.log === 'function') {
                    op.console.log(...args);
                }
            } catch (_) {}
        }

        function popupError(...args) {
            try { console.error(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.error === 'function') {
                    op.console.error(...args);
                }
            } catch (_) {}
        }

        function getPrimaryWavelength() {
            const opener = getOpener();
            if (!opener) return null;
            if (typeof opener.getPrimaryWavelength !== 'function') return null;
            const v = Number(safeCall(() => opener.getPrimaryWavelength(), 0));
            return Number.isFinite(v) && v > 0 ? v : null;
        }

        function buildWavelengthOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getSourceRows = opener.getSourceRows;
            const sources = (typeof getSourceRows === 'function')
                ? safeCall(() => getSourceRows(opener.tableSource), [])
                : [];
            const primary = getPrimaryWavelength();
            const out = [{ value: 'all', label: 'All' }];
            if (Array.isArray(sources) && sources.length > 0) {
                for (let i = 0; i < sources.length; i++) {
                    const wl = Number(sources[i]?.wavelength);
                    if (!Number.isFinite(wl) || wl <= 0) continue;
                    const nm = wl * 1000;
                    const label = Number.isFinite(primary) && Math.abs(wl - primary) < 1e-9
                        ? (nm.toFixed(1) + ' nm (primary)')
                        : (nm.toFixed(1) + ' nm');
                    out.push({ value: String(wl), label });
                }
            }
            if (out.length === 1) {
                if (Number.isFinite(primary) && primary > 0) {
                    out.push({ value: String(primary), label: ((primary * 1000).toFixed(1) + ' nm') });
                }
            }
            return out;
        }

        function buildObjectOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getObjectRows = opener.getObjectRows;
            const objects = (typeof getObjectRows === 'function')
                ? safeCall(() => getObjectRows(opener.tableObject), [])
                : [];
            const out = [];
            if (Array.isArray(objects) && objects.length > 0) {
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(obj.position ?? obj.object ?? obj.Object ?? obj.objectType ?? 'Point');
                    const x = (obj.x ?? obj.xHeightAngle ?? 0);
                    const y = (obj.y ?? obj.yHeightAngle ?? 0);
                    out.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }
            if (out.length === 0) out.push({ value: '0', label: '0' });
            return out;
        }

        function populateSelect(selectEl, options) {
            if (!selectEl) return;
            const current = selectEl.value;
            selectEl.innerHTML = '';
            for (const opt of options) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                selectEl.appendChild(o);
            }
            if (current && Array.from(selectEl.options).some(o => o.value === current)) {
                selectEl.value = current;
            }
        }

        function syncAllOptions() {
            const wlSel = document.getElementById('popup-mtf-wavelength-select');
            const prevWl = wlSel ? wlSel.value : '';
            populateSelect(wlSel, buildWavelengthOptions());
            // Default to Primary (not All) on first open.
            if (wlSel && (!prevWl || !Array.from(wlSel.options).some(o => o.value === prevWl))) {
                const primary = getPrimaryWavelength();
                if (Number.isFinite(primary) && Array.from(wlSel.options).some(o => o.value === String(primary))) {
                    wlSel.value = String(primary);
                } else {
                    // Fallback to first numeric wavelength if present
                    const firstNumeric = Array.from(wlSel.options).find(o => o.value !== 'all');
                    if (firstNumeric) wlSel.value = firstNumeric.value;
                }
            }
            populateSelect(document.getElementById('popup-mtf-object-select'), buildObjectOptions());
        }

        window['renderMTF'] = async () => {
            const containerEl = document.getElementById('popup-mtf-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-mtf-progress-wrapper');
            const progressEl = document.getElementById('popup-mtf-progress');
            const progressTextEl = document.getElementById('popup-mtf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const wlSel = document.getElementById('popup-mtf-wavelength-select');
            const objSel = document.getElementById('popup-mtf-object-select');
            const maxEl = document.getElementById('popup-mtf-max-freq-input');
            const samplingEl = document.getElementById('popup-mtf-sampling-select');
            const zeroPadEl = document.getElementById('popup-mtf-zeropad-select');
            const removePtdEl = document.getElementById('popup-mtf-remove-ptd-checkbox');
            const showDiffLimitEl = document.getElementById('popup-mtf-show-diff-limit-checkbox');

            const wlValue = wlSel ? String(wlSel.value) : '';
            const primary = getPrimaryWavelength();
            const wavelength = (wlValue === 'all') ? 'all' : Number(wlValue);
            const objectIndex = objSel ? parseInt(objSel.value, 10) : 0;
            const maxFreq = maxEl ? Number(maxEl.value) : 100;
            const sampling = samplingEl ? Number(samplingEl.value) : 256;
            const zeroPadRaw = zeroPadEl ? String(zeroPadEl.value || 'auto') : 'auto';
            const opdDisplayMode = (removePtdEl && removePtdEl.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const showDiffractionLimit = !!(showDiffLimitEl && showDiffLimitEl.checked);
            const zeroPadTo = (zeroPadRaw === 'none')
                ? (Number.isFinite(sampling) ? sampling : 256)
                : (zeroPadRaw === 'auto')
                    ? 0
                    : (Number.isFinite(parseInt(zeroPadRaw, 10)) ? parseInt(zeroPadRaw, 10) : 0);

            try {
                const opener = getOpener();
                if (!opener) {
                    throw new Error('Opener is not available');
                }
                if (wavelength !== 'all' && !Number.isFinite(wavelength) && !(Number.isFinite(primary) && primary > 0)) {
                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                }

                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();
                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    typeof opener.runDesktopNativeOpdMapForPopup === 'function'
                    && typeof opener.runDesktopNativePsfMapForPopup === 'function'
                    && typeof opener.runDesktopNativeMtfMapForPopup === 'function'
                );

                if (canUseDesktopRust) {
                    setProgress(0, 'Starting...');
                    await new Promise(r => setTimeout(r, 0));
                    const wavefrontGridSize = Number.isFinite(sampling) ? Math.max(32, Math.floor(sampling)) : 256;

                    const sourceRows = (typeof opener.getSourceRows === 'function')
                        ? (safeCall(() => opener.getSourceRows(opener.tableSource), []) || [])
                        : [];
                    const wavelengthList = (() => {
                        const out = [];
                        if (wavelength === 'all') {
                            if (Array.isArray(sourceRows) && sourceRows.length > 0) {
                                for (let i = 0; i < sourceRows.length; i++) {
                                    const wl = Number(sourceRows[i]?.wavelength);
                                    if (!Number.isFinite(wl) || wl <= 0) continue;
                                    if (out.some(v => Math.abs(v - wl) < 1e-9)) continue;
                                    out.push(wl);
                                }
                            }
                            if (out.length === 0 && Number.isFinite(primary) && primary > 0) {
                                out.push(primary);
                            }
                        } else {
                            const wl = (Number.isFinite(Number(wavelength)) && Number(wavelength) > 0)
                                ? Number(wavelength)
                                : ((Number.isFinite(primary) && primary > 0) ? primary : 0.5876);
                            out.push(wl);
                        }
                        if (out.length === 0) out.push(0.5876);
                        return out;
                    })();

                    const getColorForWavelengthPopup = (wl) => {
                        try {
                            if (typeof opener.getColorForWavelength === 'function') {
                                const c = opener.getColorForWavelength(wl);
                                if (typeof c === 'string' && c) return c;
                            }
                        } catch (_) {}
                        const nm = Number(wl) * 1000;
                        if (!Number.isFinite(nm)) return '#2563eb';
                        if (nm < 470) return '#2563eb';
                        if (nm < 530) return '#16a34a';
                        if (nm < 600) return '#f59e0b';
                        return '#dc2626';
                    };

                    const traces = [];
                    let nyquistGlobal = 0;
                    const selectedObjectIndex = Number.isFinite(objectIndex) ? objectIndex : 0;

                    const isIdealParaxialOnlyPopup = (rows: any[]) => {
                        if (!Array.isArray(rows) || rows.length === 0) return false;
                        let hasIdeal = false;
                        for (const row of rows) {
                            if (!row || typeof row !== 'object') continue;
                            const objectType = String(row?.['object type'] ?? row?.object ?? row?.Object ?? '').trim().toLowerCase();
                            const surfType = String(row?.surfType ?? row?.type ?? row?.surfaceType ?? '').trim().toLowerCase();
                            const blockType = String(row?._blockType ?? row?.blockType ?? '').trim().toLowerCase();
                            const isIdeal = (
                                blockType === 'paraxial' || blockType === 'thinlens' || surfType === 'thinlens'
                                || Number.isFinite(Number(row?._thinLensFocalLengthX))
                                || Number.isFinite(Number(row?._thinLensFocalLengthY))
                            );
                            if (isIdeal) { hasIdeal = true; continue; }
                            const isPassive = (
                                objectType === '' || objectType === 'object' || objectType === 'image' || objectType === 'stop'
                                || surfType === 'gap' || surfType === 'air gap'
                                || blockType === 'gap' || blockType === 'air gap'
                                || surfType === 'coordinate break' || surfType === 'coordbrk'
                                || blockType === 'coordinate break' || blockType === 'coordbrk'
                            );
                            if (isPassive) continue;
                            return false;
                        }
                        return hasIdeal;
                    };
                    const estimateFiniteGridRms = (grid) => {
                        if (!Array.isArray(grid) || grid.length === 0) return NaN;
                        let sumSq = 0;
                        let count = 0;
                        for (const row of grid) {
                            if (!Array.isArray(row)) continue;
                            for (const value of row) {
                                const n = Number(value);
                                if (!Number.isFinite(n)) continue;
                                sumSq += n * n;
                                count += 1;
                            }
                        }
                        return count > 0 ? Math.sqrt(sumSq / count) : NaN;
                    };
                    const opticalRowsForIdealCheck = (typeof opener.getOpticalSystemRows === 'function')
                        ? (opener.getOpticalSystemRows(opener.tableOpticalSystem) || [])
                        : [];
                    const idealParaxialSystem = isIdealParaxialOnlyPopup(opticalRowsForIdealCheck);

                    for (let wli = 0; wli < wavelengthList.length; wli++) {
                        const wl = wavelengthList[wli];
                        const titleNm = (wl * 1000).toFixed(1);
                        const baseProgress = (wli / Math.max(1, wavelengthList.length)) * 80;
                        setProgress(10 + baseProgress, 'λ=' + titleNm + 'nm: OPD (Rust native)...');

                        const nativeOpdResp = await opener.runDesktopNativeOpdMapForPopup({
                            objectIndex: selectedObjectIndex,
                            gridSize: wavefrontGridSize,
                            wavelengthUm: wl,
                            opdDisplayMode,
                        });

                        const s = wavefrontGridSize;
                        const opdGrid = Array.from({ length: s }, () => new Float32Array(s));
                        const ampGrid = Array.from({ length: s }, () => new Float32Array(s));
                        const maskGrid = Array.from({ length: s }, () => Array(s).fill(false));

                        const displayOpdGrid = Array.isArray(nativeOpdResp?.displayOpdGrid) ? nativeOpdResp.displayOpdGrid : [];
                        const rawOpdGrid = Array.isArray(nativeOpdResp?.rawOpdGrid) ? nativeOpdResp.rawOpdGrid : [];
                        for (let iy = 0; iy < s; iy++) {
                            const rowDisplay = displayOpdGrid[iy] || [];
                            const rowRaw = rawOpdGrid[iy] || [];
                            for (let ix = 0; ix < s; ix++) {
                                const rawCell = rowRaw[ix];
                                if (rawCell === null || rawCell === undefined || rawCell === '') continue;
                                const vRawWaves = Number(rawCell);
                                if (!Number.isFinite(vRawWaves)) continue;

                                const displayCell = rowDisplay[ix];
                                const vDisplayWaves = (displayCell === null || displayCell === undefined || displayCell === '')
                                    ? NaN
                                    : Number(displayCell);
                                const vWaves = Number.isFinite(vDisplayWaves) ? vDisplayWaves : vRawWaves;
                                const vMicrons = vWaves * wl;

                                maskGrid[iy][ix] = true;
                                opdGrid[iy][ix] = vMicrons;
                                ampGrid[iy][ix] = 1.0;
                            }
                        }

                        const currentWavefrontRms = estimateFiniteGridRms(displayOpdGrid);
                        const forceIdealParaxialMtf = idealParaxialSystem
                            && opdDisplayMode !== 'pistonTiltDefocusRemoved'
                            && Number.isFinite(currentWavefrontRms)
                            && currentWavefrontRms <= 2e-2;

                        const opticalRows = (typeof opener.getOpticalSystemRows === 'function')
                            ? (opener.getOpticalSystemRows(opener.tableOpticalSystem) || [])
                            : [];
                        let pupilDiameterMm = 10.0;
                        let focalLengthMm = Number.NaN;
                        try {
                            const diffParams = (typeof opener.calculateImageSpaceDiffractionParams === 'function')
                                ? opener.calculateImageSpaceDiffractionParams(opticalRows, wl)
                                : null;
                            const fWork = Number(diffParams?.fNumberWorking);
                            const fl = Number(diffParams?.focalLengthMm);
                            if (Number.isFinite(fl) && fl > 0 && Number.isFinite(fWork) && fWork > 0) {
                                focalLengthMm = Math.abs(fl);
                                pupilDiameterMm = focalLengthMm / fWork;
                            }
                        } catch (_) {}

                        if (!(Number.isFinite(pupilDiameterMm) && pupilDiameterMm > 0)) {
                            try {
                                const si = Number((typeof opener.findStopSurfaceIndex === 'function')
                                    ? opener.findStopSurfaceIndex(opticalRows)
                                    : -1);
                                const stopRow = (Number.isFinite(si) && si >= 0) ? opticalRows?.[si] : null;
                                const sdRaw = stopRow?.semidia ?? stopRow?.Semidia ?? stopRow?.['Semi Diameter'] ?? stopRow?.aperture ?? stopRow?.Aperture ?? NaN;
                                const sd = Math.abs(parseFloat(sdRaw));
                                if (Number.isFinite(sd) && sd > 0) {
                                    const isApertureField = !!(stopRow && (stopRow.aperture !== undefined || stopRow.Aperture !== undefined));
                                    const stopRadiusMm = isApertureField ? (sd * 0.5) : sd;
                                    if (Number.isFinite(stopRadiusMm) && stopRadiusMm > 0) {
                                        pupilDiameterMm = stopRadiusMm * 2;
                                    }
                                }
                            } catch (_) {}
                        }

                        if (!(Number.isFinite(focalLengthMm) && focalLengthMm > 0)) {
                            try {
                                const fl = Number((typeof opener.calculateFocalLength === 'function')
                                    ? opener.calculateFocalLength(opticalRows, wl)
                                    : NaN);
                                if (Number.isFinite(fl) && Math.abs(fl) > 1e-9 && fl !== Infinity) {
                                    focalLengthMm = Math.abs(fl);
                                }
                            } catch (_) {}
                        }

                        if (!(Number.isFinite(focalLengthMm) && focalLengthMm > 0)) {
                            // Last-resort safeguard to keep FFT scaling defined even when paraxial FL is unavailable.
                            focalLengthMm = 100.0;
                        }

                        const minRecommendedFftSize = 512;
                        const requestedFftSize = (!zeroPadTo || zeroPadTo === 0)
                            ? Math.max(wavefrontGridSize, minRecommendedFftSize)
                            : Math.max(wavefrontGridSize, zeroPadTo);
                        const basePixelPitchUm = (wl * Math.abs(Number(focalLengthMm))) / Math.max(1e-12, Math.abs(Number(pupilDiameterMm)));
                        const pixelSizeUm = basePixelPitchUm * (wavefrontGridSize / requestedFftSize);

                        setProgress(20 + baseProgress, 'λ=' + titleNm + 'nm: PSF (Rust native)...');
                        const nativePsfResp = await opener.runDesktopNativePsfMapForPopup({
                            gridOpd: Array.from({ length: s }, (_, iy) => Array.from(opdGrid[iy] || [])),
                            gridAmplitude: Array.from({ length: s }, (_, iy) => Array.from(ampGrid[iy] || [])),
                            pupilMask: Array.from({ length: s }, (_, iy) => Array.from(maskGrid[iy] || [])),
                            wavelengthUm: wl,
                            pixelSizeUm,
                            removeTilt: false,
                            zeroPadTo: requestedFftSize,
                            recenterIfWrapped: false,
                        });

                        setProgress(30 + baseProgress, 'λ=' + titleNm + 'nm: MTF (Rust native)...');
                        const mtfResp = await opener.runDesktopNativeMtfMapForPopup({
                            psfData: nativePsfResp?.psfData,
                            pixelSizeUm,
                            maxFrequencyLpmm: Number.isFinite(maxFreq) ? maxFreq : undefined,
                            // Keep parity with TFMTF native path which uses 121 plot points.
                            points: 121,
                        });

                        const freq = Array.isArray(mtfResp?.frequencyAxis) ? mtfResp.frequencyAxis : [];
                        let tan = Array.isArray(mtfResp?.mtfTangential) ? mtfResp.mtfTangential : [];
                        let sag = Array.isArray(mtfResp?.mtfSagittal) ? mtfResp.mtfSagittal : [];
                        if (!freq.length || !tan.length || !sag.length) {
                            throw new Error('Native Rust MTF result does not contain valid curves');
                        }

                        // Build the diffraction-limit reference from the same native FFT path
                        // using a zero-OPD pupil. This keeps the dashed curve consistent with
                        // the plotted T/S MTF and avoids mismatches from simplified F/# formulas.
                        let idealDiffCurveForPopup: number[] | null = null;
                        if (showDiffractionLimit || forceIdealParaxialMtf) {
                            try {
                                const zeroOpdGrid = Array.from({ length: s }, () => Array(s).fill(0));
                                const idealNativePsfResp = await opener.runDesktopNativePsfMapForPopup({
                                    gridOpd: zeroOpdGrid,
                                    gridAmplitude: Array.from({ length: s }, (_, iy) => Array.from(ampGrid[iy] || [])),
                                    pupilMask: Array.from({ length: s }, (_, iy) => Array.from(maskGrid[iy] || [])),
                                    wavelengthUm: wl,
                                    pixelSizeUm,
                                    removeTilt: false,
                                    zeroPadTo: requestedFftSize,
                                    recenterIfWrapped: false,
                                });
                                const idealMtfResp = await opener.runDesktopNativeMtfMapForPopup({
                                    psfData: idealNativePsfResp?.psfData,
                                    pixelSizeUm,
                                    maxFrequencyLpmm: Number.isFinite(maxFreq) ? maxFreq : undefined,
                                    points: 121,
                                });
                                const idealTan = Array.isArray(idealMtfResp?.mtfTangential) ? idealMtfResp.mtfTangential : [];
                                const idealSag = Array.isArray(idealMtfResp?.mtfSagittal) ? idealMtfResp.mtfSagittal : [];
                                if ((idealTan.length === freq.length) || (idealSag.length === freq.length)) {
                                    idealDiffCurveForPopup = freq.map((_, i) => {
                                        const tv = Number(idealTan[i]);
                                        const sv = Number(idealSag[i]);
                                        if (Number.isFinite(tv) && Number.isFinite(sv)) return Math.max(0, Math.min(1, 0.5 * (tv + sv)));
                                        if (Number.isFinite(tv)) return Math.max(0, Math.min(1, tv));
                                        if (Number.isFinite(sv)) return Math.max(0, Math.min(1, sv));
                                        return null;
                                    });
                                    if (idealDiffCurveForPopup.length > 0 && idealDiffCurveForPopup[0] !== null) {
                                        idealDiffCurveForPopup[0] = 1.0;
                                    }
                                }
                            } catch (_) {
                                idealDiffCurveForPopup = null;
                            }
                        }

                        const clampToIdealEnvelope = (vals: any[]) => vals.map((v, i) => {
                            const raw = Number(v);
                            if (!Number.isFinite(raw)) return null;
                            let clamped = Math.max(0, Math.min(1, raw));
                            const lim = Number(idealDiffCurveForPopup?.[i]);
                            if (Number.isFinite(lim)) {
                                clamped = Math.min(clamped, Math.max(0, Math.min(1, lim)));
                            }
                            return clamped;
                        });

                        if (Array.isArray(idealDiffCurveForPopup) && idealDiffCurveForPopup.length === freq.length) {
                            tan = clampToIdealEnvelope(tan);
                            sag = clampToIdealEnvelope(sag);
                        }

                        if (forceIdealParaxialMtf) {
                            if (Array.isArray(idealDiffCurveForPopup) && idealDiffCurveForPopup.length === freq.length) {
                                tan = idealDiffCurveForPopup.slice();
                                sag = idealDiffCurveForPopup.slice();
                            } else {
                                const sym: number[] = [];
                                for (let i = 0; i < freq.length; i++) {
                                    const tv = Number(tan[i]);
                                    const sv = Number(sag[i]);
                                    let v: number;
                                    if (Number.isFinite(tv) && Number.isFinite(sv)) v = 0.5 * (tv + sv);
                                    else if (Number.isFinite(tv)) v = tv;
                                    else if (Number.isFinite(sv)) v = sv;
                                    else v = 0;
                                    sym.push(Math.max(0, Math.min(1, v)));
                                }
                                if (sym.length > 0) sym[0] = 1.0;
                                tan = sym.slice();
                                sag = sym.slice();
                                idealDiffCurveForPopup = sym.slice();
                            }
                        }

                        const color = getColorForWavelengthPopup(wl);
                        traces.push({
                            x: freq,
                            y: tan,
                            type: 'scatter',
                            mode: 'lines',
                            name: 'Tangential (' + titleNm + 'nm)',
                            line: { color, width: 2, dash: 'solid' },
                        });
                        traces.push({
                            x: freq,
                            y: sag,
                            type: 'scatter',
                            mode: 'lines',
                            name: 'Sagittal (' + titleNm + 'nm)',
                            line: { color, width: 2, dash: 'dot' },
                        });

                        const nyquist = Number(mtfResp?.nyquistLpmm);
                        if (Number.isFinite(nyquist) && nyquist > 0) {
                            nyquistGlobal = Math.max(nyquistGlobal, nyquist);
                        }

                        if (showDiffractionLimit) {
                            try {
                                let diffY: number[] | null = null;
                                if (Array.isArray(idealDiffCurveForPopup) && idealDiffCurveForPopup.length === freq.length) {
                                    diffY = idealDiffCurveForPopup.slice();
                                } else {
                                    const fNumber = Math.abs(Number(focalLengthMm)) / Math.max(1e-12, Math.abs(Number(pupilDiameterMm)));
                                    if (Number.isFinite(fNumber) && fNumber > 0) {
                                        diffY = [];
                                        for (let i = 0; i < freq.length; i++) {
                                            const f = Number(freq[i]);
                                            const cutoff = 1000.0 / (Math.max(1e-12, wl) * fNumber);
                                            const nu = f / Math.max(1e-12, cutoff);
                                            let val = 0;
                                            if (nu <= 0) val = 1;
                                            else if (nu >= 1) val = 0;
                                            else {
                                                const c = Math.max(-1, Math.min(1, nu));
                                                val = (2 / Math.PI) * (Math.acos(c) - c * Math.sqrt(Math.max(0, 1 - c * c)));
                                            }
                                            diffY.push(Number.isFinite(val) ? Math.max(0, Math.min(1, val)) : 0);
                                        }
                                    }
                                }
                                if (Array.isArray(diffY)) {
                                    traces.push({
                                        x: freq,
                                        y: diffY,
                                        type: 'scatter',
                                        mode: 'lines',
                                        name: 'Diff. Limit (' + titleNm + 'nm)',
                                        line: { color, width: 1.5, dash: 'dash' },
                                    });
                                }
                            } catch (_) {}
                        }
                    }

                    if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                        throw new Error('Plotly is not available in MTF popup');
                    }
                    if (!traces.length) {
                        throw new Error('Native Rust MTF did not produce any traces');
                    }

                    setProgress(80, 'Rendering MTF...');
                    const xAxisRangeMax = Number.isFinite(maxFreq) && maxFreq > 0
                        ? maxFreq
                        : (nyquistGlobal > 0 ? nyquistGlobal : undefined);
                    await window.Plotly.newPlot(containerEl, traces, {
                        margin: { l: 50, r: 20, t: 28, b: 42 },
                        xaxis: {
                            title: 'Spatial frequency (lp/mm)',
                            ...(Number.isFinite(xAxisRangeMax) ? { range: [0, xAxisRangeMax] } : {}),
                        },
                        yaxis: { title: 'MTF', range: [0, 1.05] },
                        showlegend: true,
                    }, { responsive: true });

                    hideProgress();
                    return;
                }

                throw new Error('Rust MTF path is required but unavailable. Ensure desktop Rust analysis is enabled.');
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const details = String((err && err.message) ? err.message : err || 'Unknown error');
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate MTF.<br/><span style="font-size:12px;color:#555;">' + details.replace(/</g, '&lt;').replace(/>/g, '&gt;') + '</span></div>';
                }
            }
        };

        document.getElementById('popup-show-mtf-btn').addEventListener('click', () => window.renderMTF());
        window.addEventListener('focus', syncAllOptions);
        window.addEventListener('load', () => syncAllOptions());
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const y=document.getElementById("open-through-focus-spot-window-btn");y&&y.addEventListener("click",()=>{if(H.__throughFocusSpotPopup&&!H.__throughFocusSpotPopup.closed){try{H.__throughFocusSpotPopup.focus()}catch{}return}try{En(H)}catch{}const N=n("Through-Focus Spot","width=980,height=700");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__throughFocusSpotPopup=N;try{N.__analysisHostWindow=window}catch{}N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Through-Focus Spot</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 110px;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-through-focus-spot-container {
            flex: 1 1 auto;
            min-height: 0;
        }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-through-focus-spot-wavelength-mode-select">Wavelength:</label>
        <select id="popup-through-focus-spot-wavelength-mode-select">
            <option value="all" selected>All</option>
            <option value="primary">Primary</option>
        </select>

        <label for="popup-through-focus-spot-min-defocus-input">Defocus min (mm):</label>
        <input id="popup-through-focus-spot-min-defocus-input" type="number" step="0.001" value="-0.5" />

        <label for="popup-through-focus-spot-max-defocus-input">Defocus max (mm):</label>
        <input id="popup-through-focus-spot-max-defocus-input" type="number" step="0.001" value="0.5" />

        <label for="popup-through-focus-spot-steps-input">Steps:</label>
        <input id="popup-through-focus-spot-steps-input" type="number" min="3" max="61" step="1" value="5" />

        <label for="popup-through-focus-spot-scale-input">Scale (µm):</label>
        <input id="popup-through-focus-spot-scale-input" type="number" min="1" step="1" value="100" />

        <label for="popup-through-focus-spot-ray-count-input">Ray number:</label>
        <input type="number" id="popup-through-focus-spot-ray-count-input" value="501" min="1" max="20001" step="1" />

        <label for="popup-through-focus-spot-ring-count-select">Ring count:</label>
        <select id="popup-through-focus-spot-ring-count-select">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
            <option value="6">6</option>
            <option value="7">7</option>
            <option value="8">8</option>
            <option value="9">9</option>
            <option value="10" selected>10</option>
            <option value="12">12</option>
            <option value="15">15</option>
            <option value="16">16</option>
            <option value="20">20</option>
            <option value="24">24</option>
            <option value="32">32</option>
        </select>

        <label for="popup-through-focus-spot-pattern-select">Ray pattern:</label>
        <select id="popup-through-focus-spot-pattern-select">
            <option value="annular" selected>Annular</option>
            <option value="grid">Rectangle</option>
        </select>

        <button id="popup-show-through-focus-spot-btn" type="button">Show Through-Focus Spot</button>
    </div>
    <div id="popup-through-focus-spot-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-through-focus-spot-progress-text" style="margin-bottom: 6px;">Calculating Through-Focus Spot...</div>
        <progress id="popup-through-focus-spot-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-through-focus-spot-container"></div>
    </div>

    <script>
        function getHostWindow() {
            try {
                if (window.opener && !window.opener.closed) return window.opener;
            } catch (_) {}
            try {
                if (window.__analysisHostWindow) return window.__analysisHostWindow;
            } catch (_) {}
            return null;
        }

        function getOpenerEl(id) {
            try {
                const host = getHostWindow();
                return host && host.document ? host.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncInputsFromOpener() {
            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');
            const popupRay = document.getElementById('popup-through-focus-spot-ray-count-input');
            const popupRing = document.getElementById('popup-through-focus-spot-ring-count-select');
            if (popupRay && openerRay && popupRay.value !== openerRay.value) popupRay.value = openerRay.value;
            if (popupRing && openerRing && popupRing.value !== openerRing.value) popupRing.value = openerRing.value;

            const annular = getOpenerEl('annular-pattern-btn');
            const popupPattern = document.getElementById('popup-through-focus-spot-pattern-select');
            if (popupPattern) {
                const isAnnular = !!annular && annular.classList.contains('active');
                popupPattern.value = isAnnular ? 'annular' : 'grid';
            }
        }

        function setPopupPattern(pattern) {
            const isAnnular = String(pattern || 'annular') !== 'grid';
            const openerAnnular = getOpenerEl('annular-pattern-btn');
            const openerGrid = getOpenerEl('grid-pattern-btn');
            if (isAnnular && openerAnnular) openerAnnular.click();
            if (!isAnnular && openerGrid) openerGrid.click();
        }

        function syncAll() {
            syncInputsFromOpener();
        }

        window.renderThroughFocusSpot = async () => {
            const containerEl = document.getElementById('popup-through-focus-spot-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-through-focus-spot-progress-wrapper');
            const progressEl = document.getElementById('popup-through-focus-spot-progress');
            const progressTextEl = document.getElementById('popup-through-focus-spot-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');

            const wlModeEl = document.getElementById('popup-through-focus-spot-wavelength-mode-select');
            const minDefocusEl = document.getElementById('popup-through-focus-spot-min-defocus-input');
            const maxDefocusEl = document.getElementById('popup-through-focus-spot-max-defocus-input');
            const stepsEl = document.getElementById('popup-through-focus-spot-steps-input');
            const scaleEl = document.getElementById('popup-through-focus-spot-scale-input');
            const rayEl = document.getElementById('popup-through-focus-spot-ray-count-input');
            const ringEl = document.getElementById('popup-through-focus-spot-ring-count-select');
            const patternEl = document.getElementById('popup-through-focus-spot-pattern-select');

            if (openerRay && rayEl) openerRay.value = rayEl.value;
            if (openerRing && ringEl) openerRing.value = ringEl.value;

            const opener = getHostWindow();
            if (!opener) {
                if (containerEl) containerEl.textContent = 'Main window is not available.';
                return;
            }

            try {
                setProgress(0, 'Starting...');

                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();
                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    typeof opener.runDesktopAnalysisComputeForPopup === 'function'
                );
                const canUseNativeRustSpot = canUseDesktopRust && !!(
                    typeof opener.runDesktopNativeSpotRaytraceForPopup === 'function'
                );

                const toFiniteNumber = (v, fallback) => {
                    const n = Number(v);
                    return Number.isFinite(n) ? n : fallback;
                };
                const toInt = (v, fallback) => {
                    const n = parseInt(String(v ?? ''), 10);
                    return Number.isInteger(n) ? n : fallback;
                };
                const clamp = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
                const selectedSurfaceIndex = (() => {
                    try {
                        const opticalRows = (typeof opener.getOpticalSystemRows === 'function')
                            ? (opener.getOpticalSystemRows(opener.tableOpticalSystem) || [])
                            : [];
                        for (let i = opticalRows.length - 1; i >= 0; i--) {
                            const row = opticalRows[i] || {};
                            const objectType = String(row?.['object type'] ?? row?.object ?? row?.Object ?? '').trim().toLowerCase();
                            if (objectType === 'image') return i;
                        }
                        return opticalRows.length > 0 ? Math.max(0, opticalRows.length - 1) : undefined;
                    } catch (_) {
                        return undefined;
                    }
                })();
                const selectedRayCount = rayEl && rayEl.value !== '' ? parseInt(rayEl.value, 10) : undefined;
                const selectedRingCount = ringEl && ringEl.value !== '' ? parseInt(ringEl.value, 10) : undefined;
                const selectedPattern = patternEl ? String(patternEl.value || 'annular') : 'annular';
                const selectedWavelengthMode = wlModeEl ? String(wlModeEl.value || 'all') : 'all';
                const minDefocusMm = toFiniteNumber(minDefocusEl ? minDefocusEl.value : -0.5, -0.5);
                const maxDefocusMm = toFiniteNumber(maxDefocusEl ? maxDefocusEl.value : 0.5, 0.5);
                const steps = clamp(toInt(stepsEl ? stepsEl.value : 5, 5), 3, 61);
                const scaleUm = Math.max(1, toFiniteNumber(scaleEl ? scaleEl.value : 100, 100));
                const halfScaleUm = scaleUm * 0.5;

                // TFSD is intentionally fixed to Rust backend for parity.
                if (!canUseNativeRustSpot) {
                    throw new Error('Through-Focus Spot requires Rust backend (runDesktopNativeSpotRaytraceForPopup unavailable).');
                }

                if (canUseNativeRustSpot) {
                    const toWavelengthLabel = (rawLabel) => {
                        const text = String(rawLabel || '').trim();
                        const nm = text.match(/(d+(?:.d+)?)s*nm/i);
                        if (nm && nm[1]) return 'Wavelength ' + nm[1] + 'nm';
                        const lower = text.toLowerCase();
                        if (lower.includes('primary')) return 'Wavelength Primary';
                        return 'Wavelength ' + text;
                    };
                    const wavelengthLabelFromSeries = (seriesItem, rawLabel) => {
                        const wl = Number(seriesItem?.wavelengthUm);
                        if (Number.isFinite(wl) && wl > 0) {
                            return 'Wavelength ' + (wl * 1000).toFixed(1) + 'nm';
                        }
                        return toWavelengthLabel(rawLabel);
                    };
                    const parseSeriesLabel = (label, fallbackObjectLabel) => {
                        const raw = String(label || '').trim();
                        if (!raw) {
                            return { objectLabel: fallbackObjectLabel, wavelengthLabel: 'Wavelength Primary' };
                        }
                        const m = raw.match(/(Primary(?:s*([^)]*))?|d+(?:.d+)?s*nm)s*$/i);
                        if (m && m[1]) {
                            const wlRaw = String(m[1] || 'Primary');
                            const prefix = raw.slice(0, Math.max(0, m.index || 0)).replace(/[|@-s]+$/, '').trim();
                            return {
                                objectLabel: prefix || fallbackObjectLabel,
                                wavelengthLabel: toWavelengthLabel(wlRaw),
                            };
                        }
                        return { objectLabel: raw, wavelengthLabel: 'Wavelength Primary' };
                    };

                    const objectRows = (() => {
                        try {
                            if (opener.tableObject && typeof opener.tableObject.getData === 'function') {
                                const rows = opener.tableObject.getData();
                                if (Array.isArray(rows)) return rows;
                            }
                        } catch (_) {}
                        try {
                            if (typeof opener.getObjectRows === 'function') {
                                const rows = opener.getObjectRows(opener.tableObject);
                                if (Array.isArray(rows)) return rows;
                            }
                        } catch (_) {}
                        try {
                            const raw = opener.localStorage && opener.localStorage.getItem('objectTableData');
                            if (raw) {
                                const parsed = JSON.parse(raw);
                                if (Array.isArray(parsed)) return parsed;
                            }
                        } catch (_) {}
                        return [];
                    })();

                    const getObjectLabel = (row, index) => {
                        const id = row && row.id !== undefined ? String(row.id).trim() : '';
                        if (id) return id;
                        const obj = row && row.object !== undefined ? String(row.object).trim() : '';
                        if (obj) return obj;
                        const pos = row && row.position !== undefined ? String(row.position).trim() : '';
                        if (pos) return 'Object ' + (index + 1) + ' (' + pos + ')';
                        return 'Object ' + (index + 1);
                    };

                    const objectLabels = [];
                    const focusGrid = [];
                    const tfTraceStatsRows = [];
                    const getObjectLabelByIndex = (index) => {
                        if (Array.isArray(objectRows) && objectRows[index]) {
                            return getObjectLabel(objectRows[index], index);
                        }
                        return 'Object ' + (index + 1);
                    };
                    const ensureObjectRow = (preferredIndex, label) => {
                        let idx = Number(preferredIndex);
                        if (!Number.isInteger(idx) || idx < 0) {
                            idx = objectLabels.length;
                        }
                        while (objectLabels.length <= idx) {
                            const next = objectLabels.length;
                            objectLabels.push(getObjectLabelByIndex(next));
                            focusGrid.push([]);
                        }
                        const labelText = String(label || '').trim();
                        if (labelText) {
                            objectLabels[idx] = labelText;
                        }
                        return idx;
                    };
                    const defocusValues = Array.from({ length: steps }, (_, i) => {
                        if (steps <= 1) return minDefocusMm;
                        const t = i / (steps - 1);
                        return minDefocusMm + t * (maxDefocusMm - minDefocusMm);
                    });

                    for (let i = 0; i < defocusValues.length; i++) {
                        const shift = defocusValues[i];
                        const baseProgress = Math.floor((i / Math.max(1, defocusValues.length)) * 85);
                        setProgress(baseProgress, 'Computing defocus ' + shift.toFixed(4) + ' mm (' + (i + 1) + '/' + defocusValues.length + ')...');

                        const result = await opener.runDesktopNativeSpotRaytraceForPopup({
                            surfaceIndex: selectedSurfaceIndex,
                            rayCount: selectedRayCount,
                            ringCount: selectedRingCount,
                            pattern: selectedPattern,
                            wavelengthMode: selectedWavelengthMode,
                            objectRows,
                            defocusMm: shift,
                        });
                        const stats = Array.isArray(result?.seriesStats) ? result.seriesStats : [];
                        for (const stat of stats) {
                            tfTraceStatsRows.push({
                                backend: String(result?.backend || 'native-rust-raytrace'),
                                defocusMm: Number(shift),
                                label: String(stat?.label || ''),
                                attemptedRays: Number(stat?.attemptedRays || 0),
                                hitRays: Number(stat?.hitRays || 0),
                                missRays: Number(stat?.missRays || 0),
                                apertureBlockRays: Number(stat?.apertureBlockRays || 0),
                                noIntersectionRays: Number(stat?.noIntersectionRays || 0),
                                tirRays: Number(stat?.tirRays || 0),
                                unknownFailRays: Number(stat?.unknownFailRays || 0),
                                statusCounts: (stat && typeof stat.statusCounts === 'object' && !Array.isArray(stat.statusCounts)) ? stat.statusCounts : {},
                                hitRatePercent: Number(stat?.hitRatePercent || 0),
                            });
                        }
                        const airyRadiusUm = Number(result?.airyRadiusUm);
                        const safeAiryRadiusUm = (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) ? airyRadiusUm : NaN;

                        const series = Array.isArray(result?.series) ? result.series : [];
                        const groupedByObject = new Map();
                        const wlCountRaw = Number(result?.wavelengthCount);
                        const inferredWavelengthCount = (() => {
                            if (Number.isInteger(wlCountRaw) && wlCountRaw > 0) return wlCountRaw;
                            const unique = new Set();
                            for (const s of series) {
                                const key = wavelengthLabelFromSeries(s, String(s?.label || ''));
                                unique.add(key);
                            }
                            return Math.max(1, unique.size);
                        })();

                        for (let sIdx = 0; sIdx < series.length; sIdx++) {
                            const s = series[sIdx] || {};
                            const objectIndexByOrder = Math.floor(sIdx / inferredWavelengthCount);
                            const fallbackObj = getObjectLabelByIndex(objectIndexByOrder);
                            const parsed = parseSeriesLabel(s.label, fallbackObj);
                            const rowIndex = ensureObjectRow(objectIndexByOrder, fallbackObj || parsed.objectLabel);
                            const pts = Array.isArray(s.points) ? s.points : [];
                            const hasFieldAngle = !!s.hasFieldAngle;
                            const chiefXUm = Number(s?.chiefPointUm?.xUm);
                            const chiefYUm = Number(s?.chiefPointUm?.yUm);
                            const useChiefCentering = hasFieldAngle && Number.isFinite(chiefXUm) && Number.isFinite(chiefYUm);
                            const centered = useChiefCentering
                                ? pts.map((p) => ({
                                    xUm: (Number(p?.xUm) || 0) - chiefXUm,
                                    yUm: (Number(p?.yUm) || 0) - chiefYUm,
                                }))
                                : pts.map((p) => ({
                                    xUm: Number(p?.xUm) || 0,
                                    yUm: Number(p?.yUm) || 0,
                                }));

                            if (!groupedByObject.has(rowIndex)) {
                                groupedByObject.set(rowIndex, []);
                            }
                            const wlLabel = wavelengthLabelFromSeries(s, parsed.wavelengthLabel);
                            groupedByObject.get(rowIndex).push({
                                key: wlLabel,
                                label: wlLabel,
                                color: String(s?.color || '#2563eb'),
                                points: centered,
                            });
                        }

                        for (let rowIndex = 0; rowIndex < focusGrid.length; rowIndex++) {
                            const groups = Array.isArray(groupedByObject.get(rowIndex)) ? groupedByObject.get(rowIndex) : [];
                            const merged = [];
                            for (const g of groups) {
                                const pts = Array.isArray(g?.points) ? g.points : [];
                                for (const p of pts) {
                                    merged.push({ xUm: Number(p?.xUm) || 0, yUm: Number(p?.yUm) || 0 });
                                }
                            }

                            let cx = 0;
                            let cy = 0;
                            if (merged.length > 0) {
                                cx = merged.reduce((sum, p) => sum + p.xUm, 0) / merged.length;
                                cy = merged.reduce((sum, p) => sum + p.yUm, 0) / merged.length;
                            }

                            focusGrid[rowIndex].push({
                                shiftMm: shift,
                                airyRadiusUm: safeAiryRadiusUm,
                                pointsByWavelength: groups.map((g) => ({
                                    key: g.key,
                                    label: g.label,
                                    color: g.color,
                                    points: (Array.isArray(g.points) ? g.points : []).map((p) => ({
                                        xUm: (Number(p?.xUm) || 0) - cx,
                                        yUm: (Number(p?.yUm) || 0) - cy,
                                    })),
                                })),
                            });
                        }
                    }

                    try {
                        window.__cooptTfSpotLastTraceStats = tfTraceStatsRows;
                    } catch (_) {}
                    try {
                        if (opener && typeof opener === 'object') {
                            opener.__cooptTfSpotLastTraceStats = tfTraceStatsRows;
                        }
                    } catch (_) {}
                    try {
                        globalThis.__cooptTfSpotLastTraceStats = tfTraceStatsRows;
                    } catch (_) {}
                    try {
                        if (console.table) {
                            console.table(tfTraceStatsRows);
                        } else {
                            console.log('[TFSD_TRACE_STATS]', tfTraceStatsRows);
                        }
                    } catch (_) {}

                    if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                        throw new Error('Plotly is not available in Through-Focus Spot popup');
                    }

                    const rows = Math.max(1, focusGrid.length);
                    const cols = Math.max(1, defocusValues.length);
                    const traces = [];
                    const layout = {
                        showlegend: true,
                        grid: { rows, columns: cols, pattern: 'independent' },
                        margin: { l: 60, r: 20, t: 56, b: 60 },
                        paper_bgcolor: '#ffffff',
                        plot_bgcolor: '#ffffff',
                        height: Math.max(420, rows * 145 + 90),
                        legend: {
                            orientation: 'h',
                            yanchor: 'bottom',
                            y: 1.06,
                            xanchor: 'center',
                            x: 0.5,
                        },
                        legendgroupclick: 'togglegroup',
                        shapes: [],
                    };

                    const legendEntries = new Map();
                    for (let r = 0; r < rows; r++) {
                        for (let c = 0; c < cols; c++) {
                            const idx = r * cols + c + 1;
                            const axisRefX = idx === 1 ? 'x' : 'x' + idx;
                            const axisRefY = idx === 1 ? 'y' : 'y' + idx;
                            const axisKeyX = idx === 1 ? 'xaxis' : 'xaxis' + idx;
                            const axisKeyY = idx === 1 ? 'yaxis' : 'yaxis' + idx;
                            const cell = (focusGrid[r] && focusGrid[r][c]) ? focusGrid[r][c] : { pointsByWavelength: [] };
                            const airyRadiusUm = Number(cell?.airyRadiusUm);
                            const groups = Array.isArray(cell?.pointsByWavelength) ? cell.pointsByWavelength : [];

                            for (const group of groups) {
                                const pts = Array.isArray(group?.points) ? group.points : [];
                                const groupKey = String(group?.key || group?.label || 'wavelength');
                                if (!legendEntries.has(groupKey)) {
                                    legendEntries.set(groupKey, {
                                        label: String(group?.label || groupKey),
                                        color: String(group?.color || '#2563eb'),
                                    });
                                }
                                traces.push({
                                    x: pts.map((p) => Number(p?.xUm) || 0),
                                    y: pts.map((p) => Number(p?.yUm) || 0),
                                    mode: 'markers',
                                    type: 'scattergl',
                                    name: String(group?.label || groupKey),
                                    legendgroup: groupKey,
                                    showlegend: false,
                                    marker: {
                                        size: 4,
                                        color: String(group?.color || '#2563eb'),
                                        opacity: 0.75,
                                    },
                                    xaxis: axisRefX,
                                    yaxis: axisRefY,
                                    hovertemplate: 'x=%{x:.2f} µm<br>y=%{y:.2f} µm<extra></extra>',
                                });
                            }

                            layout[axisKeyX] = {
                                range: [-halfScaleUm, halfScaleUm],
                                showgrid: true,
                                zeroline: true,
                                showticklabels: r === rows - 1,
                                title: r === rows - 1 ? defocusValues[c].toFixed(3) + ' mm' : '',
                            };
                            layout[axisKeyY] = {
                                range: [-halfScaleUm, halfScaleUm],
                                showgrid: true,
                                zeroline: true,
                                showticklabels: c === 0,
                                title: c === 0 ? (objectLabels[r] || ('Object ' + (r + 1))) : '',
                                scaleanchor: axisRefX,
                                scaleratio: 1,
                            };

                            if (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) {
                                layout.shapes.push({
                                    type: 'circle',
                                    xref: axisRefX,
                                    yref: axisRefY,
                                    x0: -airyRadiusUm,
                                    y0: -airyRadiusUm,
                                    x1: airyRadiusUm,
                                    y1: airyRadiusUm,
                                    line: { color: '#111827', width: 1 },
                                    fillcolor: 'rgba(0,0,0,0)',
                                });
                            }
                        }
                    }

                    const renderablePointCount = traces.reduce((sum, trace) => {
                        const xs = Array.isArray(trace?.x) ? trace.x : [];
                        return sum + xs.filter((v) => Number.isFinite(Number(v))).length;
                    }, 0);
                    if (renderablePointCount <= 0) {
                        throw new Error('Through-Focus Spot produced no plottable spot points on the image surface.');
                    }

                    for (const [groupKey, entry] of legendEntries.entries()) {
                        traces.push({
                            x: [null],
                            y: [null],
                            mode: 'markers',
                            type: 'scatter',
                            name: entry.label,
                            legendgroup: groupKey,
                            showlegend: true,
                            marker: {
                                size: 8,
                                color: entry.color,
                                symbol: 'circle',
                            },
                            hoverinfo: 'skip',
                        });
                    }

                    setProgress(92, 'Rendering Through-Focus Spot...');
                    await window.Plotly.newPlot(containerEl, traces, layout, { responsive: true, displaylogo: false });
                    try {
                        if (progressWrapper) progressWrapper.style.display = 'none';
                    } catch (_) {}
                    return;
                }

                throw new Error('Through-Focus Spot web fallback is disabled (Rust backend fixed).');
            } catch (e) {
                if (containerEl) containerEl.textContent = String(e && e.message ? e.message : e);
                setProgress(100, 'Failed');
            }
        };

        document.getElementById('popup-through-focus-spot-pattern-select').addEventListener('change', (e) => {
            const target = e && e.target ? e.target : null;
            const v = target && target.value ? String(target.value) : 'annular';
            setPopupPattern(v);
        });
        document.getElementById('popup-show-through-focus-spot-btn').addEventListener('click', () => window.renderThroughFocusSpot());
        window.addEventListener('focus', syncAll);
        window.addEventListener('load', () => syncAll());
        syncAll();
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const m=document.getElementById("open-through-focus-mtf-window-btn");m&&m.addEventListener("click",()=>{if(H.__throughFocusMtfPopup&&!H.__throughFocusMtfPopup.closed){try{H.__throughFocusMtfPopup.focus()}catch{}return}const N=n("Through-Focus MTF","width=900,height=680");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__throughFocusMtfPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Analysis</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 110px;
        }
        .controls input[type="checkbox"] {
            width: auto;
            padding: 0;
            border: none;
            background: transparent;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-through-focus-mtf-container { flex: 1 1 auto; min-height: 0; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-through-focus-mtf-wavelength-select">Wavelength:</label>
        <select id="popup-through-focus-mtf-wavelength-select"></select>
        <label for="popup-through-focus-mtf-object-select">Object:</label>
        <select id="popup-through-focus-mtf-object-select"></select>
        <label for="popup-through-focus-mtf-target-freq-input">Freq (lp/mm):</label>
        <input id="popup-through-focus-mtf-target-freq-input" type="number" min="0" step="1" value="10" />
        <label for="popup-through-focus-mtf-min-defocus-input">Defocus min (mm):</label>
        <input id="popup-through-focus-mtf-min-defocus-input" type="number" step="0.001" value="-0.5" />
        <label for="popup-through-focus-mtf-max-defocus-input">Defocus max (mm):</label>
        <input id="popup-through-focus-mtf-max-defocus-input" type="number" step="0.001" value="0.5" />
        <label for="popup-through-focus-mtf-steps-input">Steps:</label>
        <input id="popup-through-focus-mtf-steps-input" type="number" min="3" max="201" step="1" value="21" />
        <label for="popup-through-focus-mtf-sampling-select">Sampling:</label>
        <select id="popup-through-focus-mtf-sampling-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label for="popup-through-focus-mtf-zeropad-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-through-focus-mtf-zeropad-select">
            <option value="none">None</option>
            <option value="auto" selected>Auto</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-through-focus-mtf-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <button id="popup-show-through-focus-mtf-btn" type="button">Show Plot</button>
    </div>
    <div id="popup-through-focus-mtf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-through-focus-mtf-progress-text" style="margin-bottom: 6px;">Calculating...</div>
        <progress id="popup-through-focus-mtf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-through-focus-mtf-container"></div>
    </div>

    <script>
        function safeCall(fn, fallback) {
            try { return fn(); } catch (_) { return fallback; }
        }

        function getOpener() {
            try { return window.opener || window; } catch (_) { return window; }
        }

        function popupLog(...args) {
            try { console.log(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.log === 'function') {
                    op.console.log(...args);
                }
            } catch (_) {}
        }

        function popupError(...args) {
            try { console.error(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.error === 'function') {
                    op.console.error(...args);
                }
            } catch (_) {}
        }

        function getPrimaryWavelength() {
            const opener = getOpener();
            if (!opener) return null;
            if (typeof opener.getPrimaryWavelength !== 'function') return null;
            const v = Number(safeCall(() => opener.getPrimaryWavelength(), 0));
            return Number.isFinite(v) && v > 0 ? v : null;
        }

        function buildWavelengthOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getSourceRows = opener.getSourceRows;
            const sources = (typeof getSourceRows === 'function')
                ? safeCall(() => getSourceRows(opener.tableSource), [])
                : [];
            const primary = getPrimaryWavelength();
            const out = [{ value: 'all', label: 'All' }];
            if (Array.isArray(sources) && sources.length > 0) {
                for (let i = 0; i < sources.length; i++) {
                    const wl = Number(sources[i]?.wavelength);
                    if (!Number.isFinite(wl) || wl <= 0) continue;
                    const nm = wl * 1000;
                    const label = Number.isFinite(primary) && Math.abs(wl - primary) < 1e-9
                        ? (nm.toFixed(1) + ' nm (primary)')
                        : (nm.toFixed(1) + ' nm');
                    out.push({ value: String(wl), label });
                }
            }
            if (out.length === 1) {
                if (Number.isFinite(primary) && primary > 0) {
                    out.push({ value: String(primary), label: ((primary * 1000).toFixed(1) + ' nm') });
                }
            }
            return out;
        }

        function buildObjectOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getObjectRows = opener.getObjectRows;
            const objects = (typeof getObjectRows === 'function')
                ? safeCall(() => getObjectRows(opener.tableObject), [])
                : [];
            const out = [];
            if (Array.isArray(objects) && objects.length > 0) {
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(obj.position ?? obj.object ?? obj.Object ?? obj.objectType ?? 'Point');
                    const x = (obj.x ?? obj.xHeightAngle ?? 0);
                    const y = (obj.y ?? obj.yHeightAngle ?? 0);
                    out.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }
            if (out.length === 0) out.push({ value: '0', label: '0' });
            return out;
        }

        function populateSelect(selectEl, options) {
            if (!selectEl) return;
            const current = selectEl.value;
            selectEl.innerHTML = '';
            for (const opt of options) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                selectEl.appendChild(o);
            }
            if (current && Array.from(selectEl.options).some(o => o.value === current)) {
                selectEl.value = current;
            }
        }

        function syncAllOptions() {
            const wlSel = document.getElementById('popup-through-focus-mtf-wavelength-select');
            const prevWl = wlSel ? wlSel.value : '';
            populateSelect(wlSel, buildWavelengthOptions());
            if (wlSel && (!prevWl || !Array.from(wlSel.options).some(o => o.value === prevWl))) {
                const primary = getPrimaryWavelength();
                if (Number.isFinite(primary) && Array.from(wlSel.options).some(o => o.value === String(primary))) {
                    wlSel.value = String(primary);
                } else {
                    const firstNumeric = Array.from(wlSel.options).find(o => o.value !== 'all');
                    if (firstNumeric) wlSel.value = firstNumeric.value;
                }
            }
            populateSelect(document.getElementById('popup-through-focus-mtf-object-select'), buildObjectOptions());
        }

        window['renderThroughFocusMTF'] = async () => {
            const containerEl = document.getElementById('popup-through-focus-mtf-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-through-focus-mtf-progress-wrapper');
            const progressEl = document.getElementById('popup-through-focus-mtf-progress');
            const progressTextEl = document.getElementById('popup-through-focus-mtf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const wlSel = document.getElementById('popup-through-focus-mtf-wavelength-select');
            const objSel = document.getElementById('popup-through-focus-mtf-object-select');
            const targetFreqEl = document.getElementById('popup-through-focus-mtf-target-freq-input');
            const minDefocusEl = document.getElementById('popup-through-focus-mtf-min-defocus-input');
            const maxDefocusEl = document.getElementById('popup-through-focus-mtf-max-defocus-input');
            const stepsEl = document.getElementById('popup-through-focus-mtf-steps-input');
            const samplingEl = document.getElementById('popup-through-focus-mtf-sampling-select');
            const zeroPadEl = document.getElementById('popup-through-focus-mtf-zeropad-select');
            const removePtdEl = document.getElementById('popup-through-focus-mtf-remove-ptd-checkbox');

            const wlValue = wlSel ? String(wlSel.value) : '';
            const primary = getPrimaryWavelength();
            const wavelength = (wlValue === 'all') ? 'all' : Number(wlValue);
            const objectIndex = objSel ? parseInt(objSel.value, 10) : 0;
            const targetFrequencyLpmm = targetFreqEl ? Number(targetFreqEl.value) : 10;
            const defocusMinMm = minDefocusEl ? Number(minDefocusEl.value) : -0.5;
            const defocusMaxMm = maxDefocusEl ? Number(maxDefocusEl.value) : 0.5;
            const steps = stepsEl ? Number(stepsEl.value) : 21;
            const sampling = samplingEl ? Number(samplingEl.value) : 256;
            const zeroPadRaw = zeroPadEl ? String(zeroPadEl.value || 'auto') : 'auto';
            const opdDisplayMode = (removePtdEl && removePtdEl.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const zeroPadTo = (zeroPadRaw === 'none')
                ? (Number.isFinite(sampling) ? sampling : 256)
                : (zeroPadRaw === 'auto')
                    ? 0
                    : (Number.isFinite(parseInt(zeroPadRaw, 10)) ? parseInt(zeroPadRaw, 10) : 0);

            try {
                const opener = getOpener();
                if (!opener) {
                    throw new Error('Opener is not available');
                }
                setProgress(0, 'Starting...');
                await new Promise(r => setTimeout(r, 0));
                if (wavelength !== 'all' && !Number.isFinite(wavelength) && !(Number.isFinite(primary) && primary > 0)) {
                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                }

                if (typeof opener.runDesktopNativeThroughFocusMtfForPopup !== 'function') {
                    throw new Error('runDesktopNativeThroughFocusMtfForPopup is not available on opener');
                }

                const sourceRows = (typeof opener.getSourceRows === 'function')
                    ? (safeCall(() => opener.getSourceRows(opener.tableSource), []) || [])
                    : [];
                const wavelengthList = (() => {
                    const out = [];
                    if (wavelength === 'all') {
                        if (Array.isArray(sourceRows) && sourceRows.length > 0) {
                            for (let i = 0; i < sourceRows.length; i++) {
                                const wl = Number(sourceRows[i]?.wavelength);
                                if (!Number.isFinite(wl) || wl <= 0) continue;
                                if (out.some(v => Math.abs(v - wl) < 1e-9)) continue;
                                out.push(wl);
                            }
                        }
                        if (out.length === 0 && Number.isFinite(primary) && primary > 0) {
                            out.push(primary);
                        }
                    } else {
                        const wl = (Number.isFinite(Number(wavelength)) && Number(wavelength) > 0)
                            ? Number(wavelength)
                            : ((Number.isFinite(primary) && primary > 0) ? primary : 0.5876);
                        out.push(wl);
                    }
                    if (out.length === 0) out.push(0.5876);
                    return out;
                })();

                let lastProgress = 20;
                setProgress(lastProgress, 'Computing Through-Focus MTF (Rust/WASM)...');
                const nativeResp = await opener.runDesktopNativeThroughFocusMtfForPopup({
                    objectIndex: Number.isFinite(objectIndex) ? objectIndex : 0,
                    wavelengths: wavelengthList,
                    targetFrequencyLpmm: Number.isFinite(targetFrequencyLpmm) ? targetFrequencyLpmm : 10,
                    defocusMinMm: Number.isFinite(defocusMinMm) ? defocusMinMm : -0.5,
                    defocusMaxMm: Number.isFinite(defocusMaxMm) ? defocusMaxMm : 0.5,
                    steps: Number.isFinite(steps) ? steps : 21,
                    samplingSize: Number.isFinite(sampling) ? sampling : 256,
                    zeroPadTo,
                    opdDisplayMode,
                    onProgress: (evt) => {
                        try {
                            const p = Number(evt?.percent);
                            const msg = String(evt?.message || 'Computing Through-Focus MTF...');
                            if (Number.isFinite(p)) {
                                lastProgress = Math.max(lastProgress, Math.max(0, Math.min(100, p)));
                                setProgress(lastProgress, msg);
                            } else {
                                setProgress(lastProgress, msg);
                            }
                        } catch (_) {}
                    },
                });

                if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                    throw new Error('Plotly is not available in Through-Focus MTF popup');
                }
                const xAxis = Array.isArray(nativeResp?.xAxis) ? nativeResp.xAxis : [];
                const series = Array.isArray(nativeResp?.series) ? nativeResp.series : [];
                if (!xAxis.length || !series.length) {
                    throw new Error('Native Through-Focus MTF did not produce valid data');
                }

                const getColorForWavelengthPopup = (wl) => {
                    try {
                        if (typeof opener.getColorForWavelength === 'function') {
                            const c = opener.getColorForWavelength(wl);
                            if (typeof c === 'string' && c) return c;
                        }
                    } catch (_) {}
                    const nm = Number(wl) * 1000;
                    if (!Number.isFinite(nm)) return '#2563eb';
                    if (nm < 470) return '#2563eb';
                    if (nm < 530) return '#16a34a';
                    if (nm < 600) return '#f59e0b';
                    return '#dc2626';
                };

                const traces = [];
                for (let i = 0; i < series.length; i++) {
                    const s = series[i] || {};
                    const wl = Number(s.wavelengthUm);
                    const nm = Number.isFinite(wl) ? (wl * 1000).toFixed(1) : 'N/A';
                    const color = getColorForWavelengthPopup(wl);
                    const tan = Array.isArray(s.mtfTangential) ? s.mtfTangential : [];
                    const sag = Array.isArray(s.mtfSagittal) ? s.mtfSagittal : [];
                    traces.push({
                        x: xAxis,
                        y: tan,
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Meridional (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'solid' },
                    });
                    traces.push({
                        x: xAxis,
                        y: sag,
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Sagittal (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dot' },
                    });
                }

                setProgress(85, 'Rendering Through-Focus MTF...');
                await window.Plotly.newPlot(containerEl, traces, {
                    title: String(Number.isFinite(targetFrequencyLpmm) ? targetFrequencyLpmm.toFixed(1) : 10) + ' lp/mm',
                    xaxis: { title: 'Defocus shift (mm)' },
                    yaxis: { title: 'MTF', range: [0, 1.05] },
                    margin: { l: 60, r: 20, t: 50, b: 50 },
                    showlegend: true,
                }, { responsive: true, displaylogo: false });

                hideProgress();
                return;
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const details = String((err && err.message) ? err.message : err || 'Unknown error');
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate Through-Focus MTF.<br/><span style="font-size:12px;color:#555;">' + details.replace(/</g, '&lt;').replace(/>/g, '&gt;') + '</span></div>';
                }
            }
        };

        document.getElementById('popup-show-through-focus-mtf-btn').addEventListener('click', () => window.renderThroughFocusMTF());
        window.addEventListener('focus', syncAllOptions);
        window.addEventListener('load', () => syncAllOptions());
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const h=document.getElementById("open-field-mtf-window-btn");h&&h.addEventListener("click",()=>{if(H.__fieldMtfPopup&&!H.__fieldMtfPopup.closed){try{H.__fieldMtfPopup.focus()}catch{}return}const N=n("Object MTF","width=900,height=650");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__fieldMtfPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Object MTF</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 110px;
        }
        .controls input[type="checkbox"] {
            width: auto;
            padding: 0;
            border: none;
            background: transparent;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-field-mtf-container { flex: 1 1 auto; min-height: 0; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-field-mtf-wavelength-select">Wavelength:</label>
        <select id="popup-field-mtf-wavelength-select"></select>
        <label for="popup-field-mtf-meridional-freq-input">1st Freq (lp/mm):</label>
        <input id="popup-field-mtf-meridional-freq-input" type="number" min="0" step="1" value="10" />
        <label for="popup-field-mtf-sagittal-freq-input">2nd Freq (lp/mm):</label>
        <input id="popup-field-mtf-sagittal-freq-input" type="number" min="0" step="1" value="30" />
        <label for="popup-field-mtf-min-field-input">Object min:</label>
        <input id="popup-field-mtf-min-field-input" type="number" step="0.001" value="0" />
        <label for="popup-field-mtf-max-field-input">Object max:</label>
        <input id="popup-field-mtf-max-field-input" type="number" step="0.001" value="10" />
        <label for="popup-field-mtf-steps-input">Steps:</label>
        <input id="popup-field-mtf-steps-input" type="number" min="3" max="201" step="1" value="21" />
        <label for="popup-field-mtf-sampling-select">Sampling:</label>
        <select id="popup-field-mtf-sampling-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label for="popup-field-mtf-zeropad-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-field-mtf-zeropad-select">
            <option value="none">None</option>
            <option value="auto" selected>Auto</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-field-mtf-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <button id="popup-show-field-mtf-btn" type="button">Show Plot</button>
    </div>
    <div id="popup-field-mtf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-field-mtf-progress-text" style="margin-bottom: 6px;">Calculating...</div>
        <progress id="popup-field-mtf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-field-mtf-container"></div>
    </div>

    <script>
        function safeCall(fn, fallback) {
            try { return fn(); } catch (_) { return fallback; }
        }

        function getOpener() {
            try { return window.opener || window; } catch (_) { return window; }
        }

        function popupLog(...args) {
            try { console.log(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.log === 'function') {
                    op.console.log(...args);
                }
            } catch (_) {}
        }

        function popupError(...args) {
            try { console.error(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.error === 'function') {
                    op.console.error(...args);
                }
            } catch (_) {}
        }

        function getPrimaryWavelength() {
            const opener = getOpener();
            if (!opener) return null;
            if (typeof opener.getPrimaryWavelength !== 'function') return null;
            const v = Number(safeCall(() => opener.getPrimaryWavelength(), 0));
            return Number.isFinite(v) && v > 0 ? v : null;
        }

        function buildWavelengthOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getSourceRows = opener.getSourceRows;
            const sources = (typeof getSourceRows === 'function')
                ? safeCall(() => getSourceRows(opener.tableSource), [])
                : [];
            const primary = getPrimaryWavelength();
            const out = [{ value: 'all', label: 'All' }];
            if (Array.isArray(sources) && sources.length > 0) {
                for (let i = 0; i < sources.length; i++) {
                    const wl = Number(sources[i]?.wavelength);
                    if (!Number.isFinite(wl) || wl <= 0) continue;
                    const nm = wl * 1000;
                    const label = Number.isFinite(primary) && Math.abs(wl - primary) < 1e-9
                        ? (nm.toFixed(1) + ' nm (primary)')
                        : (nm.toFixed(1) + ' nm');
                    out.push({ value: String(wl), label });
                }
            }
            if (out.length === 1) {
                if (Number.isFinite(primary) && primary > 0) {
                    out.push({ value: String(primary), label: ((primary * 1000).toFixed(1) + ' nm') });
                }
            }
            return out;
        }

        function getAxisInfo() {
            const opener = getOpener();
            if (!opener) return { mode: 'angle', label: 'Object Angle (deg)', unit: 'deg', max: 10 };

            // Priority 1: check optical system first surface thickness.
            // If INF (infinite conjugate), object coordinates MUST be angles.
            let detectedMode = null;
            try {
                const getOpticalSystemRows = opener.getOpticalSystemRows;
                if (typeof getOpticalSystemRows === 'function') {
                    const optRows = safeCall(() => getOpticalSystemRows(opener.tableOpticalSystem), []);
                    const firstSurf = Array.isArray(optRows) && optRows.length > 0 ? optRows[0] : null;
                    if (firstSurf) {
                        const thickness = firstSurf.thickness ?? firstSurf.Thickness;
                        const isInf = thickness === 'INF' || thickness === Infinity || String(thickness).trim().toUpperCase() === 'INF';
                        if (isInf) {
                            detectedMode = 'angle';
                        } else {
                            const numThick = parseFloat(String(thickness));
                            if (Number.isFinite(numThick) && numThick > 0) {
                                detectedMode = 'finite'; // finite conjugate, defer to position field
                            }
                        }
                    }
                }
            } catch (_) {}

            // Priority 2: object rows position field (used when finite conjugate or inconclusive)
            const getObjectRows = opener.getObjectRows;
            const objects = (typeof getObjectRows === 'function')
                ? safeCall(() => getObjectRows(opener.tableObject), [])
                : [];
            const first = Array.isArray(objects) && objects.length > 0 ? objects[0] : null;

            let isAngle;
            if (detectedMode === 'angle') {
                isAngle = true;
            } else {
                const posRaw = String(first?.position ?? first?.object ?? first?.objectType ?? 'Angle');
                isAngle = /\bangle\b/i.test(posRaw);
            }

            const unit = isAngle ? 'deg' : 'mm';
            const label = isAngle ? 'Object Angle (deg)' : 'Object Height (mm)';
            let maxVal = 10;
            if (Array.isArray(objects) && objects.length > 0) {
                const vals = objects.map(o => Number(o?.yHeightAngle)).filter(v => Number.isFinite(v));
                if (vals.length > 0) {
                    maxVal = Math.max(1e-6, Math.max.apply(null, vals.map(v => Math.abs(v))));
                }
            }
            return { mode: isAngle ? 'angle' : 'height', label, unit, max: maxVal };
        }

        function populateSelect(selectEl, options) {
            if (!selectEl) return;
            const current = selectEl.value;
            selectEl.innerHTML = '';
            for (const opt of options) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                selectEl.appendChild(o);
            }
            if (current && Array.from(selectEl.options).some(o => o.value === current)) {
                selectEl.value = current;
            }
        }

        function syncAllOptions() {
            const wlSel = document.getElementById('popup-field-mtf-wavelength-select');
            const prevWl = wlSel ? wlSel.value : '';
            populateSelect(wlSel, buildWavelengthOptions());
            if (wlSel && (!prevWl || !Array.from(wlSel.options).some(o => o.value === prevWl))) {
                const primary = getPrimaryWavelength();
                if (Number.isFinite(primary) && Array.from(wlSel.options).some(o => o.value === String(primary))) {
                    wlSel.value = String(primary);
                } else {
                    const firstNumeric = Array.from(wlSel.options).find(o => o.value !== 'all');
                    if (firstNumeric) wlSel.value = firstNumeric.value;
                }
            }

            const axisInfo = getAxisInfo();
            const maxEl = document.getElementById('popup-field-mtf-max-field-input');
            if (maxEl && !maxEl.dataset.cooptInit) {
                maxEl.dataset.cooptInit = '1';
                maxEl.value = String(axisInfo.max || 10);
            }
        }

        window['renderFieldMTF'] = async () => {
            const containerEl = document.getElementById('popup-field-mtf-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-field-mtf-progress-wrapper');
            const progressEl = document.getElementById('popup-field-mtf-progress');
            const progressTextEl = document.getElementById('popup-field-mtf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const wlSel = document.getElementById('popup-field-mtf-wavelength-select');
            const minFieldEl = document.getElementById('popup-field-mtf-min-field-input');
            const maxFieldEl = document.getElementById('popup-field-mtf-max-field-input');
            const stepsEl = document.getElementById('popup-field-mtf-steps-input');
            const meridionalEl = document.getElementById('popup-field-mtf-meridional-freq-input');
            const sagittalEl = document.getElementById('popup-field-mtf-sagittal-freq-input');
            const samplingEl = document.getElementById('popup-field-mtf-sampling-select');
            const zeroPadEl = document.getElementById('popup-field-mtf-zeropad-select');
            const removePtdEl = document.getElementById('popup-field-mtf-remove-ptd-checkbox');

            const wlValue = wlSel ? String(wlSel.value) : '';
            const primary = getPrimaryWavelength();
            const wavelength = (wlValue === 'all') ? 'all' : Number(wlValue);
            const fieldMin = minFieldEl ? Number(minFieldEl.value) : 0;
            const fieldMax = maxFieldEl ? Number(maxFieldEl.value) : 10;
            const steps = stepsEl ? Number(stepsEl.value) : 21;
            const meridionalFreq = meridionalEl ? Number(meridionalEl.value) : 10;
            const sagittalFreq = sagittalEl ? Number(sagittalEl.value) : 30;
            const sampling = samplingEl ? Number(samplingEl.value) : 256;
            const zeroPadRaw = zeroPadEl ? String(zeroPadEl.value || 'auto') : 'auto';
            const opdDisplayMode = (removePtdEl && removePtdEl.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const zeroPadTo = (zeroPadRaw === 'none')
                ? (Number.isFinite(sampling) ? sampling : 256)
                : (zeroPadRaw === 'auto')
                    ? 0
                    : (Number.isFinite(parseInt(zeroPadRaw, 10)) ? parseInt(zeroPadRaw, 10) : 0);

            const axisInfo = getAxisInfo();

            try {
                const opener = getOpener();
                if (!opener) {
                    throw new Error('Opener is not available');
                }
                setProgress(0, 'Starting...');
                await new Promise(r => setTimeout(r, 0));
                if (wavelength !== 'all' && !Number.isFinite(wavelength) && !(Number.isFinite(primary) && primary > 0)) {
                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                }

                if (typeof opener.runPortableFieldMtfForPopup !== 'function') {
                    throw new Error('runPortableFieldMtfForPopup is not available on opener');
                }

                const sourceRows = (typeof opener.getSourceRows === 'function')
                    ? (safeCall(() => opener.getSourceRows(opener.tableSource), []) || [])
                    : [];
                const wavelengthList = (() => {
                    const out = [];
                    if (wavelength === 'all') {
                        if (Array.isArray(sourceRows) && sourceRows.length > 0) {
                            for (let i = 0; i < sourceRows.length; i++) {
                                const wl = Number(sourceRows[i]?.wavelength);
                                if (!Number.isFinite(wl) || wl <= 0) continue;
                                if (out.some(v => Math.abs(v - wl) < 1e-9)) continue;
                                out.push(wl);
                            }
                        }
                        if (out.length === 0 && Number.isFinite(primary) && primary > 0) {
                            out.push(primary);
                        }
                    } else {
                        const wl = (Number.isFinite(Number(wavelength)) && Number(wavelength) > 0)
                            ? Number(wavelength)
                            : ((Number.isFinite(primary) && primary > 0) ? primary : 0.5876);
                        out.push(wl);
                    }
                    if (out.length === 0) out.push(0.5876);
                    return out;
                })();

                setProgress(20, 'Computing Object MTF (Rust native)...');
                const nativeResp = await opener.runPortableFieldMtfForPopup({
                    objectIndex: 0,
                    wavelengths: wavelengthList,
                    firstFrequencyLpmm: Number.isFinite(meridionalFreq) ? meridionalFreq : 10,
                    secondFrequencyLpmm: Number.isFinite(sagittalFreq) ? sagittalFreq : 30,
                    fieldMin: Number.isFinite(fieldMin) ? fieldMin : 0,
                    fieldMax: Number.isFinite(fieldMax) ? fieldMax : 10,
                    steps: Number.isFinite(steps) ? steps : 21,
                    samplingSize: Number.isFinite(sampling) ? sampling : 256,
                    zeroPadTo,
                    opdDisplayMode,
                    fieldAxisMode: axisInfo.mode,
                    onProgress: (evt) => {
                        const p = Number(evt && evt.percent);
                        const msg = String((evt && evt.message) || 'Computing Object MTF...');
                        if (Number.isFinite(p)) {
                            setProgress(p, msg);
                        } else {
                            setProgress(20, msg);
                        }
                    },
                });

                if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                    throw new Error('Plotly is not available in Object MTF popup');
                }
                const xAxis = Array.isArray(nativeResp?.xAxis) ? nativeResp.xAxis : [];
                const series = Array.isArray(nativeResp?.series) ? nativeResp.series : [];
                if (!xAxis.length || !series.length) {
                    throw new Error('Native Object MTF did not produce valid data');
                }

                try {
                    const diag = Array.isArray(series?.[0]?.fieldDiagnostics) ? series[0].fieldDiagnostics : [];
                    if (diag.length) {
                        popupLog('📊 [Object MTF diag] field diagnostics count=', diag.length);
                        const jumps = [];
                        for (let i = 1; i < diag.length; i++) {
                            const a = diag[i - 1];
                            const b = diag[i];
                            const d1 = Math.abs(Number(b?.firstValueMeridional) - Number(a?.firstValueMeridional));
                            const d2 = Math.abs(Number(b?.secondValueMeridional) - Number(a?.secondValueMeridional));
                            if ((Number.isFinite(d1) && d1 > 0.12) || (Number.isFinite(d2) && d2 > 0.12)) {
                                jumps.push({
                                    fromField: Number(a?.fieldValue),
                                    toField: Number(b?.fieldValue),
                                    deltaFirstMeridional: d1,
                                    deltaSecondMeridional: d2,
                                    modeFrom: String(a?.effectivePupilSamplingMode || ''),
                                    modeTo: String(b?.effectivePupilSamplingMode || ''),
                                    firstBracketTo: [Number(b?.firstBracketLowLpmm), Number(b?.firstBracketHighLpmm)],
                                    secondBracketTo: [Number(b?.secondBracketLowLpmm), Number(b?.secondBracketHighLpmm)],
                                });
                            }
                        }
                        if (jumps.length) {
                            popupLog('⚠️ [Object MTF diag] potential discontinuities:', jumps.slice(0, 20));
                        }
                    }
                } catch (_) {}

                const getColorForWavelengthPopup = (wl) => {
                    try {
                        if (typeof opener.getColorForWavelength === 'function') {
                            const c = opener.getColorForWavelength(wl);
                            if (typeof c === 'string' && c) return c;
                        }
                    } catch (_) {}
                    const nm = Number(wl) * 1000;
                    if (!Number.isFinite(nm)) return '#2563eb';
                    if (nm < 470) return '#2563eb';
                    if (nm < 530) return '#16a34a';
                    if (nm < 600) return '#f59e0b';
                    return '#dc2626';
                };

                const firstFreqText = String(Number.isFinite(meridionalFreq) ? meridionalFreq.toFixed(1) : '10.0');
                const secondFreqText = String(Number.isFinite(sagittalFreq) ? sagittalFreq.toFixed(1) : '30.0');
                const traces = [];
                for (let i = 0; i < series.length; i++) {
                    const s = series[i] || {};
                    const wl = Number(s.wavelengthUm);
                    const nm = Number.isFinite(wl) ? (wl * 1000).toFixed(1) : 'N/A';
                    const color = getColorForWavelengthPopup(wl);

                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.meridionalFirst) ? s.meridionalFirst : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Meridional ' + firstFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'solid' },
                    });
                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.sagittalFirst) ? s.sagittalFirst : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Sagittal ' + firstFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dot' },
                    });
                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.meridionalSecond) ? s.meridionalSecond : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Meridional ' + secondFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dash' },
                    });
                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.sagittalSecond) ? s.sagittalSecond : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Sagittal ' + secondFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dashdot' },
                    });
                }

                setProgress(85, 'Rendering Object MTF...');
                await window.Plotly.newPlot(containerEl, traces, {
                    title: firstFreqText + ' / ' + secondFreqText + ' lp/mm',
                    xaxis: { title: axisInfo.label },
                    yaxis: { title: 'MTF', range: [0, 1.05] },
                    margin: { l: 60, r: 20, t: 50, b: 50 },
                    showlegend: true,
                }, { responsive: true, displaylogo: false });

                hideProgress();
                return;
            } catch (err) {
                popupError('[Object MTF Popup] renderFieldMTF failed', err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const details = String((err && err.message) ? err.message : err || 'Unknown error');
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate Object MTF.<br/><span style="font-size:12px;color:#555;">' + details.replace(/</g, '&lt;').replace(/>/g, '&gt;') + '</span></div>';
                }
            }
        };

        document.getElementById('popup-show-field-mtf-btn').addEventListener('click', () => window.renderFieldMTF());
        window.addEventListener('focus', syncAllOptions);
        window.addEventListener('load', () => syncAllOptions());
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const g=document.getElementById("open-transverse-aberration-window-btn");g&&g.addEventListener("click",()=>{if(H.__transverseAberrationPopup&&!H.__transverseAberrationPopup.closed){try{H.__transverseAberrationPopup.focus()}catch{}try{typeof H.__transverseAberrationPopup.renderTransverseAberration=="function"&&H.__transverseAberrationPopup.renderTransverseAberration()}catch{}return}const N=n("Transverse Aberration","width=800,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}H.__transverseAberrationPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Transverse Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 90px;
        }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-transverse-aberration-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-transverse-ray-count-input">Ray number:</label>
        <input type="number" id="popup-transverse-ray-count-input" value="101" min="9" max="10001" step="1" />
        <span class="note-inline" style="font-size:12px;color:#666;">(Always normalized by stop diameter)</span>
        <button id="popup-show-transverse-aberration-btn" type="button">Show transverse aberration diagram</button>
    </div>
    <div id="popup-transverse-progress-wrapper" style="display:none;padding:10px 12px;border-bottom:1px solid #eee;background:#fff;">
        <div id="popup-transverse-progress-text" style="margin-bottom: 6px; font-size:12px; color:#555;">Starting...</div>
        <progress id="popup-transverse-progressbar" value="0" max="100" style="display:block;width:calc(100% + 24px);margin-left:-12px;height:14px;"></progress>
    </div>
    <div class="content">
        <div id="popup-transverse-aberration-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerRay = getOpenerEl('transverse-ray-count-input');
            const popupRay = document.getElementById('popup-transverse-ray-count-input');
            if (openerRay && popupRay) {
                popupRay.value = openerRay.value;
            }
        }

        window['renderTransverseAberration'] = async () => {
            const progressWrap = document.getElementById('popup-transverse-progress-wrapper');
            const progressBar = document.getElementById('popup-transverse-progressbar');
            const progressText = document.getElementById('popup-transverse-progress-text');
            const setProgress = (percent, message) => {
                try {
                    if (progressWrap) progressWrap.style.display = 'block';
                    if (progressBar && Number.isFinite(percent)) progressBar.value = Math.max(0, Math.min(100, percent));
                    if (progressText) progressText.textContent = message || '';
                } catch (_) {}
            };
            const hideProgress = () => {
                try {
                    if (progressWrap) progressWrap.style.display = 'none';
                } catch (_) {}
            };
            const onProgress = (evt) => {
                const p = Number(evt?.percent);
                const msg = (evt && (evt.message || evt.phase)) ? String(evt.message || evt.phase) : '';
                setProgress(Number.isFinite(p) ? p : 0, msg);
            };

            const popupRay = document.getElementById('popup-transverse-ray-count-input');
            const rayCount = popupRay ? parseInt(popupRay.value, 10) : 51;
            const openerRay = getOpenerEl('transverse-ray-count-input');
            if (openerRay && Number.isFinite(rayCount)) {
                openerRay.value = String(rayCount);
            }

            const containerEl = document.getElementById('popup-transverse-aberration-container');
            if (containerEl) containerEl.innerHTML = '';

            try {
                if (!window.opener || typeof window.opener.showTransverseAberrationDiagram !== 'function') {
                    throw new Error('showTransverseAberrationDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                await window.opener.showTransverseAberrationDiagram({
                    rayCount: Number.isFinite(rayCount) ? rayCount : 51,
                    containerElement: containerEl,
                    onProgress
                });
                hideProgress();
            } catch (err) {
                console.error(err);
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate transverse aberration diagram. Check console.</div>';
                }
                setProgress(100, 'Failed');
            }
        };

        document.getElementById('popup-show-transverse-aberration-btn').addEventListener('click', () => {
            window.renderTransverseAberration();
        });

        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderTransverseAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}});const w=document.getElementById("open-settings-btn"),R=()=>{Ma();const N=(()=>{try{return new URL(window.location.href).searchParams.get("coopt_settings_window")==="1"}catch{return!1}})();if(st()&&!N){(async()=>{try{const{WebviewWindow:b}=await De(async()=>{const{WebviewWindow:M}=await import("./webviewWindow-Bw_7PlN0.js");return{WebviewWindow:M}},__vite__mapDeps([4,5,0,1,2])),A="settings-window",v=await b.getByLabel(A);if(v){await v.setFocus();return}const S=new URL(window.location.href);S.searchParams.set("coopt_settings_window","1");const _=Sn();_?S.searchParams.set("coopt_force_mode",_):S.searchParams.delete("coopt_force_mode"),new b(A,{title:"Settings",url:S.toString(),width:520,height:620,resizable:!0,focus:!0})}catch(b){console.error("❌ [Settings][Desktop] WebviewWindow error:",b),alert("Failed to open Settings window.")}})();return}if(H.__settingsPopup&&!H.__settingsPopup.closed){try{H.__settingsPopup.focus()}catch{}return}const I=st()&&N?window:window.open("","Settings","width=520,height=340");if(!I){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}st()&&N||(H.__settingsPopup=I),I.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title></title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .content {
            padding: 12px;
            background: #fff;
            flex: 1 1 auto;
            overflow: auto;
        }
        .section-title { font-size: 13px; font-weight: 600; color: #333; margin: 0 0 8px 0; }
        .help { font-size: 12px; color: #666; margin: 0 0 10px 0; line-height: 1.35; }
        .radio-group { display: flex; flex-direction: column; gap: 8px; margin: 8px 0 12px 0; }
        label { font-size: 13px; color: #333; }
        .footer {
            padding: 10px 12px;
            border-top: 1px solid #ddd;
            background: #f8f8f8;
            display: flex;
            justify-content: flex-end;
            gap: 8px;
        }
        code { font-family: Menlo, Consolas, monospace; font-size: 12px; }
    </style>
</head>
<body>
    <div class="header"></div>
    <div class="content">
        <div class="section-title">Glass Map: Default Manufacturers</div>
        <div class="help">
            Choose which manufacturers are enabled by default when opening Glass Map.
            <br />If nothing is selected, Glass Map will show all manufacturers.
        </div>

        <div class="checkbox-group" style="display:flex;flex-direction:column;gap:8px;margin:8px 0 14px 0;">
            <label><input type="checkbox" class="glassmap-mfr-cb" value="SCHOTT" /> SCHOTT</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="HOYA" /> HOYA</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="HIKARI" /> HIKARI</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="OHARA" /> OHARA</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="Sumita" /> Sumita</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="CDGM" /> CDGM</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="Special" /> Special</label>
        </div>

        <div class="section-title">Dark Mode</div>
        <div class="help">
            Enable VS Code-style dark mode for the entire UI.
        </div>
        <label style="margin: 8px 0 14px 0; display: block;">
            <input type="checkbox" id="dark-mode-cb" /> Enable Dark Mode
        </label>

        <div class="section-title">Infinite Field: Pupil Sampling Mode</div>
        <div class="help">
            Fix the sampling mode used for infinite-field wavefront/PSF/MTF generation.
            <br />
            This sets <code>__COOPT_FORCE_INFINITE_PUPIL_MODE</code> to <code>stop</code> or <code>entrance</code>.
        </div>

        <div class="radio-group">
            <label><input type="radio" name="force-mode" value="" /> Auto (default)</label>
            <label><input type="radio" name="force-mode" value="stop" /> Force <code>stop</code></label>
            <label><input type="radio" name="force-mode" value="entrance" /> Force <code>entrance</code></label>
        </div>

        <div class="help" style="margin-top: 6px;">
            Note: Changes take effect on the next calculation.
        </div>
    </div>
    <div class="footer"></div>

    <script>
        const KEY = 'coopt.forceInfinitePupilMode';
        const isDesktopRuntime = !!(window && (window.__TAURI_INTERNALS__ || window.__TAURI__));
        const GLASS_MAP_MFR_KEY = 'coopt.glassMap.defaultManufacturers';
        const DARK_MODE_KEY = 'coopt.darkMode';
        const sanitize = (v) => {
            const s = (typeof v === 'string') ? v.trim().toLowerCase() : '';
            return (s === 'stop' || s === 'entrance') ? s : '';
        };

        const sanitizeMfrList = (list) => {
            if (!Array.isArray(list)) return [];
            const allow = new Set(['SCHOTT', 'HOYA', 'HIKARI', 'OHARA', 'SUMITA', 'CDGM', 'SPECIAL']);
            const out = [];
            for (const v of list) {
                const s = String(v ?? '').trim();
                if (!s) continue;
                const upper = s.toUpperCase();
                if (!allow.has(upper)) continue;
                // Preserve canonical casing used in the checkboxes.
                if (upper === 'SUMITA') out.push('Sumita');
                else if (upper === 'SPECIAL') out.push('Special');
                else out.push(upper);
            }
            // Deduplicate
            return Array.from(new Set(out));
        };

        function getOpener() {
            try { return window.opener || null; } catch (_) { return null; }
        }

        async function readDesktopModeDirect() {
            try {
                const invoke = window?.__TAURI_INTERNALS__?.invoke || window?.__TAURI__?.core?.invoke;
                if (typeof invoke !== 'function') return '';
                const raw = await invoke('read_desktop_setting', { key: KEY });
                return sanitize(raw);
            } catch (_) {
                return '';
            }
        }

        async function writeDesktopModeDirect(mode) {
            const m = sanitize(mode);
            try {
                const invoke = window?.__TAURI_INTERNALS__?.invoke || window?.__TAURI__?.core?.invoke;
                if (typeof invoke !== 'function') return;
                await invoke('write_desktop_setting', { key: KEY, value: m || null });
            } catch (_) {}
        }

        function getFromWindow(target) {
            if (!target) return '';
            try {
                if (typeof target.__cooptGetForceInfinitePupilMode === 'function') {
                    const m = sanitize(target.__cooptGetForceInfinitePupilMode());
                    if (m) return m;
                }
            } catch (_) {}
            try {
                return sanitize(target.__COOPT_FORCE_INFINITE_PUPIL_MODE ?? target.COOPT_FORCE_INFINITE_PUPIL_MODE);
            } catch (_) {
                return '';
            }
        }

        function setToWindow(target, mode) {
            if (!target) return;
            try {
                if (typeof target.__cooptSetForceInfinitePupilMode === 'function') {
                    target.__cooptSetForceInfinitePupilMode(mode);
                    return;
                }
            } catch (_) {}
            try {
                if (mode) {
                    target.__COOPT_FORCE_INFINITE_PUPIL_MODE = mode;
                    target.COOPT_FORCE_INFINITE_PUPIL_MODE = mode;
                } else {
                    try { delete target.__COOPT_FORCE_INFINITE_PUPIL_MODE; } catch (_) { target.__COOPT_FORCE_INFINITE_PUPIL_MODE = undefined; }
                    try { delete target.COOPT_FORCE_INFINITE_PUPIL_MODE; } catch (_) { target.COOPT_FORCE_INFINITE_PUPIL_MODE = undefined; }
                }
            } catch (_) {}
        }

        function getCurrent() {
            const selfMode = getFromWindow(window);
            if (selfMode) return selfMode;

            const o = getOpener();
            const openerMode = getFromWindow(o);
            if (openerMode) return openerMode;

            try {
                const stored = sanitize(localStorage.getItem(KEY));
                if (stored) return stored;
            } catch (_) {}

            try {
                const fromUrl = sanitize(new URL(window.location.href).searchParams.get('coopt_force_mode'));
                if (fromUrl) return fromUrl;
            } catch (_) {}

            return '';
        }

        function applyMode(mode) {
            const m = sanitize(mode);
            setToWindow(window, m);

            const o = getOpener();
            setToWindow(o, m);

            try {
                if (m) localStorage.setItem(KEY, m);
                else localStorage.removeItem(KEY);
            } catch (_) {}

            try {
                if (typeof window.__cooptBroadcastForceInfinitePupilMode === 'function') {
                    window.__cooptBroadcastForceInfinitePupilMode(m);
                }
            } catch (_) {}

            writeDesktopModeDirect(m);
        }

        async function hydrateFromDesktopStore() {
            const direct = await readDesktopModeDirect();
            if (direct) {
                setToWindow(window, direct);
                try { localStorage.setItem(KEY, direct); } catch (_) {}
                syncUI();
                return;
            }
        }

        function syncUI() {
            const cur = getCurrent();
            const radios = document.querySelectorAll('input[name="force-mode"]');
            radios.forEach(r => {
                r.checked = (sanitize(r.value) === cur);
                if (cur === '' && sanitize(r.value) === '') r.checked = true;
            });

            // Glass Map manufacturers
            let stored = [];
            try {
                stored = sanitizeMfrList(JSON.parse(localStorage.getItem(GLASS_MAP_MFR_KEY) || '[]'));
            } catch (_) {
                stored = [];
            }
            const storedSet = new Set(stored.map(s => String(s).toUpperCase()));
            document.querySelectorAll('.glassmap-mfr-cb').forEach(cb => {
                const v = String(cb.value || '');
                cb.checked = storedSet.has(v.toUpperCase());
            });

            // Dark Mode
            const darkModeCb = document.getElementById('dark-mode-cb');
            if (darkModeCb) {
                let isDark = false;
                try {
                    isDark = localStorage.getItem(DARK_MODE_KEY) === 'true';
                } catch (_) {}
                darkModeCb.checked = isDark;
            }
        }

        function saveGlassMapMfrSelection() {
            const selected = [];
            document.querySelectorAll('.glassmap-mfr-cb').forEach(cb => {
                if (cb.checked) selected.push(cb.value);
            });
            const sanitized = sanitizeMfrList(selected);
            try {
                if (sanitized.length) localStorage.setItem(GLASS_MAP_MFR_KEY, JSON.stringify(sanitized));
                else localStorage.removeItem(GLASS_MAP_MFR_KEY);
            } catch (_) {}
        }

        function applyDarkMode(enabled) {
            const o = getOpener();
            try {
                if (o && typeof o.__cooptSetDarkMode === 'function') {
                    o.__cooptSetDarkMode(enabled);
                }
            } catch (_) {}
            
            try {
                localStorage.setItem(DARK_MODE_KEY, enabled ? 'true' : 'false');
            } catch (_) {}
        }

        document.querySelectorAll('input[name="force-mode"]').forEach(r => {
            r.addEventListener('change', () => {
                if (r.checked) applyMode(r.value);
            });
        });

        document.querySelectorAll('.glassmap-mfr-cb').forEach(cb => {
            cb.addEventListener('change', () => {
                saveGlassMapMfrSelection();
            });
        });

        const darkModeCb = document.getElementById('dark-mode-cb');
        if (darkModeCb) {
            darkModeCb.addEventListener('change', () => {
                applyDarkMode(darkModeCb.checked);
            });
        }

        window.addEventListener('focus', syncUI);
        syncUI();
        hydrateFromDesktopStore();
    <\/script>
</body>
</html>
                        `);try{I.document.close()}catch{}};w&&!st()&&(w.getAttribute("data-react-handled")==="1"||w.addEventListener("click",R)),!st()&&!H.__cooptSettingsButtonDelegatedBound&&(document.addEventListener("click",N=>{try{const I=N.target;if(!I||typeof I.closest!="function")return;const b=I.closest("#open-settings-btn");if(!b||b.getAttribute("data-react-handled")==="1")return;R()}catch{}}),H.__cooptSettingsButtonDelegatedBound=!0),(()=>{const N="coopt.darkMode";function I(A){A?document.body.classList.add("dark-mode"):document.body.classList.remove("dark-mode")}function b(){try{return localStorage.getItem(N)==="true"}catch{return!1}}H.__cooptSetDarkMode=A=>{I(A)},I(b())})()}function Ep(){const e=document.getElementById("transform-surface-select"),t=document.getElementById("show-local-coords-btn"),n=document.getElementById("cancel-transform-btn"),o=document.getElementById("save-local-coords-btn"),r=document.getElementById("transform-error-bar"),s=document.getElementById("transform-error-text"),a=document.getElementById("transform-progress-wrapper"),c=document.getElementById("transform-progress-text"),i=document.getElementById("transform-progressbar"),l=w=>{r&&s&&(s.textContent=w,r.style.display="")},f=()=>{r&&(r.style.display="none")},p=(w,R)=>{a&&(a.style.display="block"),i&&Number.isFinite(w)&&(i.value=Math.max(0,Math.min(100,w))),c&&R&&(c.textContent=R)},d=()=>{a&&(a.style.display="none")},u=w=>{const R=String(w?.surfType??w?.["surf type"]??w?.type??"").toLowerCase().replace(/\s+/g,"");return R==="coordbreak"||R==="coordinatebreak"||R==="cb"||R==="coordtrans"||R==="coordinatetransform"||R==="ct"},y=w=>{const R=w?.["object type"]??w?.object??w?.Object;return String(R??"").toLowerCase()==="object"},m=w=>String(w?.surfType??w?.["surf type"]??w?.type??"").toLowerCase()==="gap",h=(w,R)=>{if(!Array.isArray(w)||!Number.isFinite(R))return null;const N=Math.floor(R);if(N<=0)return null;let I=0;for(let b=0;b<w.length;b++){const A=w[b];if(!u(A)&&!y(A)&&!m(A)&&(I++,I===N))return b}return null};t&&t.addEventListener("click",async function(){f();try{const w=parseInt(e?.value||"",10);if(!Number.isFinite(w)){l("Please select a surface first.");return}const R=H.getOpticalSystemRows;if(typeof R!="function"){l("Optical system data not available.");return}const N=R();if(!N||N.length===0){l("No optical system data. Please load or create an optical system.");return}const I=h(N,w);if(I===null){l("Selected surface could not be resolved.");return}t.disabled=!0,n&&(n.style.display=""),o&&(o.style.display="none"),H._transformCalculationCancelled=!1;const b=H.calculateAllSurfacesLocalCoordinates;if(typeof b!="function"){l("Coordinate transformation function not available."),t.disabled=!1,n&&(n.style.display="none");return}const A=await b(N,I,(v,S)=>p(v,S));H._cachedLocalCoords=A,H._showLocalCoords=!0,H.tableOpticalSystem&&H.tableOpticalSystem.redraw(),o&&(o.style.display=""),d()}catch(w){console.error("Coordinate transformation error:",w),l(w.message||"Failed to calculate local coordinates."),d()}finally{t.disabled=!1,n&&(n.style.display="none")}}),n&&n.addEventListener("click",function(){H._transformCalculationCancelled=!0,n&&(n.style.display="none"),d(),l("Calculation cancelled by user.")}),o&&o.addEventListener("click",function(){try{if(!H._cachedLocalCoords){l("No coordinate data to save. Please calculate first.");return}const w=H._cachedLocalCoords,R=JSON.stringify(w,null,2),N=new Blob([R],{type:"application/json"}),I=URL.createObjectURL(N),b=new Date().toISOString().replace(/[:.]/g,"-").slice(0,19),v=`local-coords-surf${w.metadata?.targetSurfaceIndex??"unknown"}-${b}.json`,S=document.createElement("a");S.href=I,S.download=v,S.click(),URL.revokeObjectURL(I)}catch(w){console.error("Save error:",w),l("Failed to save JSON file: "+w.message)}}),Ca();const g=document.getElementById("analysis-select");g&&(g.getAttribute("data-react-handled")==="1"||g.addEventListener("change",()=>{const R=g.value;if(!R)return;g.value="";const I={"spot-diagram":"open-spot-diagram-window-btn","spherical-aberration":"open-spherical-aberration-window-btn",astigmatism:"open-astigmatism-window-btn",distortion:"open-distortion-window-btn","distortion-grid":"open-distortion-grid-window-btn","magnification-chromatic-aberration":"open-magnification-chromatic-aberration-window-btn","integrated-aberration":"open-integrated-aberration-window-btn","transverse-aberration":"open-transverse-aberration-window-btn",opd:"open-opd-window-btn",psf:"open-psf-window-btn",mtf:"open-mtf-window-btn","through-focus-spot":"open-through-focus-spot-window-btn","through-focus-mtf":"open-through-focus-mtf-window-btn","field-mtf":"open-field-mtf-window-btn"}[R];if(I){try{typeof H.setupAnalysisWindows=="function"&&H.setupAnalysisWindows()}catch{}const b=document.getElementById(I);b&&b.click()}}))}function Ca(){const e=document.getElementById("transform-surface-select");if(e)try{const t=H.getOpticalSystemRows;if(typeof t!="function")return;const n=t();if(!n||n.length===0)return;e.innerHTML='<option value="">Select surface...</option>';let o=0;n.forEach((r,s)=>{if(String(r?.["object type"]??r?.object??"").toLowerCase()==="object")return;const c=String(r?.surfType??r?.type??"").toLowerCase();if(c==="ct"||c==="coordtrans"||c==="coordinatebreak"||c==="coord trans"||c==="coordinate break")return;o+=1;const i=document.createElement("option");i.value=String(o);let l=`Surf ${o}`;r.comment?l+=`: ${r.comment}`:r.material&&r.material!=="AIR"&&(l+=`: ${r.material}`),i.textContent=l,e.appendChild(i)})}catch(t){console.error("Error updating transform surface select:",t)}}const Zn=window;function Ia(){const e=document.getElementById("surface-number-select");if(!e)return;const t=e.selectedOptions&&e.selectedOptions.length>0?e.selectedOptions[0]:null,n=t&&t.dataset&&t.dataset.rowId?String(t.dataset.rowId):"",o=e.value!==void 0&&e.value!==null?String(e.value):"";e.innerHTML='<option value="">Select Surf</option>';let r=null;try{const l=Zn.tableOpticalSystem?.getData?.();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=Zn.opticalSystemTabulator?.getData?.();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=Zn.getOpticalSystemRows?.();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=Je(Zn.tableOpticalSystem);Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=Je();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r||r.length===0)return;const s=Es(r);let a=null,c=null;for(const l of s){const f=document.createElement("option");f.value=String(l.value),f.textContent=l.label,l.rowId!==void 0&&l.rowId!==null&&String(l.rowId)!==""&&(f.dataset.rowId=String(l.rowId)),Number.isInteger(l.rowIndex)&&(f.dataset.rowIndex=String(l.rowIndex)),typeof l.label=="string"&&l.label.includes("(Image)")&&(a=String(l.value)),c=String(l.value),e.appendChild(f)}let i=!1;if(n){for(const l of e.options)if(l&&l.dataset&&String(l.dataset.rowId||"")===n){e.value=String(l.value),i=!0;break}}i||(o!==""&&e.querySelector(`option[value="${CSS.escape(o)}"]`)?e.value=o:a!==null?e.value=a:c!==null&&(e.value=c));try{const l=Zn.__spotDiagramPopup;l&&!l.closed&&(typeof l.__cooptSpotPopupSyncAll=="function"?l.__cooptSpotPopupSyncAll():typeof l.postMessage=="function"&&l.postMessage({action:"coopt-spot-sync"},"*"))}catch{}}function ss(){Ia()}function _r(e){return Number.isFinite(e)}function Op(){const e=hl(),t=[];for(const n of e)if(Array.isArray(n))for(const o of n){if(!o||typeof o!="object")continue;const r=o.nd,s=o.vd;!_r(r)||!_r(s)||s<=0||r<=0||t.push({name:String(o.name??""),manufacturer:String(o.manufacturer??"Unknown").trim()||"Unknown",nd:r,vd:s,price:_r(o.price)?Number(o.price):null})}return t}function Dp(){const e=document.querySelector('script[src*="plotly" i]');return e&&e.src?e.src:null}function as(e,t){if(typeof window>"u"||typeof document>"u")throw new Error("openGlassMapWindow must be called in a browser");if(typeof e!="function")throw new TypeError("openGlassMapWindow(onRegionSelected): onRegionSelected must be a function");if(typeof t<"u"&&typeof t!="function")throw new TypeError("openGlassMapWindow(onRegionSelected, onGlassSelected): onGlassSelected must be a function");const n=Op(),o=Dp(),r=window.open("","coopt_glass_map","popup=yes,width=800,height=600");if(!r)return alert("Popup was blocked. Please allow popups for this site and try again."),null;r.__COOPT_GLASS_POINTS__=n,r.__COOPT_ON_REGION_SELECTED__=e,r.__COOPT_ON_GLASS_SELECTED__=typeof t=="function"?t:null;const s=`<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Glass Map</title>
  <style>
    html, body { height: 100%; margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; }
    .wrap { height: 100%; display: flex; flex-direction: column; }
    .toolbar { display: flex; gap: 8px; align-items: center; padding: 8px 10px; border-bottom: 1px solid rgba(0,0,0,0.1); }
    .toolbar button { padding: 6px 10px; }
    .hint { color: rgba(0,0,0,0.7); font-size: 12px; }
    .header { padding: 10px 10px 0; }
    .title { font-size: 22px; font-weight: 600; color: rgba(0,0,0,0.85); margin: 0 0 6px; }
    .note { font-size: 12px; color: #666; margin: 0 0 10px; padding: 8px 12px; background-color: #f9f9f9; border-left: 3px solid #007acc; border-radius: 3px; }
    #plot { flex: 1; min-height: 0; }
  </style>
  ${o?`<script src="${o}"><\/script>`:""}
</head>
<body>
  <div class="wrap">
    <div class="toolbar">
      <button id="apply" disabled>Apply Region</button>
      <span class="hint">Drag a box to select a region (Box Select is active by default).</span>
    </div>
    <div class="header">
      <div class="title">Glass Map</div>
      <div class="note"><strong>Note:</strong> Set default manufacturers in Settings.<br>Price indices differ by manufacturer (e.g., HOYA uses a vendor-specific index).</div>
    </div>
    <div id="plot"></div>
  </div>

  <script>
    (function() {
      const points = window.__COOPT_GLASS_POINTS__ || [];
      const applyBtn = document.getElementById('apply');

      let lastRegion = null;

      function ensurePlotlyReady() {
        if (window.Plotly) return Promise.resolve(window.Plotly);
        try {
          if (window.opener && window.opener.Plotly) {
            window['Plotly'] = window.opener.Plotly;
            return Promise.resolve(window.Plotly);
          }
        } catch (e) {}
        return new Promise((resolve, reject) => {
          const start = Date.now();
          const timer = setInterval(() => {
            if (window.Plotly) {
              clearInterval(timer);
              resolve(window.Plotly);
              return;
            }
            if (Date.now() - start > 8000) {
              clearInterval(timer);
              reject(new Error('Plotly failed to load in popup window'));
            }
          }, 50);
        });
      }

      function splitByPrice(points) {
        const priced = [];
        const missing = [];
        for (const p of points) {
          // For log-scaled coloring, require strictly positive price values.
          if (Number.isFinite(p.price) && p.price > 0) priced.push(p);
          else missing.push(p);
        }
        return { priced, missing };
      }

      function log10Safe(x) {
        if (!Number.isFinite(x) || x <= 0) return NaN;
        // Math.log10 is widely supported, but keep a fallback.
        return (typeof Math.log10 === 'function') ? Math.log10(x) : (Math.log(x) / Math.LN10);
      }

      function buildLogTickValues(minPrice, maxPrice) {
        // Nice tick candidates for typical relative-price indices.
        const candidates = [
          1, 2, 3, 5,
          8, 10,
          13, 20,
          34, 50,
          80, 100,
          130, 200,
          340, 500,
          800, 1000
        ];

        /** @type {number[]} */
        const ticks = candidates.filter(v => v >= minPrice && v <= maxPrice);
        if (!ticks.includes(minPrice)) ticks.unshift(minPrice);
        if (!ticks.includes(maxPrice)) ticks.push(maxPrice);

        // Deduplicate (e.g., when min/max coincide with candidates).
        const unique = Array.from(new Set(ticks)).sort((a, b) => a - b);

        // Avoid overlapping tick labels near the upper end (e.g., 34 vs 36.7).
        // If two adjacent ticks are too close in log space, drop the lower one.
        while (unique.length >= 2) {
          const last = unique[unique.length - 1];
          const prev = unique[unique.length - 2];
          const d = log10Safe(last) - log10Safe(prev);
          if (!Number.isFinite(d) || d >= 0.06) break; // ~15% ratio
          unique.splice(unique.length - 2, 1);
        }

        return unique;
      }

      function makeHover(p) {
        const priceStr = Number.isFinite(p.price) ? p.price.toFixed(4) : 'null';
        return (
          String(p.manufacturer) + ' ' + String(p.name) +
          '<br>n<sub>d</sub>=' + Number(p.nd).toFixed(5) +
          '<br>V<sub>d</sub>=' + Number(p.vd).toFixed(2) +
          '<br>price=' + priceStr
        );
      }

      function groupByManufacturer(points) {
        /** @type {Map<string, any[]>} */
        const m = new Map();
        for (const p of points) {
          const key = (String(p?.manufacturer ?? 'Unknown').trim() || 'Unknown');
          const arr = m.get(key);
          if (arr) arr.push(p);
          else m.set(key, [p]);
        }
        return m;
      }

      function getDefaultManufacturerSelectionUpper() {
        // Settings key written by the main app Settings popup.
        const KEY = 'coopt.glassMap.defaultManufacturers';
        try {
          const raw = localStorage.getItem(KEY);
          if (!raw) return new Set();
          const parsed = JSON.parse(raw);
          if (!Array.isArray(parsed)) return new Set();
          const s = new Set();
          for (const v of parsed) {
            const name = String(v ?? '').trim();
            if (!name) continue;
            s.add(name.toUpperCase());
          }
          return s;
        } catch (_) {
          return new Set();
        }
      }

      function renderPlot(Plotly) {
        const { priced, missing } = splitByPrice(points);

        const pricedX = priced.map(p => p.vd);
        const pricedY = priced.map(p => p.nd);
        const pricedC = priced.map(p => p.price);
        const pricedText = priced.map(makeHover);

        const missingX = missing.map(p => p.vd);
        const missingY = missing.map(p => p.nd);
        const missingText = missing.map(makeHover);

        const pricedMin = pricedC.length ? Math.min.apply(null, pricedC) : 1;
        const pricedMax = pricedC.length ? Math.max.apply(null, pricedC) : 1;

        // Log-scaled coloring emphasizes relative (ratio) differences.
        const pricedMinLog = log10Safe(pricedMin);
        const pricedMaxLog = log10Safe(pricedMax);

        // Match original co-opt price colorscale (low->high: blue->green->red).
        const PRICE_COLORSCALE = [
          [0.0, 'rgb(0, 0, 255)'],
          [0.5, 'rgb(0, 255, 0)'],
          [1.0, 'rgb(255, 0, 0)']
        ];

        const tickPrices = buildLogTickValues(pricedMin, pricedMax);
        const tickVals = tickPrices.map(v => log10Safe(v)).filter(v => Number.isFinite(v));
        const tickText = tickPrices.map(v => String(v));

        const traces = [];

        // Always show the price colorbar, independent of manufacturer toggles.
        // Keep the marker fully transparent and outside fixed axis ranges.
        traces.push({
          type: 'scatter',
          mode: 'markers',
          name: 'price',
          showlegend: false,
          x: [0, 0],
          y: [0, 0],
          hoverinfo: 'skip',
          marker: {
            size: 1,
            opacity: 0,
            color: [pricedMinLog, pricedMaxLog],
            cmin: pricedMinLog,
            cmax: pricedMaxLog,
            colorscale: PRICE_COLORSCALE,
            showscale: true,
            colorbar: {
              title: 'price (log)',
              tickmode: 'array',
              tickvals: tickVals,
              ticktext: tickText,
            },
            line: { width: 0 }
          }
        });

        // Per-manufacturer traces so legend can toggle vendors (useful when nd/vd overlap).
        const byMfr = groupByManufacturer(points);
        const manufacturers = Array.from(byMfr.keys()).sort((a, b) => a.localeCompare(b));

        const defaultSelectedUpper = getDefaultManufacturerSelectionUpper();
        const hasDefaultSelection = defaultSelectedUpper && defaultSelectedUpper.size > 0;

        for (const mfr of manufacturers) {
          const arr = byMfr.get(mfr) || [];
          const pricedLocal = arr.filter(p => Number.isFinite(p.price) && p.price > 0);
          const missingLocal = arr.filter(p => !(Number.isFinite(p.price) && p.price > 0));

          const mfrUpper = String(mfr).toUpperCase();
          const isSelected = !hasDefaultSelection || defaultSelectedUpper.has(mfrUpper);
          const initialVisible = isSelected ? true : 'legendonly';

          // Legend entry: always black marker; when toggled off, Plotly greys it.
          traces.push({
            type: 'scatter',
            mode: 'markers',
            name: mfr,
            legendgroup: mfr,
            showlegend: true,
            visible: initialVisible,
            // Plotly may hide legend items for traces with zero points.
            // Keep a single dummy point outside the fixed axis ranges.
            x: [0],
            y: [0],
            hoverinfo: 'skip',
            marker: { size: 8, color: 'rgb(0,0,0)' }
          });

          if (pricedLocal.length > 0) {
            traces.push({
              type: 'scattergl',
              mode: 'markers',
              name: mfr,
              legendgroup: mfr,
              showlegend: false,
              visible: initialVisible,
              x: pricedLocal.map(p => p.vd),
              y: pricedLocal.map(p => p.nd),
              customdata: pricedLocal.map(p => [
                p.name,
                p.manufacturer,
                Number.isFinite(p.price) ? p.price : null,
                p.nd,
                p.vd,
              ]),
              text: pricedLocal.map(makeHover),
              hoverinfo: 'text',
              marker: {
                size: 10,
                opacity: 0.9,
                color: pricedLocal.map(p => log10Safe(p.price)),
                cmin: pricedMinLog,
                cmax: pricedMaxLog,
                colorscale: PRICE_COLORSCALE,
                showscale: false,
                line: { width: 0 }
              }
            });
          }

          if (missingLocal.length > 0) {
            traces.push({
              type: 'scattergl',
              mode: 'markers',
              name: mfr,
              legendgroup: mfr,
              showlegend: false,
              visible: initialVisible,
              x: missingLocal.map(p => p.vd),
              y: missingLocal.map(p => p.nd),
              customdata: missingLocal.map(p => [
                p.name,
                p.manufacturer,
                null,
                p.nd,
                p.vd,
              ]),
              text: missingLocal.map(makeHover),
              hoverinfo: 'text',
              marker: {
                size: 10,
                opacity: 0.75,
                color: 'rgba(160,160,160,0.9)',
                line: { width: 0 }
              }
            });
          }
        }

        const layout = {
          margin: { l: 60, r: 30, t: 70, b: 55 },
          dragmode: 'select',
          xaxis: {
            title: { text: 'V<sub>d</sub>' },
            range: [105, 15],
            dtick: 10,
            zeroline: false,
            showgrid: true
          },
          yaxis: {
            title: { text: 'n<sub>d</sub>' },
            range: [1.3, 2.2],
            zeroline: false,
            showgrid: true
          },
          legend: { orientation: 'h', x: 0, y: 1.02, yanchor: 'bottom', groupclick: 'togglegroup' }
        };

        const config = {
          displayModeBar: true,
          responsive: true
        };

        const plotEl = document.getElementById('plot');

        Plotly.newPlot(plotEl, traces, layout, config);

        // Attach event handlers only when Plotly has augmented the div.
        if (plotEl && typeof plotEl.on === 'function') {
          // Double-click on a point: propose replacing current material in the opener.
          // Plotly's built-in plotly_doubleclick event doesn't provide point info reliably,
          // so we detect a double-click via two plotly_click events close in time.
          let lastClickAt = 0;
          let lastClickKey = '';
          plotEl.on('plotly_click', function(ev) {
            try {
              const pt = ev && ev.points && ev.points[0] ? ev.points[0] : null;
              const cd = pt && pt.customdata ? pt.customdata : null;
              if (!cd || cd.length < 4) return;

              const name = String(cd[0] ?? '').trim();
              const manufacturer = String(cd[1] ?? 'Unknown').trim() || 'Unknown';
              const price = Number.isFinite(cd[2]) ? Number(cd[2]) : null;
              const nd = Number.isFinite(cd[3]) ? Number(cd[3]) : null;
              const vd = Number.isFinite(cd[4]) ? Number(cd[4]) : null;
              if (!name) return;

              const now = Date.now();
              const key = manufacturer + '\\n' + name;
              const isDouble = (now - lastClickAt) < 350 && key === lastClickKey;
              lastClickAt = now;
              lastClickKey = key;
              if (!isDouble) return;

              if (typeof window.__COOPT_ON_GLASS_SELECTED__ === 'function') {
                const shouldClose = window.__COOPT_ON_GLASS_SELECTED__({ name, manufacturer, price, nd, vd });
                if (shouldClose !== false) window.close();
              }
            } catch (_) {
              // ignore
            }
          });

          plotEl.on('plotly_selected', function(ev) {
          lastRegion = null;
          applyBtn.disabled = true;
          if (!ev || !ev.range || !ev.range.x || !ev.range.y) return;

          const x0 = ev.range.x[0];
          const x1 = ev.range.x[1];
          const y0 = ev.range.y[0];
          const y1 = ev.range.y[1];

          const vdMin = Math.min(x0, x1);
          const vdMax = Math.max(x0, x1);
          const ndMin = Math.min(y0, y1);
          const ndMax = Math.max(y0, y1);

          if (![vdMin, vdMax, ndMin, ndMax].every(Number.isFinite)) return;

          lastRegion = { ndMin, ndMax, vdMin, vdMax };
          applyBtn.disabled = false;

          // Clear selection visuals (box/selected points) while keeping lastRegion.
          try {
            if (window.Plotly && plotEl) {
              window.Plotly.restyle(plotEl, { selectedpoints: [null] });
            }
          } catch (_) {}
          });
        }

        applyBtn.addEventListener('click', function() {
          if (!lastRegion) return;
          try {
            if (typeof window.__COOPT_ON_REGION_SELECTED__ === 'function') {
              window.__COOPT_ON_REGION_SELECTED__(lastRegion);
            }
          } finally {
            window.close();
          }
        });
      }

      ensurePlotlyReady()
        .then(renderPlot)
        .catch((err) => {
          console.error(err);
          document.getElementById('plot').innerHTML = '<div style="padding:12px">Failed to load Plotly.</div>';
        });
    })();
  <\/script>
</body>
</html>`;try{r.document.open(),r.document.write(s),r.document.close()}catch(a){console.error("Failed to initialize popup window",a);try{r.close()}catch{}return alert("Failed to initialize popup window."),null}return r}const q=window,Ta=10;function cs(e,t){if(!Number.isFinite(e)||!Number.isFinite(t))return 6;const n=Math.abs(e);return n===0?4:n<1e-6?15:n<.001?10:n<1||t<1?6:t<10?4:t<100?3:2}function ls(e,t,n,o){if(!o)return t+e*(n-t);if(t<0&&n>0)if(e<.5){const l=Math.abs(t),f=e*2;return l<1e-10?-1e-10*(1-f):-Math.exp(Math.log(l)*(1-f)+Math.log(1e-10)*f)}else{const l=(e-.5)*2;return n<1e-10?1e-10*l:Math.exp(Math.log(1e-10)*(1-l)+Math.log(n)*l)}const r=Math.abs(t),s=Math.abs(n),a=Math.log(Math.max(r,1e-10)),c=Math.log(Math.max(s,1e-10)),i=Math.exp(a+e*(c-a));return t<0&&n<0?-i:i}function Fr(e,t,n,o){if(!o)return n===t?.5:(e-t)/(n-t);if(t<0&&n>0)if(e<0){const f=Math.abs(t),p=Math.abs(e);return f<1e-10||p<1e-10?.25:(1-(Math.log(p)-Math.log(1e-10))/(Math.log(f)-Math.log(1e-10)))*.5}else return n<1e-10||e<1e-10?.75:.5+(Math.log(e)-Math.log(1e-10))/(Math.log(n)-Math.log(1e-10))*.5;const r=Math.abs(t),s=Math.abs(n),a=Math.abs(e);if(r<1e-10||s<1e-10||a<1e-10)return .5;const c=Math.log(r),i=Math.log(s),l=Math.log(a);return i===c?.5:(l-c)/(i-c)}function us(e,t,n){const o=parseFloat(String(n)),r=!Number.isFinite(o)||o===0;if(e==="nd"||e==="material"&&!isNaN(o)&&o>0&&o<4)return{min:1,max:2.5,step:1e-4,useLog:!1};if(e==="vd"||e==="abbe")return{min:20,max:95,step:.1,useLog:!1};if(e.includes("Radius")||e==="radius"){if(r)return{min:-100,max:100,step:.1,useLog:!1};const c=Math.abs(o),i=c*.5;return{min:o-i,max:o+i,step:c*.001,useLog:!1}}if(e.includes("Thickness")||e==="thickness"||e.includes("hickness"))return r?{min:0,max:20,step:.1,useLog:!1}:{min:0,max:o*2,step:o*.001,useLog:!1};if(e.includes("semidia")||e.includes("Semidia")||e.includes("aperture"))return r?{min:.1,max:20,step:.1,useLog:!1}:{min:Math.max(.1,o*.5),max:o*1.5,step:o*.001,useLog:!1};if(e==="conic"){if(r)return{min:-10,max:10,step:.01,useLog:!1};const c=Math.abs(o),i=Math.max(c*.5,1);return{min:o-i,max:o+i,step:c>1?c*.001:.001,useLog:!1}}if(e.startsWith("coef")||e.includes("Coef")){if(r)return{min:-10,max:10,step:.001,useLog:!1};const c=Math.abs(o),i=Math.max(c*.5,c*10);return{min:o-i,max:o+i,step:c*.01,useLog:!1}}if(r)return{min:-10,max:10,step:.01,useLog:!1};const s=Math.abs(o),a=Math.max(s*.5,s);return{min:o-a,max:o+a,step:s*.01,useLog:!1}}function Pa(e,...t){try{e.includes("🔴")||e.includes("❌")?console.log(`%c${e}`,"color: red; font-weight: bold; font-size: 13px;",...t):e.includes("🔵")?console.log(`%c${e}`,"color: blue; font-weight: bold; font-size: 12px;",...t):e.includes("⚠️")?console.log(`%c${e}`,"color: orange; font-weight: bold;",...t):e.includes("✅")?console.log(`%c${e}`,"color: green; font-weight: bold;",...t):console.log(e,...t)}catch{}try{const n=window?.opener;n&&n.console&&typeof n.console.log=="function"&&n.console.log(e,...t)}catch{}try{const n=window;Array.isArray(n.__coordTransDebugLogs)||(n.__coordTransDebugLogs=[]),n.__coordTransDebugLogs.push({time:new Date().toISOString(),message:e,args:t})}catch{}}try{window.__coordTransConsoleTest=()=>{const e=new Date().toISOString();return console.log(`[CoordTrans][TEST] console output OK at ${e}`),Pa(`✅ [CoordTrans][TEST] coordTransDebugLog OK at ${e}`),e}}catch{}try{window.__coordTransConsoleTestFire=()=>{const e=new Date().toISOString();return console.error(`[CoordTrans][TEST] console.error OK at ${e}`),console.warn(`[CoordTrans][TEST] console.warn OK at ${e}`),console.info(`[CoordTrans][TEST] console.info OK at ${e}`),e}}catch{}async function ps(e,t){const n=t;if(!(n&&n.__coordTransCalculating)){n&&(n.__coordTransCalculating=!0);try{console.log("%c🔴 [CoordTrans] performCoordTransCalculation CALLED for blockId="+e,"color: red; font-weight: bold; font-size: 14px;"),Pa(`🔴 [CoordTrans] performCoordTransCalculation called for blockId=${e}`);const o=$=>{if(!t)return null;let J=t.querySelector(`input[data-param-key="${$}"]`);return J||(J=t.querySelector(`.param-input-with-slider[data-param-key="${$}"] input[type="text"]`)||null),J||(J=t.querySelector(`input[name="${$}"]`)),J||(J=t.querySelector(`select[data-param-key="${$}"]`)),J||(J=t.querySelector(`select[name="${$}"]`)),J?String(J.value??""):null};let r=null;try{if(typeof We=="function"){const $=We(),J=$?.activeConfigId;r=(Array.isArray($?.configurations)?$.configurations.find(Z=>Z&&String(Z.id)===String(J)):null)?.blocks?.find(Z=>Z&&String(Z.blockId??"")===String(e))?.parameters||null}}catch{}const s=r&&r.toSurf!==void 0&&r.toSurf!==null?String(r.toSurf):o("toSurf"),a=r&&r.coordReturn?String(r.coordReturn):o("coordReturn")||"none";if(r){const $=J=>String(J??"").trim().toUpperCase();["A","AUTO"].includes($(r.chiefRayShiftX))&&(r={...r,decenterX:0}),["A","AUTO"].includes($(r.chiefRayShiftY))&&(r={...r,decenterY:0}),["A","AUTO"].includes($(r.chiefRayShiftZ))&&(r={...r,decenterZ:0})}if(a!=="none"&&o("order")!=="1")try{if(typeof q.__blocks_setBlockParamValue=="function"){const J=q.__blocks_setBlockParamValue(e,"order","1");(!J||J.ok!==!0)&&(!n||!n.__coordTransOrderWarned)&&(console.warn("[CoordTrans] Failed to set order to 1:",J?.reason),n&&(n.__coordTransOrderWarned=!0))}}catch{}if(!s||String(s).trim()==="")return;const c=Number(s);if(!Number.isFinite(c)){console.error("[CoordTrans] Invalid target index:",s);return}const i=q.getOpticalSystemRows;if(typeof i!="function"){console.error("[CoordTrans] getOpticalSystemRows not available");return}const l=i();if(!l||l.length===0){console.error("[CoordTrans] No optical system data");return}const f=l.map($=>({...$}));let p=null;try{p=typeof We=="function"?We():null}catch{}const d=$=>{const J=String($?.surfType??$?.["surf type"]??"").toLowerCase().replace(/\s+/g,"");return J==="coordbreak"||J==="coordinatebreak"||J==="cb"||J==="coordtrans"||J==="coordinatetransform"||J==="ct"},u=$=>{const J=$?.["object type"]??$?.object??$?.Object;return String(J??"").toLowerCase()==="object"},y=$=>String($?.surfType??$?.["surf type"]??"").toLowerCase()==="gap",h=(($,J)=>{if(!Array.isArray($)||!Number.isFinite(J))return null;const Q=Math.floor(J);if(Q<=0)return null;let le=0;for(let Z=0;Z<$.length;Z++){const B=$[Z];if(!d(B)&&!u(B)&&!y(B)&&(le++,le===Q))return Z}return null})(l,c),g=h!==null?h:Math.max(0,Math.min(Math.floor(c),Math.max(0,l.length-1))),w=l.map($=>{const J=String($._blockId??$.blockId??"");if(!J)return $;let Q=null;if(J===String(e)&&r)Q=r;else try{if(p&&Array.isArray(p.configurations)){const x=p.activeConfigId,T=p.configurations.find(j=>j&&j.id===x);if(T&&Array.isArray(T.blocks)){const j=T.blocks.find(P=>P&&String(P.blockId??"")===J);j&&(Q=j.parameters)}}}catch(x){console.warn(`[CoordTrans] Could not get block data for ${J}:`,x)}if(!Q)return $;const le=J===String(e),Z=x=>String(x??"").trim().toUpperCase(),B=le&&["A","AUTO"].includes(Z(Q.chiefRayShiftX)),C=le&&["A","AUTO"].includes(Z(Q.chiefRayShiftY)),F=le&&["A","AUTO"].includes(Z(Q.chiefRayShiftZ));return{...$,decenterX:B?0:Q.decenterX!==void 0?Q.decenterX:$.decenterX,decenterY:C?0:Q.decenterY!==void 0?Q.decenterY:$.decenterY,decenterZ:F?0:Q.decenterZ!==void 0?Q.decenterZ:$.decenterZ,tiltX:Q.tiltX!==void 0?Q.tiltX:$.tiltX,tiltY:Q.tiltY!==void 0?Q.tiltY:$.tiltY,tiltZ:Q.tiltZ!==void 0?Q.tiltZ:$.tiltZ,order:Q.order!==void 0?Q.order:$.order,chiefRayShiftX:Q.chiefRayShiftX,chiefRayShiftY:Q.chiefRayShiftY,chiefRayShiftZ:Q.chiefRayShiftZ,parameters:{...$.parameters||{},decenterX:B?0:Q.decenterX!==void 0?Q.decenterX:$.parameters?.decenterX,decenterY:C?0:Q.decenterY!==void 0?Q.decenterY:$.parameters?.decenterY,decenterZ:F?0:Q.decenterZ!==void 0?Q.decenterZ:$.parameters?.decenterZ,tiltX:Q.tiltX!==void 0?Q.tiltX:$.parameters?.tiltX,tiltY:Q.tiltY!==void 0?Q.tiltY:$.parameters?.tiltY,tiltZ:Q.tiltZ!==void 0?Q.tiltZ:$.parameters?.tiltZ,order:Q.order!==void 0?Q.order:$.parameters?.order,chiefRayShiftX:Q.chiefRayShiftX,chiefRayShiftY:Q.chiefRayShiftY,chiefRayShiftZ:Q.chiefRayShiftZ}}}),R=q.calculateAllSurfacesLocalCoordinates;if(typeof R!="function"){console.error("[CoordTrans] calculateAllSurfacesLocalCoordinates not available");return}const N=await R(w,g,null,e,f);let I=-1;for(let $=0;$<l.length;$++)if(String(l[$]._blockId??l[$].blockId??"")===String(e)){I=$;break}if(I<0){console.error("[CoordTrans] Could not find surface for block:",e);return}const b=String(l[I].id);let A=N.surfaces?.[b]||N.surfaces?.[String(b)]||N.surfaces?.[Number(b)]||N.surfaces?.[I]||N.surfaces?.[String(I)];if(!A)for(let $=I+1;$<l.length;$++){const J=String(l[$].id);if(A=N.surfaces?.[J],A)break}if(!A){console.error("[CoordTrans] No surface data found");return}try{a==="xyz"&&(console.log("[CoordTrans] Mode:",a,"blockId:",e,"targetIndex:",g),console.log("[CoordTrans] blockParams:",r),console.log("[CoordTrans] surfData tilt:",{tiltX:A.localTiltX,tiltY:A.localTiltY,tiltZ:A.localTiltZ}),console.log("[CoordTrans] surfData decenter (local):",{decenterX:A.localDecenterX,decenterY:A.localDecenterY,decenterZ:A.localDecenterZ}),console.log("[CoordTrans] surfData decenter (flat):",{decenterX:A.flatDecenterX,decenterY:A.flatDecenterY,decenterZ:A.flatDecenterZ}))}catch{}const v={},S=($,J)=>typeof J=="number"&&Number.isFinite(J)?(v[$]=J,!0):!1,_=r&&r.chiefRayShiftX!==void 0?r.chiefRayShiftX:o("chiefRayShiftX"),M=r&&r.chiefRayShiftY!==void 0?r.chiefRayShiftY:o("chiefRayShiftY"),G=r&&r.chiefRayShiftZ!==void 0?r.chiefRayShiftZ:o("chiefRayShiftZ"),z=$=>String($??"").trim().toUpperCase();let E=["A","AUTO"].includes(z(_)),L=["A","AUTO"].includes(z(M)),ne=["A","AUTO"].includes(z(G));a==="xyz"&&(ne=!0,console.log("[CoordTrans] XYZ mode detected: forcing shouldAutoZ=true"));let V={};switch(a){case"none":break;case"xyz":{const $=A.flatDecenterX!==void 0&&Number.isFinite(A.flatDecenterX)?A.flatDecenterX:A.localDecenterX,J=A.flatDecenterY!==void 0&&Number.isFinite(A.flatDecenterY)?A.flatDecenterY:A.localDecenterY,Q=A.flatDecenterZ!==void 0&&Number.isFinite(A.flatDecenterZ)?A.flatDecenterZ:A.localDecenterZ;V={decenterX:E?S("decenterX",$):!1,decenterY:L?S("decenterY",J):!1,decenterZ:ne?S("decenterZ",Q):!1,tiltX:S("tiltX",A.localTiltX),tiltY:S("tiltY",A.localTiltY),tiltZ:S("tiltZ",A.localTiltZ)}}break}if(a!=="none"?typeof window<"u"&&(window.__coordTransComputedValues||(window.__coordTransComputedValues={}),window.__coordTransComputedValues[e]=v):typeof window<"u"&&window.__coordTransComputedValues&&delete window.__coordTransComputedValues[e],a!=="none")try{window.__coordTransApplyingResults=!0;const $={};if(a==="xyz"){let J=A.localDecenterX;A.flatDecenterX!==void 0&&Number.isFinite(A.flatDecenterX)&&(J=A.flatDecenterX);let Q=A.localDecenterY;A.flatDecenterY!==void 0&&Number.isFinite(A.flatDecenterY)&&(Q=A.flatDecenterY);let le=A.localDecenterZ;A.flatDecenterZ!==void 0&&Number.isFinite(A.flatDecenterZ)&&(le=A.flatDecenterZ),console.log(`[CoordTrans XYZ] shouldAutoX=${E}, shouldAutoY=${L}, shouldAutoZ=${ne}`),console.log(`[CoordTrans XYZ] srcX=${J.toFixed(4)}, srcY=${Q.toFixed(4)}, srcZ=${le.toFixed(4)}`),E&&($.decenterX=J),L&&($.decenterY=Q),ne&&($.decenterZ=le),console.log(`[CoordTrans XYZ] After assignment: updates.decenterX=${$.decenterX}, updates.decenterY=${$.decenterY}, updates.decenterZ=${$.decenterZ}`),$.tiltX=A.localTiltX,$.tiltY=A.localTiltY,$.tiltZ=A.localTiltZ}if(typeof We=="function"&&typeof et=="function"){const J=We(),Q=J?.activeConfigId,le=Array.isArray(J?.configurations)?J.configurations.find(B=>B&&String(B.id)===String(Q)):null,Z=le?.blocks?.find(B=>B&&String(B.blockId??"")===String(e));if(Z){(!Z.parameters||typeof Z.parameters!="object")&&(Z.parameters={}),console.log(`[CoordTrans] Before block update - blockId=${e}:`,{decenterX:Z.parameters.decenterX,decenterY:Z.parameters.decenterY,decenterZ:Z.parameters.decenterZ});for(const[B,C]of Object.entries($))typeof C=="number"&&Number.isFinite(C)&&(Z.parameters[B]=C,console.log(`[CoordTrans] Set block.parameters.${B} = ${C.toFixed(6)}`));console.log(`[CoordTrans] After block update - blockId=${e}:`,{decenterX:Z.parameters.decenterX,decenterY:Z.parameters.decenterY,decenterZ:Z.parameters.decenterZ}),le?.metadata&&typeof le.metadata=="object"&&(le.metadata.modified=new Date().toISOString()),et(J)}}try{if(typeof We=="function"&&typeof at=="function"){const J=We(),Q=J?.activeConfigId,le=Array.isArray(J?.configurations)?J.configurations.find(Z=>Z&&String(Z.id)===String(Q)):null;if(le&&Array.isArray(le.blocks)){const Z=at(le.blocks);Z&&Array.isArray(Z.rows)&&(le.opticalSystem=Z.rows,typeof et=="function"&&et(J))}}}catch{}window.ConfigurationManager&&typeof window.ConfigurationManager.loadActiveConfigurationToTables=="function"?await window.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0}):typeof window.loadActiveConfigurationToTables=="function"&&await window.loadActiveConfigurationToTables({applyToUI:!0});try{window.ConfigurationManager&&typeof window.ConfigurationManager.renderBlocksUI=="function"&&window.ConfigurationManager.renderBlocksUI()}catch{}try{typeof window.__blocks_requestRedraw=="function"&&window.__blocks_requestRedraw()}catch{}try{typeof window.refreshAllUI=="function"&&window.refreshAllUI()}catch{}}finally{window.__coordTransApplyingResults=!1}const K=Object.values(V).filter($=>$).length;console.log("[CoordTrans] Updated",K,"fields:",a);try{Mt()}catch{}}catch(o){console.error("[CoordTrans] Calculation error:",o)}finally{n&&(n.__coordTransCalculating=!1)}}}function ai(e){const t=e?.semidia??e?.["semidia(mm)"]??e?.semidiameter,n=Number(t);return Number.isFinite(n)?n:NaN}function $r(e){const t=ai(e);return Number.isFinite(t)&&t>0?t:null}function ds(e){const t=String(e??"").trim();return t==="Paraxial"?["front"]:t==="Lens"||t==="PositiveLens"?["front","back"]:t==="Doublet"?["s1","s2","s3"]:t==="Triplet"?["s1","s2","s3","s4"]:[]}function Wr(e){if(!e||typeof e!="object")return!1;const t=String(e?.["object type"]??e?.object??"").trim().toLowerCase();if(t==="object"||t==="stop"||t==="image")return!1;const n=String(e?.surfType??e?.["surf type"]??"").trim().toLowerCase().replace(/\s+/g,"");return!(n==="coordtrans"||n==="coordbreak"||n==="coordinatebreak")}function ka(e){const t=e?.semidia??e?.["semidia(mm)"]??e?.semidiameter;if(t==null||String(t).trim()==="")return!0;const n=Number(t);return!Number.isFinite(n)||n<=0}function Ea(e){let t=0;for(const n of e){const o=ai(n);Number.isFinite(o)&&o>t&&(t=o)}return t>0?t:null}function jp(e){for(const t of e)if(String(t?.["object type"]??t?.object??"").toLowerCase()==="stop"){const o=ai(t);return Number.isFinite(o)&&o>0?o:null}return null}function Oa(e){if(!Array.isArray(e))return-1;for(let t=0;t<e.length;t++){const n=e[t];if(String(n?.["object type"]??n?.object??"").trim().toLowerCase()==="stop")return t}return-1}function Fn(e,t,n,o){if(!Array.isArray(e)||t<0||t>=e.length||!(Number.isFinite(o)&&o>0))return null;try{const r=e.map(c=>({...c||{}}));r[t].semidia=o;const s=uo(r,n),a=Number(s?.entrancePupilDiameter);return Number.isFinite(a)&&a>0?a:null}catch{return null}}function Lp(e,t,n){const o=Number(n);if(!(Number.isFinite(o)&&o>0))return null;const r=Oa(e);if(r<0)return null;const s=1e-4;let a=Math.max(s,o*.02),c=Math.max(1,o*1.5),i=Fn(e,r,t,a),l=Fn(e,r,t,c);for(let y=0;y<24&&(!Number.isFinite(l)||l<o)&&(c*=1.8,l=Fn(e,r,t,c),!(c>1e6));y++);for(let y=0;y<24&&Number.isFinite(i)&&i>o&&a>s&&(a=Math.max(s,a/1.8),i=Fn(e,r,t,a),!(a<=s));y++);let f=null,p=1/0;const d=(y,m)=>{if(!(Number.isFinite(y)&&y>0)||!(Number.isFinite(m)&&m>0))return;const h=Math.abs(m-o);h<p&&(p=h,f=y)};if(d(a,i),d(c,l),Number.isFinite(i)&&Number.isFinite(l)&&(i-o)*(l-o)<=0){let y=a,m=c,h=i;for(let g=0;g<36;g++){const w=(y+m)/2,R=Fn(e,r,t,w);if(!Number.isFinite(R)||R<=0)break;const N=R;if(d(w,N),Math.abs(N-o)<=1e-4||((h-o)*(N-o)<=0?m=w:(y=w,h=N),Math.abs(m-y)<=1e-8*Math.max(1,w)))break}}else{const y=Math.max(s,o*.005),m=Math.max(c,o*20),h=Math.log(m/y),g=40;for(let w=0;w<=g;w++){const R=w/g,N=y*Math.exp(h*R),I=Fn(e,r,t,N);d(N,I)}}return Number.isFinite(f)&&f>0?f:null}function Bp(e,t){const n=jp(e);if(Number.isFinite(n)&&n>0)return n;const o=Number(t);if(Number.isFinite(o)&&o>0)return o/2;const r=Ea(e);return Number.isFinite(r)&&r>0?r:10}function zp(e){const t=e?.thickness;if(t===1/0)return!0;const n=String(t??"").trim();return/^inf(inity)?$/i.test(n)}function $p(e){const t=Array.isArray(e)?e.map(r=>({...r||{}})):[],n=Ea(t),o=Math.max(1e3,Number.isFinite(n)?Number(n)*20:1e3);for(const r of t){if(!r||typeof r!="object")continue;const s=String(r?.["object type"]??r?.object??"").trim().toLowerCase(),a=String(r?.surfType??r?.["surf type"]??r?.type??"").trim().toLowerCase();s!=="stop"&&(s==="object"||s==="image"||a==="coord trans"||a==="coordinate transform"||a==="ct"||a==="coordtrans"||a==="coordinatetransform"||(r.semidia=o))}return t}function Wp(e){let t=0,n=0;if(!Array.isArray(e))return{x:0,y:0};for(const o of e){if(!o||typeof o!="object")continue;const r=Number(o?.xHeightAngle??o?.x??o?.fieldX??0),s=Number(o?.yHeightAngle??o?.y??o?.fieldY??0);Number.isFinite(r)&&(t=Math.max(t,Math.abs(r))),Number.isFinite(s)&&(n=Math.max(n,Math.abs(s)))}return{x:t,y:n}}function ms(e,t,n,o,r,s){try{let a=0,c=Math.max(2,Number(s)*2);const i=12,l=1e-4;for(let f=0;f<i;f++){const p=(a+c)/2,d=r?typeof q.generateInfiniteSystemCrossBeam=="function"?q.generateInfiniteSystemCrossBeam(e,[{x:o==="x"?p:0,y:o==="y"?p:0}],{rayCount:1,wavelength:n,debugMode:!1}):null:typeof q.generateCrossBeam=="function"?q.generateCrossBeam(e,[{x:o==="x"?p:0,y:o==="y"?p:0,z:0}],{rayCount:1,wavelength:n,debugMode:!1}):null;if(!d)return null;const u=Array.isArray(d?.allTracedRays)&&d.allTracedRays.length>0?d.allTracedRays[0]:Array.isArray(d?.objectResults)&&d.objectResults.length>0&&Array.isArray(d.objectResults[0]?.tracedRays)&&d.objectResults[0].tracedRays.length>0?d.objectResults[0].tracedRays[0]:Array.isArray(d?.rays)&&d.rays.length>0?d.rays[0]:null;if(!u)return null;const y=Array.isArray(u?.rayPath)?u.rayPath:Array.isArray(u?.rayPathToTarget)?u.rayPathToTarget:null;if(!Array.isArray(y))return null;const m=y[t];if(!m)return null;const h=o==="x"?m.x:m.y;if(!Number.isFinite(h))return null;if(Math.abs(h)<l)return p;h>0?c=p:a=p}return(a+c)/2}catch{return null}}function Up(e,t,n=[]){const o=e.findIndex(h=>String(h?.["object type"]??h?.object??"").toLowerCase()==="stop");if(o<0)return;const r=e[0],s=zp(r),a=$p(e),c=Number(e?.__zmxEntrancePupilDiameterMm),i=Bp(e,Number.isFinite(c)?c:void 0);let l=i,f=i;const p=s?Wp(n):{x:0,y:0};if(s&&(p.x>0||p.y>0))p.x>0&&(l=p.x),p.y>0&&(f=p.y);else{const h=ms(a,o,t,"x",s,i),g=ms(a,o,t,"y",s,i);l=Number.isFinite(h)?h:i,f=Number.isFinite(g)?g:i}if(!Number.isFinite(l)||!Number.isFinite(f)||l<=0||f<=0)return;const u=s?typeof q.generateInfiniteSystemCrossBeam=="function"?q.generateInfiniteSystemCrossBeam(a,[{x:l,y:0},{x:0,y:f}],{rayCount:13,wavelength:t,debugMode:!1}):null:typeof q.generateCrossBeam=="function"?q.generateCrossBeam(a,[{x:l,y:0,z:0},{x:0,y:f,z:0}],{rayCount:13,wavelength:t,debugMode:!1}):null;if(!u)return;let y=[];if(Array.isArray(u.allTracedRays))y=u.allTracedRays;else if(Array.isArray(u.rays))y=u.rays;else if(Array.isArray(u.objectResults))for(const h of u.objectResults)Array.isArray(h?.tracedRays)?y.push(...h.tracedRays):Array.isArray(h?.rays)?y.push(...h.rays):Array.isArray(h?.crossBeamRays)&&y.push(...h.crossBeamRays);if(y.length===0)return;const m=new Array(e.length).fill(0);for(const h of y){const g=Array.isArray(h?.rayPath)?h.rayPath:Array.isArray(h?.rayPathToTarget)?h.rayPathToTarget:Array.isArray(h?.path)?h.path:Array.isArray(h?.ray?.path)?h.ray.path:null;if(!Array.isArray(g))continue;const w=Math.min(g.length,e.length);for(let R=0;R<w;R++){const N=g[R];if(!N||!Number.isFinite(N.x)||!Number.isFinite(N.y))continue;const I=Math.sqrt(N.x*N.x+N.y*N.y);I>m[R]&&(m[R]=I)}}for(let h=0;h<e.length;h++){const g=e[h];if(!g||typeof g!="object"||!Wr(g))continue;const R=ka(g),N=$r(g),I=m[h];I>0&&(R||N===null||I>N+1e-6)&&(g.semidia=I)}}function Da(e,t,n={}){console.log("[autoCalculateMissingSemidia] START");const o=q.tableOpticalSystem||q.tableOpticalSystem,r=o&&typeof o.getData=="function"?o.getData():null;if(!Array.isArray(r)||r.length<2){console.warn("[autoCalculateMissingSemidia] Invalid rows:",r);return}console.log("[autoCalculateMissingSemidia] Table loaded with",r.length,"rows"),console.log("[autoCalculateMissingSemidia] Initial rows (first 5):",r.slice(0,5).map(s=>({surf:s?.surf,type:s?.type,object:s?.object,"object type":s?.["object type"],semidia:s?.semidia,radius:s?.radius,thickness:s?.thickness})));try{const s=(()=>{if(typeof q.getPrimaryWavelength=="function"){const f=Number(q.getPrimaryWavelength());if(Number.isFinite(f)&&f>0)return f}return console.warn("Primary wavelength is unavailable. Semidia auto-calculation is skipped."),NaN})();if(!Number.isFinite(s)||s<=0)return;const a=Number(n?.entrancePupilDiameterMm),c=Oa(r),i=!!n?.stopSemidiaWasMissing;if(Number.isFinite(a)&&a>0&&c>=0&&(i||ka(r[c]))){const f=Lp(r,s,a);Number.isFinite(f)&&f>0?(r[c].semidia=f,console.warn(`[autoCalculateMissingSemidia] Stop semidia backsolved from ENPD=${a}: ${f}`)):console.warn(`[autoCalculateMissingSemidia] Stop semidia backsolve failed (ENPD=${a}, stopIndex=${c})`)}Number.isFinite(a)&&a>0&&(r.__zmxEntrancePupilDiameterMm=a),Up(r,s,t),console.log("[autoCalculateMissingSemidia] Ray tracing completed. Sample rows with semidia:",r.slice(0,5).map(f=>({surf:f?.surf,type:f?.type,semidia:f?.semidia,_blockId:f?._blockId,_surfaceRole:f?._surfaceRole})));try{delete r.__zmxEntrancePupilDiameterMm}catch{}o&&typeof o.setData=="function"&&o.setData(r);try{jn(r),console.log("[autoCalculateMissingSemidia] ✅ Saved to tableOpticalSystem storage")}catch(f){console.error("[autoCalculateMissingSemidia] ❌ Failed to save tableOpticalSystem:",f)}try{const f=typeof We=="function"?We():null;if(f&&Array.isArray(f.configurations)){const p=f.activeConfigId,d=f.configurations.find(u=>u&&String(u.id)===String(p))||f.configurations[0];d&&typeof d=="object"&&(d.opticalSystem=r.map(u=>({...u||{}})),(!d.metadata||typeof d.metadata!="object")&&(d.metadata={}),d.metadata.modified=new Date().toISOString(),typeof et=="function"&&(et(f),console.log("[autoCalculateMissingSemidia] ✅ Saved to active configuration")))}}catch(f){console.error("[autoCalculateMissingSemidia] ❌ Failed to save configuration:",f)}}catch{}}function Hp(){console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] START");try{const e=q.tableOpticalSystem||q.opticalSystemTabulator,t=e&&typeof e.getData=="function"?e.getData():null;if(!Array.isArray(t)||t.length===0){console.warn("[__zmxSyncDesignIntentApertureFromOpticalRows] No table rows found");return}const n=t.filter(u=>Wr(u));console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Table rows count:",t.length,"(physical:",n.length+")");const o=typeof We=="function"?We():null;if(!o||!Array.isArray(o.configurations)||o.configurations.length===0)return;const r=o.activeConfigId,s=o.configurations.find(u=>u&&String(u.id)===String(r))||o.configurations[0];if(!s||!Array.isArray(s.blocks)||s.blocks.length===0)return;console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Active config has",s.blocks.length,"blocks");const a=new Map;for(const u of s.blocks){const y=String(u?.blockId??"").trim();y&&a.set(y,u)}const c=new Set;let i=0;for(const u of t){const y=String(u?._blockId??"").trim(),m=String(u?._surfaceRole??"").trim();if(!y||!m)continue;const h=a.get(y);if(!h||!ds(h.blockType).includes(m))continue;const w=$r(u);w!==null&&((!h.aperture||typeof h.aperture!="object")&&(h.aperture={}),h.aperture[m]=w,c.add(y),i++,console.log(`[Provenance Sync] Block ${y} (${h.blockType}) ${m} = ${w}mm`))}console.log(`[__zmxSyncDesignIntentApertureFromOpticalRows] Provenance-based updates: ${i}`);const l=t.filter(u=>Wr(u));console.log(`[__zmxSyncDesignIntentApertureFromOpticalRows] Fallback physical rows: ${l.length}`);let f=0,p=0;for(const u of s.blocks){const y=ds(u?.blockType);if(y.length===0)continue;const m=String(u?.blockId??"").trim();if(!(m&&c.has(m))){(!u.aperture||typeof u.aperture!="object")&&(u.aperture={});for(const h of y){const g=l[f++];if(!g)break;const w=$r(g);w!==null&&(u.aperture[h]=w,p++,console.log(`[Fallback Sync] Block ${m||"unknown"} (${u.blockType}) ${h} = ${w}mm (row ${f-1}: surf=${g.surf})`))}}}console.log(`[__zmxSyncDesignIntentApertureFromOpticalRows] Fallback updates: ${p}`);let d=!1;try{if(typeof ki=="function"){const u=ki(s);d=u&&Array.isArray(u.expandedOpticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] expandBlocksIntoConfiguration result:",d?"SUCCESS":"FAILED",u?`(rows: ${u.expandedOpticalSystem?.length||0}, issues: ${u.issues?.length||0})`:"")}else if(typeof q.expandBlocksIntoConfiguration=="function"){const u=q.expandBlocksIntoConfiguration(s);d=u&&Array.isArray(u.expandedOpticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] expandBlocksIntoConfiguration result:",d?"SUCCESS":"FAILED",u?`(rows: ${u.expandedOpticalSystem?.length||0}, issues: ${u.issues?.length||0})`:"")}}catch(u){console.error("[__zmxSyncDesignIntentApertureFromOpticalRows] expandBlocksIntoConfiguration ERROR:",u)}if(typeof et=="function"&&(et(o),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] ✅ Saved system configurations",d?"(with expanded blocks)":"(WARNING: expand may have failed)")),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Final block apertures:",s.blocks.map(u=>({blockId:u.blockId,blockType:u.blockType,aperture:u.aperture}))),d&&Array.isArray(s.opticalSystem)){console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Expanded opticalSystem sample (first 3):",s.opticalSystem.slice(0,3).map(u=>({surf:u?.surf,type:u?.type,semidia:u?.semidia,_blockId:u?._blockId,_surfaceRole:u?._surfaceRole}))),e&&typeof e.setData=="function"&&(e.setData(s.opticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] ✅ Updated table with expanded rows"));try{jn(s.opticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] ✅ Saved expanded rows to localStorage")}catch(u){console.error("[__zmxSyncDesignIntentApertureFromOpticalRows] ❌ Failed to save to localStorage:",u)}}else console.warn("[__zmxSyncDesignIntentApertureFromOpticalRows] ⚠️ Skipping table/storage update due to expand failure");try{Mt()}catch{}try{kn()}catch{}try{En()}catch{}try{typeof q.refreshAllUI=="function"&&q.refreshAllUI()}catch{}console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] COMPLETE")}catch(e){console.error("[__zmxSyncDesignIntentApertureFromOpticalRows] ERROR:",e)}}async function go(e,t={}){const n=t?.filename||"shared-link.json";try{if(typeof q.normalizeDesign=="function"){const p=q.normalizeDesign(e);p?.normalized&&(e=p.normalized)}}catch{}let o=null;if(e?.systemConfigurations&&e.systemConfigurations.configurations?o=e.systemConfigurations:e&&e.configurations&&e.configurations.configurations?o=e.configurations:Array.isArray(e?.configurations)?o={configurations:e.configurations,activeConfigId:e.activeConfigId,meritFunction:e.meritFunction||[],systemRequirements:e.systemRequirements||[],optimizationRules:e.optimizationRules||{}}:Array.isArray(e)?o={configurations:e}:o=e,!o||!Array.isArray(o.configurations))return console.error("❌ [Load] Invalid configurations format:",o),!1;try{let p=0;for(let d=0;d<o.configurations.length;d++){const u=o.configurations[d];if(!u)continue;(u.id===void 0||u.id===null||String(u.id).trim()==="")&&(u.id=d+1);const y=Number(u.id);Number.isFinite(y)&&y>p&&(p=y)}o.activeConfigId||(o.activeConfigId=o.configurations[0]?.id??p??1)}catch{}try{if(Array.isArray(o.configurations)&&o.configurations.length===0){const p={id:1,name:"Config 1",schemaVersion:o.schemaVersion||po,blocks:Array.isArray(e?.blocks)?e.blocks:[],source:Array.isArray(e?.source)?e.source:[],object:Array.isArray(e?.object)?e.object:[],opticalSystem:Array.isArray(e?.opticalSystem)?e.opticalSystem:[],meritFunction:Array.isArray(e?.meritFunction)?e.meritFunction:[],systemData:e?.systemData||{referenceFocalLength:""},metadata:{created:new Date().toISOString(),modified:new Date().toISOString(),locked:!1}};o.configurations.push(p),o.activeConfigId=1}}catch{}try{const p=o.activeConfigId,d=o.configurations||[],u=d.find(y=>String(y?.id??"")===String(p))||d[0];u&&((!u.source||u.source.length===0)&&Array.isArray(e?.source)&&(u.source=e.source),(!u.object||u.object.length===0)&&Array.isArray(e?.object)&&(u.object=e.object),(!u.opticalSystem||u.opticalSystem.length===0)&&Array.isArray(e?.opticalSystem)&&(u.opticalSystem=e.opticalSystem),(!u.systemData||typeof u.systemData!="object")&&e?.systemData&&(u.systemData=e.systemData)),!o.meritFunction&&Array.isArray(e?.meritFunction)&&(o.meritFunction=e.meritFunction),!o.systemRequirements&&Array.isArray(e?.systemRequirements)&&(o.systemRequirements=e.systemRequirements)}catch{}const r=Array.isArray(o?.configurations)?o.configurations:[],s=p=>{try{return p&&Array.isArray(p.blocks)&&p.blocks.length>0}catch{return!1}};for(const p of r)try{const d=Array.isArray(p?.opticalSystem)?p.opticalSystem:null;if(!d||d.length===0)continue;if(!s(p)&&typeof q.deriveBlocksFromLegacyOpticalSystemRows=="function"){const y=q.deriveBlocksFromLegacyOpticalSystemRows(d);!(Array.isArray(y?.issues)&&y.issues.some(h=>h&&h.severity==="fatal"))&&Array.isArray(y?.blocks)&&y.blocks.length>0&&(p.blocks=Array.isArray(y?.blocks)?y.blocks:[],(!p.metadata||typeof p.metadata!="object")&&(p.metadata={}),p.metadata.importAnalyzeMode=!1)}}catch{}for(const p of r)if(s(p))try{if(typeof q.validateBlocksConfiguration=="function"){const d=q.validateBlocksConfiguration(p),u=Array.isArray(d)?d.filter(y=>y&&y.severity==="fatal"):[];u.length>0&&console.warn("⚠️ Block validation errors:",u)}}catch{}try{const p=o?.activeConfigId||1,d=r.find(u=>u.id===p)||r[0];if(d&&s(d)){const u=Array.isArray(d.opticalSystem)?d.opticalSystem:null;if(typeof q.expandBlocksToOpticalSystemRows=="function"){const y=q.expandBlocksToOpticalSystemRows(d.blocks);if(Array.isArray(u)&&u.length>0){try{typeof q.__blocks_overlayExpandedProvenanceIntoLegacyRows=="function"&&q.__blocks_overlayExpandedProvenanceIntoLegacyRows(u,y.rows)}catch{}try{for(let m=0;m<u.length;m++)u[m]&&typeof u[m]=="object"&&(u[m].id=m)}catch{}d.opticalSystem=u}else Array.isArray(y?.rows)&&(d.opticalSystem=y.rows)}}}catch{}try{et(o)}catch(p){return console.error("❌ Failed to save configurations:",p),!1}let a=e.source,c=e.object,i=e.opticalSystem,l=e.meritFunction,f=e.systemRequirements;try{const p=o?.activeConfigId||1,d=r.find(u=>u.id===p)||r[0];d&&(s(d)&&Array.isArray(d.opticalSystem)&&(i=d.opticalSystem),d.source&&Array.isArray(d.source)&&d.source.length>0&&(a=d.source),d.object&&Array.isArray(d.object)&&d.object.length>0&&(c=d.object),!i&&d.opticalSystem&&(i=d.opticalSystem)),!l&&o?.meritFunction&&(l=o.meritFunction),!f&&o?.systemRequirements&&(f=o.systemRequirements)}catch{}try{a&&Vo(a)}catch{}try{c&&Vr(c)}catch{}try{i&&jn(i)}catch{}try{f&&js(f)}catch{}try{l&&Os(l)}catch{}try{let p=!1;try{p=(Array.isArray(o?.configurations)?o.configurations:[]).some(m=>m&&Array.isArray(m.blocks)&&m.blocks.length>0)}catch{}const d=!p;try{const{setLoadedFileState:y}=await De(async()=>{const{setLoadedFileState:m}=await import("./loaded-file-storage-BCc7IGxJ.js");return{setLoadedFileState:m}},__vite__mapDeps([6,1]));y(n,d)}catch{}const u=document.getElementById("loaded-file-name");u&&(u.textContent=n,u.style.color=d?"#b45309":"#1a4d8f",d&&!u.textContent.includes("(surfaces only)")&&(u.textContent=`${u.textContent} (surfaces only)`));try{window.dispatchEvent(new CustomEvent("coopt:loaded-file-updated"))}catch{}}catch{}try{setTimeout(()=>{try{typeof Uo=="function"&&Uo()}catch{}try{const d=bn(),u=q.tableSource;u&&typeof u.replaceData=="function"?u.replaceData(d):u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{const d=Ls(),u=q.tableObject;u&&typeof u.replaceData=="function"?u.replaceData(d):u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{const d=Ec(),u=q.tableOpticalSystem||q.opticalSystemTabulator;u&&typeof u.replaceData=="function"?u.replaceData(d):u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{const d=Oc(),u=q.meritFunctionEditor;u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{(async()=>{const u=q.systemRequirementsEditor;if(!u)return;const y=Bs();typeof u.setData=="function"&&u.setData(y);const m=async h=>{try{if(typeof u.evaluateAndUpdateNow=="function"){const g=u.evaluateAndUpdateNow({reason:h,forceSilent:!0,silent:!0});g&&typeof g.then=="function"&&await g}}catch{}};await m("load-file-seq-initial"),typeof u.scheduleEvaluateAndUpdate=="function"&&u.scheduleEvaluateAndUpdate();for(let h=0;h<4;h++)await new Promise(g=>setTimeout(g,120)),await m(`load-file-seq-retry-${h+1}`);try{window.dispatchEvent(new CustomEvent("coopt:requirements-updated"))}catch{}})()}catch{}try{Mt()}catch{}try{typeof q.updateTransformSurfaceSelect=="function"&&q.updateTransformSurfaceSelect()}catch{}const p=()=>{document.getElementById("config-select")&&typeof q.initializeConfigurationUI=="function"?q.initializeConfigurationUI():setTimeout(p,100)};p()},0)}catch{}return!0}if(typeof window<"u")try{q.__loadAllDataObjectIntoApp=go,q.autoCalculateMissingSemidia=Da}catch{}function gn(e){return e?e.getAttribute("data-react-handled")==="1":!1}function Gp(){const e=document.getElementById("load-all-btn");if(!e||gn(e))return;const t=()=>{const o=document.createElement("input");o.type="file",o.accept=".json",o.style.display="none",o.addEventListener("change",async r=>{const a=r.target?.files?.[0];try{o.parentNode&&o.parentNode.removeChild(o)}catch{}if(!a){console.warn("⚠️ [Load] No file selected");return}try{const c=await a.text(),i=JSON.parse(c);await go(i,{filename:a.name})}catch(c){console.error("❌ Load failed:",c),alert(`Load failed: ${c?.message||String(c)}`)}}),document.body.appendChild(o),o.click()},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function rn(e){if(e===1/0)return!0;const t=String(e??"").trim().toUpperCase();return t==="INF"||t==="INFINITY"||t==="∞"}function fs(e){const t=Array.isArray(e)?e:[],n=[],o=()=>{for(let d=t.length-1;d>=0;d--){const u=t[d]||{},y=u?.semidia??u?.semiDiameter??u?.semiDia??u?.["semi diameter"]??u?.["Semi Diameter"],m=Number(y);if(Number.isFinite(m)&&m>0)return m}return null},r=t[0]||{},s=rn(r?.thickness)?"INF":"Finite",a=Number(r?.thickness);n.push({blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:s==="INF"?{objectDistanceMode:"INF"}:{objectDistanceMode:"Finite",objectDistance:Number.isFinite(a)?a:10},variables:{},metadata:{source:"zemax-fallback"}});let c=0,i=0,l=0;const f=Math.max(1,t.length-1);for(let d=1;d<f;d++){const u=t[d]||{},y=String(u?.["object type"]??u?.object??"").trim().toLowerCase();if(y==="stop"||y==="sto"){c++;const S=Number(u?.semidia);n.push({blockId:`Stop-${c}`,blockType:"Stop",role:null,constraints:{},parameters:Number.isFinite(S)&&S>0?{semiDiameter:S}:{},variables:{},metadata:{source:"zemax-fallback"}});const _=u?.thickness,M=Number(_);(rn(_)||Number.isFinite(M)&&Math.abs(M)>1e-12)&&(l++,n.push({blockId:`Gap-${l}`,blockType:"Gap",role:null,constraints:{},parameters:{thickness:rn(_)?"INF":M,material:"AIR"},variables:{},metadata:{source:"zemax-fallback",from:"stop-thickness"}}));continue}i++;const g=String(u?.surfType??"").trim()||"Spherical",w=rn(u?.radius)||String(u?.radius??"").trim()===""?"INF":u.radius,R=u?.thickness,N=Number(R),I=rn(R)?"INF":Number.isFinite(N)?N:0,b=String(u?.material??"").trim(),A=Number(u?.conic),v={radius:w,thickness:I,material:b,surfType:g,conic:Number.isFinite(A)?A:0,semidia:u?.semidia??""};if(g==="Toric"){v.radiusX=rn(u?.radiusX)||String(u?.radiusX??"").trim()===""?"INF":u.radiusX,v.radiusY=rn(u?.radiusY)||String(u?.radiusY??"").trim()===""?"INF":u.radiusY;const S=Number(u?.axis);v.axis=Number.isFinite(S)?S:0}for(let S=1;S<=10;S++){const _=Number(u?.[`coef${S}`]);v[`coef${S}`]=Number.isFinite(_)?_:0}n.push({blockId:`SingleSurface-${i}`,blockType:"SingleSurface",role:null,constraints:{},parameters:v,variables:{},metadata:{source:"zemax-fallback",rowIndex:d}})}const p=o();return n.push({blockId:"ImageSurface-1",blockType:"ImageSurface",role:null,constraints:{},parameters:Number.isFinite(p)&&p>0?{semidia:p,semidiaMode:"Auto",optimizeSemiDia:"A"}:{semidiaMode:"Auto",optimizeSemiDia:"A"},variables:{},metadata:{source:"zemax-fallback"}}),n}function Mr(e){if(!Array.isArray(e))return[];let t=!1;for(const n of e){if(!n||n.blockType!=="ObjectSurface")continue;t=!0;const o=n.parameters&&typeof n.parameters=="object"?n.parameters:n.parameters={},r=String(o.objectDistanceMode??"").trim();if(rn(r)){o.objectDistanceMode="INF";const c=Number(o.objectDistance);o.objectDistance=Number.isFinite(c)?c:10;continue}o.objectDistanceMode="Finite";const a=Number(o.objectDistance);o.objectDistance=Number.isFinite(a)?a:10}return t||e.unshift({blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:{objectDistanceMode:"Finite",objectDistance:10},variables:{},metadata:{source:"zemax-fallback",inserted:!0}}),e}function qp(e){if(e&&Array.isArray(e.configurations))return e;const t=Array.isArray(e?.rows)?e.rows:[],n=Array.isArray(e?.sourceRows)?e.sourceRows:[],o=Array.isArray(e?.objectRows)?e.objectRows:[];let r=[];try{const a=Zs(t),c=Array.isArray(a?.issues)?a.issues.filter(i=>i?.severity==="fatal"):[];Array.isArray(a?.blocks)&&a.blocks.length>0&&c.length===0?r=Mr(a.blocks):(r=Mr(fs(t)),c.length>0&&console.warn("⚠️ [Zemax Import] deriveBlocks had fatals; fallback blocks generated:",c))}catch(a){console.warn("⚠️ [Zemax Import] deriveBlocks failed; fallback blocks generated:",a),r=Mr(fs(t))}const s=new Date().toISOString();return{configurations:[{id:1,name:"Config 1",schemaVersion:po,blocks:r,source:n,object:o,opticalSystem:t,meritFunction:[],systemData:{referenceFocalLength:""},metadata:{created:s,modified:s,locked:!1,importedFrom:"zemax"}}],activeConfigId:1,meritFunction:[],systemRequirements:[],optimizationRules:{}}}function Vp(e){const t=String(e??"").trim().replace(/\s*\(surfaces only\)\s*$/i,"");return t?/\.json$/i.test(t)?t.replace(/\.json$/i,".zmx"):/\.zmx$/i.test(t)?t:`${t}.zmx`:"co-opt-export.zmx"}function Yp(){const e=document.getElementById("import-zemax-btn");if(!e||gn(e))return;const t=()=>{const o=document.createElement("input");o.type="file",o.accept=".zmx",o.addEventListener("change",async r=>{const a=r.target?.files?.[0];try{o.parentNode&&o.parentNode.removeChild(o)}catch{}if(a)try{console.log("📥 [Zemax Import] Selected file:",a.name,`(${a.size} bytes)`);const c=await a.arrayBuffer(),i=Ds(c);if(!i||typeof i!="object")throw new Error("Invalid Zemax parse result.");console.log("📥 [Zemax Import] Parsed:",{rows:Array.isArray(i?.rows)?i.rows.length:0,sourceRows:Array.isArray(i?.sourceRows)?i.sourceRows.length:0,objectRows:Array.isArray(i?.objectRows)?i.objectRows.length:0,issues:Array.isArray(i?.issues)?i.issues.length:0});const l=qp(i);if(!await go(l,{filename:a.name}))throw new Error("Zemax import parsed, but app load step returned false.");try{const{loadActiveConfigurationToTables:p}=await De(async()=>{const{loadActiveConfigurationToTables:d}=await import("./table-configuration-Beu6h-Rk.js").then(u=>u.S);return{loadActiveConfigurationToTables:d}},[]);typeof p=="function"&&(await p({applyToUI:!0}),console.log("[Zemax Import] ✅ Loaded active configuration to tables (UI updated)"))}catch(p){console.error("[Zemax Import] ❌ Failed to load configuration to tables:",p)}try{const p=Array.isArray(i?.rows)?i.rows:[],d=p.findIndex(y=>String(y?.["object type"]??y?.object??"").trim().toLowerCase()==="stop"),u=(()=>{if(d<0)return!1;const y=p[d]||{},m=y?.semidia??y?.semiDiameter??y?.semiDia??y?.["semi diameter"]??y?.["Semi Diameter"];if(m==null)return!0;const h=String(m).trim();if(h==="")return!0;const g=Number(h);return!(Number.isFinite(g)&&g>0)})();Da(Array.isArray(i?.sourceRows)?i.sourceRows:[],Array.isArray(i?.objectRows)?i.objectRows:[],{entrancePupilDiameterMm:Number(i?.entrancePupilDiameterMm),stopSemidiaWasMissing:u})}catch{}try{if(typeof q.calculateImageSemiDiaFromChiefRays=="function"){const p=d=>{setTimeout(()=>{try{Promise.resolve(q.calculateImageSemiDiaFromChiefRays()).then(u=>{if(u===!0){try{Mt()}catch{}try{typeof q.refreshAllUI=="function"&&q.refreshAllUI()}catch{}return}d>0&&p(d-1)}).catch(()=>{d>0&&p(d-1)})}catch{d>0&&p(d-1)}},200)};p(4)}}catch{}try{Hp()}catch{}console.log("✅ [Zemax Import] Completed:",a.name)}catch(c){console.error("❌ Zemax import failed:",c),alert(`Import failed: ${c?.message||String(c)}`)}}),document.body.appendChild(o),o.click()},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Zp(){const e=document.getElementById("export-zemax-btn");if(!e||gn(e))return;const t=async()=>{try{const o=q.getOpticalSystemRows?q.getOpticalSystemRows(q.tableOpticalSystem):[],r=q.tableSource&&typeof q.tableSource.getData=="function"?q.tableSource.getData():[],s=q.tableObject&&typeof q.tableObject.getData=="function"?q.tableObject.getData():[];if(!o||o.length===0){alert("No optical system data to export");return}let a=null;try{a=(await De(()=>import("./loaded-file-storage-BCc7IGxJ.js"),__vite__mapDeps([6,1]))).getLoadedFileName()}catch{try{a=q.__cooptLoadedFileStorage?.getLoadedFileName?.()??null}catch{}}const c=Vp(a);let i=prompt("Zemaxエクスポートのファイル名を入力してください（.zmx は自動補完）",c);if(!i||(i=i.trim(),!i))return;if(/\.zmx$/i.test(i)||(i+=".zmx"),typeof q.generateZMXText=="function"){const l=q.generateZMXText(o,{sourceRows:r,objectRows:s});typeof q.downloadZMX=="function"?(q.downloadZMX(l,i),console.log("✅ Zemax file exported successfully")):(console.error("❌ downloadZMX function not available"),alert("Export function not available"))}else console.error("❌ generateZMXText function not available"),alert("Export function not available")}catch(o){console.error("❌ Zemax export failed:",o),alert(`Export failed: ${o?.message||String(o)}`)}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Xp(){const e=document.getElementById("optimize-design-intent-btn");e&&(gn(e)||e.addEventListener("click",async()=>{const t=typeof globalThis<"u"?globalThis:{},n=!!t.__cooptOptimizerIsRunning,o=t.__cooptOptimizerSchedulerWindow,r=n&&(!o||o.closed);if(n&&!r){alert("Optimization is already running. Please wait for it to complete or stop it first.");return}r&&(t.__cooptOptimizerIsRunning=!1),e.disabled,e.disabled=!0;try{if(st()){const Z=q.getOpticalSystemRows&&q.tableOpticalSystem?q.getOpticalSystemRows(q.tableOpticalSystem):q.tableOpticalSystem&&typeof q.tableOpticalSystem.getData=="function"?q.tableOpticalSystem.getData():[];if(!Array.isArray(Z)||Z.length===0){alert("最適化対象の光学系データがありません。");return}const B=(()=>{try{const oe=window.systemRequirementsEditor;if(oe&&typeof oe.getData=="function"){const ae=oe.getData();if(Array.isArray(ae))return ae}}catch{}return[]})(),C=q.tableSource&&typeof q.tableSource.getData=="function"?q.tableSource.getData():[],F=q.tableObject&&typeof q.tableObject.getData=="function"?q.tableObject.getData():[],x=(()=>{try{const oe=typeof q.loadSystemConfigurationsFromTableConfig=="function"?q.loadSystemConfigurationsFromTableConfig():typeof q.loadSystemConfigurations=="function"?q.loadSystemConfigurations():null;if(oe&&oe.activeConfigId!==void 0&&oe.activeConfigId!==null)return String(oe.activeConfigId).trim()}catch{}return""})(),T=q.OptimizationMVP;if(!T||typeof T.run!="function"){alert("OptimizationMVP が利用できません。");return}const j=[],P=await T.run({opticalSystemRows:Z,sourceRows:C,objectRows:F,activeConfigId:x,systemRequirementsRows:B,maxIterations:24,method:"kkt",forceTs:!0,onProgress:oe=>{!oe||typeof oe!="object"||j.push(oe)}});Array.isArray(j)&&j.length>0&&console.log("📈 [Optimize][TS][Progress]",j.slice(-8));try{typeof window.drawOpticalSystem=="function"&&window.drawOpticalSystem()}catch(oe){console.warn("⚠️ [Optimize][TS] result apply failed:",oe)}const O=String(P?.method||"kkt"),X=Number(P?.iterations??0),k=Number(P?.variables??0),Y=Number(P?.before??Number.NaN),D=Number(P?.violationScore??Number.NaN),U=Number.isFinite(D)?D:Number(P?.best??Number.NaN),ee=!P?.aborted;alert(["TS optimizer step completed",`mode: ${O}`,`iterations: ${X}`,`variables: ${k}`,`merit: ${Number.isFinite(Y)?Y.toFixed(6):"NaN"} -> ${Number.isFinite(U)?U.toFixed(6):"NaN"}`,`requirements: ${Number.isFinite(D)?D.toFixed(6):"NaN"}`,ee?"status: converged":"status: in-progress"].join(`
`));return}const s=q.OptimizationMVP;if(!s||typeof s.run!="function"){alert("OptimizationMVP が利用できません。"),e.disabled=!1;return}let a=!1,c=null,i=0,l=0,f=0;try{const Z=typeof q.loadSystemConfigurationsFromTableConfig=="function"?q.loadSystemConfigurationsFromTableConfig():We(),B=Z?.activeConfigId;c=Z?.configurations?.find(x=>x&&x.id===B)||Z?.configurations?.[0]||null,c&&Array.isArray(c.scenarios)&&c.scenarios.length>=2&&(a=!0);const C=Ni(c||{}),F=Array.isArray(C)?C.filter(x=>typeof x?.value=="number"&&Number.isFinite(x.value)):[];i=Array.isArray(C)?C.length:0,l=F.length,f=Math.max(0,i-l)}catch{}let p=null;const d={stop:!1};let u=null,y=!1;try{if(p=window.open("","coopt-optimizer-progress","width=500,height=550,resizable=yes,scrollbars=no"),p&&p.document){const Z=window&&window.location&&window.location.origin?window.location.origin:"",B=`${Z}/favicon.svg`;p.document.open(),p.document.write(`<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Optimize Progress</title>
  <base href="${Z}/" />
  <link rel="icon" type="image/svg+xml" href="${B}" />
</head>
<body style="font-family: system-ui, -apple-system, Segoe UI, sans-serif; margin: 12px;">
<div style="display:flex; align-items:center; gap:10px; flex-wrap:nowrap; white-space:nowrap; margin-bottom:8px;">
    <div style="font-size:14px; font-weight:600; flex:0 0 auto;">Optimize Progress</div>
    <div id="opt-startup-progress-wrap" style="display:flex; align-items:center; gap:8px; flex:1 1 auto; min-width:0; margin:0;">
        <div style="height:6px; background:#eceef2; border-radius:999px; overflow:hidden; flex:1 1 auto; min-width:120px;">
            <div id="opt-startup-progress-bar" style="width:0%; height:100%; background:#4f8cff; transition:width 120ms linear;"></div>
        </div>
        <div id="opt-startup-progress-label" style="font-size:11px; color:#666; flex:0 0 auto;">Idle</div>
    </div>
</div>
<div style="font-size:12px; color:#555; margin-bottom:10px; white-space:nowrap;">Updates per candidate evaluation (±step)</div>
<div style="margin-bottom:10px; display:flex; align-items:center; gap:6px;">
    <button id="opt-run" style="padding:6px 10px;">Run</button>
    <button id="opt-stop" style="padding:6px 10px;" disabled>Stop</button>
    <span id="opt-stop-state" style="margin-left:8px; font-size:12px; color:#555;"></span>
</div>
<div style="margin-bottom:10px; display:flex; align-items:center; gap:10px; flex-wrap:wrap;">
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        Method
        <select id="opt-method" style="padding:4px 6px;">
            <option value="kkt" selected>Augmented Lagrangian (AL)</option>
            <option value="lm">Levenberg-Marquardt (LM)</option>
            <option value="cd">Coordinate Descent (CD)</option>
        </select>
    </label>
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        Max Iterations
        <input id="opt-max-iter" type="number" min="1" step="1" value="5000" style="width:100px; padding:4px 6px;" />
    </label>
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        Convergence
        <select id="opt-convergence-profile" style="padding:4px 6px;">
            <option value="fast">Fast</option>
            <option value="balanced" selected>Balanced</option>
            <option value="deep">Deep</option>
        </select>
    </label>
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        <input id="opt-auto-render" type="checkbox" checked style="width:16px; height:16px;" />
        Auto-render on Accept
    </label>
</div>
<div style="display:flex; gap:10px; flex-direction:column;">
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Phase</span><span id="opt-phase" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Decision</span><span id="opt-decision" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Accept/Reject</span><span id="opt-decision-count" style="margin-left:8px;">0 / 0</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Iter</span><span id="opt-iter" style="margin-left:8px;">0</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Vars</span><span id="opt-vars" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Req</span><span id="opt-req" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Res</span><span id="opt-res" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">ReqScore(active)</span><span id="opt-cur" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Violation</span><span id="opt-vio" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Soft</span><span id="opt-soft" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Best</span><span id="opt-best" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Penalty ρ</span><span id="opt-rho" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Issue</span><span id="opt-issue" style="margin-left:8px;">-</span></div>
</div>
<details style="margin-top:10px; margin-bottom:10px; font-size:12px; color:#555;">
    <summary style="font-weight:600; margin-bottom:6px; cursor:pointer;">Stability Tuning</summary>
    <div style="display:grid; grid-template-columns: 180px 140px 1fr; gap:6px 10px; align-items:center; margin-top:6px;">
        <div>stepFraction</div>
        <input id="opt-step-fraction" type="number" step="0.001" value="0.02" style="width:120px; padding:4px 6px;" />
        <div>CDの初期ステップ比率（小さくすると安定）</div>

        <div>minStep</div>
        <input id="opt-min-step" type="number" step="1e-7" value="1e-6" style="width:120px; padding:4px 6px;" />
        <div>CDの最小ステップ</div>

        <div>stepDecay</div>
        <input id="opt-step-decay" type="number" step="0.05" value="0.5" style="width:120px; padding:4px 6px;" />
        <div>CDの失敗時縮小率</div>

        <div>lmLambda0</div>
        <input id="opt-lm-lambda0" type="number" step="1e-4" value="1e-3" style="width:120px; padding:4px 6px;" />
        <div>LM初期ダンピング</div>

        <div>lmLambdaUp</div>
        <input id="opt-lm-lambdaup" type="number" step="1" value="10" style="width:120px; padding:4px 6px;" />
        <div>LM拒否時の増加係数</div>

        <div>lmLambdaDown</div>
        <input id="opt-lm-lambdadown" type="number" step="0.05" value="0.3" style="width:120px; padding:4px 6px;" />
        <div>LM受理時の減少係数</div>

        <div>trustRegion</div>
        <input id="opt-trust-region" type="checkbox" checked style="width:16px; height:16px;" />
        <div>信頼領域を有効化</div>

        <div>trustRegionDelta</div>
        <input id="opt-trust-region-delta" type="number" step="0.01" value="0.05" style="width:120px; padding:4px 6px;" />
        <div>信頼領域の基本半径</div>

        <div>trustRegionDeltaMax</div>
        <input id="opt-trust-region-delta-max" type="number" step="0.1" value="1.0" style="width:120px; padding:4px 6px;" />
        <div>信頼領域の最大半径</div>

        <div>backtracking</div>
        <input id="opt-backtracking" type="checkbox" checked style="width:16px; height:16px;" />
        <div>LMのバックトラック探索</div>

        <div>backtrackingMaxTries</div>
        <input id="opt-backtracking-max-tries" type="number" step="1" value="8" style="width:120px; padding:4px 6px;" />
        <div>バックトラック試行回数</div>

        <div>fdStepFraction</div>
        <input id="opt-fd-step-fraction" type="number" step="1e-5" value="1e-4" style="width:120px; padding:4px 6px;" />
        <div>数値微分の相対ステップ</div>

        <div>fdMinStep</div>
        <input id="opt-fd-min-step" type="number" step="1e-19" value="1e-18" style="width:120px; padding:4px 6px;" />
        <div>数値微分の最小ステップ</div>

        <div>fdScaledStep</div>
        <input id="opt-fd-scaled-step" type="number" step="1e-4" value="1e-3" style="width:120px; padding:4px 6px;" />
        <div>スケール付き微分ステップ</div>

        <div>staged</div>
        <input id="opt-staged" type="checkbox" checked style="width:16px; height:16px;" />
        <div>係数の段階的解放</div>

        <div>stageStallLimit</div>
        <input id="opt-stage-stall-limit" type="number" step="1" value="5" style="width:120px; padding:4px 6px;" />
        <div>段階の停滞許容回数</div>

        <div>restartOnRejectStreak</div>
        <input id="opt-restart-on-reject-streak" type="number" step="1" value="8" style="width:120px; padding:4px 6px;" />
        <div>連続拒否でリスタート</div>

        <div>restartMaxCount</div>
        <input id="opt-restart-max-count" type="number" step="1" value="2" style="width:120px; padding:4px 6px;" />
        <div>リスタートの最大回数</div>

        <div>restartJitterScaled</div>
        <input id="opt-restart-jitter-scaled" type="number" step="0.005" value="0.035" style="width:120px; padding:4px 6px;" />
        <div>リスタート時のジッタ量</div>

        <div>lmExploreWhenFlat</div>
        <input id="opt-lm-explore-when-flat" type="checkbox" style="width:16px; height:16px;" />
        <div>LMが平坦時に探索を許可</div>

        <div>lmExploreTries</div>
        <input id="opt-lm-explore-tries" type="number" step="1" value="3" style="width:120px; padding:4px 6px;" />
        <div>探索ステップ試行回数</div>
    </div>
</details>
     </body>
    </html>`),p.document.close();try{const C=p.document.getElementById("opt-vars");if(C){const F=[];Number.isFinite(i)&&F.push(String(i)),(Number.isFinite(l)||Number.isFinite(f))&&F.push(`(num ${l}, cat ${f})`),C.textContent=F.length?F.join(" "):"-"}}catch{}try{const C=p.document.getElementById("opt-stop"),F=p.document.getElementById("opt-run"),x=p.document.getElementById("opt-stop-state");C&&C.addEventListener("click",()=>{d.stop=!0;try{globalThis.__cooptLastUiStopReason="popup-stop-button"}catch{}try{const T=q.OptimizationMVP;T&&typeof T.stop=="function"&&T.stop()}catch{}try{C&&(C.disabled=!0)}catch{}try{F&&(F.disabled=!0)}catch{}x&&(x.textContent="Stopping...")}),F&&F.addEventListener("click",()=>{try{x&&(x.textContent="Running...");const T=p?.document?.getElementById("opt-phase");T&&String(T.textContent||"").trim()==="-"&&(T.textContent="starting");const j=p?.document?.getElementById("opt-startup-progress-wrap"),P=p?.document?.getElementById("opt-startup-progress-bar"),O=p?.document?.getElementById("opt-startup-progress-label");j&&(j.style.display="block"),P&&(P.style.width="5%"),O&&(O.textContent="Run clicked. Preparing..."),F&&(F.disabled=!0),C&&(C.disabled=!1)}catch{}try{const T=q.__cooptStartOptimizationFromPopup;typeof T=="function"&&window.requestAnimationFrame(()=>{window.setTimeout(()=>{try{T()}catch{}},0)})}catch{}})}catch{}}}catch{p=null}if(p){try{t.__cooptOptimizerSchedulerWindow=p,t.__cooptOptimizerIsRunning=!1}catch{}try{p.onbeforeunload=function(Z){if(t.__cooptOptimizerIsRunning){const B="Optimization is still running. Closing this window may cause instability. Are you sure?";return Z.returnValue=B,B}}}catch{}try{u=window.setInterval(()=>{if(!p||p.closed){if(t.__cooptOptimizerIsRunning){alert(`⚠️ Warning: Optimize Progress window was closed while optimization was running.
This may cause instability. Use the Stop button before closing the window.`),d.stop=!0;try{globalThis.__cooptLastUiStopReason="popup-closed-watchdog"}catch{}try{const Z=q.OptimizationMVP;Z&&typeof Z.stop=="function"&&Z.stop()}catch{}t.__cooptOptimizerIsRunning=!1}if(u){try{window.clearInterval(u)}catch{}u=null}try{t.__cooptOptimizerSchedulerWindow===p&&(t.__cooptOptimizerSchedulerWindow=null)}catch{}}},250)}catch{}}const m=document.getElementById("total-merit-value");let h="-",g="-",w="-",R="-",N="-",I="-",b="-",A=Number.POSITIVE_INFINITY,v=0,S=0,_="balanced",M=0;const G=500;let z=null,E=0,L=!1,ne=!1;const V=(Z=!1)=>{try{typeof q.__cooptInitMeritFunctionEditor=="function"&&q.__cooptInitMeritFunctionEditor()}catch{}z||(z=De(()=>import("./optimizer-wasm-bridge-8Q3TNa4c.js"),__vite__mapDeps([7,0,1])).then(B=>B&&typeof B.preloadOptimizerWasmBridge=="function"?B.preloadOptimizerWasmBridge():null).then(()=>{}).catch(()=>{}))};try{window.setTimeout(()=>V(!1),0)}catch{}const K=()=>{try{const Z=q.systemRequirementsEditor,B=(()=>{try{if(Z&&typeof Z.getData=="function"){const T=Z.getData();if(Array.isArray(T))return T}}catch{}try{if(Z&&Array.isArray(Z.requirements))return Z.requirements}catch{}return[]})(),C=T=>{if(!T||typeof T!="object")return!1;const j=T.enabled===void 0||T.enabled===null?!0:!!T.enabled,P=String(T.operand??"").trim(),O=Number(T.weight??1);return!(!j||!P||!(Number.isFinite(O)&&O>0))},F=Array.isArray(B)?B.filter(T=>C(T)):[];let x=Number.NaN;if(F.length>0){let T=0,j=0;for(const P of F){const O=Number(P?._contribution);Number.isFinite(O)&&(O>0&&(T+=O),j+=1)}j>0&&Number.isFinite(T)&&(x=T)}return{score:x,reqCount:F.length}}catch{return{score:Number.NaN,reqCount:Number.NaN}}},$=async()=>{let Z=NaN,B=NaN;try{const C=q.systemRequirementsEditor;if(C&&typeof C.evaluateAndUpdateNow=="function"){const x=C.evaluateAndUpdateNow({reason:"optimize-progress-prerun"});x&&typeof x.then=="function"&&await x}const F=K();Z=F.score,B=F.reqCount}catch{}if(Number.isFinite(Z)||(Z=NaN),!(!p||p.closed))try{const C=p.document,F=C.getElementById("opt-cur"),x=C.getElementById("opt-best"),T=C.getElementById("opt-req"),j=C.getElementById("opt-vio"),P=C.getElementById("opt-soft"),O=C.getElementById("opt-phase");F&&(F.textContent=Number.isFinite(Z)?Z.toFixed(6):"-"),x&&(x.textContent=Number.isFinite(Z)?Z.toFixed(6):"-"),T&&Number.isFinite(B)&&(T.textContent=String(Math.max(0,Math.floor(B)))),j&&(j.textContent=Number.isFinite(Z)?Z.toFixed(6):"-"),P&&(P.textContent=Number.isFinite(Z)?"0.000000":"-"),O&&String(O.textContent||"").trim()==="-"&&(O.textContent="ready")}catch{}},J=()=>{try{const Z=q.popup3DWindow;if(!!!(Z&&!Z.closed)){const F=q.__cooptOpenRenderWindow||window.__cooptOpenRenderWindow;if(st()&&typeof F=="function"&&Promise.resolve(F()),typeof q.handleRender3D=="function")q.handleRender3D();else try{const x=document.getElementById("open-3d-window-btn");x&&typeof x.click=="function"?x.click():typeof q.__open3DWindowLegacy=="function"&&q.__open3DWindowLegacy()}catch{}}try{const F=typeof globalThis<"u"?globalThis:null,T=(F&&Array.isArray(F.__cooptOpticalSystemRowsOverride)&&F.__cooptOpticalSystemRowsOverride.length>0?F.__cooptOpticalSystemRowsOverride:null)??(typeof q.getOpticalSystemRows=="function"?q.getOpticalSystemRows(q.tableOpticalSystem):[]),j={ts:Date.now(),rows:Array.isArray(T)?T:[]};localStorage.setItem("coopt.renderSyncRequest",JSON.stringify(j)),st()&&(async()=>{try{const P=await De(()=>import("./event-H3glLpTN.js"),__vite__mapDeps([2,0,1]));P&&typeof P.emit=="function"&&await P.emit("coopt-render-sync-request",j)}catch{}})()}catch{}const C=()=>{try{const F=q.popup3DWindow;if(F&&!F.closed){const x=F.document?.getElementById("draw-btn");x&&x.click()}}catch{}};C(),window.setTimeout(C,120)}catch{}},Q=Z=>{const B=String(Z?.phase??""),C=(k,Y,D)=>{try{if(!p||p.closed)return;const U=p.document.getElementById("opt-startup-progress-wrap"),ee=p.document.getElementById("opt-startup-progress-bar"),oe=p.document.getElementById("opt-startup-progress-label");U&&(U.style.display="block"),ee&&(ee.style.width=`${Math.max(0,Math.min(100,Number(k)||0))}%`),oe&&Y!==void 0&&(oe.textContent=Y),D&&window.setTimeout(()=>{try{if(!p||p.closed)return;const ae=p.document.getElementById("opt-startup-progress-wrap");ae&&(ae.style.display="none")}catch{}},350)}catch{}};if(B==="start"?C(92,"Optimization started. Waiting first iteration..."):B==="iter"||B==="candidate"||B==="accept"||B==="reject"?C(100,"First iteration reached",!0):(B==="done"||B==="stopped"||B==="error")&&C(100,"Finished",!0),!L&&E>0&&(B==="start"||B==="iter"||B==="candidate"||B==="accept"||B==="reject")){L=!0;const k=Math.max(0,Date.now()-E),Y=`startup=${k}ms`;try{const D=String(h||"").trim();!D||D==="-"?h=Y:D.includes("startup=")||(h=`${D} | ${Y}`)}catch{}try{console.log(`⏱️ [Optimize] Run→first progress: ${k} ms`)}catch{}}if(B==="stopped"||B==="done"||B==="error"){try{e.disabled=!1}catch{}y=!1}if(B==="done"&&!ne)try{if(p&&!p.closed){const k=p.document.getElementById("opt-auto-render");k&&k.checked&&(ne=!0,window.setTimeout(()=>J(),80),window.setTimeout(()=>J(),260))}}catch{}if(B==="accept"){v++;const k=Z&&"alpha"in Z?Number(Z.alpha):NaN,Y=Z&&"rho"in Z?Number(Z.rho):NaN,D=Number.isFinite(k)?k.toFixed(6):"-",U=Number.isFinite(Y)?Y.toFixed(6):"-";b=`ACCEPT (α=${D}, ρ=${U})`,R=U;try{if(p&&!p.closed){const ee=p.document.getElementById("opt-auto-render");ee&&ee.checked&&J()}}catch{}}else B==="reject"&&(S++,b="REJECT");const F=Number(Z?.current),x=Number(Z?.best),T=Number(Z?.violationScore),j=K(),P=Number(j.score),O=Number.isFinite(P)?P:Number.NaN;Number.isFinite(P)&&(A=Math.min(A,P));const X=Number.isFinite(A)?A:Number.NaN;m&&Number.isFinite(O)&&(m.textContent=O.toFixed(6));try{const k=Date.now();if(k-M>=G&&(B==="start"||B==="iter"||B==="candidate"||B==="accept"||B==="reject")){const Y=q.systemRequirementsEditor;Y&&typeof Y.scheduleEvaluateAndUpdate=="function"&&(M=k,Y.scheduleEvaluateAndUpdate())}}catch{}Z&&"materialIssue"in Z&&(h=Z.materialIssue===void 0||Z.materialIssue===null||Z.materialIssue===""?"-":String(Z.materialIssue));try{const k=q.__cooptLastOptimizerResidualDebug&&typeof q.__cooptLastOptimizerResidualDebug=="object"?q.__cooptLastOptimizerResidualDebug:null,Y=k&&k.worst&&typeof k.worst=="object"?k.worst:null,D=k?Number(k.at):NaN,U=Number.isFinite(D)?Date.now()-D<3e3:!1,ee=oe=>{const ae=Number(oe);return Number.isFinite(ae)?ae.toFixed(6):String(oe??"-")};if(U&&Y&&Y.operand){const oe=String(Y.operand),ae=String(Y.configId??""),Ae=Y.scenarioId!==void 0&&Y.scenarioId!==null&&String(Y.scenarioId).trim()?String(Y.scenarioId):"",te=ee(Y.amount),re=ee(Y.current),se=String(Y.reason??"").trim(),ue=(()=>{try{if(!oe.startsWith("SPOT_SIZE"))return"";const xe=k&&k.spotDebug&&typeof k.spotDebug=="object"?k.spotDebug:null;if(!xe)return"";const W=String(xe.impl??"").trim(),fe=String(xe.reason??"").trim(),we=xe.earlyAbortHitRate,Se=we==null||we===""?NaN:Number(we),ye=String(xe.failPenaltyKind??"").trim(),ce=xe.lastRayTraceFailure&&typeof xe.lastRayTraceFailure=="object"?xe.lastRayTraceFailure:null,pe=ce&&ce.details&&typeof ce.details=="object"?ce.details:null,he=Number(xe.blockSurfaceNumber??pe?.surfaceNumber),_e=Number(xe.blockHitRadiusMm??pe?.hitRadiusMm),ve=Number(xe.blockApertureLimitMm??pe?.apertureLimitMm),Ce=Number(xe.targetSurfaceIndex),Te=Number(xe.wavelength),Ie=Number(xe.rayCountRequested),de=Number(xe.hits),be=[];return W&&be.push(W),fe&&be.push(fe),Number.isFinite(Se)&&be.push(`hitRate=${Se.toFixed(3)}`),ye&&be.push(ye),Number.isFinite(Ce)&&Ce>=0&&be.push(`Sidx=${Math.floor(Ce)}`),Number.isFinite(Te)&&Te>0&&be.push(`wl=${Te.toFixed(4)}um`),Number.isFinite(Ie)&&Ie>0&&be.push(`rays=${Math.floor(Ie)}`),Number.isFinite(de)&&de>=0&&be.push(`hits=${Math.floor(de)}`),ye==="PHYSICAL_APERTURE_BLOCK"&&(Number.isFinite(he)&&he>0&&be.push(`S${Math.floor(he)}`),Number.isFinite(_e)&&Number.isFinite(ve)&&ve>0&&be.push(`r=${_e.toFixed(3)}/${ve.toFixed(3)}mm`)),be.length>0?` [spot:${be.join(" ")}]`:""}catch{return""}})(),me=Ae?` cfg=${ae} scn=${Ae}`:ae?` cfg=${ae}`:"",ge=se?` (${se})`:"";h=`Worst: ${oe}${me} cur=${re} amt=${te}${ge}${ue}`}}catch{}if(Z?.requirementCount!==void 0&&(g=String(Z.requirementCount)),Number.isFinite(Number(j.reqCount))&&(g=String(Math.max(0,Math.floor(Number(j.reqCount))))),Z?.residualCount!==void 0&&(w=String(Z.residualCount)),Z&&"rho"in Z){const k=Number(Z.rho);R=Number.isFinite(k)?k.toFixed(6):"-"}if(Number.isFinite(P)&&(N=P.toFixed(6)),Z&&"softPenalty"in Z){const k=Number(Z.softPenalty);I=Number.isFinite(k)?k.toFixed(6):"-"}if(p&&!p.closed)try{const k=p.document,Y=(D,U)=>{const ee=k.getElementById(D);ee&&(ee.textContent=U)};if(Y("opt-phase",String(Z?.phase??"-")),Y("opt-decision",b),Y("opt-decision-count",`${v} / ${S}`),Y("opt-iter",String(Z?.iter??"-")),Y("opt-req",g),Y("opt-res",w),Y("opt-cur",Number.isFinite(O)?O.toFixed(6):"-"),Y("opt-vio",N),Y("opt-soft",I),Y("opt-best",Number.isFinite(X)?X.toFixed(6):"-"),Y("opt-rho",R),Y("opt-issue",h),String(Z?.phase)==="stopped"){Y("opt-stop-state","Stopped");try{const D=k.getElementById("opt-stop");D&&(D.disabled=!0);const U=k.getElementById("opt-run");U&&(U.disabled=!1)}catch{}}else if(String(Z?.phase)==="done"){Y("opt-stop-state","Done");try{const D=k.getElementById("opt-stop");D&&(D.disabled=!0);const U=k.getElementById("opt-run");U&&(U.disabled=!1)}catch{}}else if(String(Z?.phase)==="error"){Y("opt-stop-state","Error");try{const D=k.getElementById("opt-stop");D&&(D.disabled=!0);const U=k.getElementById("opt-run");U&&(U.disabled=!1)}catch{}}else d.stop&&Y("opt-stop-state","Stopping...")}catch{}(B==="done"||B==="stopped")&&(async()=>{try{const k=q.systemRequirementsEditor;if(k&&typeof k.evaluateAndUpdateNow=="function"){const oe=k.evaluateAndUpdateNow({reason:"optimize-progress-final-sync"});oe&&typeof oe.then=="function"&&await oe}const Y=K(),D=Number(Y.score);if(!Number.isFinite(D)||(A=Math.min(A,D),!p||p.closed))return;const U=p.document,ee=(oe,ae)=>{const Ae=U.getElementById(oe);Ae&&(Ae.textContent=ae)};ee("opt-cur",D.toFixed(6)),ee("opt-vio",D.toFixed(6)),ee("opt-best",A.toFixed(6)),Number.isFinite(Number(Y.reqCount))&&ee("opt-req",String(Math.max(0,Math.floor(Number(Y.reqCount)))))}catch{}})()},le=async()=>{y=!0,t.__cooptOptimizerIsRunning=!0,E=Date.now(),L=!1,ne=!1;const Z=(B,C,F)=>{try{if(!p||p.closed)return;const x=p.document,T=x.getElementById("opt-phase"),j=x.getElementById("opt-issue"),P=x.getElementById("opt-iter"),O=x.getElementById("opt-startup-progress-wrap"),X=x.getElementById("opt-startup-progress-bar"),k=x.getElementById("opt-startup-progress-label");T&&(T.textContent=String(B||"-")),j&&C!==void 0&&(j.textContent=String(C||"-")),P&&F!==void 0&&(P.textContent=String(F||"-")),O&&(O.style.display="block");const Y=String(B||"").toLowerCase();let D=10;Y==="prepare"?D=35:Y==="warmup"?D=70:Y==="start"&&(D=88),X&&(X.style.width=`${D}%`),k&&C!==void 0&&(k.textContent=String(C||""))}catch{}};try{let B=null;try{const k=We();B=k?JSON.parse(JSON.stringify(k)):null}catch{}d.stop=!1;try{globalThis.__cooptLastUiStopReason=null}catch{}v=0,S=0,A=Number.POSITIVE_INFINITY,h="-",g="-",w="-",R="-",N="-",I="-",b="-",Z("prepare","Loading active configuration...");try{const k=typeof q.loadSystemConfigurationsFromTableConfig=="function"?q.loadSystemConfigurationsFromTableConfig():We(),Y=k?.activeConfigId;c=k?.configurations?.find(ee=>ee&&ee.id===Y)||k?.configurations?.[0]||null,c&&Array.isArray(c.scenarios)&&c.scenarios.length>=2?a=!0:a=!1;const D=Ni(c||{}),U=Array.isArray(D)?D.filter(ee=>typeof ee?.value=="number"&&Number.isFinite(ee.value)):[];if(i=Array.isArray(D)?D.length:0,l=U.length,f=Math.max(0,i-l),p&&!p.closed)try{const ee=p.document.getElementById("opt-vars");if(ee){const oe=[];Number.isFinite(i)&&oe.push(String(i)),(Number.isFinite(l)||Number.isFinite(f))&&oe.push(`(num ${l}, cat ${f})`),ee.textContent=oe.length?oe.join(" "):"-"}}catch{}}catch{}Z("prepare",`Configuration loaded (vars=${i}, num=${l}, cat=${f})`);try{if(p&&!p.closed){const k=p.document,Y=k.getElementById("opt-stop"),D=k.getElementById("opt-run"),U=k.getElementById("opt-stop-state"),ee=k.getElementById("opt-phase");Y&&(Y.disabled=!1),D&&(D.disabled=!0),U&&(U.textContent="Running..."),ee&&String(ee.textContent||"").trim()==="-"&&(ee.textContent="starting")}}catch{}try{e.disabled=!0}catch{}const C=()=>!!d.stop,F=()=>{let k=5e3;try{if(p&&!p.closed){const Y=p.document.getElementById("opt-max-iter"),D=Y?Number(Y.value):NaN;Number.isFinite(D)&&(k=Math.trunc(D))}}catch{}return(!Number.isFinite(k)||k<1)&&(k=5e3),k},x=()=>{const k=(Ae,te)=>{let re=te;try{if(p&&!p.closed){const se=p.document.getElementById(Ae),ue=se?Number(se.value):NaN;Number.isFinite(ue)&&(re=ue)}}catch{}return re},Y=(Ae,te)=>{let re=te;try{if(p&&!p.closed){const se=p.document.getElementById(Ae);se&&typeof se.checked=="boolean"&&(re=!!se.checked)}}catch{}return re},D=(Ae,te)=>{let re=te;try{if(p&&!p.closed){const se=p.document.getElementById(Ae),ue=se?String(se.value).trim().toLowerCase():"";ue&&(re=ue)}}catch{}return re},U=k("opt-trust-region-delta",.05),ee=Math.max(U,k("opt-trust-region-delta-max",1)),oe=D("opt-convergence-profile","balanced"),ae=oe==="fast"?{kktPlateauStopMinIter:30,kktPlateauStopWindow:30,kktTailStopMinIter:80,kktTailStopWindow:40,kktWindowNoGainMinIter:70,kktWindowNoGainWindow:30,kktGoodEnoughStopMinIter:55,kktGoodEnoughStopWindow:20,kktNoBestImproveMinIter:120,kktNoBestImproveWindow:80,kktPostBestNoImproveWindow:10,kktPostBestPatienceWindow:10,kktHardIterCap:180,kktMaxWallMs:12e4}:oe==="deep"?{kktPlateauStopMinIter:70,kktPlateauStopWindow:70,kktTailStopMinIter:180,kktTailStopWindow:90,kktWindowNoGainMinIter:170,kktWindowNoGainWindow:90,kktGoodEnoughStopMinIter:130,kktGoodEnoughStopWindow:70,kktNoBestImproveMinIter:260,kktNoBestImproveWindow:180,kktPostBestNoImproveWindow:24,kktPostBestPatienceWindow:24,kktHardIterCap:520,kktMaxWallMs:36e4}:{kktPlateauStopMinIter:45,kktPlateauStopWindow:45,kktTailStopMinIter:120,kktTailStopWindow:60,kktWindowNoGainMinIter:110,kktWindowNoGainWindow:50,kktGoodEnoughStopMinIter:90,kktGoodEnoughStopWindow:35,kktNoBestImproveMinIter:180,kktNoBestImproveWindow:120,kktPostBestNoImproveWindow:16,kktPostBestPatienceWindow:16,kktHardIterCap:320,kktMaxWallMs:24e4};return{stepFraction:k("opt-step-fraction",.02),minStep:k("opt-min-step",1e-6),stepDecay:k("opt-step-decay",.5),lmLambda0:k("opt-lm-lambda0",.001),lmLambdaUp:k("opt-lm-lambdaup",10),lmLambdaDown:k("opt-lm-lambdadown",.3),trustRegion:Y("opt-trust-region",!0),trustRegionDelta:U,trustRegionDeltaMax:ee,backtracking:Y("opt-backtracking",!0),backtrackingMaxTries:Math.max(1,Math.floor(k("opt-backtracking-max-tries",8))),fdStepFraction:k("opt-fd-step-fraction",1e-4),fdMinStep:k("opt-fd-min-step",1e-18),fdScaledStep:k("opt-fd-scaled-step",.001),staged:Y("opt-staged",!0),stageStallLimit:Math.max(1,Math.floor(k("opt-stage-stall-limit",5))),restartOnRejectStreak:Math.max(1,Math.floor(k("opt-restart-on-reject-streak",8))),restartMaxCount:Math.max(0,Math.floor(k("opt-restart-max-count",2))),restartJitterScaled:Math.max(0,k("opt-restart-jitter-scaled",.035)),lmExploreWhenFlat:Y("opt-lm-explore-when-flat",!1),lmExploreTries:Math.max(1,Math.floor(k("opt-lm-explore-tries",3))),convergenceProfile:oe,...ae,useWasmLinearSolve:!0,profile:!1}},T=F(),j=x();_=String(j?.convergenceProfile||"balanced").toLowerCase(),Z("prepare",`Options resolved (conv=${_}, maxIter=${T})`);try{const k=`conv=${_}`,Y=String(h||"").trim();h=!Y||Y==="-"?k:`${Y} | ${k}`}catch{}Z("warmup","Initializing WASM bridge..."),V(!0);try{z&&await z,Z("warmup","WASM bridge ready")}catch{Z("warmup","WASM warmup skipped (fallback available)")}const P=()=>{let k="kkt";try{if(p&&!p.closed){const Y=p.document.getElementById("opt-method"),D=Y?String(Y.value).toLowerCase().trim():"";(D==="cd"||D==="lm"||D==="kkt")&&(k=D)}}catch{}return k};let O=null,X;try{q.undoHistory&&(q.undoHistory.isExecuting=!0);try{X=t.__COOPT_DISABLE_RAYTRACE_DEBUG}catch{X=void 0}try{t.__COOPT_DISABLE_RAYTRACE_DEBUG=!0}catch{}Z("start","Starting optimization and computing initial residuals...","0"),O=await s.run({multiScenario:a,runUntilStopped:!1,maxIterations:T,method:P(),stageMaxCoef:[10],...j,onProgress:Q,shouldStop:C});try{if(X!==void 0)t.__COOPT_DISABLE_RAYTRACE_DEBUG=X;else try{delete t.__COOPT_DISABLE_RAYTRACE_DEBUG}catch{}t.__cooptOptimizerIsRunning=!1}catch{}q.undoHistory&&(q.undoHistory.isExecuting=!1);try{if(B&&q.undoHistory&&O?.ok){const k=We(),Y=k?JSON.parse(JSON.stringify(k)):null;if(JSON.stringify(B)!==JSON.stringify(Y)){const D={name:"Optimization",execute:async()=>{et(Y);try{q.ConfigurationManager&&typeof q.ConfigurationManager.loadActiveConfigurationToTables=="function"&&await q.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0})}catch{}try{kn(q)}catch{}},undo:async()=>{et(B);try{q.ConfigurationManager&&typeof q.ConfigurationManager.loadActiveConfigurationToTables=="function"&&await q.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0})}catch{}try{kn(q)}catch{}},redo:function(){return this.execute()}};q.undoHistory.record(D)}}}catch{}}catch(k){console.warn("⚠️ [Optimize] Failed:",k),O={ok:!1,reason:k?.message??String(k)};try{if(X!==void 0)t.__COOPT_DISABLE_RAYTRACE_DEBUG=X;else try{delete t.__COOPT_DISABLE_RAYTRACE_DEBUG}catch{}t.__cooptOptimizerIsRunning=!1}catch{}q.undoHistory&&(q.undoHistory.isExecuting=!1)}finally{y=!1,t.__cooptOptimizerIsRunning=!1;try{e.disabled=!1}catch{}try{if(p&&!p.closed){const k=p.document,Y=k.getElementById("opt-stop"),D=k.getElementById("opt-run"),U=k.getElementById("opt-stop-state");Y&&(Y.disabled=!0),D&&(D.disabled=!1),U&&d.stop&&(U.textContent="Stopped")}}catch{}}if(O&&O.ok===!1){const k=String(O.reason||"Optimize did not run.");try{if(p&&!p.closed){const Y=p.document.getElementById("opt-phase");Y&&(Y.textContent="error");const D=p.document.getElementById("opt-cur");D&&(D.textContent=k)}}catch{}alert(k)}}catch(B){console.warn("⚠️ [Optimize] Outer error:",B),t.__cooptOptimizerIsRunning=!1,y=!1}};try{q.__cooptStartOptimizationFromPopup=le}catch{}try{const Z=()=>{y||Promise.resolve().then(()=>$()).catch(()=>{})},B=typeof globalThis<"u"?globalThis:null;B&&typeof B.requestIdleCallback=="function"?B.requestIdleCallback(()=>Z(),{timeout:1500}):window.setTimeout(()=>Z(),600)}catch{}}catch(s){console.warn("⚠️ [Optimize] Failed:",s),alert("Optimize の実行に失敗しました。console を確認してください。")}finally{try{e.disabled=!1}catch{}const s=typeof globalThis<"u"?globalThis:{};s.__cooptOptimizerIsRunning=!1}}))}function Kp(){const e=document.getElementById("suggest-optimize-btn");e&&e.addEventListener("click",async()=>{try{const t=window.open("","OptimizationProgress","width=900,height=700");if(!t){alert("Popup blocked. Please allow popups for this site.");return}t.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Optimization Progress</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            margin: 0;
            padding: 20px;
            background: #f5f5f5;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        h1 {
            margin: 0 0 20px 0;
            font-size: 24px;
            color: #333;
        }
        .progress-bar {
            width: 100%;
            height: 30px;
            background: #e0e0e0;
            border-radius: 4px;
            overflow: hidden;
            margin-bottom: 20px;
        }
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #4CAF50, #45a049);
            transition: width 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
        }
        .metrics {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
            margin-bottom: 20px;
        }
        .metric {
            padding: 12px;
            background: #f9f9f9;
            border-radius: 4px;
            border: 1px solid #e0e0e0;
        }
        .metric-label {
            font-size: 12px;
            color: #666;
            margin-bottom: 4px;
        }
        .metric-value {
            font-size: 18px;
            font-weight: bold;
            color: #333;
        }
        .tuning-params {
            margin-top: 20px;
            padding: 15px;
            background: #f0f8ff;
            border-radius: 4px;
        }
        .tuning-params h3 {
            margin: 0 0 12px 0;
            font-size: 16px;
        }
        .param-row {
            display: flex;
            align-items: center;
            margin-bottom: 8px;
        }
        .param-label {
            flex: 0 0 200px;
            font-size: 13px;
        }
        .param-input {
            flex: 1;
            padding: 4px 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        .button-row {
            display: flex;
            gap: 12px;
            margin-top: 20px;
        }
        .btn {
            flex: 1;
            padding: 12px;
            border: none;
            border-radius: 4px;
            font-size: 14px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s;
        }
        .btn-start {
            background: #4CAF50;
            color: white;
        }
        .btn-start:hover {
            background: #45a049;
        }
        .btn-start:disabled {
            background: #ccc;
            cursor: not-allowed;
        }
        .btn-stop {
            background: #f44336;
            color: white;
        }
        .btn-stop:hover {
            background: #da190b;
        }
        .btn-accept {
            background: #2196F3;
            color: white;
        }
        .btn-accept:hover {
            background: #0b7dda;
        }
        .btn-reject {
            background: #ff9800;
            color: white;
        }
        .btn-reject:hover {
            background: #e68900;
        }
        .log {
            margin-top: 20px;
            padding: 12px;
            background: #f5f5f5;
            border-radius: 4px;
            max-height: 200px;
            overflow-y: auto;
            font-family: monospace;
            font-size: 12px;
        }
        .log-entry {
            margin-bottom: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Optimization Progress</h1>
        <div class="progress-bar">
            <div class="progress-fill" id="progress" style="width: 0%">0%</div>
        </div>
        <div class="metrics">
            <div class="metric">
                <div class="metric-label">Iteration</div>
                <div class="metric-value" id="iteration">0</div>
            </div>
            <div class="metric">
                <div class="metric-label">Merit Function</div>
                <div class="metric-value" id="merit">—</div>
            </div>
            <div class="metric">
                <div class="metric-label">Best Merit</div>
                <div class="metric-value" id="best-merit">—</div>
            </div>
            <div class="metric">
                <div class="metric-label">Accepted</div>
                <div class="metric-value" id="accepted">0</div>
            </div>
            <div class="metric">
                <div class="metric-label">Rejected</div>
                <div class="metric-value" id="rejected">0</div>
            </div>
            <div class="metric">
                <div class="metric-label">Variables</div>
                <div class="metric-value" id="variables">0</div>
            </div>
        </div>
        <div class="tuning-params">
            <h3>Stability Tuning Parameters</h3>
            <div class="param-row">
                <label class="param-label">Step Fraction:</label>
                <input type="number" class="param-input" id="step-fraction" value="0.1" step="0.01" min="0.01" max="1">
            </div>
            <div class="param-row">
                <label class="param-label">Min Step:</label>
                <input type="number" class="param-input" id="min-step" value="1e-6" step="1e-7">
            </div>
            <div class="param-row">
                <label class="param-label">LM Lambda:</label>
                <input type="number" class="param-input" id="lm-lambda" value="0.001" step="0.001">
            </div>
            <div class="param-row">
                <label class="param-label">Max Iterations:</label>
                <input type="number" class="param-input" id="max-iter" value="100" step="10" min="10">
            </div>
            <div class="param-row">
                <label class="param-label">
                    <input type="checkbox" id="auto-render" checked> Auto-render on Accept
                </label>
            </div>
        </div>
        <div class="button-row">
            <button class="btn btn-start" id="start-btn">Start Optimization</button>
            <button class="btn btn-stop" id="stop-btn" disabled>Stop</button>
            <button class="btn btn-accept" id="accept-btn" disabled>Accept</button>
            <button class="btn btn-reject" id="reject-btn" disabled>Reject</button>
        </div>
        <div class="log" id="log"></div>
    </div>
    <script>
        let isRunning = false;
        let currentBest = null;
        
        function log(message) {
            const logEl = document.getElementById('log');
            const entry = document.createElement('div');
            entry.className = 'log-entry';
            entry.textContent = new Date().toLocaleTimeString() + ': ' + message;
            logEl.appendChild(entry);
            logEl.scrollTop = logEl.scrollHeight;
        }
        
        document.getElementById('start-btn').addEventListener('click', () => {
            if (isRunning) return;
            isRunning = true;
            document.getElementById('start-btn').disabled = true;
            document.getElementById('stop-btn').disabled = false;
            
            const params = {
                stepFraction: parseFloat(document.getElementById('step-fraction').value),
                minStep: parseFloat(document.getElementById('min-step').value),
                lmLambda: parseFloat(document.getElementById('lm-lambda').value),
                maxIter: parseInt(document.getElementById('max-iter').value)
            };
            
            window.opener.postMessage({ action: 'start-optimization', params }, '*');
            log('Optimization started');
        });
        
        document.getElementById('stop-btn').addEventListener('click', () => {
            if (!isRunning) return;
            isRunning = false;
            document.getElementById('start-btn').disabled = false;
            document.getElementById('stop-btn').disabled = true;
            window.opener.postMessage({ action: 'stop-optimization' }, '*');
            log('Stop requested');
        });
        
        document.getElementById('accept-btn').addEventListener('click', () => {
            window.opener.postMessage({ action: 'accept-result' }, '*');
            document.getElementById('accept-btn').disabled = true;
            document.getElementById('reject-btn').disabled = true;
            log('Result accepted');
            
            if (document.getElementById('auto-render').checked) {
                window.opener.postMessage({ action: 'request-render' }, '*');
            }
        });
        
        document.getElementById('reject-btn').addEventListener('click', () => {
            window.opener.postMessage({ action: 'reject-result' }, '*');
            document.getElementById('accept-btn').disabled = true;
            document.getElementById('reject-btn').disabled = true;
            log('Result rejected');
        });
        
        window.addEventListener('message', (e) => {
            if (e.data.type === 'optimization-progress') {
                const data = e.data;
                document.getElementById('progress').style.width = data.progress + '%';
                document.getElementById('progress').textContent = data.progress + '%';
                document.getElementById('iteration').textContent = data.iteration;
                document.getElementById('merit').textContent = data.merit?.toFixed(6) || '—';
                document.getElementById('best-merit').textContent = data.bestMerit?.toFixed(6) || '—';
                document.getElementById('accepted').textContent = data.accepted || 0;
                document.getElementById('rejected').textContent = data.rejected || 0;
                document.getElementById('variables').textContent = data.variables || 0;
            }
            
            if (e.data.type === 'optimization-complete') {
                isRunning = false;
                document.getElementById('start-btn').disabled = false;
                document.getElementById('stop-btn').disabled = true;
                document.getElementById('accept-btn').disabled = false;
                document.getElementById('reject-btn').disabled = false;
                log('Optimization complete');
            }
        });
    <\/script>
</body>
</html>
            `)}catch(t){console.error("❌ Failed to open optimization popup:",t)}})}function Jp(){const e=document.getElementById("new-file-btn");if(!e||gn(e))return;const t=()=>{if(confirm("Create new file? Current data will be cleared."))try{console.log("🔵 [New File] Clearing localStorage and creating default configuration..."),bl();const r={configurations:[{id:1,name:"Config 1",schemaVersion:po,blocks:[{blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:{objectDistanceMode:"INF",objectDistance:10},variables:{},metadata:{source:"default"}},{blockId:"Stop-1",blockType:"Stop",role:null,constraints:{},parameters:{semiDiameter:10},variables:{},metadata:{source:"default"}},{blockId:"ImageSurface-1",blockType:"ImageSurface",role:null,constraints:{},parameters:{semidiaMode:"Manual"},variables:{},metadata:{source:"default"}}],source:[{id:1,wavelength:.4358343,weight:1,primary:"",angle:0},{id:2,wavelength:.5875618,weight:1,primary:"Primary Wavelength",angle:0},{id:3,wavelength:.6562725,weight:1,primary:"",angle:0}],object:[{id:1,xHeightAngle:0,yHeightAngle:0,position:"Angle",angle:0},{id:2,xHeightAngle:0,yHeightAngle:17.05,position:"Angle",angle:0}],opticalSystem:[],systemData:{referenceFocalLength:""},metadata:{created:new Date().toISOString(),modified:new Date().toISOString(),locked:!1},meritFunction:[]}],activeConfigId:1,meritFunction:[],systemRequirements:[],optimizationRules:{}};et(r),console.log("✅ [New File] Default configuration created, reloading..."),location.reload()}catch(o){console.error("❌ Failed to create new file:",o),alert("Failed to create new file. See console for details.")}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Qp(){const e=document.getElementById("save-all-btn");if(!e||gn(e))return;const t=async()=>{try{document.activeElement&&document.activeElement.blur();const o=ja();let r=null;try{const{getLoadedFileName:f}=await De(async()=>{const{getLoadedFileName:p}=await import("./loaded-file-storage-BCc7IGxJ.js");return{getLoadedFileName:p}},__vite__mapDeps([6,1]));r=f()}catch{}let s="optical_system_data";r&&(s=r.replace(/\.json$/i,""));let a=prompt(`保存するファイル名を入力してください（拡張子 .json は自動で付きます）

※ダウンロードフォルダに既存ファイルがある場合はブラウザが自動的に連番を付けます`,s);if(!a)return;a.endsWith(".json")||(a+=".json");const c=new Blob([JSON.stringify(o,null,2)],{type:"application/json"}),i=URL.createObjectURL(c),l=document.createElement("a");l.href=i,l.download=a,l.click(),URL.revokeObjectURL(i);try{const{setLoadedFileName:f}=await De(async()=>{const{setLoadedFileName:p}=await import("./loaded-file-storage-BCc7IGxJ.js");return{setLoadedFileName:p}},__vite__mapDeps([6,1]));f(a)}catch{}console.log("✅ データが保存されました:",a)}catch(o){console.error("❌ Failed to save:",o),alert(`Save failed: ${o?.message||String(o)}`)}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function ed(){const e=We(),t=e?JSON.parse(JSON.stringify(e)):null;if(t){try{delete t.meritFunction}catch{}try{delete t.systemRequirements}catch{}try{if(Array.isArray(t.configurations)){for(const n of t.configurations)if(n&&typeof n=="object")try{delete n.source}catch{}}}catch{}}return t}function ja(){const e=document.getElementById("reference-focal-length"),t=e?e.value:"";let n=q.tableOpticalSystem?q.tableOpticalSystem.getData():[];try{const o=typeof q.loadSystemConfigurations=="function"?q.loadSystemConfigurations():null,r=o?.activeConfigId,s=Array.isArray(o?.configurations)?o.configurations.find(c=>String(c?.id)===String(r))||o.configurations[0]:null;if(s&&(c=>{try{return c&&Array.isArray(c.blocks)&&c.blocks.length>0}catch{return!1}})(s)&&typeof q.expandBlocksToOpticalSystemRows=="function"){const c=q.expandBlocksToOpticalSystemRows(s.blocks);c&&Array.isArray(c.rows)&&(n=c.rows)}}catch{}return{source:q.tableSource?q.tableSource.getData():[],object:q.tableObject?q.tableObject.getData():[],opticalSystem:n,meritFunction:q.meritFunctionEditor?q.meritFunctionEditor.getData():[],systemRequirements:q.systemRequirementsEditor?q.systemRequirementsEditor.getData():[],systemData:{referenceFocalLength:t},configurations:ed()}}function td(){const e=document.getElementById("load-default-btn");if(!e||gn(e))return;const t=async()=>{if(confirm("Load default optical system? Current data will be replaced."))try{let o=await fetch("/co-opt/defaults/default-load.json");if(o.ok||(o=await fetch("/defaults/default-load.json")),!o.ok)throw new Error(`Failed to load default system: ${o.statusText}`);const r=await o.json();await go(r,{filename:"default-load.json"})}catch(o){console.error("❌ Failed to load default system:",o),alert("Failed to load default optical system. Check console for details.")}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function nd(){const e=document.getElementById("share-url-btn");if(!e||gn(e))return;const t=2e3,n=3e4,o=async()=>{try{document.activeElement&&document.activeElement.blur();let s;try{const l=ja();s=Pc(l)}catch(l){console.warn("❌ [Share] Failed to encode:",l),alert(l?.message||"Failed to generate share URL");return}const a=`${location.origin}${location.pathname}`;let c;try{c=kc(s,a)}catch(l){console.warn("❌ [Share] Failed to build URL:",l),alert(l?.message||"Failed to generate share URL");return}const i=c.length;if(i>n){alert(`Share URL is too long (${i} chars). Please use Save instead.`);return}if(i>=t&&!confirm(`Share URL is long (${i} chars) and may not work in some apps.

Continue?`))return;try{await navigator.clipboard.writeText(c),alert("Share URL copied to clipboard.")}catch{prompt("Copy this URL:",c)}}catch(s){console.error("❌ Failed to share:",s),alert(`Share failed: ${s?.message||String(s)}`)}},r=e.cloneNode(!0);e.parentNode?.replaceChild(r,e),r.addEventListener("click",o)}function od(){const e=document.getElementById("calculate-paraxial-btn");e&&e.addEventListener("click",async()=>{try{if(typeof q.outputParaxialDataToDebug=="function"){const t=q.tableOpticalSystem;q.outputParaxialDataToDebug(t)}else console.error("❌ outputParaxialDataToDebug関数が見つかりません")}catch(t){console.error("❌ 近軸計算ボタンエラー:",t)}})}function rd(){const e=document.getElementById("calculate-seidel-btn");e&&e.addEventListener("click",async()=>{try{typeof q.outputSeidelCoefficientsToDebug=="function"?q.outputSeidelCoefficientsToDebug():console.error("❌ outputSeidelCoefficientsToDebug関数が見つかりません")}catch(t){console.error("❌ Seidel係数計算ボタンエラー:",t)}})}function id(){const e=document.getElementById("calculate-seidel-afocal-btn");e&&e.addEventListener("click",async()=>{try{const{calculateAfocalSeidelCoefficientsIntegrated:t}=await De(async()=>{const{calculateAfocalSeidelCoefficientsIntegrated:d}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(u=>u.bS);return{calculateAfocalSeidelCoefficientsIntegrated:d}},__vite__mapDeps([0,1])),{formatSeidelCoefficients:n}=await De(async()=>{const{formatSeidelCoefficients:d}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(u=>u.bR);return{formatSeidelCoefficients:d}},__vite__mapDeps([0,1])),o=q.getOpticalSystemRows?q.getOpticalSystemRows():[],r=q.getObjectTableRows?q.getObjectTableRows():[],s=q.getSourceTableRows?q.getSourceTableRows():[];if(o.length===0){console.error("❌ Optical system data is empty"),alert("光学系データがありません。");return}const a=(()=>{if(typeof q.getPrimaryWavelength=="function"){const d=Number(q.getPrimaryWavelength());if(Number.isFinite(d)&&d>0)return d}return alert("Primary wavelength is unavailable. Please set Source Primary Wavelength."),NaN})();if(!Number.isFinite(a)||a<=0)return;let c=o.findIndex(d=>d["object type"]==="Stop"||d.object==="Stop");c===-1&&(console.warn("⚠️ Stop surface not found, using surface 1"),c=1);const i=document.getElementById("reference-focal-length");let l;if(i){const d=i.value.trim();if(d!==""&&d.toLowerCase()!=="auto"){const u=parseFloat(d);l=isFinite(u)?u:void 0}}const f=t(o,a,c,r,l);if(!f){console.error("❌ Afocal Seidel coefficients calculation failed"),alert("アフォーカル系収差係数の計算に失敗しました。");return}const p=document.getElementById("system-data");if(p){if(p.value=n(f),typeof q.renderBlockContributionSummaryFromSeidel=="function")try{q.renderBlockContributionSummaryFromSeidel(f,o)}catch(d){console.warn("⚠️ Block contribution summary render failed (afocal):",d)}}else console.error("❌ System Data textarea not found")}catch(t){console.error("❌ アフォーカル系Seidel係数計算ボタンエラー:",t),alert(`エラーが発生しました: ${t.message}`)}})}function sd(){const e=document.getElementById("coord-transform-btn");e&&e.addEventListener("click",()=>{try{typeof q.displayCoordinateTransformMatrix=="function"?q.displayCoordinateTransformMatrix():console.error("❌ displayCoordinateTransformMatrix関数が見つかりません")}catch(t){console.error("❌ 座標変換ボタンエラー:",t)}})}function ad(){const e=document.getElementById("show-spot-diagram-btn");e&&e.addEventListener("click",()=>{typeof q.showSpotDiagram=="function"&&q.showSpotDiagram()})}function cd(){const e=document.getElementById("show-longitudinal-aberration-diagram-btn");e&&e.addEventListener("click",()=>{typeof q.showLongitudinalAberration=="function"&&q.showLongitudinalAberration()})}function ld(){const e=document.getElementById("show-transverse-aberration-diagram-btn");e&&e.addEventListener("click",()=>{typeof q.showTransverseAberration=="function"&&q.showTransverseAberration()})}function ud(){const e=document.getElementById("show-magnification-chromatic-aberration-btn");e&&e.addEventListener("click",()=>{if(typeof q.showMagnificationChromaticAberrationDiagram!="function")return;const t=document.getElementById("mca-progress-wrapper"),n=document.getElementById("mca-progressbar"),o=n instanceof HTMLProgressElement?n:null,r=document.getElementById("mca-progress-text"),s=(c,i)=>{try{t&&(t.style.display="block"),o&&Number.isFinite(c)&&(o.value=Math.max(0,Math.min(100,c))),r&&typeof i=="string"&&(r.textContent=i)}catch{}};s(0,"Starting...");const a=c=>{try{const i=Number(c?.percent),l=c?.message||c?.phase||"Working...";Number.isFinite(i)?s(i,l):s(void 0,l)}catch{}};q.showMagnificationChromaticAberrationDiagram({onProgress:a})})}function pd(){const e=document.getElementById("show-distortion-diagram-btn");e&&(e.__cooptDistortionBound||(e.__cooptDistortionBound=!0,e.addEventListener("click",()=>{typeof q.showDistortion=="function"&&q.showDistortion()})))}function dd(){const e=document.getElementById("show-integrated-aberration-btn");e&&e.addEventListener("click",()=>{typeof q.showIntegratedAberration=="function"&&q.showIntegratedAberration()})}function md(){const e=document.getElementById("show-astigmatism-diagram-btn");e&&e.addEventListener("click",()=>{typeof q.showAstigmatism=="function"&&q.showAstigmatism()})}function fd(){const e=document.getElementById("psf-log-scale"),t=document.getElementById("psf-contours"),n=document.getElementById("psf-characteristics");e?.addEventListener("change",()=>{typeof q.updatePSFDisplay=="function"&&q.updatePSFDisplay()}),t?.addEventListener("change",()=>{typeof q.updatePSFDisplay=="function"&&q.updatePSFDisplay()}),n?.addEventListener("change",()=>{typeof q.updatePSFDisplay=="function"&&q.updatePSFDisplay()})}function gd(){const e=[{id:"psf-2d-btn",mode:"2d"},{id:"psf-3d-btn",mode:"3d"},{id:"psf-profile-btn",mode:"profile"},{id:"psf-energy-btn",mode:"energy"},{id:"psf-wavefront-btn",mode:"wavefront"}];e.forEach(({id:t,mode:n})=>{const o=document.getElementById(t);o&&o.addEventListener("click",()=>{e.forEach(({id:r})=>{document.getElementById(r)?.classList.remove("active")}),o.classList.add("active"),typeof q.switchPSFDisplayMode=="function"&&q.switchPSFDisplayMode(n)})})}function La(e,t){return{id:e,name:t,schemaVersion:po,blocks:[{blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:{objectDistanceMode:"INF",objectDistance:10},variables:{},metadata:{source:"default"}},{blockId:"Stop-1",blockType:"Stop",role:null,constraints:{},parameters:{semiDiameter:Ta},variables:{},metadata:{source:"default"}},{blockId:"ImageSurface-1",blockType:"ImageSurface",role:null,constraints:{},parameters:{semidiaMode:"Manual"},variables:{},metadata:{source:"default"}}],source:[{id:1,wavelength:.4358343,weight:1,primary:"",angle:0},{id:2,wavelength:.5875618,weight:1,primary:"Primary Wavelength",angle:0},{id:3,wavelength:.6562725,weight:1,primary:"",angle:0}],object:[{id:1,xHeightAngle:0,yHeightAngle:0,position:"Angle",angle:0},{id:2,xHeightAngle:0,yHeightAngle:17.05,position:"Angle",angle:0}],opticalSystem:[],meritFunction:[],metadata:{created:new Date().toISOString(),modified:new Date().toISOString(),optimizationTarget:null,locked:!1}}}const yd={configurations:[La(1,"Config 1")],activeConfigId:1,optimizationRules:{}};function We(){try{return Ot()}catch(e){return console.warn("⚠️ [Configuration] Load failed; using default system config:",e),yd}}function et(e){try{Jr(e);try{window.dispatchEvent(new CustomEvent("coopt:system-configurations-updated"))}catch{}}catch(t){console.warn("⚠️ [Configuration] Save failed:",t)}}function co(){const e=We(),t=e.configurations.find(n=>n.id===e.activeConfigId);return t||(console.warn("⚠️ [Configuration] Active config not found, using first"),e.configurations[0])}function hd(){return We().activeConfigId}function bd(e){const t=We(),n=t.configurations.find(o=>o.id===e);return n?(t.activeConfigId=e,et(t),console.log(`✅ [Configuration] Active config changed to: ${n.name}`),!0):(console.error("❌ [Configuration] Config not found:",e),!1)}function wd(){console.log("🔵 [Configuration] Saving current table data to active configuration...");const e=We(),t=e.configurations.find(n=>n.id===e.activeConfigId);if(!t){console.error("❌ [Configuration] Active config not found");return}try{const n=q.tableSource?q.tableSource.getData():[];Vo(n)}catch{}t.object=q.tableObject?q.tableObject.getData():[],t.opticalSystem=q.tableOpticalSystem?q.tableOpticalSystem.getData():[],t.meritFunction=q.meritFunctionEditor?q.meritFunctionEditor.getData():[],t.metadata.modified=new Date().toISOString(),t.metadata.designer||(t.metadata.designer={type:"human",name:"user",confidence:null}),et(e),console.log(`✅ [Configuration] Saved to: ${t.name}`)}function Uo(){const e=co();if(!e){console.error("❌ [Configuration] No active config found");return}try{const t=Tc()!==null,n=Array.isArray(e.source)?e.source:null;!t&&n&&n.length>0&&Vo(n)}catch{}e.object&&Vr(e.object),e.opticalSystem&&jn(e.opticalSystem),e.meritFunction&&Os(e.meritFunction)}function vd(e){const t=We(),o=Math.max(...t.configurations.map(a=>a.id),0)+1,r=La(o,e),s=co();return s&&(r.object=JSON.parse(JSON.stringify(s.object)),r.opticalSystem=JSON.parse(JSON.stringify(s.opticalSystem)),r.meritFunction=JSON.parse(JSON.stringify(s.meritFunction))),t.configurations.push(r),et(t),console.log(`✅ [Configuration] Added new configuration: ${e} (ID: ${o})`),o}function xd(e){const t=We();if(t.configurations.length<=1)return console.warn("⚠️ [Configuration] Cannot delete last configuration"),!1;const n=t.configurations.findIndex(r=>r.id===e);if(n===-1)return console.error("❌ [Configuration] Config not found:",e),!1;const o=t.configurations[n].name;return t.configurations.splice(n,1),t.activeConfigId===e&&(t.activeConfigId=t.configurations[0].id,console.log(`🔄 [Configuration] Active config changed to: ${t.configurations[0].name}`)),et(t),console.log(`✅ [Configuration] Deleted configuration: ${o}`),!0}function Sd(e){const t=We(),n=t.configurations.find(a=>a.id===e);if(!n)return console.error("❌ [Configuration] Config not found:",e),null;const r=Math.max(...t.configurations.map(a=>a.id),0)+1,s=JSON.parse(JSON.stringify(n));return s.id=r,s.name=`${n.name} (Copy)`,s.metadata.created=new Date().toISOString(),s.metadata.modified=new Date().toISOString(),t.configurations.push(s),et(t),console.log(`✅ [Configuration] Duplicated configuration: ${s.name} (ID: ${r})`),r}function Ad(e,t){const n=We(),o=n.configurations.find(s=>s.id===e);if(!o)return console.error("❌ [Configuration] Config not found:",e),!1;const r=o.name;return o.name=t,o.metadata.modified=new Date().toISOString(),et(n),console.log(`✅ [Configuration] Renamed: ${r} → ${t}`),!0}function Nd(){const e=We();return e.configurations.map(t=>({id:t.id,name:t.name,active:t.id===e.activeConfigId,created:t.metadata.created,modified:t.metadata.modified,locked:t.metadata.locked}))}if(typeof window<"u"){const e=q.ConfigurationManager,t=e&&typeof e=="object"?e:{};q.ConfigurationManager={...t,loadSystemConfigurations:t.loadSystemConfigurations||We,saveSystemConfigurations:t.saveSystemConfigurations||et,getActiveConfiguration:t.getActiveConfiguration||co,getActiveConfigId:t.getActiveConfigId||hd,setActiveConfiguration:t.setActiveConfiguration||bd,saveCurrentToActiveConfiguration:t.saveCurrentToActiveConfiguration||wd,loadActiveConfigurationToTables:t.loadActiveConfigurationToTables||Uo,addConfiguration:t.addConfiguration||vd,deleteConfiguration:t.deleteConfiguration||xd,duplicateConfiguration:t.duplicateConfiguration||Sd,renameConfiguration:t.renameConfiguration||Ad,getConfigurationList:t.getConfigurationList||Nd}}let mn=null,_d=[];function gs(e){if(!e||typeof e!="object")return!1;const t=e?.optimize?.mode;return t==="V"||t===!0}function ys(e){try{const t=String(e?.optimize?.scope??"").trim();if(t==="global"||t==="shared")return"global";if(t==="perConfig"||t==="local"||t==="per-config")return"perConfig"}catch{}return"perConfig"}function hs(e,t,n){try{const o=We();if(!o||!Array.isArray(o.configurations))return;const r=o.activeConfigId,s=o.configurations.findIndex(i=>i&&i.id===r);if(s<0)return;const a=o.configurations[s];if(!a||!Array.isArray(a.blocks))return;const c=a.blocks.find(i=>i&&String(i.blockId??"")===String(e));if(!c)return;(!c.variables||typeof c.variables!="object")&&(c.variables={}),(!c.variables[t]||typeof c.variables[t]!="object")&&(c.variables[t]={value:c.parameters?.[t]??""}),(!c.variables[t].optimize||typeof c.variables[t].optimize!="object")&&(c.variables[t].optimize={}),c.variables[t].optimize.scope=n==="global"?"global":"perConfig";try{et(o)}catch{}}catch{}}function Co(e,t,n,o="perConfig"){try{const r=We();if(!r||!Array.isArray(r.configurations))return;const s=[],a=r.activeConfigId,c=o==="global"?r.configurations||[]:[r.configurations.find(l=>l&&l.id===a)||r.configurations[0]];let i=null;if(n&&o==="global")try{const l=r.configurations.find(u=>u&&u.id===a),f=l&&Array.isArray(l.blocks)?l.blocks.find(u=>u&&String(u.blockId??"")===String(e)):null,p=f?.parameters?.[t]??f?.variables?.[t]?.value,d=typeof p=="number"?p:Number(String(p??"").trim());Number.isFinite(d)&&(i=d)}catch{}for(const l of c){if(!l||!Array.isArray(l.blocks)){s.push({configId:String(l?.id??"(none)"),configName:l?.name});continue}const f=l.blocks.find(p=>p&&String(p.blockId??"")===String(e));if(!f){s.push({configId:String(l?.id??"(none)"),configName:l?.name});continue}if((!f.variables||typeof f.variables!="object")&&(f.variables={}),(!f.variables[t]||typeof f.variables[t]!="object")&&(f.variables[t]={value:f.parameters?.[t]??""}),(!f.variables[t].optimize||typeof f.variables[t].optimize!="object")&&(f.variables[t].optimize={}),f.variables[t].optimize.mode=n?"V":"F",f.variables[t].optimize.scope=o==="global"?"global":"perConfig",i!==null&&o==="global")try{(!f.parameters||typeof f.parameters!="object")&&(f.parameters={}),f.parameters[t]=i,f.variables[t]&&typeof f.variables[t]=="object"&&Object.prototype.hasOwnProperty.call(f.variables[t],"value")&&(f.variables[t].value=i)}catch{}}_d=s.length>0?[{blockId:String(e),key:String(t),scope:String(o),missing:s}]:[];try{et(r)}catch{}}catch{}}function bs(e){try{const t=We();if(!t||!Array.isArray(t.configurations))return{ok:!1,changedCount:0,reason:"no system configurations"};const n=t.activeConfigId,o=t.configurations.find(i=>i&&i.id===n)||null;if(!o||!Array.isArray(o.blocks))return{ok:!1,changedCount:0,reason:"active configuration or blocks not found"};const r=JSON.parse(JSON.stringify(o.blocks)),s=e?"V":"F";let a=0;for(const i of o.blocks){if(!i||typeof i!="object")continue;(!i.variables||typeof i.variables!="object")&&(i.variables={});const l=i.parameters&&typeof i.parameters=="object"?i.parameters:null,f=l?Object.keys(l):[];for(const u of f)(!i.variables[u]||typeof i.variables[u]!="object")&&(i.variables[u]={value:l?l[u]:""}),Object.prototype.hasOwnProperty.call(i.variables[u],"value")===!1&&(i.variables[u].value=l?l[u]:""),(!i.variables[u].optimize||typeof i.variables[u].optimize!="object")&&(i.variables[u].optimize={}),String(i.variables[u].optimize.mode??"").trim()!==s&&a++,i.variables[u].optimize.mode=s,i.variables[u].optimize.scope||(i.variables[u].optimize.scope="perConfig");const p=i.aperture&&typeof i.aperture=="object"?i.aperture:null,d=p?Object.keys(p):[];for(const u of d)(!i.variables[u]||typeof i.variables[u]!="object")&&(i.variables[u]={value:p?p[u]:""}),Object.prototype.hasOwnProperty.call(i.variables[u],"value")===!1&&(i.variables[u].value=p?p[u]:""),(!i.variables[u].optimize||typeof i.variables[u].optimize!="object")&&(i.variables[u].optimize={}),String(i.variables[u].optimize.mode??"").trim()!==s&&a++,i.variables[u].optimize.mode=s,i.variables[u].optimize.scope||(i.variables[u].optimize.scope="perConfig")}if(a<=0)return{ok:!0,changedCount:0};const c=JSON.parse(JSON.stringify(o.blocks));try{if(q.undoHistory&&q.SetDesignIntentOptimizeBulkCommand&&!q.undoHistory.isExecuting){const i=new q.SetDesignIntentOptimizeBulkCommand(String(o.id??n??""),r,c,e);q.undoHistory.record(i)}}catch{}et(t);try{Mt()}catch{}try{typeof q.loadActiveConfigurationToTables=="function"&&q.loadActiveConfigurationToTables()}catch{}try{q.popup3DWindow&&!q.popup3DWindow.closed&&q.popup3DWindow.postMessage({action:"request-redraw"},"*")}catch{}return{ok:!0,changedCount:a}}catch(t){return{ok:!1,changedCount:0,reason:String(t?.message||t)}}}function Fd(e){const t=e&&typeof e=="object"?e:null;if(!t)return"";const n=s=>{const a=t.parameters&&typeof t.parameters=="object"?t.parameters:null,c=a?a[s]:void 0;if(c!=null&&String(c).trim()!=="")return c;const i=t.variables&&typeof t.variables=="object"?t.variables:null,l=i&&i[s]&&typeof i[s]=="object"?i[s].value:void 0;return l!=null&&String(l).trim()!==""?l:""},o=String(t.blockType??""),r=s=>String(s??"").trim().toLowerCase().replace(/\s+/g,"").includes("aspheric");if(o==="Paraxial"){const s=n("focalLengthX")||n("focalLength"),a=n("focalLengthY")||n("focalLength"),c=[];return(String(s)!==""||String(a)!=="")&&(c.push(`Fx=${String(s||a)}`),c.push(`Fy=${String(a||s)}`)),c.join(" ")}if(o==="Lens"||o==="PositiveLens"){const s=n("frontRadius"),a=n("backRadius"),c=n("centerThickness"),i=n("material"),l=n("frontSurfType"),f=n("backSurfType"),p=[];return String(s)!==""&&p.push(`R1=${String(s)}`),String(a)!==""&&p.push(`R2=${String(a)}`),String(c)!==""&&p.push(`CT=${String(c)}`),String(i)!==""&&p.push(`G=${String(i)}`),r(l)&&p.push("Front=Asphere"),r(f)&&p.push("Back=Asphere"),p.join(" ")}if(o==="Doublet"){const s=n("radius1"),a=n("radius2"),c=n("radius3"),i=n("thickness1"),l=n("thickness2"),f=n("material1"),p=n("abbe1")||n("vd1"),d=n("material2"),u=n("abbe2")||n("vd2"),y=n("surf1SurfType"),m=n("surf2SurfType"),h=n("surf3SurfType"),g=[];return String(s)!==""&&g.push(`R1=${String(s)}`),String(a)!==""&&g.push(`R2=${String(a)}`),String(c)!==""&&g.push(`R3=${String(c)}`),String(i)!==""&&g.push(`T1=${String(i)}`),String(l)!==""&&g.push(`T2=${String(l)}`),String(f)!==""&&g.push(`G1=${String(f)}`),String(p)!==""&&g.push(`V1=${String(p)}`),String(d)!==""&&g.push(`G2=${String(d)}`),String(u)!==""&&g.push(`V2=${String(u)}`),r(y)&&g.push("S1=Asphere"),r(m)&&g.push("S2=Asphere"),r(h)&&g.push("S3=Asphere"),g.join(" ")}if(o==="SingleSurface"||o==="Mirror"){const s=n("radius"),a=n("thickness"),c=n("material"),i=n("surfType"),l=n("apertureShape"),f=n("apertureWidth"),p=n("apertureHeight"),d=[];return String(s)!==""&&d.push(`R=${String(s)}`),String(a)!==""&&d.push(`T=${String(a)}`),String(c)!==""&&d.push(`M=${String(c)}`),r(i)&&d.push("Asphere"),String(l)!==""&&String(l)!=="Circular"&&d.push(`Aperture=${String(l)}`),String(f)!==""&&d.push(`AW=${String(f)}`),String(p)!==""&&d.push(`AH=${String(p)}`),d.join(" ")}if(o==="ImageSurface"){const s=n("radius"),a=n("thickness"),c=n("surfType"),i=n("apertureShape"),l=n("apertureWidth"),f=n("apertureHeight"),p=[];return String(s)!==""&&p.push(`R=${String(s)}`),String(a)!==""&&p.push(`T=${String(a)}`),r(c)&&p.push("Asphere"),String(i)!==""&&String(i)!=="Circular"&&p.push(`Aperture=${String(i)}`),String(l)!==""&&p.push(`AW=${String(l)}`),String(f)!==""&&p.push(`AH=${String(f)}`),p.join(" ")}if(o==="Gap"||o==="AirGap"){const s=n("thickness"),a=n("material"),c=[];return String(s)!==""&&c.push(`T=${String(s)}`),String(a)!==""&&String(a).trim().toUpperCase()!=="AIR"&&c.push(`M=${String(a)}`),c.join(" ")}if(o==="Stop"){const s=n("semiDiameter");return String(s)!==""?`SD=${String(s)}`:""}return""}function Md(e,t,n){if(!e||typeof e!="object")return;const o=String(t||"").split(".").filter(Boolean);if(o.length===0)return;let r=e;for(let a=0;a<o.length-1;a++){const c=o[a];(!r[c]||typeof r[c]!="object")&&(r[c]={}),r=r[c]}const s=o[o.length-1];r[s]=n}function Io(e,t){const n=String(e??"").trim();return n===""?"":/^[-+]?((\d+\.\d*)|(\d*\.\d+)|(\d+))(e[-+]?\d+)?$/i.test(n)?Number(n):typeof t=="boolean"?n.toLowerCase()==="true":n}function Rd(e,t=""){if(!Array.isArray(e)||e.length===0)return!1;const n=(()=>{try{if(typeof q.getPrimaryWavelength=="function"){const o=Number(q.getPrimaryWavelength());if(Number.isFinite(o)&&o>0)return o}}catch{}return NaN})();if(!(Number.isFinite(n)&&n>0))return!1;try{const o=at(e),r=o&&Array.isArray(o.rows)?o.rows:[];if(r.length===0)return!1;const s=uo(r,n);if(!s)return!1;let a=!1;for(const c of e){if(!c||typeof c!="object")continue;const i=String(c.blockType??"").trim();if(i!=="Gap"&&i!=="AirGap")continue;const l=c.parameters&&typeof c.parameters=="object"?c.parameters:null;if(!l)continue;const f=String(l.thicknessMode??"").trim().replace(/\s+/g,"").toUpperCase();if(f!=="IMD"&&f!=="BFL")continue;const p=f==="IMD"?s.imageDistance:s.backFocalLength,d=Number(p);if(!Number.isFinite(d))continue;const u=Number(l.thickness);Number.isFinite(u)&&Math.abs(u-d)<=1e-9||(l.thickness=d,c.variables&&typeof c.variables=="object"&&c.variables.thickness&&typeof c.variables.thickness=="object"&&Object.prototype.hasOwnProperty.call(c.variables.thickness,"value")&&(c.variables.thickness.value=d),a=!0)}return a}catch(o){return console.warn("⚠️ [DesignIntent] Failed to auto-apply thicknessMode:",o),!1}}function gt(e,t,n,o){const r=We(),s=r?.configurations?.find(i=>i.id===r?.activeConfigId)||r?.configurations?.[0];if(!s)return;const a=Array.isArray(s.blocks)?s.blocks:[],c=a.find(i=>i&&String(i.blockId??"")===String(e));if(c){if(n!==o)try{if(q.undoHistory&&q.SetBlockParameterCommand&&!q.undoHistory.isExecuting){const i=new q.SetBlockParameterCommand(s.name,String(e),String(t),n,o);q.undoHistory.record(i)}}catch{}Md(c,t,o),Rd(a,t);try{const i=at(a);if(i&&Array.isArray(i.rows)){s.opticalSystem=i.rows;try{jn(i.rows)}catch{}try{typeof q.saveLensTableData=="function"&&q.saveLensTableData(i.rows)}catch{}try{const l=q.tableOpticalSystem||q.opticalSystemTabulator;l&&typeof l.replaceData=="function"?l.replaceData(i.rows):l&&typeof l.setData=="function"&&l.setData(i.rows)}catch{}}}catch{}try{s.metadata&&(s.metadata.modified=new Date().toISOString())}catch{}try{et(r)}catch{}try{Mt()}catch{}try{typeof q.loadActiveConfigurationToTables=="function"&&q.loadActiveConfigurationToTables()}catch{}try{const i=q.popup3DWindow;i&&!i.closed&&typeof i.postMessage=="function"&&i.postMessage({action:"request-redraw"},"*")}catch{}}}function ws(e,t,n=null,o=null){const r=document.getElementById("block-inspector");if(!r)return;r.innerHTML="";try{if(Array.isArray(q.__blocks_lastScopeErrors)&&q.__blocks_lastScopeErrors.length>0){const y=q.__blocks_lastScopeErrors[0],m=Array.isArray(y?.missing)?y.missing:[],h=m.slice(0,6).map(w=>w?.configName?`${String(w.configName)}(${String(w.configId)})`:String(w?.configId??"")).filter(Boolean),g=document.createElement("div");g.style.padding="8px 10px",g.style.margin="6px 0 10px 0",g.style.border="1px solid #f2c2c2",g.style.background="#fff5f5",g.style.color="#8a1f1f",g.style.borderRadius="6px",g.style.fontSize="12px",g.textContent=`ERROR: Cannot apply "Shared (all configs)" because this Block is missing in some configurations: ${String(y?.blockId??"")}.${String(y?.key??"")} / missing in ${m.length} config(s): ${h.join(", ")}${m.length>h.length?", ...":""}`,r.appendChild(g)}}catch{}const s=Array.isArray(e)?e:[];if(s.length===0){const y=document.createElement("div");y.style.padding="8px",y.style.fontSize="13px",y.style.color="#666",y.textContent="No blocks (or no provenance).",r.appendChild(y);return}const a=y=>{if(!y||typeof y!="object")return!1;const m=String(y?._blockType??y?.blockType??"").trim().toLowerCase(),h=String(y?._surfaceRole??y?.surfaceRole??"").trim().toLowerCase();return!(m==="gap"||m==="airgap"||m==="air gap"||m==="coordtrans"||m==="coord trans"||m==="objectsurface"||m==="objectplane"||m==="object"||(m==="paraxial"||m==="thinlens")&&h==="back")},c=y=>{const m=String(y?.blockId??"").trim(),h=m?i.get(m):null;if(h&&Number.isFinite(h.min)&&Number.isFinite(h.max))return Math.max(0,h.max-h.min+1);const g=String(y?.blockType??"").trim();if(g==="Paraxial"||g==="ThinLens")return 1;const w=Number(y?.surfaceCount??0);return Number.isFinite(w)?w:0},i=new Map;try{if(Array.isArray(o)&&o.length>0&&typeof q.expandBlocksToOpticalSystemRows=="function"){const y=q.expandBlocksToOpticalSystemRows(o),m=y&&Array.isArray(y.rows)?y.rows:[];let h=0;for(let g=0;g<m.length;g++){const w=m[g],R=String(w?._blockId??"").trim();if(!R||!a(w))continue;h+=1;const N=i.get(R);N?(h<N.min&&(N.min=h),h>N.max&&(N.max=h)):i.set(R,{min:h,max:h})}}}catch{}if(i.size===0)try{let y=0;for(const m of s){const h=String(m?.blockId??"").trim();if(!h)continue;const g=String(m?.blockType??"").trim();if(g==="ObjectSurface"||g==="ObjectPlane"||g==="Object")continue;const w=c(m);if(!Number.isFinite(w)||w<=0)continue;const R=y+1,N=y+w;y=N,i.set(h,{min:R,max:N})}}catch{}const l=(y,m)=>{const h=String(y??"").trim(),g=String(m??"").trim();if(h==="ObjectSurface"||h==="ObjectPlane"||h==="ImageSurface")return h==="ObjectPlane"?"ObjectSurface":h;if(/^ObjectPlane-(\d+)$/i.exec(g))return"ObjectSurface";const R=/^(ObjectSurface|ImageSurface)-(\d+)$/i.exec(g);return R?R[1]:g||"(none)"},f=new Map;try{const y=new Map,m=Array.isArray(o)?o:[];for(const h of m){if(!h||typeof h!="object")continue;const g=String(h.blockId??"").trim();if(!g)continue;const w=String(h.blockType??"").trim();if(!w)continue;if(w==="ObjectSurface"||w==="ObjectPlane"||w==="ImageSurface"){const I=w==="ObjectPlane"?"ObjectSurface":w;f.set(g,I);continue}const R=w==="PositiveLens"?"Lens":w,N=(y.get(R)||0)+1;y.set(R,N),f.set(g,`${R}-${N}`)}}catch{}const p=new Set(["lens","doublet","triplet"]),d=y=>{const m=String(y?.blockId??"(none)"),h=f.get(m)||l(y?.blockType,m),g=String(y?.blockType??"").trim();if(g==="ObjectSurface"||g==="ObjectPlane")return`${h} → Surf 0`;const w=i.get(String(y?.blockId??"").trim());if(w&&Number.isFinite(w.min)&&Number.isFinite(w.max)){const R=w.min===w.max?`Surf ${w.min}`:`Surf ${w.min}–${w.max}`;return`${h} → ${R}`}return h},u=(()=>{let y=0;for(const m of s){const h=String(m?.blockType??"").trim(),g=(()=>{const w=h==="ObjectPlane"?"ObjectSurface":h;return w==="PositiveLens"||w==="Paraxial"?"lens":w==="AirGap"?"gap":String(w||"unknown").trim().toLowerCase()})();p.has(g)&&(y=Math.max(y,d(m).length))}return Math.max(14,y+1)})();for(const y of s){const m=String(y.blockId??"").trim(),h=document.createElement("div");h.className="block-inspector-row",m&&mn===m&&h.classList.add("selected");const g=document.createElement("div");g.className="block-inspector-col-id",g.textContent=d(y);const w=String(y.blockType??"").trim(),R=w==="ObjectPlane"?"ObjectSurface":String(y.blockType??"(none)"),N=(()=>{const S=w==="ObjectPlane"?"ObjectSurface":w;return S==="PositiveLens"||S==="Paraxial"?"lens":S==="AirGap"?"gap":String(S||"unknown").trim().toLowerCase()})();if(h.dataset.blockType=N,g.dataset.blockType=N,g.title=R,p.has(N)){const S=`${u}ch`;g.style.width=S,g.style.minWidth=S,g.style.maxWidth=S,g.style.flex=`0 0 ${S}`}const I=document.createElement("div");I.className="block-inspector-col-params",I.textContent=String(y.preview??"");const b=document.createElement("div");b.className="block-inspector-col-count";const A=c(y);b.textContent=`→ ${Number.isFinite(A)?A:0} surfaces`,h.appendChild(g),h.appendChild(I),h.appendChild(b),h.onclick=()=>{if(m){mn=mn===m?null:m;try{Mt()}catch{}}},r.appendChild(h);const v=n&&typeof n.get=="function"?n.get(m):null;if(v&&mn===m){const S=document.createElement("div");S.className="block-inspector-expanded-panel",S.style.padding="8px 10px",S.style.margin="0";const _=document.body.classList.contains("dark-mode");S.style.borderTop=_?"1px solid #333":"1px solid #eee",S.style.fontSize="12px",S.style.color=_?"#ffffff":"#333",S.dataset.blockId=String(m),S.setAttribute("data-block-id",String(m));const M=v.parameters&&typeof v.parameters=="object"?v.parameters:{},G=v.variables&&typeof v.variables=="object"?v.variables:{},z=F=>F.sort((x,T)=>{const j=x.toLowerCase(),P=T.toLowerCase();if(E==="CoordTrans"){const U=ae=>{const Ae=ae.toLowerCase();return Ae==="decenterx"?0:Ae==="decentery"?1:Ae==="decenterz"?2:Ae==="tiltx"?3:Ae==="tilty"?4:Ae==="tiltz"?5:Ae==="order"?6:Ae==="coordreturn"?7:Ae==="tosurf"?8:100},ee=U(x),oe=U(T);if(ee!==100||oe!==100)return ee-oe}const O=U=>{switch(U){case"material1":return 0;case"abbe1":case"vd1":return 1;case"material2":return 2;case"abbe2":case"vd2":return 3;default:return 100}},X=O(x),k=O(T);if(X!==k)return X-k;if(x==="material1")return-1;if(T==="material1")return 1;if(x==="material2"&&T!=="material1")return-1;if(T==="material2"&&x!=="material1")return 1;if(j.includes("material")&&!P.includes("material"))return-1;if(P.includes("material")&&!j.includes("material"))return 1;if(x==="abbe"||x==="vd")return-1;if(T==="abbe"||T==="vd")return 1;if(j.startsWith("front")&&!P.startsWith("front"))return-1;if(!j.startsWith("front")&&P.startsWith("front"))return 1;if(j.startsWith("front")&&P.startsWith("front")){const U=j.includes("surf"),ee=P.includes("surf"),oe=j.includes("radius"),ae=P.includes("radius"),Ae=j.includes("conic"),te=P.includes("conic"),re=j.includes("coef"),se=P.includes("coef");if(U&&!ee)return-1;if(!U&&ee)return 1;if(oe&&!ae)return-1;if(!oe&&ae)return 1;if(Ae&&!te)return-1;if(!Ae&&te)return 1;if(re&&se){const ue=x.match(/\d+/),me=T.match(/\d+/);if(ue&&me)return parseInt(ue[0])-parseInt(me[0])}}if(j.startsWith("back")&&!P.startsWith("back"))return-1;if(!j.startsWith("back")&&P.startsWith("back"))return 1;if(j.startsWith("back")&&P.startsWith("back")){const U=j.includes("surf"),ee=P.includes("surf"),oe=j.includes("radius"),ae=P.includes("radius"),Ae=j.includes("conic"),te=P.includes("conic"),re=j.includes("coef"),se=P.includes("coef");if(U&&!ee)return-1;if(!U&&ee)return 1;if(oe&&!ae)return-1;if(!oe&&ae)return 1;if(Ae&&!te)return-1;if(!Ae&&te)return 1;if(re&&se){const ue=x.match(/\d+/),me=T.match(/\d+/);if(ue&&me)return parseInt(ue[0])-parseInt(me[0])}}if(j.includes("radius")&&!j.startsWith("front")&&!j.startsWith("back")&&!P.includes("radius"))return-1;if(P.includes("radius")&&!P.startsWith("front")&&!P.startsWith("back")&&!j.includes("radius"))return 1;if(j.includes("conic")&&!j.startsWith("front")&&!j.startsWith("back")&&!P.includes("conic"))return-1;if(P.includes("conic")&&!P.startsWith("front")&&!P.startsWith("back")&&!j.includes("conic"))return 1;if(j.includes("thickness")&&!P.includes("thickness"))return-1;if(!j.includes("thickness")&&P.includes("thickness"))return 1;if(x==="apertureShape"&&T!=="apertureShape")return-1;if(T==="apertureShape"&&x!=="apertureShape")return 1;if(x==="apertureWidth"&&T!=="apertureWidth"&&T!=="apertureShape")return-1;if(T==="apertureWidth"&&x!=="apertureWidth"&&x!=="apertureShape")return 1;if(x==="apertureHeight"&&T!=="apertureHeight"&&T!=="apertureShape"&&T!=="apertureWidth")return-1;if(T==="apertureHeight"&&x!=="apertureHeight"&&x!=="apertureShape"&&x!=="apertureWidth")return 1;if(j.includes("semidia")&&!P.includes("semidia"))return-1;if(!j.includes("semidia")&&P.includes("semidia"))return 1;const Y=j.includes("coef"),D=P.includes("coef");if(Y&&D){const U=x.match(/\d+/),ee=T.match(/\d+/);if(U&&ee)return parseInt(U[0])-parseInt(ee[0])}return Y&&!D?1:!Y&&D?-1:x.localeCompare(T)}),E=String(v.blockType||v.type||"unknown"),L=Object.keys(M||{}).filter(F=>{const x=F.toLowerCase();return!(x==="chiefrayshiftx"||x==="chiefrayshifty"||x==="chiefrayshiftz"||E==="Paraxial"&&(x==="material"||x==="abbe"||x==="vd"||x==="nd"||x==="rindex"||x==="bending"||x==="frontradius"||x==="backradius"||x==="centerthickness"||x==="radiusx"||x==="focallength"||x==="conic"||x==="axis"||/^coef\d+$/.test(x)||x==="backsurftype"||x==="backconic"||/^backcoef\d+$/.test(x)||x==="frontsurftype"||x==="frontconic"||/^frontcoef\d+$/.test(x)))});if((E==="Gap"||E==="AirGap")&&!L.includes("material")&&L.push("material"),(E==="Gap"||E==="AirGap")&&!L.includes("thicknessMode")&&L.push("thicknessMode"),(E==="ObjectSurface"||E==="ObjectPlane")&&!L.includes("objectDistance")&&L.push("objectDistance"),E==="ImageSurface"){L.includes("semidiaMode")||L.push("semidiaMode"),L.includes("apertureShape")||L.push("apertureShape"),L.includes("apertureWidth")||L.push("apertureWidth"),L.includes("apertureHeight")||L.push("apertureHeight"),L.includes("radius")||L.push("radius"),L.includes("thickness")||L.push("thickness"),L.includes("surfType")||L.push("surfType"),L.includes("conic")||L.push("conic");for(let F=1;F<=10;F++){const x=`coef${F}`;L.includes(x)||L.push(x)}}if(E==="Paraxial"&&(L.includes("surfType")||L.push("surfType"),L.includes("focalLengthX")||L.push("focalLengthX"),L.includes("focalLengthY")||L.push("focalLengthY")),E==="Lens"||E==="PositiveLens"||E==="SingleSurface"||E==="Mirror"){L.includes("frontSurfType")||L.push("frontSurfType"),L.includes("backSurfType")||L.push("backSurfType");for(let F=1;F<=10;F++){const x=`frontCoef${F}`,T=`backCoef${F}`;L.includes(x)||L.push(x),L.includes(T)||L.push(T)}}const ne=z(L),V=Object.keys(G||{}).sort(),K=F=>String(F??"").trim().toLowerCase().replace(/\s+/g,""),$=F=>{const x=String(F).toLowerCase();return x.startsWith("frontcoef")?M.frontSurfType:x.startsWith("backcoef")?M.backSurfType:x.startsWith("surf1coef")?M.surf1SurfType:x.startsWith("surf2coef")?M.surf2SurfType:x.startsWith("surf3coef")?M.surf3SurfType:M.surfType},J=F=>{const x=String(F).match(/coef(\d+)/i);if(!x)return null;const T=parseInt(x[1],10);if(!Number.isFinite(T)||T<=0)return null;const j=$(F),P=K(j),O=P==="asphericeven"||P==="asphericaleven"||P==="aspheric-even"||P==="aspherical-even";if(!O&&!(P==="asphericodd"||P==="asphericalodd"||P==="aspheric-odd"||P==="aspherical-odd"))return null;const k=O?2*T+2:2*T+1,Y=String(F).toLowerCase();let D="";return Y.startsWith("frontcoef")?D="s1 ":Y.startsWith("backcoef")?D="s2 ":Y.startsWith("surf1coef")?D="s1 ":Y.startsWith("surf2coef")?D="s2 ":Y.startsWith("surf3coef")?D="s3 ":Y.startsWith("surf4coef")&&(D="s4 "),`${D}A${k}`.trim()},Q=F=>{const x=String(F??"").trim();if(E==="Paraxial"){if(x==="surfType"||x==="frontSurfType")return"X/Y Power";if(x==="focalLengthX")return"Focal Length X";if(x==="focalLengthY"||x==="focalLength")return"Focal Length Y"}return x==="frontSurfType"?"s1 Surf Type":x==="backSurfType"?"s2 Surf Type":x==="surf1SurfType"?"s1 Surf Type":x==="surf2SurfType"?"s2 Surf Type":x==="surf3SurfType"?"s3 Surf Type":x==="surf4SurfType"?"s4 Surf Type":x==="frontConic"?"s1 Conic":x==="backConic"?"s2 Conic":x==="surf1Conic"?"s1 Conic":x==="surf2Conic"?"s2 Conic":x==="surf3Conic"?"s3 Conic":x==="surf4Conic"?"s4 Conic":x},le=F=>{const x=document.createElement("div");return x.className="block-inspector-section-title",x.textContent=F,x.style.fontWeight="600",x.style.margin="6px 0",x.style.fontSize="12px",x},Z=(F,x,T,j,P)=>{const O=document.createElement("div");O.className="block-inspector-detail-row",O.style.display="flex",O.style.gap="8px",O.style.alignItems="center",O.style.minHeight="32px",O.style.marginBottom="4px";const X=document.createElement("div"),k=J(F),Y=Q(F);X.textContent=k||Y,X.title=k||Y,X.style.fontSize="12px",X.style.color=_?"#d1d5db":"#374151",X.style.flex="0 0 140px",X.style.whiteSpace="nowrap",X.style.overflow="hidden",X.style.textOverflow="ellipsis",X.style.lineHeight="1.2";const D=F==="surfType"||F==="frontSurfType"||F==="backSurfType"||F==="surf1SurfType"||F==="surf2SurfType"||F==="surf3SurfType",U=F.toLowerCase().includes("material")||P==="material",ee=(E==="Gap"||E==="AirGap")&&F==="thicknessMode",oe=(E==="ObjectSurface"||E==="ObjectPlane")&&F==="objectDistanceMode",ae=E==="ImageSurface"&&F==="semidiaMode",Ae=(E==="Mirror"||E==="SingleSurface"||E==="ImageSurface")&&F==="apertureShape",te=E==="CoordTrans"&&F==="coordReturn",re=E==="CoordTrans"&&F==="order",se=E==="CoordTrans"&&F==="toSurf",me=!U&&!D&&!(F==="nd"||F==="vd"||F==="abbe")&&!ee&&!oe&&!ae&&!Ae&&!te&&!re&&!se&&!isNaN(parseFloat(String(x)));let ge;if(D){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=E==="Paraxial"&&F==="surfType"?[{value:"Toric",label:"X/Y power"}]:[{value:"Spherical",label:"Spherical"},{value:"Aspherical even",label:"Aspherical even"},{value:"Aspherical odd",label:"Aspherical odd"},{value:"Toric",label:"Astigmatic (X/Y power)"}],we=String(x||"Spherical");fe.forEach(({value:Se,label:ye})=>{const ce=document.createElement("option");ce.value=Se,ce.textContent=ye,Se===we&&(ce.selected=!0),W.appendChild(ce)}),W.addEventListener("change",()=>{const Se=W.value;Se!==x&&gt(m,T,x,Se)}),ge=W}else if(ee){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=String(x??"").trim().replace(/\s+/g,"").toUpperCase(),we=fe==="IMD"||fe==="BFL"?fe:"";[{value:"",label:"Manual"},{value:"IMD",label:"Image distance (IMD)"},{value:"BFL",label:"Back focal length (BFL)"}].forEach(({value:ce,label:pe})=>{const he=document.createElement("option");he.value=ce,he.textContent=pe,ce===we&&(he.selected=!0),W.appendChild(he)});const ye=ce=>{if(!(ce!=="IMD"&&ce!=="BFL"))try{const pe=Array.isArray(o)&&o.length>0?o:(()=>{const de=We(),be=de?.configurations?.find(Pe=>Pe.id===de?.activeConfigId)||de?.configurations?.[0];return Array.isArray(be?.blocks)?be.blocks:[]})(),he=at(pe),_e=he&&Array.isArray(he.rows)?he.rows:[];if(_e.length===0)return;const ve=(()=>{try{if(typeof q.getPrimaryWavelength=="function"){const de=Number(q.getPrimaryWavelength());if(Number.isFinite(de)&&de>0)return de}}catch{}return NaN})();if(!(Number.isFinite(ve)&&ve>0)){console.warn("⚠️ [DesignIntent] Primary wavelength is unavailable. thicknessMode auto-apply is skipped.");return}const Ce=uo(_e,ve),Te=ce==="IMD"?Ce?.imageDistance:Ce?.backFocalLength,Ie=Number(Te);if(Number.isFinite(Ie)){const de=M?.thickness;gt(m,"parameters.thickness",de,Ie)}}catch(pe){console.warn("⚠️ [DesignIntent] Failed to apply thicknessMode:",pe)}};W.addEventListener("change",()=>{const ce=W.value;ce!==x&&(gt(m,T,x,ce),ye(ce))}),ge=W}else if(oe){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=String(x??"").trim(),Se=rn(fe)||fe.toUpperCase()==="INFINITY"?"INF":"Finite";[{value:"INF",label:"Infinity"},{value:"Finite",label:"Finite"}].forEach(({value:ce,label:pe})=>{const he=document.createElement("option");he.value=ce,he.textContent=pe,ce===Se&&(he.selected=!0),W.appendChild(he)}),W.addEventListener("change",()=>{const ce=W.value;if(ce!==x&&(gt(m,T,x,ce),ce==="Finite")){const pe=M?.objectDistance,he=Number(pe);Number.isFinite(he)||gt(m,"parameters.objectDistance",pe,10)}}),ge=W}else if(ae){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const we=String(x??"").trim().toLowerCase()==="auto"?"Auto":"Manual";["Manual","Auto"].forEach(ye=>{const ce=document.createElement("option");ce.value=ye,ce.textContent=ye,ye===we&&(ce.selected=!0),W.appendChild(ce)}),W.addEventListener("change",()=>{const ye=W.value;if(ye!==x){gt(m,T,x,ye);const ce=M?M.optimizeSemiDia:void 0,pe=ye==="Auto"?"A":"";ce!==pe&&gt(m,"parameters.optimizeSemiDia",ce,pe),ye==="Auto"&&setTimeout(()=>{try{typeof q.calculateImageSemiDiaFromChiefRays=="function"&&Promise.resolve(q.calculateImageSemiDiaFromChiefRays()).then(()=>{try{Mt()}catch{}}).catch(he=>{console.warn("⚠️ [DesignIntent] semidiaMode Auto recalculation failed:",he)})}catch(he){console.warn("⚠️ [DesignIntent] semidiaMode Auto trigger failed:",he)}},0)}}),ge=W}else if(te){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=String(x??"").trim().toLowerCase(),we=fe==="xyz"||fe==="none"?fe:"none";[{value:"xyz",label:"On"},{value:"none",label:"Off"}].forEach(({value:ye,label:ce})=>{const pe=document.createElement("option");pe.value=ye,pe.textContent=ce,ye===we&&(pe.selected=!0),W.appendChild(pe)}),W.addEventListener("change",async()=>{const ye=W.value;if(ye!==x&&(gt(m,T,x,ye),ye==="xyz")){const ce=Number(M?.order??0);ce!==1&&gt(m,"parameters.order",ce,1);try{await ps(m,S)}catch(pe){console.error("[CoordTrans] Auto calculation on ON failed:",pe)}}}),ge=W}else if(re){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=Number(x??1),we=fe===0||fe===1?fe:1;[{value:"0",label:"Tilt → Decenter"},{value:"1",label:"Decenter → Tilt"}].forEach(({value:ye,label:ce})=>{const pe=document.createElement("option");pe.value=ye,pe.textContent=ce,parseInt(ye)===we&&(pe.selected=!0),W.appendChild(pe)}),W.addEventListener("change",()=>{const ye=parseInt(W.value),ce=Number(x??1);ye!==ce&&gt(m,T,ce,ye)}),ge=W}else if(se){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=[];try{if(Array.isArray(o)&&o.length>0&&typeof at=="function"){const ce=at(o),pe=ce&&Array.isArray(ce.rows)?ce.rows:[];let he=0;const _e=new Map;for(let ve=0;ve<pe.length;ve++){const Ce=pe[ve],Te=String(Ce?._blockType??Ce?.type??"").trim();if(Te==="Gap"||Te==="AirGap"||Te==="CoordTrans"||Te==="ObjectSurface"||Te==="ObjectPlane"||Te==="Object")continue;he+=1;const Ie=String(Ce?._blockId??"").trim(),de=(_e.get(Ie)||0)+1;_e.set(Ie,de);const be=f.get(Ie)||Ie||"Surface";fe.push({value:String(he),label:`${he}: ${be} S${de}`})}}}catch{}const we=Number(x),Se=Number.isFinite(we)&&fe.some(ce=>Number(ce.value)===we);!Se&&Number.isFinite(we)&&fe.unshift({value:String(we),label:`${we}: (current)`}),fe.length===0&&fe.push({value:String(Number.isFinite(we)?we:1),label:"1: Surface 1"});const ye=String(Se||Number.isFinite(we)?we:Number(fe[0].value));fe.forEach(({value:ce,label:pe})=>{const he=document.createElement("option");he.value=ce,he.textContent=pe,ce===ye&&(he.selected=!0),W.appendChild(he)}),W.addEventListener("change",async()=>{const ce=Number(W.value),pe=Number(x);if(Number.isFinite(ce)&&ce!==pe&&(gt(m,T,pe,ce),String(M?.coordReturn??"").trim().toLowerCase()==="xyz"))try{await ps(m,S)}catch(_e){console.error("[CoordTrans] Auto calculation on toSurf change failed:",_e)}}),ge=W}else if(Ae){const W=document.createElement("select");W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.cursor="pointer",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box";const fe=String(x??"").trim(),Se=(ce=>{const pe=ce.replace(/\s+/g,"").replace(/[_-]+/g,"").toLowerCase();return pe==="circle"||pe==="circular"?"Circular":pe==="square"||pe==="sq"?"Square":pe==="rect"||pe==="rectangle"||pe==="rectangular"?"Rectangular":"Circular"})(fe)||"Circular";["Circular","Square"].forEach(ce=>{const pe=document.createElement("option");pe.value=ce,pe.textContent=ce,ce===Se&&(pe.selected=!0),W.appendChild(pe)}),W.addEventListener("change",()=>{const ce=W.value;ce!==Se&&gt(m,T,x,ce)}),ge=W}else if(me){const W=document.createElement("div");W.className="param-input-with-slider",W.style.display="grid",W.style.gridTemplateColumns="120px 40px 40px 40px 140px 220px",W.style.columnGap="6px",W.style.alignItems="center",W.style.flex="1";const fe=parseFloat(String(x)),we=Number.isFinite(fe);let Se=us(F,E,x),{min:ye,max:ce,step:pe}=Se,he=!1,_e=1;const ve=document.createElement("input");ve.type="text",ve.value=x==null?"":String(x),ve.style.fontSize="12px",ve.style.padding="4px 6px",ve.style.border=_?"1px solid #444":"1px solid #ddd",ve.style.background=_?"#111827":"#fff",ve.style.color=_?"#f9fafb":"#111827",ve.style.borderRadius="4px",ve.style.boxSizing="border-box",ve.style.height="28px";const Ce=document.createElement("button");Ce.type="button",Ce.className="scale-mode-btn",Ce.textContent="Lin",Ce.title="Toggle linear/logarithmic scale",Ce.style.fontSize="10px",Ce.style.padding="2px",Ce.style.boxSizing="border-box",Ce.style.height="28px";const Te=document.createElement("button");Te.type="button",Te.className="magnitude-btn",Te.textContent="×0.1",Te.title="Decrease range by 10x",Te.style.fontSize="9px",Te.style.padding="2px",Te.style.boxSizing="border-box",Te.style.height="28px";const Ie=document.createElement("button");Ie.type="button",Ie.className="magnitude-btn",Ie.textContent="×10",Ie.title="Increase range by 10x",Ie.style.fontSize="9px",Ie.style.padding="2px",Ie.style.boxSizing="border-box",Ie.style.height="28px";const de=document.createElement("div");de.className="slider-range-display",de.style.fontSize="9px",de.style.color="#666",de.style.whiteSpace="nowrap",de.style.overflow="hidden",de.style.textOverflow="ellipsis",de.style.fontFamily="monospace",de.title="Slider range (min ~ max)";const be=document.createElement("input");be.type="range",be.min="0",be.max="1",be.step="0.001",be.value=we?String(Fr(fe,ye,ce,he)):"0.5";const Pe=(Me,ze)=>{const Fe=Number(Me);if(!Number.isFinite(Fe))return String(Me??"");const $e=Math.abs(Fe);return $e>0&&$e<1e-6?Fe.toExponential(2):Fe.toFixed(ze)},Ee=()=>{const Me=Math.max(2,Math.min(6,-Math.floor(Math.log10(Math.abs(ce-ye)/100))));de.textContent=`[${Pe(ye,Me)} ~ ${Pe(ce,Me)}]`};Ee();const Ue=()=>{const Me=us(F,E,x),ze=(Me.min+Me.max)/2,$e=(Me.max-Me.min)/2*_e;ye=ze-$e,ce=ze+$e,pe=Me.step*_e,(F.includes("Thickness")||F.includes("hickness")||F.includes("semidia")||F.includes("aperture"))&&ye<0&&(ye=0,ce=ze*2),Ge(),Ee()},Ge=()=>{const Me=parseFloat(ve.value);Number.isFinite(Me)&&(be.value=String(Fr(Me,ye,ce,he)))};Ce.addEventListener("click",Me=>{Me.stopPropagation(),he=!he,Ce.textContent=he?"Log":"Lin",Ce.style.background=he?"#007acc":"",Ce.style.color=he?"white":"",Ge(),Ee()}),Te.addEventListener("click",Me=>{Me.stopPropagation(),_e*=.1,Ue()}),Ie.addEventListener("click",Me=>{Me.stopPropagation(),_e*=10,Ue()}),be.addEventListener("input",Me=>{Me.stopPropagation();const ze=parseFloat(be.value),Fe=ls(ze,ye,ce,he),$e=cs(Fe,ce-ye);ve.value=Fe.toFixed($e)}),be.addEventListener("change",Me=>{Me.stopPropagation();const ze=parseFloat(be.value),Fe=ls(ze,ye,ce,he),$e=cs(Fe,ce-ye),Qe=Fe.toFixed($e);ve.value=Qe,gt(m,T,x,Qe)});const He=()=>{const Me=ve.value,ze=parseFloat(Me);Number.isFinite(ze)&&(be.value=String(Fr(ze,ye,ce,he))),gt(m,T,x,Me)};ve.addEventListener("keydown",Me=>{Me.key==="Enter"&&(Me.preventDefault(),Me.stopPropagation(),He())}),ve.addEventListener("blur",()=>{He()}),W.appendChild(ve),W.appendChild(Ce),W.appendChild(Te),W.appendChild(Ie),W.appendChild(de),W.appendChild(be),ge=W}else if(U){const W=document.createElement("div");W.style.display="flex",W.style.alignItems="center",W.style.gap="8px",W.style.flex="1",W.style.flexWrap="nowrap",W.style.minHeight="28px";const fe=document.createElement("input");fe.type="text",fe.value=x==null?"":String(x),fe.style.fontSize="12px",fe.style.padding="4px 6px",fe.style.border=_?"1px solid #444":"1px solid #ddd",fe.style.background=_?"#111827":"#fff",fe.style.color=_?"#f9fafb":"#111827",fe.style.borderRadius="4px",fe.style.flex="1",fe.style.minWidth="200px",fe.style.height="28px",fe.style.boxSizing="border-box";const we=document.createElement("button");we.type="button",we.className="block-inspector-icon-btn",we.textContent="🔍",we.title="Find Glass",we.style.fontSize="14px",we.style.padding="0 8px",we.style.boxSizing="border-box",we.style.height="28px";const Se=document.createElement("button");Se.type="button",Se.className="block-inspector-icon-btn",Se.textContent="🗺️",Se.title="Open Glass Map",Se.style.fontSize="14px",Se.style.padding="0 8px",Se.style.boxSizing="border-box",Se.style.height="28px",Se.onclick=pe=>{pe.preventDefault(),pe.stopPropagation(),typeof as=="function"&&as(he=>{console.log("Region selected:",he)},he=>{if(he&&he.name){fe.value=he.name;const _e=Io(he.name,x);_e!==x&&gt(m,T,x,_e);const ve=T.replace(/material/i,"abbe");if(ve!==T&&Number.isFinite(he.vd))try{gt(m,ve,void 0,String(he.vd))}catch{}return!0}return!1})},we.onclick=pe=>{pe.preventDefault(),pe.stopPropagation();const he=fe.value.trim(),_e=Me=>{const Fe=String(Me||"").trim().toLowerCase().match(/^material(\d+)$/);return Fe&&Fe[1]?`abbe${Fe[1]}`:"abbe"},ve=Me=>{const Fe=String(Me||"").trim().toLowerCase().match(/^material(\d+)$/);return Fe&&Fe[1]?`vd${Fe[1]}`:"vd"},Ce=()=>{const Me=M&&typeof M=="object"?M:null;if(!Me)return null;const ze=String(F||"").trim(),Fe=_e(ze),$e=ve(ze),Qe=parseFloat(String(Me[Fe]));if(Number.isFinite(Qe)&&Qe>0)return Qe;const pt=parseFloat(String(Me[$e]));return Number.isFinite(pt)&&pt>0?pt:null},Te=Me=>{const ze=String(Me||"").trim();if(!ze||!/^[+-]?(?:\d+\.?\d*|\.\d+)$/.test(ze))return null;const Fe=Number(ze);return!Number.isFinite(Fe)||Fe<=0||Fe>=4?null:Fe};let Ie=[];const de=Te(he);if(de!==null)try{const Me=Ce();if(Me===null){alert("Abbe (Vd) value is required for numeric material search.");return}Ie=Pi(de,Me,20),console.log("✅ Found",Ie.length,"glasses with similar nd/vd to",de,Me)}catch(Me){console.error("❌ Failed to find glasses by numeric nd/abbe:",Me)}else{let Me=null,ze=null;if(he)try{const Fe=wl(he);if(Fe&&Fe.nd!==void 0&&Fe.vd!==void 0)Me=Fe.nd,ze=Fe.vd,console.log("✅ Found glass properties - nd:",Me,"vd:",ze);else{alert("Current material does not have valid nd/vd in the glass database.");return}}catch(Fe){console.warn("❌ Failed to get glass data:",Fe),alert("Failed to resolve nd/vd from current material.");return}else{alert("Enter a material name or numeric nd value first.");return}if(!Number.isFinite(Me)||!Number.isFinite(ze)){alert("Valid nd/vd values are required to search similar glasses.");return}console.log("🔍 Searching for glasses similar to nd:",Me,"vd:",ze);try{Ie=Pi(Me,ze,20),console.log("✅ Found",Ie.length,"similar glasses")}catch(Fe){console.error("❌ Failed to find similar glasses:",Fe)}}if(Ie.length===0){alert("No glasses found in database.");return}const be=document.documentElement.classList.contains("dark"),Pe=document.createElement("div");Pe.style.position="fixed",Pe.style.top="0",Pe.style.left="0",Pe.style.right="0",Pe.style.bottom="0",Pe.style.background="rgba(0,0,0,0.6)",Pe.style.display="flex",Pe.style.alignItems="center",Pe.style.justifyContent="center",Pe.style.zIndex="9999";const Ee=document.createElement("div");Ee.style.background=be?"#1f2937":"#fff",Ee.style.borderRadius="8px",Ee.style.padding="20px",Ee.style.maxWidth="600px",Ee.style.maxHeight="80vh",Ee.style.overflow="auto",Ee.style.boxShadow="0 4px 20px rgba(0,0,0,0.3)";const Ue=document.createElement("h3");Ue.textContent="🔍 Select Glass Material",Ue.style.margin="0 0 15px 0",Ue.style.color=be?"#f9fafb":"#111827";const Ge=document.createElement("div");Ge.style.display="flex",Ge.style.flexDirection="column",Ge.style.gap="4px",Ge.style.marginBottom="15px",Ie.slice(0,15).forEach((Me,ze)=>{const Fe=document.createElement("div");Fe.style.padding="8px 12px",Fe.style.cursor="pointer",Fe.style.borderRadius="4px",Fe.style.background=be?"#374151":"#f3f4f6",Fe.style.transition="background 0.15s",Fe.textContent=`${ze+1}. ${Me.name} [${Me.manufacturer}] (nd=${Me.nd.toFixed(4)}, vd=${Me.vd.toFixed(1)})`,Fe.style.fontSize="13px",Fe.style.color=be?"#f9fafb":"#111827",Fe.onmouseenter=()=>{Fe.style.background=be?"#4b5563":"#e5e7eb"},Fe.onmouseleave=()=>{Fe.style.background=be?"#374151":"#f3f4f6"},Fe.onclick=()=>{fe.value=Me.name;const $e=Io(Me.name,x);$e!==x&&gt(m,T,x,$e);const Qe=T.replace(/material/i,"abbe");if(Qe!==T&&Me.vd!==void 0)try{gt(m,Qe,void 0,String(Me.vd))}catch{}document.body.removeChild(Pe)},Ge.appendChild(Fe)});const He=document.createElement("button");He.textContent="Cancel",He.style.padding="6px 16px",He.style.border="none",He.style.borderRadius="4px",He.style.background=be?"#4b5563":"#d1d5db",He.style.color=be?"#f9fafb":"#111827",He.style.cursor="pointer",He.onclick=()=>document.body.removeChild(Pe),Ee.appendChild(Ue),Ee.appendChild(Ge),Ee.appendChild(He),Pe.appendChild(Ee),Pe.onclick=Me=>{Me.target===Pe&&document.body.removeChild(Pe)},document.body.appendChild(Pe)},fe.addEventListener("blur",()=>{const pe=Io(fe.value,x);pe!==x&&gt(m,T,x,pe)});const ye=pe=>{const he=String(pe||"").trim();return he?/^[+-]?(?:\d+\.?\d*|\d*\.\d+)$/.test(he):!1},ce=()=>{if(T.replace(/material/i,"abbe")===T)return;const he=ye(fe.value),_e=Array.from(S.querySelectorAll('div[style*="display: flex"]'));let ve=-1;for(let Ce=0;Ce<_e.length;Ce++)if(_e[Ce].contains(fe)){ve=Ce;break}if(ve>=0)for(let Ce=ve+1;Ce<_e.length&&Ce<ve+3;Ce++){const Te=_e[Ce];if(Array.from(Te.querySelectorAll("span")).some(be=>String(be.textContent||"").toLowerCase().includes("abbe"))){const be=Array.from(Te.querySelectorAll('input[type="text"]'));for(const Pe of be)Pe.disabled=!he,Pe.style.opacity=he?"1":"0.5",Pe.style.pointerEvents=he?"auto":"none";console.log(`📝 [Abbe Control] Material=${he?"numeric":"glass name"} → Abbe ${he?"enabled":"disabled"}`);return}}};fe.addEventListener("input",ce),fe.addEventListener("change",ce),setTimeout(ce,200),W.appendChild(fe),W.appendChild(we),W.appendChild(Se),ge=W}else{const W=document.createElement("input");W.type="text",W.value=x==null?"":String(x),W.style.fontSize="12px",W.style.padding="4px 6px",W.style.border=_?"1px solid #444":"1px solid #ddd",W.style.background=_?"#111827":"#fff",W.style.color=_?"#f9fafb":"#111827",W.style.borderRadius="4px",W.style.flex="1",W.style.minWidth="200px",W.style.height="28px",W.style.boxSizing="border-box",W.addEventListener("blur",()=>{const fe=Io(W.value,x);fe!==x&&gt(m,T,x,fe)}),ge=W}const xe=document.createElement("div");return xe.textContent=j||"",xe.style.fontSize="10px",xe.style.padding="2px 6px",xe.style.borderRadius="999px",xe.style.border=j?_?"1px solid #374151":"1px solid #e5e7eb":"none",xe.style.color=_?"#9ca3af":"#6b7280",xe.style.visibility=j?"visible":"hidden",O.appendChild(X),O.appendChild(ge),O.appendChild(xe),O};if(ne.length>0){S.appendChild(le("Parameters"));for(const F of ne){if(E==="ImageSurface"&&F==="optimizeSemiDia"||E==="ImageSurface"&&F==="thickness"||/^coef\d+$/.test(F)&&M.surfType==="Spherical"||/^frontCoef\d+$/.test(F)&&M.frontSurfType==="Spherical"||/^backCoef\d+$/.test(F)&&M.backSurfType==="Spherical"||/^surf1Coef\d+$/.test(F)&&M.surf1SurfType==="Spherical"||/^surf2Coef\d+$/.test(F)&&M.surf2SurfType==="Spherical"||/^surf3Coef\d+$/.test(F)&&M.surf3SurfType==="Spherical")continue;let x=M[F];if(E==="Paraxial"&&F==="focalLengthX"&&(x==null||String(x).trim()==="")&&(x=M.focalLengthY??M.focalLength??100),E==="Paraxial"&&(F==="focalLengthY"||F==="focalLength")&&(x==null||String(x).trim()==="")&&(x=M.focalLengthX??M.focalLength??100),E==="Paraxial"&&F==="surfType"&&(x==null||String(x).trim()==="")&&(x="Toric"),E==="ImageSurface"&&F==="semidiaMode"&&(x==null||String(x).trim()==="")){const D=String(M?.optimizeSemiDia??"").trim().toUpperCase();x=D==="A"||D==="AUTO"?"Auto":"Manual"}(E==="Gap"||E==="AirGap")&&F==="material"&&(x==null||x==="")&&(x="AIR");const T=G[F],j=F==="abbe"||F==="vd"||/^abbe\d+$/.test(F)||/^vd\d+$/.test(F),P=(E==="Doublet"||E==="Triplet")&&/^surf\d+SurfType$/.test(F),O=document.createElement("div");O.style.display="flex",O.style.alignItems="center",O.style.gap="6px",O.style.minHeight="32px",O.style.marginBottom=j||P?"8px":"4px";const X=document.createElement("input");X.type="checkbox",X.style.flex="0 0 auto",X.style.width="16px",X.style.height="16px",X.style.margin="0 4px 0 0",X.checked=gs(T),X.addEventListener("click",D=>D.stopPropagation());const k=document.createElement("select");k.style.flex="0 0 110px",k.style.fontSize="12px",k.style.padding="2px 4px",k.innerHTML='<option value="perConfig">Per-config</option><option value="global">Shared (all configs)</option>',k.value=ys(T),k.disabled=!X.checked,k.addEventListener("click",D=>D.stopPropagation()),X.addEventListener("change",D=>{D.stopPropagation();try{k.disabled=!X.checked}catch{}Co(m,F,X.checked,String(k.value));try{Mt()}catch{}}),k.addEventListener("change",D=>{D.stopPropagation();const U=String(k.value);hs(m,F,U),X.checked&&Co(m,F,!0,U);try{Mt()}catch{}});const Y=Z(F,x,`parameters.${F}`);Y.style.flex="1",Y.style.marginBottom="0",O.appendChild(X),O.appendChild(k),O.appendChild(Y),S.appendChild(O)}}const B=v.aperture&&typeof v.aperture=="object"?v.aperture:null,C=new Set;if(B&&Object.keys(B).length>0){S.appendChild(le("Aperture (Semidiameter)"));const F=[],x=(T,j)=>{for(const P of j)if(Object.prototype.hasOwnProperty.call(B,P)){F.push({rawKey:P,displayKey:T,value:B[P]}),C.add(T.toLowerCase());return}};if(E==="Paraxial")x("s1",["s1","front","surf1"]);else if(E==="Lens"||E==="PositiveLens")x("s1",["s1","front","surf1"]),x("s2",["s2","back","surf2"]);else if(E==="Doublet")x("s1",["s1","front","surf1"]),x("s2",["s2","middle","mid","center","surf2"]),x("s3",["s3","back","rear","surf3"]);else if(E==="Triplet")x("s1",["s1","front","surf1"]),x("s2",["s2","surf2"]),x("s3",["s3","surf3"]),x("s4",["s4","back","rear","surf4"]);else for(const T of Object.keys(B))F.push({rawKey:T,displayKey:T,value:B[T]}),C.add(String(T).toLowerCase());for(const{rawKey:T,displayKey:j,value:P}of F){const O=document.createElement("div");O.style.display="flex",O.style.alignItems="center",O.style.gap="6px",O.style.minHeight="32px",O.style.marginBottom="4px";const X=document.createElement("input");X.type="checkbox",X.style.flex="0 0 auto",X.style.width="16px",X.style.height="16px",X.style.margin="0 4px 0 0",X.checked=!1,X.addEventListener("click",D=>D.stopPropagation());const k=document.createElement("select");k.style.flex="0 0 110px",k.style.fontSize="12px",k.style.padding="2px 4px",k.innerHTML='<option value="perConfig">Per-config</option><option value="global">Shared (all configs)</option>',k.value="perConfig",k.disabled=!0,k.addEventListener("click",D=>D.stopPropagation()),X.addEventListener("change",D=>{D.stopPropagation();try{k.disabled=!X.checked}catch{}});const Y=Z(j,P,`aperture.${T}`);Y.style.flex="1",Y.style.marginBottom="0",O.appendChild(X),O.appendChild(k),O.appendChild(Y),S.appendChild(O)}}if(V.length>0)for(const F of V){const x=String(F??"").trim().toLowerCase().replace(/^front$/,"s1").replace(/^back$/,E==="Lens"||E==="PositiveLens"?"s2":"s3").replace(/^surf1$/,"s1").replace(/^surf2$/,"s2").replace(/^surf3$/,"s3").replace(/^surf4$/,"s4");if(ne.includes(F)||C.has(x))continue;const T=G[F],j=T&&typeof T=="object"&&"value"in T?T.value:T,P=document.createElement("div");P.style.display="flex",P.style.alignItems="center",P.style.gap="6px",P.style.marginBottom="4px";const O=document.createElement("input");O.type="checkbox",O.style.flex="0 0 auto",O.style.width="16px",O.style.height="16px",O.style.margin="0 4px 0 0",O.checked=gs(T),O.addEventListener("click",D=>D.stopPropagation());const X=document.createElement("select");X.style.flex="0 0 110px",X.style.fontSize="12px",X.style.padding="2px 4px",X.innerHTML='<option value="perConfig">Per-config</option><option value="global">Shared (all configs)</option>',X.value=ys(T),X.disabled=!O.checked,X.addEventListener("click",D=>D.stopPropagation()),O.addEventListener("change",D=>{D.stopPropagation();try{X.disabled=!O.checked}catch{}Co(m,F,O.checked,String(X.value));try{Mt()}catch{}}),X.addEventListener("change",D=>{D.stopPropagation();const U=String(X.value);hs(m,F,U),O.checked&&Co(m,F,!0,U);try{Mt()}catch{}});const k=T&&typeof T=="object"&&T.optimize&&T.optimize.mode?`V:${T.optimize.mode}`:"V",Y=Z(F,j,`variables.${F}.value`,k);Y.style.flex="1",Y.style.marginBottom="0",P.appendChild(O),P.appendChild(X),P.appendChild(Y),S.appendChild(P)}if(ne.length===0&&V.length===0){const F=document.createElement("div");F.textContent="No parameters defined for this block.",F.style.fontSize="12px",F.style.color=_?"#9ca3af":"#6b7280",S.appendChild(F)}r.appendChild(S)}}}function Mt(){const e=document.getElementById("import-analyze-mode-banner"),t=n=>{e&&(e.style.display=n?"":"none")};try{const n=typeof co=="function"?co():null,o=n&&Array.isArray(n.blocks)?n.blocks:null;try{const r=!o||o.length===0;t(!!r)}catch{}if(o&&o.length>0){const r=new Map;let s=null;try{if(typeof at=="function"){const i=at(o),l=i&&Array.isArray(i.rows)?i.rows:[];s=l;for(const f of l){const p=f?._blockId;if(p==null)continue;const d=String(p).trim();if(!d||d==="(none)")continue;const u=String(f?._blockType??"").trim();if(!(u==="Gap"||u==="CoordTrans")&&!(u==="ObjectSurface"||u==="ObjectPlane"||u==="Object")){if(u==="Paraxial"){r.has(d)||r.set(d,1);continue}r.set(d,(r.get(d)||0)+1)}}}}catch{}try{if(Array.isArray(s)&&s.length>0){const i=s.map((f,p)=>{const d=f&&typeof f=="object"?{...f}:{};return d.id=p,p===0?d["object type"]="Object":p===s.length-1&&(d["object type"]="Image"),d}),l=q.tableOpticalSystem&&typeof q.tableOpticalSystem.getData=="function"?q.tableOpticalSystem:q.opticalSystemTabulator&&typeof q.opticalSystemTabulator.getData=="function"?q.opticalSystemTabulator:null;l&&(typeof l.replaceData=="function"?l.replaceData(i):typeof l.setData=="function"&&l.setData(i));try{En(q)}catch{}}}catch{}const a=o.map(i=>{const l=String(i?.blockId??"(none)");return{blockId:l,blockType:String(i?.blockType??"(none)"),surfaceCount:r.has(l)?r.get(l):0,preview:Fd(i)}}),c=new Map;for(const i of o){const l=String(i?.blockId??"").trim();l&&c.set(l,i)}ws(a,{},c,o)}else{if(typeof q.dumpOpticalSystemProvenance!="function")return;const r=q.dumpOpticalSystemProvenance({quiet:!0});ws(r?.summary||[],r?.groups||{},null,null)}}catch(n){console.warn("⚠️ [Blocks] Failed to refresh block inspector:",n)}}function Cd(){const e=document.getElementById("apply-to-design-intent-btn");e&&(e.dataset&&e.dataset.applyToDesignIntentBound==="1"||(e.dataset&&(e.dataset.applyToDesignIntentBound="1"),e.addEventListener("click",()=>{try{const t=q.tableOpticalSystem||q.tableOpticalSystem,n=t&&typeof t.getData=="function"?t.getData():null;if(!Array.isArray(n)||n.length===0){alert("Expanded Optical System が見つかりません。");return}const o=[];try{const r=q.__pendingSurfaceEdits;if(r&&typeof r=="object")for(const[s,a]of Object.entries(r)){const[c,i]=String(s).split(":"),l=Number(c),f=String(i??"").trim();if(!Number.isFinite(l)||!f)continue;const p=n.find(d=>d&&typeof d.id=="number"&&d.id===l);p&&o.push({row:p,field:f,oldValue:a?.oldValue,newValue:p[f]})}}catch{}if(o.length===0&&q.__lastSurfaceEdit&&o.push(q.__lastSurfaceEdit),o.length===0)try{const r=t&&typeof t.getSelectedCells=="function"?t.getSelectedCells():[],s=Array.isArray(r)&&r.length>0?r[r.length-1]:null;if(s&&typeof s.getField=="function"&&typeof s.getRow=="function"){const a=s.getField(),c=s.getRow()?.getData?.()??null,i=typeof s.getValue=="function"?s.getValue():c?c[a]:void 0;let l;try{l=typeof s.getOldValue=="function"?s.getOldValue():void 0}catch{}l===void 0&&(l=null),c&&o.push({row:c,field:a,oldValue:l,newValue:i})}}catch{}if(o.length===0)try{const r=q.__lastActiveSurfaceCell||q.__lastSelectedSurfaceCell,s=Number(r?.surfaceId),a=String(r?.field??"").trim();if(Number.isFinite(s)&&a){const c=n.find(i=>i&&typeof i.id=="number"&&i.id===s);c&&o.push({row:c,field:a,oldValue:null,newValue:c[a]})}}catch{}if(o.length===0){alert("Apply対象の変更が見つかりません。");return}console.log(`✅ Apply to Design Intent: ${o.length} edits processed`);try{Mt()}catch{}try{q.__pendingSurfaceEdits={}}catch{}try{const r=q.popup3DWindow;r&&!r.closed&&typeof r.postMessage=="function"&&r.postMessage({action:"request-redraw"},"*")}catch{}}catch(t){console.error("❌ Apply to Design Intent failed:",t),alert(`Apply failed: ${t?.message||String(t)}`)}})))}function ci(e){const t=String(e??"").trim();return t==="ObjectPlane"?"ObjectSurface":t==="ImagePlane"?"ImageSurface":t==="AirGap"?"Gap":t==="ThinLens"?"Paraxial":t}function Ba(e,t){const o=ci(t)||"Block";let r=0;const s=new RegExp(`^${o}-(\\d+)$`);for(const a of e||[]){const c=s.exec(String(a?.blockId||""));if(c){const i=parseInt(c[1],10);i>r&&(r=i)}}return`${o}-${r+1}`}function za(e,t){const n=ci(e),r={blockId:String(t??"").trim(),blockType:n,role:null,constraints:{},parameters:{},variables:{},metadata:{source:"ui-add"}};return n==="Paraxial"?(r.parameters={surfType:"Toric",focalLengthX:100,focalLengthY:100},r.aperture={front:10},r):n==="Lens"||n==="PositiveLens"?(r.parameters={frontRadius:"INF",backRadius:"INF",centerThickness:1,material:"N-BK7",abbe:"",frontSurfType:"Spherical",backSurfType:"Spherical",frontConic:0,backConic:0},r.aperture={front:10,back:10},r):n==="Doublet"?(r.parameters={radius1:"INF",radius2:"INF",radius3:"INF",thickness1:1,thickness2:1,material1:"N-BK7",material2:"N-F2",abbe1:"",abbe2:"",surf1SurfType:"Spherical",surf2SurfType:"Spherical",surf3SurfType:"Spherical",surf1Conic:0,surf2Conic:0,surf3Conic:0},r.aperture={s1:10,s2:10,s3:10},r):n==="Triplet"?(r.parameters={radius1:"INF",radius2:"INF",radius3:"INF",radius4:"INF",thickness1:1,thickness2:1,thickness3:1,material1:"N-BK7",material2:"N-F2",material3:"N-BK7",abbe1:"",abbe2:"",abbe3:"",surf1SurfType:"Spherical",surf2SurfType:"Spherical",surf3SurfType:"Spherical",surf4SurfType:"Spherical",surf1Conic:0,surf2Conic:0,surf3Conic:0,surf4Conic:0},r.aperture={s1:10,s2:10,s3:10,s4:10},r):n==="Gap"?(r.blockType="Gap",r.parameters={thickness:1,material:"AIR",abbe:"",thicknessMode:""},r):n==="ObjectSurface"?(r.parameters={objectDistanceMode:"Finite",objectDistance:100},r):n==="Stop"?(r.parameters={semiDiameter:Ta},r):n==="Mirror"?(r.parameters={radius:"INF",thickness:0,material:"MIRROR",surfType:"Spherical",conic:0,coef1:0,coef2:0,coef3:0,coef4:0,coef5:0,coef6:0,coef7:0,coef8:0,coef9:0,coef10:0,apertureShape:"Circular",semidia:10,apertureWidth:20,apertureHeight:20},r):n==="CoordTrans"?(r.parameters={decenterX:0,decenterY:0,decenterZ:0,tiltX:0,tiltY:0,tiltZ:0,order:0,coordReturn:"none",toSurf:0},r):n==="SingleSurface"?(r.parameters={radius:"INF",thickness:10,material:"AIR",surfType:"Spherical",conic:0,coef1:0,coef2:0,coef3:0,coef4:0,coef5:0,coef6:0,coef7:0,coef8:0,coef9:0,coef10:0,apertureShape:"Circular",semidia:10,apertureWidth:20,apertureHeight:20},r):n==="ImageSurface"?(r.parameters={semidia:"",semidiaMode:"Manual",optimizeSemiDia:""},delete r.variables,r):(r.parameters={},r)}function Id(e,t=null){const n=We();if(!n||!Array.isArray(n.configurations))return{ok:!1,reason:"systemConfigurations not found."};const o=n.activeConfigId,r=n.configurations.findIndex(u=>u&&u.id===o);if(r<0)return{ok:!1,reason:"active config not found."};const s=n.configurations[r];if(!s||!Array.isArray(s.blocks))return{ok:!1,reason:"active config has no blocks."};const a=s.blocks,c=ci(e);if(!c)return{ok:!1,reason:"blockType is required."};if(c==="ImageSurface"&&a.some(y=>y&&String(y.blockType??"").trim()==="ImageSurface"))return{ok:!1,reason:"ImageSurface already exists (only one is supported)."};if(c==="ObjectSurface"&&a.some(y=>{const m=String(y?.blockType??"").trim();return m==="ObjectSurface"||m==="ObjectPlane"}))return{ok:!1,reason:"ObjectSurface/ObjectPlane already exists (only one is supported)."};if(c==="Gap"||c==="AirGap"){const u=String(t??"").trim();let y=-1;if(u)y=a.findIndex(g=>g&&String(g.blockId??"").trim()===u);else for(let g=a.length-1;g>=0;g--)if(String(a[g]?.blockType??"").trim()!=="ImageSurface"){y=g;break}if(y<0)return{ok:!1,reason:"Gap requires a preceding block (e.g., Lens or Stop). Add a Lens/Stop first."};const m=a[y],h=String(m?.blockType??"").trim();if(h==="ObjectSurface"||h==="ObjectPlane")return{ok:!1,reason:"Gap cannot be placed directly after ObjectSurface. Add a Lens or Stop first."}}const i=Ba(a,c),l=za(c,i);let f=a.findIndex(u=>u&&String(u.blockType??"").trim()==="ImageSurface");f<0&&(f=a.length);let p=f;c==="ObjectSurface"&&(p=0);const d=String(t??"").trim();if(d){const u=a.findIndex(y=>y&&String(y.blockId??"").trim()===d);u>=0&&(p=Math.min(u+1,f))}a.splice(p,0,l);try{(!s.metadata||typeof s.metadata!="object")&&(s.metadata={}),s.metadata.modified=new Date().toISOString()}catch{}try{if(Js(s).filter(m=>m&&m.severity==="fatal").length>0)return a.splice(p,1),{ok:!1,reason:"block validation failed."}}catch{}try{et(n)}catch(u){return{ok:!1,reason:`failed to save: ${u?.message||String(u)}`}}return{ok:!0,blockId:i,blockData:JSON.parse(JSON.stringify(l)),insertIndex:p}}function Td(e){const t=We();if(!t||!Array.isArray(t.configurations))return{ok:!1,reason:"systemConfigurations not found."};const n=t.activeConfigId,o=t.configurations.findIndex(p=>p&&p.id===n);if(o<0)return{ok:!1,reason:"active config not found."};const r=t.configurations[o];if(!r||!Array.isArray(r.blocks))return{ok:!1,reason:"active config has no blocks."};const s=r.blocks,a=String(e??"").trim();if(!a)return{ok:!1,reason:"blockId is required."};const c=s.findIndex(p=>p&&String(p.blockId??"").trim()===a);if(c<0)return{ok:!1,reason:`block not found: ${a}`};const i=String(s[c]?.blockType??"").trim(),l=JSON.parse(JSON.stringify(s[c])),f=s.splice(c,1);if(i==="ImageSurface"){const p=Ba(s,"ImageSurface"),d=za("ImageSurface",p);s.push(d)}try{(!r.metadata||typeof r.metadata!="object")&&(r.metadata={}),r.metadata.modified=new Date().toISOString()}catch{}try{if(Js(r).filter(u=>u&&u.severity==="fatal").length>0)return s.splice(c,0,...f||[]),{ok:!1,reason:"block validation failed."}}catch{}try{const p=at(r.blocks);if(p&&Array.isArray(p.rows)){r.opticalSystem=p.rows;try{jn(p.rows)}catch{}try{typeof q.saveLensTableData=="function"&&q.saveLensTableData(p.rows)}catch{}try{q.tableOpticalSystem&&typeof q.tableOpticalSystem.setData=="function"&&q.tableOpticalSystem.setData(p.rows)}catch{}}}catch{}try{et(t)}catch(p){return{ok:!1,reason:`failed to save: ${p?.message||String(p)}`}}return{ok:!0,blockData:l,blockIndex:c}}function Pd(){const e=document.getElementById("design-intent-add-block-btn"),t=document.getElementById("design-intent-delete-block-btn"),n=document.getElementById("design-intent-param-all-on-btn"),o=document.getElementById("design-intent-param-all-off-btn"),r=document.getElementById("design-intent-add-block-type");e&&!e.dataset.designIntentAddBound&&(e.dataset.designIntentAddBound="1",e.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}try{const a=String(r?.value??"Lens").trim(),i=Id(a,mn);if(!i||i.ok!==!0){alert(`Failed to add block: ${i?.reason||"unknown error"}`);return}mn=String(i.blockId??"")||null;try{if(q.undoHistory&&q.AddBlockCommand&&!q.undoHistory.isExecuting&&i.blockData&&typeof i.insertIndex=="number"){const l=We(),f=new q.AddBlockCommand(l.activeConfigId,i.blockData,i.insertIndex);q.undoHistory.record(f)}}catch{}try{Mt()}catch{}try{typeof q.loadActiveConfigurationToTables=="function"&&q.loadActiveConfigurationToTables()}catch{}try{q.popup3DWindow&&!q.popup3DWindow.closed&&q.popup3DWindow.postMessage({action:"request-redraw"},"*")}catch{}}catch(a){console.error("❌ Failed to add block:",a),alert(`Failed to add block: ${a?.message||String(a)}`)}})),t&&!t.dataset.designIntentDeleteBound&&(t.dataset.designIntentDeleteBound="1",t.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}try{const a=String(mn??"").trim();if(!a){alert("Select (expand) a block first to delete.");return}const c=Td(a);if(!c||c.ok!==!0){alert(`Failed to delete block: ${c?.reason||"unknown error"}`);return}try{if(q.undoHistory&&q.DeleteBlockCommand&&!q.undoHistory.isExecuting&&c.blockData&&typeof c.blockIndex=="number"){const i=We(),l=new q.DeleteBlockCommand(i.activeConfigId,c.blockData,c.blockIndex);q.undoHistory.record(l)}}catch{}mn=null;try{Mt()}catch{}try{typeof q.loadActiveConfigurationToTables=="function"&&q.loadActiveConfigurationToTables()}catch{}try{q.popup3DWindow&&!q.popup3DWindow.closed&&q.popup3DWindow.postMessage({action:"request-redraw"},"*")}catch{}}catch(a){console.error("❌ Failed to delete block:",a),alert(`Failed to delete block: ${a?.message||String(a)}`)}})),n&&!n.dataset.designIntentParamAllOnBound&&(n.dataset.designIntentParamAllOnBound="1",n.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}const a=bs(!0);(!a||a.ok!==!0)&&alert(`Failed to set Parameter All ON: ${a?.reason||"unknown error"}`)})),o&&!o.dataset.designIntentParamAllOffBound&&(o.dataset.designIntentParamAllOffBound="1",o.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}const a=bs(!1);(!a||a.ok!==!0)&&alert(`Failed to set Parameter All OFF: ${a?.reason||"unknown error"}`)}))}function no(){try{Yp(),Zp(),Xp(),Kp(),Jp(),Qp(),nd(),td(),Gp(),Pd(),od(),rd(),id(),sd(),ad(),cd(),ld(),ud(),pd(),dd(),md(),fd(),gd(),Cd()}catch(e){console.error("❌ [DOM] Failed to setup event handlers:",e)}}async function kd(){const e=Dc();if(!e)return{ok:!1,reason:"no_hash"};if(!confirm(`リンクから設計を読み込みます。現在の設計は上書きされます。続行しますか？

Load design from URL? Current design will be overwritten.`))return{ok:!1,reason:"cancelled"};let n;try{n=jc(e)}catch(r){return console.warn("❌ [URL Load] Decode failed:",r),alert(r?.message||"Failed to load design from URL"),{ok:!1,reason:"decode_failed"}}const o=await go(n,{filename:"shared-link.json"});if(o)try{history.replaceState(null,"",`${location.origin}${location.pathname}${location.search}`)}catch{}return{ok:o}}typeof window<"u"&&(window.addEventListener("coopt:react-mounted",()=>{setTimeout(()=>{no(),Tn(),Pn()},200)}),document.readyState==="loading"?document.addEventListener("DOMContentLoaded",()=>{setTimeout(()=>{no(),Tn(),Pn()},1e3)}):setTimeout(()=>{no(),Tn(),Pn()},1e3));const Zt=window;function vs(){try{const e=document.getElementById("wavefront-object-select");if(!e)return;let t=[];if(typeof window<"u"&&Zt.tableObject&&Zt.tableObject.getData){const o=Zt.tableObject.getData();t=o.filter(r=>r&&r!==null&&r!==void 0),o.length>t.length&&console.warn(`無効なObjectデータが${o.length-t.length}個あります。Clear Cacheでリセットを推奨。`)}else{console.warn("⚠️ tableObjectが利用できません");return}const n=e.value;if(e.innerHTML="",t.length===0){const o=document.createElement("option");o.value="0",o.textContent="Object 1 (Empty)",o.disabled=!0,e.appendChild(o)}else t.forEach((o,r)=>{const s=document.createElement("option");s.value=r.toString();let a=`Object ${r+1}`;const c=o.xHeightAngle||0,i=o.yHeightAngle||0;c!==0||i!==0?a+=` (${c.toFixed(2)}, ${i.toFixed(2)})`:a+=" (0.00, 0.00)",s.textContent=a,e.appendChild(s)});n&&e.querySelector(`option[value="${n}"]`)?e.value=n:t.length>0&&(e.value="0")}catch(e){console.error("❌ Object選択ドロップダウン更新エラー:",e)}}function To(){try{const e=document.getElementById("psf-object-select");if(!e)return;let t=[];if(typeof window<"u"&&Zt.tableObject&&Zt.tableObject.getData){const o=Zt.tableObject.getData();t=Array.isArray(o)?o.filter(r=>r&&r!==null&&r!==void 0):[]}const n=e.value;if(e.innerHTML="",t.length===0){const o=document.createElement("option");o.value="0",o.textContent="Object 1 (Empty)",o.disabled=!0,e.appendChild(o);return}t.forEach((o,r)=>{const s=document.createElement("option");s.value=r.toString();const a=o?.xHeightAngle??o?.xFieldAngle??o?.xAngle??o?.x??0,c=o?.yHeightAngle??o?.yFieldAngle??o?.yAngle??o?.y??o?.fieldAngle??0,i=Number.isFinite(Number(a))?Number(a):0,l=Number.isFinite(Number(c))?Number(c):0,f=String(o?.position??o?.object??o?.objectType??"Object");s.textContent=`${r+1}: ${f} (${i}, ${l})`,e.appendChild(s)}),n&&e.querySelector(`option[value="${n}"]`)?e.value=n:e.value="0"}catch(e){console.error("❌ PSF Object選択ドロップダウン更新エラー:",e)}}function Ed(){To(),Zt.updatePSFObjectOptions=To,Zt.updatePSFObjectSelect=To,Zt.setupPSFObjectSelect=To}function Od(){const e=document.getElementById("wavefront-object-select");e&&e.addEventListener("change",function(){parseInt(this.value),Qs()})}function Dd(){Od(),vs(),Zt.updateWavefrontObjectSelect=vs,Zt.debugResetObjectTable=jd}function jd(){try{Qs(),location.reload()}catch(e){console.error("❌ Objectテーブルリセットエラー:",e)}}const yt={};function Ld(e){try{if(typeof window>"u")return;e.scene!==void 0&&(window.scene=yt.scene),e.camera!==void 0&&(window.camera=yt.camera),e.renderer!==void 0&&(window.renderer=yt.renderer),e.controls!==void 0&&(window.controls=yt.controls),window.__cooptRenderingContext={scene:window.scene,camera:window.camera,renderer:window.renderer,controls:window.controls}}catch{}}function li(e){return e&&typeof e=="object"&&(e.scene!==void 0&&(yt.scene=e.scene),e.camera!==void 0&&(yt.camera=e.camera),e.renderer!==void 0&&(yt.renderer=e.renderer),e.controls!==void 0&&(yt.controls=e.controls),Ld(e)),Bd()}try{typeof window<"u"&&typeof window.__cooptSetRenderingContext!="function"&&(window.__cooptSetRenderingContext=e=>{try{li(e||{})}catch{}})}catch{}function Bd(){try{typeof window<"u"&&(yt.scene===void 0&&window.scene!==void 0&&(yt.scene=window.scene),yt.camera===void 0&&window.camera!==void 0&&(yt.camera=window.camera),yt.renderer===void 0&&window.renderer!==void 0&&(yt.renderer=window.renderer),yt.controls===void 0&&window.controls!==void 0&&(yt.controls=window.controls))}catch{}return{scene:yt.scene,camera:yt.camera,renderer:yt.renderer,controls:yt.controls}}function Rt(e){return!!e&&typeof e=="object"&&!Array.isArray(e)}function Pt(e){return JSON.parse(JSON.stringify(e))}function zd(){try{return new Date().toLocaleString()}catch{return String(Date.now())}}function $d(e){Bc("suggestDesignIntent","lastSuggest",e&&typeof e=="object"?e:null)}function Wd(){const e=Lc("suggestDesignIntent","lastSuggest",null);if(e&&typeof e=="object")return e;try{if(typeof window>"u")return null;const t=window.__cooptLastSuggest;return t&&typeof t=="object"?t:null}catch{return null}}function $a(){try{if(typeof localStorage>"u")return{systemConfig:null,cfg:null};const e=Ot(),t=e?.activeConfigId,n=e?.configurations?.find(o=>o&&o.id===t)||e?.configurations?.[0];return{systemConfig:e,cfg:n}}catch{return{systemConfig:null,cfg:null}}}function Ud(){return typeof document>"u"?null:document.getElementById("system-data")}function Hd(e){try{const t=localStorage.getItem(e);if(!t)return null;const n=JSON.parse(t);return Array.isArray(n)?n:null}catch{return null}}function Gd(e,t,n){try{if(Array.isArray(e?.blocks)&&e.blocks.some(r=>{const s=String(r?.blockType??"").trim();return s==="ObjectSurface"||s==="ObjectPlane"}))return}catch{}try{const o=e?.opticalSystem?.[0]?.thickness;if(String(o??"").trim()!=="")return o}catch{}try{if(t&&String(t.activeConfigId)===String(n)){const r=Hd("OpticalSystemTableData")?.[0]?.thickness;if(String(r??"").trim()!=="")return r}}catch{}}async function qd(e){const{systemConfig:t,cfg:n}=$a();if(!t||!n)return{ok:!1,reason:"systemConfigurations / active configuration が見つかりません。"};if(!Array.isArray(e)||e.length===0)return{ok:!1,reason:"Recommendation blocks が空です。"};const o=n.id,r=Gd(n,t,o);n.blocks=Pt(e);try{const s=at(n.blocks);s&&Array.isArray(s.rows)&&(r!==void 0&&s.rows[0]&&typeof s.rows[0]=="object"&&(s.rows[0].thickness=r),n.opticalSystem=s.rows)}catch{}try{Jr(t)}catch{return{ok:!1,reason:"localStorage への保存に失敗しました。"}}try{window.ConfigurationManager&&typeof window.ConfigurationManager.loadActiveConfigurationToTables=="function"&&await window.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0,suppressOpticalSystemDataChanged:!0})}catch{}try{kn()}catch{}try{window.meritFunctionEditor&&typeof window.meritFunctionEditor.calculateMerit=="function"&&window.meritFunctionEditor.calculateMerit()}catch{}return{ok:!0}}function Et(e){if(typeof e=="number")return Number.isFinite(e)?e:null;if(typeof e=="string"){const t=e.trim();if(t===""||/^inf(inity)?$/i.test(t))return null;const n=Number(t);return Number.isFinite(n)?n:null}return null}function xs(e,{radiusScale:t=1,thicknessScale:n=1,gapScale:o=1}={}){for(const r of e){if(!Rt(r))continue;const s=String(r.blockType??"").trim(),a=Rt(r.parameters)?r.parameters:null;if(a){if(s==="Paraxial"){const c=Et(a.focalLength),i=Et(a.focalLengthX),l=Et(a.focalLengthY);c!==null&&Number.isFinite(t)&&Math.abs(t)>1e-12&&(a.focalLength=c/t),i!==null&&Number.isFinite(t)&&Math.abs(t)>1e-12&&(a.focalLengthX=i/t),l!==null&&Number.isFinite(t)&&Math.abs(t)>1e-12&&(a.focalLengthY=l/t)}else if(s==="Lens"||s==="PositiveLens"){const c=Et(a.frontRadius),i=Et(a.backRadius),l=Et(a.centerThickness);c!==null&&(a.frontRadius=c*t),i!==null&&(a.backRadius=i*t),l!==null&&(a.centerThickness=Math.max(1e-6,l*n))}else if(s==="AirGap"){const c=Et(a.thickness);c!==null&&(a.thickness=Math.max(0,c*o))}}}}function Po(e,{deltaCurvature:t=0}={}){for(const r of e){if(!Rt(r))continue;const s=String(r.blockType??"").trim();if(!(s==="Lens"||s==="PositiveLens"))continue;const a=Rt(r.parameters)?r.parameters:null;if(!a)continue;const c=Et(a.frontRadius),i=Et(a.backRadius);if(c===null||i===null||!Number.isFinite(c)||!Number.isFinite(i)||Math.abs(c)<1e-12||Math.abs(i)<1e-12)continue;const l=1/c,f=1/i,p=l+t,d=f+t;!Number.isFinite(p)||!Number.isFinite(d)||Math.abs(p)<1e-12||Math.abs(d)<1e-12||Math.abs(p)>1e6||Math.abs(d)>1e6||(a.frontRadius=1/p,a.backRadius=1/d)}}function Ss(e,{deltaCurvature:t=0}={}){for(const r of e){if(!Rt(r))continue;const s=String(r.blockType??"").trim();if(!(s==="Lens"||s==="PositiveLens"))continue;const a=Rt(r.parameters)?r.parameters:null;if(!a)continue;const c=Et(a.frontRadius),i=Et(a.backRadius);if(c===null||i===null||!Number.isFinite(c)||!Number.isFinite(i)||Math.abs(c)<1e-12||Math.abs(i)<1e-12)continue;const l=1/c,f=1/i,p=l+t,d=f-t;!Number.isFinite(p)||!Number.isFinite(d)||Math.abs(p)<1e-12||Math.abs(d)<1e-12||Math.abs(p)>1e6||Math.abs(d)>1e6||(a.frontRadius=1/p,a.backRadius=1/d)}}function Vd(e,{oddScale:t=1,evenScale:n=1}={}){let o=0;for(const r of e){if(!Rt(r)||String(r.blockType??"").trim()!=="AirGap")continue;const a=Rt(r.parameters)?r.parameters:null;if(!a)continue;const c=Et(a.thickness);if(c===null)continue;const i=o%2===0?t:n;a.thickness=Math.max(0,c*i),o++}}function As(e,{scale:t=1}={}){for(const n of e){if(!Rt(n)||String(n.blockType??"").trim()!=="Stop")continue;const r=Rt(n.parameters)?n.parameters:null;if(!r)continue;const s=Et(r.semiDiameter);s!==null&&(r.semiDiameter=Math.max(1e-9,s*t))}}function Yd(e,t,n=12){const o=[],r=new Map;for(const s of e){if(!Rt(s))continue;const a=String(s.blockId??"");a&&r.set(a,s)}for(const s of t){if(!Rt(s))continue;const a=String(s.blockId??"");if(!a)continue;const c=r.get(a);if(!c){if(o.push(`+ ${a} (${s.blockType})`),o.length>=n)break;continue}const i=Rt(c.parameters)?c.parameters:null,l=Rt(s.parameters)?s.parameters:null;if(!i||!l)continue;const f=["frontRadius","backRadius","centerThickness","thickness","semiDiameter","material"];for(const p of f){if(!(p in l)&&!(p in i))continue;const d=i[p],u=l[p],y=String(d??""),m=String(u??"");if(y!==m&&(o.push(`~ ${a}.${p}: ${y} → ${m}`),o.length>=n))break}if(o.length>=n)break}return o}function Ns({configId:e,blocks:t}){const n=typeof window<"u"?window.meritFunctionEditor:null;if(!n)return{ok:!1,reason:"meritFunctionEditor is not available."};const o=typeof window<"u"?Object.prototype.hasOwnProperty.call(window,"__cooptBlocksOverride"):!1,r=typeof window<"u"?window.__cooptBlocksOverride:null,s=r&&typeof r=="object"?{...r}:null,a=r&&typeof r=="object"?r:typeof window<"u"?window.__cooptBlocksOverride||={}:{};a[String(e)]=t;try{if(typeof n.calculateMeritBreakdownOnly=="function"){const c=n.calculateMeritBreakdownOnly(),i=Number(c?.total),l=Array.isArray(c?.terms)?c.terms:[];return{ok:!0,total:Number.isFinite(i)?i:1/0,terms:l}}if(typeof n.calculateMeritValueOnly=="function"){const c=Number(n.calculateMeritValueOnly());return{ok:!0,total:Number.isFinite(c)?c:1/0,terms:[]}}return{ok:!1,reason:"No merit evaluator available."}}finally{if(typeof window<"u")if(o&&s&&typeof s=="object"){const c=window.__cooptBlocksOverride&&typeof window.__cooptBlocksOverride=="object"?window.__cooptBlocksOverride:window.__cooptBlocksOverride||={};for(const i of Object.keys(c))try{delete c[i]}catch{}Object.assign(c,s)}else try{delete window.__cooptBlocksOverride}catch{}}}function Zd(e){const t={};for(const n of e){const o=String(n?.operand??"");o&&(o==="EFFL"&&t.EFFL===void 0&&(t.EFFL=n?.value),o==="TOT3_SPH"&&t.TOT3_SPH===void 0&&(t.TOT3_SPH=n?.value),o==="TOT3_COMA"&&t.TOT3_COMA===void 0&&(t.TOT3_COMA=n?.value),o==="TOT3_ASTI"&&t.TOT3_ASTI===void 0&&(t.TOT3_ASTI=n?.value),o==="TOT3_FCUR"&&t.TOT3_FCUR===void 0&&(t.TOT3_FCUR=n?.value),o==="TOT3_DIST"&&t.TOT3_DIST===void 0&&(t.TOT3_DIST=n?.value),o==="TOT_LCA"&&t.TOT_LCA===void 0&&(t.TOT_LCA=n?.value),o==="TOT_TCA"&&t.TOT_TCA===void 0&&(t.TOT_TCA=n?.value))}return t}function nt(e){const t=Number(e);return Number.isFinite(t)?t.toFixed(6):String(e??"-")}function _s(e,t,n){const o=Number(e);return Number.isFinite(o)?Math.max(t,Math.min(n,o)):t}function Xd(e){const t=(Array.isArray(e)?e:[]).filter(o=>Number.isFinite(Number(o))).map(Number).sort((o,r)=>o-r);if(t.length===0)return NaN;const n=Math.floor(t.length/2);return t.length%2===1?t[n]:.5*(t[n-1]+t[n])}function Kd(e){const t=[];for(const s of Array.isArray(e)?e:[]){if(!Rt(s))continue;const a=String(s.blockType??"").trim();if(!(a==="Lens"||a==="PositiveLens"||a==="Paraxial"))continue;const c=Rt(s.parameters)?s.parameters:null;if(!c)continue;const i=Et(c.frontRadius),l=Et(c.backRadius);i!==null&&Number.isFinite(i)&&i!==0&&t.push(Math.abs(1/i)),l!==null&&Number.isFinite(l)&&l!==0&&t.push(Math.abs(1/l))}const n=Xd(t);if(!Number.isFinite(n)||n<=0)return{small:.002,large:.004};const o=_s(.02*n,1e-5,.005),r=_s(2*o,2e-5,.01);return{small:o,large:r}}function Wa(e){const t=String(e??"").trim().toLowerCase();return t?t==="tele"||t==="telephoto"||t==="tele-photo"?"telephoto":t==="wide"||t==="wideangle"||t==="wide-angle"?"wide":"balanced":"balanced"}function Jd(e,t){const n=Wa(t),o={balanced:["global","bending_small","asym","gap","stop","bending_large","material"],telephoto:["gap","asym","bending_small","global","stop","bending_large","material"],wide:["stop","bending_small","asym","global","gap","bending_large","material"]},r=o[n]||o.balanced,s=new Map(r.map((c,i)=>[c,i])),a=(Array.isArray(e)?e:[]).map((c,i)=>({c,idx:i}));return a.sort((c,i)=>{const l=String(c.c?.tag??""),f=String(i.c?.tag??""),p=s.has(l)?s.get(l):999,d=s.has(f)?s.get(f):999;return p!==d?p-d:c.idx-i.idx}),a.map(c=>c.c)}function Dn(e){const t=e&&e.id!==void 0?String(e.id):"?",n=String(e?.operand??"");return`#${t} ${n||"(unknown)"}`}function Qd(e,t=5){const n=Array.isArray(e)?e.slice():[];n.sort((r,s)=>Number(s?.term??0)-Number(r?.term??0));const o=[];for(const r of n.slice(0,t)){const s=Number(r?.term),a=Number(r?.impactPct);o.push(`    - ${Dn(r)}: term=${nt(s)}, impact=${nt(a)}% (value=${nt(r?.value)}, target=${nt(r?.target)})`)}return o}function Ua(e,t=2){const n=Array.isArray(e)?e.slice():[];n.sort((r,s)=>Number(s?.term??0)-Number(r?.term??0));const o=n.slice(0,Math.max(0,t));return o.length===0?"-":o.map(r=>{const s=Number(r?.impactPct),a=Number.isFinite(s)?`${s.toFixed(1)}%`:"-";return`${Dn(r)} ${a}`}).join(", ")}function em(e,t,{improveK:n=2,regressK:o=1}={}){const r=Ho(e),s=Ho(t),a=[];for(const[p,d]of s.entries()){const u=r.get(p);if(!u)continue;const y=Number(u?.term),h=Number(d?.term)-y;Number.isFinite(h)&&a.push({d:h,ct:d})}a.sort((p,d)=>p.d-d.d);const c=a.filter(p=>p.d<0).slice(0,Math.max(0,n)),i=a.filter(p=>p.d>0).slice(-Math.max(0,o)).reverse(),l=c.length?c.map(p=>`${Dn(p.ct)} ${nt(p.d)}`).join(", "):"-",f=i.length?i.map(p=>`${Dn(p.ct)} +${nt(p.d)}`).join(", "):"-";return{improveStr:l,regressStr:f}}function Ho(e){const t=new Map;for(const n of Array.isArray(e)?e:[]){const o=String(n?.id??"");o&&t.set(o,n)}return t}function tm(e,t,{kImprove:n=3,kRegress:o=2}={}){const r=Ho(e),s=Ho(t),a=[];for(const[f,p]of s.entries()){const d=r.get(f);if(!d)continue;const u=Number(d?.term),m=Number(p?.term)-u;Number.isFinite(m)&&a.push({d:m,bt:d,ct:p})}a.sort((f,p)=>f.d-p.d);const c=a.filter(f=>f.d<0).slice(0,n),i=a.filter(f=>f.d>0).slice(-o).reverse(),l=[];if(c.length>0){l.push("  Biggest improvements vs current (Δterm < 0):");for(const f of c)l.push(`    - ${Dn(f.ct)}: Δterm=${nt(f.d)} (cur=${nt(f.bt?.term)} → cand=${nt(f.ct?.term)})`)}if(i.length>0){l.push("  Biggest regressions vs current (Δterm > 0):");for(const f of i)l.push(`    - ${Dn(f.ct)}: Δterm=${nt(f.d)} (cur=${nt(f.bt?.term)} → cand=${nt(f.ct?.term)})`)}return l}function nm({idx:e,title:t,intent:n,merit:o,metrics:r,diffs:s,baseTerms:a,candTerms:c},i={}){const l=[];l.push(`Candidate ${e}: ${t}`),l.push(`  Intent: ${n}`),l.push(`  Merit:  ${nt(o)}`);const f=Object.keys(r||{});if(f.length>0){l.push("  Key Metrics:");for(const y of f)l.push(`    ${y}: ${nt(r[y])}`)}if(s&&s.length>0){l.push("  Diffs (vs current):");for(const y of s)l.push(`    ${y}`)}const p=Number.isFinite(Number(i.topK))?Number(i.topK):5,d=Number.isFinite(Number(i.improveK))?Number(i.improveK):3,u=Number.isFinite(Number(i.regressK))?Number(i.regressK):2;return Array.isArray(c)&&c.length>0&&(l.push("  Top contributors (candidate):"),l.push(...Qd(c,p))),Array.isArray(a)&&a.length>0&&Array.isArray(c)&&c.length>0&&l.push(...tm(a,c,{kImprove:d,kRegress:u})),l.join(`
`)}function om({idx:e,title:t,tag:n,merit:o,deltaMerit:r,baseTerms:s,candTerms:a},c={}){const i=Number.isFinite(Number(c.topK))?Number(c.topK):2,l=Number.isFinite(Number(c.improveK))?Number(c.improveK):2,f=Number.isFinite(Number(c.regressK))?Number(c.regressK):1,p=String(n??"").trim(),d=p?` [${p}]`:"",u=Number(r),y=Number.isFinite(u)?u>=0?`+${nt(u)}`:`${nt(u)}`:"-",m=Ua(a,i),h=em(s,a,{improveK:l,regressK:f}),g=[];return g.push(`C${e}${d}: ${t} | Merit ${nt(o)} (Δ ${y})`),g.push(`  Top: ${m}`),g.push(`  Δterm: - ${h.improveStr} | + ${h.regressStr}`),g.join(`
`)}function rm(e){const t=[];for(const n of e){if(!Rt(n))continue;const o=String(n.blockType??"").trim();o&&o!=="ImageSurface"&&t.push(`${o}${n.blockId?`(${n.blockId})`:""}`)}return t.push("ImageSurface"),t.join(" → ")}function im(e={}){const t={candidateCount:5,topK:2,improveK:2,regressK:1,designType:"balanced",outputMode:"compact"},n=typeof window<"u"&&window.__cooptSuggestOptions&&typeof window.__cooptSuggestOptions=="object"?window.__cooptSuggestOptions:{},o={...t,...n,...e&&typeof e=="object"?e:{}},{systemConfig:r,cfg:s}=$a();if(!r||!s){alert("systemConfigurations / active configuration が見つかりません。");return}if(!Array.isArray(s.blocks)||s.blocks.length===0){alert("Blocks がありません。Suggest は Blocks ベースで動作します。");return}const a=s.id,c=Pt(s.blocks),i=Kd(c),l=Ns({configId:a,blocks:c}),f=l.ok?l.total:1/0,p=l.ok?Array.isArray(l.terms)?l.terms:[]:[],d=[];{const b=Pt(c);xs(b,{radiusScale:.95,thicknessScale:1,gapScale:1.02}),d.push({title:"Global bias A (R×0.95, gaps×1.02)",intent:"やや強めパワー方向へ（近傍探索）",blocks:b,tag:"global"})}{const b=Pt(c);xs(b,{radiusScale:1.05,thicknessScale:1,gapScale:.98}),d.push({title:"Global bias B (R×1.05, gaps×0.98)",intent:"やや弱めパワー方向へ（別の局所解）",blocks:b,tag:"global"})}{const b=Pt(c);Po(b,{deltaCurvature:+i.small}),d.push({title:`Bending + (Δc=${nt(i.small)})`,intent:"形状因子方向（小ステップ）",blocks:b,tag:"bending_small"})}{const b=Pt(c);Po(b,{deltaCurvature:-i.small}),d.push({title:`Bending - (Δc=-${nt(i.small)})`,intent:"形状因子方向（小ステップ）",blocks:b,tag:"bending_small"})}{const b=Pt(c);Po(b,{deltaCurvature:+i.large}),d.push({title:`Bending ++ (Δc=${nt(i.large)})`,intent:"形状因子方向（大ステップ）",blocks:b,tag:"bending_large"})}{const b=Pt(c);Po(b,{deltaCurvature:-i.large}),d.push({title:`Bending -- (Δc=-${nt(i.large)})`,intent:"形状因子方向（大ステップ）",blocks:b,tag:"bending_large"})}{const b=Pt(c);Ss(b,{deltaCurvature:+i.small}),d.push({title:`Asymmetric bend + (Δc=${nt(i.small)})`,intent:"前後面の配分を変える（小ステップ）",blocks:b,tag:"asym"})}{const b=Pt(c);Ss(b,{deltaCurvature:-i.small}),d.push({title:`Asymmetric bend - (Δc=${nt(i.small)})`,intent:"前後面の配分を変える（小ステップ）",blocks:b,tag:"asym"})}{const b=Pt(c);Vd(b,{oddScale:1.05,evenScale:.95}),d.push({title:"Gap redistribute (odd×1.05, even×0.95)",intent:"ギャップの再配分で探索（全体スケール以外）",blocks:b,tag:"gap"})}{const b=Pt(c);As(b,{scale:1.02}),d.push({title:"Stop +2% (semiDiameter×1.02)",intent:"ストップ径の微調整で反応を見る",blocks:b,tag:"stop"})}{const b=Pt(c);As(b,{scale:.98}),d.push({title:"Stop -2% (semiDiameter×0.98)",intent:"逆方向のストップ径微調整で反応を見る",blocks:b,tag:"stop"})}{const b=Pt(c);d.push({title:"Material exploration preset",intent:"形状固定でガラス変更の余地を評価（入口）",blocks:b,tag:"material"})}const u=Jd(d,o.designType),y=Number(o.candidateCount),m=Number.isFinite(y)?Math.max(1,Math.floor(y)):u.length,h=u.slice(0,m),g=[];for(let b=0;b<h.length;b++){const A=h[b],v=Ns({configId:a,blocks:A.blocks}),S=v.ok?v.total:1/0,_=v.ok?Zd(v.terms):{},M=Yd(c,A.blocks,12),G=v.ok?Array.isArray(v.terms)?v.terms:[]:[];g.push({idx:b+1,...A,ok:v.ok,merit:S,deltaMerit:S-f,metrics:_,diffs:M,baseTerms:p,candTerms:G})}let w=g[0];for(const b of g)b.merit<w.merit&&(w=b);try{$d({version:1,createdAt:Date.now(),configId:String(a),recommendation:{idx:w.idx,title:w.title,blocks:Pt(w.blocks)},candidates:g.map(b=>({idx:b.idx,title:b.title,merit:b.merit}))})}catch{}const R=[];R.push("=== Suggest (B案: Candidates + Compare) ==="),R.push(`Time: ${zd()}`),R.push(`Config: ${s.name||"(unnamed)"} (ID: ${String(s.id)})`),R.push(`Blocks: ${rm(c)}`),R.push(""),R.push(`Current Merit: ${nt(f)}`),p.length>0&&R.push(`Current Top: ${Ua(p,Number(o.topK)||2)}`),R.push(`Suggest Options: designType=${Wa(o.designType)}, candidates=${h.length}, outputMode=${String(o.outputMode||"compact")}, topK=${o.topK}, improveK=${o.improveK}, regressK=${o.regressK}`),R.push(`Heuristic: Δc small=${nt(i.small)}, large=${nt(i.large)} (from current lens curvatures)`),R.push("");for(const b of g)String(o.outputMode||"").toLowerCase()==="detailed"?R.push(nm(b,{topK:o.topK,improveK:o.improveK,regressK:o.regressK})):R.push(om(b,{topK:o.topK,improveK:o.improveK,regressK:o.regressK})),R.push("");R.push(`Recommendation: Candidate ${w.idx} (${w.title})`),R.push("Next: このCandidateを初期値として Optimize を実行（数値V→収束後 material V）。");try{const b=at(w.blocks);if(b?.issues?.length){R.push(""),R.push("Expand Issues (best candidate):");for(const A of b.issues.slice(0,8))R.push(`- [${A.severity}] ${A.phase}: ${A.message}`);b.issues.length>8&&R.push(`- ... (${b.issues.length-8} more)`)}}catch{}const N=R.join(`
`),I=Ud();I?I.value=N:(console.log(N),alert("Suggest 出力先(#system-data)が見つからないため、consoleに出力しました。"))}typeof window<"u"&&(window.SuggestDesignIntent={run:im,applyLastRecommendation:async()=>{const t=Wd()?.recommendation?.blocks;return!Array.isArray(t)||t.length===0?{ok:!1,reason:"Suggestion の Recommendation がありません。先に Suggest を実行してください。"}:qd(t)}});async function sm(){console.log("🧪 [benchmarkTFMTF] Function called at",new Date().toISOString());const e=performance.now();console.log("🧪 [Benchmark] TFMTF Through-Focus Performance Analysis"),console.log("=".repeat(80)),console.log("⏱️  Benchmark started at",new Date().toISOString());try{console.log("📦 Importing mtf-plot module...");let t;try{t=(await De(()=>Promise.resolve().then(()=>sa),void 0)).showThroughFocusMTFDiagram,console.log("✅ Successfully imported showThroughFocusMTFDiagram")}catch(w){throw console.error("❌ Failed to import mtf-plot.ts:",w),new Error(`Module import failed: ${w}`)}if(!t)throw new Error("showThroughFocusMTFDiagram function not found in module");let n=document.getElementById("tfmtf-benchmark-container");if(!n){n=document.createElement("div"),n.id="tfmtf-benchmark-container",n.style.width="100%",n.style.height="500px",n.style.marginTop="20px",n.style.border="1px solid #ccc",n.style.padding="10px";const w=document.getElementById("mtf-container");w&&w.parentElement?w.parentElement.appendChild(n):document.body.appendChild(n)}console.log(`📊 Running TFMTF benchmark with different worker configurations...
`),console.log("📈 Configuration 1: Baseline (128×128, 5 steps)");const o=performance.now(),r=[];try{await Rr(t({wavelengthMicrons:.5876,objectIndex:0,targetFrequencyLpmm:30,defocusMinMm:-.05,defocusMaxMm:.05,steps:5,samplingSize:128,zeroPadTo:256,containerElement:n,onProgress:w=>{const R={percent:w.percent,message:w.message??""};r.push(w),Fs("Sequential",R)}}),6e4)}catch(w){console.error("❌ Sequential benchmark failed:",w);const N=performance.now()-o;return console.log(`⏱️  Sequential took ${N.toFixed(2)} ms (before error)`),{success:!1,error:`Sequential benchmark failed: ${w}`,duration:performance.now()-e,partialResults:{sequential:{duration:N,state:"failed"}},timestamp:new Date().toISOString()}}const a=performance.now()-o;console.log(`✅ Sequential completed in ${a.toFixed(2)} ms
`),console.log("📈 Configuration 2: Second run (128×128, 5 steps)");const c=performance.now(),i=[];try{await Rr(t({wavelengthMicrons:.5876,objectIndex:0,targetFrequencyLpmm:30,defocusMinMm:-.05,defocusMaxMm:.05,steps:5,samplingSize:128,zeroPadTo:256,containerElement:n,onProgress:w=>{const R={percent:w.percent,message:w.message??""};i.push(w),Fs("Parallel",R)}}),6e4)}catch(w){console.error("❌ Parallel benchmark failed:",w);const N=performance.now()-c;return console.log(`⏱️  Parallel took ${N.toFixed(2)} ms (before error)`),{success:!1,error:`Parallel benchmark failed: ${w}`,duration:performance.now()-e,partialResults:{sequential:{duration:a,state:"success"},parallel:{duration:N,state:"failed"}},timestamp:new Date().toISOString()}}const f=performance.now()-c;console.log(`✅ Parallel completed in ${f.toFixed(2)} ms
`),console.log("📈 Configuration 3: Higher-Resolution (256×256, 5 steps)");const p=performance.now();try{await Rr(t({wavelengthMicrons:.5876,objectIndex:0,targetFrequencyLpmm:30,defocusMinMm:-.05,defocusMaxMm:.05,steps:5,samplingSize:256,zeroPadTo:256,containerElement:n,onProgress:w=>{(w.percent%20===0||w.percent===100)&&console.log(`  ${w.percent}%: ${w.message}`)}}),12e4)}catch(w){console.error("❌ Higher-resolution benchmark failed:",w);const N=performance.now()-p;return console.log(`⏱️  Higher-resolution took ${N.toFixed(2)} ms (before error)`),{success:!1,error:`Higher-resolution benchmark failed: ${w}`,duration:performance.now()-e,partialResults:{sequential:{duration:a,state:"success"},parallel:{duration:f,state:"success"},fullResolution:{duration:N,state:"failed"}},timestamp:new Date().toISOString()}}const u=performance.now()-p;console.log(`✅ Higher-resolution completed in ${u.toFixed(2)} ms
`);const y=performance.now()-e,m=a/Math.max(f,1),h=u/Math.max(a,1);console.log("=".repeat(80)),console.log("📊 BENCHMARK SUMMARY"),console.log("=".repeat(80)),console.log(`Config 1 - 128×128, 11 steps: ${a.toFixed(2)} ms`),console.log(`Config 2 - 128×128, 11 steps: ${f.toFixed(2)} ms`),console.log(`Run-to-run variance:          ${m.toFixed(2)}× (expect ~1.0)`),console.log(""),console.log(`Config 3 - 256×256, 11 steps: ${u.toFixed(2)} ms`),console.log(`256/128 scaling factor:       ${h.toFixed(2)}× (expect ~4× for O(N²) FFT)`),console.log(""),console.log(`Total benchmark time:         ${y.toFixed(2)} ms`),console.log(""),console.log("📝 Phase 1 (Rust FFT) active. Phase 2 worker parallelization pending PSF refactor."),console.log("=".repeat(80));const g={success:!0,config1:{duration:a,steps:5,resolution:128,zeroPadTo:256},config2:{duration:f,steps:5,resolution:128,zeroPadTo:256},config3:{duration:u,steps:5,resolution:256,zeroPadTo:256},runVariance:m,resolutionScaling:h,metadata:{timestamp:new Date().toISOString(),platform:navigator.userAgent,hardwareConcurrency:navigator.hardwareConcurrency||"unknown"}};return console.log("📦 Benchmark Results:",g),typeof window<"u"&&(window.__benchmarkResult=g),g}catch(t){console.error("❌ [Benchmark] Error during TFMTF benchmark:",t);const n=t instanceof Error?t.message:String(t),o=performance.now()-e,r={success:!1,error:n,duration:o,timestamp:new Date().toISOString(),platform:navigator.userAgent,hardwareConcurrency:navigator.hardwareConcurrency||"unknown"};return console.log("📦 Benchmark Error Result:",r),r}}function Fs(e,t){if(t.percent%10===0||t.percent===100){const n=t.message?` - ${t.message}`:"";console.log(`  ${e}: ${t.percent}%${n}`)}}async function am(){console.log("🧪 [benchmarkTFMTFQuick] Function called at",new Date().toISOString()),console.log("🧪 [Benchmark] Quick TFMTF Test (No MTF computation)"),console.log("=".repeat(80));const e=performance.now();try{console.log("Test 1: Importing mtf-plot module...");const t=await De(()=>Promise.resolve().then(()=>sa),void 0);console.log("✅ mtf-plot imported"),t.showThroughFocusMTFDiagram?console.log("✅ showThroughFocusMTFDiagram found"):console.warn("⚠️  showThroughFocusMTFDiagram is falsy"),console.log("Test 2: Checking worker pool...");const n=await De(()=>Promise.resolve().then(()=>Gl),void 0);if(console.log("✅ tfmtf-worker-pool imported"),n.getGlobalTFMTFWorkerPool){console.log("✅ getGlobalTFMTFWorkerPool found");const a=n.getGlobalTFMTFWorkerPool();console.log("   Worker pool instance:",a?"✅ initialized":"⚠️  not initialized")}console.log("Test 3: Checking PSF serialization...");const o=await De(()=>import("./psf-serialization-CPSFDk_1.js"),[]);console.log("✅ psf-serialization imported"),o.extractPSFGridFromCalculatorResult&&console.log("✅ PSF serialization utilities found");const s={success:!0,duration:performance.now()-e,timestamp:new Date().toISOString(),tests:{mtfModule:!0,workerPool:!0,psfSerialization:!0}};return console.log("=".repeat(80)),console.log("✅ Quick test passed! Result:",JSON.stringify(s)),typeof window<"u"&&(window.__benchmarkResult=s),s}catch(t){return console.error("❌ Quick test failed:",t),{success:!1,duration:performance.now()-e,error:String(t),timestamp:new Date().toISOString()}}}async function Rr(e,t){let n;const o=new Promise((r,s)=>{n=setTimeout(()=>{s(new Error(`Operation timed out after ${t}ms`))},t)});try{return await Promise.race([e,o])}finally{n&&clearTimeout(n)}}if(typeof window<"u"){const e=sm,t=am;window.__tfmtfBenchmark=e,window.__tfmtfBenchmarkQuick=t,window.__tfmtfBenchmarkTest=function(){return console.log("✅ __tfmtfBenchmarkTest called"),{test:!0,timestamp:new Date().toISOString()}}}const Xe=window,ie=globalThis.console;let cm=0,Xn=null,pn=null,Kn=null;async function Ha(e={}){const t=e.forceRustWasm===!0,n=e.requireRustWasm===!0,o=`${t?"force":"auto"}:${n?"require":"allow"}`,r=Date.now();if(pn&&pn.forceKey===o&&r-pn.at<3e3)return pn.options;if(Kn)return Kn;Kn=(async()=>{const s=(()=>{try{return typeof window<"u"&&window.__COOPT_ALLOW_TS_ANALYSIS_FALLBACK===!0}catch{return!1}})(),a=(()=>{if(t||!s)return!0;try{return!(typeof window<"u"&&window.__COOPT_DISABLE_RUST_WASM_ANALYSIS===!0)}catch{return!0}})(),c=(()=>{if(!s||n)return!0;try{return typeof window<"u"&&window.__COOPT_REQUIRE_RUST_WASM_ANALYSIS===!0}catch{return!1}})();if(!a)return pn={options:null,at:Date.now(),forceKey:o},null;try{const i=await De(()=>import("./toolbar-collapsed-storage-oKonxCdL.js").then(p=>p.bO),__vite__mapDeps([0,1]));if(i.getRustRayTracingWasmSync?.()||await i.preloadRustRayTracingWasm?.()){const p={useRustWasm:!0,requireRustWasm:c,allowNonStrict:!c};try{window.__COOPT_LAST_ANALYSIS_BACKEND={kind:"rust-wasm",forced:t,at:Date.now()},ie.warn("🧭 [Analysis Backend] Rust-WASM")}catch{}return pn={options:p,at:Date.now(),forceKey:o},p}const f=typeof i.getRustRayTracingWasmInitError=="function"?i.getRustRayTracingWasmInitError():null;if(c)throw new Error(`Rust WASM is unavailable${f?` (${f})`:""}`);try{window.__COOPT_LAST_ANALYSIS_BACKEND={kind:"js",detail:f||"Rust WASM unavailable",forced:t,at:Date.now()},ie.warn(`🧭 [Analysis Backend] JavaScript fallback${f?` (${f})`:""}`)}catch{}return pn={options:null,at:Date.now(),forceKey:o},null}catch(i){if(c)throw i;try{const l=String(i?.message||i||"Rust WASM init failed");window.__COOPT_LAST_ANALYSIS_BACKEND={kind:"js",detail:l,forced:t,at:Date.now()},ie.warn(`🧭 [Analysis Backend] JavaScript fallback (${l})`)}catch{}return pn={options:null,at:Date.now(),forceKey:o},null}})();try{return await Kn}finally{Kn=null}}function Ga(e,t,n=!1){const o=Number(t);if(!Array.isArray(e))return[];const r=e.map(f=>f&&typeof f=="object"?{...f}:f);if(!Number.isFinite(o)||Math.abs(o)<1e-15)return r;const s=r.findIndex(f=>f&&(f["object type"]==="Image"||f.object==="Image")),a=s>0?s-1:Math.max(0,r.length-2);if(a<0||a>=r.length)return r;const c=r[a]&&typeof r[a]=="object"?{...r[a]}:{},i=Number(c.thickness),l=Number.isFinite(i)?i:0;return c.thickness=l+o,r[a]=c,r}function lm(){const e=Dt();if(!e)return;ie.log("🧹 Clearing all drawing elements...");const t=[];e.children.forEach(n=>{n.type!=="AmbientLight"&&n.type!=="DirectionalLight"&&t.push(n)}),t.forEach(n=>{e.remove(n),n.geometry&&n.geometry.dispose(),n.material&&(Array.isArray(n.material)?n.material.forEach(o=>o.dispose()):n.material.dispose())}),ie.log(`✅ Cleared ${t.length} objects from scene`)}async function Ur(e={}){const t=e&&typeof e=="object"?e.forceRustWasmTrace===!0:!1,n=e&&typeof e=="object"?e.requireRustWasmTrace===!0:!1;ie.log("🎯 Starting spot diagram generation...");const o=()=>{try{if(typeof performance<"u"&&typeof performance.now=="function")return performance.now()}catch{}return Date.now()},r=o(),s=++cm,a={requestId:s,startedAt:Date.now(),backend:null,input:null,result:null,timingsMs:{resolveBackend:0,generateData:0,render:0,total:0},status:"running",error:null};try{if(typeof window<"u"&&Xe.__configurationSwitching===!0){Xn={requestId:s,options:e,requestedAt:Date.now()},ie.warn(`⚠️ Spot diagram requested during configuration switching; queued request ${s}`),setTimeout(()=>{try{typeof window<"u"&&Xe.__configurationSwitching===!0||Ur(e).catch(()=>{})}catch{}},50);return}}catch{}const c=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;e&&typeof e=="object"&&typeof e.chiefRayDefinition=="string"&&e.chiefRayDefinition,e&&typeof e=="object"&&e.logChiefRayDefinition,e&&typeof e=="object"&&e.useActiveConfigSnapshot,e&&typeof e=="object"&&e.configId;let i="spot-diagram-container";if(e&&typeof e=="object"&&(e.containerElement?i=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(i=e.containerId)),Wc()){Xn={requestId:s,options:e,requestedAt:Date.now()},ie.warn(`⚠️ Spot diagram generation already in progress; queued request ${s}`);return}try{_i(!0);try{c?.({percent:0,message:"Preparing spot diagram..."})}catch{}const l=Number.isInteger(e?.surfaceIndex)?e.surfaceIndex:null,f=Number.isInteger(e?.surfaceRowIndex)?e.surfaceRowIndex:null,p=e&&typeof e=="object"&&e.surfaceRowId!==void 0&&e.surfaceRowId!==null?String(e.surfaceRowId).trim():"",d=e&&typeof e=="object"&&e.surfaceRowSig!==void 0&&e.surfaceRowSig!==null?String(e.surfaceRowSig).trim():"",u=e&&typeof e=="object"?e.surfaceIsImage===!0:!1,y=Number.isInteger(e?.rayCount)?e.rayCount:null,m=Number.isFinite(e?.wavelengthNm)?e.wavelengthNm:null,h=Number.isInteger(e?.ringCount)?e.ringCount:null,g=document.getElementById("surface-number-select"),w=document.getElementById("ray-count-input"),R=document.getElementById("wavelength-input"),N=document.getElementById("ring-count-select"),A=(()=>{try{if(typeof localStorage>"u")return"";const P=Ot();return P&&P.activeConfigId!==void 0&&P.activeConfigId!==null?String(P.activeConfigId).trim():""}catch{return""}})();let v=0,S=501,_=550,M=3;if(l!==null&&l>=0)v=l,ie.log(`📊 Using surface id from options: ${v}`);else if(g&&g.value!=="")v=parseInt(g.value,10),ie.log(`📊 Using surface id from select: ${v}`);else{ie.warn("⚠️ Surface select not found, using default (image surface)");const P=Bt(),O=Je(P);O&&O.length>0?(v=O.length-1,ie.log(`📊 Using last surface (fallback) as default: ${v}`)):ie.warn("⚠️ No optical system data available for default surface calculation")}y!==null&&y>0?S=y:w&&w.value!==""?S=parseInt(w.value)||501:ie.warn("⚠️ Ray count input not found, using default (501)");const G=P=>{if(P===!0||P===1)return!0;const O=String(P??"").trim().toLowerCase();return O.includes("primary")||O==="true"||O==="yes"||O==="1"},z=()=>{try{const P=typeof window<"u"&&Xe.tableSource&&typeof Xe.tableSource.getData=="function"?Xe.tableSource.getData():null;if(!Array.isArray(P)||P.length===0)return null;const O=P.map(k=>({wlUm:Number(k?.wavelength),isPrimary:G(k?.primary)})).filter(k=>Number.isFinite(k.wlUm)&&k.wlUm>0),X=O.find(k=>k.isPrimary)||O[0]||null;return X?Number(X.wlUm)*1e3:null}catch{return null}},E=(()=>{if(m!==null&&m>0)return m;const P=z();return Number.isFinite(P)&&P>0?P:null})();if(E!==null&&E>0?_=E:ie.log("ℹ️ No explicit wavelength option, will use Source primary wavelength after rows load"),h!==null&&h>0)M=h;else if(N&&N.value!==""){const P=parseInt(N.value,10);M=Number.isInteger(P)&&P>0?P:3}else ie.warn("⚠️ Ring count select not found, using default (3)");(isNaN(v)||v<0)&&(v=0,ie.warn("⚠️ Invalid surface id, using default (0)"));const ne=(()=>{const P=String(e?.pattern||"").trim().toLowerCase();if(P==="grid"||P==="annular")return P;try{const k=Yr();if(k==="grid"||k==="annular")return k}catch{}const O=document.getElementById("annular-pattern-btn"),X=document.getElementById("grid-pattern-btn");if(X&&X.classList.contains("active"))return"grid";if(O&&O.classList.contains("active"))return"annular";try{if(typeof window<"u"&&typeof Xe.getRayEmissionPattern=="function"){const k=String(Xe.getRayEmissionPattern()||"").trim().toLowerCase();if(k==="grid"||k==="annular")return k}}catch{}try{const k=Fi(),Y=k&&A?k[A]:null,D=Y&&typeof Y.pattern=="string"?Y.pattern.trim().toLowerCase():"";if(D==="grid"||D==="annular")return D}catch{}return"annular"})();try{typeof window<"u"&&typeof Xe.setRayEmissionPattern=="function"&&Xe.setRayEmissionPattern(ne)}catch{}const V=v;ie.log(`🎯 Generating spot diagram for surfaceId ${V}, ${S} rays, ${_}nm, ring count ${M}`);const K=()=>{if(!A){ie.log("🔍 [Load Rows] Using CURRENT tables (no config selected)");const U=Bt(),ee=Qt(),oe=en(),ae={opticalSystemRows:Je(U),objectRows:ht(ee),sourceRows:Tt(oe)};return ie.log(`🔍 [Load Rows] Current: opticalSystem=${ae.opticalSystemRows?.length}, objects=${ae.objectRows?.length}`),ae}ie.log(`🔍 [Load Rows] Loading Config "${A}"`);const O=U=>!!U&&typeof U=="object"&&!Array.isArray(U),X=U=>{try{return JSON.parse(JSON.stringify(U))}catch{return null}};try{if(typeof localStorage>"u")throw new Error("localStorage unavailable");const U=Ot(),ee=Array.isArray(U?.configurations)?U.configurations.find(oe=>String(oe?.id)===String(A)):null;ee?ie.log(`🔍 [Config Content] Config "${A}":`,{hasBlocks:Array.isArray(ee.blocks)&&ee.blocks.length>0,blockCount:ee.blocks?.length||0,hasOpticalSystem:Array.isArray(ee.opticalSystem)&&ee.opticalSystem.length>0,opticalSystemLength:ee.opticalSystem?.length||0,hasObject:Array.isArray(ee.object)&&ee.object.length>0,objectLength:ee.object?.length||0,firstOpticalSurface:ee.opticalSystem?.[0]||null}):ie.error(`❌ [Config Not Found] Config "${A}" not found in localStorage`)}catch(U){ie.error("❌ [Config Read Error]:",U)}const k=U=>{const ee=String(U??""),oe=ee.indexOf(".");if(oe<=0)return null;const ae=ee.slice(0,oe),Ae=ee.slice(oe+1);return!ae||!Ae?null:{blockId:ae,key:Ae}},Y=U=>Array.isArray(U)?U.map(ee=>{if(!ee||typeof ee!="object")return ee;const oe={...ee};return oe.xHeightAngle==null&&oe["object x"]!=null&&(oe.xHeightAngle=oe["object x"]),oe.yHeightAngle==null&&oe["object y"]!=null&&(oe.yHeightAngle=oe["object y"]),oe.xHeightAngle==null&&oe.x!=null&&(oe.xHeightAngle=oe.x),oe.yHeightAngle==null&&oe.y!=null&&(oe.yHeightAngle=oe.y),oe.position==null&&oe.objectType!=null&&(oe.position=oe.objectType),oe}):[],D=(U,ee)=>{const oe=X(U);if(!Array.isArray(oe))return Array.isArray(U)?U:[];if(!O(ee))return oe;const ae=new Map;for(const Ae of oe){const te=O(Ae)?String(Ae.blockId??""):"";te&&ae.set(te,Ae)}for(const[Ae,te]of Object.entries(ee)){const re=k(Ae);if(!re)continue;const se=ae.get(String(re.blockId));if(!se||!O(se.parameters))continue;const ue=Number(te);se.parameters[re.key]=Number.isFinite(ue)?ue:te}return oe};try{const U=typeof localStorage>"u"?null:Ot(),ee=U&&U.activeConfigId!==void 0&&U.activeConfigId!==null?String(U.activeConfigId):"",oe=(()=>{try{return typeof window<"u"&&Xe.__configurationSwitching===!0}catch{return!1}})();if(ee&&String(ee)===String(A)&&!oe){ie.log("🔍 [Load Rows] Selected config is ACTIVE; using current tables instead of snapshot");const re=Bt(),se=Qt(),ue=en(),me={opticalSystemRows:Je(re),objectRows:ht(se),sourceRows:Tt(ue)};try{const ge=Array.isArray(U?.configurations)?U.configurations.find(ve=>String(ve?.id)===String(A)):null,xe=Y(Array.isArray(ge?.object)?ge.object:[]),W=Y(Array.isArray(me.objectRows)?me.objectRows:[]),fe=ve=>String(ve?.id??""),we=ve=>{const Ce=new Map;for(const Te of ve)if(Te&&typeof Te=="object"){const Ie=fe(Te);Ie&&Ce.set(Ie,Te)}return Ce},Se=we(W),ye=we(xe),ce="2",pe=Se.get(ce),he=ye.get(ce),_e=ve=>String(ve??"").trim().toLowerCase();if(pe&&he){const ve=_e(pe.position),Ce=_e(he.position),Te=Number(pe.yHeightAngle??pe.y??pe["object y"]),Ie=Number(he.yHeightAngle??he.y??he["object y"]);if(ve!==Ce||Number.isFinite(Te)&&Number.isFinite(Ie)&&Math.abs(Te-Ie)>1e-9)return ie.warn("⚠️ [Load Rows] Live Object table differs from config snapshot; using snapshot for Object rows",{live:{position:pe.position,yHeightAngle:pe.yHeightAngle},snapshot:{position:he.position,yHeightAngle:he.yHeightAngle}}),{...me,objectRows:xe}}}catch{}return me}const ae=Array.isArray(U?.configurations)?U.configurations.find(re=>String(re?.id)===String(A)):null;try{if(ae&&typeof window<"u"&&Xe.__cooptOpticalSystemByConfigId){const re=Xe.__cooptOpticalSystemByConfigId[String(A)];if(Array.isArray(re)&&re.length>0)return ie.log(`🔍 [Cache Hit] Config "${A}": Using cached opticalSystemRows (${re.length} surfaces)`),{opticalSystemRows:JSON.parse(JSON.stringify(re)),objectRows:Y(Array.isArray(ae?.object)?ae.object:[]),sourceRows:(()=>{try{const ue=bn();return Array.isArray(ue)?ue:[]}catch{return[]}})()};ie.log(`🔍 [Cache Miss] Config "${A}": Cache empty or invalid, will expand blocks`)}else ie.log(`🔍 [No Cache] Config "${A}": No cache object found, will expand blocks`)}catch{}ie.log(`🔍 [Block Expansion] Config "${A}": Expanding blocks to optical system`);const Ae=(()=>{try{if(!ae||!Array.isArray(ae.blocks)||ae.blocks.length===0)return ie.log(`⚠️ [Block Expansion] Config "${A}": No blocks found (cfg=${!!ae}, blocks=${ae?.blocks?.length})`),null;const re=Array.isArray(ae.scenarios)?ae.scenarios:null,se=ae.activeScenarioId?String(ae.activeScenarioId):"",ue=se&&re?re.find(W=>W&&String(W.id)===String(se)):null,me=ue&&O(ue.overrides)?ue.overrides:null,ge=me?D(ae.blocks,me):ae.blocks,xe=at(ge);if(ie.log(`🔍 [Block Expansion] Config "${A}": exp=${!!xe}, exp.rows=${xe?.rows?.length}`),!xe||!Array.isArray(xe.rows))return ie.log(`⚠️ [Block Expansion] Config "${A}": Block expansion failed`),null;try{{const fe=Array.isArray(ae?.opticalSystem)?ae.opticalSystem:null,we=xe.rows,Se=pe=>String(pe?.["object type"]??pe?.object??"").trim().toLowerCase(),ye=pe=>{if(!pe)return null;const he=String(pe);return Array.isArray(ge)?ge.find(_e=>_e&&String(_e.blockId)===he):null},ce=pe=>{const _e=ye(pe)?.parameters?.semiDiameter,ve=Number(_e);return Number.isFinite(ve)&&ve>0?ve:null};if(fe&&we.length>0){const pe=fe[0];String(pe?.semidia??"").trim()!==""&&(we[0]={...we[0],semidia:pe.semidia});const _e=Math.min(fe.length,we.length);for(let ve=0;ve<_e;ve++){const Ce=fe[ve],Te=we[ve];if(!Ce||typeof Ce!="object"||!Te||typeof Te!="object")continue;const Ie=Ce.semidia;if(String(Ie??"").trim()==="")continue;const be=Se(Te);be!=="image"&&(be==="stop"&&ce(Te._blockId)!==null||(Te.semidia=Ie))}}}}catch{}try{const W=xe.rows;if(!(Array.isArray(ae?.blocks)&&ae.blocks.some(we=>String(we?.blockType??"").trim()==="ObjectSurface"))){const we=Array.isArray(ae?.opticalSystem)?ae.opticalSystem[0]:null;W.length>0&&we?(ie.log(`🔧 [Object Row Restore] Config "${ae?.name||ae?.id}": Using saved Object row`),ie.log(`  Old Object: thickness=${W[0]?.thickness}, fieldX=${W[0]?.fieldX}, fieldY=${W[0]?.fieldY}`),ie.log(`  New Object: thickness=${we?.thickness}, fieldX=${we?.fieldX}, fieldY=${we?.fieldY}`),W[0]={...W[0],...we}):ie.log(`⚠️ [Object Row Restore] Config "${ae?.name||ae?.id}": No saved Object row to restore (hasRows=${W.length>0}, hasLegacyObject=${!!we})`)}}catch(W){ie.error("❌ [Object Row Restore Error]:",W)}try{const W=xe.rows;if(ie.log(`🔍 [Image Semidia Override Check] Config "${ae?.name||ae?.id}", rows.length=${W.length}`),W.length>0){const fe=Bt(),we=Je(fe);ie.log(`🔍 [Image Semidia Override] currentOpticalRows.length=${we?.length}`);const Se=pe=>String(pe?.["object type"]??pe?.object??"").trim().toLowerCase(),ye=we?.findIndex(pe=>Se(pe)==="image"),ce=W.findIndex(pe=>Se(pe)==="image");if(ie.log(`🔍 [Image Semidia Override] currentImageIdx=${ye}, configImageIdx=${ce}`),ye>=0&&ce>=0){const pe=we[ye]?.semidia,he=W[ce]?.semidia;if(ie.log(`🔍 [Image Semidia Override] current=${pe}, config=${he}`),pe!=null&&String(pe).trim()!==""){const _e=Number(pe);ie.log(`🔍 [Image Semidia Override] currentVal=${_e}, isFinite=${Number.isFinite(_e)}, gt0=${_e>0}`),Number.isFinite(_e)&&_e>0&&(ie.log(`🔧 [Image Semidia Override] Config "${ae?.name||ae?.id}", surface ${ce}: ${he} → ${pe} (FORCED)`),W[ce]={...W[ce],semidia:pe})}}}}catch(W){ie.error("❌ [Image Semidia Override Error]",W)}try{const W=xe.rows,fe=Bt(),we=Je(fe);ie.log(`📊 [SEMIDIA COMPARISON] Config "${ae?.name||ae?.id}"`);const Se=Math.max(W?.length||0,we?.length||0);for(let ye=0;ye<Se;ye++){const ce=W?.[ye],pe=we?.[ye],he=ce?.semidia,_e=pe?.semidia,ve=ce?.surfType||ce?.["object type"]||ce?.object,Ce=pe?.surfType||pe?.["object type"]||pe?.object;he!==_e?ie.log(`  ⚠️ Surface ${ye} (${ve}): config=${he}, current=${_e}`):ie.log(`  ✅ Surface ${ye} (${ve}): ${he}`)}}catch(W){ie.error("❌ [SEMIDIA COMPARISON Error]",W)}return xe.rows}catch{return null}})(),te={opticalSystemRows:Array.isArray(Ae)?Ae:Array.isArray(ae?.opticalSystem)?ae.opticalSystem:[],objectRows:Y(Array.isArray(ae?.object)?ae.object:[]),sourceRows:(()=>{try{const re=bn();return Array.isArray(re)?re:[]}catch{return[]}})()};return ie.log(`🔍 [Load Result] Config "${A}": opticalSystem=${te.opticalSystemRows?.length} (from ${Array.isArray(Ae)?"BLOCK EXPANSION":"DIRECT cfg.opticalSystem"}), objects=${te.objectRows?.length}`),te}catch(U){ie.warn("⚠️ Failed to load Spot Diagram config snapshot, falling back to active tables:",U);const ee=Bt(),oe=Qt(),ae=en();return{opticalSystemRows:Je(ee),objectRows:ht(oe),sourceRows:Tt(ae)}}};let{opticalSystemRows:$,objectRows:J,sourceRows:Q}=K();const Z=(P=>{if(E!==null&&E>0)return[{id:"spot-explicit-wavelength",wavelength:Number(E)/1e3,weight:1,primary:"Primary Wavelength",name:"Explicit Wavelength"}];if(!Array.isArray(P)||P.length===0)return[{id:"spot-default-wavelength",wavelength:.5876,weight:1,primary:"Primary Wavelength",name:"Default d-line"}];const O=P.map((k,Y)=>({idx:Y,row:k,wl:Number(k?.wavelength),isPrimary:G(k?.primary)})).filter(k=>Number.isFinite(k.wl)&&k.wl>0),X=O.find(k=>k.isPrimary)||O[0]||null;return X?[{...X.row&&typeof X.row=="object"?X.row:{},wavelength:Number(X.wl),weight:Number.isFinite(Number(X.row?.weight))?Number(X.row.weight):1,primary:"Primary Wavelength"}]:[{id:"spot-default-wavelength",wavelength:.5876,weight:1,primary:"Primary Wavelength",name:"Default d-line"}]})(Q),B=Number(Z[0]?.wavelength)||.5876;if(_=B*1e3,$&&$.length>20){const P=$[20];ie.log("🔍 [IMAGE SURFACE DEBUG] Index=20:",{surfType:P.surfType||P["surf type"],objectType:P["object type"]||P.objectType,semidia:P.semidia,radius:P.radius,thickness:P.thickness})}J&&J.length>0?(ie.log(`🔍 [Object Debug] objectRows.length=${J.length}`),J.forEach((P,O)=>{ie.log(`🔍 [Object Debug] Object ${O+1}:`,{id:P.id,position:P.position,"object x":P["object x"],"object y":P["object y"],angle:P.angle,"decenter y":P["decenter y"],vignetting:P.vignetting})})):ie.warn(`⚠️ [Object Debug] No objectRows found for config "${A}"`);let C=null;const F=P=>{try{const O=U=>String(U??"").trim().toLowerCase(),X=U=>{const ee=Number(U);return Number.isFinite(ee)?String(ee):O(U)},k=P?.["object type"]??P?.objectType??P?.object??"",Y=P?.surfType??P?.["surf type"]??P?.type??"";return[`t:${O(k||Y||"Standard")}`,`r:${X(P?.radius??P?.R??"")}`,`th:${X(P?.thickness??P?.T??"")}`,`sd:${X(P?.semidia??P?.semiDia??"")}`,`m:${O(P?.material??P?.glass??P?.glass??P?.refractiveIndex??"")}`,`c:${O(P?.comment??P?.name??"")}`].join("|")}catch{return""}};if(p){const P=Array.isArray($)?$.findIndex(O=>O&&O.id!==void 0&&O.id!==null&&String(O.id)===p):-1;Number.isInteger(P)&&P>=0?(C=P,ie.log(`✅ [Surface Resolution] Using explicit surfaceRowId=${p} -> rowIndex=${C}`)):ie.warn(`⚠️ [Surface Resolution] surfaceRowId=${p} not found in current rows`)}if(C===null&&d){const P=Array.isArray($)?$.findIndex(O=>F(O)===d):-1;Number.isInteger(P)&&P>=0?(C=P,ie.log(`✅ [Surface Resolution] Using explicit surfaceRowSig -> rowIndex=${C}`)):ie.warn("⚠️ [Surface Resolution] surfaceRowSig not found in current rows")}if(Number.isInteger(f)&&f!==null&&(f>=0&&f<$.length?C===null&&(C=f,ie.log(`✅ [Surface Resolution] Using explicit surfaceRowIndex=${C}`)):ie.warn(`⚠️ [Surface Resolution] Ignoring out-of-range surfaceRowIndex=${f} (rows=${$.length})`)),C===null&&u){const P=Array.isArray($)?$.findIndex(O=>{const X=String(O?.["object type"]??O?.objectType??O?.object??"").toLowerCase(),k=String(O?.surfType??O?.["surf type"]??O?.type??"").toLowerCase();return X.includes("image")||k.includes("image")}):-1;Number.isInteger(P)&&P>=0?(C=P,ie.log(`✅ [Surface Resolution] Using explicit Image selection -> rowIndex=${C}`)):ie.warn("⚠️ [Surface Resolution] Explicit Image selection requested but no Image row found")}try{const{generateSurfaceOptions:P}=await De(async()=>{const{generateSurfaceOptions:Y}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(D=>D.bU);return{generateSurfaceOptions:Y}},__vite__mapDeps([0,1])),O=P($||[]);if(u&&Array.isArray(O)&&O.length>0){const Y=O.find(D=>{const U=String(D?.label??"").toLowerCase();return U.includes("(image)")||U.includes(" image")});Y&&Number.isInteger(Y.rowIndex)?(C=Y.rowIndex,ie.log(`✅ [Surface Resolution] Explicit Image selection mapped via options -> rowIndex=${C} (${Y.label})`)):Number.isInteger(O[O.length-1]?.rowIndex)&&(C=O[O.length-1].rowIndex,ie.warn(`⚠️ [Surface Resolution] Explicit Image selection fallback -> last option rowIndex=${C}`))}const X=zs($);ie.log(`🔍 [Surface Resolution] Conjugate: ${X}, Looking for surfaceId=${V} in ${O.length} options`),ie.log("🔍 [Surface Options Sample]:",O.slice(0,3).map(Y=>({surfaceId:Y.surfaceId,value:Y.value,rowIndex:Y.rowIndex,label:Y.label}))),C===null&&(C=(()=>{const Y=O.find(U=>typeof U?.label=="string"&&U.label.includes("(Image)")),D=O.find(U=>Number(U?.surfaceId)===Number(V));return D&&Number.isInteger(D.rowIndex)?(ie.log(`✅ [Surface Resolution] Matched surfaceId=${V} → rowIndex=${D.rowIndex} (${D.label})`),D.rowIndex):(ie.warn(`⚠️ [Surface Resolution] surfaceId=${V} not found in current config; fallback to Image/last`),Y&&Number.isInteger(Y.rowIndex)?(ie.warn(`⚠️ [Surface Resolution] Using Image surface at rowIndex=${Y.rowIndex}`),Y.rowIndex):O.length>0&&Number.isInteger(O[O.length-1].rowIndex)?(ie.warn(`⚠️ [Surface Resolution] Using last surface at rowIndex=${O[O.length-1].rowIndex}`),O[O.length-1].rowIndex):null)})()),Number.isInteger(C)&&ie.log(`✅ [Surface Resolution] ${X} resolved: surfaceId=${V} → rowIndex=${C}`)}catch(P){C=Number.isInteger(V)?V:0,ie.error("❌ [Surface Resolution] Error:",P)}Number.isInteger(C)&&C!==null&&(v=C),(C===null||!Number.isInteger(C)||C<0)&&(C=0),v=C;try{const P=ne;let O=B;E!==null&&E>0||(_=O*1e3),zc({surfaceId:V,surfaceRowIndex:v,rayCount:S,ringCount:M,pattern:P||null,primaryWavelengthUm:O,configId:A||null,updatedAt:Date.now()});try{let X=A?String(A).trim():"";if(!X){if(typeof localStorage>"u")return;const k=Ot();X=k&&k.activeConfigId!==void 0&&k.activeConfigId!==null?String(k.activeConfigId).trim():""}if(X){const k=Fi();k[X]={surfaceIndex:v,surfaceId:V,surfaceRowIndex:v,rayCount:S,ringCount:M,pattern:P||null,primaryWavelengthUm:O,configId:X,updatedAt:Date.now()},$c(k),typeof window<"u"&&((!Xe.__cooptSpotDiagramSettingsByConfigId||typeof Xe.__cooptSpotDiagramSettingsByConfigId!="object")&&(Xe.__cooptSpotDiagramSettingsByConfigId={}),Xe.__cooptSpotDiagramSettingsByConfigId[X]=k[X])}}catch{}}catch{}if(ie.log("📊 Retrieved data:",{configId:A||"(Current)"}),ie.log("  - opticalSystemRows:",$?$.length:"null",$),$&&$.length>0?$.forEach((P,O)=>{ie.log(`    [${O}]`,P)}):(ie.warn("⚠️ opticalSystemRows is empty! サンプルデータを自動生成します。"),$=[{surfaceType:"object",radius:"INF",thickness:"INF",refractiveIndex:1,comment:"Object surface"},{surfaceType:"sphere",radius:50,thickness:5,refractiveIndex:1.5,comment:"Lens front"},{surfaceType:"sphere",radius:-50,thickness:10,refractiveIndex:1,comment:"Lens back"},{surfaceType:"image",radius:"INF",thickness:0,refractiveIndex:1,comment:"Image surface"}],ie.log("📊 Generated sample optical system:",$)),ie.log("  - objectRows:",J?J.length:"null",J),ie.log("  - sourceRows:",Q?Q.length:"null",Q),ie.log("  - effectiveSourceRowsForSpot:",Z),$&&$.length>0){const P=$.length-1;v>P&&(ie.warn(`⚠️ Surface index ${v} is too large, using last surface (${P})`),v=P)}const x=v+1,T=o(),j=await Ha({forceRustWasm:t,requireRustWasm:n});if(a.timingsMs.resolveBackend=Math.max(0,o()-T),a.backend=(()=>{try{const P=typeof window<"u"?Xe.__COOPT_LAST_ANALYSIS_BACKEND:null;if(P&&typeof P=="object")return P}catch{}return{kind:j&&j.useRustWasm===!0?"rust-wasm":"js",at:Date.now()}})(),a.input={surfaceId:V,surfaceIndex:v,surfaceNumber:x,rayCount:S,ringCount:M,wavelengthNm:_,pattern:ne,objectCount:Array.isArray(J)?J.length:0,opticalSurfaceCount:Array.isArray($)?$.length:0},ie.log(`🎯 Final surface resolution: surfaceId(input)=${V} → rowIndex=${v} → surfaceNumber=${x}`),ie.log("🎯 Target surface:",$[v]),!$||$.length===0)throw new Error("No optical system data available");if(!J||J.length===0){ie.warn("⚠️ No object data available, creating default object data");const P=[{id:1,height:10,distance:100,angle:0,wavelength:_/1e3,primary:!0}];ie.log("📊 Using default object data:",P);const{generateSpotDiagramAsync:O,drawSpotDiagram:X}=await De(async()=>{const{generateSpotDiagramAsync:U,drawSpotDiagram:ee}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(oe=>oe.bU);return{generateSpotDiagramAsync:U,drawSpotDiagram:ee}},__vite__mapDeps([0,1])),k=o(),Y=await O($,Z,P,x,S,M,{onProgress:c,physicalVignetting:!0,displaySurfaceNumber:V,pattern:ne,traceOptions:j});if(a.timingsMs.generateData+=Math.max(0,o()-k),!Y)throw new Error("Failed to generate spot diagram data");a.result={objectCount:Array.isArray(Y?.spotData)?Y.spotData.length:0,totalRays:Array.isArray(Y?.spotData)?Y.spotData.reduce((U,ee)=>U+(Number(ee?.totalRays)||0),0):0,successfulRays:Array.isArray(Y?.spotData)?Y.spotData.reduce((U,ee)=>U+(Number(ee?.successfulRays)||0),0):0,generationProfile:Y&&typeof Y=="object"&&Y.profile?Y.profile:null};try{c?.({percent:90,message:"Rendering..."})}catch{}const D=o();await X(Y,x,i,_/1e3),a.timingsMs.render+=Math.max(0,o()-D);try{c?.({percent:100,message:"Done"})}catch{}}else{const{generateSpotDiagramAsync:P,drawSpotDiagram:O}=await De(async()=>{const{generateSpotDiagramAsync:D,drawSpotDiagram:U}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(ee=>ee.bU);return{generateSpotDiagramAsync:D,drawSpotDiagram:U}},__vite__mapDeps([0,1])),X=o(),k=await P($,Z,J,x,S,M,{onProgress:c,physicalVignetting:!0,displaySurfaceNumber:V,pattern:ne,traceOptions:j});if(a.timingsMs.generateData+=Math.max(0,o()-X),!k)throw new Error("Failed to generate spot diagram data");a.result={objectCount:Array.isArray(k?.spotData)?k.spotData.length:0,totalRays:Array.isArray(k?.spotData)?k.spotData.reduce((D,U)=>D+(Number(U?.totalRays)||0),0):0,successfulRays:Array.isArray(k?.spotData)?k.spotData.reduce((D,U)=>D+(Number(U?.successfulRays)||0),0):0,generationProfile:k&&typeof k=="object"&&k.profile?k.profile:null},ie.log("📋 [SPOT DIAGRAM] About to call drawSpotDiagram with:",{spotDataType:typeof k,spotDataKeys:k?Object.keys(k):"null",actualSpotDataLength:k.spotData?k.spotData.length:"null",surfaceNumber:x,containerId:typeof i=="string"?i:"(element)",wavelength:_/1e3});try{c?.({percent:90,message:"Rendering..."})}catch{}const Y=o();await O(k,x,i,_/1e3),a.timingsMs.render+=Math.max(0,o()-Y);try{c?.({percent:100,message:"Done"})}catch{}ie.log("✅ [SPOT DIAGRAM] drawSpotDiagram call completed")}a.timingsMs.total=Math.max(0,o()-r),a.status="ok";try{if(typeof window<"u"){Xe.__cooptLastSpotDiagramProfile=a;const O=(Array.isArray(Xe.__cooptSpotDiagramProfileHistory)?Xe.__cooptSpotDiagramProfileHistory:[]).concat([a]);Xe.__cooptSpotDiagramProfileHistory=O.slice(Math.max(0,O.length-20))}}catch{}ie.warn("⏱️ [SPOT PROFILE]",{backend:a?.backend?.kind||"unknown",totalMs:Number(a?.timingsMs?.total||0).toFixed(1),resolveBackendMs:Number(a?.timingsMs?.resolveBackend||0).toFixed(1),generateDataMs:Number(a?.timingsMs?.generateData||0).toFixed(1),generateCloneRowsMs:Number(a?.result?.generationProfile?.timingsMs?.cloneRows||0).toFixed(1),generateStartsMs:Number(a?.result?.generationProfile?.timingsMs?.generateStarts||0).toFixed(1),generateTraceRayMs:Number(a?.result?.generationProfile?.timingsMs?.traceRay||0).toFixed(1),generateNonTraceMs:Number(a?.result?.generationProfile?.timingsMs?.nonTrace||0).toFixed(1),renderMs:Number(a?.timingsMs?.render||0).toFixed(1),traceRayCalls:Number(a?.result?.generationProfile?.counters?.traceRayCalls||0),pupilAttempts:Number(a?.result?.generationProfile?.counters?.pupilAttempts||0),startGenerationCacheHits:Number(a?.result?.generationProfile?.counters?.startGenerationCacheHits||0),startGenerationCacheMisses:Number(a?.result?.generationProfile?.counters?.startGenerationCacheMisses||0),raysTried:Number(a?.result?.generationProfile?.counters?.raysTried||0),objects:Number(a?.result?.objectCount||0),rays:Number(a?.result?.successfulRays||0)+"/"+Number(a?.result?.totalRays||0)}),ie.log("✅ Spot diagram generated successfully")}catch(l){a.timingsMs.total=Math.max(0,o()-r),a.status="error",a.error=String(l?.message||l||"unknown error");try{if(typeof window<"u"){Xe.__cooptLastSpotDiagramProfile=a;const d=(Array.isArray(Xe.__cooptSpotDiagramProfileHistory)?Xe.__cooptSpotDiagramProfileHistory:[]).concat([a]);Xe.__cooptSpotDiagramProfileHistory=d.slice(Math.max(0,d.length-20))}}catch{}ie.error("❌ Error generating spot diagram:",l),ie.error("Error details:",l.stack);const f=typeof i=="string"?document.getElementById(i):i;f&&(f.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Spot diagram error:</strong><br>
                ${l.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Spot diagram error:
${l.message}`)}finally{_i(!1);try{const l=Xn;l&&l.requestId>s?(Xn=null,setTimeout(()=>{Ur(l.options).catch(f=>{ie.error("❌ Error running queued spot diagram request:",f)})},0)):l&&l.requestId===s&&(Xn=null)}catch{}}}async function qa(e={}){try{if(typeof window<"u"&&Xe.__configurationSwitching===!0){ie.warn("⚠️ Through-Focus Spot requested during configuration switching; retrying shortly"),setTimeout(()=>{try{typeof window<"u"&&Xe.__configurationSwitching===!0||qa(e).catch(()=>{})}catch{}},60);return}}catch{}const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;let n="through-focus-spot-container";e&&typeof e=="object"&&(e.containerElement?n=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(n=e.containerId));const o=(i,l)=>{try{t?.({percent:i,message:l})}catch{}},r=(i,l)=>{const f=parseInt(String(i??""),10);return Number.isInteger(f)?f:l},s=(i,l)=>{const f=Number(i);return Number.isFinite(f)?f:l},a=(i,l,f)=>Math.max(l,Math.min(f,i)),c=i=>!Number.isFinite(i)||i<=0?"#1f77b4":i<.45?"#8B00FF":i<.495?"#0000FF":i<.57?"#00CC44":i<.59?"#C6C400":i<.62?"#FF8800":"#FF0000";try{try{const te=Xe.loadActiveConfigurationToTables;typeof te=="function"&&(await te({applyToUI:!0,suppressOpticalSystemDataChanged:!0}),await new Promise(re=>setTimeout(re,0)))}catch{}const i=Bt(),l=Qt(),f=en();let p=Je(i),d=ht(l);const u=Tt(f);try{const te=Ot(),re=Array.isArray(te?.configurations)?te.configurations:[],se=re.find(ue=>String(ue?.id)===String(te?.activeConfigId))||re[0]||null;if(se){let ue=Array.isArray(se?.opticalSystem)?se.opticalSystem.map(ge=>ge&&typeof ge=="object"?{...ge}:ge):[];if(Array.isArray(se?.blocks)&&se.blocks.length>0){const ge=at(se.blocks);Array.isArray(ge?.rows)&&ge.rows.length>0&&(ue=ge.rows.map(xe=>xe&&typeof xe=="object"?{...xe}:xe))}const me=Array.isArray(se?.object)?se.object.map(ge=>ge&&typeof ge=="object"?{...ge}:ge):[];ue.length>0&&(p=ue),me.length>0&&(d=me)}}catch{}if(!Array.isArray(p)||p.length===0)throw new Error("No optical system data available");if(!Array.isArray(d)||d.length===0)throw new Error("No object data available");const y=document.getElementById("surface-number-select"),m=document.getElementById("ray-count-input"),h=document.getElementById("ring-count-select"),g=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):r(y?.value,0),w=a(Number.isInteger(e?.rayCount)?Number(e.rayCount):r(m?.value,101),1,20001),R=a(Number.isInteger(e?.ringCount)?Number(e.ringCount):r(h?.value,10),1,64),N=s(e?.defocusMinMm,-.1),I=s(e?.defocusMaxMm,.1),b=a(r(e?.steps,5),3,61),v=Math.max(1,s(e?.scaleUm,100))*.5,_=String(e?.wavelengthMode||"all").trim().toLowerCase()==="primary"?"primary":"all",{generateSurfaceOptions:M}=await De(async()=>{const{generateSurfaceOptions:te}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(re=>re.bU);return{generateSurfaceOptions:te}},__vite__mapDeps([0,1])),{runNativeSpotRaytrace:G}=await De(async()=>{const{runNativeSpotRaytrace:te}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(re=>re.bZ);return{runNativeSpotRaytrace:te}},__vite__mapDeps([0,1])),z=M(p||[]);let E=0;const L=Array.isArray(z)?z.find(te=>Number(te?.surfaceId)===Number(g)):null;if(L&&Number.isInteger(L.rowIndex))E=L.rowIndex;else{const te=Array.isArray(z)?z.find(re=>typeof re?.label=="string"&&re.label.includes("(Image)")):null;te&&Number.isInteger(te.rowIndex)?E=te.rowIndex:E=Math.max(0,p.length-1)}const ne=E+1,V=Array.from({length:b},(te,re)=>{if(b<=1)return N;const se=re/(b-1);return N+se*(I-N)}),K=!Array.isArray(u)||u.length===0?[]:u.map((te,re)=>{const se=Number(te?.wavelength);if(!Number.isFinite(se)||se<=0)return null;const me=String(te?.primary||"").toLowerCase().includes("primary");return{...te,wavelength:se,__wlIndex:re,__isPrimary:me,__label:`${(se*1e3).toFixed(1)} nm${me?" (primary)":""}`}}).filter(Boolean),$=K.find(te=>te?.__isPrimary)||(K.length>0?K[0]:null),J=_==="primary"?$?[$]:[{wavelength:.5876,weight:1,__isPrimary:!0,__label:"587.6 nm (primary)"}]:K.length>0?K:[{wavelength:.5876,weight:1,__isPrimary:!0,__label:"587.6 nm (primary)"}],Q=(te,re)=>{const se=te&&te.id!==void 0?String(te.id).trim():"";if(se)return se;const ue=te&&te.object!==void 0?String(te.object).trim():"";if(ue)return ue;const me=te&&te.position!==void 0?String(te.position).trim():"";return me?`Object ${re+1} (${me})`:`Object ${re+1}`},le=te=>{const re=String(te||"").trim(),se=re.match(/(\d+(?:\.\d+)?)\s*nm/i);return se&&se[1]?`Wavelength ${se[1]}nm`:re.toLowerCase().includes("primary")?"Wavelength Primary":`Wavelength ${re}`},Z=(te,re)=>{const se=Number(te?.wavelengthUm);return Number.isFinite(se)&&se>0?`Wavelength ${(se*1e3).toFixed(1)}nm`:le(re)},B=(te,re)=>{const se=String(te||"").trim();if(!se)return{objectLabel:re,wavelengthLabel:"Wavelength Primary"};const ue=se.match(/(Primary(?:\s*\([^)]*\))?|\d+(?:\.\d+)?\s*nm)\s*$/i);if(ue&&ue[1]){const me=String(ue[1]||"Primary");return{objectLabel:se.slice(0,Math.max(0,ue.index||0)).replace(/[|@\-\s]+$/,"").trim()||re,wavelengthLabel:le(me)}}return{objectLabel:se,wavelengthLabel:"Wavelength Primary"}},C=d.map((te,re)=>Q(te,re)),F=new Map(C.map((te,re)=>[te,re])),x=Array.from({length:d.length},()=>[]),T=[],j=String(e?.pattern||"").trim().toLowerCase(),P=j==="grid"||j==="annular"?j:Yr();for(let te=0;te<V.length;te++){const re=V[te],se=Math.floor(te/Math.max(1,V.length)*90);o(se,`Defocus ${re.toFixed(4)} mm (${te+1}/${V.length})`);const ue=Ga(p,re),me=[];for(let Se=0;Se<d.length;Se++){const ye=d[Se]||{},ce=C[Se]||Q(ye,Se),pe=String(ye.position??ye.object??ye.Object??"").trim().toLowerCase()==="angle";for(let he=0;he<J.length;he++){const _e=J[he],ve=Number(_e?.wavelength),Ce=String(_e?.__label||`${(ve*1e3).toFixed(1)} nm`),Te=qo(ye,ue,w,null,{annularRingCount:R,wavelengthUm:ve,pattern:P}),Ie=(Array.isArray(Te)?Te:[]).map(de=>({startP:{x:Number(de?.startP?.x)||0,y:Number(de?.startP?.y)||0,z:Number(de?.startP?.z)||0},dir:{x:Number(de?.dir?.x)||0,y:Number(de?.dir?.y)||0,z:Number(de?.dir?.z)||1},wavelengthUm:ve,pupilU:Number.isFinite(Number(de?.planeCoords?.u))?Number(de.planeCoords.u):void 0,pupilV:Number.isFinite(Number(de?.planeCoords?.v))?Number(de.planeCoords.v):void 0,isChief:de?.isChief===!0||de?.isChief==null&&(de?.rayIndex===0||de?.index===0)})).filter(de=>Number.isFinite(de.startP.x)&&Number.isFinite(de.startP.y)&&Number.isFinite(de.startP.z));Ie.length!==0&&me.push({label:`${ce} ${Ce}`,color:c(ve),hasFieldAngle:pe,rays:Ie})}}const ge=await G({opticalSystemRows:ue,sourceRows:J,objectRows:d,surfaceIndex:E,rayCount:w,ringCount:R,pattern:P,wavelengthMode:_,raySeries:me}),xe=Array.isArray(ge?.seriesStats)?ge.seriesStats:[];for(const Se of xe)T.push({backend:String(ge?.backend||"unknown"),defocusMm:Number(re),label:String(Se?.label||""),attemptedRays:Number(Se?.attemptedRays||0),hitRays:Number(Se?.hitRays||0),missRays:Number(Se?.missRays||0),apertureBlockRays:Number(Se?.apertureBlockRays||0),noIntersectionRays:Number(Se?.noIntersectionRays||0),tirRays:Number(Se?.tirRays||0),unknownFailRays:Number(Se?.unknownFailRays||0),statusCounts:Se&&typeof Se.statusCounts=="object"&&!Array.isArray(Se.statusCounts)?Se.statusCounts:{},hitRatePercent:Number(Se?.hitRatePercent||0)});const W=new Map,fe=Array.isArray(ge?.series)?ge.series:[],we=Math.max(1,J.length);for(let Se=0;Se<fe.length;Se++){const ye=fe[Se]||{},ce=Math.floor(Se/we),pe=C[ce]||`Object ${ce+1}`,he=B(ye?.label,pe),_e=F.has(he.objectLabel)?Number(F.get(he.objectLabel)):Math.max(0,Math.min(d.length-1,ce)),ve=Z(ye,he.wavelengthLabel),Ce=(Array.isArray(ye?.points)?ye.points:[]).map(Te=>({xUm:Number(Te?.xUm),yUm:Number(Te?.yUm)})).filter(Te=>Number.isFinite(Te.xUm)&&Number.isFinite(Te.yUm));W.has(_e)||W.set(_e,[]),W.get(_e).push({key:ve,label:ve,color:String(ye?.color||c(Number(ye?.wavelengthUm))),points:Ce})}for(let Se=0;Se<d.length;Se++){const ye=Array.isArray(W.get(Se))?W.get(Se):[],ce=[];for(const _e of ye){const ve=Array.isArray(_e?.points)?_e.points:[];for(const Ce of ve)ce.push({xUm:Number(Ce?.xUm)||0,yUm:Number(Ce?.yUm)||0})}let pe=0,he=0;ce.length>0&&(pe=ce.reduce((_e,ve)=>_e+ve.xUm,0)/ce.length,he=ce.reduce((_e,ve)=>_e+ve.yUm,0)/ce.length),x[Se].push({shiftMm:re,pointsByWavelength:ye.map(_e=>({key:_e.key,label:_e.label,color:_e.color,points:(Array.isArray(_e.points)?_e.points:[]).map(ve=>({xUm:(Number(ve?.xUm)||0)-pe,yUm:(Number(ve?.yUm)||0)-he}))}))})}}try{Xe.__cooptTfSpotLastTraceStats=T}catch{}try{globalThis.__cooptTfSpotLastTraceStats=T}catch{}try{ie.table?ie.table(T):ie.log("[TFSD_TRACE_STATS]",T)}catch{}const O=typeof n=="string"?document.getElementById(n):n;if(!O)throw new Error("Through-Focus Spot container element not found");const k=(O?.ownerDocument?.defaultView||window)?.Plotly||window?.Plotly;if(!k||typeof k.newPlot!="function")throw new Error("Plotly is not available");o(92,"Building plot...");const Y=d.length,D=V.length,U=[],ee={title:{text:"Through-Focus Spot Diagram",x:.5,xanchor:"center",y:.98,yanchor:"top"},showlegend:!0,grid:{rows:Y,columns:D,pattern:"independent"},margin:{l:60,r:20,t:95,b:60},paper_bgcolor:"#ffffff",plot_bgcolor:"#ffffff",height:Math.max(420,Y*145+90),legend:{orientation:"h",yanchor:"bottom",y:1.06,xanchor:"center",x:.5},legendgroupclick:"togglegroup"},oe=new Set,ae=new Map;for(let te=0;te<Y;te++)for(let re=0;re<D;re++){const se=te*D+re+1,ue=se===1?"x":`x${se}`,me=se===1?"y":`y${se}`,ge=se===1?"xaxis":`xaxis${se}`,xe=se===1?"yaxis":`yaxis${se}`,W=x?.[te]?.[re]||{pointsByWavelength:[]},fe=Array.isArray(W.pointsByWavelength)?W.pointsByWavelength:[];for(const we of fe){const Se=Array.isArray(we?.points)?we.points:[],ye=String(we?.key||we?.label||"wavelength");ae.has(ye)||ae.set(ye,{label:String(we?.label||ye),color:String(we?.color||"blue")}),U.push({x:Se.map(ce=>ce.xUm),y:Se.map(ce=>ce.yUm),mode:"markers",type:"scattergl",name:String(we?.label||ye),legendgroup:ye,showlegend:!1,marker:{size:3,color:String(we?.color||"blue"),opacity:.75},xaxis:ue,yaxis:me,hovertemplate:"x=%{x:.2f} µm<br>y=%{y:.2f} µm<extra></extra>"}),oe.add(ye)}ee[ge]={range:[-v,v],showgrid:!0,zeroline:!0,showticklabels:te===Y-1,title:te===Y-1?`${V[re].toFixed(3)} mm`:""},ee[xe]={range:[-v,v],showgrid:!0,zeroline:!0,showticklabels:re===0,title:re===0?`Field ${te+1}`:"",scaleanchor:ue,scaleratio:1}}if(U.reduce((te,re)=>{const se=Array.isArray(re?.x)?re.x:[];return te+se.filter(ue=>Number.isFinite(Number(ue))).length},0)<=0)throw new Error("Through-Focus Spot produced no plottable spot points on the image surface.");for(const[te,re]of ae.entries())U.push({x:[null],y:[null],mode:"markers",type:"scatter",name:re.label,legendgroup:te,showlegend:!0,marker:{size:8,color:re.color,symbol:"circle"},hoverinfo:"skip"});o(98,"Rendering plot..."),await k.newPlot(O,U,ee,{responsive:!0,displaylogo:!1}),o(100,"Done")}catch(i){const l=typeof n=="string"?document.getElementById(n):n;throw l&&(l.innerHTML=`<div style="padding:20px;color:red;font-family:Arial;">Failed to generate Through-Focus Spot Diagram.<br>${String(i?.message||i)}</div>`),i}}async function um(e={}){const t=(te,re)=>{const se=parseInt(String(te??""),10);return Number.isInteger(se)?se:re},n=(te,re)=>{const se=Number(te);return Number.isFinite(se)?se:re},o=(te,re,se)=>Math.max(re,Math.min(se,te)),r=te=>{const re=Array.isArray(te)?te.map(me=>({x:Number(me?.x),y:Number(me?.y)})).filter(me=>Number.isFinite(me.x)&&Number.isFinite(me.y)):[];if(re.length===0)return[];const se=re.reduce((me,ge)=>me+ge.x,0)/re.length,ue=re.reduce((me,ge)=>me+ge.y,0)/re.length;return re.map(me=>({xUm:(me.x-se)*1e3,yUm:(me.y-ue)*1e3}))},s=te=>{if(!Array.isArray(te)||te.length===0)return{count:0,rmsUm:NaN,diaUm:NaN};const re=te.map(me=>me.xUm*me.xUm+me.yUm*me.yUm),se=Math.sqrt(Math.max(...re)),ue=re.reduce((me,ge)=>me+ge,0)/re.length;return{count:te.length,rmsUm:Math.sqrt(ue),diaUm:2*se}},a=(te,re)=>Array.from(new Set([...Array.from(te.keys()),...Array.from(re.keys())])).sort().map(ue=>{const me=te.get(ue)||{},ge=re.get(ue)||{},xe=Number(me.rmsUm),W=Number(ge.rmsUm),fe=Number(me.diaUm),we=Number(ge.diaUm);return{key:ue,tsCount:Number(me.count||0),rustCount:Number(ge.count||0),tsRmsUm:xe,rustRmsUm:W,deltaRmsUm:Number.isFinite(xe)&&Number.isFinite(W)?W-xe:NaN,tsDiaUm:fe,rustDiaUm:we,deltaDiaUm:Number.isFinite(fe)&&Number.isFinite(we)?we-fe:NaN}}),c=te=>{const re=Number(te);return Number.isFinite(re)?re:null},i=te=>{const re=(te||[]).map(W=>c(W?.deltaRmsUm)).filter(W=>W!==null).map(W=>Math.abs(W)),se=(te||[]).map(W=>c(W?.deltaDiaUm)).filter(W=>W!==null).map(W=>Math.abs(W)),ue=(te||[]).map(W=>c(W?.deltaRmsUm)).filter(W=>W!==null),me=(te||[]).map(W=>c(W?.deltaDiaUm)).filter(W=>W!==null),ge=W=>W.length>0?W.reduce((fe,we)=>fe+we,0)/W.length:NaN,xe=W=>W.length>0?Math.max(...W):NaN;return{rowCount:(te||[]).length,meanAbsDeltaRmsUm:ge(re),maxAbsDeltaRmsUm:xe(re),meanAbsDeltaDiaUm:ge(se),maxAbsDeltaDiaUm:xe(se),signedMeanDeltaRmsUm:ge(ue),signedMeanDeltaDiaUm:ge(me)}},l=(te,re,se)=>(te||[]).map(me=>({...me,__abs:Math.abs(Number(me?.[re]))})).filter(me=>Number.isFinite(me.__abs)).sort((me,ge)=>ge.__abs-me.__abs).slice(0,se).map(({__abs:me,...ge})=>({...ge,absDelta:me})),f=te=>{const[re,se]=String(te||"").split("|").map(me=>me.trim()),ue=Number(re);return{defocusMm:Number.isFinite(ue)?ue:NaN,wavelengthLabel:se||""}},p=te=>{const re=new Map;for(const ue of te||[]){const ge=f(String(ue?.key||"")).wavelengthLabel||"unknown";re.has(ge)||re.set(ge,{absRms:[],absDia:[]});const xe=re.get(ge),W=c(ue?.deltaRmsUm),fe=c(ue?.deltaDiaUm);W!==null&&xe.absRms.push(Math.abs(W)),fe!==null&&xe.absDia.push(Math.abs(fe))}const se=ue=>ue.length>0?ue.reduce((me,ge)=>me+ge,0)/ue.length:NaN;return Array.from(re.entries()).map(([ue,me])=>({wavelengthLabel:ue,meanAbsDeltaRmsUm:se(me.absRms),meanAbsDeltaDiaUm:se(me.absDia),sampleCount:Math.max(me.absRms.length,me.absDia.length)})).sort((ue,me)=>{const ge=Number.isFinite(ue.meanAbsDeltaRmsUm)?ue.meanAbsDeltaRmsUm:-1;return(Number.isFinite(me.meanAbsDeltaRmsUm)?me.meanAbsDeltaRmsUm:-1)-ge})},d=(te,re,se)=>{const ue=[],me=[...te||[],...re||[]].filter(fe=>Number(fe?.tsCount||0)!==Number(fe?.rustCount||0));me.length>0&&ue.push({area:"sampling",reason:`TS/Rust の点数不一致が ${me.length} ケース`,action:"Rust 側の rayCount clamp・grid side 算出・annular 点生成順を TS 実装と完全一致させる"});const ge=Number(se?.throughFocus?.signedMeanDeltaRmsUm);Number.isFinite(ge)&&Math.abs(ge)>.5&&ue.push({area:"spot-scale",reason:`through-focus の平均RMS差が ${ge.toFixed(3)} µm`,action:ge>0?"Rust の spot σ 係数（base/defocus gain）を小さくして全体幅を縮小":"Rust の spot σ 係数（base/defocus gain）を大きくして全体幅を拡大"});const xe=Number(se?.spotDiagram?.maxAbsDeltaRmsUm),W=Number(se?.throughFocus?.maxAbsDeltaRmsUm);return(Number.isFinite(xe)&&xe>3||Number.isFinite(W)&&W>3)&&ue.push({area:"phase-to-spot model",reason:`最大RMS差が大きい (spot=${Number.isFinite(xe)?xe.toFixed(3):"n/a"} µm, tf=${Number.isFinite(W)?W.toFixed(3):"n/a"} µm)`,action:"PSF二次モーメント→spot σ 変換の倍率と anisotropy 項を波長・defocusごとに再フィットする"}),ue};try{if(!(await De(()=>import("./toolbar-collapsed-storage-oKonxCdL.js").then(re=>re.bP),__vite__mapDeps([0,1]))).isTauriRuntime())throw new Error("runSpotParityDiagnostics requires Tauri desktop runtime")}catch(te){throw new Error(String(te?.message||te||"runtime check failed"))}const{runAnalysisCompute:u}=await De(async()=>{const{runAnalysisCompute:te}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(re=>re.bZ);return{runAnalysisCompute:te}},__vite__mapDeps([0,1])),{generateSurfaceOptions:y,generateSpotDiagramAsync:m}=await De(async()=>{const{generateSurfaceOptions:te,generateSpotDiagramAsync:re}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(se=>se.bU);return{generateSurfaceOptions:te,generateSpotDiagramAsync:re}},__vite__mapDeps([0,1])),h=Bt(),g=Qt(),w=en(),R=Je(h),N=ht(g),I=Tt(w);if(!Array.isArray(R)||R.length===0)throw new Error("No optical system rows available");if(!Array.isArray(N)||N.length===0)throw new Error("No object rows available");const b=o(t(e?.objectIndex,0),0,N.length-1),A=N[b]||N[0],v=y(R||[]),S=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):0,_=Array.isArray(v)?v.find(te=>Number(te?.surfaceId)===Number(S)):null,M=Number.isInteger(_?.rowIndex)?Number(_.rowIndex):Math.max(0,R.length-1),G=M+1,z=o(t(e?.rayCount,501),9,20001),E=o(t(e?.ringCount,10),1,64),L=String(e?.pattern||Yr()||"annular").toLowerCase()==="grid"?"grid":"annular",ne=String(e?.wavelengthMode||"all").toLowerCase()==="primary"?"primary":"all",V=n(e?.defocusMinMm,-.1),K=n(e?.defocusMaxMm,.1),$=o(t(e?.steps,5),3,61),J=Math.max(1,n(e?.scaleUm,100)),Q=!Array.isArray(I)||I.length===0?[]:I.map(te=>{const re=Number(te?.wavelength);if(!Number.isFinite(re)||re<=0)return null;const ue=String(te?.primary||"").toLowerCase().includes("primary");return{...te,wavelength:re,__isPrimary:ue,__label:`${(re*1e3).toFixed(1)} nm${ue?" (primary)":""}`}}).filter(Boolean),le=Q.find(te=>te?.__isPrimary)||(Q.length>0?Q[0]:null),Z=ne==="primary"?le?[le]:[{wavelength:.5876,__label:"587.6 nm (primary)"}]:Q.length>0?Q:[{wavelength:.5876,__label:"587.6 nm (primary)"}],B=await Ha({forceRustWasm:e?.forceRustWasm===!0,requireRustWasm:e?.requireRustWasm!==!1}),C=new Map;for(const te of Z){const re=String(te?.__label||`${(Number(te?.wavelength||.5876)*1e3).toFixed(1)} nm`),ue=(await m(R,[te],[A],G,z,E,{onProgress:null,physicalVignetting:!0,displaySurfaceNumber:S,pattern:L,traceOptions:B}))?.spotData?.[0]?.spotPoints||[],me=r(ue);C.set(re,s(me))}const F=await u({kind:"spot-diagram",opticalSystemRows:R,sourceRows:I,objectRows:[A],surfaceIndex:M,rayCount:z,ringCount:E,pattern:L,wavelengthMode:ne}),x=new Map,T=Array.isArray(F?.spotDiagramSeries)?F.spotDiagramSeries:[];for(const te of T){const re=String(te?.label||"series"),se=Array.isArray(te?.points)?te.points.map(ue=>({xUm:Number(ue?.xUm),yUm:Number(ue?.yUm)})).filter(ue=>Number.isFinite(ue.xUm)&&Number.isFinite(ue.yUm)):[];x.set(re,s(se))}const j=Array.from({length:$},(te,re)=>{if($<=1)return V;const se=re/($-1);return V+se*(K-V)}),P=new Map;for(const te of j){const re=Ga(R,te);for(const se of Z){const ue=String(se?.__label||`${(Number(se?.wavelength||.5876)*1e3).toFixed(1)} nm`),me=`${te.toFixed(6)} | ${ue}`,xe=(await m(re,[se],[A],G,z,E,{onProgress:null,physicalVignetting:!0,displaySurfaceNumber:S,pattern:L,traceOptions:B}))?.spotData?.[0]?.spotPoints||[],W=r(xe);P.set(me,s(W))}}const O=await u({kind:"through-focus-spot",opticalSystemRows:R,sourceRows:I,objectRows:[A],surfaceIndex:M,defocusMinMm:V,defocusMaxMm:K,steps:$,rayCount:z,ringCount:E,scaleUm:J,pattern:L,wavelengthMode:ne}),X=new Map,k=Array.isArray(O?.spotSeries)?O.spotSeries:[];for(const te of k){const re=Number(te?.defocusMm),se=String(te?.wavelengthLabel||"wavelength"),ue=`${re.toFixed(6)} | ${se}`,me=Array.isArray(te?.points)?te.points.map(ge=>({xUm:Number(ge?.xUm),yUm:Number(ge?.yUm)})).filter(ge=>Number.isFinite(ge.xUm)&&Number.isFinite(ge.yUm)):[];X.set(ue,s(me))}const Y=a(C,x),D=a(P,X),U=o(t(e?.topN,12),3,100),ee={spotDiagram:i(Y),throughFocus:i(D)},oe={spotDiagramByRms:l(Y,"deltaRmsUm",U),spotDiagramByDia:l(Y,"deltaDiaUm",U),throughFocusByRms:l(D,"deltaRmsUm",U),throughFocusByDia:l(D,"deltaDiaUm",U),throughFocusByWavelength:p(D)},ae=d(Y,D,ee),Ae={config:{objectIndex:b,surfaceIndex:M,rayCount:z,ringCount:E,pattern:L,wavelengthMode:ne,defocusMinMm:V,defocusMaxMm:K,steps:$,scaleUm:J},summaries:ee,hotspots:oe,correctionCandidates:ae,spotDiagramRows:Y,throughFocusRows:D,generatedAt:Date.now()};try{window.__cooptLastSpotParity=Ae,ie.groupCollapsed("📊 [SpotParity] TS vs Rust"),ie.log("config",Ae.config),ie.log("summaries",ee),ie.log("hotspots",oe),ie.log("correctionCandidates",ae),ie.log("spotDiagramRows",Y),ie.log("throughFocusRows",D),ie.table&&(ie.table(oe.spotDiagramByRms),ie.table(oe.throughFocusByRms),ie.table(oe.throughFocusByWavelength),ie.table(Y),ie.table(D)),ie.groupEnd()}catch{}return Ae}async function Ms(e={}){ie.log("📊 Starting transverse aberration calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;let n="transverse-aberration-container";if(e&&typeof e=="object"&&(e.containerElement?n=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(n=e.containerId)),Zr()){ie.warn("⚠️ Transverse aberration calculation already in progress");return}try{On(!0);try{t?.({percent:0,message:"Preparing transverse aberration..."})}catch{}const o=document.getElementById("transverse-ray-count-input");let r=51;const s=Number.isInteger(e?.rayCount)?e.rayCount:null;if(s!==null&&s>0)r=s;else if(o&&o.value!==""){const b=parseInt(o.value);!isNaN(b)&&b>0&&(r=b)}r=Math.max(9,Math.min(10001,Math.round(r)));const a=Bt(),c=Je(a);if(!c||c.length===0)throw new Error("光学系データが見つかりません");const i=b=>{const v=String(b?.surfType??b?.["surf type"]??b?.surface_type??"").toLowerCase().trim();return v==="coord trans"||v==="coordinate break"||v==="coordtrans"||v==="coordinatebreak"||v==="ct"},l=b=>String(b?.["object type"]??b?.object??b?.Object??b?.surface_type??"").toLowerCase()==="object",f=b=>{const A=z=>String(z??"").trim().toLowerCase(),v=z=>A(z).replace(/[\s_-]+/g,""),S=A(b?.surfType??b?.["surf type"]??b?.type??b?.surface_type??""),_=v(b?.surfType??b?.["surf type"]??b?.type??b?.surface_type??""),M=A(b?._blockType??b?.blockType??""),G=v(b?._blockType??b?.blockType??"");return S==="gap"||S==="air gap"||_==="gap"||_==="airgap"||M==="gap"||M==="air gap"||G==="gap"||G==="airgap"},p=b=>String(b?.["object type"]??b?.object??b?.Object??"").toLowerCase()==="image";let d=-1;for(let b=0;b<c.length;b++)p(c[b])&&(d=b);if(d<0)for(let b=c.length-1;b>=0;b--){const A=c[b];if(!(i(A)||l(A)||f(A))){d=b;break}}d<0&&(d=c.length-1),ie.log(`📊 評価面: Surface ${d+1}`),ie.log(`📊 光線本数: ${r}本`);const{getPrimaryWavelengthForAberration:u}=await De(async()=>{const{getPrimaryWavelengthForAberration:b}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(A=>A.bX);return{getPrimaryWavelengthForAberration:b}},__vite__mapDeps([0,1])),{plotTransverseAberrationDiagram:y}=await De(async()=>{const{plotTransverseAberrationDiagram:b}=await import("./transverse-aberration-plot-DHnc-iG-.js");return{plotTransverseAberrationDiagram:b}},[]),{runNativeTransverseAberration:m}=await De(async()=>{const{runNativeTransverseAberration:b}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(A=>A.bZ);return{runNativeTransverseAberration:b}},__vite__mapDeps([0,1])),h=u();ie.log(`📊 Wavelength: ${h} μm`);const g=en(),w=Qt(),R=Tt(g),N=ht(w);try{t?.({percent:10,message:"Computing transverse aberration (Rust API)..."})}catch{}const I=await m({opticalSystemRows:c,sourceRows:Array.isArray(R)?R:[],objectRows:Array.isArray(N)?N:[],surfaceIndex:d,rayCount:r,pattern:"cross",wavelengthMode:"primary",wavelength:h});if(!I)throw new Error("Failed to calculate transverse aberration data");try{t?.({percent:95,message:"Rendering..."})}catch{}y(I,n,typeof n=="string"?document:n.ownerDocument);try{t?.({percent:100,message:"Done"})}catch{}return ie.log("✅ Transverse aberration diagram generated successfully"),I}catch(o){ie.error("❌ Transverse aberration diagram error:",o);const r=typeof n=="string"?document.getElementById(n):n;return r&&(r.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Transverse aberration error:</strong><br>
                ${o.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Transverse aberration error: ${o.message}`),null}finally{On(!1)}}async function pm(e={}){ie.log("📊 Starting lateral chromatic aberration calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null,n=e&&typeof e=="object"&&typeof e.chiefRayDefinition=="string"?e.chiefRayDefinition:"stop-center";let o="magnification-chromatic-aberration-container";e&&typeof e=="object"&&(e.containerElement?o=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(o=e.containerId));try{try{t?.({percent:0,message:"Preparing lateral chromatic aberration..."})}catch{}const r=document.getElementById("mca-xmin-input"),s=document.getElementById("mca-xmax-input"),a=e&&typeof e=="object"?Number(e.xMin):NaN,c=e&&typeof e=="object"?Number(e.xMax):NaN;let i=Number.isFinite(a)?a:Number(r?.value),l=Number.isFinite(c)?c:Number(s?.value);Number.isFinite(i)||(i=-.05),Number.isFinite(l)||(l=.05),i>=l&&(i=-.05,l=.05);const f=Bt(),p=Qt(),d=en(),u=Je(f),y=ht(p),m=Tt(d);if(!u||u.length===0)throw new Error("光学系データが見つかりません");const w=(V=>{const K=Array.isArray(V)?V:[],$=C=>(C?.position??C?.fieldType??C?.field_type??C?.field??C?.type??"").toString().toLowerCase(),J=K.map($).filter(Boolean),Q=J.some(C=>C.includes("rect")||C.includes("rectangle")),le=J.some(C=>C.includes("height"));return Q||le?{mode:"height"}:J.some(C=>C.includes("angle"))?{mode:"angle"}:K.map(C=>parseFloat(C?.yHeight??C?.y??NaN)).filter(C=>Number.isFinite(C)).length>0?{mode:"height"}:{mode:"angle"}})(y).mode==="height",R=(y||[]).map(V=>parseFloat(w?V?.yHeight??V?.y??V?.yHeightAngle??NaN:V?.yHeightAngle??V?.yFieldAngle??V?.yAngle??V?.fieldAngle??V?.y??NaN)).filter(V=>Number.isFinite(V)).map(V=>Math.abs(V));if(R.length===0)throw new Error("Objectテーブルに有効な値がありません");const N=Math.max(...R.map(V=>Number(V)));if(!Number.isFinite(N)||N<=0)throw new Error("Objectテーブルに有効な最大値がありません");const I=document.getElementById("mca-point-count-input"),b=e&&typeof e=="object"?Number(e.pointCount):NaN;let A=Number.isFinite(b)?Math.round(b):Number(I?.value);(!Number.isFinite(A)||A<2)&&(A=11);const v=[];if(A<=1)v.push(N);else for(let V=0;V<A;V++){const K=N*V/(A-1);v.push(Number(K.toFixed(6)))}const S=V=>{const K=Number(V);return!Number.isFinite(K)||K<=0?null:K>10?K/1e3:K},_=[.4358,.5876,.6563],M=(()=>{const V=Array.isArray(m)?m:[],K=[];for(const $ of V){const J=S($?.wavelength??$?.Wavelength);if(J!==null&&(K.some(Q=>Math.abs(Q-J)<1e-12)||K.push(J),K.length>=6))break}return K.length>0?K:_.slice()})(),G=.5876;M.some(V=>Math.abs(V-G)<1e-6)||(M.push(G),M.sort((V,K)=>V-K));const{runNativeMagnificationChromaticAberration:z}=await De(async()=>{const{runNativeMagnificationChromaticAberration:V}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(K=>K.bZ);return{runNativeMagnificationChromaticAberration:V}},__vite__mapDeps([0,1])),{plotMagnificationChromaticAberration:E}=await De(async()=>{const{plotMagnificationChromaticAberration:V}=await import("./magnification-chromatic-aberration-plot-DX_CVh81.js");return{plotMagnificationChromaticAberration:V}},[]),L=await z({opticalSystemRows:u,sourceRows:m,fieldSamples:v,wavelengths:M,referenceWavelength:G,heightMode:w,chiefRayDefinition:n});try{ie.log("📊 [LCA] backend:",L?.backend||L?.meta?.backend||"unknown")}catch{}if(!L)throw new Error("倍率色収差の計算に失敗しました");try{t?.({percent:95,message:"Rendering..."})}catch{}if(!E(L,o,{xMin:i,xMax:l}))throw new Error("倍率色収差: 描画可能な有効データがありません");try{t?.({percent:100,message:"Done"})}catch{}ie.log("✅ Lateral chromatic aberration diagram generated successfully")}catch(r){ie.error("❌ Lateral chromatic aberration diagram error:",r);const s=typeof o=="string"?document.getElementById(o):o;s&&(s.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Lateral chromatic aberration error:</strong><br>
                ${r.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Lateral chromatic aberration error: ${r.message}`)}}async function Rs(e={}){ie.log("📊 Starting astigmatism calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null,n=e&&typeof e=="object"&&typeof e.chiefRayDefinition=="string"?e.chiefRayDefinition:"stop-center";e&&typeof e=="object"&&e.logChiefRayDefinition;const o=e&&typeof e=="object"?e.useActiveConfigSnapshot===!0:!1,r=e&&typeof e=="object"?e.configId:null;let s="astigmatic-field-curves-container";if(e&&typeof e=="object"&&(e.containerElement?s=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(s=e.containerId)),Zr()){ie.warn("⚠️ Astigmatism calculation already in progress");return}try{On(!0);try{t?.({percent:0,message:"Preparing..."})}catch{}const a=C=>Array.isArray(C)?C.map(F=>{if(!F||typeof F!="object")return F;const x={...F};return x.xHeightAngle==null&&x["object x"]!=null&&(x.xHeightAngle=x["object x"]),x.yHeightAngle==null&&x["object y"]!=null&&(x.yHeightAngle=x["object y"]),x.xHeightAngle==null&&x.x!=null&&(x.xHeightAngle=x.x),x.yHeightAngle==null&&x.y!=null&&(x.yHeightAngle=x.y),x.position==null&&x.objectType!=null&&(x.position=x.objectType),x}):[],c=()=>{try{if(typeof localStorage>"u")return null;const C=Ot();if(!C||!Array.isArray(C.configurations))return null;const F=C.activeConfigId!==void 0&&C.activeConfigId!==null?String(C.activeConfigId):"",x=r!=null?String(r):F;if(!x)return null;const T=C.configurations.find(P=>String(P?.id)===x);if(!T)return null;const j=(()=>{try{const P=bn();return Array.isArray(P)?P:[]}catch{return[]}})();return{opticalSystemRows:Array.isArray(T.opticalSystem)?T.opticalSystem:[],objectRows:a(Array.isArray(T.object)?T.object:[]),sourceRows:j}}catch(C){return ie.warn("⚠️ Failed to load config snapshot for astigmatism:",C),null}},i=Bt(),l=en(),f=Qt();let p=Je(i),d=Tt(l),u=ht(f);if(o||r!=null){const C=c();C?.opticalSystemRows?.length&&(p=C.opticalSystemRows,Array.isArray(C.sourceRows)&&C.sourceRows.length>0&&(d=C.sourceRows),Array.isArray(C.objectRows)&&C.objectRows.length>0&&(u=C.objectRows))}if(!Array.isArray(u)||u.length===0)try{const C=window.tableObject&&Array.isArray(window.tableObject.data)?window.tableObject.data:null;Array.isArray(C)&&C.length>0&&(u=C)}catch{}if((!Array.isArray(u)||u.length===0)&&typeof localStorage<"u")try{const C=localStorage.getItem("objectTableData");if(C){const F=JSON.parse(C);Array.isArray(F)&&F.length>0&&(u=F)}}catch{}if(!Array.isArray(d)||d.length===0)try{const C=bn();Array.isArray(C)&&C.length>0&&(d=C)}catch{}if(!Array.isArray(u)||u.length===0){const C=zs(p);u=[String(C).toLowerCase()!=="finite"?{name:"AutoField0",position:"Angle",xHeightAngle:0,yHeightAngle:0,x:0,y:0}:{name:"AutoField0",position:"Rectangle",xHeight:0,yHeight:0,x:0,y:0}],ie.warn(`[ASTIG_DEBUG][FALLBACK_OBJECT] Injected default object row for conjugate=${C}`)}ie.log(`[ASTIG_DEBUG][INPUT_ROWS] optical=${Array.isArray(p)?p.length:0} source=${Array.isArray(d)?d.length:0} object=${Array.isArray(u)?u.length:0} snapshot=${o||r!=null}`);try{t?.({percent:12,message:`Input optical=${Array.isArray(p)?p.length:0} source=${Array.isArray(d)?d.length:0} object=${Array.isArray(u)?u.length:0}`})}catch{}if(!p||p.length===0)throw new Error("光学系データが見つかりません");const y=Number(e?.rayCount),m=Number.isFinite(y)?Math.max(9,Math.min(2001,Math.round(y))):30,h=Number(e?.ringCount),g=Number.isFinite(h)?Math.max(1,Math.min(64,Math.round(h))):32,w=String(e?.pattern||"").trim().toLowerCase(),R=w==="grid"||w==="cross"||w==="annular"?w:"annular",N=document.getElementById("astigmatism-ray-filter"),I=N?N.value:"all",b=document.getElementById("astigmatism-field-mode"),A=b?b.value:"interpolate",v=document.getElementById("astigmatism-chief-ray-mode"),_={"stop-center":"stopCenter","beam-midpoint":"beamCenter","beam-centroid":"centroid"}[n]||null,M=_||(v?v.value:"stopCenter"),z=["stopCenter","beamCenter","centroid"].includes(M)?M:"stopCenter";let E=u;const L=(u||[]).some(C=>{const F=(C.position||C.fieldType||C.type||"").toLowerCase();return F.includes("height")||F.includes("rect")});if(A==="interpolate"&&(u||[]).length>0&&!L){const C=Math.max(...u.map(j=>Math.abs(parseFloat(j.yHeightAngle||0)))),F=50,x=16,T=60;E=[];try{t?.({percent:x,message:`Interpolating fields 0/${F} (angle=0.00° / max=${C.toFixed(2)}°)`})}catch{}for(let j=0;j<=F;j++){const P=C*j/F;E.push({name:`Field${j}`,xHeightAngle:0,yHeightAngle:P,position:"angle"});const O=F>0?j/F:1,X=x+(T-x)*O;try{t?.({percent:X,message:`Interpolating fields ${j}/${F} (angle=${P.toFixed(2)}° / max=${C.toFixed(2)}°)`})}catch{}}}const ne=p.length-1,{runNativeAstigmatism:V}=await De(async()=>{const{runNativeAstigmatism:C}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(F=>F.bZ);return{runNativeAstigmatism:C}},__vite__mapDeps([0,1])),{plotAstigmaticFieldCurves:K}=await De(async()=>{const{plotAstigmaticFieldCurves:C}=await import("./astigmatism-plot-D5wQAwv2.js");return{plotAstigmaticFieldCurves:C}},[]),$=await V({opticalSystemRows:p,sourceRows:d||[],objectRows:E||[],surfaceIndex:ne,rayCount:m,ringCount:g,pattern:R,chiefRayMode:z,wavelengthMode:"all"}),J=String($?.backend||"tauri-native").trim();ie.log(`[ASTIG_BACKEND] ${J}`);try{t?.({percent:92,message:`Backend: ${J}`})}catch{}const Q=Array.isArray($?.data)?$.data:[],le=Q.filter(C=>C?.meridionalDeviation!==null&&C?.meridionalDeviation!==void 0&&Number.isFinite(Number(C?.meridionalDeviation))).length,Z=Q.filter(C=>C?.sagittalDeviation!==null&&C?.sagittalDeviation!==void 0&&Number.isFinite(Number(C?.sagittalDeviation))).length,B=new Set(Q.map(C=>Number(C?.wavelength)).filter(C=>Number.isFinite(C))).size;try{t?.({percent:94,message:`Summary rows=${Q.length} validM=${le} validS=${Z} wl=${B}`})}catch{}if(!$||!$.data||$.data.length===0){ie.warn("⚠️ 非点収差曲線データの生成に失敗しました");try{t?.({percent:100,message:`No plot data (rows=${Q.length}, validM=${le}, validS=${Z})`})}catch{}const C=typeof s=="string"?document.getElementById(s):s;C&&(C.innerHTML=`<div style="padding:16px;color:#b00020;font-family:Arial;"><div style="font-weight:600;margin-bottom:6px;">No astigmatism plot data</div><div style="font-size:12px;color:#444;">rows=${Q.length}, validM=${le}, validS=${Z}</div></div>`)}else{try{t?.({percent:95,message:"Rendering..."})}catch{}try{K(s,$);try{t?.({percent:100,message:""})}catch{}}catch(C){throw C}}ie.log("✅ Astigmatism diagram generated successfully")}catch(a){ie.error("❌ Astigmatism diagram error:",a),alert(`Astigmatism diagram error: ${a.message}`)}finally{On(!1)}}async function dm(e={}){ie.log("📊 Starting spherical aberration calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;let n="longitudinal-aberration-container";if(e&&typeof e=="object"&&(e.containerElement?n=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(n=e.containerId)),Zr()){ie.warn("⚠️ Spherical aberration calculation already in progress");return}try{On(!0);try{t?.({percent:0,message:"Preparing spherical aberration..."})}catch{}const o=e&&typeof e=="object"?e.precomputedAberrationData:null;if(o&&typeof o=="object"){const{plotLongitudinalAberrationDiagram:b}=await De(async()=>{const{plotLongitudinalAberrationDiagram:A}=await import("./longitudinal-aberration-plot-B8hkQC6I.js");return{plotLongitudinalAberrationDiagram:A}},[]);try{t?.({percent:95,message:"Rendering..."})}catch{}await b(o,n);try{t?.({percent:100,message:"Done"})}catch{}return}const r=document.getElementById("longitudinal-ray-count-input"),s=document.getElementById("longitudinal-reference-focus-mode");let a=0,c=51,i="current-paraxial";const l=en(),f=Tt(l),p=(()=>{const b=S=>{const _=Number(S);return!Number.isFinite(_)||_<=0?null:_>10?_/1e3:_},A=Array.isArray(f)?f:[],v=[];for(const S of A){const _=b(S?.wavelength??S?.Wavelength);if(!(_===null||!Number.isFinite(_)||_<=0)&&(v.some(M=>Math.abs(M-_)<1e-12)||v.push(_),v.length>=6))break}return v.length>0?v:[.5876]})();ie.log(`📊 Wavelengths from Source table: ${p.map(b=>b.toFixed(4)).join(", ")} μm`);let d=Bt(),u=Je(d);u&&u.length>0&&(a=u.length-1,ie.log(`📊 Using default image surface: Surface ${a+1} (0-indexed: ${a})`));const y=Number.isInteger(e?.rayCount)?e.rayCount:null;y!==null&&y>0?c=y:r&&r.value!==""?c=parseInt(r.value)||51:ie.warn("⚠️ Ray count input not found, using default (51)");const m=typeof e?.referenceFocusMode=="string"?e.referenceFocusMode:null;m?i=m:s&&s.value&&(i=s.value),(isNaN(a)||a<0)&&(a=0,ie.warn("⚠️ Invalid surface index, using default (0)")),ie.log(`📊 Calculating spherical aberration for surface ${a}, ${c} rays, wavelengths: ${p.map(b=>b.toFixed(4)).join(", ")} μm`),d=Bt();const h=Qt(),g=Je(d),w=ht(h);if(g&&g.length>0){const b=g.length-1;a>b&&(ie.warn(`⚠️ Surface index ${a} is too large, using last surface (${b})`),a=b)}if(ie.log(`📊 Final surface index: ${a} (0-indexed), using as targetSurfaceIndex: ${a}`),!g||g.length===0)throw new Error("No optical system data available");if(!w||w.length===0)throw new Error("No object data available");const{calculateLongitudinalAberrationAsync:R}=await De(async()=>{const{calculateLongitudinalAberrationAsync:b}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(A=>A.bW);return{calculateLongitudinalAberrationAsync:b}},__vite__mapDeps([0,1])),{plotLongitudinalAberrationDiagram:N}=await De(async()=>{const{plotLongitudinalAberrationDiagram:b}=await import("./longitudinal-aberration-plot-B8hkQC6I.js");return{plotLongitudinalAberrationDiagram:b}},[]),I=await R(g,a,p,c,{onProgress:t,debugSA:!!Xe.__COOPT_DEBUG_SA,referenceFocusMode:i});if(!I)throw new Error("Failed to calculate spherical aberration data");try{t?.({percent:95,message:"Rendering..."})}catch{}await N(I,n);try{t?.({percent:100,message:"Done"})}catch{}ie.log("✅ Spherical aberration diagram generated successfully")}catch(o){ie.error("❌ Error generating longitudinal aberration diagram:",o);const r=typeof n=="string"?document.getElementById(n):n;r&&(r.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Spherical aberration error:</strong><br>
                ${o.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Error generating longitudinal aberration diagram: ${o.message}`)}finally{On(!1)}}function mm(e){if(ie.log("📈 === Chief Ray Convergence Data ==="),!e||!e.chiefRayData){ie.warn("⚠️ No chief ray data available");return}const t=e.chiefRayData;ie.log(`Field angles: X=${t.fieldAngleX}°, Y=${t.fieldAngleY}°`),ie.log(`Entrance pupil position: ${t.entrancePupilPosition?.toFixed(4)||"N/A"}`),ie.log(`Exit pupil position: ${t.exitPupilPosition?.toFixed(4)||"N/A"}`),t.convergencePoint&&ie.log(`Chief ray convergence point: (${t.convergencePoint.x.toFixed(4)}, ${t.convergencePoint.y.toFixed(4)}, ${t.convergencePoint.z.toFixed(4)})`),t.aberrationCoefficients&&(ie.log("Aberration coefficients:"),Object.entries(t.aberrationCoefficients).forEach(([n,o])=>{ie.log(`  ${n}: ${o.toFixed(6)}`)})),ie.log("================================")}function fm(){const e=Dt();if(!e)return null;const t={minX:1/0,maxX:-1/0,minY:1/0,maxY:-1/0,minZ:1/0,maxZ:-1/0};let n=!1;return e.children.forEach(o=>{if(o.visible&&(o.isMesh||o.isLine||o.isGroup)&&o.type!=="AmbientLight"&&o.type!=="DirectionalLight"){const r=new Pr().setFromObject(o);r.isEmpty()||(t.minX=Math.min(t.minX,r.min.x),t.maxX=Math.max(t.maxX,r.max.x),t.minY=Math.min(t.minY,r.min.y),t.maxY=Math.max(t.maxY,r.max.y),t.minZ=Math.min(t.minZ,r.min.z),t.maxZ=Math.max(t.maxZ,r.max.z),n=!0)}}),n?(t.centerX=(t.minX+t.maxX)/2,t.centerY=(t.minY+t.maxY)/2,t.centerZ=(t.minZ+t.maxZ)/2,t.sizeX=t.maxX-t.minX,t.sizeY=t.maxY-t.minY,t.sizeZ=t.maxZ-t.minZ,t.maxSize=Math.max(t.sizeX,t.sizeY,t.sizeZ),t):(ie.warn("⚠️ No visible objects found for bounds calculation"),null)}function gm(){const e=xn(),t=Ln(),n=an();if(!e||!t||!n){ie.warn("⚠️ Camera, controls, or renderer not available");return}const o=fm();if(!o){ie.warn("⚠️ No scene bounds available for camera fitting");return}ie.log("🎥 Fitting camera to scene bounds..."),ie.log(`Scene bounds: (${o.minX.toFixed(2)}, ${o.minY.toFixed(2)}, ${o.minZ.toFixed(2)}) to (${o.maxX.toFixed(2)}, ${o.maxY.toFixed(2)}, ${o.maxZ.toFixed(2)})`);const r=o.maxSize*1.5,s={x:o.centerX,y:o.centerY,z:o.centerZ+r};if(e.position.set(s.x,s.y,s.z),t.target.set(o.centerX,o.centerY,o.centerZ),e.isOrthographicCamera){const a=e.right/e.top,c=o.maxSize*.6;e.left=-c*a/2,e.right=c*a/2,e.top=c/2,e.bottom=-c/2,e.updateProjectionMatrix()}t.update(),n.render(Dt(),e),ie.log(`🎥 Camera fitted to scene, distance: ${r.toFixed(2)}`),ie.log(`🎥 Camera position: (${e.position.x.toFixed(2)}, ${e.position.y.toFixed(2)}, ${e.position.z.toFixed(2)})`)}async function ym(e={}){ie.log("📊 Starting integrated aberration diagram calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null,n=e&&typeof e=="object"?e.useActiveConfigSnapshot===!0:!1,o=e&&typeof e=="object"?e.configId:null;try{try{t?.({percent:0,message:"Starting..."})}catch{}const r=B=>Array.isArray(B)?B.map(C=>{if(!C||typeof C!="object")return C;const F={...C};return F.xHeightAngle==null&&F["object x"]!=null&&(F.xHeightAngle=F["object x"]),F.yHeightAngle==null&&F["object y"]!=null&&(F.yHeightAngle=F["object y"]),F.xHeightAngle==null&&F.x!=null&&(F.xHeightAngle=F.x),F.yHeightAngle==null&&F.y!=null&&(F.yHeightAngle=F.y),F.position==null&&F.objectType!=null&&(F.position=F.objectType),F}):[],s=()=>{try{if(typeof localStorage>"u")return null;const B=Ot();if(!B||!Array.isArray(B.configurations))return null;const C=B.activeConfigId!==void 0&&B.activeConfigId!==null?String(B.activeConfigId):"",F=o!=null?String(o):C;if(!F)return null;const x=B.configurations.find(j=>String(j?.id)===F);if(!x)return null;const T=(()=>{try{const j=bn();return Array.isArray(j)?j:[]}catch{return[]}})();return{opticalSystemRows:Array.isArray(x.opticalSystem)?x.opticalSystem:[],objectRows:r(Array.isArray(x.object)?x.object:[]),sourceRows:T}}catch(B){return ie.warn("⚠️ Failed to load config snapshot for integrated aberration:",B),null}},a=Bt(),c=Qt(),i=en();let l=Je(a),f=r(ht(c)),p=Tt(i);if(n||o!=null){const B=s();B?.opticalSystemRows?.length&&(l=B.opticalSystemRows,f=B.objectRows,p=B.sourceRows)}if(!l||l.length===0){alert("光学系データがありません。");return}const d=100,u=30,y=(()=>{const B=[.4308,.5876,.6563],C=T=>{const j=Number(T);return!Number.isFinite(j)||j<=0?null:j>10?j/1e3:j},F=Array.isArray(p)?p:[],x=[];for(const T of F){const j=C(T?.wavelength);if(!(j===null||!Number.isFinite(j)||j<=0)&&(x.some(P=>Math.abs(P-j)<1e-12)||x.push(j),x.length>=6))break}return x.length>0?x:B})(),m=l.length-1;ie.log("📊 Calculating aberrations..."),ie.log("📊 Calculating spherical aberration...");try{t?.({percent:5,message:"Spherical: Preparing Rust/WASM..."})}catch{}const{runNativeSphericalAberration:h}=await De(async()=>{const{runNativeSphericalAberration:B}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(C=>C.bZ);return{runNativeSphericalAberration:B}},__vite__mapDeps([0,1])),g=await h({opticalSystemRows:l,sourceRows:p||[],surfaceIndex:m,rayCount:d,referenceFocusMode:"current-paraxial",wavelengthMode:"all"});try{t?.({percent:35,message:"Spherical: Done"})}catch{}if(!g)throw new Error("Failed to calculate longitudinal aberration");ie.log("📊 Calculating astigmatism...");let w=null;const{runNativeAstigmatism:R}=await De(async()=>{const{runNativeAstigmatism:B}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(C=>C.bZ);return{runNativeAstigmatism:B}},__vite__mapDeps([0,1]));if(w=await R({opticalSystemRows:l,sourceRows:p||[],objectRows:f||[],surfaceIndex:m,rayCount:u,ringCount:32,pattern:"annular",chiefRayMode:"stopCenter",wavelengthMode:"all"}),!w)throw new Error("Failed to calculate astigmatism");ie.log("📊 Calculating distortion...");const{runNativeDistortion:b,runNativeMagnificationChromaticAberration:A}=await De(async()=>{const{runNativeDistortion:B,runNativeMagnificationChromaticAberration:C}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(F=>F.bZ);return{runNativeDistortion:B,runNativeMagnificationChromaticAberration:C}},__vite__mapDeps([0,1])),{deriveMaxFieldAngleFromObjects:v}=await De(async()=>{const{deriveMaxFieldAngleFromObjects:B}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(C=>C.b_);return{deriveMaxFieldAngleFromObjects:B}},__vite__mapDeps([0,1])),M=(B=>{const C=Array.isArray(B)?B:[],F=k=>(k?.position??k?.fieldType??k?.field_type??k?.field??k?.type??"").toString().toLowerCase(),x=C.map(F).filter(Boolean),T=x.some(k=>k.includes("rect")||k.includes("rectangle")),j=x.some(k=>k.includes("height"));if(T||j)return{mode:"height"};if(x.some(k=>k.includes("angle")))return{mode:"angle"};const O=(C||[]).map(k=>parseFloat(k?.yHeight??k?.y??k?.yHeightAngle??NaN)).filter(k=>Number.isFinite(k)),X=(C||[]).map(k=>parseFloat(k?.fieldAngle??k?.yFieldAngle??k?.yAngle??NaN)).filter(k=>Number.isFinite(k));return O.length>0&&X.length===0?{mode:"height"}:{mode:"angle"}})(f).mode==="height",G=(f||[]).map(B=>parseFloat(B.yHeight??B.y??B.yHeightAngle??NaN)).filter(B=>Number.isFinite(B)),z=10;let E=[];if(M){let B=Math.min(...G),C=Math.max(...G);if(B<=0&&(B=.001,C<B&&(C=B)),B===C)E=[B];else for(let F=0;F<z;F++){const x=B+(C-B)*F/(z-1);E.push(parseFloat(x.toFixed(6)))}ie.log(`📊 Object heights for distortion (${E.length} points): ${E.join(", ")} mm`)}else{const B=v(),C=B*.001;for(let F=0;F<z;F++){const x=C+(B-C)*F/(z-1);E.push(parseFloat(x.toFixed(6)))}ie.log(`📊 Field angles for distortion (${z} points, starting from ${C.toFixed(6)}°): ${E.join(", ")}°`)}const L=[];for(let B=0;B<y.length;B++){const C=y[B],F=70+25*B/Math.max(1,y.length),x=25/Math.max(1,y.length),T=await b({opticalSystemRows:l,fieldSamples:E,heightMode:M,wavelength:C});T&&L.push({wavelength:C,data:T})}if(L.length===0)throw new Error("Failed to calculate distortion for any wavelength");ie.log("📊 Calculating lateral chromatic aberration...");const ne=Math.max(...M?G:E.map(B=>Math.abs(B))),V=21,K=[];if(Number.isFinite(ne)&&ne>0)for(let B=0;B<V;B++){const C=ne*B/(V-1);K.push(Number(C.toFixed(6)))}const $=K.length>0?await A({opticalSystemRows:l,sourceRows:p,fieldSamples:K,wavelengths:y,referenceWavelength:.5876,heightMode:M}):null;ie.log("📊 Plotting integrated aberration diagram...");const{plotIntegratedAberrationDiagram:J}=await De(async()=>{const{plotIntegratedAberrationDiagram:B}=await import("./integrated-aberration-plot-BuuUq4Y6.js");return{plotIntegratedAberrationDiagram:B}},[]);try{t?.({percent:96,message:"Rendering..."})}catch{}const Q=typeof localStorage>"u"?null:Ot(),le=Q?.configurations?.find(B=>B&&String(B.id)===String(Q.activeConfigId)),Z=le?le.name:"Default";J(g,w,L,$,{width:1440,height:600,mainTitle:`Integrated Aberration Diagram - ${Z}`,configName:Z,...e?.containerElement?{containerElement:e.containerElement}:{},...e?.infoElement?{infoElement:e.infoElement}:{}});try{t?.({percent:100,message:"Done"})}catch{}ie.log("✅ Integrated aberration diagram generated successfully")}catch(r){ie.error("❌ Error generating integrated aberration diagram:",r),alert(`Error generating integrated aberration diagram: ${r.message}`)}}(()=>{const e=globalThis;if(!e.__cooptConsoleRestoreApplied){e.__cooptConsoleRestoreApplied=!0;try{typeof e.isDebugEnabled=="function"&&(e.isDebugEnabled=()=>!0);const t=Object.getPrototypeOf(console);if(!t)return;const n=["log","info","warn","debug"];for(const o of n){const r=console[o],s=t[o];if(typeof s!="function")continue;Function.prototype.toString.call(r).includes("isDebugEnabled")&&(console[o]=s.bind(console))}}catch{}}})();(()=>{try{const e=globalThis;typeof e.__COOPT_FORCE_RUST_WASM_OPD>"u"&&(e.__COOPT_FORCE_RUST_WASM_OPD=!0),e&&e.__TAURI_INTERNALS__||(typeof e.__COOPT_ENABLE_RUST_RAYTRACE_WEB>"u"&&(e.__COOPT_ENABLE_RUST_RAYTRACE_WEB=!0),typeof e.__COOPT_REQUIRE_RUST_WASM_ANALYSIS>"u"&&(e.__COOPT_REQUIRE_RUST_WASM_ANALYSIS=!0))}catch{}})();window.THREE=Gr;window.OrbitControls=ta;let eo=null;const Hr=e=>new Promise(t=>setTimeout(t,e));function hm(){try{return typeof window>"u"||!window.location?null:new URL(window.location.href).searchParams}catch{return null}}function Mn(e,t,n=!1){try{if(!e||!e.has(t))return n;const o=String(e.get(t)??"").trim().toLowerCase();return!o||["1","true","yes","on"].includes(o)?!0:["0","false","no","off"].includes(o)?!1:n}catch{return n}}function Rn(e,t,n,o={}){try{if(!e||!e.has(t))return n;const r=Number(e.get(t));if(!Number.isFinite(r))return n;let s=o.integer?Math.floor(r):r;return Number.isFinite(o.min)&&(s=Math.max(o.min,s)),Number.isFinite(o.max)&&(s=Math.min(o.max,s)),s}catch{return n}}function Jn(e){try{typeof window<"u"&&(window.__cooptPhaseCAutorunStatus=e,window.dispatchEvent(new CustomEvent("coopt:phasec-autorun-status",{detail:e})))}catch{}try{typeof localStorage<"u"&&localStorage.setItem("coopt.phaseCAutorunStatus",JSON.stringify(e))}catch{}try{typeof document<"u"&&document.documentElement&&(document.documentElement.dataset.phasecAutorunState=String(e?.state??"unknown"))}catch{}}async function Va(e,t,n=1e4,o=50){const r=Date.now();for(;Date.now()-r<=n;){const s=t();if(s)return s;await Hr(o)}throw new Error(`${e} was not ready within ${n}ms`)}async function Cs(e=2e3){try{await new Promise(t=>{let n=!1;const o=()=>{if(!n){n=!0;try{window.removeEventListener("coopt:loaded-file-updated",r)}catch{}try{clearTimeout(s)}catch{}t()}},r=()=>o(),s=setTimeout(()=>o(),e);window.addEventListener("coopt:loaded-file-updated",r,{once:!0})})}catch{await Hr(250)}await Hr(250)}async function bm(){const e=await Va("__loadAllDataObjectIntoApp",()=>typeof window<"u"?window.__loadAllDataObjectIntoApp:null,1e4,50);if(st()){const{project:o}=await gl();await e(o,{filename:"default-load.json"}),await Cs();return}let t=await fetch("/co-opt/defaults/default-load.json");if(t.ok||(t=await fetch("/defaults/default-load.json")),!t.ok)throw new Error(`Failed to load default system: ${t.statusText}`);const n=await t.json();await e(n,{filename:"default-load.json"}),await Cs()}async function wm(){try{if(typeof window>"u"||window.__cooptPhaseCAutorunStarted)return;const e=hm();if(!(Mn(e,"phasec",!1)||Mn(e,"phasecAutorun",!1)))return;window.__cooptPhaseCAutorunStarted=!0;const n=new Date().toISOString();Jn({state:"starting",startedAt:n}),Mn(e,"phasecLoadDefault",!0)&&(Jn({state:"loading-default",startedAt:n}),await bm());const o=await Va("OptimizationMVP.exportMatrixFreeJson",()=>window?.OptimizationMVP?.exportMatrixFreeJson,1e4,50),r={repeat:Rn(e,"phasecRepeat",6,{integer:!0,min:1}),warmupDiscard:Rn(e,"phasecWarmupDiscard",1,{integer:!0,min:0}),filterOutliers:Mn(e,"phasecFilterOutliers",!0),matchBaselineBestStop:Mn(e,"phasecMatchBaselineBestStop",!0),matchBaselineBestMinIter:Rn(e,"phasecMatchBaselineBestMinIter",8,{integer:!0,min:0}),download:Mn(e,"phasecDownload",!0)},s=e?.get("phasecFileName");s&&(r.fileName=s);const a=e?.get("phasecMethod");a&&(r.method=a),e?.has("phasecMaxIter")&&(r.maxIter=Rn(e,"phasecMaxIter",0,{integer:!0,min:1})),e?.has("phasecMatchBaselineBestRelTol")&&(r.matchBaselineBestRelTol=Rn(e,"phasecMatchBaselineBestRelTol",0,{min:0})),e?.has("phasecMatchBaselineBestAbsTol")&&(r.matchBaselineBestAbsTol=Rn(e,"phasecMatchBaselineBestAbsTol",0,{min:0})),Jn({state:"running",startedAt:n,options:r});const c=await o(r);try{window.__cooptPhaseCAutorunResult=c}catch{}try{typeof localStorage<"u"&&c?.summary&&localStorage.setItem("coopt.phaseCLastSummary",JSON.stringify(c.summary))}catch{}Jn({state:"done",startedAt:n,completedAt:new Date().toISOString(),selectedMode:c?.summary?.phaseC?.selectedMode??null,elapsedSpeedup:c?.summary?.phaseC?.elapsedSpeedup??null,fileName:s||null})}catch(e){const t=e instanceof Error?e.message:String(e);console.error("❌ [PhaseC Autorun] Failed:",e),Jn({state:"error",completedAt:new Date().toISOString(),error:t})}}async function Ya(){try{try{const i=globalThis,l=typeof i.__COOPT_RAYTRACE_WASM_STRICT=="boolean"?i.__COOPT_RAYTRACE_WASM_STRICT:!0,f=typeof i.__COOPT_PSF_WASM_STRICT=="boolean"?i.__COOPT_PSF_WASM_STRICT:!0;Qn(l),Uc(f),i.__COOPT_RAYTRACE_WASM_STRICT=l,i.__COOPT_PSF_WASM_STRICT=f,console.log("🔒 [Init] WASM strict mode",{rayStrict:l,psfStrict:f})}catch{}const e=(async()=>{try{const i=await Hc();if(!i){const l=Gc?.();l&&console.warn("⚠️ [Init] Rust-WASM preload failed:",l);return}eo={backend:"rust-wasm",isWASMReady:!0,api:i},console.log("🔧 [Init] Rust-WASM backend is ready"),typeof window<"u"&&typeof window._setWASMSystem=="function"&&(window._setWASMSystem(eo),console.log("🔧 [Init] window._setWASMSystemでインスタンスを更新しました")),console.log("✅ [Init] Rust-WASM初期化が完了しました")}catch(i){console.warn("⚠️ WASM initialization failed:",i),eo&&(eo.isWASMReady=!1)}})(),{scene:t,camera:n,renderer:o,controls:r}=Dl();try{Mi()}catch{}try{Ri()}catch{}try{Ci()}catch{}const s=jl(t),{ambientLight:a,directionalLight:c}=s||{ambientLight:null,directionalLight:null};qc(t,n,o,r,Ii,$s,Eo),Ll();try{Tn(t)}catch{}try{Tp()}catch{}try{Pp()}catch{}try{Ep()}catch{}try{Pn(),setTimeout(()=>{try{typeof window.updateSpotDiagramConfigSelect=="function"&&window.updateSpotDiagramConfigSelect(),En()}catch(i){console.warn("Failed to initialize spot diagram selects:",i)}},100)}catch(i){console.error("Failed to setup analysis windows:",i)}try{kp()}catch{}try{}catch{}try{no()}catch{}try{br()}catch{}try{Dd()}catch{}try{Ed()}catch{}try{ss()}catch{}try{window.initializeAllTables=()=>{try{Mi()}catch{}try{Ri()}catch{}try{Ci()}catch{}try{ss()}catch{}try{kn()}catch{}try{En()}catch{}}}catch{}try{window.setupAnalysisWindows=Pn,window.setupOpticalSystemChangeListeners=Tn}catch{}try{window.rebindEventHandlers=()=>{console.log("🔄 [Rebind] rebindEventHandlers called");const i=["new-file-btn","save-all-btn","load-all-btn","load-default-btn","import-zemax-btn","export-zemax-btn","optimize-design-intent-btn"];console.log("🔄 [Rebind] Checking button existence:"),i.forEach(l=>{const f=document.getElementById(l);console.log(`  ${l}: ${f?"✅ found":"❌ NOT FOUND"}`)});try{}catch{}try{no()}catch{}try{br()}catch{}try{const l=Dt?.();l&&Tn(l)}catch{}try{Pn()}catch{}console.log("✅ [Rebind] rebindEventHandlers completed")}}catch{}return setTimeout(async()=>{Ti();try{window.updateWavefrontObjectSelect&&window.updateWavefrontObjectSelect(),window.updatePSFObjectOptions&&window.updatePSFObjectOptions()}catch{}},1e3),window.refreshBlockInspector=Mt,window.loadSystemConfigurations=We,window.saveSystemConfigurations=et,window.fitCameraToScene=gm,window.clearAllDrawing=lm,window.showSpotDiagram=Ur,window.showTransverseAberrationDiagram=Ms,window.showTransverseAberration=async()=>{const i=document.getElementById("transverse-ray-count-input");let l=51;if(i&&i.value!==""){const f=parseInt(i.value,10);!isNaN(f)&&f>0&&(l=f)}l=Math.max(9,Math.min(10001,Math.round(l))),await Ms({rayCount:l})},window.showLongitudinalAberrationDiagram=dm,window.showMagnificationChromaticAberrationDiagram=pm,window.showAstigmatismDiagram=Rs,window.showAstigmatism=async()=>{const i=document.getElementById("astigmatism-progress-wrapper"),l=document.getElementById("astigmatism-progressbar"),f=l instanceof HTMLProgressElement?l:null,p=document.getElementById("astigmatism-progress-text"),d=(u,y)=>{try{i&&(i.style.display="block"),f&&Number.isFinite(u)&&(f.value=Math.max(0,Math.min(100,u))),p&&typeof y=="string"&&(p.textContent=y)}catch{}};d(0,"Starting...");try{await Rs({onProgress:y=>{try{const m=Number(y?.percent),h=y?.message||y?.phase||"Working...";Number.isFinite(m)?d(m,h):d(void 0,h)}catch{}}}),d(100,"Done")}catch(u){console.error("❌ Astigmatism diagram error:",u),d(100,"Failed")}},window.showIntegratedAberrationDiagram=ym,window.showWavefrontDiagram=Hl,window.compareOpdNativeVsWasm=async(i={})=>{const l=typeof performance<"u"&&typeof performance.now=="function"?()=>performance.now():()=>Date.now(),f=Y=>{const D=Y.filter(ae=>Number.isFinite(ae));if(!D.length)return{count:0,rms:NaN,peakToPeak:NaN,min:NaN,max:NaN};let U=0,ee=1/0,oe=-1/0;for(const ae of D)U+=ae*ae,ae<ee&&(ee=ae),ae>oe&&(oe=ae);return{count:D.length,rms:Math.sqrt(U/D.length),peakToPeak:oe-ee,min:ee,max:oe}},p=Y=>{const D=[];if(!Array.isArray(Y))return D;for(const U of Y)if(Array.isArray(U))for(const ee of U){if(ee==null)continue;const oe=Number(ee);Number.isFinite(oe)&&D.push(oe)}return D},d=(Y,D,U)=>{if(!Array.isArray(Y)||!Array.isArray(D)||!Number.isFinite(U)||U<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};let ee=0,oe=0;for(let ue=0;ue<Y.length;ue++){const me=Y[ue],ge=D[ue];if(!me||!ge)continue;const xe=Math.min(me.length||0,ge.length||0);for(let W=0;W<xe;W++){if(!ge[W])continue;const fe=Number(me[W]);Number.isFinite(fe)&&(oe+=fe,ee+=1)}}if(ee<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};const ae=oe/ee;let Ae=0;for(let ue=0;ue<Y.length;ue++){const me=Y[ue],ge=D[ue];if(!me||!ge)continue;const xe=Math.min(me.length||0,ge.length||0);for(let W=0;W<xe;W++){if(!ge[W])continue;const fe=Number(me[W]);if(!Number.isFinite(fe))continue;const we=fe-ae;Ae+=we*we}}const te=Math.sqrt(Ae/ee),re=te/U,se=Math.exp(-Math.pow(2*Math.PI*re,2));return{validCount:ee,meanUm:ae,rmsUm:te,rmsWaves:re,marechalStrehl:Number.isFinite(se)?Math.max(0,Math.min(1,se)):NaN}},u=Je(),y=ht(),m=Tt(Eo),h=Number.isFinite(Number(i?.objectIndex))?Number(i.objectIndex):0,g=Number.isFinite(Number(i?.gridSize))?Math.max(17,Math.min(513,Math.floor(Number(i.gridSize)))):129,w=String(i?.opdDisplayMode||"pistonTiltRemoved"),R=String(i?.wasmOpdMode||"simple"),N=(()=>{try{if(typeof window.getPrimaryWavelength=="function"){const D=Number(window.getPrimaryWavelength());if(Number.isFinite(D)&&D>0)return D}}catch{}const Y=Number(Ei?.());return Number.isFinite(Y)&&Y>0?Y:.5876})(),I=Array.isArray(y)&&y[h]?y[h]:y?.[0]||{},b=String(I?.position??I?.Position??"").toLowerCase(),A=Number(I?.xHeightAngle??I?.x??0)||0,v=Number(I?.yHeightAngle??I?.y??0)||0,S=b==="angle"||b==="field angle"||b==="angles"||b==="point",_={id:I?.id||h+1,displayName:`Object ${h+1}`,type:S?"Angle":"Rectangle",fieldAngle:S?{x:A,y:v}:{x:0,y:0},xHeight:S?0:A,yHeight:S?0:v,objectIndex:h,wavelength:N},M=l(),G=oo(u,N),E=await ro(G).generateWavefrontMap(_,g,"circular",{recordRays:!1,progressEvery:0,opdMode:R,skipZernikeFit:!0,renderFromZernike:!1,opdDisplayMode:w,fullBatchTraceExperimental:!0}),L=l()-M;if(E?.error)throw new Error(`WASM/legacy OPD failed: ${E.error?.message||E.error}`);const ne=(Array.isArray(E?.display?.opdsInWavelengths)?E.display.opdsInWavelengths:Array.isArray(E?.opdsInWavelengths)?E.opdsInWavelengths:Array.isArray(E?.opds)?E.opds:[]).map(Y=>Number(Y)).filter(Y=>Number.isFinite(Y)),V=(Array.isArray(E?.raw?.opdsInWavelengths)?E.raw.opdsInWavelengths:Array.isArray(E?.raw?.opds)?E.raw.opds:[]).map(Y=>Number(Y)).filter(Y=>Number.isFinite(Y)),K=l(),{runNativeOpdMap:$}=await De(async()=>{const{runNativeOpdMap:Y}=await import("./toolbar-collapsed-storage-oKonxCdL.js").then(D=>D.bZ);return{runNativeOpdMap:Y}},__vite__mapDeps([0,1])),J=await $({opticalSystemRows:Array.isArray(u)?u:[],sourceRows:Array.isArray(m)?m:[],objectRows:Array.isArray(y)?y:[],objectIndex:h,gridSize:g,wavelengthUm:N,opdDisplayMode:w==="raw"||w==="pistonTiltRemoved"||w==="pistonTiltDefocusRemoved"?w:"pistonTiltRemoved"}),Q=l()-K,le=Array.isArray(J?.displayOpdGrid)&&J.displayOpdGrid.length?J.displayOpdGrid:J?.rawOpdGrid,Z=p(le),B=p(J?.rawOpdGrid),C=f(ne),F=f(Z),x=Number.isFinite(Number(J?.hitCount))?Number(J.hitCount):F.count||0,T={rmsAbs:Number.isFinite(C.rms)&&Number.isFinite(F.rms)?Math.abs(F.rms-C.rms):NaN,peakToPeakAbs:Number.isFinite(C.peakToPeak)&&Number.isFinite(F.peakToPeak)?Math.abs(F.peakToPeak-C.peakToPeak):NaN,countDiff:Math.abs(x-(C.count||0))},j=f(V),P=f(B),O=Number.isFinite(Number(J?.hitCount))?Number(J.hitCount):P.count||0,X={rmsAbs:Number.isFinite(j.rms)&&Number.isFinite(P.rms)?Math.abs(P.rms-j.rms):NaN,peakToPeakAbs:Number.isFinite(j.peakToPeak)&&Number.isFinite(P.peakToPeak)?Math.abs(P.peakToPeak-j.peakToPeak):NaN,countDiff:Math.abs(O-(j.count||0))},k={backend:{wasm:"wavefront.generateWavefrontMap",native:J?.backend||"run_native_opd_map"},options:{objectIndex:h,gridSize:g,wavelength:N,opdDisplayMode:w,wasmOpdMode:R},wasm:{elapsedMs:L,stats:C},native:{elapsedMs:Q,stats:F,sampleCount:Number(J?.sampleCount??0),hitCount:Number(J?.hitCount??0)},diff:T,raw:{wasm:j,native:P,diff:X}};console.log("📊 [OPD Parity] native vs wasm",k);try{console.log(`[OPD Parity Summary] display rms wasm=${Number(C.rms).toFixed(6)} native=${Number(F.rms).toFixed(6)} diff=${Number(T.rmsAbs).toFixed(6)} pp wasm=${Number(C.peakToPeak).toFixed(6)} native=${Number(F.peakToPeak).toFixed(6)} diff=${Number(T.peakToPeakAbs).toFixed(6)} count wasm=${C.count} native=${x} (gridNonNull=${F.count})`),console.log(`[OPD Parity Summary][raw] rms wasm=${Number(j.rms).toFixed(6)} native=${Number(P.rms).toFixed(6)} diff=${Number(X.rmsAbs).toFixed(6)} pp wasm=${Number(j.peakToPeak).toFixed(6)} native=${Number(P.peakToPeak).toFixed(6)} diff=${Number(X.peakToPeakAbs).toFixed(6)} count wasm=${j.count} native=${O} (gridNonNull=${P.count})`)}catch{}return k},window.comparePsfNativeVsJs=async(i={})=>{const l=typeof performance<"u"&&typeof performance.now=="function"?()=>performance.now():()=>Date.now(),f=de=>{if(!Array.isArray(de)||!de.length||!Array.isArray(de[0]))return{count:0,sum:NaN,sumSq:NaN,min:NaN,max:NaN,hash:"na"};let be=0,Pe=0,Ee=0,Ue=1/0,Ge=-1/0,He=2166136261;for(let Me=0;Me<de.length;Me++){const ze=de[Me];if(Array.isArray(ze))for(let Fe=0;Fe<ze.length;Fe++){const $e=Number(ze[Fe]);if(!Number.isFinite($e))continue;be++,Pe+=$e,Ee+=$e*$e,$e<Ue&&(Ue=$e),$e>Ge&&(Ge=$e);const Qe=Math.round($e*1e9);He^=Qe&255,He=Math.imul(He,16777619)>>>0,He^=Qe>>8&255,He=Math.imul(He,16777619)>>>0,He^=Qe>>16&255,He=Math.imul(He,16777619)>>>0,He^=Qe>>24&255,He=Math.imul(He,16777619)>>>0}}return{count:be,sum:Pe,sumSq:Ee,min:Number.isFinite(Ue)?Ue:NaN,max:Number.isFinite(Ge)?Ge:NaN,hash:He.toString(16)}},p=de=>{const be=[];if(!Array.isArray(de))return be;for(const Pe of de)if(Array.isArray(Pe))for(const Ee of Pe){const Ue=Number(Ee);Number.isFinite(Ue)&&be.push(Ue)}return be},d=(de,be,Pe)=>{if(!Array.isArray(de)||!Array.isArray(be)||!Number.isFinite(Pe)||Pe<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};let Ee=0,Ue=0;for(let $e=0;$e<de.length;$e++){const Qe=de[$e],pt=be[$e];if(!Qe||!pt)continue;const Xt=Math.min(Qe.length||0,pt.length||0);for(let Ct=0;Ct<Xt;Ct++){if(!pt[Ct])continue;const Ne=Number(Qe[Ct]);Number.isFinite(Ne)&&(Ue+=Ne,Ee+=1)}}if(Ee<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};const Ge=Ue/Ee;let He=0;for(let $e=0;$e<de.length;$e++){const Qe=de[$e],pt=be[$e];if(!Qe||!pt)continue;const Xt=Math.min(Qe.length||0,pt.length||0);for(let Ct=0;Ct<Xt;Ct++){if(!pt[Ct])continue;const Ne=Number(Qe[Ct]);if(!Number.isFinite(Ne))continue;const qe=Ne-Ge;He+=qe*qe}}const Me=Math.sqrt(He/Ee),ze=Me/Pe,Fe=Math.exp(-Math.pow(2*Math.PI*ze,2));return{validCount:Ee,meanUm:Ge,rmsUm:Me,rmsWaves:ze,marechalStrehl:Number.isFinite(Fe)?Math.max(0,Math.min(1,Fe)):NaN}},u=Je(),y=ht(),m=Tt(Eo),h=Number.isFinite(Number(i?.objectIndex))?Number(i.objectIndex):0,g=Number.isFinite(Number(i?.samplingSize))?Math.max(32,Math.min(1024,Math.floor(Number(i.samplingSize)))):128,w=i?.zeroPadTo,R=Number.isFinite(Number(w))?Math.max(0,Math.min(4096,Math.floor(Number(w)))):0,N=String(i?.opdDisplayMode||"pistonTiltRemoved"),I=i?.removeTilt===void 0?!1:!!i.removeTilt,b=i?.recenterIfWrapped===void 0?void 0:!!i.recenterIfWrapped,A=(()=>{try{if(typeof window.getPrimaryWavelength=="function"){const be=Number(window.getPrimaryWavelength());if(Number.isFinite(be)&&be>0)return be}}catch{}const de=Number(Ei?.());return Number.isFinite(de)&&de>0?de:.5876})();if(!st())throw new Error("comparePsfNativeVsJs requires Tauri runtime");const v=Array.isArray(y)&&y[h]?y[h]:y?.[0]||{},S=String(v?.position??v?.Position??"").toLowerCase(),_=Number(v?.xHeightAngle??v?.x??0)||0,M=Number(v?.yHeightAngle??v?.y??0)||0,G=S==="angle"||S==="field angle"||S==="angles",z={id:v?.id||h+1,displayName:`Object ${h+1}`,type:G?"Angle":"Rectangle",fieldAngle:G?{x:_,y:M}:{x:0,y:0},xHeight:G?0:_,yHeight:G?0:M,objectIndex:h,wavelength:A},E=l(),L=oo(u,A),V=await ro(L).generateWavefrontMap(z,g,"circular",{recordRays:!1,progressEvery:0,opdMode:"simple",skipZernikeFit:!0,renderFromZernike:!1,opdDisplayMode:N,fullBatchTraceExperimental:!0}),K=l()-E;if(V?.error)throw new Error(`Wavefront generation failed: ${V.error?.message||V.error}`);const $=Math.max(16,Math.floor(Number(g))),J=Array.from({length:$},()=>new Float32Array($)),Q=Array.from({length:$},()=>new Float32Array($)),le=Array.from({length:$},()=>Array($).fill(!1)),Z=new Float32Array($),B=new Float32Array($),C=Number.isFinite(Number(V?.pupilRange))&&Number(V.pupilRange)>0?Number(V.pupilRange):1;for(let de=0;de<$;de++){const be=de/($-1||1)*2-1;Z[de]=be*C,B[de]=be*C}const F=Array.isArray(V?.pupilCoordinates)?V.pupilCoordinates:[],T=N!=="raw"&&Array.isArray(V?.display?.opds)?V.display.opds:Array.isArray(V?.opds)?V.opds:[],j=Math.min(F.length,T.length),P=[];for(let de=0;de<j;de++){const be=F[de],Pe=Number.isInteger(be?.ix)?be.ix:null,Ee=Number.isInteger(be?.iy)?be.iy:null,Ue=Number(T[de]);Pe!==null&&Ee!==null&&Pe>=0&&Pe<$&&Ee>=0&&Ee<$&&Number.isFinite(Ue)&&(le[Ee][Pe]=!0,J[Ee][Pe]=Ue,Q[Ee][Pe]=1);const Ge=Number(be?.x),He=Number(be?.y);Number.isFinite(Ge)&&Number.isFinite(He)&&Number.isFinite(Ue)&&P.push({pupilX:Ge,pupilY:He,opd:Ue,isVignetted:!1})}const O={gridSize:$,wavelength:A,rayData:P,gridData:{opd:J,amplitude:Q,pupilMask:le,xCoords:Z,yCoords:B}};let X=24;try{const de=Number(window.findStopSurfaceIndex?.(u)),be=Number.isFinite(de)&&de>=0&&u?.[de]?u[de]:null,Pe=be&&be.semidia!==void 0&&be.semidia!==null?be.semidia:be&&be.Semidia!==void 0&&be.Semidia!==null?be.Semidia:be&&be["Semi Diameter"]!==void 0&&be["Semi Diameter"]!==null?be["Semi Diameter"]:be&&be.aperture!==void 0&&be.aperture!==null?be.aperture:be&&be.Aperture!==void 0&&be.Aperture!==null?be.Aperture:NaN,Ee=Math.abs(parseFloat(Pe));if(Number.isFinite(Ee)&&Ee>0){const Ge=be&&(be.aperture!==void 0||be.Aperture!==void 0)?Ee*.5:Ee;Number.isFinite(Ge)&&Ge>0&&(X=Ge*2)}}catch{}let k=100;try{if(typeof window.calculateFocalLength=="function"){const de=Number(window.calculateFocalLength(u,A));Number.isFinite(de)&&Math.abs(de)>1e-9&&de!==1/0&&(k=Math.abs(de))}}catch{}const{PSFCalculator:Y}=await De(async()=>{const{PSFCalculator:de}=await import("./psf-calculator-Bc6xBvfp.js");return{PSFCalculator:de}},__vite__mapDeps([3,0,1])),D=new Y,U=l(),ee=await D.calculatePSF(O,{samplingSize:$,wavelength:A,zeroPadTo:R,pupilDiameter:X,focalLength:k,forceImplementation:"native",removeTilt:I,recenterIfWrapped:b}),oe=l()-U,ae=l(),Ae=await D.calculatePSF(O,{samplingSize:$,wavelength:A,zeroPadTo:R,pupilDiameter:X,focalLength:k,forceImplementation:"javascript",removeTilt:I,recenterIfWrapped:b}),te=l()-ae,re=de=>Array.isArray(de?.psfData)?de.psfData:Array.isArray(de?.psf)?de.psf:[],se=de=>de?.metrics&&typeof de.metrics=="object"?de.metrics:{strehlRatio:Number(de?.strehlRatio),fwhm:de?.fwhm,centerPosition:de?.centerPosition},ue=de=>String(de?.implementationUsed||de?.metadata?.method||"unknown"),me=re(ee),ge=re(Ae),xe=se(ee),W=se(Ae),fe=Math.min(me.length,ge.length),we=fe>0?Math.min((me[0]||[]).length,(ge[0]||[]).length):0,Se=f(me),ye=f(ge);let ce=0,pe=0,he=0,_e=0;for(let de=0;de<fe;de++)for(let be=0;be<we;be++){const Pe=Number(me[de]?.[be]),Ee=Number(ge[de]?.[be]);if(!Number.isFinite(Pe)||!Number.isFinite(Ee))continue;const Ue=Pe-Ee,Ge=Math.abs(Ue);pe+=Ue*Ue,he+=Ge,Ge>_e&&(_e=Ge),ce++}const ve=d(J,le,A),Ce=Number(xe?.strehlRatio),Te=Number(W?.strehlRatio),Ie={options:{objectIndex:h,samplingSize:$,zeroPadTo:R,wavelength:A,opdDisplayMode:N,removeTilt:I,recenterIfWrapped:b===void 0?null:b,pupilDiameterMm:X,focalLengthMm:k},timings:{wavefrontMs:K,nativeMs:oe,javascriptMs:te},backend:{native:ue(ee),javascript:ue(Ae),nativeMethod:String(ee?.metadata?.method||""),javascriptMethod:String(Ae?.metadata?.method||"")},diff:{comparedGrid:`${fe}x${we}`,count:ce,rmsAbs:ce>0?Math.sqrt(pe/ce):NaN,meanAbs:ce>0?he/ce:NaN,maxAbs:_e},metrics:{native:xe||null,javascript:W||null,expected:ve,delta:{strehl:Ce-Te,fwhmAvgUm:Number(xe?.fwhm?.average)-Number(W?.fwhm?.average),centerX:Number(xe?.centerPosition?.x)-Number(W?.centerPosition?.x),centerY:Number(xe?.centerPosition?.y)-Number(W?.centerPosition?.y)}},grids:{native:p(me).length,javascript:p(ge).length,nativeHash:Se.hash,javascriptHash:ye.hash,nativeSum:Se.sum,javascriptSum:ye.sum}};console.log("📊 [PSF Parity] native vs javascript",Ie);try{console.log(`[PSF Parity Summary] grid=${Ie.diff.comparedGrid} n=${Ie.diff.count} rmsAbs=${Number(Ie.diff.rmsAbs).toExponential(4)} meanAbs=${Number(Ie.diff.meanAbs).toExponential(4)} maxAbs=${Number(Ie.diff.maxAbs).toExponential(4)} strehl(native=${Number(Ce).toFixed(6)},js=${Number(Te).toFixed(6)},Δ=${Number(Ie.metrics.delta.strehl).toExponential(4)}) marechal≈${Number(Ie.metrics.expected?.marechalStrehl).toFixed(6)} opdRms=${Number(Ie.metrics.expected?.rmsWaves).toExponential(4)}waves fwhmAvgΔ=${Number(Ie.metrics.delta.fwhmAvgUm).toExponential(4)}µm centerΔ=(${Ie.metrics.delta.centerX},${Ie.metrics.delta.centerY}) backend(native=${Ie.backend.native}, js=${Ie.backend.javascript})`)}catch{}return Ie},window.comparePsfNativeVsJsForObjects=async(i={})=>{const l=ht(),f=Array.isArray(l)?l.length:0,d=(Array.isArray(i?.objectIndices)?i.objectIndices:f>=2?[0,1]:[0]).map(g=>Number(g)).filter(g=>Number.isFinite(g)&&g>=0&&g<Math.max(1,f));if(!d.length)throw new Error("No valid objectIndices. Example: [0, 1]");const u=[];for(const g of d){const w=await window.comparePsfNativeVsJs({...i,objectIndex:g});u.push(w)}const y=u.map(g=>({objectIndex:Number(g?.options?.objectIndex),nativeHash:g?.grids?.nativeHash,jsHash:g?.grids?.javascriptHash,nativeStrehl:Number(g?.metrics?.native?.strehlRatio),jsStrehl:Number(g?.metrics?.javascript?.strehlRatio),marechalStrehl:Number(g?.metrics?.expected?.marechalStrehl),opdRmsWaves:Number(g?.metrics?.expected?.rmsWaves),nativeFwhmAvg:Number(g?.metrics?.native?.fwhm?.average),jsFwhmAvg:Number(g?.metrics?.javascript?.fwhm?.average),centerNative:g?.metrics?.native?.centerPosition,centerJs:g?.metrics?.javascript?.centerPosition})),m=[];for(let g=1;g<y.length;g++){const w=y[g-1],R=y[g];m.push({fromObject:w.objectIndex,toObject:R.objectIndex,nativeHashChanged:w.nativeHash!==R.nativeHash,jsHashChanged:w.jsHash!==R.jsHash,nativeStrehlDelta:R.nativeStrehl-w.nativeStrehl,jsStrehlDelta:R.jsStrehl-w.jsStrehl,nativeFwhmDelta:R.nativeFwhmAvg-w.nativeFwhmAvg,jsFwhmDelta:R.jsFwhmAvg-w.jsFwhmAvg})}const h={objectIndices:d,byObject:y,variation:m};return console.log("📊 [PSF Object Compare] native vs javascript",h),h},window.runOPDProfiling=Vc,window.showMTFDiagram=fn,window.showMTFComparisonDiagram=ia,window.showThroughFocusSpotDiagram=qa,window.runSpotParityDiagnostics=um,window.showThroughFocusMTFDiagram=Er,window.benchmarkMTFOnce=async(i={})=>{const l=typeof globalThis<"u"?globalThis:null,f=l?l.__WAVEFRONT_PROFILE:void 0,p=l?l.__OPD_DEBUG:void 0,d=l?l.__PSF_DEBUG:void 0,u=typeof performance<"u"&&typeof performance.now=="function"?()=>performance.now():()=>Date.now(),y=document.createElement("div");y.id=`mtf-benchmark-${Date.now()}`,y.style.cssText="position:fixed;left:-99999px;top:-99999px;width:800px;height:600px;overflow:hidden;",document.body.appendChild(y);try{l&&(l.__WAVEFRONT_PROFILE=!0,l.__OPD_DEBUG=!1,l.__PSF_DEBUG=!1);const m=u();await fn({wavelengthMicrons:i.wavelengthMicrons??"all",objectIndex:Number.isFinite(Number(i.objectIndex))?Number(i.objectIndex):0,maxFrequencyLpmm:Number.isFinite(Number(i.maxFrequencyLpmm))?Number(i.maxFrequencyLpmm):100,samplingSize:Number.isFinite(Number(i.samplingSize))?Number(i.samplingSize):256,samplingPoints:Number.isFinite(Number(i.samplingPoints))?Number(i.samplingPoints):64,opdDisplayMode:i.opdDisplayMode??"pistonTiltRemoved",containerElement:y,onProgress:null});const h=u()-m,g=l&&typeof l.getRayTracingProfile=="function"?l.getRayTracingProfile({reset:!0}):null,w=Number(g?.traceCalls)||0,R=Number(g?.traceBatchLockstepCalls)||0,N=Number(g?.traceBatchLockstepRays)||0,I=w>0?R/w:0,b=R>0?N/R:0,A=h>0?w/h:0,v={elapsedMs:h,samplingSize:Number.isFinite(Number(i.samplingSize))?Number(i.samplingSize):256,samplingPoints:Number.isFinite(Number(i.samplingPoints))?Number(i.samplingPoints):64,wavelengthMicrons:i.wavelengthMicrons??"all",maxFrequencyLpmm:Number.isFinite(Number(i.maxFrequencyLpmm))?Number(i.maxFrequencyLpmm):100,objectIndex:Number.isFinite(Number(i.objectIndex))?Number(i.objectIndex):0,rayTracing:{traceCalls:w,traceCallsPerMs:A,lockstepCalls:R,lockstepRays:N,lockstepCallRatio:I,lockstepRaysPerCall:b},note:"See [OPD Profile] logs for wavefront/raytrace breakdown."};return console.log("📊 benchmarkMTFOnce result:",v),v}finally{try{y.remove()}catch{}l&&(l.__WAVEFRONT_PROFILE=f,l.__OPD_DEBUG=p,l.__PSF_DEBUG=d)}},window.benchmarkMTF=async(i={})=>{const l=Math.max(1,Math.floor(Number(i.runs)||3)),f=Math.max(0,Math.floor(Number(i.warmup)||1)),p=[],d=[],u=[];for(let N=0;N<f;N++)await window.benchmarkMTFOnce(i);for(let N=0;N<l;N++){const I=await window.benchmarkMTFOnce(i),b=Number(I?.elapsedMs);Number.isFinite(b)&&p.push(b);const A=Number(I?.rayTracing?.lockstepCallRatio);Number.isFinite(A)&&d.push(A);const v=Number(I?.rayTracing?.lockstepRaysPerCall);Number.isFinite(v)&&u.push(v)}const y=p.length>0?p.reduce((N,I)=>N+I,0)/p.length:NaN,m=p.length>0?Math.min(...p):NaN,h=p.length>0?Math.max(...p):NaN,g=d.length>0?d.reduce((N,I)=>N+I,0)/d.length:NaN,w=u.length>0?u.reduce((N,I)=>N+I,0)/u.length:NaN,R={runs:l,warmup:f,samples:p,avgMs:y,minMs:m,maxMs:h,lockstep:{avgCallRatio:g,avgRaysPerCall:w}};return console.log("📈 benchmarkMTF summary:",R),R},window.benchmarkMTFCompare=async(i={})=>{const l=String(i.beforeLabel??"before"),f=String(i.afterLabel??"after"),p={...i.beforeOptions??{}},d={...i.afterOptions??{}};i.runs!==void 0&&(p.runs=i.runs,d.runs=i.runs),i.warmup!==void 0&&(p.warmup=i.warmup,d.warmup=i.warmup);const u=await window.benchmarkMTF(p),y=await window.benchmarkMTF(d),m=Number(u?.avgMs),h=Number(y?.avgMs),g=Number(u?.minMs),w=Number(y?.minMs),R=Number(u?.maxMs),N=Number(y?.maxMs),I=Number.isFinite(m)&&Number.isFinite(h)?h-m:NaN,b=Number.isFinite(g)&&Number.isFinite(w)?w-g:NaN,A=Number.isFinite(R)&&Number.isFinite(N)?N-R:NaN,v=Number.isFinite(m)&&m>0&&Number.isFinite(h)?(m-h)/m*100:NaN,S=Number(u?.lockstep?.avgCallRatio),_=Number(y?.lockstep?.avgCallRatio),M=Number.isFinite(S)&&Number.isFinite(_)?_-S:NaN,G={labels:{before:l,after:f},before:u,after:y,delta:{avgMs:I,minMs:b,maxMs:A,avgImprovementPct:v,lockstepCallRatioDelta:M}};return console.log("🧪 benchmarkMTFCompare result:",G),G},window.compareMTFvsTFMTFAtFrequency=async(i={})=>{const l=Number.isFinite(Number(i?.frequencyLpmm))?Number(i.frequencyLpmm):10,f=i?.wavelengthMicrons==="all"?"all":Number.isFinite(Number(i?.wavelengthMicrons))?Number(i.wavelengthMicrons):.5876,p=Number.isFinite(Number(i?.objectIndex))?Number(i.objectIndex):0,d=Number.isFinite(Number(i?.samplingSize))?Number(i.samplingSize):256,u=Number.isFinite(Number(i?.zeroPadTo))?Number(i.zeroPadTo):0,y=String(i?.opdDisplayMode||"pistonTiltRemoved"),m=(R,N)=>{const I=Array.isArray(R?.x)?R.x:[],b=Array.isArray(R?.y)?R.y:[];if(!I.length||I.length!==b.length)return NaN;const A=I.map((v,S)=>({x:Number(v),y:Number(b[S])})).filter(v=>Number.isFinite(v.x)&&Number.isFinite(v.y)).sort((v,S)=>v.x-S.x);if(!A.length)return NaN;if(A.length===1||N<=A[0].x)return A[0].y;if(N>=A[A.length-1].x)return A[A.length-1].y;for(let v=1;v<A.length;v++){const S=A[v-1],_=A[v];if(N<=_.x&&_.x>S.x){const M=(N-S.x)/(_.x-S.x);return S.y+M*(_.y-S.y)}}return A[A.length-1].y},h=R=>{const N=(Array.isArray(R)?R:[]).filter(A=>A?.meta?.overlayType!=="diffractionLimit"),I=N.find(A=>/meridional|tangential/i.test(String(A?.name||""))),b=N.find(A=>/sagittal/i.test(String(A?.name||"")));return{meridional:I,sagittal:b,regularCount:N.length}},g=R=>{const N=Array.isArray(R?.x)?R.x:[],I=Array.isArray(R?.y)?R.y:[];if(!N.length||N.length!==I.length)return NaN;let b=0,A=1/0;for(let S=0;S<N.length;S++){const _=Number(N[S]);if(!Number.isFinite(_))continue;const M=Math.abs(_);M<A&&(A=M,b=S)}const v=Number(I[b]);return Number.isFinite(v)?v:NaN},w=document.createElement("div");w.id=`mtf-parity-${Date.now()}`,w.style.cssText="position:fixed;left:-99999px;top:-99999px;width:800px;height:600px;overflow:hidden;",document.body.appendChild(w);try{const R=await fn({wavelengthMicrons:f,objectIndex:p,maxFrequencyLpmm:Math.max(20,l),samplingSize:d,zeroPadTo:u,opdDisplayMode:y,skipPlot:!0,containerElement:w}),N=Array.isArray(R?.traces)?R.traces:[],I=h(N),b=m(I.meridional,l),A=m(I.sagittal,l),v=await Er({wavelengthMicrons:f,objectIndex:p,targetFrequencyLpmm:l,defocusMinMm:0,defocusMaxMm:0,steps:3,samplingSize:d,zeroPadTo:u,opdDisplayMode:y,containerElement:w}),S=Array.isArray(v?.traces)?v.traces:[],_=h(S),M=g(_.meridional),G=g(_.sagittal),z={conditions:{frequencyLpmm:l,wavelengthMicrons:f,objectIndex:p,samplingSize:d,zeroPadTo:u,opdDisplayMode:y},mtf:{meridional:b,sagittal:A,traceCount:I.regularCount},tfmtfAtDefocus0:{meridional:M,sagittal:G,traceCount:_.regularCount},delta:{meridional:Number.isFinite(b)&&Number.isFinite(M)?M-b:NaN,sagittal:Number.isFinite(A)&&Number.isFinite(G)?G-A:NaN}};return console.log("📊 [MTF vs TFMTF parity]",z),z}finally{try{w.remove()}catch{}}},window.compareNativeVsTsObjectMtf=async(i={})=>{const l=Number.isFinite(Number(i?.firstFrequencyLpmm))?Number(i.firstFrequencyLpmm):10,f=Number.isFinite(Number(i?.secondFrequencyLpmm))?Number(i.secondFrequencyLpmm):30,p=i?.wavelengthMicrons==="all"?"all":Number.isFinite(Number(i?.wavelengthMicrons))?Number(i.wavelengthMicrons):.5876,d=Number.isFinite(Number(i?.samplingSize))?Number(i.samplingSize):256,u=Number.isFinite(Number(i?.zeroPadTo))?Number(i.zeroPadTo):0,y=Number.isFinite(Number(i?.fieldMin))?Number(i.fieldMin):0,m=Number.isFinite(Number(i?.fieldMax))?Number(i.fieldMax):10,h=Number.isFinite(Number(i?.steps))?Math.max(3,Math.floor(Number(i.steps))):21,g=i?.fieldAxisMode==="height"||i?.fieldAxisMode==="angle"?i.fieldAxisMode:"angle",w=String(i?.opdDisplayMode||"pistonTiltRemoved"),R=document.createElement("div");R.id=`field-mtf-compare-${Date.now()}`,R.style.cssText="position:fixed;left:-99999px;top:-99999px;width:900px;height:650px;overflow:hidden;",document.body.appendChild(R);const N=A=>{const v=String(A?.name||""),S=Array.isArray(A?.x)?A.x.map(M=>Number(M)):[],_=Array.isArray(A?.y)?A.y.map(M=>Number(M)):[];return!S.length||S.length!==_.length?null:{name:v,x:S,y:_}},I=A=>{const v={};for(const S of Array.isArray(A)?A:[]){const _=N(S);_&&(v[_.name]={x:_.x,y:_.y})}return v},b=(A,v)=>{if(!A||!Array.isArray(A.x)||!Array.isArray(A.y)||A.x.length!==A.y.length||A.x.length===0)return NaN;let S=0,_=1/0;for(let G=0;G<A.x.length;G++){const z=Number(A.x[G]);if(!Number.isFinite(z))continue;const E=Math.abs(z-v);E<_&&(_=E,S=G)}const M=Number(A.y[S]);return Number.isFinite(M)?M:NaN};try{const A=window.runDesktopNativeFieldMtfForPopup;if(typeof A!="function")throw new Error("runDesktopNativeFieldMtfForPopup is not available");const v=await A({objectIndex:0,wavelengths:p==="all"?[]:[Number(p)],firstFrequencyLpmm:l,secondFrequencyLpmm:f,fieldMin:y,fieldMax:m,steps:h,samplingSize:d,zeroPadTo:u,opdDisplayMode:w,fieldAxisMode:g}),S=await ra({wavelengthMicrons:p,firstFrequencyLpmm:l,secondFrequencyLpmm:f,fieldMin:y,fieldMax:m,steps:h,samplingSize:d,zeroPadTo:u,opdDisplayMode:w,fieldAxisMode:g,containerElement:R}),_=Array.isArray(v?.series)?v.series[0]:null,M=Array.isArray(v?.xAxis)?v.xAxis.map(V=>Number(V)):[],G=[{name:`Meridional ${l.toFixed(1)} lp/mm`,x:M,y:Array.isArray(_?.meridionalFirst)?_.meridionalFirst:[]},{name:`Sagittal ${l.toFixed(1)} lp/mm`,x:M,y:Array.isArray(_?.sagittalFirst)?_.sagittalFirst:[]},{name:`Meridional ${f.toFixed(1)} lp/mm`,x:M,y:Array.isArray(_?.meridionalSecond)?_.meridionalSecond:[]},{name:`Sagittal ${f.toFixed(1)} lp/mm`,x:M,y:Array.isArray(_?.sagittalSecond)?_.sagittalSecond:[]}],z=I(Array.isArray(S?.traces)?S.traces:[]),L=(M.length?M:Array.isArray(Object.values(z)?.[0]?.x)?Object.values(z)[0].x:[]).map(V=>{const K=b({x:M,y:G[0].y},V),$=b({x:M,y:G[1].y},V),J=b({x:M,y:G[2].y},V),Q=b({x:M,y:G[3].y},V),le=b(z[Object.keys(z).find(F=>F.startsWith(`Meridional ${l.toFixed(1)} lp/mm`))||""],V),Z=b(z[Object.keys(z).find(F=>F.startsWith(`Sagittal ${l.toFixed(1)} lp/mm`))||""],V),B=b(z[Object.keys(z).find(F=>F.startsWith(`Meridional ${f.toFixed(1)} lp/mm`))||""],V),C=b(z[Object.keys(z).find(F=>F.startsWith(`Sagittal ${f.toFixed(1)} lp/mm`))||""],V);return{field:V,native:{m1:K,s1:$,m2:J,s2:Q},ts:{m1:le,s1:Z,m2:B,s2:C},delta:{m1:Number.isFinite(K)&&Number.isFinite(le)?K-le:NaN,s1:Number.isFinite($)&&Number.isFinite(Z)?$-Z:NaN,m2:Number.isFinite(J)&&Number.isFinite(B)?J-B:NaN,s2:Number.isFinite(Q)&&Number.isFinite(C)?Q-C:NaN}}}),ne={conditions:{wl:p,freq1:l,freq2:f,fieldMin:y,fieldMax:m,steps:h,samplingSize:d,zeroPadTo:u,axisMode:g,opdDisplayMode:w},native:{xAxis:M,series:_},tsTraceNames:Object.keys(z),perField:L};return console.log("📊 [Object MTF Native vs TS]",ne),ne}finally{try{R.remove()}catch{}}},window.outputParaxialDataToDebug=Yc,window.outputSeidelCoefficientsToDebug=Zc,window.outputDebugSystemData=Xc,window.displayCoordinateTransformMatrix=Kc,window.renderBlockContributionSummaryFromSeidel=Jc,window.renderSystemConstraintsFromSurfaceRows=Qc,window.debugTableStatus=Ti,window.initializeTablesWithDummyData=el,window.generateZMXText=ep,window.downloadZMX=tp,window.parseZMXTextToOpticalSystemRows=tl,window.parseZMXArrayBufferToOpticalSystemRows=Ds,window.calculateFocalLength=Ks,window.calculateParaxialData=uo,window.calculateEntrancePupilDiameter=vl,window.calculateImageSpaceDiffractionParams=lo,window.derivePupilAndFocalLengthMmFromParaxial=ks,window.findStopSurfaceIndex=Xs,window.calculateAllSurfacesLocalCoordinates=nl,window.resetToSurfaceCoordinates=ol,window.shiftToChiefRayOrigin=rl,window.restoreFromLocalCoordinates=il,window.transformToChiefRayLocalCoordinates=sl,window.calculateChiefRaySurfaceIntersections=al,window.updateTransformSurfaceSelect=Ca,window.loadSystemConfigurations=We,window.saveSystemConfigurations=et,window.loadActiveConfigurationToTables=Uo,Ws(),Us("refreshBlockInspector",Mt,{overwrite:!0}),window.expandBlocksToOpticalSystemRows=at,window.getActiveConfiguration=xl,window.loadSourceTableData=bn,window.saveSourceTableData=Vo,window.loadObjectTableData=Ls,window.saveObjectTableData=Vr,window.loadSystemRequirementsTableData=Bs,window.saveSystemRequirementsTableData=js,window.initializeConfigurationUI=br,setTimeout(()=>{try{const i=Je(Ii);window.renderSystemConstraintsFromSurfaceRows?.(i)}catch{}},0),window.outputChiefRayConvergenceData=mm,li({scene:t,camera:n,renderer:o,controls:r}),{scene:t,camera:n,renderer:o,controls:r,ambientLight:a,directionalLight:c}}catch(e){throw e}}function vm(e={}){const n={...{crossSectionOnly:!1,showSurfaceOrigins:!1,showSemidiaRing:!0,showMirrorBackText:!1,crossSectionDirection:"YZ",crossSectionCenterOffset:0,opticalSystemData:null},...e};try{if(n.opticalSystemData||(n.opticalSystemData=Je()),!n.opticalSystemData||n.opticalSystemData.length===0)return;const r=n.opticalSystemData[0]?.thickness,a=r==="INF"||r==="Infinity"||r===1/0?"infinite":"finite",c=window.lastSystemType||null,i=c&&c!==a,l=Dt?.(),f=an?.();i&&(f&&f.clear(),l&&[...l.children].forEach(d=>{l.remove(d),d.geometry&&d.geometry.dispose(),d.material&&(Array.isArray(d.material)?d.material.forEach(u=>u.dispose()):d.material.dispose())})),Za(a),Yo({opticalSystemData:n.opticalSystemData,scene:l||document.scene,crossSectionOnly:n.crossSectionOnly,showSemidiaRing:n.showSemidiaRing,showSurfaceOrigins:n.showSurfaceOrigins,showMirrorBackText:n.showMirrorBackText,crossSectionDirection:n.crossSectionDirection,crossSectionCenterOffset:n.crossSectionCenterOffset})}catch(o){console.error("[RenderWindow] drawOpticalSystemSurfaceWrapper failed:",o)}}function xm(){try{const e=Dt?.(),t=xn?.(),n=Ln?.(),o=an?.();e&&Lo(e);const r=Je();if(!r||r.length===0)return;Yo({opticalSystemData:r,scene:e,crossSectionOnly:!1,showSurfaceOrigins:!1,showSemidiaRing:!0,showMirrorBackText:!1,crossSectionDirection:"YZ",crossSectionCenterOffset:0}),typeof window.adjustCameraView=="function"&&window.adjustCameraView(e,t,n,o)}catch(e){console.error("[RenderWindow] improvedDrawOpticalSystemSurfaceWrapper failed:",e)}}function Za(e){window.lastSystemType=e}function Is(e){window.currentDrawCrossRays=e}function Sm(e){try{const t=ht(),n=Dt?.();if(!n||!t||t.length===0||!e||e.length===0)return;t.forEach((o,r)=>{const s=document.getElementById("render-ray-count-input")||document.getElementById("draw-ray-count-input"),a=s&&parseInt(s.value||"5",10)||5,c=o?.position==="Angle"||o?.position==="angle",i=qo(o,e,a,null,{aimThroughStop:!!c,useChiefRayAnalysis:!0,allowStopBasedOriginSolve:!0,disableCrossExtent:!0});if(!Array.isArray(i)||i.length===0)return;let l=0;for(const f of i)if(!(!f||!f.startP||!f.dir)){if(l>=a)break;try{const p={pos:f.startP,dir:f.dir};console.log(`🔍 正確光線${l} for object ${r}: start=(${p.pos.x}, ${p.pos.y}, ${p.pos.z}), dir=(${p.dir.x}, ${p.dir.y}, ${p.dir.z})`);const d=window.traceRay?window.traceRay(e,p,1,null,null,{allowNonStrict:!0,requireWasmRayTracing:!1,useRustWasm:!0,disableWasmRayTracing:!1}):null;if(d&&d.length>1){console.log(`   開始位置確認: (${d[0].x.toFixed(3)}, ${d[0].y.toFixed(3)}, ${d[0].z.toFixed(3)})`);const u=d.map(g=>new window.THREE.Vector3(g.x,g.y,g.z)),y=new window.THREE.BufferGeometry().setFromPoints(u),m=new window.THREE.LineBasicMaterial({color:65280+r*13056,depthTest:!1,depthWrite:!1,transparent:!0,opacity:1}),h=new window.THREE.Line(y,m);h.renderOrder=2e3,h.frustumCulled=!1,h.userData={type:"optical-ray",objectId:r,rayNumber:l,rayType:"accurate",isRayLine:!0,accurateRayTracing:!0},n.add(h)}}catch{}l++}})}catch{}}function Am(){try{const e=xn?.(),t=Ln?.(),n=Dt?.(),o=an?.();if(!e||!t||!n)return;const{minZ:r,maxZ:s,centerZ:a,totalLength:c,maxY:i}=yo(),l=a,p=Math.max(c,i*2),d=Math.max(p*1.5,600);e.position.set(d*.7,d*.5,l),e.lookAt(0,0,l),e.up.set(0,1,0),t.target.set(0,0,l),t.update(),e.updateProjectionMatrix(),o&&o.render(n,e)}catch{}}function yo(){try{const e=Je();if(!e||e.length===0)return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:50};const t=Go(e);if(!t||t.length===0)return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:50};const n=[];let o=0;if(t.forEach((i,l)=>{if(i&&i.origin){const f=i.origin.z;isFinite(f)&&n.push(f)}}),e.forEach((i,l)=>{const f=parseFloat(i.semidia);isFinite(f)&&f>0&&(o=Math.max(o,f))}),n.length===0)return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:o||50};const r=Math.min(...n),s=Math.max(...n),a=(r+s)/2,c=s-r;return{minZ:r,maxZ:s,centerZ:a,totalLength:c,maxY:o}}catch{return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:50}}}function Nm(e,t){try{if(!e||!Array.isArray(e)||e.length===0||!t||t.length===0)return;const n=u=>{const m=String(u?.surfType??u?.["surf type"]??u?.surface_type??"").toLowerCase().trim();return m==="coord trans"||m==="coordinate break"||m==="coordtrans"||m==="coordinatebreak"||m==="ct"},o=u=>String(u?.["object type"]??u?.object??u?.Object??"").toLowerCase()==="object",r=(u,y)=>{if(!Array.isArray(u)||y===null||y===void 0)return null;const m=Math.max(0,Math.min(y,u.length-1));let h=0;for(let g=0;g<=m;g++){const w=u[g];n(w)||o(w)||h++}return h>0?h:null},s=(u,y,m)=>{if(!Array.isArray(u))return null;const h=r(y,m);if(h!==null&&h>=0&&h<u.length){const g=u[h];if(g&&Number.isFinite(Number(g.x))&&Number.isFinite(Number(g.y)))return g}for(let g=u.length-1;g>=0;g--){const w=u[g],R=Number(w?.surfaceIndex??w?.surface??w?.surfaceIdx);if(Number.isInteger(R)&&R===m&&Number.isFinite(Number(w?.x))&&Number.isFinite(Number(w?.y)))return w}for(let g=u.length-1;g>=0;g--){const w=u[g];if(w&&Number.isFinite(Number(w.x))&&Number.isFinite(Number(w.y)))return w}return null},a=t.length-1,c=t[a],i=Go(t),l=Array.isArray(i)?i[a]:null;if(!(c.optimizeSemiDia==="U"||c.semidia==="Auto"))return;const p=e.filter(u=>(u.beamType||u.type||"").toLowerCase().includes("chief"));if(p.length===0)return;let d=0;p.forEach((u,y)=>{if(!u.rayPath||!Array.isArray(u.rayPath))return;const m=s(u.rayPath,t,a);if(m&&Number.isFinite(m.x)&&Number.isFinite(m.y)){const h=l?fl(m,l):m,g=u.objectPosition||u.originalRay?.objectPosition||null;let w=0;if(g&&(g.x||g.y)){const R=Math.abs(Number(g.x)||0),N=Math.abs(Number(g.y)||0);w=R>N?Math.abs(Number(h.x)||0):Math.abs(Number(h.y)||0)}else w=Math.max(Math.abs(Number(h.x)||0),Math.abs(Number(h.y)||0));console.log(`   主光線${y}: Image面ローカル高さ = ${w.toFixed(6)}`),d=Math.max(d,w)}}),d>0&&(c.semidia=d,window.tableOpticalSystem&&window.tableOpticalSystem.updateData([c]))}catch{}}function _m(){const e=xn?.();if(e&&e.isOrthographicCamera)try{const t=Dt?.(),n=Fm(t),o=yo();if(!o)return;let{minZ:r,maxZ:s,centerZ:a,totalLength:c,maxY:i}=o;if(n){r=Math.min(r,n.min.z),s=Math.max(s,n.max.z),a=(r+s)/2,c=s-r;const b=n.max.y-n.min.y;Number.isFinite(b)&&b>0&&(i=Math.max(i||0,b/2))}const f=Math.min(r,-25),d=s-f;let u=1.5;const y=an?.();if(y){const b=y.getSize(new kt);u=b.x/b.y}const m=1.1,g=(Number.isFinite(i)&&i>0?i:50)*2*m,w=d*m;let R,N;w/Math.max(1e-9,g)>u?(N=w/2,R=N/u):(R=g/2,N=R*u),e.left=-N,e.right=N,e.top=R,e.bottom=-R,e.updateProjectionMatrix()}catch{}}window.updateCameraViewBounds=_m;function Fm(e){try{if(!e)return null;const t=new Pr;let n=!1;return e.traverse(o=>{if(!o||o.visible===!1||!(o.isMesh||o.isLine||o.isGroup)||o.type==="GridHelper"||o.type==="AxesHelper"||o.type==="AmbientLight"||o.type==="DirectionalLight")return;const r=String(o.name||""),s=o.userData||{};if(!!!(s.isOpticalElement||s.isLensSurface||s.isRayLine||s.type==="ray"||s.type==="surfaceProfile"||s.type==="semidiaRing"||s.type==="ring"||s.type==="crossSection"||s.surfaceIndex!==void 0||/surface|lens|cross-section|semidia|mirror|profile|ring|connection/i.test(r)))return;const c=new Pr().setFromObject(o);c.isEmpty()||(t.union(c),n=!0)}),n?t:null}catch{return null}}function Xa(e={}){try{const t=e.camera||xn?.(),n=e.controls||Ln?.(),o=e.scene||Dt?.(),r=e.renderer||an?.();if(!t||!n||!o)return;const{minZ:s,maxZ:a,centerZ:c,totalLength:i,maxY:l}=yo(),p=e.includeRayStartMargin!==!1?25:0,d=Math.min(s,-p),u=a,y=u-d,m=(d+u)/2;let h=-l,g=l,w=0;if(e.cameraBoundsOverride){const L=e.cameraBoundsOverride;Number.isFinite(L.minY)&&Number.isFinite(L.maxY)&&(h=L.minY,g=L.maxY,w=(L.minY+L.maxY)/2,console.log(`[RAY-DEBUG] Using camera bounds override: minY=${h.toFixed(1)}, maxY=${g.toFixed(1)}`))}if(Number.isFinite(h)&&Number.isFinite(g)||(h=-l,g=l,w=0),g<h){const L=h;h=g,g=L}const R=t?.userData?.__drawCrossOrthoBounds,N=e.preserveDrawCrossBounds===!0&&R,I=Number.isFinite(e.centerZOverride)?e.centerZOverride:N&&Number.isFinite(R.centerZ)?R.centerZ:m,b=e.targetOverride&&Number.isFinite(e.targetOverride.x)&&Number.isFinite(e.targetOverride.y)&&Number.isFinite(e.targetOverride.z)?e.targetOverride:null;let A=1.5;if(r){const L=r.getSize(new kt);A=L.x/L.y}const v=1.1,S=Math.max((g-h)*v,1e-6),_=y*v;if(t.isOrthographicCamera)if(N)t.left=R.left,t.right=R.right,t.top=R.top,t.bottom=R.bottom;else{let L,ne;_/S>A?(ne=_/2,L=ne/A):(L=S/2,ne=L*A),t.left=-ne,t.right=ne,t.top=L,t.bottom=-L}const M=300,G=b?b.x:0,z=b?b.y:w,E=b?b.z:I;t.position.set(G-M,z,E),t.lookAt(G,z,E),t.up.set(0,1,0),n.target.set(G,z,E),n.update(),t.updateProjectionMatrix(),e.storeDrawCrossBounds===!0&&t.isOrthographicCamera&&(t.userData.__drawCrossOrthoBounds={left:t.left,right:t.right,top:t.top,bottom:t.bottom,centerZ:E}),r&&o&&r.render(o,t)}catch{}}function Mm(e={}){try{const t=e.camera||xn?.(),n=e.controls||Ln?.(),o=e.scene||Dt?.(),r=e.renderer||an?.();if(!t||!n||!o)return;const s=yo();if(!s)return;const{minZ:a,maxZ:c,maxY:i}=s,f=e.includeRayStartMargin!==!1?25:0,p=Math.min(a,-f),d=c,u=d-p,y=(p+d)/2,m=t?.userData?.__drawCrossOrthoBounds,h=e.preserveDrawCrossBounds===!0&&m,g=Number.isFinite(e.centerZOverride)?e.centerZOverride:h&&Number.isFinite(m.centerZ)?m.centerZ:y,w=e.targetOverride&&Number.isFinite(e.targetOverride.x)&&Number.isFinite(e.targetOverride.y)&&Number.isFinite(e.targetOverride.z)?e.targetOverride:null;let R=1.5;if(r){const M=r.getSize(new kt);R=M.x/M.y}const N=1.1,I=i*2*N,b=u*N;if(t.isOrthographicCamera)if(h)t.left=m.left,t.right=m.right,t.top=m.top,t.bottom=m.bottom;else{let M,G;b/I>R?(G=b/2,M=G/R):(M=I/2,G=M*R),t.left=-G,t.right=G,t.top=M,t.bottom=-M}const A=e.cameraDistance||300,v=w?w.x:0,S=w?w.y:0,_=w?w.z:g;t.position.set(v,S+A,_),t.lookAt(v,S,_),t.up.set(1,0,0),t.updateProjectionMatrix(),n.target.set(v,S,_),n.update(),r&&o&&r.render(o,t)}catch{}}function Rm(){console.log("🖼️ Debugging 3D canvas status...");const e=document.getElementById("threejs-canvas-container"),t=an?.(),n=Dt?.(),o=xn?.(),r=Ln?.(),s=t?.domElement;if(console.log("Canvas container:",!!e),e&&(console.log("Container dimensions:",e.offsetWidth,"x",e.offsetHeight),console.log("Container style:",e.style.cssText)),console.log("Canvas element:",!!s),s&&(console.log("Canvas dimensions:",s.width,"x",s.height),console.log("Canvas style:",s.style.cssText),console.log("Canvas parent:",s.parentElement?.id)),console.log("Renderer:",!!t),t){const a=t.getSize(new kt);console.log("Renderer size:",a.x,"x",a.y)}return console.log("Scene children count:",n?.children?.length||0),console.log("Camera position:",o?.position),console.log("Controls target:",r?.target),{canvasContainer:!!e,canvas:!!s,renderer:!!t,scene:!!n,camera:!!o,controls:!!r}}window.drawOpticalSystemSurfaceWrapper=vm;window.improvedDrawOpticalSystemSurfaceWrapper=xm;window.drawOptimizedRaysFromObjects=Sm;window.generateRayStartPointsForObject=qo;window.drawRayWithSegmentColors=Hs;window.setRayEmissionPattern=Tr;window.getRayEmissionPattern=cl;window.setRayColorMode=io;window.getRayColorMode=kr;window.fitCameraToOpticalSystem=Am;window.setCameraForYZCrossSection=Xa;window.setCameraForXZCrossSection=Mm;window.calculateOpticalSystemZRange=yo;window.debug3DCanvas=Rm;window.traceRay=Gs;window.getOpticalSystemRows=Je;window.getObjectRows=ht;window.getSourceRows=Tt;window.generateSurfaceOptions=Es;window.initializeApplication=Ya;Ws();Us("updateSurfaceNumberSelect",Ia,{overwrite:!0});const ko=(()=>{let e=!1,t=!1;const n=()=>{if(!t){t=!0;try{window.__cooptMainReady=!0,window.dispatchEvent(new CustomEvent("coopt:main-ready"))}catch(o){console.warn("⚠️ [Init] Failed to dispatch coopt:main-ready",o)}}};return async()=>{if(!e){e=!0;try{ou();const o=await Ya();if(!o)throw new Error("Failed to initialize application components");n(),setTimeout(()=>{Promise.resolve(wm()).catch(d=>{console.error("❌ [PhaseC Autorun] Unexpected startup failure:",d)})},0),o&&(li({scene:o.scene,camera:o.camera,renderer:o.renderer,controls:o.controls}),window.ambientLight=o.ambientLight,window.directionalLight=o.directionalLight),window.tableObject=$s,window.tableSource=Eo,setTimeout(()=>{try{Promise.resolve(kd()).catch(d=>{})}catch{}},0);const r=document.getElementById("debug-object-data");r&&r.addEventListener("click",()=>{const d=window.getObjectRows?window.getObjectRows():[],u=document.getElementById("wavefront-object-select"),y=u?parseInt(u.value):0;if(console.log(`  Object総数: ${d.length}`),console.log(`  選択インデックス: ${y}`),console.log(`  ドロップダウン存在: ${!!u}`),d.length===0){alert("Objectデータが読み込まれていません。JSONファイルをロードしてください。");return}d.forEach((g,w)=>{console.log(`  Object ${w+1}:`,g),console.log(`    Type: ${g.Type||g.type||"未設定"}`),console.log(`    X: ${g.X||g.x||"未設定"}`),console.log(`    Y: ${g.Y||g.y||"未設定"}`);const R=g.Type==="Angle"||g.type==="Angle";if(console.log(`    角度タイプ: ${R}`),R){const N=parseFloat(g.X||g.x||0),I=parseFloat(g.Y||g.y||0);console.log(`    画角: X=${N}°, Y=${I}°`)}});const m=d[y]||d[0];console.log("  データ:",m);const h=Ja(m,y);console.log("  変換後フィールド設定:",h),console.log('💡 [ObjectDebug] ヒント: コンソールをクリアするには、ブラウザのF12で開発者ツールを開き、コンソールタブで右クリック→"Clear console"を選択してください。')});const s=document.getElementById("debug-ray-angles");s&&s.addEventListener("click",()=>{window.debugOPDRayAngles?window.debugOPDRayAngles():console.log("💡 [RayAngleDebug] debug-opd-ray-angles.jsが正しく読み込まれているか確認してください")});const a=document.getElementById("draw-cross-btn");a&&a.addEventListener("click",async()=>{try{a.disabled=!0,a.textContent="Generating...";const d=Je();if(!d||d.length===0){alert("光学系データが設定されていません。");return}const y=d[0]?.thickness,m=y==="INF"||y==="Infinity"||y===1/0,h=m?"infinite":"finite",g=window.lastSystemType||null;if(g&&g!==h){const ne=an?.(),V=Dt?.();ne&&ne.clear(),V&&[...V.children].forEach($=>{V.remove($),$.geometry&&$.geometry.dispose(),$.material&&(Array.isArray($.material)?$.material.forEach(J=>J.dispose()):$.material.dispose())})}Za(h);const R=ht();if(!R||R.length===0){alert("Objectが設定されていません。");return}const N=[];R.forEach((ne,V)=>{let K;if(Array.isArray(ne)){const $=parseFloat(ne[1]),J=parseFloat(ne[2]);K={x:$||0,y:J||0,z:0}}else{const $=parseFloat(ne.xHeightAngle)||0,J=parseFloat(ne.yHeightAngle)||0;K={x:$,y:J,z:0}}N.push(K)});const I=document.getElementById("draw-ray-count-input"),b=I&&parseInt(I.value,10)||7,A=document.getElementById("transverse-surface-select");let v=null;if(A&&A.value!=="")v=parseInt(A.value)-1;else{const ne=d.findIndex(V=>V&&(V["object type"]==="Image"||V.object==="Image"));v=ne>=0?ne:Math.max(0,d.length-1)}let S;const _=typeof window.getPrimaryWavelength=="function"&&Number(window.getPrimaryWavelength())||.5876;if(m){const ne=N.map(V=>({x:V.x||0,y:V.y||0}));S=await qs(d,ne,{rayCount:b,debugMode:!1,wavelength:_,crossType:"both",targetSurfaceIndex:v,angleUnit:"deg",chiefZ:-20})}else S=await Xr(d,N,{rayCount:b,debugMode:!1,wavelength:_,crossType:"both"});if(!S.success){alert(`クロスビーム生成失敗: ${S.error}`);return}let M=[],G=0,z=0;const E=Array.isArray(S.objectResults)?S.objectResults:Array.isArray(S.results)?S.results:null;E?(E.forEach((ne,V)=>{console.log(`   Result${V+1}:`,ne);const K=Array.isArray(ne?.tracedRays)?ne.tracedRays:Array.isArray(ne?.rays)?ne.rays:[];K.length>0&&(M=M.concat(K),console.log(`   Result${V+1} 光線数: ${K.length}`))}),G=E.length,z=E.length):S.allCrossBeamRays&&Array.isArray(S.allCrossBeamRays)&&S.allTracedRays&&Array.isArray(S.allTracedRays)?(M=S.allTracedRays.map((ne,V)=>{const K=S.allCrossBeamRays[V];return K&&(ne.type=K.type,ne.beamType=K.beamType),ne}),G=S.processedObjectCount||0,z=S.objectCount||0):S.allCrossBeamRays&&Array.isArray(S.allCrossBeamRays)?(M=S.allCrossBeamRays,G=S.processedObjectCount||0,z=S.objectCount||0):S.allTracedRays&&Array.isArray(S.allTracedRays)?(M=S.allTracedRays,G=S.processedObjectCount||0,z=S.objectCount||0):S.tracedRays&&Array.isArray(S.tracedRays)?(M=S.tracedRays,G=1,z=1):Array.isArray(S)&&(M=S,G=1,z=1);const L=Dt?.();if(L&&Lo(L),Yo({opticalSystemData:d,scene:L||document.scene,showSemidiaRing:!0,showSurfaceOrigins:!1,crossSectionOnly:!1}),Xa(),M.length>0){const ne={};M.forEach(K=>{const $=K.objectIndex||0;ne[$]=(ne[$]||0)+1}),console.log("   Object分布:",ne);const V=M.filter(K=>K&&K.success&&Array.isArray(K.rayPath)&&K.rayPath.length>0);Is(V.map(K=>({orientation:(()=>{const J=[K.beamType,K.type,K.originalRay?.type,K.originalRay?.beamType].filter(Boolean).map(Q=>String(Q).toLowerCase()).join(" ");return J.includes("horizontal")||J.includes("x")?"horizontal":J.includes("vertical")||J.includes("y")?"vertical":"unknown"})(),rayPath:K.rayPath,objectIndex:K.objectIndex??K.originalRay?.objectIndex??0,crossParameter:K.originalRay?.crossParameter??K.crossParameter??null,description:K.description||K.originalRay?.description||"",source:K}))),console.log("Stored draw-cross rays for overlay:",window.currentDrawCrossRays.length),Ka(M,L)}else Is([]);window.crossBeamResult=S,window.lastGeneratedRays=M,Nm(M,d)}catch(d){alert(`クロスビーム描画エラー: ${d.message}`)}finally{a.disabled=!1,a.textContent="Draw Cross"}});const c=document.getElementById("undo-btn"),i=document.getElementById("redo-btn");c&&c.addEventListener("click",()=>{window.undoHistory&&window.undoHistory.undo()}),i&&i.addEventListener("click",()=>{window.undoHistory&&window.undoHistory.redo()});const l=document.getElementById("toggle-toolbar-btn"),f=document.getElementById("top-buttons-row"),p=l?.getAttribute("data-toggle-handled")==="react";l&&f&&!p&&(l.addEventListener("click",()=>{const d=f.classList.toggle("collapsed");l.classList.toggle("collapsed",d),dl(d)}),ml()&&(f.classList.add("collapsed"),l.classList.add("collapsed")))}catch(o){alert(`Failed to initialize application: ${o.message}`)}}}})(),Ts=()=>{if(!!document.getElementById("react-root")){if(window.__cooptReactMounted){ko();return}window.addEventListener("coopt:react-mounted",()=>{ko()},{once:!0}),setTimeout(()=>{window.__cooptReactMounted||(console.warn("⚠️ [Init] React mount event timeout, starting anyway"),ko())},2e3)}else ko()};typeof document<"u"&&document?.addEventListener&&(document.readyState==="loading"?document.addEventListener("DOMContentLoaded",Ts):Ts());function Ka(e,t){const n=t||Dt?.();if(!e||e.length===0)return;const o=new Set(["chief","left_marginal","right_marginal","upper_marginal","lower_marginal","horizontal_cross","vertical_cross"]);e.forEach(l=>{const f=l?.originalRay||l;if(f?.type==="boundary"){const p=f.side||l?.side;p==="left"?f.type="left_marginal":p==="right"?f.type="right_marginal":p==="upper"||p==="top"?f.type="upper_marginal":(p==="lower"||p==="bottom")&&(f.type="lower_marginal")}});const r=e.filter(l=>{const f=String(l?.originalRay?.type||l?.type||"").trim().toLowerCase();return!(!(!!(l&&(l.success||Array.isArray(l.rayPath)||Array.isArray(l.rayPathToTarget)))&&f&&o.has(f))||l.fallback||(Array.isArray(l.rayPath)?l.rayPath:Array.isArray(l.rayPathToTarget)?l.rayPathToTarget:[]).filter(y=>y&&typeof y.x=="number"&&typeof y.y=="number"&&typeof y.z=="number").length===0)});if(console.log(`[RAY-DEBUG] drawCrossBeamRays: input=${e.length}, filtered=${r.length}`),r.length!==e.length&&console.warn(`[RAY-DEBUG] ${e.length-r.length} rays dropped. Dropped types:`,e.filter(l=>!r.includes(l)).map(l=>l?.originalRay?.type||l?.type||"NO_TYPE").slice(0,10)),r.filter(l=>l.fallback).length,e=r,!n)return;const s=n.children.filter(l=>l.userData?.rayType==="crossBeam").length,a={};e.forEach(l=>{const f=l.objectIndex||0;a[f]=(a[f]||0)+(Array.isArray(l.rayPath)?l.rayPath.length-1:0)}),console.log(`[RAY-DEBUG] Before draw: scene has ${s} cross-ray segments. Per-object expected segments:`,a);const c=[];n.traverse(l=>{(l?.userData?.rayType==="crossBeam"||l?.userData?.type==="optical-ray"&&l?.userData?.objectId?.includes("ray"))&&c.push(l)}),c.forEach(l=>{n.remove(l),l.geometry&&l.geometry.dispose(),l.material&&(Array.isArray(l.material)?l.material.forEach(f=>f.dispose()):l.material.dispose())}),console.log(`[RAY-DEBUG] Cleared ${c.length} existing crossBeam rays before drawing new ones`);let i=null;try{if(i=kr(),i!=="object")try{io("object")}catch{}const l={};e.forEach(m=>{const h=m.objectIndex||0;l[h]=(l[h]||0)+1});const f=e.map(m=>Number.parseInt(String(m?.objectIndex??m?.originalRay?.objectIndex??m?.originalRay?.objIndex??NaN),10)).filter(m=>Number.isFinite(m)),p=f.length>0&&!f.includes(0)&&f.every(m=>m>=1),d=e.filter(m=>m.success);console.log(`[RAY-DEBUG] drawCrossBeamRays draw loop: total=${e.length}, success=${d.length}, no-success=${e.length-d.length}`),e.length>0&&d.length===0&&console.warn("[RAY-DEBUG] ALL rays have success=false! Sample:",JSON.stringify(e[0]).slice(0,200));const u={};e.forEach(m=>{const h=m.objectIndex||0;u[h]=(u[h]||0)+1}),console.log("[RAY-DEBUG] Per-object draw count:",u),e.forEach((m,h)=>{const g=Array.isArray(m.rayPath)&&m.rayPath.length>=2||Array.isArray(m.rayPathToTarget)&&m.rayPathToTarget.length>=2;if(!m.success&&!g)return;const w=Array.isArray(m.rayPath)?m.rayPath:Array.isArray(m.rayPathToTarget)?m.rayPathToTarget:[];if(!w||w.length===0)return;const R=m.objectIndex??m.originalRay?.objectIndex??m.originalRay?.objIndex??0,N=Number.parseInt(String(R),10),I=Number.isFinite(N)?Math.max(0,N):0,b=p?Math.max(0,I-1):I,A=m.objectPosition,v={...m.originalRay||{},type:m.originalRay?.type||m.type,side:m.originalRay?.side||m.side},S=(v.type||"").toString(),_=(v.side||"").toString();let M=m.beamType;if(!M){const K=S.toLowerCase(),$=_.toLowerCase();K.includes("horizontal")?M="horizontal":K.includes("vertical")?M="vertical":$==="left"||$==="right"?M="horizontal":$==="upper"||$==="lower"||$==="top"||$==="bottom"?M="vertical":K==="chief"?M="chief":M="vertical"}const G=_.toLowerCase()==="top"?"upper":_.toLowerCase()==="bottom"?"lower":_||"center";A&&console.log(`   Object${b+1}位置: (${A.x}, ${A.y}, ${A.z})`);const z=kr();let E;const L=pl;if(z==="object")E=L.getColor(L.MODE.OBJECT,b,null);else if(z==="segment"){const K=m.segmentType||"chief";E=L.getColor(L.MODE.SEGMENT,0,K)}else E=16777215;m.optimized;let ne;M==="chief"?ne=`chief-obj${b}`:M==="horizontal"?ne=`cross-horizontal-obj${b}`:M==="vertical"?ne=`cross-vertical-obj${b}`:ne=`cross-vertical-obj${b}`;const V=`${ne}-ray${h}`;Hs(w,V,h,n)});const y=n.children.filter(m=>m.userData?.rayType==="crossBeam").length;if(console.log(`[RAY-DEBUG] After draw: scene now has ${y} cross-ray segments (added ${y-s})`),y>0){const m={minY:1/0,maxY:-1/0,minZ:1/0,maxZ:-1/0};n.traverse(h=>{if(h?.userData?.rayType==="crossBeam"&&h.geometry){const g=h.geometry.attributes?.position;if(g){const w=g.array;for(let R=0;R<w.length;R+=3){const N=w[R+1],I=w[R+2];Number.isFinite(N)&&(m.minY=Math.min(m.minY,N),m.maxY=Math.max(m.maxY,N)),Number.isFinite(I)&&(m.minZ=Math.min(m.minZ,I),m.maxZ=Math.max(m.maxZ,I))}}}}),Number.isFinite(m.minY)&&Number.isFinite(m.maxY)&&console.log(`[RAY-DEBUG] Drawn rays Y range: [${m.minY.toFixed(1)}, ${m.maxY.toFixed(1)}], Z range: [${m.minZ.toFixed(1)}, ${m.maxZ.toFixed(1)}]`)}}catch{}finally{if(i&&i!=="object")try{io(i)}catch{}}}window.drawCrossBeamRays=Ka;window.generateInfiniteSystemCrossBeam=qs;window.generateCrossBeam=Xr;window.drawOpticalSystemSurfaces=Yo;window.calculateChiefRayNewton=Vs;window.findStopSurface=Ys;window.isFiniteSystem=function(e){if(e&&e.length>0){const n=e[0].thickness;if(n==="INF"||n===1/0)return!1;const o=parseFloat(n);return Number.isFinite(o)&&o>0}return!1};window.mainDebugFunctions={generateCrossBeam:Xr,calculateChiefRayNewton:Vs,traceRay:Gs,findStopSurface:Ys,calculateSurfaceOrigins:Go,isFiniteSystem:window.isFiniteSystem};window.mainDebugFunctions.generateDistortionPlots=ll;window.mainDebugFunctions.calculateDistortionData=ul;function Ja(e,t){if(!e)return{fieldAngle:{x:0,y:0},xHeight:0,yHeight:0,displayName:"On-Axis (No Data)"};const n=e.position==="Angle"||e.Type==="Angle"||e.type==="Angle";if(e.position==="ImageHeight"){const r=parseFloat(e.xHeightAngle||0),s=parseFloat(e.yHeightAngle||0);return{fieldAngle:{x:0,y:0},xHeight:r,yHeight:s,fieldType:"ImageHeight",displayName:`Object ${t+1} - img ${r}mm, ${s}mm`}}if(n){const r=parseFloat(e.xHeightAngle||e.X||e.x||0),s=parseFloat(e.yHeightAngle||e.Y||e.y||0);return{fieldAngle:{x:r,y:s},fieldType:"Angle",displayName:`Object ${t+1} - ${r}°, ${s}°`}}else{const r=parseFloat(e.xHeight||e.X||e.x||0),s=parseFloat(e.yHeight||e.Y||e.y||0);return{xHeight:r,yHeight:s,fieldType:"Rectangle",displayName:`Object ${t+1} - ${r}mm, ${s}mm`}}}window.convertObjectToFieldSetting=Ja;function Dm(){return eo}const Cr=document.getElementById("analysis-select");Cr&&Cr.getAttribute("data-react-handled")!=="1"&&Cr.addEventListener("change",e=>{const t=e.target,n=t.value;if(!n)return;const r={"spot-diagram":"open-spot-diagram-window-btn","spherical-aberration":"open-spherical-aberration-window-btn",astigmatism:"open-astigmatism-window-btn",distortion:"open-distortion-window-btn","magnification-chromatic-aberration":"open-magnification-chromatic-aberration-window-btn","integrated-aberration":"open-integrated-aberration-window-btn","transverse-aberration":"open-transverse-aberration-window-btn",opd:"open-opd-window-btn",psf:"open-psf-window-btn",mtf:"open-mtf-window-btn","through-focus-spot":"open-through-focus-spot-window-btn","through-focus-mtf":"open-through-focus-mtf-window-btn","field-mtf":"open-field-mtf-window-btn"}[n];if(r){try{typeof window.setupAnalysisWindows=="function"&&window.setupAnalysisWindows()}catch{}const s=document.getElementById(r);s&&s.click()}t.value=""});document.addEventListener("keydown",e=>{const t=document.activeElement,n=t&&(t.tagName==="INPUT"||t.tagName==="TEXTAREA"||t.isContentEditable);(e.ctrlKey||e.metaKey)&&e.key==="z"&&!e.shiftKey&&!n&&(e.preventDefault(),window.undoHistory&&window.undoHistory.undo()),!n&&((e.ctrlKey||e.metaKey)&&e.key==="y"||(e.ctrlKey||e.metaKey)&&e.shiftKey&&e.key==="z")&&(e.preventDefault(),window.undoHistory&&window.undoHistory.redo())});function Cm(e){window.undoHistory&&window.undoHistory.clear()}window.clearUndoHistoryOnMajorChange=Cm;export{vm as drawOpticalSystemSurfaceWrapper,Sm as drawOptimizedRaysFromObjects,Dm as getWASMSystem,xm as improvedDrawOpticalSystemSurfaceWrapper,Ya as initializeApplication};
